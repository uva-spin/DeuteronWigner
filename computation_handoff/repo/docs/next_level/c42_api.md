# C42/M0C API

`deuteron_wigner.bridge.m0c.authority_audit()` evaluates source presence and
content hashes from repository files.  `assert_gauge_action_incomplete()`
enforces the C42 Branch-A no-go; it creates no physical operator.

Regenerate the C42 records with:

```bash
PYTHONPATH=src python3 scripts/build_c42_fail_closed_artifacts.py
```

This is an authority gate, not a light-front QCD, Wilson, bilocal,
counterterm, refinement, or one-loop API.
