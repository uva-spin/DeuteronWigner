# ADR 143: operator-level crosswalk

Decision: bridge matching compares species, flavor, polarization, target,
twist, rank, link, color, scheme, scale, and domain. TMD names and array shapes
are insufficient. This is an exact type rule. Revisit individual entries when
new source-qualified operator evidence closes their missing fields.
