# ADR 088: nuclear operator-specific OPE

**Decision.** Target-independent coefficients act on a nuclear matrix element
only after same-local-operator proof. Otherwise each many-body block has its
own coefficient/evolution capability or an explicit unavailable status.
Ancestry is never collapsed before matching.
