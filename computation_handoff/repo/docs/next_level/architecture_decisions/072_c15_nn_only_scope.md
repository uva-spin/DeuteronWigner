# ADR 072: NN-only N0 scope

Only the normalized NN amplitude is active. NNPI, DeltaDelta, compact,
hidden-color, coherent, and full exchange-current sectors remain typed
unavailable and cannot be silently treated as zero.
