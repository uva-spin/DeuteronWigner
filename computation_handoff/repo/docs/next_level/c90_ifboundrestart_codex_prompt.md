# C90/IFBOUNDRESTART Codex Work Package

## Title

**Exact factorized-semantic attestation for the C82 scientific domain: canonical pair-program normal forms, independent historical compilers, structural expansion-equivalence proof, compact restartable ledgers, bounded leaf audits, and C91 descendant-equivalence handoff**

## Authoritative baseline

Start from the clean local C89 completion commit:

```text
14fc6205837d566f8a06ee1254e3e01ae23875ab
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "14fc6205837d566f8a06ee1254e3e01ae23875ab"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c90_ifboundrestart_contract.json
```

If that contract freezes exact status names, schemas, APIs, or a narrower scientific boundary, preserve them. This work package supplies the exact factorized-semantic, bounded-execution, checkpoint, and validation plan; it does not authorize weakening the committed contract.

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical execution environment remains:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

The baseline is authoritative only when it contains and reproduces:

```text
C87 canonical C72 color authority:
    scientific root and immutable compatibility capsule;
    canonical source-chain equivalent;
    not exact historical runtime-instance recovery;

C88:
    exact C78-derived logical census;
    expanded safe-record persistence rejected on capacity grounds;

C89:
    exact factorized programs for all 154,830 supported pairs;
    mixed-radix rank/unrank closure;
    exact logical counts:
        K9  = 28,606,464;
        K11 = 165,991,250;
        K13 = 697,394,304;
        total = 891,992,018;
    maximum records in one pair:
        9,216 / 22,500 / 46,656;
    on-demand C88 record reconstruction;
    one historical C82 lazy-route coefficient/bound holdout closed;
    no expanded stream;

C89 performance result:
    approximately 310.6 reconstructed records per second;
    approximately 2.87 million seconds for one complete leaf route;
    approximately 11.49 million seconds for the required
    two-route/two-clean minimum;

C89 status:
    C89_IFBOUNDSTREAM_EXECUTION_INCOMPLETE;

not created:
    dual-digest pair ledger;
    attestation root;
    restart result;
    historical-versus-descendant equivalence claim;
    coefficient-times-kernel product;
    contact matrix or action.
```

Verify all actual roots, hashes, counts, benchmark results, schemas, and contract fields from the repository. This prompt is not numerical authority.

Create a local completion commit. Do not push.

---

# 1. Scientific interpretation of C89

C89 has already established the exact logical structure:

```text
all supported pairs are known;

every pair has an executable factorized program;

every logical record has an exact mixed-radix ordinal;

rank and unrank close;

the complete logical count is known.
```

The remaining problem is not missing scientific authority. It is the choice to reconstruct and serialize every one of the 891,992,018 logical leaves through Python objects in every validation route.

C90 must not authorize a multi-month brute-force Python run merely to satisfy an implementation-shaped acceptance test.

The scientific domain is a finite, typed expansion of exact factorized programs. Its complete semantics can be attested exactly by binding:

```text
the canonical program structure;

all primitive scientific tables;

all axis values and orderings;

all selection and multiplicity rules;

the record-construction expressions;

the coefficient, bound, status, and ancestry rules;

the exact logical cardinality;

and a proof that the normalized program expands to the C88 record
sequence.
```

This is an exact semantic commitment, not a probabilistic shortcut and not a claim that a conventional SHA-256 hash of 891,992,018 serialized records was computed.

No external paper is required for a repository-owned intermediate representation, normalization theorem, structural-induction certificate, compact ledger, or pair-level checkpoint.

---

# 2. Exact purpose and stopping point

C90 must produce:

```text
a canonical typed intermediate representation for factorized C82
scientific pair programs;

one independently constructed historical program route derived from
the C82 lazy scientific semantics;

one normalized route derived from the C89 factorized programs;

complete primitive-table roots;

a canonical normal form for every one of the 154,830 pairs;

a machine-readable structural-induction certificate proving that
equal normal forms and primitive roots imply equal expanded C88
record sequences;

exact pair-local counts and closed-form scientific summaries;

pair-program semantic roots;

resolution and aggregate historical semantic-attestation roots;

bounded deterministic pair-level execution;

pair-atomic checkpoints and restart;

compact authenticated ledgers;

exhaustive route-A/route-B normal-form equivalence;

bounded record-level audits at all semantic boundaries;

a verified historical attestation API;

and a C91/IFEQUIV6 import contract.
```

C90 must not:

```text
claim to have computed the conventional expanded-record stream hash
when it has not;

run four Python-level 891,992,018-record passes;

change the C88 scientific record schema;

change the C89 logical domain or rank/unrank semantics;

compare the full current descendant domain with the full historical
domain;

declare historical-versus-descendant equivalence;

persist the final public C82 coefficient snapshot;

form a coefficient-times-kernel product;

construct a contact matrix or action;

choose a physical coupling or counterterm;

add C53 or C58;

or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY
```

If `c90_ifboundrestart_contract.json` freezes another exact spelling, preserve it and record an explicit semantic alias.

The favorable continuation is:

> **C91/IFEQUIV6 — construct the current descendant C82 canonical pair-program ledger under the same semantic IR, compare all 154,830 pair roots exactly, and perform bounded record-level diagnostics only for mismatched pairs**

---

# 3. Authority hierarchy

Use existing source authority for:

```text
C77 physical/raw projection amplitudes and bounds;

C78 supported pairs, endpoints, witnesses, source order, and
structural support;

C80 canonical raw contact-coordinate schema and exact equivalence
identities;

C82 projected-coefficient, factor-ownership, status, bound, and
ancestry semantics;

C87 canonical color authority;

C88 scientific record fields and canonical logical ordering;

C89 exact factorized pair domains and rank/unrank semantics.
```

Use exact project-derived authority for:

```text
the semantic program IR;

canonical normal form;

typed bytecode or symbolic map expressions;

primitive-table roots;

structural-induction proof certificates;

pair-program roots;

compact ledgers;

checkpoint and restart identities.
```

Do not issue an outside-authority no-go for internal program syntax, canonical node IDs, hashes, or proof certificates.

Fail closed only if:

```text
a scientific field cannot be represented in the typed IR;

the historical C82 semantics and C89 programs normalize to
different exact programs;

a primitive scientific table differs;

the expansion theorem has an unproved node type;

a pair count or ordering cannot be proved;

or bounded/restart execution cannot close.
```

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C77:
    basis order;
    physical/raw component identities;
    amplitudes, exact records, and bounds;
    metric and conjugation conventions;

C78:
    154,830 supported-pair identities;
    source-ordered endpoint and witness domains;
    logical coordinate axes;
    structural support and ancestry;

C80:
    canonical ContactKernelCoordinate schema;
    exact coordinate-equivalence identity;
    no numerical kernel value inside the C82 coefficient stream;

C82:
    witness-leaf to C80 coordinate construction;
    projected coefficient construction;
    factor ownership;
    pair-local grouping;
    statuses, bounds, and reverse ancestry;

C87:
    canonical color scientific and capsule roots;
    historical environment qualification;

C88:
    complete scientific-record schema;
    field classification;
    canonical ordering;
    exact logical census;
    capacity no-go;

C89:
    factorized pair-program implementation;
    mixed-radix rank/unrank;
    logical counts;
    performance benchmark;
    record reconstruction;
    C90 contract.
```

Create:

```text
docs/next_level/c90_derivation_authority_manifest.json
docs/next_level/c90_input_fidelity_audit.json
```

---

# 5. Freeze the historical scientific environment

Create a clean detached historical C82 worktree and stage the immutable C87 capsule read-only.

Freeze:

```text
historical C82 source commit;

C87 scientific and capsule roots;

C88 scientific-record schema and field classification;

C88/C89 resolution and pair ordering;

C89 mixed-radix local ordering;

all C77/C78/C80/C82 scientific roots and primitive identities;

the C90 semantic-IR schema and normalization version.
```

Issue:

```text
C90_HISTORICAL_ENVIRONMENT_AND_SEMANTIC_SCHEMA_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c90_input_freeze.json
```

Poison current descendant builders, current runtime fallbacks, build-if-missing behavior, C80 numerical kernel values, C53/C58 values, physical coupling, and counterterm interfaces.

---

# 6. Canonical semantic-program IR

Define a closed typed intermediate representation.

It must include only node types with completely declared expansion semantics, such as:

```text
ATOM_TABLE
    ordered immutable primitive records;

ORDERED_RANGE
    exact integer or rational ordinal domain;

ORDERED_UNION
    concatenated child sequences with explicit disjointness or
    retained-multiplicity semantics;

CARTESIAN_PRODUCT
    mixed-radix ordered product with explicit axis order;

FILTER
    exact source-derived predicate with cardinality proof;

PERMUTE
    exact reversible basis/order adapter;

MAP_RECORD
    typed construction of one C88 scientific record from the
    current product tuple;

GROUP_TEMPLATE
    exact pair-local coordinate/coefficient grouping semantics
    where present in C82;

ANCESTRY_TEMPLATE
    exact ordered witness/endpoint/projection ancestry;

BOUND_TEMPLATE
    exact/certified coefficient-bound composition;

STATUS_TEMPLATE
    terminal coefficient-status rule.
```

No node may contain:

```text
an opaque Python callable;

an unhashable closure;

a filesystem-dependent iterator;

floating-value equality;

a C80 numerical kernel value;

or an implementation-defined unordered mapping.
```

Every expression must be represented through typed symbolic bytecode or a canonical expression AST.

Create:

```text
docs/next_level/c90_semantic_ir_schema.json
docs/next_level/c90_semantic_ir_validation.json
```

---

# 7. Primitive scientific tables

Inventory every primitive table referenced by the programs.

At minimum bind the actual repository equivalents of:

```text
supported-pair table;

physical bra/ket identity table;

C77 projection-amplitude exact/value/bound table;

C78 witness and endpoint tables;

retained-q intermediate table;

C74/C87 color-record table;

C80 coordinate-template and equivalence table;

source-order and fermion-sign table;

metric/conjugation table;

coefficient-expression primitive table;

bound primitive table;

status-rule table;

ancestry primitive table.
```

For every table freeze:

```text
schema;

canonical order;

record count;

scientific root;

producer/source identity;

safe immutable public loader.
```

Create:

```text
docs/next_level/c90_primitive_table_manifest.json
docs/next_level/c90_primitive_table_validation.json
```

A favorable result requires zero unowned primitive.

---

# 8. Route A — independent historical-C82 semantic compiler

Implement one compiler directly from the historical C82 scientific semantics.

It must consume:

```text
the historical C77/C78/C80/C82 public/scientific construction
contracts;

the exact source-ordered loop axes;

the witness, endpoint, projection, coordinate, coefficient, bound,
status, and ancestry formulas.
```

It must emit the semantic IR without reconstructing every logical leaf.

It must not call:

```text
the C89 pair-program constructor;

the C90 Route-B normalizer;

a C89 program deserializer;

or a precomputed C89 program root.
```

The compiler must preserve the exact C88 scientific-record expansion.

Create:

```text
docs/next_level/c90_route_a_historical_semantic_compiler.json
docs/next_level/c90_route_a_validation.json
```

---

# 9. Route B — C89 program normalization

Implement a separate normalizer for the existing C89 factorized programs.

It must:

```text
read the C89 pair programs through their immutable API;

translate every program into the C90 semantic IR;

canonicalize node and primitive references;

preserve the C89 mixed-radix order;

and emit the same C88 scientific-record semantics.
```

It must not call the Route-A compiler.

Create:

```text
docs/next_level/c90_route_b_c89_normalizer.json
docs/next_level/c90_route_b_validation.json
```

---

# 10. Canonical normal form

Define one exact normal form.

At minimum:

```text
flatten nested ORDERED_UNION nodes while preserving order;

flatten nested CARTESIAN_PRODUCT nodes only under proved identical
mixed-radix semantics;

remove identity PERMUTE nodes;

compose adjacent exact permutations;

canonicalize symbolic expressions;

canonicalize primitive references by scientific root and record ID;

retain source-significant ordering;

retain multiplicity explicitly;

retain filters unless their elimination is proved with an exact
replacement cardinality and map;

forbid sorting of semantically ordered children.
```

Every normalized node must have:

```text
node type;

canonical child order;

exact cardinality;

first/last logical ordinal;

scientific-field output schema;

primitive dependency roots;

normal-form digest.
```

Create:

```text
docs/next_level/c90_normal_form_contract.json
docs/next_level/c90_normal_form_validation.json
```

---

# 11. Structural expansion-equivalence theorem

Implement a machine-readable proof certificate that establishes, by structural induction over the semantic IR:

```text
ATOM_TABLE expands to its frozen ordered records;

ORDERED_RANGE has exact cardinality and ordinal map;

ORDERED_UNION preserves child sequence concatenation and exact
count;

CARTESIAN_PRODUCT preserves the declared mixed-radix order and
rank/unrank bijection;

FILTER preserves exactly the predicate-selected sequence;

PERMUTE is bijective and order explicit;

MAP_RECORD constructs every C88 scientific field exactly once;

GROUP/BOUND/STATUS/ANCESTRY templates preserve the frozen C82
semantics.
```

The theorem must conclude:

```text
if two normalized programs have identical IR, primitive roots,
schemas, expressions, cardinalities, and order versions, then their
complete expanded C88 scientific-record sequences are identical.
```

Create:

```text
docs/next_level/c90_expansion_equivalence_theorem.json
docs/next_level/c90_expansion_equivalence_validation.json
```

The theorem may be implemented as a typed proof/checker, not merely prose.

---

# 12. Exhaustive Route-A/Route-B program equivalence

For every one of the 154,830 supported pairs compare:

```text
pair identity;

normal-form IR;

node graph;

primitive roots;

exact logical count;

mixed-radix order;

record-construction expression;

coefficient expression;

bound rule;

status rule;

ancestry rule;

first and last record identities;

normal-form root.
```

Require:

```text
missing pairs = 0;

extra pairs = 0;

normal-form mismatches = 0;

primitive mismatches = 0;

count mismatches = 0;

order mismatches = 0;

expression mismatches = 0;

bound/status/ancestry mismatches = 0.
```

Create:

```text
docs/next_level/c90_exhaustive_program_equivalence.json
```

This is the primary complete-domain proof. It replaces the infeasible four-pass Python leaf loop only after the structural expansion theorem closes.

---

# 13. Exact closed-form pair summaries

For every normalized pair program derive without leaf iteration:

```text
logical record count;

first and last record IDs;

coordinate/equivalence-class counts;

coefficient-status counts;

exact-expression versus numerical-only counts;

witness-multiplicity distribution;

minimum and maximum local ordinal;

maximum certified coefficient bound or an exact bound envelope;

ancestry-family counts.
```

Each summary must be derived through exact node cardinalities and primitive-table summaries.

Compare Route A and Route B summaries for all pairs.

Create:

```text
docs/next_level/c90_pair_summary_contract.json
docs/next_level/c90_pair_summary_validation.json
```

Require exact reproduction of:

```text
28,606,464;
165,991,250;
697,394,304;
891,992,018 total.
```

---

# 14. Pair-program semantic roots and compact ledger

For every pair compute a semantic root binding:

```text
C88 scientific schema;

C90 semantic-IR and normal-form versions;

pair identity;

normalized IR root;

primitive scientific roots;

exact logical count;

canonical order;

record/coefficient/bound/status/ancestry expression roots;

closed-form summary.
```

Persist one compact ledger entry per supported pair.

Construct:

```text
per-resolution roots;

one aggregate historical semantic-attestation root.
```

Use at least SHA-256 and one independent 256-bit digest for the compact pair-program record itself.

Label these explicitly:

```text
FACTORIZED_SEMANTIC_PROGRAM_ROOT
```

and not:

```text
EXPANDED_SERIALIZED_RECORD_STREAM_HASH.
```

Create:

```text
docs/next_level/c90_pair_program_ledger_schema.json
docs/next_level/c90_pair_program_ledger_validation.json
docs/next_level/c90_historical_semantic_attestation_root.json
docs/next_level/c90_historical_semantic_attestation_validation.json
```

---

# 15. Bounded record-level audits

The exact program proof must be supplemented by live record audits designed to catch compiler/interpreter errors.

Audit both Route A and Route B on:

```text
every pair whose logical count is below a frozen exhaustive
threshold;

the smallest, median, largest, and maximum-cardinality pairs at
each resolution;

all first and last ordinals;

all mixed-radix carry boundaries;

all axis-transition boundaries;

all filter acceptance/rejection boundaries;

all status transitions;

all bound-expression transitions;

all witness-multiplicity transitions;

deterministic pseudorandom ordinals seeded by pair-program roots.
```

For selected bounded pairs, exhaustively compare every reconstructed record.

For selected large pairs, compare complete blocks around every semantic boundary and deterministic interior blocks.

Require equality of every C88 scientific field.

Create:

```text
docs/next_level/c90_record_audit_plan.json
docs/next_level/c90_record_audit_report.json
```

Do not claim that these audits alone establish complete equivalence. Complete equivalence comes from normal-form equality plus the expansion theorem.

---

# 16. Optional accelerated leaf-digest holdout

Implement an optional compiled or vectorized leaf-digest holdout when it can be done without reopening the full infeasible requirement.

Permitted implementations include:

```text
C++ or Rust extension;

Numba/Cython;

vectorized NumPy fixed-width batches;

another repository-supported compiled route.
```

Requirements:

```text
the encoding must preserve the C88 scientific fields;

the route must not use Python object construction per leaf;

the route must be bounded and pair local;

its output is a holdout against the semantic-program root, not the
primary authority;

performance must be benchmarked before large execution.
```

Execute at least one complete large pair and, when practical, one complete
resolution-range shard.

Create:

```text
docs/next_level/c90_accelerated_leaf_audit.json
```

Do not require a complete 891,992,018-leaf digest for the positive semantic-attestation branch.

---

# 17. Bounded pair-level execution

The complete C90 run must scale with:

```text
number of supported pairs;

number of program nodes;

number of primitive scientific records;
```

and not with 891,992,018 leaf reconstructions.

Set hard limits on:

```text
resident programs;

resident primitive-table pages;

pending ledger entries;

checkpoint size;

ledger-shard size;

peak RSS.
```

Record measured complexity, pair-program node counts, maximum program size,
peak memory, total ledger size, and execution throughput.

Create:

```text
docs/next_level/c90_bounded_execution_contract.json
docs/next_level/c90_resource_and_scaling_report.json
```

---

# 18. Restart and pair-atomic commit

Commit one pair-program ledger entry only after:

```text
both route normal forms are complete;

all primitive roots verify;

the pair equivalence result closes;

the exact summaries close;

the selected record audits close.
```

Checkpoint only at completed pair boundaries.

A checkpoint must bind:

```text
all frozen scientific roots;

semantic-IR and normal-form versions;

next pair sequence;

completed ledger count;

closed ledger-shard hashes;

rolling resolution and aggregate roots;

compiler and proof-checker source/API fingerprints.
```

Restart must reject changed roots, schemas, compilers, normalizers, proof
checkers, closed shards, or cursor.

Create:

```text
docs/next_level/c90_restart_contract.json
docs/next_level/c90_restart_validation.json
```

---

# 19. Two clean passes and deterministic parallel execution

Run:

```text
two complete clean semantic-attestation passes;

one controlled interrupted/resumed pass;

one serial pass;

one deterministic pair-range parallel pass with canonical commit
order.
```

Require equality of:

```text
pair normal-form roots;

all pair summaries;

compact ledger entries;

resolution roots;

aggregate semantic-attestation root.
```

Create:

```text
docs/next_level/c90_two_clean_pass_determinism.json
docs/next_level/c90_interrupted_resume_report.json
docs/next_level/c90_parallel_layout_report.json
```

---

# 20. Historical attestation API

Implement under:

```text
src/deuteron_wigner/bridge/ifboundrestart/
```

or the exact repository-equivalent package.

Use compact runtime space equivalent to:

```text
data/runtime/c90_ifboundrestart/
```

Provide immutable operations equivalent to:

```python
load_verified_historical_semantic_attestation()

verify_historical_semantic_attestation_root()

historical_pair_program_root(pair_id, resolution)

historical_pair_normal_form(pair_id, resolution)

historical_pair_summary(pair_id, resolution)

historical_pair_record_count(pair_id, resolution)

unrank_historical_pair_record(pair_id, ordinal, resolution)

audit_historical_pair_records(pair_id, ordinal_ranges, resolution)
```

The API must:

```text
verify compact ledger/index payloads;

return frozen records and non-writeable arrays;

answer one pair query without reconstructing the full domain;

query no C80 numerical kernel value;

form no coefficient-times-kernel product.
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Create:

```text
docs/next_level/c90_api_contract.json
docs/next_level/c90_api_validation.json
docs/next_level/c90_runtime_inventory.json
docs/next_level/c90_deterministic_reconstruction_report.json
```

---

# 21. Descendant semantic-program preflight

Prepare the current descendant C82 adapter under the same:

```text
C88 scientific schema;

C90 semantic IR;

normal-form rules;

primitive-table root semantics;

pair ordering;

summary semantics;

program-root semantics.
```

Run beginning, middle, ending, largest-program, and scientific-boundary preflights.

Do not perform the complete historical-versus-descendant comparison in C90 unless the frozen contract explicitly requires it.

Create:

```text
docs/next_level/c90_descendant_semantic_program_preflight.json
```

---

# 22. C91/IFEQUIV6 import contract

Define the exact contract by which C91 consumes:

```text
the historical C90 pair-program ledger;

historical normal forms and primitive roots;

historical pair summaries;

historical resolution and aggregate roots;

the expansion-equivalence theorem and proof checker;

the historical unrank/audit API;

the descendant semantic-program adapter;

the scientific versus instance-field classification.
```

C91 must:

```text
construct the complete descendant pair-program ledger;

compare all 154,830 pair normal forms, primitive roots, counts,
summaries, and semantic roots;

declare exact matches through the expansion theorem;

perform bounded record-level diagnostics only for mismatched
pairs;

type scientific and instance-only differences;

and issue the historical-versus-descendant equivalence decision.
```

Create:

```text
docs/next_level/c90_c91_ifequiv6_import_contract.json
```

---

# 23. Isolation and mutation controls

Required failures include mutations of:

```text
historical C82 commit;

C87 root;

C88 scientific schema;

semantic-IR schema;

normal-form rule;

node type;

axis order;

axis cardinality;

union disjointness;

retained multiplicity;

filter predicate;

permutation;

record expression;

coefficient expression;

bound rule;

status rule;

ancestry template;

primitive table record;

primitive root;

pair count;

first/last record;

pair normal-form root;

summary count;

proof certificate;

proof-checker logic;

ledger entry;

resolution root;

aggregate root;

checkpoint cursor;

closed ledger shard;

unsafe loading;

mutable return;

C91 import-contract field.
```

The semantic-attestation root must remain invariant under mutations of:

```text
temporary path;

host/process ID;

pair batch size;

ledger-shard size;

checkpoint frequency;

parallel scheduling;

narrative report path.
```

Poison:

```text
C80 numerical kernel values;

C53 values and propagators;

C58 values;

physical coupling;

counterterm interfaces.
```

The C90 programs and roots must remain unchanged.

Create at least **384 focused live mutations** of actual IR nodes, primitive
tables, normal forms, summaries, proof certificates, ledgers, checkpoints,
APIs, and roots.

Create:

```text
docs/next_level/c90_isolation_report.json
docs/next_level/c90_regression_report.json
```

---

# 24. Readiness and continuation decisions

Select exactly one branch.

## 24.1 Favorable branch

Issue the exact positive status frozen by the C90 contract, with preferred
semantic identity:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY
```

Required:

```text
the semantic IR represents every C88 scientific field;

all primitive scientific tables are complete and authenticated;

Route A and Route B compile independently;

all 154,830 normalized programs agree exactly;

the structural expansion-equivalence theorem closes for every node
type;

all exact counts and summaries agree;

the total 891,992,018 logical count is reproduced;

all compact pair ledgers are committed exactly once;

bounded record audits close;

two clean passes agree;

interrupted/resumed execution agrees;

serial/parallel execution preserves all semantic roots;

the historical API passes;

the descendant preflight and C91 import contract are complete;

the mutation suite passes.
```

Next:

> **C91/IFEQUIV6 — exhaustive historical-versus-descendant factorized-semantic equivalence with mismatch-localized record diagnostics**

## 24.2 Semantic IR incomplete

```text
C90_IFBOUNDRESTART_SEMANTIC_IR_INCOMPLETE
```

Next:

> **C91/IFIR — repair only the specifically identified missing node, scientific field, expression, ordering, multiplicity, or ancestry representation**

## 24.3 Primitive authority incomplete

```text
C90_IFBOUNDRESTART_PRIMITIVE_AUTHORITY_INCOMPLETE
```

Next:

> **C91/IFPRIMITIVE — repair only the specifically identified missing, unauthenticated, ambiguous, or double-owned primitive scientific table**

## 24.4 Historical/C89 program mismatch

```text
C90_IFBOUNDRESTART_FACTORIZED_PAYLOAD_MISMATCH
```

Next:

> **C91/IFPROGRAMDIFF — resolve only the specifically identified pair normal-form, primitive, count, ordering, expression, bound, status, or ancestry mismatch**

## 24.5 Expansion theorem incomplete

```text
C90_IFBOUNDRESTART_EXPANSION_PROOF_INCOMPLETE
```

Next:

> **C91/IFPROOF — repair only the specifically identified node-semantics, induction, rank/unrank, filter, map, or multiplicity proof defect**

## 24.6 Bounded/restart execution incomplete

```text
C90_IFBOUNDRESTART_EXECUTION_INCOMPLETE
```

Next:

> **C91/IFEXEC — repair only the pair-level resource bound, ledger, checkpoint, canonical commit, restart, or deterministic parallel defect**

## 24.7 Public attestation API incomplete

```text
C90_IFBOUNDRESTART_PUBLIC_ATTESTATION_INCOMPLETE
```

Next:

> **C91/IFATTESTAPI2 — repair only the compact ledger, root verification, immutable normal-form query, unrank audit, or descendant-adapter defect**

Do not issue historical-versus-descendant equivalence, final-public-persisted-import, coefficient-times-kernel, contact-matrix, complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD, matching, inference, or production status.

---

# 25. Essential deliverables

Create at least:

```text
docs/next_level/c90_implementation_report.md
docs/next_level/c90_derivation_authority_manifest.json
docs/next_level/c90_input_fidelity_audit.json
docs/next_level/c90_input_freeze.json

docs/next_level/c90_semantic_ir_schema.json
docs/next_level/c90_semantic_ir_validation.json
docs/next_level/c90_primitive_table_manifest.json
docs/next_level/c90_primitive_table_validation.json

docs/next_level/c90_route_a_historical_semantic_compiler.json
docs/next_level/c90_route_a_validation.json
docs/next_level/c90_route_b_c89_normalizer.json
docs/next_level/c90_route_b_validation.json
docs/next_level/c90_normal_form_contract.json
docs/next_level/c90_normal_form_validation.json

docs/next_level/c90_expansion_equivalence_theorem.json
docs/next_level/c90_expansion_equivalence_validation.json
docs/next_level/c90_exhaustive_program_equivalence.json

docs/next_level/c90_pair_summary_contract.json
docs/next_level/c90_pair_summary_validation.json
docs/next_level/c90_pair_program_ledger_schema.json
docs/next_level/c90_pair_program_ledger_validation.json
docs/next_level/c90_historical_semantic_attestation_root.json
docs/next_level/c90_historical_semantic_attestation_validation.json

docs/next_level/c90_record_audit_plan.json
docs/next_level/c90_record_audit_report.json
docs/next_level/c90_accelerated_leaf_audit.json

docs/next_level/c90_bounded_execution_contract.json
docs/next_level/c90_resource_and_scaling_report.json
docs/next_level/c90_restart_contract.json
docs/next_level/c90_restart_validation.json
docs/next_level/c90_two_clean_pass_determinism.json
docs/next_level/c90_interrupted_resume_report.json
docs/next_level/c90_parallel_layout_report.json

docs/next_level/c90_api_contract.json
docs/next_level/c90_api_validation.json
docs/next_level/c90_runtime_inventory.json
docs/next_level/c90_deterministic_reconstruction_report.json
docs/next_level/c90_descendant_semantic_program_preflight.json
docs/next_level/c90_c91_ifequiv6_import_contract.json

docs/next_level/c90_isolation_report.json
docs/next_level/c90_readiness_report.json
docs/next_level/c90_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifboundrestart/
```

Use only compact runtime payloads under:

```text
data/runtime/c90_ifboundrestart/
```

No expanded full-record stream may be written.

Create exactly one next-package contract corresponding to the selected branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 26. Acceptance criteria

C90 is complete only when:

1. Baseline `14fc6205837d566f8a06ee1254e3e01ae23875ab` reproduces.
2. Both protected untracked paths remain untouched.
3. C77/C78/C80/C82/C87/C88/C89 historical artifacts remain unchanged.
4. `c90_ifboundrestart_contract.json` is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. The historical environment and C87 capsule verify.
7. The C88 scientific fields and order remain unchanged.
8. No multi-month brute-force Python leaf run is launched.
9. Every scientific field is represented in the semantic IR.
10. No opaque callable or unordered map enters scientific identity.
11. Every primitive scientific table is authenticated and uniquely owned.
12. Route A is independent of the C89 program constructor.
13. Route B is independent of the historical semantic compiler.
14. Every pair normalizes to one canonical program.
15. All 154,830 pair normal forms agree between routes.
16. Every pair count agrees.
17. The three resolution counts and total 891,992,018 reproduce exactly.
18. Every ordering, coefficient, bound, status, and ancestry expression agrees.
19. The structural expansion-equivalence theorem covers every node type.
20. The theorem checker passes all pair programs.
21. The compact pair-program ledger contains every supported pair exactly once.
22. No unsupported pair is introduced.
23. Resolution and aggregate semantic roots close.
24. The roots are explicitly labeled semantic program roots, not expanded stream hashes.
25. Bounded record audits cover all semantic boundaries.
26. Exhaustive small-pair audits pass.
27. Peak memory and persistent size remain within hard bounds.
28. Checkpoints commit only completed pair results.
29. Interrupted/resumed execution agrees.
30. Two clean passes agree.
31. Serial and deterministic parallel execution agree.
32. The historical attestation API verifies and is immutable.
33. One pair query does not reconstruct the full domain.
34. The descendant semantic-program preflight passes.
35. The C91 import contract is complete.
36. At least 384 focused live mutations pass.
37. No historical-versus-descendant equivalence claim is made.
38. No final public C82 persisted snapshot is claimed.
39. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
40. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
41. The working tree is clean except for the two protected untracked paths.
42. A local completion commit is created and not pushed.

Do not weaken the C88 scientific schema, C89 exact factorization, primitive ownership, canonical ordering, expansion theorem, or record-audit coverage to avoid the infeasible leaf loop.

---

# 27. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C90 contract identity;
- historical C82 commit and environment qualification;
- complete C87/C88/C89 roots and hashes;
- semantic-IR and normal-form schema hashes;
- primitive-table counts and roots;
- Route-A and Route-B compiler identities;
- supported-pair and logical-record counts by resolution;
- normal-form mismatch counts by category;
- expansion-theorem coverage and proof-checker result;
- pair-summary mismatch counts;
- pair-program ledger size;
- pair, resolution, and aggregate semantic roots;
- record-audit pair/ordinal counts and mismatch counts;
- optional accelerated leaf-audit results;
- peak memory, execution time, and persistent footprint;
- two-clean-pass equality;
- interrupted/resumed equality;
- serial/parallel equality;
- checkpoint and pair-atomic commit results;
- historical API result;
- descendant preflight;
- C91 import-contract result;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that the roots are factorized semantic program roots, not expanded serialized-record hashes;
- confirmation that exact historical C72 runtime-instance recovery remains unclaimed;
- confirmation that no brute-force multi-month leaf run, expanded full-record stream, historical-versus-descendant equivalence claim, final public persisted snapshot, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
