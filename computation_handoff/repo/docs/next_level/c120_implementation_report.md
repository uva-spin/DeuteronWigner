# C120 ICASM3 implementation report

Status: `C120_ICASM3_COMPONENT_EVALUATION_INCOMPLETE`.

The C120 import contract was consumed from
`docs/next_level/c120_icasm3_import_contract.json` at baseline
`76ba78b61951d778421b233501418e43d127025b`. C114 through C119 are consumed
through their existing immutable public records. C119 contributes eight
program authorities and 36 exact current-factor leaves; these values and
zero-radius bounds were not reopened.

The value-level assembly route was audited through grouped primitive
contraction, exact symbolic factorization, bounded compiled traversal,
certified hybrid evaluation, and an independent matrix-free route. None can
start because the authenticated C118 source-witness inventory is structural
only (`values: blocked`) and contains no per-witness matrix target, value, or
bound records. This is a concrete scientific/API blocker, not a runtime-size
or naive-loop claim. The C119 program-level crosswalk remains total and is
preserved; it cannot manufacture the missing C118 witness records.

All eight diagonal programs therefore receive the terminal status
`UNAVAILABLE_BLOCKING`; their `value` and `bound` remain `None`. The eight
cross-sector entries remain exact C114 even-gluon-parity zeros with explicit
certificates. No unavailable component is encoded as an empty or zero
matrix, and the complete current block fails closed.

No numerical `L`, `P_plus`, physical coupling, or counterterm coefficient was
selected. No C53, C112, free-block, non-current gluon, kernel, contact, or
local-QCD object was consumed or created. The targeted continuation is
`C121/ICSUM2`, whose contract requests the missing authenticated witness
value/target/bound domain before rerunning C120.
