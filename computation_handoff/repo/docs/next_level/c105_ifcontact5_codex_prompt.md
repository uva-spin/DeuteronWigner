# C105/IFCONTACT5 Codex Work Package

## Title

**Authenticated C104-coefficient × C80-kernel contraction and source-ordered qg direct-contact operator assembly: exact factor ownership, bounded factorized aggregation over 891,992,018 logical leaves, certified sparse and matrix-free \(M^2/g_s^2\) actions, Hermiticity without post-hoc averaging, and C106 instantaneous-fermion block handoff**

## Authoritative baseline

Start from the clean local C104 completion commit:

```text
fc48c303c3f7eeb991904ab019900ad2dfb20aa0
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "fc48c303c3f7eeb991904ab019900ad2dfb20aa0"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, consume, or use either protected path as scientific authority.

Read completely and execute the sole committed C104-to-C105 import contract:

```text
docs/next_level/c104_c105_ifcontact5_import_contract.json
```

If the sole tracked C105 import contract has a different exact filename or extension, discover it, record its path and SHA-256, and do not create or use a competing contract.

If the import contract freezes exact status names, schemas, public signatures, package names, unit conventions, factor-ownership rules, or a narrower boundary, preserve them. This work package authorizes the first legal C104 coefficient × C80 kernel composition and direct qg contact operator assembly. It does not authorize a physical coupling, counterterm coefficient, C58 combination, or complete instantaneous-fermion operator unless the committed contract explicitly narrows or changes that boundary.

Create one local completion commit. Do not push.

---

# 1. Preserve the positive C104 coefficient authority

Preserve unchanged:

```text
C104_C82_UPSTREAM_FREE_CANONICAL_FACTORIZED_COEFFICIENT_PACKAGE_READY
```

C104 completion identity:

```text
commit:
fc48c303c3f7eeb991904ab019900ad2dfb20aa0

package root:
42d3dc72def67806245875cf8c9fdfd1d801b212716e6735ade0763b4b2028de

persisted pair programs:
154,830

factorized logical-record census:
891,992,018
```

C104 guarantees:

```text
exact mixed-radix rank/unrank;
immutable on-demand canonical records;
canonical C80 coordinate identities;
projected coefficient expressions/values;
coefficient-side bounds;
terminal statuses;
witness multiplicities;
factor ownership;
reversible ancestry;
C103 equivalence-certificate references;
zero C80 evaluator calls;
zero kernel values loaded;
zero coefficient-times-kernel products;
zero contact-matrix entries;
no expanded record stream.
```

Preserve the C103 scientific-equivalence roots and all C90-C103 roots read-only.

Do not reopen the historical-versus-descendant comparison.

Create:

```text
docs/next_level/c105_derivation_authority_manifest.json
docs/next_level/c105_input_fidelity_audit.json
docs/next_level/c105_c104_input_freeze.json
```

Issue:

```text
C105_C104_CANONICAL_COEFFICIENT_AUTHORITY_FROZEN_COMPLETE
```

---

# 2. Preserve and authenticate the C80 kernel authority

Read and verify the complete committed C80 implementation, public API, source chain, coordinate schema, unit convention, numerical/error-bound convention, and runtime artifacts.

Freeze the repository-equivalent of:

```text
C80 status and package root;
C80 source/API fingerprints;
C80 coordinate-schema root;
C80 raw-coordinate canonical serializer;
C80 W3 kernel-expression root;
C80 finite-cell normalization;
C80 spin/polarization rule;
C80 ordered-color rule;
C80 four-HO overlap rule;
C80 P^- and M^2 conversion rule;
C80 exact/numerical evaluation policy;
C80 error-bound policy;
explicit g_s^2 factor-ownership declaration.
```

The accepted scientific boundary is:

```text
C104 owns:
    projected embedding coefficient;
    bra conjugation;
    witness multiplicity;
    coefficient bound/status;
    canonical coordinate identity;
    coefficient ancestry.

C80 owns:
    the source-derived W3 direct-contact kernel factors;
    finite-cell denominator/normalization;
    spin/polarization contraction;
    ordered generator/color contraction;
    exact four-HO overlap;
    P^- to M^2 kernel conversion;
    kernel numerical/error bounds;
    explicit symbolic factor g_s^2.
```

Create:

```text
docs/next_level/c105_c80_kernel_authority_manifest.json
docs/next_level/c105_c80_coordinate_schema_crosswalk.json
docs/next_level/c105_factor_ownership_freeze.json
```

Issue:

```text
C105_C80_KERNEL_AUTHORITY_FROZEN_COMPLETE
```

Do not reinterpret C80 as already containing a projected physical matrix element. C80 evaluates a caller-supplied raw coordinate; C104 supplies the projected coefficient and the total canonical coordinate map.

---

# 3. Scientific classification

C105 is the **bare direct qg contact contraction and operator-assembly package**.

It is not:

```text
a new C82 coefficient model;
a new C80 kernel derivation;
a physical-coupling selection;
a renormalized instantaneous-fermion operator;
a counterterm solution;
a C58 self-induced-inertia combination;
a C53 sequential-propagation contribution;
a complete local-QCD Hamiltonian;
or a downstream TMD/matching/inference object.
```

For a canonical C104 logical record \(\ell\), define the factored bare contribution

\[
\mathcal A_\ell^{(M^2/g_s^2)}
=
c_\ell\,
\widehat{\mathcal K}^{\,M^2}_{\kappa(\ell)},
\]

where:

```text
c_l:
    C104 projected coefficient, including the frozen bra-conjugation rule
    and only the C104-owned multiplicity/ownership factors;

kappa(l):
    C104 canonical C80 raw-coordinate identity;

K_hat:
    C80 direct W3 contact kernel with g_s^2 removed/factored.
```

For each physical qg bra/ket pair \(p\),

\[
H^{\rm contact}_{p}/g_s^2
=
\sum_{\ell\in\mathcal L_p}
c_\ell\,
\widehat{\mathcal K}^{\,M^2}_{\kappa(\ell)}.
\]

C105 must compute and authenticate this source-ordered sum.

The result is a **bare \(M^2/g_s^2\) qg-sector direct-contact operator**.

Do not insert a numerical \(g_s\), \(\alpha_s\), counterterm, or C58 contribution.

---

# 4. Exact purpose and stopping point

C105 must produce:

```text
a verified C104-to-C80 coordinate and unit crosswalk;

a complete audit proving every C104 canonical record is legally evaluable
by C80 exactly once;

a deterministic C80 kernel cache or factorized kernel authority over every
unique coordinate actually required by C104;

an exact/bounded factorized aggregation program for every supported qg pair;

one bare source-ordered \(M^2/g_s^2\) matrix entry for every supported pair;

certified coefficient, kernel, product, and summed-entry bounds;

a sparse operator for each resolution;

an independently implemented matrix-free action for each resolution;

source-derived Hermiticity and diagonal-reality validation without
post-hoc symmetrization;

complete support, ancestry, count-once, unit, and factor-ownership ledgers;

bounded mismatch localization;

safe immutable public APIs;

two clean, serial/sharded, interrupted/resumed deterministic builds;

at least 384 focused live mutations;

and exactly one C106 instantaneous-fermion assembly import contract.
```

C105 must not:

```text
write the expanded 891,992,018-record stream;

require 891,992,018 Python-object records in memory;

silently drop records by threshold;

use C53 sequential propagation as a contact substitute;

include C58 self-induced inertia in the qg contact matrix;

choose or run a physical g_s;

solve a counterterm coefficient;

post-hoc Hermitianize the matrix;

construct a proton TMD, matching kernel, ART25 bridge, fit, inference,
process, or production object.
```

The preferred positive status is:

```text
C105_C104_C80_SOURCE_DERIVED_QG_DIRECT_CONTACT_OPERATOR_READY
```

If the import contract freezes another exact spelling, use it and record the preferred status only as a semantic alias.

The preferred favorable continuation is:

> **C106/IFERM3 — source-ordered instantaneous-fermion block assembly from the separate C58 q-sector self-induced-inertia primitive and C105 qg direct-contact operator, with counterterm directions and coefficients kept explicit**

---

# 5. Verify C104/C80 coordinate totality and identity

For every C104 pair program and every algebraically reachable logical record class, prove that its canonical coordinate identity maps to exactly one legal C80 input.

Compare:

```text
resolution;
longitudinal mode labels;
incoming/outgoing q and g mode identities;
spin/helicity labels;
ordered generator/color labels;
four-HO mode labels;
basis scale and truncation labels;
raw-coordinate orientation;
P^- versus M^2 output request;
zero-mode exclusion status;
coordinate schema/API version;
canonical coordinate digest.
```

Require:

```text
C104 logical records represented: 891,992,018
unmapped coordinate classes: 0
multiply mapped coordinate classes: 0
orientation ambiguities: 0
schema mismatches: 0
unit mismatches: 0
zero-mode policy mismatches: 0
```

This may be proved factorized over pair programs and coordinate constructors. Do not expand the full leaf domain merely to count it.

Create:

```text
docs/next_level/c105_coordinate_totality_contract.json
docs/next_level/c105_coordinate_totality_validation.json
docs/next_level/c105_unit_and_orientation_validation.json
```

---

# 6. Reconcile factor ownership and prevent double counting

For every multiplicative factor in a logical contribution, assign exactly one owner.

Audit at least:

```text
C104 projection amplitude;
C104 bra conjugation;
C104 witness multiplicity;
C104 endpoint/embedding ancestry;
C80 finite-cell denominator;
C80 longitudinal delta;
C80 spin/polarization factor;
C80 ordered-color factor;
C80 four-HO overlap;
C80 P^- to M^2 conversion;
symbolic g_s^2;
resolution normalization.
```

Require:

```text
unowned required factors: 0
multiply owned factors: 0
silent absorbed multiplicities: 0
duplicate bra conjugations: 0
duplicate color factors: 0
duplicate HO overlaps: 0
numerical g_s^2 insertions: 0
```

Create:

```text
docs/next_level/c105_factor_ownership_validation.json
docs/next_level/c105_no_double_counting_ledger.json
```

---

# 7. Kernel evaluation strategy and cache

Audit the complete unique-coordinate domain implied by C104 before expensive evaluation.

Report by resolution:

```text
logical leaf count;
unique C80 coordinate count;
coordinate reuse multiplicity distribution;
largest coordinate family;
exact-expression classes;
numerical-evaluation classes;
estimated CPU time;
estimated persistent bytes;
peak-memory plan.
```

Select exactly one production strategy:

## Route A — exact factorized kernel family

Use when C80 expressions can be represented and evaluated by compact coordinate families without one stored value per coordinate.

## Route B — authenticated unique-coordinate cache

Use when the unique-coordinate census is tractable. Evaluate C80 once per unique coordinate and persist:

```text
coordinate identity;
kernel exact expression or canonical numerical value;
kernel interval/error bound;
P^- and/or M^2 identity;
C80 source/API roots;
evaluation status;
record digest.
```

## Route C — bounded hybrid

Use compact exact family rules for some classes and a cache for residual classes.

## Route D — unavailable

Use only if no complete exact/bounded evaluation route can close. Do not issue a partial operator as complete.

Create:

```text
docs/next_level/c105_kernel_evaluation_route_decision.json
docs/next_level/c105_unique_coordinate_census.json
docs/next_level/c105_kernel_cache_contract.json
docs/next_level/c105_kernel_cache_validation.json
```

All kernel cache entries must retain \(g_s^2\) as an external symbolic factor.

No physical coupling value may enter any cache root.

---

# 8. Avoid global leaf expansion

The full logical domain contains 891,992,018 records. C105 must use a bounded factorized contraction.

For each pair, select a mathematically exact route equivalent to one of:

```text
factorized dynamic-program contraction over mixed-radix axes;

group-by-coordinate coefficient reduction followed by one C80 evaluation
per unique coordinate;

compiled/vectorized bounded pair-local traversal with deterministic
coordinate memoization;

a hybrid of these routes with exact equality certificates.
```

A pair-local streaming traversal is permitted because the maximum pair size is bounded, but the implementation must avoid retaining the global leaf domain.

Record:

```text
maximum pair logical count;
maximum pair working-set bytes;
maximum pair unique coordinates;
global cache bytes;
total kernel evaluations;
total product evaluations;
total factorized reductions;
elapsed time and peak RSS.
```

Require:

```text
expanded global stream files: 0
global Python leaf list: 0
partial pair silently accepted: 0
threshold-pruned contribution: 0
```

Create:

```text
docs/next_level/c105_factorized_aggregation_contract.json
docs/next_level/c105_factorized_aggregation_validation.json
docs/next_level/c105_resource_and_scaling_report.json
```

---

# 9. Canonical product and bound arithmetic

Define one immutable per-contribution product record or factorized equivalent.

For coefficient enclosure

\[
c \in \widehat c \pm \epsilon_c
\]

and kernel enclosure

\[
K \in \widehat K \pm \epsilon_K,
\]

use a certified complex-norm product enclosure at least as strong as

\[
\epsilon_{cK}
\le
|\widehat c|\,\epsilon_K
+
|\widehat K|\,\epsilon_c
+
\epsilon_c\epsilon_K.
\]

Use directed interval arithmetic or the exact contract-frozen bound rule when available.

For every pair sum contributions in canonical source order and produce:

```text
central complex M^2/g_s^2 value;
certified absolute error/enclosure;
exact/nonzero/zero/interval status;
logical contribution count;
unique-coordinate count;
cancellation diagnostic;
ancestry root;
factor-ownership root.
```

Do not reinterpret coefficient-side or kernel-side bounds as statistical errors.

Create:

```text
docs/next_level/c105_product_bound_contract.json
docs/next_level/c105_product_bound_validation.json
docs/next_level/c105_pair_aggregation_schema.json
```

---

# 10. Independent contraction routes

Implement at least two scientifically independent routes for the final pair entries.

## Route A — sparse-entry builder

For each pair:

```text
consume its C104 factorized program;
obtain C80 kernel values through the selected authority;
form source-ordered products;
sum to one sparse entry and bound;
persist the entry.
```

## Route B — independent matrix-free action

Given a vector \(v\), compute

\[
(H_{\rm contact}/g_s^2)v
\]

directly from compact pair ledgers or independently grouped factorized programs without reading the assembled sparse matrix values as the action source.

The matrix-free route may share immutable primitives and kernels but must have a separate pair/action accumulation implementation.

Require agreement on:

```text
all basis vectors for small/diagnostic subspaces;
deterministic random complex vectors;
worst-support vectors;
Hermitian quadratic forms;
adjoint actions.
```

Create:

```text
docs/next_level/c105_sparse_assembly_contract.json
docs/next_level/c105_matrix_free_action_contract.json
docs/next_level/c105_sparse_matrix_free_validation.json
```

---

# 11. Resolution-specific operator assembly

Verify the physical qg basis dimensions and pair support from frozen C47/C77/C78/C104 authority.

Expected dimensions to verify from repository artifacts:

```text
K9:  qg dimension 1,344
K11: qg dimension 2,700
K13: qg dimension 4,752
```

Expected supported pair counts:

```text
K9:   16,224
K11:  43,350
K13:  95,256
total: 154,830
```

Build one sparse bare direct-contact operator per resolution:

```text
shape = qg_dim × qg_dim
units = GeV^2 / g_s^2, or exact contract-frozen equivalent
storage = deterministic CSR plus enough metadata to verify canonical COO
```

Report:

```text
shape;
supported pair count;
numerically nonzero entries;
certified exact-zero entries;
maximum row/column support;
diagonal entries;
entry norm statistics;
bound statistics;
matrix root.
```

Do not insert unsupported zeros as scientific entries.

Create:

```text
docs/next_level/c105_resolution_operator_inventory.json
docs/next_level/c105_sparse_operator_validation.json
```

---

# 12. Source-derived Hermiticity and diagonal reality

The direct contact operator must be Hermitian because of the source-ordered \(b^\dagger a^\dagger a b\) structure and the frozen bra/ket/conjugation rules.

Verify from source identities:

\[
H_{ji}=H_{ij}^{*}
\]

and, on the diagonal,

\[
\operatorname{Im}H_{ii}=0
\]

within certified bounds.

Do not repair a defect by:

```text
(H + H^\dagger)/2;
copying one triangle;
dropping an imaginary diagonal;
or averaging conflicting entries.
```

Require:

```text
missing conjugate partners: 0
pair-conjugation identity mismatches: 0
Hermiticity defects outside bounds: 0
diagonal-imaginary defects outside bounds: 0
post-hoc Hermitianization calls: 0
```

Create:

```text
docs/next_level/c105_source_ordered_hermiticity_contract.json
docs/next_level/c105_source_ordered_hermiticity_validation.json
```

---

# 13. Count-once and ancestry closure

Every C104 logical contribution must enter exactly one pair aggregation exactly once.

Prove factorized count-once closure:

```text
logical records represented = 891,992,018
records omitted = 0
records duplicated = 0
records assigned to wrong pair = 0
records assigned to wrong coordinate = 0
records assigned wrong multiplicity = 0
records with broken ancestry = 0
```

For each sparse entry retain reversible ancestry to:

```text
pair identity;
C104 pair program;
logical leaf classes or exact rank intervals;
C104 coefficient expressions;
C80 coordinate/kernel entries;
product-bound records;
C103 equivalence certificates.
```

Do not persist all leaf ancestries separately when a factorized ancestry certificate closes.

Create:

```text
docs/next_level/c105_count_once_ledger.json
docs/next_level/c105_ancestry_closure.json
```

---

# 14. Holdouts and independent numerical checks

Freeze a deterministic holdout plan before evaluating final values.

Include:

```text
first/middle/final supported pair of each resolution;
smallest and largest logical-count pairs;
largest-cancellation pairs;
largest kernel-bound pairs;
diagonal and off-diagonal pairs;
conjugate pair partners;
zero and nonzero coefficient classes;
zero and nonzero kernel classes;
mixed-radix carry boundaries;
coordinate-reuse boundaries;
color/generator-order boundaries;
spin/helicity boundaries;
four-HO overlap boundaries.
```

For holdouts:

1. Materialize the relevant C104 logical records within a byte cap.
2. Evaluate C80 directly for every required coordinate.
3. Form products independently.
4. Sum with high precision or exact arithmetic where feasible.
5. Compare with the production factorized aggregation.

Require zero mismatch outside certified bounds.

Create:

```text
docs/next_level/c105_holdout_plan.json
docs/next_level/c105_independent_holdout_validation.json
```

---

# 15. C58, C53, coupling, and counterterm isolation

Poison and forbid during C105 production:

```text
C58 self-induced-inertia numerical values;
C53 sequential canonical-vertex propagation values;
physical g_s or alpha_s values;
counterterm coefficients;
renormalized operator assemblers;
downstream local-HQCD matrices.
```

Require:

```text
C58 values consumed: 0
C53 propagated values consumed: 0
physical coupling values consumed: 0
counterterm coefficients consumed: 0
```

Retain only references/status records needed for the C106 handoff.

Create:

```text
docs/next_level/c105_contribution_isolation_report.json
docs/next_level/c105_bare_operator_scope_contract.json
```

---

# 16. Public immutable C105 API

Implement under:

```text
src/deuteron_wigner/bridge/ifcontact5/
```

or the exact contract-frozen equivalent.

Use compact runtime artifacts under:

```text
data/runtime/c105_ifcontact5/
```

Expose operations equivalent to:

```python
load_verified_qg_direct_contact_authority()

verify_qg_direct_contact_authority()

direct_contact_operator(resolution)

direct_contact_sparse_matrix(resolution)

direct_contact_matrix_free_action(resolution, vector)

direct_contact_entry(
    resolution,
    bra_index,
    ket_index,
)

direct_contact_pair_entry(pair_id, resolution)

direct_contact_entry_ancestry(pair_id, resolution)

kernel_value_for_coordinate(coordinate_id)

verify_source_ordered_hermiticity(resolution)

factor_ownership_contract()
```

Returned matrices and arrays must be immutable.

One-entry lookup must not rebuild the operator or scan all logical leaves.

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
unsafe paths or symlinks;
unindexed files;
unknown coordinate/unit/schema versions;
writable arrays;
mutable records;
root mismatches;
hidden build-if-missing;
hidden repair behavior;
network access.
```

Create:

```text
docs/next_level/c105_api_contract.json
docs/next_level/c105_api_validation.json
docs/next_level/c105_safe_loading_contract.json
docs/next_level/c105_safe_loading_validation.json
docs/next_level/c105_no_recomputation_report.json
```

---

# 17. Compact runtime roots

Construct and keep distinct:

```text
C105_KERNEL_AUTHORITY_ROOT

C105_PAIR_PRODUCT_AGGREGATION_ROOT

C105_QG_DIRECT_CONTACT_OPERATOR_ROOT

C105_PACKAGE_ROOT
```

The operator root binds the bare \(M^2/g_s^2\) direct-contact science.

It must not bind:

```text
a numerical physical coupling;
C58;
a counterterm coefficient;
a renormalized full instantaneous-fermion operator.
```

Create:

```text
docs/next_level/c105_root_semantics.json
docs/next_level/c105_package_root_manifest.json
docs/next_level/c105_runtime_inventory.json
```

---

# 18. Determinism, restart, serial, and sharded execution

Run:

```text
two complete clean kernel/aggregation/operator builds;

one serial build;

one deterministic pair-range sharded build with canonical merge order;

one controlled interrupted/resumed build;

two matrix-free validation traversals in different query/vector orders.
```

All gzip output must use deterministic headers:

```text
mtime = 0;
stable or omitted embedded filename;
fixed compression level;
canonical record/file/shard order.
```

Use pair-atomic checkpoints binding:

```text
C104 package root;
C80 authority roots;
coordinate-crosswalk root;
factor-ownership root;
kernel-cache root;
next pair sequence;
closed shard hashes;
rolling aggregation root;
builder/API fingerprints.
```

Restart must reject changed roots, schemas, ownership rules, C80 source/API identity, cache entries, or closed shards.

Require identical:

```text
kernel cache/family records;
all pair entries and bounds;
sparse matrices;
matrix-free action roots/holdouts;
Hermiticity results;
all C105 roots;
final readiness decision.
```

Create:

```text
docs/next_level/c105_restart_contract.json
docs/next_level/c105_restart_validation.json
docs/next_level/c105_two_clean_build_determinism.json
docs/next_level/c105_sharded_build_report.json
docs/next_level/c105_resource_and_scaling_report.json
```

---

# 19. Isolation and mutation controls

Execute at least **384 focused live mutations** across actual:

```text
C104 coefficients, bounds, statuses, multiplicities, coordinates, and ancestry;
C80 kernel expressions, values, bounds, units, and coordinate identities;
factor-ownership records;
product-bound rules;
pair logical counts;
coordinate grouping;
sparse-entry values and bounds;
conjugate partner relations;
diagonal entries;
matrix-free action;
count-once ledger;
kernel cache;
checkpoints;
public API roots;
C106 import-contract fields.
```

Required failures include mutation of every scientific field.

Required invariance includes:

```text
temporary path;
query order;
shard layout;
process ID;
wall-clock timestamp;
documentation path;
excluded instance metadata.
```

Poison and verify independence from:

```text
C58 values;
C53 propagated values;
physical coupling;
counterterm coefficients;
downstream contact-plus-self-energy assembly;
TMD/matching/inference/production APIs.
```

Create:

```text
docs/next_level/c105_isolation_report.json
docs/next_level/c105_regression_report.json
```

---

# 20. C104 consumption boundary

Create an explicit consumption record:

```text
C104 coefficient authority
    -> consumed read-only to supply projected coefficients, canonical C80
       coordinates, multiplicities, bounds, statuses, ownership, ancestry,
       and C103 equivalence certificates;

C80 kernel authority
    -> consumed read-only to supply the W3 kernel with g_s^2 factored;

C105
    -> owns only the product, aggregation, bare qg direct-contact entry,
       sparse operator, matrix-free action, and their bounds.
```

State explicitly:

```text
C105 does not modify C104 coefficients or C80 kernels;

C105 does not reopen C103 equivalence;

C105 does not combine C58 or solve renormalization.
```

Create:

```text
docs/next_level/c105_c104_c80_consumption_report.json
```

---

# 21. C106 import contract

On favorable closure create the sole next contract:

```text
docs/next_level/c105_c106_iferm3_import_contract.json
```

C106 must consume:

```text
C58 q-sector self-induced-inertia bare primitive and its status/bounds;
C105 qg-sector bare direct-contact operator and matrix-free action;
frozen q and qg basis orders;
sector-specific counterterm directions/statuses;
C43/C55 normal-ordering and contribution-separation contracts;
C105 factor-ownership/no-double-counting ledgers.
```

C106 must:

```text
assemble the source-ordered bare instantaneous-fermion block structure;

keep q self-induced inertia and qg direct contact in their correct sectors;

preserve explicit g_s^2 factorization;

type unresolved qg counterterm-only directions without assigning a
coefficient;

validate block Hermiticity and matrix-free action;

avoid adding C53 sequential propagation;

avoid selecting a physical coupling or counterterm coefficient.
```

C106 must not silently interpret a missing counterterm coefficient as zero.

If the committed C104 contract freezes a different next package, preserve it.

---

# 22. Readiness branches

Select exactly one branch.

## Favorable

Issue the exact contract-frozen positive status, with preferred semantic identity:

```text
C105_C104_C80_SOURCE_DERIVED_QG_DIRECT_CONTACT_OPERATOR_READY
```

Required:

```text
C104 and C80 authorities verify read-only;

the C104-to-C80 coordinate map is total and unique;

factor ownership closes with no missing or duplicate factor;

the complete 891,992,018-record logical domain is represented exactly once;

all required C80 kernel coordinates are evaluated or represented exactly
with certified bounds;

all 154,830 pair entries are aggregated;

all three sparse qg operators and independent matrix-free actions close;

source-derived Hermiticity and diagonal reality close without post-hoc
repair;

product and aggregation bounds close;

C58, C53, physical coupling, and counterterms remain unconsumed;

safe-loading, determinism, restart, sharding, API, and mutation gates pass;

the C106 import contract is complete.
```

Next:

> **C106/IFERM3 — source-ordered bare instantaneous-fermion block assembly**

## Coordinate map incomplete

```text
C105_IFCONTACT5_C104_C80_COORDINATE_MAP_INCOMPLETE
```

## Factor ownership incomplete

```text
C105_IFCONTACT5_FACTOR_OWNERSHIP_INCOMPLETE
```

## Kernel evaluation incomplete

```text
C105_IFCONTACT5_KERNEL_EVALUATION_INCOMPLETE
```

## Factorized aggregation incomplete

```text
C105_IFCONTACT5_AGGREGATION_INCOMPLETE
```

## Sparse/matrix-free operator mismatch

```text
C105_IFCONTACT5_OPERATOR_ACTION_MISMATCH
```

## Source Hermiticity incomplete

```text
C105_IFCONTACT5_SOURCE_HERMITICITY_INCOMPLETE
```

## Public package incomplete

```text
C105_IFCONTACT5_PUBLIC_PACKAGE_INCOMPLETE
```

Use only the exact branch authorized by the import contract. Localize the precise coordinate, factor, pair, kernel, bound, or action defect.

Do not insert a physical coupling, post-hoc symmetrize, or combine C58 to bypass a defect.

---

# 23. Essential deliverables

Create at least:

```text
docs/next_level/c105_implementation_report.md
docs/next_level/c105_derivation_authority_manifest.json
docs/next_level/c105_input_fidelity_audit.json
docs/next_level/c105_c104_input_freeze.json

docs/next_level/c105_c80_kernel_authority_manifest.json
docs/next_level/c105_c80_coordinate_schema_crosswalk.json
docs/next_level/c105_factor_ownership_freeze.json

docs/next_level/c105_coordinate_totality_contract.json
docs/next_level/c105_coordinate_totality_validation.json
docs/next_level/c105_unit_and_orientation_validation.json
docs/next_level/c105_factor_ownership_validation.json
docs/next_level/c105_no_double_counting_ledger.json

docs/next_level/c105_kernel_evaluation_route_decision.json
docs/next_level/c105_unique_coordinate_census.json
docs/next_level/c105_kernel_cache_contract.json
docs/next_level/c105_kernel_cache_validation.json

docs/next_level/c105_factorized_aggregation_contract.json
docs/next_level/c105_factorized_aggregation_validation.json
docs/next_level/c105_product_bound_contract.json
docs/next_level/c105_product_bound_validation.json
docs/next_level/c105_pair_aggregation_schema.json

docs/next_level/c105_sparse_assembly_contract.json
docs/next_level/c105_matrix_free_action_contract.json
docs/next_level/c105_sparse_matrix_free_validation.json
docs/next_level/c105_resolution_operator_inventory.json
docs/next_level/c105_sparse_operator_validation.json

docs/next_level/c105_source_ordered_hermiticity_contract.json
docs/next_level/c105_source_ordered_hermiticity_validation.json
docs/next_level/c105_count_once_ledger.json
docs/next_level/c105_ancestry_closure.json

docs/next_level/c105_holdout_plan.json
docs/next_level/c105_independent_holdout_validation.json
docs/next_level/c105_contribution_isolation_report.json
docs/next_level/c105_bare_operator_scope_contract.json

docs/next_level/c105_api_contract.json
docs/next_level/c105_api_validation.json
docs/next_level/c105_safe_loading_contract.json
docs/next_level/c105_safe_loading_validation.json
docs/next_level/c105_no_recomputation_report.json

docs/next_level/c105_root_semantics.json
docs/next_level/c105_package_root_manifest.json
docs/next_level/c105_runtime_inventory.json

docs/next_level/c105_restart_contract.json
docs/next_level/c105_restart_validation.json
docs/next_level/c105_two_clean_build_determinism.json
docs/next_level/c105_sharded_build_report.json
docs/next_level/c105_resource_and_scaling_report.json

docs/next_level/c105_c104_c80_consumption_report.json
docs/next_level/c105_c106_iferm3_import_contract.json
docs/next_level/c105_isolation_report.json
docs/next_level/c105_readiness_report.json
docs/next_level/c105_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifcontact5/
```

Use compact runtime metadata under:

```text
data/runtime/c105_ifcontact5/
```

Create exactly one next-package contract corresponding to the selected branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not modify the protected C69 prompt.

---

# 24. Acceptance criteria

C105 is complete only when:

1. Baseline `fc48c303c3f7eeb991904ab019900ad2dfb20aa0` reproduces.
2. Both protected paths remain untouched and untracked.
3. C90-C104 tracked scientific artifacts remain unchanged.
4. The sole C104-to-C105 import contract is consumed and preserved.
5. C104 package root verifies exactly.
6. C80 kernel authority, source/API identity, coordinate schema, and unit convention verify exactly.
7. C104/C80 coordinate mapping is total and unique.
8. Factor ownership assigns every factor exactly once.
9. Numerical \(g_s^2\) is absent and the symbolic factor remains external.
10. No C58 or C53 numerical contribution is consumed.
11. The factorized 891,992,018-record domain enters exactly once.
12. No expanded global record stream is written.
13. Every required unique C80 coordinate is evaluated or represented under a complete exact route.
14. Kernel values and bounds are authenticated.
15. Every product has a certified bound.
16. Every one of the 154,830 pair entries is aggregated.
17. qg dimensions and supported-pair counts reproduce frozen authority.
18. Sparse operators are produced for all three resolutions.
19. Independent matrix-free actions agree with sparse actions.
20. Source-derived Hermiticity closes without post-hoc repair.
21. Diagonal reality closes within certified bounds.
22. Count-once and ancestry ledgers close.
23. Holdouts reproduce independent direct C104×C80 calculation.
24. One-entry lookup is bounded and does not rebuild the operator.
25. Every NumPy load uses `allow_pickle=False`.
26. Safe immutable loading passes.
27. Two clean builds agree.
28. Serial and sharded builds agree.
29. Interrupted/resumed construction agrees.
30. At least 384 focused live mutations pass.
31. C104 and C80 consumption boundaries are documented.
32. The C106 import contract is complete.
33. No physical coupling is chosen.
34. No counterterm coefficient is solved.
35. No C58 combination, complete instantaneous-fermion operator, TMD/matching, fit, inference, process, or production object is created.
36. `NO_JOINT_MEASURE`, 216 production routes, all 642 ART25 identities/covariance, and all authoritative artifacts remain unchanged.
37. The working tree is clean except for the two protected untracked paths.
38. One local completion commit is created and not pushed.

Do not weaken coordinate totality, factor ownership, exact count-once aggregation, source Hermiticity, product bounds, or contribution isolation to obtain a positive result.

---

# 25. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed import-contract path and hash;
- complete C80, C103, and C104 authority roots;
- exact C104 package root;
- selected kernel-evaluation route;
- unique-coordinate census by resolution;
- kernel-cache/family records, bytes, and roots;
- logical leaf census and count-once results;
- total product and factorized reduction counts;
- qg basis dimensions;
- supported-pair and sparse-entry counts;
- operator units;
- sparse operator roots by resolution;
- matrix-free action roots and residuals;
- Hermiticity and diagonal-reality results;
- product/aggregation bound statistics;
- holdout counts and mismatches;
- C58/C53/coupling/counterterm consumption counts;
- public API signatures;
- package files, bytes, and roots;
- safe-loading/no-recomputation results;
- deterministic/restart/sharded results;
- mutation and test results;
- C104/C80 consumption result;
- C106 import-contract result;
- exact readiness/no-go status;
- exact next branch;
- confirmation that no expanded stream, physical coupling, counterterm, C58 combination, complete instantaneous-fermion operator, or downstream physics object was created;
- confirmation that nothing was pushed.
