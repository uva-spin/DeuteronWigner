# C96/IFHISTPUBLIC Codex Work Package

## Title

**Authenticated immutable completion of the C94 historical theorem-input surface: exact public loaders for historical pair normal forms, pair proof inputs, and primitive records; root-preimage verification, strict no-recomputation access, and C97 exhaustive-equivalence handoff**

## Authoritative baseline

Start from the clean local C95 completion commit:

```text
8e9be179236507171d670bbcc9e540d018260fa3
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "8e9be179236507171d670bbcc9e540d018260fa3"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either protected path as scientific authority.

Consume completely the sole committed C96 continuation contract. The expected path is:

```text
docs/next_level/c96_ifhistpublic_contract.json
```

If the tracked continuation contract has a different exact filename or extension, discover the sole C96/IFHISTPUBLIC contract, record its path and SHA-256, and do not create or use a competing contract.

If the contract freezes exact status names, schemas, method signatures, or a narrower boundary, preserve them. This work package supplies only the three missing authenticated immutable public loaders and their integrity layer; it does not authorize a historical-versus-descendant comparison or any change to C90–C94 scientific content.

The historical C90 completion commit remains:

```text
ac622ab358b83f090717d7e7fa179b58f18f526d
```

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical environment remains:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

The C93 payload relation remains:

```text
RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY
```

The baseline is authoritative only when it contains and reproduces:

```text
C93_C90_AUTHENTICATED_SEMANTIC_PAYLOAD_READY

C94_C93_PUBLIC_EQUIVALENCE_IMPORT_READY

C95_IFEQUIV7_HISTORICAL_PUBLIC_INPUT_INCOMPLETE
```

with the exact C95 result:

```text
C94 authority:
    verifies through exported immutable public interfaces;

fresh descendant compilation:
    154,830 supported pairs;
    891,992,018 represented logical records;

comparison status:
    not executed;

exact missing C94 public methods:
    historical_pair_normal_form
    historical_pair_proof_inputs
    historical_primitive_record

reason:
    these are mandatory inputs to the public expansion checker and
    may not be inferred from roots, family pages, summaries, hashes,
    or private modules;

scientific conclusion:
    no historical-versus-descendant equivalence or mismatch is
    claimed;

sole continuation:
    C96/IFHISTPUBLIC, limited to those three authenticated immutable
    public loaders.
```

Verify and record the complete C90, C93, C94, and C95 roots, package indices, source/API fingerprints, pair counts, primitive-family counts, theorem/checker identities, and continuation-contract hash from the repository. Prefixes in this prompt are not authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification

C96 is a **narrow public retrieval and authentication completion**.

It is not:

```text
a new scientific derivation;
a replay of C90;
a new C93 recovery route;
a new C94 facade architecture;
a historical-versus-descendant comparison;
or a reconstruction of any object from a hash.
```

C93 already contains the complete normalized programs, primitive-family
records, typed expressions, theorem/checker corpus, and pair proof inputs.
C94 already authenticates that capsule and publicly exposes pair
enumeration, DAG nodes, primitive fileset families, theorem access, proof
records, and the checker.

The exact defect is that C95 cannot obtain three complete typed objects
through stable public calls.

No external source is required for:

```text
a pair-root-to-normal-form public assembler;
a content-addressed proof-input lookup;
a primitive-record direct-lookup index;
or immutable public dataclasses.
```

Fail closed only when one of the three required objects is absent from the
authenticated C93/C94 payload, cannot be bound to the frozen roots, or
requires scientific recomputation.

---

# 2. Descendant qualifications and claim boundary

Do not alter historical C93, C94, or C95 artifacts.

Create the descendant qualifications:

```text
C94_C93_PUBLIC_EQUIVALENCE_IMPORT_READY
    -> C94_GENERAL_PUBLIC_AUTHORITY_READY_BUT_THREE_THEOREM_INPUT_LOADERS_ABSENT

C95_IFEQUIV7_HISTORICAL_PUBLIC_INPUT_INCOMPLETE
    -> descendant domain reproduced;
       comparison blocked solely by the three missing historical
       public loaders.
```

Every C96 object must retain:

```text
scientific_relation =
    RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY

original_c90_runtime_payload =
    NOT_CLAIMED

exact_historical_c72_runtime_instance =
    UNKNOWN_NOT_CLAIMED

root_semantics =
    FACTORIZED_SEMANTIC_PROGRAM_ROOT
```

Create:

```text
docs/next_level/c96_descendant_qualification.json
docs/next_level/c96_claim_boundary.json
```

---

# 3. Exact purpose and stopping point

C96 must add exactly these new scientific-data retrieval operations:

```python
historical_pair_normal_form(pair_id, resolution)

historical_pair_proof_inputs(pair_id, resolution)

historical_primitive_record(family_id, record_id)
```

Administrative package verification and inclusion-proof helpers may be
added only as required to authenticate those three calls.

C96 must produce:

```text
a complete audit locating the exact persisted source objects for all
three loaders;

a selected data-only implementation route for each loader;

authenticated direct-lookup indices where absent;

immutable typed return schemas;

full package/capsule/scientific-root binding;

strict no-recomputation and no-private-fallback behavior;

exhaustive equivalence of every returned object to C93/C94 payload
content;

all-pair historical self-regression through the public checker;

a public-only C97 comparison preflight;

and a C97/IFEQUIV8 import contract.
```

C96 must not:

```text
add a fourth scientific-data loader unrelated to the exact blocker;

compile the current descendant domain beyond bounded preflight;

perform the full historical-versus-descendant comparison;

issue a scientific equivalence or mismatch decision;

write an expanded 891,992,018-record stream;

construct the final factorized coefficient package;

query C80 numerical kernel values;

form a coefficient-times-kernel product;

construct a contact matrix or action;

choose a coupling or counterterm;

add C53 or C58;

or create any TMD, matching, fit, inference, process, or production
object.
```

The preferred positive status is:

```text
C96_C94_HISTORICAL_PUBLIC_THEOREM_INPUT_READY
```

If the committed C96 contract freezes a different exact status, preserve it
and record an explicit semantic alias.

The favorable continuation is:

> **C97/IFEQUIV8 — resume the exhaustive public-only historical-versus-descendant factorized-semantic comparison**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C90:
    semantic-IR schema;
    normal-form rules;
    expansion theorem and checker semantics;
    historical pair identities and proof-result semantics;

C93:
    authenticated capsule;
    pair bindings;
    complete normal-form DAG;
    primitive-family records;
    expression library;
    theorem/checker corpus;
    pair proof-input store;
    proof-result/certificate store;
    capsule-only loader;

C94:
    package root/index;
    pair attestation schema;
    exact pair enumeration and pagination;
    public normal-form-node access;
    primitive fileset-family access;
    theorem and checker access;
    pair proof records;
    safe-loading and no-recomputation policies;

C95:
    historical public-input audit;
    fresh descendant compiler census;
    exact three-method blocker;
    C96 continuation contract.
```

Create:

```text
docs/next_level/c96_derivation_authority_manifest.json
docs/next_level/c96_input_fidelity_audit.json
```

---

# 5. Freeze C94 and C93 authority

Consume C94 through its verified public package loader and C93 only through
the content-addressed payload identities already bound by C94.

Freeze:

```text
C94 package root and index;
C93 capsule root and index;
C90 aggregate semantic root;
historical C90/C82 commits;
historical environment qualification;
pair-enumeration root;
normal-form DAG root;
primitive-family roots;
expression-library root;
theorem/checker roots;
proof-input and proof-certificate roots;
all 154,830 pair bindings.
```

Issue:

```text
C96_HISTORICAL_PUBLIC_AUTHORITY_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c96_input_freeze.json
```

No scientific content may be altered to make a loader easier to implement.

---

# 6. Three-object payload-location audit

Locate the exact authenticated payload source for each required return
object.

For every loader record:

```text
public method name;
C93 store/file;
C94 index or authenticated reference;
source schema;
source scientific root;
record count;
current public accessibility;
required direct-lookup key;
required inclusion chain;
required safe decoder;
whether a new index is needed;
whether any content copy is needed.
```

Create:

```text
docs/next_level/c96_required_loader_payload_audit.json
docs/next_level/c96_public_surface_gap_matrix.json
```

The preferred outcome is:

```text
normal-form content:
    composed from existing C94 public pair attestation and DAG node
    access;

proof inputs:
    directly indexed from the existing authenticated C93 proof-input
    store bound by C94;

primitive record:
    directly indexed from the existing authenticated C93 primitive
    stores bound by C94.
```

Do not reconstruct an absent object from its root.

---

# 7. Select one implementation route per loader

For each of the three methods select exactly one route.

## Route A — composition from existing C94 public calls

Use only when the complete object can be assembled without private access
and every component is already public and authenticated.

This is preferred for `historical_pair_normal_form`.

## Route B — new C96 lookup index over unchanged C93/C94 payload bytes

Use when content exists in the authenticated capsule but C94 lacks a direct
record index.

C96 may add only:

```text
record-ID-to-content-location tables;
pair-ID-to-proof-input-location tables;
inclusion trees;
public immutable wrappers;
API fingerprints.
```

This is expected for `historical_pair_proof_inputs` and
`historical_primitive_record` when direct indices are absent.

## Route C — unavailable

Use when the exact object is not present in the authenticated payload and
would require C90/C93 reconstruction or inference.

Create:

```text
docs/next_level/c96_loader_route_decision.json
```

A mixed A/B selection across the three methods is allowed and expected.

---

# 8. C96 adapter package root

Create a compact adapter package under:

```text
data/runtime/c96_ifhistpublic/
```

The C96 root must bind:

```text
C94 package root;
C93 capsule root;
C90 aggregate scientific root;
the three loader schemas;
lookup-index roots;
inclusion-proof roots;
safe-loading and no-recomputation policies;
public API fingerprint;
complete adapter file inventory.
```

The C96 root is an interface identity only. It is not a new normal-form,
primitive-family, theorem, or scientific-equivalence root.

Create:

```text
docs/next_level/c96_package_root_manifest.json
docs/next_level/c96_runtime_inventory.json
docs/next_level/c96_package_root_validation.json
```

Do not duplicate the 225 MB C93 capsule except for compact authenticated
indices or canonical immutable wrappers.

---

# 9. Loader 1 — `historical_pair_normal_form`

Implement the exact public operation:

```python
historical_pair_normal_form(
    pair_id: str,
    resolution: str,
) -> PublicHistoricalNormalForm
```

The method must:

1. load and verify the C94 pair attestation;
2. obtain the pair's normal-form root;
3. traverse the complete reachable DAG through authenticated C94/C93 node
   records;
4. preserve ordered children and all typed node semantics;
5. verify every node hash and the recomputed root;
6. reject orphan references, forbidden cycles, unknown node types, missing
   expressions, or primitive dependencies;
7. return one frozen typed normal-form object accepted directly by the
   public expansion checker.

The return object must bind:

```text
pair ID and resolution;
global and resolution-local pair sequence;
semantic-IR and normal-form versions;
root node and reachable node set;
primitive dependencies;
expression dependencies;
exact cardinality;
canonical mixed-radix ordering;
record/coefficient/bound/status/multiplicity/ancestry expression
identities;
first/last logical identities;
pair summary;
normal-form root;
C90/C93/C94/C96 roots.
```

The loader must not scan all historical pairs to answer one query.

Create:

```text
docs/next_level/c96_historical_pair_normal_form_contract.json
docs/next_level/c96_historical_pair_normal_form_validation.json
```

---

# 10. Loader 2 — `historical_pair_proof_inputs`

Implement the exact public operation:

```python
historical_pair_proof_inputs(
    pair_id: str,
    resolution: str,
) -> PublicHistoricalPairProofInputs
```

The returned object must be the exact persisted historical proof input, not
a reconstruction from the proof result.

It must bind:

```text
pair identity and order;
historical Route-A normal-form root/reference;
historical Route-B normal-form root/reference;
primitive-equivalence bindings;
scientific schema;
semantic-IR and normal-form versions;
theorem/checker version;
exact cardinality;
canonical order identity;
record-expression identity;
coefficient-expression identity;
bound-rule identity;
status-rule identity;
multiplicity-rule identity;
ancestry-rule identity;
expected proof result;
proof-input root;
proof-certificate root;
C90/C93/C94/C96 roots.
```

The loader must:

```text
verify the proof-input record hash;
verify pair and theorem identity;
verify all referenced normal forms, primitives, and expressions
exist;
verify inclusion in the C93 proof-input store and C94/C96 package;
return a frozen object directly accepted by the public checker.
```

Create:

```text
docs/next_level/c96_historical_pair_proof_inputs_contract.json
docs/next_level/c96_historical_pair_proof_inputs_validation.json
```

---

# 11. Loader 3 — `historical_primitive_record`

Implement the exact public operation:

```python
historical_primitive_record(
    family_id: str,
    record_id: str,
) -> PublicHistoricalPrimitiveRecord
```

The method must provide direct lookup across every public primitive family.

The returned object must bind:

```text
family ID and schema;
record ID;
canonical family sequence;
complete canonical scientific record;
producer/provenance identity;
family scientific root;
record digest;
record inclusion proof;
C90/C93/C94/C96 roots.
```

The method must:

```text
verify the family manifest;
verify the direct-lookup index;
verify record hash and canonical ID;
verify inclusion in the family root;
return the same canonical content obtained through C94 family
pagination;
reject a record ID belonging to another family;
reject duplicates and aliases without an explicit canonical adapter.
```

Create:

```text
docs/next_level/c96_historical_primitive_record_contract.json
docs/next_level/c96_historical_primitive_record_validation.json
```

---

# 12. Authentication chains

For each method, record and validate the complete chain.

## Pair normal form

\[
\text{returned normal form}
\rightarrow
\text{normal-form root}
\rightarrow
\text{pair attestation}
\rightarrow
\text{C94 root}
\rightarrow
\text{C93 capsule root}
\rightarrow
\text{C90 aggregate root}.
\]

## Pair proof inputs

\[
\text{proof-input record}
\rightarrow
\text{proof-input store root}
\rightarrow
\text{pair proof record}
\rightarrow
\text{C94/C96 roots}
\rightarrow
\text{C93 capsule root}
\rightarrow
\text{C90 aggregate root}.
\]

## Primitive record

\[
\text{primitive record}
\rightarrow
\text{family root}
\rightarrow
\text{primitive package root}
\rightarrow
\text{C94/C96 roots}
\rightarrow
\text{C93 capsule root}
\rightarrow
\text{C90 aggregate root}.
\]

Create:

```text
docs/next_level/c96_loader_inclusion_proof_contract.json
docs/next_level/c96_loader_inclusion_proof_validation.json
```

---

# 13. Strict no-recomputation and no-private-fallback behavior

Implement under:

```text
src/deuteron_wigner/bridge/ifhistpublic/
```

or the exact repository-equivalent package.

The public methods may read only:

```text
authenticated C94 public package records;
authenticated C93 payload files referenced by C94;
authenticated C96 lookup indices.
```

Poison and forbid:

```text
C77 builders;
C78 builders;
C82 witness, coordinate, coefficient, or aggregation constructors;
C89 pair-program construction;
C90 compilers, normalizers, theorem-input builders, or worktree
replay;
C93 Route-A or Route-B recovery;
private C93 loader methods not explicitly incorporated into the
C94/C96 safe public decoder;
historical worktree staging;
build-if-missing;
repair-if-missing;
network access.
```

Create:

```text
docs/next_level/c96_no_recomputation_contract.json
docs/next_level/c96_no_recomputation_validation.json
```

No public method may fall back to paginating the entire domain and
reconstructing an index in memory on each query.

---

# 14. Safe immutable loading

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payloads;
path traversal;
unexpected symlinks;
unindexed files;
shape, dtype, or endianness mismatch;
unknown node/expression/primitive type;
writable arrays;
mutable records;
unknown schema or theorem versions.
```

Create:

```text
docs/next_level/c96_safe_loading_contract.json
docs/next_level/c96_safe_loading_validation.json
```

---

# 15. Exhaustive normal-form loader validation

For all 154,830 historical pairs:

```text
call historical_pair_normal_form;
verify pair identity and sequence;
verify reachable-node closure;
recompute the normal-form root;
compare the returned content with the C93 capsule source record;
compare pair summary/cardinality/order;
confirm public-checker acceptance on historical self-proof inputs.
```

Require:

```text
missing pair = 0;
failed load = 0;
root mismatch = 0;
node mismatch = 0;
order mismatch = 0;
summary mismatch = 0.
```

Create:

```text
docs/next_level/c96_exhaustive_normal_form_loader_regression.json
```

No expanded leaf traversal is required.

---

# 16. Exhaustive proof-input loader and checker regression

For all 154,830 historical pairs:

```text
call historical_pair_proof_inputs;
load referenced normal forms through the new public loader;
load required primitive records through the new direct loader;
call the unchanged C94 public checker;
compare the returned proof result and proof-certificate root with
the frozen C93/C94 historical result.
```

Require:

```text
proof inputs loaded = 154,830;
checker results reproduced = 154,830;
proof-input mismatch = 0;
proof-result mismatch = 0;
proof-certificate mismatch = 0.
```

Create:

```text
docs/next_level/c96_exhaustive_proof_input_checker_regression.json
```

This is historical self-regression only, not a current-descendant
comparison.

---

# 17. Exhaustive primitive direct-lookup validation

For every record in every C94 primitive family:

```text
enumerate it once through the existing authenticated family pages;
load it directly through historical_primitive_record;
compare complete canonical content, sequence, digest, and family
inclusion proof.
```

Require:

```text
missing primitive record = 0;
extra primitive record = 0;
duplicate canonical ID = 0;
content mismatch = 0;
family-root mismatch = 0;
inclusion failure = 0.
```

Create:

```text
docs/next_level/c96_exhaustive_primitive_direct_lookup_regression.json
```

A sampled family comparison is insufficient.

---

# 18. C95 blocker supersession

Create the explicit descendant supersession:

```text
C95_IFEQUIV7_HISTORICAL_PUBLIC_INPUT_INCOMPLETE
    -> closed by authenticated immutable C96 loaders:
       historical_pair_normal_form;
       historical_pair_proof_inputs;
       historical_primitive_record.
```

The record must state:

```text
no C95 scientific comparison result is retroactively created;
no mismatch or equivalence is inferred;
C97 must execute the comparison.
```

Create:

```text
docs/next_level/c96_c95_blocker_supersession_report.json
```

---

# 19. Public-only C97 preflight

Using only:

```text
C94/C96 historical public APIs;
the frozen independent descendant compiler/artifacts;
```

verify:

```text
all 154,830 historical pair IDs remain enumerable;
the three new methods succeed for beginning, middle, ending,
maximum-cardinality, and every semantic-boundary holdout;
all historical theorem inputs are now obtainable publicly;
all primitive records required by the crosswalk are directly
obtainable;
the checker accepts a bounded historical-versus-descendant
preflight set;
no private historical access occurs.
```

Do not execute the complete comparison.

Create:

```text
docs/next_level/c96_c97_ifequiv8_preflight.json
docs/next_level/c96_c97_ifequiv8_import_contract.json
```

The C97 contract must resume the C95 scientific comparison rather than
reopening C93/C94 recovery or packaging.

---

# 20. Determinism and resource behavior

Run:

```text
two complete index/adapter builds;
one clean build;
one serial build;
one deterministic sharded build where applicable;
one interrupted/resumed build;
two complete public-loader traversals with different bounded query
orders.
```

Require:

```text
identical lookup indices;
identical C96 package root;
identical loader outputs;
identical historical checker regression;
identical C97 preflight.
```

Measure:

```text
adapter package size;
normal-form query peak memory;
proof-input query peak memory;
primitive-record query peak memory;
worst-case query latency;
index construction time.
```

Create:

```text
docs/next_level/c96_deterministic_reconstruction_report.json
docs/next_level/c96_restart_validation.json
docs/next_level/c96_resource_and_scaling_report.json
```

---

# 21. Isolation and mutation controls

Required failures include mutations of:

```text
C94 package root;
C93 capsule root;
C90 aggregate root;
C96 package root/index;
pair ID or resolution;
normal-form root;
normal-form node or child order;
primitive dependency;
expression dependency;
proof-input record;
theorem/checker version;
expected proof result;
proof-certificate root;
primitive family ID;
primitive record ID;
primitive sequence;
primitive content;
lookup-index location;
record inclusion proof;
safe-decoder policy;
no-recomputation guard;
C97 import-contract field.
```

The loader outputs must remain invariant under mutations of:

```text
temporary build path;
host/process ID;
query order;
page size used to build the lookup index;
wall-clock timestamp;
narrative report path.
```

Poison:

```text
C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical coupling;
counterterm interfaces.
```

C96 must remain unchanged.

Create at least **384 focused live mutations** of actual indices, normal
forms, DAG nodes, proof-input records, primitive records, inclusion proofs,
loaders, API objects, and roots.

Create:

```text
docs/next_level/c96_isolation_report.json
docs/next_level/c96_regression_report.json
```

---

# 22. Readiness and continuation decisions

Select exactly one branch.

## 22.1 Favorable branch

Issue the exact positive status frozen by the C96 contract, with preferred
semantic identity:

```text
C96_C94_HISTORICAL_PUBLIC_THEOREM_INPUT_READY
```

Required:

```text
C94/C93 authority verifies;
the source payload for each loader is identified;
all three implementation routes are explicit;
the C96 adapter root binds C94/C93/C90;
historical_pair_normal_form returns the complete verified typed
program for every pair;
historical_pair_proof_inputs returns the exact persisted proof input
for every pair;
historical_primitive_record returns every primitive record directly;
all three methods are immutable and safe;
all authentication chains close;
no scientific reconstruction or private fallback occurs;
all 154,830 normal-form loads pass;
all 154,830 proof-input loads and checker regressions pass;
all primitive direct lookups equal family-page records;
the C95 blocker is explicitly superseded;
the public-only C97 preflight passes;
determinism, restart, resource, and mutation controls pass.
```

Next:

> **C97/IFEQUIV8 — exhaustive public-only historical-versus-descendant factorized-semantic equivalence**

## 22.2 Normal-form loader incomplete

```text
C96_IFHISTPUBLIC_NORMAL_FORM_LOADER_INCOMPLETE
```

Next:

> **C97/IFNFLOADER — repair only the pair-root, DAG traversal, ordered-node, typed-program, or root-verification defect**

## 22.3 Proof-input loader incomplete

```text
C96_IFHISTPUBLIC_PROOF_INPUT_LOADER_INCOMPLETE
```

Next:

> **C97/IFPROOFINPUT — repair only the pair proof-input index, schema, reference, inclusion, or checker-compatibility defect**

## 22.4 Primitive-record loader incomplete

```text
C96_IFHISTPUBLIC_PRIMITIVE_RECORD_LOADER_INCOMPLETE
```

Next:

> **C97/IFPRIMRECORD — repair only the family index, direct lookup, canonical ID, inclusion, or record-decoding defect**

## 22.5 Authentication/public loader incomplete

```text
C96_IFHISTPUBLIC_AUTHENTICATION_INCOMPLETE
```

Next:

> **C97/IFHISTAUTH — repair only the C94/C93/C96 root chain, safe loading, immutability, hidden private access, deterministic index, or no-recomputation defect**

Do not issue historical-versus-descendant equivalence, final coefficient
persistence, coefficient-times-kernel, contact-matrix,
complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD,
matching, inference, or production status.

---

# 23. Essential deliverables

Create at least:

```text
docs/next_level/c96_implementation_report.md
docs/next_level/c96_derivation_authority_manifest.json
docs/next_level/c96_input_fidelity_audit.json
docs/next_level/c96_descendant_qualification.json
docs/next_level/c96_claim_boundary.json
docs/next_level/c96_input_freeze.json

docs/next_level/c96_required_loader_payload_audit.json
docs/next_level/c96_public_surface_gap_matrix.json
docs/next_level/c96_loader_route_decision.json
docs/next_level/c96_package_root_manifest.json
docs/next_level/c96_runtime_inventory.json
docs/next_level/c96_package_root_validation.json

docs/next_level/c96_historical_pair_normal_form_contract.json
docs/next_level/c96_historical_pair_normal_form_validation.json
docs/next_level/c96_historical_pair_proof_inputs_contract.json
docs/next_level/c96_historical_pair_proof_inputs_validation.json
docs/next_level/c96_historical_primitive_record_contract.json
docs/next_level/c96_historical_primitive_record_validation.json

docs/next_level/c96_loader_inclusion_proof_contract.json
docs/next_level/c96_loader_inclusion_proof_validation.json
docs/next_level/c96_no_recomputation_contract.json
docs/next_level/c96_no_recomputation_validation.json
docs/next_level/c96_safe_loading_contract.json
docs/next_level/c96_safe_loading_validation.json

docs/next_level/c96_exhaustive_normal_form_loader_regression.json
docs/next_level/c96_exhaustive_proof_input_checker_regression.json
docs/next_level/c96_exhaustive_primitive_direct_lookup_regression.json
docs/next_level/c96_c95_blocker_supersession_report.json
docs/next_level/c96_c97_ifequiv8_preflight.json
docs/next_level/c96_c97_ifequiv8_import_contract.json

docs/next_level/c96_deterministic_reconstruction_report.json
docs/next_level/c96_restart_validation.json
docs/next_level/c96_resource_and_scaling_report.json
docs/next_level/c96_isolation_report.json
docs/next_level/c96_readiness_report.json
docs/next_level/c96_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifhistpublic/
```

Use only compact adapter/index payloads under:

```text
data/runtime/c96_ifhistpublic/
```

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the protected C69 prompt.

---

# 24. Acceptance criteria

C96 is complete only when:

1. Baseline `8e9be179236507171d670bbcc9e540d018260fa3` reproduces.
2. Both protected untracked paths remain untouched.
3. C90/C91/C92/C93/C94/C95 historical artifacts remain unchanged.
4. The sole committed C96 contract is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. C93 remains a descendant recovered preimage, not the original C90 runtime.
7. C90/C93/C94 roots remain factorized-semantic authorities, not expanded-stream hashes.
8. C94/C93/C90 roots verify before index construction.
9. The exact persisted source object for each loader is identified.
10. No object is inferred from a root or summary.
11. The only new scientific-data methods are the three required methods.
12. `historical_pair_normal_form` returns a complete typed checker-compatible normal form.
13. It verifies every reachable node and the pair root.
14. It answers one pair query without a full-domain scan.
15. `historical_pair_proof_inputs` returns the exact persisted proof input.
16. It verifies pair, theorem, normal-form, primitive, expression, and certificate identities.
17. `historical_primitive_record` supports every primitive family.
18. It returns the exact canonical paged-family record by direct ID.
19. All returned records are frozen and immutable.
20. All three inclusion chains reach C90 through C94/C93/C96.
21. No C77/C78/C82/C89/C90/C93 scientific reconstruction route is called.
22. No private historical fallback is used.
23. Every NumPy load explicitly uses `allow_pickle=False`.
24. Object dtype, unsafe paths, unindexed files, writable arrays, mutable records, and unknown schemas are rejected.
25. All 154,830 historical normal forms load and reproduce their roots.
26. All 154,830 proof inputs load.
27. All 154,830 historical proof results reproduce through the public checker.
28. Every primitive record direct lookup agrees with public family pagination.
29. Primitive missing, extra, duplicate, content-mismatch, and inclusion-failure counts are zero.
30. The C95 blocker is explicitly superseded without creating a comparison result.
31. The C97 public-only preflight passes.
32. Two clean index builds agree.
33. Serial, sharded, and interrupted/resumed builds agree.
34. Query-order changes do not change package identity.
35. At least 384 focused live mutations pass.
36. No historical-versus-descendant equivalence or mismatch is claimed.
37. No expanded record stream is written.
38. No final coefficient package is claimed.
39. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
40. `NO_JOINT_MEASURE`, 216 production routes, all 642 ART25 identities, and all authoritative artifacts remain unchanged.
41. The working tree is clean except for the two protected untracked paths.
42. A local completion commit is created and not pushed.

Do not broaden C96 into another general facade or scientific package. Do
not weaken direct-object completeness, root authentication,
checker-compatibility, primitive-record coverage, or no-recomputation
behavior to open the gate.

---

# 25. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed C96 contract path and hash;
- historical C90/C82 commits and environment qualification;
- complete C90, C93, C94, and C96 roots;
- selected route for each of the three loaders;
- lookup-index file counts and size;
- exact public method signatures;
- normal-form loader count, root-closure results, and maximum query memory;
- proof-input loader count and checker-regression results;
- primitive-family and primitive-record direct-lookup counts;
- primitive page-versus-direct mismatch counts;
- inclusion-chain results for all three loaders;
- no-recomputation and private-access poisoning results;
- safe-loading results;
- C95 blocker supersession result;
- C97 public-only preflight result;
- deterministic/restart/query-order invariance results;
- peak memory, query latency, and persistent footprint;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime recovery remains unclaimed;
- confirmation that C93 remains a descendant recovered preimage and not the original C90 runtime;
- confirmation that roots remain factorized-semantic roots, not expanded serialized-record hashes;
- confirmation that no historical-versus-descendant comparison, expanded record stream, final coefficient package, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
