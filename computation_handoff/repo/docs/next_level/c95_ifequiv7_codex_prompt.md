# C95/IFEQUIV7 Codex Work Package

## Title

**Exhaustive public-only historical-versus-descendant C82 factorized-semantic equivalence: complete primitive crosswalk, 154,830-pair normal-form comparison, theorem-certified expanded-sequence identity, mismatch-localized bounded diagnostics, deterministic equivalence roots, and C96 canonical-persistence handoff**

## Authoritative baseline

Start from the clean local C94 completion commit:

```text
922f5a6e9d771561340992e84888db2ad1fd8328
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "922f5a6e9d771561340992e84888db2ad1fd8328"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either protected path as scientific authority.

Read completely and preserve the committed C94-to-C95 import contract. The expected path is:

```text
docs/next_level/c94_c95_ifequiv7_import_contract.json
```

If the exact tracked filename or extension differs, discover the sole C95 import contract, record its path and SHA-256, and do not create or use a competing contract.

If that contract freezes exact status names, scientific-relation names, schemas, APIs, or a narrower boundary, preserve them. This work package supplies the exhaustive comparison, theorem application, difference classification, deterministic equivalence certificate, and C96 handoff plan; it does not authorize weakening the import contract.

The historical C90 completion commit is:

```text
ac622ab358b83f090717d7e7fa179b58f18f526d
```

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical environment remains qualified as:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

The C93 payload relation remains:

```text
RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY
```

The baseline is authoritative only when it contains and reproduces:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE

C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE

C93_C90_AUTHENTICATED_SEMANTIC_PAYLOAD_READY

C94_C93_PUBLIC_EQUIVALENCE_IMPORT_READY
```

with the exact C94 result:

```text
historical public authority:
    direct immutable facade over unchanged authenticated C93
    recovered-preimage capsule content;

historical pair domain:
    all 154,830 pairs publicly enumerable in exact order;

historical public objects:
    root-bound pagination and cursors;
    direct pair lookup;
    normalized semantic programs;
    primitive fileset families;
    typed expressions;
    theorem specification;
    pair proof inputs and records;
    no-leaf expansion checker;

checker regression:
    all 154,830 historical C90/C93 proof results reproduced with
    zero mismatches;

scope boundary:
    C91/C92 public-import blockers superseded;
    no historical-versus-descendant comparison performed.
```

The exact logical census remains:

| Resolution | Supported pairs | Logical records |
|---|---:|---:|
| `K9_2_N8_b0.40` | 16,224 | 28,606,464 |
| `K11_2_N10_b0.45` | 43,350 | 165,991,250 |
| `K13_2_N12_b0.50` | 95,256 | 697,394,304 |
| **Total** | **154,830** | **891,992,018** |

Verify every full root, package hash, pair count, resolution split, theorem/checker fingerprint, public API identity, and C91 descendant-domain identity from the repository. Prefixes and prose summaries are not authority.

Create a local completion commit. Do not push.

---

# 1. Scientific question and exact claim

C95 must answer:

> Does the current descendant C82 scientific construction define exactly the same ordered C88 scientific-record sequence as the historical C82 construction represented by the immutable C90/C93/C94 public authority, for every one of the 154,830 supported physical pairs?

For each pair \(p\), let

\[
\mathcal P^{\rm hist}_p
\]

be the historical normalized typed program returned through C94, and let

\[
\mathcal P^{\rm desc}_p
\]

be the independently compiled current-descendant program.

The C94 public expansion checker establishes complete record-sequence identity when:

```text
the historical and descendant programs satisfy the same typed C90
semantic IR;

their canonical normal forms agree;

their primitive scientific records are identical or related by an
explicit verified scientific adapter;

their exact cardinalities and ordering agree;

their record, coefficient, bound, status, multiplicity, and
ancestry expressions agree.
```

Then C95 may conclude:

\[
\llbracket\mathcal P^{\rm hist}_p\rrbracket
=
\llbracket\mathcal P^{\rm desc}_p\rrbracket .
\]

This is a complete factorized-semantic proof of equality of the expanded
C88 record sequences. It is not:

```text
a random sample;

a top-level count comparison;

a package-root coincidence;

a claim that 891,992,018 records were serialized;

or recovery of the exact historical C72 runtime directory.
```

---

# 2. Claim boundary

C95 may establish:

```text
historical-canonical versus current-descendant C82 scientific
payload equivalence at the complete factorized-semantic level.
```

C95 must not claim:

```text
the original ignored historical C72 runtime instance was recovered;

the C90/C93/C94 roots are expanded serialized-record hashes;

a final upstream-free coefficient package already exists;

a projected coefficient has been multiplied by a C80 numerical
kernel;

a direct-contact matrix or matrix-free action exists;

a physical coupling or counterterm has been chosen;

the complete instantaneous-fermion operator exists;

a renormalized microscopic TMD or ART25 bridge exists;

or any fit, inference, process, or production object exists.
```

Every historical-side result must retain:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

Every C93-derived object must retain:

```text
RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY
```

---

# 3. Exact purpose and stopping point

C95 must produce:

```text
a frozen immutable historical C94 public authority;

a frozen current-descendant C77/C78/C80/C82 scientific
environment;

an independently validated descendant compiler under the unchanged
C90 semantic IR;

a complete scientific primitive-family crosswalk;

a complete color-authority crosswalk;

a complete 154,830-pair identity and ordering crosswalk;

a normalized descendant program for every supported pair;

an exhaustive pair-by-pair comparison of normal forms, primitive
dependencies, counts, ordering, expressions, summaries, and roots;

one public-checker expansion-equivalence proof certificate for
every matched pair;

bounded structural and record-level diagnostics for every
mismatched pair;

a typed scientific-versus-instance difference classification;

historical, descendant, and equivalence-certificate roots;

two clean comparison passes;

one interrupted/resumed pass;

one serial and one deterministic parallel pass;

an immutable public equivalence API;

and a C96 canonical factorized-persistence import contract.
```

C95 must not write an expanded 891,992,018-record stream.

The preferred positive semantic status is:

```text
C95_C82_HISTORICAL_DESCENDANT_FACTORIZED_SEMANTIC_EQUIVALENCE_READY
```

If the committed C94-to-C95 contract freezes another exact spelling,
preserve it and record an explicit semantic alias.

The favorable continuation is:

> **C96/IFPERSIST4 — authenticated upstream-free canonical factorized pair-coordinate coefficient package, with exact on-demand reconstruction and no C80 kernel multiplication**

If the C95 import contract freezes a different C96 package name, preserve
that exact name.

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C77:
    physical qg basis order;
    physical/raw projection identities;
    exact/value/bound records;
    metric and conjugation conventions;

C78:
    all 154,830 supported-pair identities;
    source-ordered endpoints, witnesses, and retained-q
    intermediates;
    structural support, multiplicity, and ancestry;

C80:
    canonical raw ContactKernelCoordinate schema;
    exact coordinate-equivalence semantics;
    factor ownership;
    explicit exclusion of numerical kernel values from the C82
    coefficient payload;

C82:
    witness-leaf-to-C80-coordinate construction;
    projected-coefficient construction;
    pair-local grouping;
    coefficient expressions, values, bounds, statuses;
    factor ownership and reversible ancestry;

C87:
    canonical source-chain-equivalent color scientific authority;
    exact claim boundary;

C88:
    complete scientific-record schema and field classification;
    canonical ordering and logical census;

C89:
    factorized pair programs and exact rank/unrank semantics;

C90:
    typed semantic IR;
    normal-form rules;
    primitive families;
    expansion theorem;
    historical semantic roots;

C91:
    independently compiled descendant domain;
    complete descendant census;
    public-import blocker;
    descendant compiler and any immutable descendant artifacts;

C93:
    recovered historical payload claim boundary;

C94:
    immutable public historical pair, DAG, primitive, expression,
    theorem, proof-input, and checker APIs;
    complete public package roots;
    C95 import contract.
```

Create:

```text
docs/next_level/c95_derivation_authority_manifest.json
docs/next_level/c95_input_fidelity_audit.json
```

---

# 5. Freeze and verify the historical public authority

Consume the historical side **only** through the immutable C94 public API.

Freeze and verify:

```text
C94 package root and index;
C93 capsule root;
C90 aggregate semantic root;
historical C90 and C82 commits;
historical environment qualification;
C87 scientific and capsule roots;
C88 schema and order versions;
semantic-IR and normal-form versions;
theorem and checker roots;
all historical primitive-family roots;
all expression-library roots;
all 154,830 pair sequence and identity records;
all historical normal-form roots and contents;
all historical summaries;
all historical proof inputs and results;
all resolution roots.
```

Issue:

```text
C95_HISTORICAL_PUBLIC_AUTHORITY_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c95_historical_input_freeze.json
```

Poison and forbid all private access to:

```text
C90 internal ledger readers;
C93 internal recovery objects not exposed by C94;
C90 compilers and normalizers;
historical worktree staging;
C82 historical builders.
```

Any historical object unavailable through C94 is a legitimate public-input
failure. It may not be obtained through private fallback.

---

# 6. Freeze the current descendant scientific environment

Freeze the current descendant route at the exact C95 baseline.

Record:

```text
current source commit;
current C77/C78/C80/C82 scientific package roots;
current public color-science root;
current supported-pair domain;
current coordinate schema;
current coefficient, bound, status, multiplicity, and ancestry
semantics;
current source/API fingerprints;
the C91 descendant compiler identity and frozen descendant-domain
artifacts.
```

Issue:

```text
C95_DESCENDANT_SCIENTIFIC_ENVIRONMENT_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c95_descendant_input_freeze.json
```

The descendant compiler must remain independent of historical expected
normal forms and roots.

Poison:

```text
C94 pair normal forms during descendant compilation;
historical expected pair roots;
historical primitive records as descendant inputs;
C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical-coupling and counterterm interfaces.
```

---

# 7. Descendant compiler integrity

Use the independently constructed C91 descendant compiler when its immutable
implementation and artifacts verify.

Also perform a clean C95 baseline recompilation or semantic replay sufficient
to establish that interface-only C92–C94 changes did not alter descendant
science.

Require agreement between:

```text
C91 frozen descendant compiler result;
C95 clean current-descendant recompilation.
```

Compare:

```text
supported-pair IDs and order;
pair-program roots;
primitive-family roots;
logical counts;
record/coefficient/bound/status/ancestry expression roots;
summaries.
```

Create:

```text
docs/next_level/c95_descendant_compiler_integrity.json
docs/next_level/c95_descendant_recompilation_report.json
```

A mismatch must be classified before any historical comparison begins.

---

# 8. Complete primitive-family crosswalk

Construct a complete historical/descendant crosswalk using C94 public
primitive access and the frozen descendant primitive authorities.

Required scientific families include the actual repository equivalents of:

```text
supported-pair records;
physical bra/ket identities and basis order;
C77 projection amplitudes, exact records, and certified bounds;
C78 witnesses, endpoints, retained-q intermediates, and source
signs;
historical C87 and current descendant color records;
C80 coordinate templates and exact equivalence identities;
metric and conjugation records;
projected-coefficient primitives;
bound primitives;
terminal-status rules;
witness-multiplicity rules;
ancestry primitives;
record and summary expression primitives.
```

For each family record:

```text
historical family ID/root;
descendant family ID/root;
schemas;
record counts;
canonical orders;
record-level comparison result;
explicit adapter, when needed;
scientific relation;
instance-only differences;
proof-certificate root.
```

Classify each relation as exactly one of:

```text
BYTE_IDENTICAL_SCIENTIFIC_TABLE;

CANONICALLY_IDENTICAL_SCIENTIFIC_TABLE;

SCIENTIFICALLY_IDENTICAL_WITH_EXPLICIT_ADAPTER;

SCIENTIFIC_TABLE_DIFFERENCE;

INSTANCE_ONLY_DIFFERENCE;

UNRESOLVED.
```

A favorable C95 result requires no `SCIENTIFIC_TABLE_DIFFERENCE` and no
`UNRESOLVED`.

Create:

```text
docs/next_level/c95_primitive_root_crosswalk.json
docs/next_level/c95_primitive_record_equivalence.json
docs/next_level/c95_primitive_equivalence_certificate_manifest.json
```

Equal counts or roots without public content comparison are insufficient.

---

# 9. Complete color-authority crosswalk

Compare the historical C87 canonical color payload, accessed through C94,
with the descendant color authority.

Compare all scientific fields:

```text
24 ordered product-color rows;
3 ordered retained-triplet columns;
72 ordered pair records;
terminal statuses;
exact expressions and zero certificates;
per-entry values and bounds;
E_src;
Gram matrix;
triplet adapter;
U3 and U3-dagger;
P3;
basis permutations;
all-eight-generator intertwining;
rank/trace/projector identities;
anti-sextet and 15 leakage.
```

Issue one exact relation:

```text
DESCENDANT_COLOR_SCIENCE_IDENTICAL_TO_C87_CANONICAL;

DESCENDANT_COLOR_INSTANCE_DIFFERS_SCIENCE_IDENTICAL;

DESCENDANT_COLOR_SCIENCE_DIFFERS_FROM_C87_CANONICAL;

UNRESOLVED.
```

Create:

```text
docs/next_level/c95_color_authority_crosswalk.json
```

The exact original historical C72 runtime instance remains unknown under
all outcomes.

---

# 10. Complete supported-pair crosswalk

Compare the full historical and descendant pair domains.

Require identity of:

```text
resolution order;
global pair sequence;
resolution-local sequence;
canonical pair ID;
physical bra/ket IDs;
row/column indices;
structural-support status;
pair-local logical-domain identity.
```

Report:

```text
missing historical pair;
extra descendant pair;
duplicate pair;
pair-ID mismatch;
pair-order mismatch;
resolution mismatch;
support-status mismatch.
```

A favorable result requires all counts to be zero.

Create:

```text
docs/next_level/c95_supported_pair_crosswalk.json
docs/next_level/c95_supported_pair_order_validation.json
```

---

# 11. Compile and normalize every descendant pair

For every current descendant supported pair:

1. compile it independently into the unchanged C90 semantic IR;
2. bind descendant primitive records through the approved primitive
   crosswalk;
3. apply the unchanged C90 normal-form rules;
4. compute exact cardinality and canonical order;
5. compute record/coefficient/bound/status/multiplicity/ancestry
   expression roots;
6. compute closed-form summaries;
7. compute the descendant pair semantic-program root.

Each descendant pair record must expose:

```text
pair identity and sequence;
normal-form DAG root and reachable node set;
primitive dependencies;
exact logical count;
mixed-radix/order identity;
record-expression root;
coefficient-expression root;
bound-rule root;
status-rule root;
multiplicity-rule root;
ancestry-rule root;
first and last logical-record identities;
closed-form summary;
descendant pair root.
```

Create:

```text
docs/next_level/c95_descendant_pair_program_manifest.json
docs/next_level/c95_descendant_pair_program_validation.json
```

Do not persist expanded records.

---

# 12. Exhaustive 154,830-pair semantic comparison

For every pair compare historical C94 and descendant C95 objects:

```text
pair identity and order;
normal-form DAG and typed node content;
primitive scientific dependencies under approved certificates;
exact logical count;
mixed-radix and canonical order;
record-construction expression;
projected-coefficient expression;
bound composition;
terminal-status rule;
witness-multiplicity rule;
ancestry rule;
first/last logical identities;
closed-form summary;
pair semantic-program root.
```

Report mismatch counts in at least these categories:

```text
MISSING_PAIR;
EXTRA_PAIR;
PAIR_IDENTITY;
PAIR_ORDER;
NORMAL_FORM_ROOT;
NORMAL_FORM_CONTENT;
NODE_GRAPH;
PRIMITIVE_DEPENDENCY;
LOGICAL_COUNT;
ORDER;
RECORD_EXPRESSION;
COEFFICIENT_EXPRESSION;
BOUND_RULE;
STATUS_RULE;
MULTIPLICITY_RULE;
ANCESTRY_RULE;
FIRST_LAST_IDENTITY;
SUMMARY;
PAIR_ROOT.
```

Create:

```text
docs/next_level/c95_exhaustive_pair_semantic_comparison.json
```

A favorable result requires every scientific mismatch count to be zero.

---

# 13. Public-checker theorem application

For every semantically matched pair, call the C94 public checker with:

```text
historical normalized program from C94;
descendant normalized program from C95;
primitive-equivalence certificates;
the frozen theorem version;
the frozen C88 scientific schema and order;
all record/coefficient/bound/status/multiplicity/ancestry
expression identities.
```

The result must be:

```text
EXPANDED_C88_SEQUENCE_IDENTICAL_BY_FACTORIZED_SEMANTIC_PROOF
```

for every favorable pair.

For each pair record:

```text
proof-input root;
checker source/API/theorem identities;
proof result;
proof-certificate root.
```

Construct:

```text
per-pair equivalence proof roots;
per-resolution equivalence roots;
one aggregate historical-descendant equivalence-certificate root.
```

Create:

```text
docs/next_level/c95_expansion_equivalence_application.json
docs/next_level/c95_equivalence_certificate_manifest.json
docs/next_level/c95_equivalence_root_manifest.json
```

The equivalence roots must be labeled:

```text
FACTORIZED_SEMANTIC_EQUIVALENCE_CERTIFICATE_ROOT
```

and not:

```text
EXPANDED_SERIALIZED_RECORD_STREAM_HASH.
```

A favorable result requires:

```text
proof certificates = 154,830;
proof failures = 0;
proof unresolved = 0.
```

---

# 14. Exact logical-census closure

Aggregate descendant pair cardinalities and theorem-certified matched
cardinalities.

Require exact reproduction of:

```text
K9_2_N8_b0.40:
    16,224 pairs;
    28,606,464 logical records;

K11_2_N10_b0.45:
    43,350 pairs;
    165,991,250 logical records;

K13_2_N12_b0.50:
    95,256 pairs;
    697,394,304 logical records;

total:
    154,830 pairs;
    891,992,018 logical records.
```

Create:

```text
docs/next_level/c95_logical_census_closure.json
```

No expanded leaf traversal is required or permitted.

---

# 15. Bounded mismatch localization

If any pair differs, do not expand the global domain.

For each mismatched pair:

```text
produce a typed DAG/tree diff;
identify the first differing node or primitive dependency;
compare axis cardinalities and order;
derive the affected ordinal interval through mixed-radix algebra;
recursively narrow the interval when needed;
materialize byte-capped historical and descendant records only at
the first differing range and required semantic boundaries;
compare every C88 scientific field.
```

Audit at least:

```text
first differing ordinal;
first and last pair ordinals;
mixed-radix carry boundary;
filter/selection boundary;
status transition;
bound transition;
multiplicity transition;
root-seeded interior holdouts.
```

Provide operations equivalent to:

```python
diagnose_c82_pair_semantic_difference(
    pair_id,
    resolution,
    *,
    max_bytes,
)

materialize_c82_pair_record_difference(
    pair_id,
    ordinal_ranges,
    resolution,
    *,
    max_bytes,
)
```

Create:

```text
docs/next_level/c95_mismatch_localization_contract.json
docs/next_level/c95_mismatch_diagnostic_report.json
```

For a favorable run, this route must still be exercised through mutation
tests.

---

# 16. Difference classification

Classify every observed difference as exactly one of:

```text
SCIENTIFIC_PRIMITIVE_DIFFERENCE;

SCIENTIFIC_PROGRAM_DIFFERENCE;

SCIENTIFIC_ORDER_OR_MULTIPLICITY_DIFFERENCE;

SCIENTIFIC_COEFFICIENT_OR_BOUND_DIFFERENCE;

SCIENTIFIC_STATUS_OR_ANCESTRY_DIFFERENCE;

SCIENTIFIC_SCHEMA_DIFFERENCE;

INSTANCE_ONLY_PACKAGE_ROOT_DIFFERENCE;

INSTANCE_ONLY_PATH_OR_LAYOUT_DIFFERENCE;

INSTANCE_ONLY_SOURCE_API_FINGERPRINT_DIFFERENCE;

DESCENDANT_DOCUMENTATION_DIFFERENCE;

UNRESOLVED_DIFFERENCE.
```

For every instance-only classification, provide field-level evidence that:

```text
the canonical primitive records agree;
the normalized programs agree;
the theorem checker returns equivalence.
```

Create:

```text
docs/next_level/c95_difference_classification.json
docs/next_level/c95_instance_science_separation_report.md
```

Do not use “metadata-only” without the public record comparison and theorem
certificate.

---

# 17. Scientific equivalence decision

Select exactly one scientific relation:

```text
COMPLETE_FACTORIZED_SEMANTIC_EQUIVALENCE;

SCIENTIFICALLY_EQUIVALENT_WITH_INSTANCE_ONLY_DIFFERENCES;

SCIENTIFIC_PAYLOAD_MISMATCH;

SCIENTIFIC_EQUIVALENCE_UNRESOLVED.
```

Definitions:

```text
COMPLETE_FACTORIZED_SEMANTIC_EQUIVALENCE:
    historical and descendant canonical scientific primitive
    records and all normalized pair programs are identical;

SCIENTIFICALLY_EQUIVALENT_WITH_INSTANCE_ONLY_DIFFERENCES:
    package/API instances differ, but complete primitive and
    program equivalence is proved through explicit adapters;

SCIENTIFIC_PAYLOAD_MISMATCH:
    at least one scientific primitive, program, expression,
    ordering, count, bound, status, multiplicity, or ancestry
    differs;

SCIENTIFIC_EQUIVALENCE_UNRESOLVED:
    at least one required comparison, adapter, or theorem proof
    cannot be completed.
```

Create:

```text
docs/next_level/c95_scientific_equivalence_decision.json
```

Either of the first two may support the positive readiness status when all
adapters are explicit and verified.

---

# 18. Historical, descendant, and equivalence roots

Preserve the historical C90/C93/C94 roots unchanged.

Construct a compact descendant ledger with one entry per pair, binding:

```text
pair identity;
descendant normal-form root;
approved primitive equivalence;
exact logical count;
closed-form summary;
proof-certificate root;
comparison status.
```

Construct:

```text
descendant resolution roots;
descendant aggregate semantic-program root;
equivalence resolution roots;
aggregate historical-descendant equivalence-certificate root.
```

Create:

```text
docs/next_level/c95_descendant_semantic_root_manifest.json
docs/next_level/c95_historical_descendant_equivalence_root.json
```

Do not require unexplained equality of package-instance roots. Scientific
equivalence is established by canonical records and proof certificates.

---

# 19. Two clean passes, restart, and deterministic parallel comparison

Run:

```text
two complete clean C95 comparison passes;

one controlled interrupted/resumed comparison;

one serial comparison;

one deterministic pair-range parallel comparison with canonical
pair-result commit order.
```

Use pair-atomic checkpoints.

A checkpoint must bind:

```text
C94 package and C93 capsule roots;
historical C90 aggregate root;
descendant source/scientific roots;
semantic-IR, normal-form, theorem, and checker versions;
primitive-crosswalk root;
next pair sequence;
closed comparison-ledger shard hashes;
rolling descendant and equivalence roots;
compiler/checker source/API fingerprints.
```

Restart must reject changed roots, schema, crosswalk, compiler, checker,
closed shard, or cursor.

Require equality of:

```text
primitive-crosswalk decisions;
all pair comparison outcomes;
all proof-certificate roots;
descendant resolution and aggregate roots;
equivalence-certificate root;
scientific equivalence decision.
```

Create:

```text
docs/next_level/c95_restart_contract.json
docs/next_level/c95_restart_validation.json
docs/next_level/c95_two_clean_pass_determinism.json
docs/next_level/c95_parallel_comparison_report.json
docs/next_level/c95_resource_and_scaling_report.json
```

---

# 20. Public immutable equivalence API

Implement under:

```text
src/deuteron_wigner/bridge/ifequiv7/
```

or the exact repository-equivalent package.

Use compact runtime payloads under:

```text
data/runtime/c95_ifequiv7/
```

Provide immutable operations equivalent to:

```python
load_verified_c82_historical_descendant_equivalence()

verify_c82_historical_descendant_equivalence_root()

c82_scientific_equivalence_decision()

c82_pair_equivalence(pair_id, resolution)

historical_pair_attestation(pair_id, resolution)

descendant_pair_attestation(pair_id, resolution)

c82_pair_equivalence_proof(pair_id, resolution)

c82_primitive_equivalence(family_id)

diagnose_c82_pair_difference(
    pair_id,
    resolution,
    *,
    max_bytes,
)
```

The API must:

```text
verify all compact ledgers and roots;
return frozen records and non-writeable arrays;
answer one pair query without comparing the complete domain again;
access historical data only through C94 public package payloads;
query no C80 numerical kernel values;
form no coefficient-times-kernel product;
perform no build-if-missing or hidden recomputation.
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Create:

```text
docs/next_level/c95_api_contract.json
docs/next_level/c95_api_validation.json
docs/next_level/c95_runtime_inventory.json
docs/next_level/c95_deterministic_reconstruction_report.json
```

---

# 21. C96 canonical factorized-persistence contract

Define the exact contract by which C96 consumes:

```text
the positive C95 scientific equivalence decision;
the C94 historical public authority;
the approved canonical descendant primitive records;
the C88 scientific-record schema;
the C90 semantic IR, normal-form, theorem, and checker versions;
the complete supported-pair order;
the canonical pair-program ledger;
the exact rank/unrank and on-demand reconstruction semantics;
the pair summaries;
the primitive-equivalence certificates;
the C95 pair proof certificates and equivalence roots.
```

C96 must create an authenticated, upstream-free public package that can
return, for any supported pair and pair-local ordinal:

```text
the complete canonical C82 pair-coordinate scientific record;
physical bra/ket identity;
canonical C80 coordinate identity;
projected coefficient exact expression or value;
certified bound;
terminal status;
factor-ownership identity;
witness multiplicity;
complete reversible ancestry;
historical-descendant equivalence certificate.
```

C96 public loading must not call:

```text
historical or current C77/C78/C82 builders;
C89/C90/C91/C93 compilers;
C94 historical reconstruction;
or a build-if-missing path.
```

C96 must not materialize all 891,992,018 records and must not multiply a
coefficient by a C80 kernel.

Create:

```text
docs/next_level/c95_c96_ifpersist4_import_contract.json
```

If the frozen continuation uses a different package suffix, preserve it.

---

# 22. Isolation and mutation controls

Required failures include mutations of:

```text
C94 package root;
C93 capsule root;
C90 aggregate root;
descendant source/scientific root;
C88 schema;
semantic-IR or normal-form version;
theorem or checker identity;
primitive historical record;
primitive descendant record;
primitive adapter;
color record;
pair sequence or ID;
normal-form node or child order;
logical count;
record expression;
coefficient expression;
bound rule;
status rule;
multiplicity rule;
ancestry rule;
first/last identity;
pair summary;
pair root;
proof input;
proof result;
proof-certificate root;
scientific equivalence decision;
comparison-ledger shard;
checkpoint cursor;
public API index;
safe-loader policy;
C96 import-contract field.
```

The positive equivalence decision must remain invariant under mutations of
proved instance-only fields:

```text
temporary paths;
page boundaries;
runtime shard names;
host/process IDs;
wall-clock timestamps;
current roadmap bytes;
narrative report paths;
package-instance metadata whose canonical scientific records are
unchanged.
```

Poison:

```text
C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical coupling;
counterterm interfaces.
```

C95 comparison outputs must remain unchanged.

Create at least **384 focused live mutations** of actual primitive records,
adapters, programs, DAG nodes, expressions, proof inputs, proof
certificates, comparison ledgers, checkpoints, APIs, and decisions.

Create:

```text
docs/next_level/c95_isolation_report.json
docs/next_level/c95_regression_report.json
```

---

# 23. Readiness and continuation decisions

Select exactly one branch.

## 23.1 Favorable branch

Issue the exact positive status frozen by the C94-to-C95 contract, with
preferred semantic identity:

```text
C95_C82_HISTORICAL_DESCENDANT_FACTORIZED_SEMANTIC_EQUIVALENCE_READY
```

Required:

```text
the C94 historical public authority verifies;
the current descendant scientific environment freezes;
the C91/C95 descendant compiler integrity closes;
all primitive scientific families are crosswalked completely;
the color authority is scientifically reconciled;
all 154,830 pair identities and ordering records agree;
all 154,830 descendant programs compile and normalize;
all pair normal forms, primitive dependencies, counts, orders,
expressions, bounds, statuses, multiplicities, ancestry, summaries,
and roots match under approved adapters;
the C94 public checker proves all 154,830 pair sequences equivalent;
the exact 891,992,018-record census closes;
all scientific mismatch counts are zero;
no unresolved difference remains;
the scientific equivalence decision is positive;
two clean, restart, serial, and parallel passes agree;
the immutable equivalence API passes;
the C96 import contract is complete;
the mutation suite passes.
```

Next:

> **C96/IFPERSIST4 — authenticated upstream-free canonical factorized pair-coordinate coefficient package**

## 23.2 Historical public input incomplete

```text
C95_IFEQUIV7_HISTORICAL_PUBLIC_INPUT_INCOMPLETE
```

Next:

> **C96/IFHISTPUBLIC — repair only the specifically identified C94 pair, primitive, expression, theorem, proof-input, pagination, or inclusion defect**

## 23.3 Descendant compiler incomplete

```text
C95_IFEQUIV7_DESCENDANT_COMPILER_INCOMPLETE
```

Next:

> **C96/IFDESCCOMP2 — repair only the current descendant compiler, field mapping, pair ordering, primitive reference, or recompilation defect**

## 23.4 Primitive authority unresolved

```text
C95_IFEQUIV7_PRIMITIVE_AUTHORITY_INCOMPLETE
```

Next:

> **C96/IFPRIMX2 — repair only the specifically identified primitive family, color crosswalk, adapter, record comparison, or root-equivalence defect**

## 23.5 Scientific payload mismatch

```text
C95_IFEQUIV7_SCIENTIFIC_PAYLOAD_MISMATCH
```

Next:

> **C96/IFSCIDIFF3 — resolve only the localized pair, node, primitive, count, order, coefficient, bound, status, multiplicity, or ancestry difference**

## 23.6 Expansion proof incomplete

```text
C95_IFEQUIV7_EXPANSION_PROOF_INCOMPLETE
```

Next:

> **C96/IFPROOF3 — repair only the public checker input, primitive certificate, theorem application, or proof-certificate defect**

## 23.7 Instance/science classification unresolved

```text
C95_IFEQUIV7_INSTANCE_DIFFERENCE_UNRESOLVED
```

Next:

> **C96/IFINSTDIFF2 — localize only the unresolved package, API, path, serializer, or scientific-field distinction**

## 23.8 Public equivalence API incomplete

```text
C95_IFEQUIV7_PUBLIC_EQUIVALENCE_INCOMPLETE
```

Next:

> **C96/IFEQUIVAPI3 — repair only the compact comparison ledger, equivalence-root verification, pair query, diagnostic materializer, immutability, or safe-loading defect**

Do not issue final coefficient-persistence, coefficient-times-kernel,
contact-matrix, complete-instantaneous-fermion, counterterm-solved,
local-QCD, TMD, matching, inference, or production status.

---

# 24. Essential deliverables

Create at least:

```text
docs/next_level/c95_implementation_report.md
docs/next_level/c95_derivation_authority_manifest.json
docs/next_level/c95_input_fidelity_audit.json
docs/next_level/c95_historical_input_freeze.json
docs/next_level/c95_descendant_input_freeze.json

docs/next_level/c95_descendant_compiler_integrity.json
docs/next_level/c95_descendant_recompilation_report.json

docs/next_level/c95_primitive_root_crosswalk.json
docs/next_level/c95_primitive_record_equivalence.json
docs/next_level/c95_primitive_equivalence_certificate_manifest.json
docs/next_level/c95_color_authority_crosswalk.json
docs/next_level/c95_supported_pair_crosswalk.json
docs/next_level/c95_supported_pair_order_validation.json

docs/next_level/c95_descendant_pair_program_manifest.json
docs/next_level/c95_descendant_pair_program_validation.json
docs/next_level/c95_exhaustive_pair_semantic_comparison.json

docs/next_level/c95_expansion_equivalence_application.json
docs/next_level/c95_equivalence_certificate_manifest.json
docs/next_level/c95_equivalence_root_manifest.json
docs/next_level/c95_logical_census_closure.json

docs/next_level/c95_mismatch_localization_contract.json
docs/next_level/c95_mismatch_diagnostic_report.json
docs/next_level/c95_difference_classification.json
docs/next_level/c95_instance_science_separation_report.md
docs/next_level/c95_scientific_equivalence_decision.json

docs/next_level/c95_descendant_semantic_root_manifest.json
docs/next_level/c95_historical_descendant_equivalence_root.json

docs/next_level/c95_restart_contract.json
docs/next_level/c95_restart_validation.json
docs/next_level/c95_two_clean_pass_determinism.json
docs/next_level/c95_parallel_comparison_report.json
docs/next_level/c95_resource_and_scaling_report.json

docs/next_level/c95_api_contract.json
docs/next_level/c95_api_validation.json
docs/next_level/c95_runtime_inventory.json
docs/next_level/c95_deterministic_reconstruction_report.json
docs/next_level/c95_c96_ifpersist4_import_contract.json

docs/next_level/c95_isolation_report.json
docs/next_level/c95_readiness_report.json
docs/next_level/c95_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifequiv7/
```

Use only compact runtime payloads under:

```text
data/runtime/c95_ifequiv7/
```

No expanded full-record stream may be written.

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the protected C69 prompt.

---

# 25. Acceptance criteria

C95 is complete only when:

1. Baseline `922f5a6e9d771561340992e84888db2ad1fd8328` reproduces.
2. Both protected untracked paths remain untouched.
3. C77/C78/C80/C82/C87/C88/C89/C90/C91/C92/C93/C94 historical artifacts remain unchanged.
4. The sole committed C94-to-C95 import contract is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. The C93 capsule remains a descendant recovered preimage, not the original C90 runtime.
7. The C90/C93/C94 roots remain factorized-semantic authorities, not expanded-stream hashes.
8. The historical side is consumed only through C94 public APIs.
9. No private C90/C93 historical access is used.
10. The current descendant environment is frozen explicitly.
11. The descendant compiler is independent of historical expected outputs.
12. C91 and C95 descendant compilation agree.
13. Every primitive scientific family is compared completely.
14. Every primitive adapter is explicit and verified.
15. The complete historical and descendant color sciences are compared.
16. All 154,830 pair identities are present in both routes.
17. No missing, extra, or duplicate pair exists.
18. Pair and resolution ordering agree exactly.
19. Every descendant pair compiles into the unchanged C90 semantic IR.
20. All 154,830 descendant normal forms are constructed.
21. Every pair normal form and node graph is compared.
22. Primitive dependencies agree under approved certificates.
23. Exact logical counts reproduce 891,992,018 records.
24. Record, coefficient, bound, status, multiplicity, and ancestry expressions agree.
25. First/last identities and summaries agree.
26. Every pair receives a public-checker expansion-equivalence proof certificate.
27. Proof-certificate count is 154,830.
28. Scientific proof failures and unresolved results are zero in the favorable branch.
29. Every scientific mismatch count is zero in the favorable branch.
30. Every instance-only difference is justified at field level.
31. The scientific equivalence decision is explicit.
32. Bounded mismatch diagnostics are operational through mutation tests.
33. Two clean comparison passes agree.
34. Interrupted/resumed execution agrees.
35. Serial and deterministic parallel comparison agree.
36. The public API returns immutable verified objects.
37. One pair query does not compare the full domain again.
38. The C96 canonical-persistence contract is complete.
39. At least 384 focused live mutations pass.
40. No expanded logical-record stream is written.
41. No final canonical coefficient package is claimed.
42. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
43. `NO_JOINT_MEASURE`, 216 production routes, all 642 ART25 identities, and all authoritative artifacts remain unchanged.
44. The working tree is clean except for the two protected untracked paths.
45. A local completion commit is created and not pushed.

Do not weaken primitive coverage, pair identity/order, normal-form
comparison, expression equality, public theorem application,
scientific-versus-instance classification, or bounded diagnostics to obtain
a positive equivalence decision.

---

# 26. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed C94-to-C95 contract path and hash;
- historical C90/C82 commits and environment qualification;
- full C90, C93, and C94 roots;
- descendant source/scientific roots;
- semantic-IR, normal-form, theorem, and checker hashes;
- descendant compiler integrity and recompilation result;
- primitive-family crosswalk counts by status;
- primitive-record mismatch counts;
- historical/descendant color-authority relation;
- supported-pair totals, resolution splits, and ordering result;
- descendant pair-program count;
- mismatch counts for every semantic category;
- proof-certificate count and aggregate root;
- exact logical-record counts by resolution and total;
- historical, descendant, and equivalence resolution/aggregate roots;
- scientific versus instance-only difference counts;
- mismatch-diagnostic results;
- final scientific equivalence decision;
- two-clean-pass equality;
- interrupted/resumed equality;
- serial/parallel equality;
- compact runtime size and peak memory;
- public API result;
- C96 import-contract result;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime-instance recovery remains unclaimed;
- confirmation that C93 remains a descendant recovered preimage rather than the original C90 runtime;
- confirmation that all roots are factorized-semantic/equivalence roots rather than expanded serialized-record hashes;
- confirmation that no expanded full-record stream, final coefficient package, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
