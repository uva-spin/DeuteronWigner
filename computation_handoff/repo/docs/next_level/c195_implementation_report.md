# C195/HQCDB1QQBARQVERT1 implementation report

The C194-to-C195 contract was present and hash-verified before implementation:

`docs/next_level/c194_c195_hqcdb1qqbarqvert1_continuation_contract.json`

Contract SHA-256: `2a53c87078d3e01c050f8f5d96444d024b039c39bb35d32e066fffbb9748f9d3`.
The C195 prompt SHA-256 is
`22d0042457fd340dd2477c614323496930cd5be6c6e75c27c5106de32080d05e`.

## Result

The first missing object, `C185-Q-QQBARQ-ORDER2-SOURCE_SCOPE_PARTIAL_NOT_ZERO`,
is normalized with its aliases and closed by the frozen, public C191
`C127-JQ-K-JQ` source owner. The C191 ordered current pair is consumed without
introducing a factor of two. Its number-preserving, pair-creation, and
pair-annihilation branch records are mapped to the exact C185 qqbarq domain.

The C185 pair transition and qqbarq resolvent remain read-only. Same-flavor
direct/exchange and exact Pauli records are separate from different-flavor
records; the symbolic active-flavor record has no hidden sum or average. The
two C185 color channels, `QQBARQ_COLOR_QQ_BAR3` and
`QQBARQ_COLOR_QQ_6`, remain separate and their source-provided recoupling
matrix is consumed rather than reconstructed. The finite-cell `P0`/`Q0`
antisymmetric/PV denominator is explicit; ordinary zero modes and infinite-line
denominators are rejected.

The package exposes 18 terminal qqbarq component records (three resolutions,
three flavor classes, two color channels) and 36 symbolic coefficient records
(two directions, three resolutions, three flavor classes, two channels). It
provides paged sparse and independent matrix-free actions, explicit Hermitian
reverse actions, derivatives for `g_s`, signed `m_R`, `m_R^2`, six counterterm
directions, and nine null coordinates, plus the exact C185 transition and
resolvent handoff. Values are named nonphysical symbolic fixtures only.

C112 remains excluded by its frozen source scope for this qqbarq primitive;
C129 remains sequential/normal-ordering only; C131 remains aggregate-only; and
C130/C182 remain typed nonmatrix boundary/link interfaces. QGG, C194
connected/subtraction/amputation/projector ledgers, all C166 graph records,
Q0/Q1/Q2, C158 values, physical inputs, and the complete qg proper vertex are
unchanged or unconsumed.

## Verification

The C195 suite passed 6 tests, including 384 focused live mutations. The
targeted C153-C195 collection had 212 tests: 210 passed and 2 failed at the
pre-existing untracked C157 boundary (`MATCHIR2-D` and
`C158/HQCDMATCHWINDOW2` expectations versus the frozen C157 implementation).
Those failures were recorded and quarantined; C157 was not repaired and its
preserved untracked test was not modified.

Two network-disabled clean wheels were byte-identical. Both used
`SOURCE_DATE_EPOCH=1787616000` and had SHA-256
`763de01da15307e9e54cacc803ab99abc6a1a770c0609dce64b75f93080f37af`.
Reload, restart, sharding, paging, query-order, safe-loading, and
no-recomputation checks passed. `git diff --check` passed.

The sole evidence-driven continuation is
`C196/HQCDQGVERT3`; no push was performed.
