# C411 implementation report

## Result

`C411_C117_I2_SOURCE_QUALIFIED_FINITE_C43_ADAPTER_CONTRACT_READY_CERTIFICATE_UNAVAILABLE`

C411 binds the remaining interface between the C410 source-reduced retained
connected shape and the C260 `PROJECT_C117_RI_SMOM_V1` first coordinate at
K9, K11, and K13. The contract is K-local and records the exact
`P^- -> M^2` convention without applying it a second time.

The implementation accepts a future source-qualified numerical certificate,
but rejects a nonzero row entry pointing to any of the three C117 source
directions that do not yet have numerical shapes. It also rejects an
undocumented zero target row, wrong basis/source hashes, wrong units, missing
authority, and a conversion record that is not explicitly owned exactly once.

## Current boundary

- C410 source coefficient `-1/2`: closed and applied once.
- `g_s^2`: factored under the C259/C260 operator convention.
- `P^- -> M^2`: formula fixed as `delta M^2 = 2*pi*K/L * delta P^-`,
  application still source-certificate-owned.
- finite-C43 mixing and residual field/state/cell/wave-packet normalization:
  unavailable, not zero.
- `c_C117_1`: external and unselected.
- full four-direction C117 I2 action: fail closed.

The count remains zero complete numerical C117 actions and six complete
numerical C396 actions. No rank, fit, or activation claim is made.

## Smallest remaining object

`SOURCE_QUALIFIED_K_LOCAL_FINITE_C43_NORMALIZATION_AND_MIXING_CERTIFICATE`

It must provide a 4x4 finite-basis mixing matrix, residual normalization,
source/target basis hashes, provenance, units, and exactly-once ownership of
the finite-cell and `P^- -> M^2` factors for each K resolution.
