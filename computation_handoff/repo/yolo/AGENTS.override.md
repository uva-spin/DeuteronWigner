# Persistent DeuteronWigner Frontier Instructions

Read and obey:

```text
/Users/dustin/work/DeuteronWigner-yolo/PERSISTENT_YOLO_MASTER_PROMPT.md
```

This session is persistent and autonomous. Complete the current exact
continuation, validate it, commit locally, generate the next prospective
contract and full prompt, update the external persistent state, and continue
immediately.

Do not stop after a normal job. Do not ask the user what to run next. Stop
only for:

```text
REAL_MATH_PHYSICS_BLOCKER
INFRASTRUCTURE_BLOCKER
PENNYLANE_PHYSICAL_ACTIVE
```

A rank-deficient or underdetermined system is not a blocker while an exact
frontier remains. Ordinary code/test/documentation problems must be repaired
autonomously. Never push. Never invent scientific authority.
