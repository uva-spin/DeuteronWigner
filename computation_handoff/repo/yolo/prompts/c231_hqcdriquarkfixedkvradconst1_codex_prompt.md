# C231/HQCDRIQUARKFIXEDKVRADCONST1 Codex Work Package

## Mission

Derive computable positive-domain polynomial-growth constants and exact
critical-point splits for the eight source-bound C230 radial factors. These
are the missing prerequisites for directed core integration and analytic
Gaussian-tail bounds. Validate, commit locally, publish one successor
contract/prompt, atomically advance state, and continue. Never push.

## Frozen authority

```text
baseline: 9d1978006c03af4d09bc1a7649370d8ca70ee960
C230 root: 19c962b163d744e3868c050d0cc722c6b9087d2149c535bc03a6720c057c030f
C230 status: C230_EIGHT_EXACT_RADIAL_FACTORS_SOURCE_BOUND_CERTIFIED_GROWTH_CONSTANTS_AND_CORE_TAIL_BOUNDS_INCOMPLETE
C230 plan: RIQUARKFIXEDKVRADBOUND1-D
contract: docs/next_level/c230_c231_hqcdriquarkfixedkvradconst1_continuation_contract.json
contract SHA-256: 5fb84caaf84c5c25b640fc77e108935a8e6c1c5433c9f2e495ac5dabef7ce1c8
object: C168-REQUEST-1-OMITTED-INTERFACE-OUTSIDE-FIXED-K-V1-RADIAL-GROWTH-CONSTANTS
exact object: computable positive-domain polynomial-growth constants and critical-point splits for the eight exact C230 radial factors
```

Read C43/C45/C50-C53 and C225-C230 through committed public APIs and verify
all roots and hashes. Freeze protected paths, C134 quarantine, inherited C157
boundaries, C166 graphs, later C168 requests, counterterm/null directions,
and Q0/Q1/Q2. Preserve `x_q,x_g,P+>0`, `x_q+x_g=1`, `m_q>=0`, `b_HO>0`,
caller `n>=0`, signed `m`, helicities, branches, units, and orientations.

Choose exactly one plan:

```text
RIQUARKFIXEDKVRADCONST1-A  computable constants and critical splits complete for eight
RIQUARKFIXEDKVRADCONST1-B  rigorous common compact-capsule constants complete for eight
RIQUARKFIXEDKVRADCONST1-C  exact source-qualified subset complete with explicit remainder
RIQUARKFIXEDKVRADCONST1-D  algebraic degree/critical structure complete, constants incomplete
RIQUARKFIXEDKVRADCONST1-E  prerequisite symbolic normalization incomplete
RIQUARKFIXEDKVRADCONST1-F  certified inconsistency
RIQUARKFIXEDKVRADCONST1-G  regression
```

Reconstruct each C230 primitive only through the authenticated structural
expression or the same source derivation; never use unrestricted eval.
Normalize it as a real positive-branch function of radial `q` after the
matching angular phase is removed. Identify exact numerator/denominator and
radicand degrees, zeros, poles, branch points, and removable points on
`q>=0`. Prove denominator/radicand positivity or split at exact lawful
critical points.

Produce a computable bound `|F_h(q)| <= C_h(1+q)^p_h` on a precisely declared
positive parameter domain. Constants may be symbolic expressions or
algorithms with directed arithmetic, but must be finite, reproducible, and
monotone under domain enlargement. If the full open positive domain has no
uniform finite constant, define an exact caller-supplied compact capsule with
strict lower/upper parameter bounds and preserve its excluded boundary as
unavailable. Separate primitive growth from the explicit HO polynomial and
Laguerre factors.

Check two independent routes: direct algebraic degree/positivity analysis and
a source-expression interval or asymptotic majorant. Validate endpoint and
infinity behavior, critical-point completeness, dimensions, adjoint pairing,
helicity separation, domain monotonicity, deterministic roots, restart,
sharding, and query order. Floating sampling is holdout-only and cannot prove
positivity or select constants.

Implement preferably under
`src/deuteron_wigner/bridge/hqcdriquarkfixedkvradconst1/` with immutable safe
loaders and public normalization, critical-set, growth-bound, route,
residual, release, handoff, isolation, mutation, completeness, and verified
authority APIs. No eval/pickle/callbacks, fitted constants, thresholds,
averaging, extrapolation, physical defaults, missing-as-zero, dense omitted
spaces, retained-ID reuse, graph changes, quantum changes, or push.

Generate deterministic `c231_*` evidence, two clean builds, authoritative
regressions, and at least 384 live mutations. Create exactly one local commit
and one prospective successor contract plus one complete prompt. Hash them,
atomically advance `AUTOPILOT_STATE.json`, and continue immediately. Stop only
for a schema-certified `REAL_MATH_PHYSICS_BLOCKER`, exceptional
`INFRASTRUCTURE_BLOCKER`, or committed `PENNYLANE_PHYSICAL_ACTIVE`.
