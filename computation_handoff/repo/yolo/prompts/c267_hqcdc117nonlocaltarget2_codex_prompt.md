# C267/HQCDC117NONLOCALTARGET2 Codex Work Package

## Mission

Evaluate the four C266 source-derived instantaneous-current/packet functionals, their Ward/Slavnov–Taylor descendants, the standard physical matching residuals, and correlated packet/scale/regulator uncertainty. Publish exact parameterized results when the C266 symbolic packet coordinates are not fixed by authenticated authority.

Do not select finite C43/C117 subtraction coefficients in C267.

## Baseline and provenance

Start from the C266 completion commit recorded in `AUTOPILOT_STATE.json`. The frozen C266 package root is:

```text
945937e5ca5f64fde86c985a5c7b0fd1343424199846abfb9d963022b6333950
```

The sole prospective authority is:

```text
docs/next_level/c266_c267_hqcdc117nonlocaltarget2_continuation_contract.json
```

Hash-verify that contract and read its complete C266 ancestry: C266 current kernel, packet programs and capsules; C265 target audit and 20-field schema; C264 nonlocal functionals and full-rank conditions; C263 locality classification; C254 Abel topology; C114–C117 source APIs; authenticated C43 source objects; and the applicable C203–C212 BRST/ST records. Preserve every committed root separately.

## Frozen completed authority

C266 supplies the exact light-front instantaneous-current decomposition

```text
j_a^+ = J_q,a^+ + J_g,a^+
H_inst = -g_s^2/2 integral dx dy j_a^+(x) K_Q0(x-y) j_a^+(y)
```

with `J_q`, `J_g`, the Q0 inverse-partial-plus prescription, ordered current products, and four algebraic normalized compact-support bump/transverse-Gaussian packet families. Packet center, longitudinal width, transverse width, transverse center, scale, flavor, boundary, link, and holonomy data remain explicit coordinates unless authenticated authority fixes them. C266 did not evaluate targets, choose a physical packet, or select any finite coefficient.

## Strict claim boundary and plans

Select exactly one mutually exclusive plan:

```text
C117NONLOCALTARGET2-A  all four parameterized target, Ward/ST, matching, and uncertainty records close executably
C117NONLOCALTARGET2-B  exact packet/current integrals close but a standard physical matching input remains absent
C117NONLOCALTARGET2-C  a current renormalization or Ward/ST descendant required for evaluation remains absent
C117NONLOCALTARGET2-D  evaluation is executable but authenticated packet-parameter/physical-condition authority is required next
C117NONLOCALTARGET2-E  the required target map is proven nonexistent
C117NONLOCALTARGET2-F  independent source-qualified evaluation routes contradict
```

A–D are lawful positive continuations. E/F require the master-prompt blocker protocol. A parameterized target is a closed C267 object when every dependence and domain is explicit; it is not permission to fabricate numerical widths, scales, regulators, or physical values.

## Scientific construction

For each direction in the exact order

```text
I2_density_projector
derivative_density
CM_ground
triplet_projected
```

publish an immutable evaluation capsule containing:

- the C266 current and packet roots, explicit integration measure and conservation distributions;
- separate `J_qJ_q`, `J_qJ_g`, `J_gJ_q`, and `J_gJ_g` contributions with ordered color and spin/helicity tensors;
- the exact packet normalization and finite-HO coefficient program at K9, K11, and K13;
- the regulated functional value as an algebraic expression or deterministic quadrature program over the explicit packet coordinates;
- Abel pair-first ordering, Q0 prescription, regulator-removal domain, boundary/link/holonomy class, scale, flavor, and scheme;
- the projected Ward/ST descendant and a diagnostic residual which preserves unavailable terms as unavailable rather than zero;
- the standard/physical matching equation and residual, with source-side and standard-side availability distinguished;
- a correlated covariance/uncertainty program covering packet coordinates, scale variation, regulator removal, quadrature/HO truncation, and source/scheme terms without double counting;
- source ancestry, units, orientation, physical/nonphysical status, and object roots.

Keep CM/color channels, source/sink orientation, flavor ordering, K9/K11/K13, boundaries, links, holonomies, and regulator limits separate. No averaging or representative choice may erase a direction.

## Independent routes and checks

Use two source-qualified routes:

1. direct pairing of the C266 continuum `J_q/J_g/K_Q0` kernel with each normalized packet and C264 functional;
2. finite-HO spectral reconstruction using the C266 coefficient integrals and the authenticated C114–C117 matrix-element representation.

Prove route agreement over the common symbolic domain, or publish the smallest exact residual. Check momentum/color/helicity closure, Hermitian reversal, packet normalization, distributional pairing legality, pair-first Abel order, regulator-limit ordering, Ward/ST ancestry, K-separated reconstruction, and covariance symmetry/positive-semidefinite semantics. Do not infer numerical convergence from a symbolic program.

## Public API and roots

Provide a safe-loading `deuteron_wigner.bridge.hqcdc117nonlocaltarget2` API with immutable records for target evaluations, current-product decomposition, Ward/ST diagnostics, physical matching residuals, correlated uncertainty, two-route derivation, residual frontier, release status, isolation, live mutations, verified loading, and package roots. Keep input, target, Ward/ST, matching, uncertainty, route, residual, release, and scope roots distinct. Runtime artifacts must be JSON-only with `allow_pickle=false`.

## Validation

Run C250–C266 adjacent tests plus focused C267 tests and at least 384 live mutations. Validate source/root hashes, four-direction completeness, symbolic/numerical executability as claimed, ordered products, conservation and indices, Hermiticity, gauge/BRST/ST scope, normalization/support, Abel order, CM/color leakage, K separation, units/orientations/boundaries/links/holonomies, covariance semantics, safe loading, restart/sharding/paging/query-order stability, two clean deterministic builds, protected paths, C166 nonmutation, Q0/Q1/Q2 nonmutation, and no push.

## Isolation and nonclaims

Do not mutate C166 graphs, Q0/Q1/Q2, quarantined C134/C157 evidence, or unrelated user work. Do not claim physical activation, a numerical continuum target, regulator independence beyond the proved domain, standard matching when its side is absent, or fixed C43/C117 coefficients. Unsupported quantities remain explicit, conditional, or unavailable—never zero.

## Continuation and final response

After validation, create one local C267 completion commit and never push. Select the smallest exact next object from the committed residual frontier: coefficient solution only if four full-rank authenticated target conditions are executable; otherwise the missing standard-side matching input, current-renormalization/Ward object, packet-parameter prescription, or residual evaluation object. Generate exactly one prospective continuation contract and one complete prompt, atomically advance `AUTOPILOT_STATE.json`, and continue immediately.

Report the selected plan, exact target/diagnostic closure, parameterized versus unavailable boundaries, tests and mutations, package root, local commit, next job/object, and explicit no-push/nonactivation status.
