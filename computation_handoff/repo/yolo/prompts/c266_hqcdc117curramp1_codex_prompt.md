# C266/HQCDC117CURRAMP1 Codex Work Package

## Mission

Derive and publish executable continuum instantaneous-current kernels plus
explicit normalized packet coefficient functions for all four C264 nonlocal
matching functionals. Fill the complete C265 20-field current/packet capsule
schema from authenticated C43/C114–C117 action and source authority.

Do not evaluate target values or solve C117 coefficients in C266.

## Baseline

Start from the C265 completion commit recorded in AUTOPILOT_STATE.json. Frozen
C265 package root:

```text
cfc5ae1080cab2071a80f4e170a8bdc5968f24afce8f4deee3e58c399b81fa64
```

Read and hash-verify the C265→C266 contract, all C265 audit/schema records,
C264 packet/functionals, C263 locality classification, C254 Abel topology,
C114–C117 source APIs, authenticated C43 source archives, and relevant
light-front current/Ward primary sources.

## Boundary and plans

Preserve nonlocal/distributional semantics, the exact four direction order,
C264 packets/functionals, pair-first Abel order, CM/color channels,
K9/K11/K13 separation, orientations, units, boundaries/links/holonomies,
source/color/flavor ordering, C166, and Q0/Q1/Q2. Never push.

Select exactly one:

```text
C117CURRAMP1-A  all four executable current/packet capsules closed
C117CURRAMP1-B  current kernels closed; packet parameter authority remains
C117CURRAMP1-C  packets closed; continuum current source derivation remains
C117CURRAMP1-D  one direction requires a distinct nonlocal source representation
C117CURRAMP1-E  required source representation is proven nonexistent
C117CURRAMP1-F  independent derivations contradict
```

B/C/D are positive continuations; E/F require blocker certification.

## Construction

For each direction publish exact current source ID, continuum kernel
expression, momentum measure/conservation, spin/helicity and ordered color
tensors, gauge/BRST and renormalization conventions, scale/flavor labels,
packet coefficient function with support/width parameters and normalization
integral, boundary/link/holonomy and Abel data, Ward/ST descendant, physical
matching equation, and roots.

Use two derivations: direct C43 constraint-current Fourier/preimage route and
inverse C114–C117 finite-basis spectral reconstruction. Keep regulator kernels
and external projectors explicit. Define packet functions algebraically with
all parameters; a named smooth/Schwartz class is insufficient.

Prove momentum/color/helicity closure, Hermitian reversal, packet
normalization, distributional pairing legality, finite-HO expansion and
K-separated reconstruction. Do not choose packet widths or scales without a
scheme rule; if their authority is missing, publish the exact parameterized
family and select the smallest continuation.

## API, validation, continuation

Publish immutable safe-loading capsules, kernels, packet programs,
normalization proofs, ancestry, Ward/ST and physical-match interfaces,
residual/release/scope records with separate roots.

Run C250-C265 adjacent tests and C266 tests; two derivations; symbolic
executability; source hashes; index/momentum closure; Hermiticity; BRST/ST;
packet normalization/support; Abel order; CM/color leakage; K separation;
units/orientations/boundaries/holonomies; safe loading; restart/sharding/
paging/query order; two clean builds; protected paths; and 384 mutations.

Create one local commit, never push, generate exactly one next contract and
full prompt, atomically advance state, and continue. Closed capsules return to
target evaluation; partial closure gets the smallest exact continuation. Stop
only for a certified real blocker, exceptional infrastructure blocker, or
PENNYLANE_PHYSICAL_ACTIVE.
