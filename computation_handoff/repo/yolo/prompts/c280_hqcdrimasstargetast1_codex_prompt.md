# C280/HQCDRIMASSTARGETAST1 Codex Work Package

## Mission

Construct or rigorously reduce the source-qualified executable RI/SMOM continuum signed-quark-mass projected coefficient AST and numerical enclosure required by C279.

## Baseline and authority

Start from the C279 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C279 package root:

```text
b2c27f7886c7210bc3c6eeae19583ec596948bd505c681183a33272225574830
```

The sole prospective contract is `docs/next_level/c279_c280_hqcdrimasstargetast1_continuation_contract.json`; hash-verify it. Audit C153 matching conventions; C157 common-IR validator; C158 finite-basis evaluator; C164–C166 authenticated locators and dependency graphs, especially arXiv:0901.2599 equations (11), (20), and (24); C276–C279 signed-mass conventions; and exact authenticated source artifacts in repository provenance.

## Boundary and plans

C279 proves the finite-basis route is executable while the continuum target remains a symbolic label. Select exactly one:

```text
RIMASSTARGETAST1-A  complete executable target AST and enclosure close
RIMASSTARGETAST1-B  maximal AST closes with one named dependency symbolic
RIMASSTARGETAST1-C  a narrower source equation/definition/branch is required
RIMASSTARGETAST1-D  lawful scheme, gauge, or conversion branches remain separate
RIMASSTARGETAST1-E  indispensable source authority is proven unavailable
RIMASSTARGETAST1-F  authenticated source definitions contradict
```

A–D are positive continuations; E/F require a newly schema-certified blocker. Do not transcribe from memory, invent coefficients, fit plots, reuse diagnostic fixtures as targets, or recalculate C158.

## Construction

Bind exact source hashes and equation locators, scalar/pseudoscalar Ward convention, signed-mass orientation, symmetric nonexceptional momenta, projector normalization, gauge, active `N_f`, scale and coupling coordinates, conversion direction, branch/special-function definitions, perturbative order, units and uncertainty. Encode only allowlisted immutable AST operations with no eval, callbacks, pickle, dynamic imports, or hidden defaults. Provide direct formula, Ward-identity, inverse-conversion and independent numerical routes with outward enclosures.

Demonstrate compatibility with C149/C153/C157/C164/C276 and preserve rho/mu independence, K separation, flavor separation, explicit C117 coordinates, and common-IR subtraction order. If a dependency remains unavailable, isolate its exact source object rather than filling it numerically.

## API, validation, continuation

Publish immutable safe-loading source, convention, AST schema/program, evaluator, enclosure, route, residual, release, mutation and isolation APIs with JSON-only runtime. Run C250–C279 plus focused C280 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order checks, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

After exactly one local commit, create exactly one next prospective contract and full prompt and atomically advance `AUTOPILOT_STATE.json`. If the target AST closes, return to C157 common-IR records; otherwise continue with the smallest exact missing source dependency.

Report plan, AST closure boundary, tests/mutations, root, commit, next job/object, and no-push/nonactivation status.
