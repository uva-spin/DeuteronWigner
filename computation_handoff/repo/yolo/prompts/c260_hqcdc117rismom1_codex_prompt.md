# C260/HQCDC117RISMOM1 Codex Work Package

## Mission

Implement the exact executable continuum authority for the C259-selected
`PROJECT_C117_RI_SMOM_V1` scheme. Freeze the four C117 directions, define the
symmetric nonexceptional subtraction point and Landau-gauge conventions,
construct four exact dual projectors and the tree target matrix, prove rank
four, close the declared mixing/evanescent basis, and bind the conversion
boundary to MSbar and later physical matching.

Do not calculate or select any finite-C43 C117 coefficient in C260.

## Baseline and provenance

Start from the local C259 completion commit recorded in AUTOPILOT_STATE.json.
The frozen C259 package root is:

```text
3316addcea31e845dc16f57f0e921a6409b87c2764635f261e8dff3454cd62ed
```

Read completely and hash-verify:

```text
docs/next_level/c259_c260_hqcdc117rismom1_continuation_contract.json
docs/next_level/c259_operator_basis_manifest.json
docs/next_level/c259_literature_corpus_manifest.json
docs/next_level/c259_response_diagnostics_manifest.json
docs/next_level/c259_candidate_schemes_manifest.json
docs/next_level/c259_adapter_plan_manifest.json
docs/next_level/c259_release_manifest.json
data/runtime/c259_hqcdc117renormdesign1/manifest.json
```

The C258 blocker remains historical evidence. The human-authorized recovery
policy and C259 prospective contract are the authority for this continuation.

## Frozen completed authority

Preserve separately:

```text
C117 direction order:
I2_density_projector
derivative_density
CM_ground
triplet_projected

scheme label:
PROJECT_C117_RI_SMOM_V1

gauge:
Landau

subtraction kinematics:
p1^2 = p2^2 = (p1-p2)^2 = mu^2, Euclidean and nonexceptional

resolutions:
K9_2_N8_b0.40
K11_2_N8_b0.40
K13_2_N8_b0.40
```

Preserve all C43/C117 source ownership, P0/Q0, boundary/link/holonomy,
orientation, units, graph count-once, Abel-regulator order, C166 graphs, and
Q0/Q1/Q2 roots. Never push.

## Strict claim boundary

C260 may define intermediate-scheme tree targets, including a zero unwanted
mixing projection when that zero follows from the explicit scheme definition.
It must not call those targets physical or infer them from the bare action.

C260 creates no numerical C117 subtraction coefficient, physical target,
finite-C43 solve, resolution average, physical Hamiltonian, PennyLane
activation, spectrum, TMD, or production object.

## Mutually exclusive plans

Select exactly one:

```text
C117RISMOM1-A  executable rank-four PROJECT_C117_RI_SMOM_V1 authority ready
C117RISMOM1-B  mixing or evanescent basis requires an exact additional design job
C117RISMOM1-C  dual projector construction is rank deficient after lawful refinement
C117RISMOM1-D  symmetric kinematics or conversion boundary contradicts source authority
C117RISMOM1-E  no standard/physical matching path remains
```

A software failure or one deficient projector family is not a blocker.

## Scientific construction

Publish an exact operator-basis record for all four C117 directions including
field, spin/helicity, ordered color, longitudinal distribution, transverse-HO
ancestry, dimension, coupling order, Hermitian partner, BRST/ST scope,
EOM/total-derivative status, mixing partners, and source roots.

Define external legs, flavor routing, source/sink order, off-shellness, momentum
orientation, scale, massless limit, and Landau gauge. Prove all channels are
nonexceptional and distinguish Euclidean scheme coordinates from later
light-front finite-cell coordinates.

Construct the exact Gram matrix and dual projectors. Publish symbolic tree
response matrix, determinant/rank, singular values, condition number, left and
right nullspaces, symmetry blocks, normalization, and two independent rank
routes. Route A must directly contract operators and projectors. Route B must
use exact Gram inversion/duality and reverse the basis ordering.

Define the tree target vector/matrix as scheme data. Separate tree target,
continuum perturbative target, Ward/ST target, nonperturbative target, and
physical observable target. Unknown coefficients remain unavailable, never
zero.

Close the mixing basis at the declared order. State flavor/color channels,
gauge-variant or BRST-exact/EOM partners, evanescent basis and gamma5
convention, and what C261 must calculate. If closure cannot be established,
select plan B with the smallest exact missing partner rather than silently
dropping it.

Bind a standard conversion boundary: RI/SMOM mixing matrix to MSbar in the
same operator/evanescent convention, continuum step scaling into a perturbative
window, threshold/flavor labels, and later physical matching. Do not invent a
conversion coefficient in C260.

Specify the C262 finite-C43 adapter interface: Euclidean-to-light-front
coordinates, normalized finite-cell wavepackets, continuum-to-HO overlaps,
operator/source normalization, Abel topology, boundaries/links/holonomies,
and separate K9/K11/K13 matrices.

## Public API and roots

Provide immutable, safe-loading public records for:

```text
operator_basis
symmetric_kinematics
projector_basis
tree_response_matrix
tree_target_definition
mixing_and_evanescent_convention
conversion_boundary
finite_C43_adapter_interface
scheme_variation_holdouts
release_manifest
```

Hash every source and derived record. Keep input, basis, kinematics, projector,
target, mixing, conversion, adapter, release, scope, and package roots separate.
Safe loading must reject stale runtime roots and must not use pickle.

## Validation

Run all C250-C259 adjacent tests plus C260 tests; two independent operator and
projector classifications; direct and Gram-dual rank routes; basis-order
reversal; source-archive hash checks; kinematic nonexceptionality; gauge,
flavor, color, BRST/ST, evanescent, unit, orientation, boundary, holonomy and
count-once checks; K9/K11/K13 separation; safe loading; restart, paging,
sharding and query-order stability; two deterministic clean builds; protected
path checks; and at least 384 focused live mutations.

Repair ordinary failures autonomously. Create exactly one local completion
commit and never push.

## Readiness and continuation

Positive C260 status is `C260_PROJECT_C117_RI_SMOM_V1_EXECUTABLE_AUTHORITY_READY`.
Activation remains `NOT_READY`.

If positive, create exactly one continuation for
`C261/HQCDC117CONTTARGET1`, whose first object is the continuum projected target
amplitudes, RI/SMOM-to-MSbar conversion matrix, running/step-scaling authority,
and perturbative/scheme uncertainty in the exact C260 conventions. Do not jump
to the finite-C43 coefficient solve.

Commit locally, generate one full next prompt, atomically update the external
AUTOPILOT_STATE.json, and continue immediately. Stop only for a newly
schema-certified real mathematical/physical blocker, exceptional infrastructure
blocker, or PENNYLANE_PHYSICAL_ACTIVE.
