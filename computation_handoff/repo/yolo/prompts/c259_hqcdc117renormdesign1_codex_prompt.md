# C259/HQCDC117RENORMDESIGN1 Codex Work Package

## Title

**Human-authorized literature-backed renormalization design for the four C117
instantaneous-current complement subtraction directions: exact operator and
projector classification, primary-source NPR/light-front literature corpus,
candidate RI/SMOM, Ward/ST-hybrid, GIRS, and gradient-flow scheme construction,
full-rank response-matrix and conditioning tests, standard-scheme matching
path, and fail-closed selection of the first executable C117 renormalization
scheme**

## Authority and baseline

This is a new human-authorized prospective continuation from the terminal C258
blocker. It is not a claim that the missing target was already present.

Start from:

```text
baseline commit:
ad81b2d61c97c9eb4624189df2713193673a4580

C258 package root:
b690bad5e4e47c39e5a9c482c26d673ac6eb6a5531df7806664d35c71098e897

C258 blocker:
ABSENT_INDISPENSABLE_AUTHORITY

missing object:
authenticated renormalization target capsule fixing the four C117
instantaneous-current complement subtraction coefficients
```

Read and hash-verify:

```text
the C254-C258 implementation reports;
REAL_BLOCKER.json;
the four C117 coefficient/operator records;
C251 projector programs;
C252 finite-core evaluators;
C253 tail classification;
C254 regulator/topology records;
C255 condition/rank records;
C256 target-capsule schema;
C257 discovery and derivability audit;
C258 release/readiness/blocker records.
```

Preserve every prior authority and no-push/nonmutation boundary.

## Human authorization

The project authorizes a broad primary-source literature study and the
definition of an explicitly labeled project intermediate renormalization
scheme.

The bare action is not claimed to determine the finite parts.

A project scheme is lawful only when:

```text
four independent conditions exist;
the exact C117 operator basis is closed;
the conditions preserve authenticated symmetries and ownership;
the finite-C43 adapter is explicit;
a conversion or matching path to a standard or physical scheme exists;
scheme dependence is retained as a systematic;
and no unknown coefficient is set to zero for convenience.
```

## Mission

Determine whether the C117 blocker can be replaced by a complete
renormalization-design authority.

Do not calculate or select the four physical coefficients in C259.

## Phase 1 — exact four-direction classification

For each C117 direction publish:

```text
operator ID;
coefficient ID;
source owner;
graph-local role;
field content;
current component;
color tensor;
spin/helicity tensor;
longitudinal distribution;
transverse-HO structure;
mass dimension;
coupling order;
Hermitian partner;
P0/Q0/boundary support;
BRST/ST transformation;
EOM/total-derivative/exact/redundant status;
mixing partners;
projector IDs;
activation-relevant Hamiltonian matrix elements;
and source roots.
```

Prove that the basis is exactly four-dimensional at the declared scope.

Do not infer independence from the number of stored coefficients.

## Phase 2 — projector audit

Read the four C251 projectors and construct the exact response matrix:

\[
M_{ia}^{(K)} =
\mathcal P_i \Gamma[\Delta H_a^{\mathrm{C117}}].
\]

Publish symbolic/tree response matrices for K9/K11/K13 and candidate continuum
coordinates.

Report:

```text
rank;
singular values;
condition number;
left/right nullspaces;
symmetry blocks;
and projector redundancies.
```

If the existing projectors do not give rank four, construct candidate dual
projectors from the exact operator Gram matrix. Do not alter the operator basis
silently.

## Phase 3 — primary-source literature corpus

Perform a broad literature search, restricted to primary sources and official
repositories.

At minimum authenticate exact versions and source files for:

```text
hep-lat/9411010;
0901.2599;
hep-lat/9902030;
1104.4948;
1109.1223;
2406.08065;
2310.18059;
hep-th/0510230;
0801.4507;
hep-th/0002062;
0805.0707;
hep-th/9407056.
```

Prefer official TeX or machine-readable source. Hash all acquired bytes.
Publish exact equation/source locators relevant to:

```text
matrix projector renormalization;
symmetric nonexceptional kinematics;
finite subtraction coefficients;
Ward-identity equivalence;
operator mixing;
conversion matrices;
step scaling;
gauge-invariant schemes;
Fock-sector-dependent counterterms;
light-front gauge-restoring counterterms;
and instantaneous-current Ward identities.
```

Secondary sources may be used only to locate the primary source and may not
supply authority.

## Phase 4 — candidate schemes

Construct at least these candidates:

### Candidate A — PROJECT_C117_RI_SMOM_V1

Specify:

```text
gauge;
symmetric nonexceptional external kinematics;
renormalization scale;
mass/flavor limit;
external source and sink states;
four exact projectors;
tree target matrix;
operator-mixing basis;
evanescent convention;
conversion target;
and finite-C43 wavepacket adapter requirements.
```

### Candidate B — PROJECT_C117_WARD_RISMOM_V1

Use every exact independent Ward/BRST/ST descendant available for the C117
current and supplement only the remaining rank with RI/SMOM projector
conditions.

### Candidate C — PROJECT_C117_GIRS_V1

Construct a gauge-invariant coordinate-space candidate using separated
operator insertions and a conversion path to MSbar, only when the C117
operator basis admits a source-faithful coordinate-space representation.

### Candidate D — gradient-flow holdout

Classify whether a flowed operator basis and short-flow-time matching can
supply a viable independent check.

Do not choose a candidate by familiarity. Score each by:

```text
rank four;
conditioning;
IR safety;
gauge dependence;
operator-mixing closure;
finite-C43 adaptability;
conversion availability;
step-scaling availability;
computational cost;
and activation relevance.
```

## Phase 5 — target-vector semantics

For every candidate distinguish:

```text
scheme-defining tree target;
continuum perturbative target;
nonperturbative lattice target;
Ward/ST target;
and physical observable target.
```

A target value of zero requires an exact scheme or symmetry reason.

Do not call intermediate-scheme targets physical.

## Phase 6 — finite-C43 matching architecture

Publish the planned affine system:

\[
M^{(K)}(\mu,\mathcal S)c^{(K)}
=
t^{\mathcal S}(\mu)-r^{(K)}(\mu,\mathcal S)
\]

for K9/K11/K13.

Define every required adapter:

```text
Euclidean/continuum to light-front coordinates;
off-shell momentum to finite-cell wavepacket;
continuum transverse momentum to HO basis;
operator/source normalization;
gauge and BRST convention;
Abel-regulator topology;
boundary/link/holonomy scope;
and standard-scheme conversion.
```

Do not evaluate coefficients yet.

## Phase 7 — plan selection

Select exactly one:

```text
C117RENORMDESIGN1-A:
PROJECT_C117_RI_SMOM_V1 full-rank design ready;

C117RENORMDESIGN1-B:
Ward/RI-SMOM hybrid full-rank design ready;

C117RENORMDESIGN1-C:
GIRS full-rank design ready;

C117RENORMDESIGN1-D:
operator basis or mixing closure incomplete;

C117RENORMDESIGN1-E:
projector duality/rank incomplete;

C117RENORMDESIGN1-F:
no candidate has a standard/physical matching path;

C117RENORMDESIGN1-G:
source-qualified candidate schemes contradict;

C117RENORMDESIGN1-H:
no full-rank lawful scheme exists.
```

Rank deficiency in one candidate is not a blocker until all candidates and
lawful projector refinements have been exhausted.

## Required positive result

A positive C259 must provide:

```text
an exact four-direction operator basis;
a full-rank candidate scheme;
a complete four-condition definition;
a named scale/gauge/kinematic prescription;
a standard-scheme conversion plan;
a finite-C43 adapter plan;
K9/K11/K13 conditioning diagnostics;
scheme-variation holdouts;
and exactly one executable continuation.
```

## Next continuation

When RI/SMOM is selected, create:

```text
C260/HQCDC117RISMOM1
```

to implement the exact scheme, continuum basis, target matrix, kinematics,
and conversion-boundary authority.

Use an exact scheme-specific alternative only when selected by C259.

Do not jump directly to coefficient solving.

## Validation

Run:

```text
all C250-C258 adjacent tests;
operator/projector source-root verification;
two independent basis classifications;
two independent projector-rank routes;
literature-source hash and locator checks;
candidate-scheme route reversal;
K9/K11/K13 separation;
safe loading;
two deterministic clean builds;
and at least 384 focused mutations.
```

Create one local completion commit. Do not push.

## Nonclaims

C259 creates no:

```text
numerical C117 coefficient;
physical target value;
physical Hamiltonian;
counterterm/null representative;
resolution average;
continuum extrapolation;
PennyLane activation;
state;
spectrum;
TMD;
or production object.
```
