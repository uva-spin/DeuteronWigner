# C230/HQCDRIQUARKFIXEDKVRADBOUND1 Codex Work Package

## Mission

Construct the explicit radial factorization and a certified core-plus-tail
enclosure program for all eight convergent C228 matching-channel radial HO
integrals. Preserve full symbolic dependence on `x_q,x_g`, `P+`, `m_q`,
`b_HO`, and radial index. Validate, commit locally, publish one successor
contract/prompt, atomically advance state, and continue. Never push.

## Authority and provenance

```text
baseline: 3a80a3d874ca578cb82f4ebd22a3bdd34d6c9dcd
C229 root: d72b1879de53cc79fd915f9d88930858bd0a170891c36f9d3ce5a21696a6bf84
C229 status: C229_C228_EIGHT_RADIAL_FAMILIES_ABSOLUTE_CONVERGENCE_READY_EXPLICIT_FACTOR_AND_TAIL_ENCLOSURE_INCOMPLETE
C229 plan: RIQUARKFIXEDKVRAD1-E
contract: docs/next_level/c229_c230_hqcdriquarkfixedkvradbound1_continuation_contract.json
contract SHA-256: 0890ae2479eaab2640c17679f5075fc3d67bfca2fb767e8d9a79def1e2e976fe
object: C168-REQUEST-1-OMITTED-INTERFACE-OUTSIDE-FIXED-K-V1-RADIAL-BOUND
exact object: explicit radial factorization and certified core-plus-tail enclosure program for the eight C228 matching HO integrals
```

Read C43/C45/C50-C53 and C225-C229 through their committed public APIs and
evidence. Verify every named root and hash. Freeze all completed authority,
protected paths, C134 quarantine, inherited C157 boundaries, C166 graphs,
later C168 requests, counterterm/null directions, and Q0/Q1/Q2. Keep every
parameter, positive branch, helicity, `n,m`, orientation, normalization, and
unit explicit. The C229 convergence certificates authorize construction of
bounds; they do not authorize physical values or treating unavailable
integrals as zero.

## Strict claim boundary and plans

Choose exactly one mutually exclusive plan:

```text
RIQUARKFIXEDKVRADBOUND1-A  all eight explicit factors and certified enclosures complete
RIQUARKFIXEDKVRADBOUND1-B  rigorous common parametric enclosure complete for all eight
RIQUARKFIXEDKVRADBOUND1-C  a strict source-qualified subset complete with exact remainder
RIQUARKFIXEDKVRADBOUND1-D  factorization complete but certified bounds incomplete
RIQUARKFIXEDKVRADBOUND1-E  growth/tail constants remain incomplete
RIQUARKFIXEDKVRADBOUND1-F  certified scientific inconsistency
RIQUARKFIXEDKVRADBOUND1-G  regression/no new authority
```

Plans F or a blocker require the master prompt's full independent scientific
and provenance audits. Rank deficiency, difficult symbolic algebra, or a
missing software artifact is not a blocker while an exact continuation
exists.

## Scientific construction

For each of the eight C228 radial IDs, derive the canonical positive-branch
radial factor directly from the authenticated C227 normal form and C228 HO
projection. Separate the Laguerre polynomial, Gaussian, radial power,
kinematic prefactor, and remaining algebraic square-root/rational factor.
Specify the positive parameter domain and every possible endpoint or
denominator singularity.

Construct a reproducible enclosure with:

1. an explicit polynomial-growth exponent and computable constant on the
   declared parameter domain;
2. a finite core interval split at all source-derived critical points;
3. outward-directed interval or exact special-function evaluation on the
   core;
4. an analytic Gaussian/Laguerre tail majorant with a caller-selected cutoff;
5. a transparent error allocator combining core and tail bounds;
6. exact propagation of symbolic prefactors and dimensions.

If a uniform symbolic enclosure cannot be certified over the whole positive
domain, narrow only to an exact source-qualified compact parameter capsule
and record the excluded boundary as unavailable, never zero. Numerical
quadrature may be a holdout comparison only; do not promote floating output
without a rigorous directed enclosure.

Provide two independent routes, preferably direct radial algebra plus a
Mellin/Laplace or interval-majorant derivation. Check route parity,
adjoint/Hermiticity pairing, dimensions, origin behavior, large-`q`
asymptotics, cutoff monotonicity, error-budget monotonicity, radial-index and
helicity separation, and deterministic serialization.

## Public package and validation

Implement preferably under
`src/deuteron_wigner/bridge/hqcdriquarkfixedkvradbound1/`. Expose immutable
metadata and safe loaders; factor, bound, route, residual, release, handoff,
isolation, mutation, completeness, and verified-authority APIs. Keep input,
derived, enclosure, package, and continuation roots separate. No eval,
pickle, callbacks, fitted constants, thresholds, averaging, extrapolation,
dense omitted-space allocation, reused retained IDs, graph mutation, or
quantum mutation.

Generate deterministic `c230_*` evidence including provenance, plan,
factorization, bound certificates, route checks, package roots, two clean
builds, restart/shard/query-order stability, safe loading, protected-path and
user-worktree preservation, regression/readiness reports, release, residual,
and exactly one prospective continuation. Run authoritative upstream and
focused tests plus at least 384 live mutations. Use numerical quadrature only
as an independent holdout and state its non-authoritative role.

Create exactly one local completion commit. Never push. Then select the exact
next object from the committed residual frontier, write exactly one
prospective continuation contract and one full source-authority prompt, hash
them, atomically increment `AUTOPILOT_STATE.json`, and continue immediately.
Stop only for a schema-certified `REAL_MATH_PHYSICS_BLOCKER`, an exceptional
`INFRASTRUCTURE_BLOCKER`, or committed `PENNYLANE_PHYSICAL_ACTIVE`.
