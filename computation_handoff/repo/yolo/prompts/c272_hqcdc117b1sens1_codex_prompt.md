# C272/HQCDC117B1SENS1 Codex Work Package

## Mission

Evaluate the four derivatives of the normalized tensor-DIS `b1` packet functional with respect to the C117 finite subtraction coordinates and certify the rank of those rows together with the three C269 electromagnetic conditions.

## Baseline and authority

Start from the C271 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C271 package root:

```text
7a55d193904b3c19d7c29d61d0abcce421801b4e6852349af2aa571d69d07cbe
```

The sole prospective contract is `docs/next_level/c271_c272_hqcdc117b1sens1_continuation_contract.json`. Hash-verify it and read C259–C271, C43/C114–C117, C266/C267 packet functionals, repository tensor-DIS code, and the hashed HERMES/1702.05337 sources.

## Boundary and plans

C271 closes the hadron tensor, tensor-density combination, `b1` projector, state normalization, `x/x_D/alpha/y` mapping, flavor weights, factorization labels, packet pairing, and symbolic sensitivities `S_b1,i=dB1_i/dc_i`. It does not supply their values or claim rank four.

Select exactly one:

```text
C117B1SENS1-A  four sensitivities evaluate and combined rank four is certified
C117B1SENS1-B  exact derivative programs close but physical input/eigenstate remains next
C117B1SENS1-C  some operator response sector remains source-incomplete
C117B1SENS1-D  lawful factorization/packet variants require separate rank results
C117B1SENS1-E  rank four is proven impossible
C117B1SENS1-F  independent derivations contradict
```

A–D are positive continuations; E/F require blocker certification. Do not use missing values as zero or select finite coefficients.

## Construction and validation

For every C117 direction publish the differentiated current-current insertion, state/eigenvector response, energy-denominator/contact terms, tensor polarization, packet and regulator pairing, flavor weights, factorization scale, Q0/Abel order, boundary/link/holonomy data, K9/K11/K13 results, uncertainty/covariance, and roots. Use direct differentiated hadron-tensor evaluation and spectral/Feynman–Hellmann reconstruction as independent routes. Certify the combined response rank and conditioning only on their common authenticated domain.

Publish immutable safe-loading sensitivity, rank, route, uncertainty, residual, release, mutation, and scope APIs with separate roots and JSON-only runtime. Run C250–C271 plus focused C272 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order gates, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

Do not claim activation. After one local commit, create exactly one next contract and prompt, atomically advance state, and continue. Advance to four-target coefficient matching only after full-rank authenticated sensitivity and target values; otherwise choose the smallest missing eigenstate, response sector, factorization, or physical-input object.

Report plan, sensitivity/rank result, unavailable boundary, tests/mutations, root, commit, next job/object, and no-push/nonactivation status.
