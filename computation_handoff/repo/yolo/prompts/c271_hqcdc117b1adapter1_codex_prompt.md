# C271/HQCDC117B1ADAPTER1 Codex Work Package

## Mission

Construct the source-faithful tensor-polarized DIS `b1` operator, state-normalization, and kinematic adapter into the four C117 packet-response coordinates. Prove whether the C269 electromagnetic rank-three rows plus the adapted `b1` row have rank four.

## Baseline and authority

Start from the C270 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C270 package root:

```text
69675d52d7c2aa6ac9a88c4ce175cfce6a92d646b52aaedf9644101296119e4d
```

The sole prospective contract is `docs/next_level/c270_c271_hqcdc117b1adapter1_continuation_contract.json`. Hash-verify it and read C259–C270, C43/C114–C117, the C266 packet definitions, the C267 project-side functionals, repository tensor-polarized DIS implementations, and the hashed `1702.05337` and HERMES `hep-ex/0506018` source authorities. Never substitute model memory or a related tensor observable.

## Frozen boundary and plans

C270 authenticates `b1(x,Q2)` as an inelastic tensor-polarized spin-one observable independent in operator and kinematic class from elastic `G_C,G_M,G_Q`. It does not yet authenticate the four-component C117 response row. Preserve the distinction between channel independence and response-matrix rank.

Select exactly one:

```text
C117B1ADAPTER1-A  exact adapter closes and combined C117 response rank is four
C117B1ADAPTER1-B  operator/normalization closes but one response component remains unavailable
C117B1ADAPTER1-C  packet response closes but factorization/current conversion remains next
C117B1ADAPTER1-D  lawful adapter variants remain; publish separate rank diagnostics
C117B1ADAPTER1-E  adapter is proven nonexistent
C117B1ADAPTER1-F  independent source-qualified derivations contradict
```

A–D are positive continuations; E/F require blocker certification. No unavailable response component may be set to zero.

## Construction

Publish the exact DIS hadron tensor, tensor-polarization density combination, `b1` projector, current-product ordering, `x=Q2/(2M_N nu)` versus deuteron `x_D` conversion, active-nucleon fraction convention, state normalization, flavor/charge weights, factorization scheme and scale, packet pairing, Q0/Abel ordering, boundary/link/holonomy data, Ward/ST scope, K9/K11/K13 reconstruction, and uncertainty/covariance inputs. Produce a four-component C117 response row and an exact/symbolic rank certificate with the three C269 electromagnetic rows.

Use two routes: direct hadron-tensor projection paired with C266 packets, and the authenticated tensor-polarized convolution/parton representation mapped through C114–C117. Check their common domain, orientations, units, conservation, Hermitian reversal, distributional legality, packet normalization, regulator order, flavor weights, no double counting, and rank stability.

## API and validation

Publish immutable safe-loading operator, normalization, kinematics, response, rank, route, uncertainty, residual, release, mutation, and scope APIs with separate roots. Runtime artifacts are JSON-only with `allow_pickle=false`. Run C250–C270 plus focused C271 tests, at least 384 live mutations, two deterministic builds, reload/restart/sharding/paging/query-order gates, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

Do not select finite coefficients or claim activation. After one local completion commit, create exactly one prospective contract and prompt, atomically advance state, and continue. Advance to four-target evaluation only if rank four is source-qualified; otherwise select the smallest missing response, conversion, normalization, or factorization object.

Report plan, adapter and rank closure, unavailable boundary, tests/mutations, package root, commit, next job/object, and no-push/nonactivation status.
