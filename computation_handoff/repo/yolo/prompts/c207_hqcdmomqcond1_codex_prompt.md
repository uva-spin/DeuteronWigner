# C207/HQCDMOMQCOND1 Codex Work Package

## Mission

Complete `C197-ST-9`, **target MOMq renormalization conditions**, from the
committed C206 exact affine ST family. Apply only authenticated project target
conditions, determine their rank and effect on the affine family, and publish
the resulting target-matched conditional family without importing physical
inputs or performing a standard-scheme conversion. Validate, commit locally,
create/hash the single successor contract and full prompt, atomically advance
state, and continue immediately. Never push.

## Baseline and provenance

```text
baseline: 671366981ef1a521bbc208b79a3c0ae3daf6f683
completed package: C206/HQCDSTCTSOLVE1
C206 root: b404a853c2c9f63620bf970b4230ef67c59003a73f43de8f51e7aefab0ea371d
C206 status: C206_C205_EXACT_CONDITIONAL_ST_COMPATIBLE_AFFINE_COUNTERTERM_SOLUTION_FAMILY_READY_TARGET_FIXING_EXPLICIT
C206 plan: STCTSOLVE1-B
contract: docs/next_level/c206_c207_hqcdmomqcond1_continuation_contract.json
contract SHA-256: a26e97b0aa7c80f96e0a7d36490d70d31f2f9d6fe5a2eedafbb4d2189f4ba2f8
```

Read the contract and all C197/C198/C206 target-request, scheme, coordinate,
affine-family, relevance, frontier, completeness, readiness, and root records
completely. Resolve exact target definitions, subtraction coordinates,
projectors, field/vertex combinations, units, schemes, and source roots from
committed public authority—not memory.

Preserve `handoff/ROADMAP.md`, protected/untracked paths, C134/C157, C166
graphs, C158 values, Q0/Q1/Q2, and all C164-C206 records. Do not consume a
C158 value unless an exact later physical-input contract authorizes it.

## Scope and mutually exclusive plans

Freeze ST-1 through ST-8 read-only. First object is C197-ST-9, alias
`TARGET_RENORMALIZATION_CONDITION`; C197-ST-10 physical input remains blocked.

Choose exactly one:

```text
MOMQCOND1-A — complete exact project MOMq target-condition family ready;
MOMQCOND1-B — target quotient matched with residual physical-input directions explicit;
MOMQCOND1-C — target condition/source/projector incomplete;
MOMQCOND1-D — subtraction coordinate or kinematic domain incomplete;
MOMQCOND1-E — target-system compatibility incomplete;
MOMQCOND1-F — target rank/solution route mismatch;
MOMQCOND1-G — resolution/scheme/holonomy mismatch;
MOMQCOND1-H — current-chain regression.
```

Keep target finite-basis MOMq conditions separate from standard MSbar/other
scheme conversion, running/threshold transport, and physical inputs. Do not
select a physical coupling, mass, flavor count, scale, holonomy, frame,
gauge-volume normalization, state, or observable.

## Construction

Bind the exact normalized ST-9 record and all aliases, target condition IDs,
subtraction kinematics, nonzero guards, qg/ghost/gluon field and vertex
crosswalks, projectors/tensors, resolutions K9/K11/K13, scheme, holonomy/BC
classes, units, coordinate order, source roots, and affected affine
directions.

Publish strict no-default target parameter records and named nonphysical
fixtures. Reject implicit momentum points, exceptional/zero denominators,
physical couplings, active flavor defaults, identity holonomy defaults,
resolution averaging, or mixed coordinate forms.

Use a safe data-only grammar equivalent to:

```text
LOAD_C197_TARGET_REQUEST;
LOAD_C196_C202_FIELD_VERTEX_AUTHORITIES;
LOAD_C206_AFFINE_FAMILY;
LOAD_TARGET_PROJECTOR_AND_KINEMATICS;
FORM_MOMQ_NORMALIZATION_RESIDUAL;
DIFFERENTIATE_WITH_RESPECT_TO_AFFINE_PARAMETERS;
VERIFY_TARGET_LEFT_NULL_COMPATIBILITY;
EXACT_ROW_REDUCE_TARGET_SYSTEM;
INTERSECT_AFFINE_FAMILY_WITH_TARGET_CONDITIONS;
CLASSIFY_FIXED_AND_REMAINING_DIRECTIONS;
RETURN_TYPED_TARGET_MATCHED_FAMILY.
```

No eval, callbacks, pickle, pseudoinverse, optimization, minimum norm,
remembered MOMq formula, standard conversion, or physical default.

Require routes:

```text
MOMQ-A direct authenticated normalization condition;
MOMQ-B frozen field/vertex crosswalk;
MOMQ-C substitution into C206 affine family;
MOMQ-D exact symbolic/fraction-free solve;
MOMQ-E left-null compatibility;
MOMQ-F row/column/null-basis reversal;
MOMQ-G K9/K11/K13 and holonomy holdouts;
MOMQ-H free/tree nonphysical fixture.
```

Publish target residual/Jacobian shape, rank/nullity/left-nullity,
compatibility, fixed/free affine parameters, target-matched family dimension,
reversible maps, and exact substitution certificates. A remaining
underdetermined family is valid progress. Never set residual free directions
to zero or call them physical values.

Replace only ST-9. Preserve ST-1 through ST-8 and unrelated rows. Keep target
conditions distinct from identity rows and physical input. Publish topology
and count-once evidence.

## API, roots, evidence, validation

Implement preferably in `src/deuteron_wigner/bridge/hqcdmomqcond1/` with
runtime metadata under `data/runtime/c207_hqcdmomqcond1/`. Expose immutable
verified loaders, plan/frontier, target authority freeze, parameter
schema/fixtures/validation, target program schema/programs, target residuals,
Jacobians, compatibility, target-family intersection/evaluation,
fixed/remaining-direction classifications, ST replacement, analyticity,
topology/count-once, release, requests, missing object, handoff, dependency,
quantum nonmutation, isolation, mutations, and completeness. All NumPy loads
use `allow_pickle=False`.

Create complete `c207_*` contracts/manifests/validations and reports for
implementation, readiness, roots, runtime, safe loading, two deterministic
builds, restart, sharding, paging, query/order reversal, holdouts, mutations,
regressions, isolation, nonmutation, and completeness.

Use `/Users/dustin/miniforge3/bin/python3.9` with pytest 8.4.2. Run C157,
C158 public regression without values, targeted C161-C206, C196-C207 target/ST
tests, two deterministic builds, and at least 384 live mutations over actual
roots and target records. Repair ordinary defects autonomously.

## Release and successor

A positive C207 closes only authenticated finite-basis target MOMq conditions.
Then read the exact frontier. If confirmed, create one narrow continuation for:

```text
C197-ST-10 — physical input
alias PHYSICAL_INPUT_CONDITION
```

Do not activate a Hamiltonian or PennyLane until the exact physical-input,
matching, running/threshold, and Hamiltonian gates are committed. Commit C207
locally once, verify, generate/hash the successor prompt, atomically increment
state once, and continue under the persistent master instruction. Report all
roots, ranks, dimensions, tests/mutations, nonclaims, preserved paths, and no
push.
