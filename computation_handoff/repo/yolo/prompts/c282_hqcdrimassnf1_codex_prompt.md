# C282/HQCDRIMASSNF1 Codex Work Package

Resolve or rigorously reduce `C165-REQ-C165-MISSING-C164-LOC-TGT-SIGNED_QUARK_MASS-RI_SMOM-1`: the exact source object binding active loop-flavor convention and external state for the RI/SMOM signed-mass target.

Start from the C281 commit in `AUTOPILOT_STATE.json`; frozen root `10d82a013afa2e25a77e45c744aff26812875537ba7c0b6a78eda03fb13d65c8`. Hash-verify `docs/next_level/c281_c282_hqcdrimassnf1_continuation_contract.json`. Audit the authenticated arXiv:0901.2599v2 PDF/TeX archive, C153–C166 flavor and target records, C155 reversible u/d lift, and C276–C281 conventions.

Choose `RIMASSNF1-A` through `RIMASSNF1-F`: exact closure, maximal symbolic closure, narrower dependency, lawful branches, certified absence, or contradiction. A–D continue; E/F require a new certified blocker.

Bind active loop `N_f`, external nonsinglet flavor identities, sea/external separation, massless-limit scope, gauge, scale, color and projector conventions, perturbative order, and uncertainty. Publish direct/reverse flavor audits and immutable JSON-only safe APIs. Never infer `N_f`, flavor-average, promote a fixture, or modify the remaining C43-adapter and renormalization-layer leaves.

Run C250–C281 plus focused C282 tests, at least 384 mutations, deterministic dual builds, safe reload/restart/order checks, protected-path and C166/Q0/Q1/Q2 nonmutation. Commit exactly once, create exactly one next contract and full prompt, atomically advance state, never push, and report closure, validation, root, commit and next object.
