# C205/HQCDSTGLOBAL1 Codex Work Package

## Mission

Complete the exact `C197-ST-7` object, **global zero-mode/gauge-volume
treatment**, from the committed C204 endpoint ghost/link authority.  Keep Q0,
P0, global SU(3), holonomy stabilizers, global frames, orbit volumes, and
zero-mode identities mathematically distinct.  Validate and commit locally,
publish exactly one prospective successor contract and full prompt, atomically
advance persistent state, and continue immediately.  Never push.

Stop only at `PENNYLANE_PHYSICAL_ACTIVE` or a certified blocker under
`/Users/dustin/work/DeuteronWigner-yolo/PERSISTENT_YOLO_MASTER_PROMPT.md`.

## Exact baseline and contract

```text
baseline commit:
651c9697d31833fa420dd6f51b3219e8b7c861b0

completed package:
C204/HQCDSTBOUNDARY2

C204 package root:
2794f40129791a7ae87af07426284f77f2a0df1067b4b244e4b3e0d877e6f351

C204 status:
C204_C203_FINITE_HO_ENDPOINT_GHOST_LINK_IDENTITY_AUTHORITY_READY_GLOBAL_ZERO_MODE_FRONTIER_EXPLICIT

C204 plan:
STBOUNDARY2-B

continuation contract:
docs/next_level/c204_c205_hqcdstglobal1_continuation_contract.json

contract SHA-256:
0501d7aced8c6a6e27bafc7a9adc3b94cede880f325e85a4fa86a66c53fe2a7c
```

Read the contract and all C204 public handoff, frontier, completeness,
readiness, package-root, zero/holonomy remainder, and ST replacement records
completely. Resolve every omitted root and ID from committed public loaders;
never reconstruct scientific authority from this prose or model memory.

## Preserve the worktree and completed chain

Preserve the user modification `handoff/ROADMAP.md` and all pre-existing
untracked paths byte-for-byte, including the protected C69 prompt, inherited
C157 artifacts/test, `MSHT20_REP/`, `PennyLaneBackend/`, and the Q0/Q1
worktrees. Do not mutate C166 graphs, C158 values, Q0/Q1/Q2, or C164-C204
records. Consume C197-ST-1 through C197-ST-6 read-only.

Freeze the ordered frontier:

```text
C197-ST-1 through C197-ST-6: closed, read-only
C197-ST-7: global zero-mode/gauge-volume treatment — first exact object
C197-ST-8: ST-compatible counterterm solution
C197-ST-9: target MOMq renormalization conditions
C197-ST-10: physical input
```

## Authority and claim boundary

Consume read-only:

```text
C43 gauge algebra, representations, action, signs, and normalization;
C130 zero-mode and boundary nonmatrix interfaces;
C172/C174 exact Q0/P0 decomposition, residual gauge algebra, and FP operator;
C175 ghost domains, excluded/global modes, Berezin order, parity, ghost number;
C181/C182 finite-HO boundary and residual-link endpoint authorities;
C183 periodic SU(3) holonomy, cut transition, conjugacy, stabilizer, global
frame, global SU(3), and gauge-volume metadata;
C198-C202 ST systems and conditional vertex/field authorities;
C203 local-P0 BRST source functional and descendants;
C204 endpoint ghost/link identities and explicit global remainder.
```

A positive C205 may publish a source-derived finite-basis global zero-mode and
gauge-volume identity, its incremental `C197-ST-7` replacement, and exact
counterterm/null sensitivities. It must keep distinct:

```text
Q0 nonzero modes and P0 local modes;
the global SU(3) Lie algebra and group orbit;
residual global gauge transformations;
holonomy conjugacy classes and stabilizers;
generic, central, wall, and identity diagnostic capsules;
global frame choice and frame covariance;
zero modes of the FP operator;
local FP determinant and excluded zero modes;
group/orbit volume and stabilizer volume;
Gribov/fundamental-domain scope if source-qualified;
global ghost and antighost coordinates if present;
global BRST source identity if present;
gauge-volume normalization versus physical normalization;
six counterterm and nine null directions;
target, standard, and physical conditions.
```

Never assume a normalized Haar measure, divide by a gauge volume, select a
global frame, choose a physical holonomy/center sector, introduce global
ghosts/antifields, or declare a zero mode removed unless authenticated project
authority supplies that operation. Missing objects are unavailable, not zero.

C205 must not select counterterms/null representatives, target MOMq values,
standard-scheme conversions, physical couplings/inputs, Hamiltonians, states,
spectra, circuits, TMDs, or production observables. Full ST closure is not
claimed unless all declared gates actually close.

## Mutually exclusive plans

Choose exactly one:

```text
STGLOBAL1-A — complete finite-basis global zero-mode/gauge-volume identity ready;
STGLOBAL1-B — global orbit/stabilizer identity ready with physical volume normalization unselected;
STGLOBAL1-C — global gauge-algebra or zero-mode domain incomplete;
STGLOBAL1-D — holonomy conjugacy/stabilizer authority incomplete;
STGLOBAL1-E — global frame covariance incomplete;
STGLOBAL1-F — ghost/antighost/global-source convention incomplete;
STGLOBAL1-G — orbit/gauge-volume measure authority incomplete;
STGLOBAL1-H — global BRST/ST identity or nilpotency incomplete;
STGLOBAL1-I — C197-ST-7 descendant/replacement incomplete;
STGLOBAL1-J — Jacobian/cohomology/count-once incomplete;
STGLOBAL1-K — independent-route mismatch;
STGLOBAL1-L — current-chain regression.
```

Rank deficiency or an unselected normalization is not itself a blocker when a
conditional exact identity or narrower source-qualified continuation remains.

## Scientific construction

### Normalize the exact object

Bind `C197-ST-7`, alias `GLOBAL_GAUGE_VOLUME_IDENTITY`, all committed aliases,
required sectors/fields/sources, source order, ghost number, parity, coupling
degree, resolutions, schemes, holonomy/BC classes, affected rows, variables,
and exact continuation capsule. Never hide the first missing subobject behind
a generic “global identity incomplete” label.

### Immutable global inventory and parameters

Publish typed records for Q0/P0 projectors, global algebra generators, global
group/orbit coordinates, global frames, holonomy conjugacy representatives,
centralizers/stabilizers, FP zero modes, local determinant exclusions, any
source-qualified global ghosts/antighosts/sources, gauge-volume records, and
boundary/link interfaces. Bind dimensions, representations, units, measures,
normalizations, orientations, resolution, boundary/holonomy class, owner,
source root, and no-default guards.

Parameter records must explicitly bind the named nonphysical fixture,
holonomy capsule, global frame, orbit/stabilizer record, measure convention,
Q0/P0/global sector, counterterm/null coordinate order, branch/enclosure, and
`physical=false`. Reject identity holonomy or unit gauge volume as a default.

### Safe global programs

Use a data-only grammar with source-authorized equivalents of:

```text
LOAD_C172_C174_Q0_P0_DECOMPOSITION;
LOAD_C175_FP_GHOST_ZERO_MODE_DOMAIN;
LOAD_C183_HOLONOMY_CONJUGACY;
LOAD_GLOBAL_FRAME;
LOAD_GLOBAL_SU3_ORBIT;
LOAD_STABILIZER_OR_CENTRALIZER;
LOAD_GAUGE_VOLUME_RECORD;
APPLY_C203_BRST_DIFFERENTIAL;
APPLY_C204_ENDPOINT_REMAINDER;
APPLY_GLOBAL_GAUGE_TRANSFORMATION;
APPLY_HOLONOMY_CONJUGATION;
APPLY_FRAME_CHANGE;
PROJECT_LOCAL_AND_GLOBAL_MODES;
TAKE_GLOBAL_SOURCE_DERIVATIVE;
FORM_ORBIT_STABILIZER_RATIO;
RETURN_TYPED_GLOBAL_IDENTITY.
```

No eval, callbacks, pickle, dynamic imports, hidden network use, implicit
measure, remembered BRST/ST formula, or default physical sector.

### Independent derivations

Derive, where source-qualified, the global transformation, global ghost
algebra, second BRST variation, action/source-functional residual, holonomy
conjugation covariance, frame covariance, FP zero-mode separation, and
orbit/stabilizer gauge-volume identity. Require routes:

```text
GLOBAL-A direct C43/C172/C174 orbit action;
GLOBAL-B C175 FP zero-mode/ghost-domain route;
GLOBAL-C C183 conjugacy and stabilizer route;
GLOBAL-D frame-change covariance route;
GLOBAL-E C203 source-functional route;
GLOBAL-F C204 endpoint/global remainder route;
GLOBAL-G Lie/Jacobi/nilpotency route;
GLOBAL-H generic/central/wall/identity fixture holdouts.
```

Audit whether a source-qualified global BRST-source identity exists. Do not
invent a global ghost, antifield, auxiliary field, Haar normalization, or
Faddeev-Popov volume factor. If only an orbit/stabilizer ratio is authorized,
publish exactly that conditional scope and leave absolute normalization
unselected.

### Descendants, Jacobian, and ST replacement

Differentiate only accepted global identities through public APIs. Keep
global modes, holonomy coordinates, frame coordinates, endpoint remainders,
six counterterm directions, nine null coordinates, and identified coordinates
separate. Publish dimensions, rank, nullity, left nullity, compatibility,
condition diagnostics, independent/dependent rows, closed/exact/unconstrained
directions, and solution-family dimension without selecting a representative.

Replace only blocked `C197-ST-7` rows. Preserve ST-1 through ST-6 and all
unrelated rows. Do not drop a row, count a definition as a constraint, absorb
global volume into the local source functional, or encode a missing residual
as zero.

### Topology and count once

Separate local FP determinant, excluded zero modes, global orbit, stabilizer,
frame, holonomy conjugacy, boundary remainder, source definition, global
identity, Jacobian sensitivity, target condition, physical normalization, and
solution family. Prove each owner is counted exactly once.

## Public API and roots

Implement preferably under:

```text
src/deuteron_wigner/bridge/hqcdstglobal1/
data/runtime/c205_hqcdstglobal1/
```

Expose immutable verified loaders plus plan/frontier, global inventory,
parameter schema/fixtures/validation, safe program schema/programs, global
transformations, zero-mode decomposition, holonomy/stabilizer, frame
covariance, orbit/gauge-volume identity evaluation, nilpotency, action/source
functional, descendants, Jacobian, ST replacement, analyticity, topology,
count-once, release, request, missing-object, next-handoff, dependency,
quantum-nonmutation, isolation, mutation, and completeness APIs. Follow the
C204 signature/filter/immutability pattern. All NumPy loads use
`allow_pickle=False`.

Publish distinct roots for every scientific layer and the package. No root
may bind physical normalization, selected counterterms/nulls, target values,
quantum state, or full-ST claims beyond proven scope.

## Validation and evidence

Create complete `c205_*` contracts, manifests, validations, implementation
and readiness reports, package-root/runtime inventories, safe-loading,
nonmutation, isolation, deterministic-build, restart, sharding, paging,
query-order, holdout, mutation, regression, and completeness evidence. Evidence
must carry the actual authority record, not merely a filename claim.

Use `/Users/dustin/miniforge3/bin/python3.9` and pytest 8.4.2. Run the tracked
C157 replacement, C158 public regression without values, targeted C161-C204
tests, focused C130/C172/C174/C175/C183/C203/C204 global/BRST tests, and C205
tests. Require two deterministic clean builds and reversed order across
Q0/P0/global sector, generator, holonomy capsule, conjugacy/stabilizer class,
frame, zero-mode basis, ghost order, resolution K9/K11/K13, scheme,
counterterm/null coordinates, Jacobian rows/columns, topology, and handoff.

Run at least 384 live mutations over actual roots and global records. Validate
reload, safe loading, restart, sharding, paging, query order, source ownership,
units, grading, nilpotency scope, covariance, compatibility, topology,
count-once, protected paths, and no push. Diagnose and repair ordinary
failures autonomously.

## Release and successor

A positive release closes `C197-ST-7` only at its exact authenticated scope.
Then read the ordered committed frontier. If confirmed, the next object is:

```text
C197-ST-8 — ST-compatible counterterm solution
alias ST_COMPATIBLE_COUNTERTERM_SOLUTION
```

Create one narrow prospective contract for that object. Do not jump to target
MOMq, physical input, Hamiltonian, PennyLane, or TMD work.

Commit C205 locally exactly once, verify, generate/hash the full successor
prompt, atomically increment persistent state once, and continue immediately.
Report exact commits, roots, censuses, scopes, system ranks, tests/mutations,
remaining frontier, nonclaims, preserved paths, and confirmation of no push.
