# C298/HQCDRIMASSCONSTRAINTKERNEL1 Codex Work Package

Construct the renormalized SU(3) constrained-zero-mode current/resolvent kernel and mass input needed to evaluate the C297 boundary remainder at K9/K11/K13.

Start from the C297 completion commit in `AUTOPILOT_STATE.json`; frozen C297 package root `44b6ca07acb3e3ea970667244072988158ca495fb7829972a210842b448a6502`. Hash-verify `docs/next_level/c297_c298_hqcdrimassconstraintkernel1_continuation_contract.json` and load C297 through its verified public API. Frozen authority includes the exact Soyez `a3contr/a8contr` equations, formal solutions `a0,3=sqrt(4*pi)g^2 F3/mu0^2` and `a0,8=(3/4)sqrt(4*pi)g^2 F8/mu0^2`, the exact boundary-remainder substitution functional, and the joint covariance formula `Sigma_y=J Sigma_eta J^T+Sigma_trunc`.

Audit the hash-locked Soyez `hep-th/0101072` source, especially the full charged-root current definitions, Gauss-law inverse operators, symmetric zero-mode projection, mass-renormalization statement, and Hamiltonian construction; audit Fujita–Shvartsman `hep-th/9506046` for the lawful P/Q inverse and physical-Hamiltonian elimination framework; then crosswalk C43/C130/C274/C278 operators and caller parameters. Seek an exact repository/Git/source kernel first, then a project-owned derivation from authenticated equations. Do not use the paper’s later omission as a zero, invent current matrix elements, take an unsupported massless inverse, collapse six charged channels, average K, flatten holonomy, promote 1+1 results to 3+1 without an adapter, or select C117 coordinates.

Choose exactly one plan `RIMASSCONSTRAINTKERNEL1-A` through `-F`: executable renormalized kernel; maximal symbolic operator kernel; narrower current/resolvent component; lawful alternative authority; certified absence; or contradiction. A-D continue. E/F require the complete newly schema-certified blocker protocol; a missing evaluable input with a source-qualified continuation is not a blocker.

Publish immutable JSON-only APIs for source freeze, field/current basis, six root-shifted Q-sector resolvents, Cartan constraint kernel, renormalized mass scheme/input, Hamiltonian insertion, K9/K11/K13 discretization, joint covariance, residual frontier, release, and isolation. Validate charge/root pairing, conjugation and Hermiticity, inverse-domain exclusions, Weyl covariance, units, count-once insertion, common-source off-diagonal covariance, restart/order stability, and independent symbolic or matrix route parity. Keep unavailable numerical values explicit and `physical=false`.

Run the cumulative C250-C297 chain plus focused C298 tests, at least 384 mutations, deterministic dual builds, safe JSON reload without pickle, protected paths, and C166/Q0/Q1/Q2 nonmutation. Preserve unrelated worktree changes. Make exactly one local completion commit and never push. Generate exactly one next prospective contract and one full prompt, hash them, atomically advance `AUTOPILOT_STATE.json`, and continue immediately. Report plan, exact closure/nonclaims, validation, root, commit, and next object.
