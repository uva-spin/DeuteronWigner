# C277/HQCDRIMASSSELF1 Codex Work Package

## Mission

Evaluate or rigorously reduce the exact C276 frontier: the order-`g_s^2` C43 signed-mass-projected quark self-energy on the authenticated RI/SMOM common-state/common-IR record, separately at K9, K11, and K13.

## Baseline and authority

Start from the C276 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C276 package root:

```text
97d8fd6b6bd40d1295d7cdf9d3d3b849c375c7adad1f24794d86261b3e2d3994
```

The sole prospective contract is `docs/next_level/c276_c277_hqcdrimassself1_continuation_contract.json`; hash-verify it. Audit C142–C150 source, two-point, signed-mass projector and conditional-renormalization records; C153–C168 common-IR and target authority; the C216–C258 quark self-energy construction for reusable graph-local operator evidence only; C275–C276 slot and convention roots; and exact authenticated source artifacts already recorded in repository provenance.

## Boundary and plans

C276 binds the signed-linear projector and structural adapter but does not evaluate the mass projection. Select exactly one:

```text
RIMASSSELF1-A  exact three-resolution mass-projected self-energy closes
RIMASSSELF1-B  maximal executable contribution bundle closes with one named remainder
RIMASSSELF1-C  a narrower graph/operator/source calculation is required
RIMASSSELF1-D  lawful regulator, boundary, or scheme branches remain separate
RIMASSSELF1-E  indispensable source authority is proven unavailable
RIMASSSELF1-F  source-qualified contributions contradict
```

A–D are positive continuations; E/F require a newly schema-certified blocker. Do not import the unrelated C117 finite-current target, assume a finite subtraction coefficient, or treat absent contributions as zero.

## Construction

Freeze the complete common-state record, symmetric nonexceptional momenta, signed `m_R` coordinate, independent `m_R^2`, active flavor and `N_f`, gauge/pole prescription, boundary/holonomy capsule, common-IR map, units, and K-local normalization. Assemble count-once free, canonical q–qg–q, instantaneous-fermion, instantaneous-current, zero-mode/boundary/residual-link, counterterm-direction, and omitted-Fock interfaces. Apply the C149 unit-response signed-mass projector before common-IR subtraction.

Publish independent direct projection, derivative/Feynman–Hellmann, spectral/resolvent, owner-decomposition, and sign-reversal checks where lawful. Separate diagnostic fixtures from physical inputs and propagate unavailable terms symbolically with covariance and truncation semantics. Preserve all C117 coordinates and later non-C117 adapter requests.

## API, validation, continuation

Publish immutable safe-loading state, contribution, projector, self-energy, route, uncertainty, residual, release, mutation, and isolation APIs with JSON-only runtime. Run C250–C276 plus focused C277 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order checks, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

After exactly one local commit, create exactly one next prospective contract and full prompt and atomically advance `AUTOPILOT_STATE.json`. If the projection closes, finish the RI/SMOM signed-mass adapter; otherwise continue with the smallest exact graph, source, or normalization object.

Report plan, evaluated contribution boundary, tests/mutations, root, commit, next job/object, and no-push/nonactivation status.
