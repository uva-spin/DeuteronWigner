# C393/HQCDRIMASSC43RUNNINGPHASE1

Start from the accepted C392 commit recorded in `AUTOPILOT_STATE.json`, the
C392 package root `caefe37c197bdf6f70f03c526df064a3a4ac8a8257742318aa75af27a38b5a00`,
and the prospective contract
`docs/next_level/c392_c393_hqcdrimassc43runningphase1_continuation_contract.json`.

Freeze the C391 JMY source authority and C392 full-rank
`DW-JMY-COMMON-IR-PROJECT-v1` scheme, its operator-identical common-IR
crosswalk, symbolic conversion/inverse/composition law, covariance ownership,
and separate K9/K11/K13 adapters. Never label the project scheme as MSbar,
Collins, ART25, or another standard scheme.

Complete one coherent running/threshold/standard-scheme phase. First bind
authenticated standard-scheme conversion coefficients or exact symbolic
source expressions, anomalous dimensions, beta function, active-flavor
records, threshold matching conditions, scale domains, and order of limits.
Use repository/Git/local primary-source authority first and exact official
primary sources when required. Hash acquired bytes. A project intermediate
scheme is lawful only with an explicit standard conversion path and
scheme-dependence diagnostics; unavailable coefficients remain unavailable,
never zero.

Keep flavor, color, distribution component, source/sink orientation, rapidity
and UV scales, threshold sides, and K9/K11/K13 resolution records separate.
Require forward/inverse conversion, composition, RG group law, threshold
up/down round trips, continuity or exact decoupling remainder, independent
route parity, covariance transport, and no double counting. Do not choose a
physical coupling, mass, scale, active-flavor count, threshold, boundary
ensemble, or counterterm representative without authenticated records.

Expose immutable safe-loading APIs for input freeze, standard conversion,
running, thresholds, active flavors, resolution transport, covariance,
release, completeness, and next handoff. Validate at least 384 focused live
mutations, adjacent C392 regression, two deterministic clean builds, source
roots, units, orientations, Hermiticity scope, count-once, restart/order
stability, safe load with `allow_pickle=False`, isolation, and no push.

Choose exactly one result: positive authenticated running/threshold authority;
exact symbolic authority with the smallest physical-input remainder explicit;
or fail closed after all lawful authority-recovery routes. Complete internal
stages in one accepted C393 phase commit, create exactly one next prospective
contract and full prompt, atomically advance state, and continue immediately.
Preserve C134, C157, C166, unrelated work, and frozen Q0/Q1/Q2.
