# C204/HQCDSTBOUNDARY2 Codex Work Package

## Mission

Complete the exact `C197-ST-6` object, **endpoint ghost/link identities**, from
the committed C203 local-P0 BRST-source authority.  Construct a finite-HO
boundary, endpoint-ghost, residual-link, and cut/holonomy-remainder authority
without promoting local closure to a global theorem.  Validate, create one
local completion commit, generate the single exact successor contract and
full prompt, atomically advance persistent state, and continue immediately.

Never push.  Never invent scientific authority or encode an unavailable
boundary/global residual as zero.

## Exact baseline and provenance

Start from clean committed baseline:

```text
2d2fa094e44b6f092e763078a67805ab86562f72
```

Consume and hash:

```text
docs/next_level/c203_c204_hqcdstboundary2_continuation_contract.json
expected SHA-256:
6ebd59ccb69c02635c6945b6df8b81166e4976d3ee87ba93687a9439cb038379
```

Freeze C203:

```text
package: C203/HQCDBRST1
package root: bb881fbf5576e0ce98b69f3171de79e24b0a1bbdf32bb2370a2270e37652d61e
status: C203_C202_LOCAL_P0_BRST_SOURCE_IDENTITY_AUTHORITY_READY_BOUNDARY_GLOBAL_FRONTIER_EXPLICIT
plan: BRST1-B
completed object: C197-ST-5 — BRST source identities
role: REDUCED_P0_RESIDUAL_BRST_SOURCE_AUTHORITY
```

Freeze the inherited ordered frontier exactly:

```text
C197-ST-1 through C197-ST-5: closed/read-only
C197-ST-6: endpoint ghost/link identities — first exact object
C197-ST-7: global zero-mode/gauge-volume treatment
C197-ST-8: ST-compatible counterterm solution
C197-ST-9: target MOMq renormalization conditions
C197-ST-10: physical input
```

Read completely before implementation:

```text
docs/next_level/c203_c204_hqcdstboundary2_continuation_contract.json
docs/next_level/c203_implementation_report.md
docs/next_level/c203_package_root_manifest.json
docs/next_level/c203_frontier_manifest.json
docs/next_level/c203_boundary_global_manifest.json
docs/next_level/c203_next_st_handoff_contract.json
docs/next_level/c203_st_replacement_manifest.json
docs/next_level/c203_hqcdbrst1_completeness_certificate.json
```

Resolve all omitted roots and identifiers from committed public loaders and
manifests.  Do not reconstruct them from this prose or model memory.

## Worktree preservation

Preserve the pre-existing user modification `handoff/ROADMAP.md` byte for
byte unless a narrow append-only update is expressly required.  Preserve all
pre-existing untracked paths, especially:

```text
MSHT20_REP/
PennyLaneBackend/
deuteron_wigner_q0_plhqcd0/
deuteron_wigner_q1_plhqcdstate/
docs/next_level/c69_qgembed5_codex_prompt.md
tests/test_c157_hqcdmatchir2.py
docs/next_level/c157_common_ir_gate_validation.json
docs/next_level/c157_numerical_evidence_contract.json
docs/next_level/c157_perturbative_remainder_validation.json
docs/next_level/c157_window_validation.json
```

Do not modify C166 graphs or Q0/Q1/Q2.  Do not consume C158 values.

## Authority and strict claim boundary

Use, read-only, the exact public authorities:

```text
C43 gauge transformation, representations, signs, derivatives, and action;
C130 zero-mode/boundary nonmatrix interfaces;
C172/C174 residual Q0/P0 gauge and FP operators;
C175 ghost/antighost, parity, ghost number, Berezin order, and ghost links;
C181 finite-HO boundary ownership and pullback;
C182 residual-link source/operator, endpoint support, and orientations;
C183 cut transition, holonomy conjugation, global frame, and gauge volume;
C196-C202 conditional field/proper-vertex/ST authorities;
C203 BRST differential, sources, functional, descendants, and replacement.
```

A positive C204 may claim only source-derived finite-basis endpoint
ghost/link identities and their exact incremental ST-row replacement.
It must keep separate:

```text
local P0 bulk BRST;
finite-HO boundary pullback;
left and right endpoints;
incoming and outgoing source orientation;
ghost and antighost endpoint insertions;
one-link and two-link source records;
residual-link endpoint transformations;
cut-side transition records;
future, past, principal-value, and cut-shift boundary classes;
holonomy conjugation remainders;
Q0/nonzero-mode interfaces;
global SU(3), global frame, and global gauge volume;
counterterm/null sensitivity;
target, standard, and physical conditions.
```

C204 must not claim global zero-mode/gauge-volume closure, full ST closure,
a selected counterterm or null representative, a physical coupling or BRST
charge, target MOMq matching, standard conversion, physical input, a
Hamiltonian, PennyLane activation, a state, spectrum, TMD, or process result.

## Mutually exclusive plans

Select exactly one before publishing:

```text
STBOUNDARY2-A — complete endpoint ghost/link identity authority ready;
STBOUNDARY2-B — finite-HO endpoint identity ready with typed holonomy/global remainder;
STBOUNDARY2-C — endpoint field/source inventory incomplete;
STBOUNDARY2-D — ghost-link orientation or graded-sign authority incomplete;
STBOUNDARY2-E — residual-link transformation or endpoint covariance incomplete;
STBOUNDARY2-F — cut/holonomy compatibility incomplete;
STBOUNDARY2-G — identity descendant or C197-ST-6 replacement incomplete;
STBOUNDARY2-H — Jacobian/cohomology/count-once incomplete;
STBOUNDARY2-I — independent-route mismatch;
STBOUNDARY2-J — current-chain regression.
```

Do not combine plans silently.  A rank-deficient compatible result is not a
blocker while the next exact frontier remains.

## Scientific construction

### Normalize `C197-ST-6`

Bind the committed normalized record and all aliases, required fields,
ghost/antighost insertions, endpoint/link source IDs, source order, ghost
number, Grassmann parity, coupling degree, resolutions, schemes,
holonomy/BC classes, affected identity rows, and exact continuation capsule.

### Immutable endpoint inventory

Publish immutable records for every source-qualified object:

```text
finite-HO boundary cells and pullbacks;
left/right endpoint supports;
residual one-link and two-link variables;
incoming/outgoing link orientations;
endpoint ghost and antighost fields;
endpoint ghost-link composite/source records;
cut transitions and cut side;
holonomy transport/conjugation capsule;
Q0/P0/global ownership;
global-frame and gauge-volume interfaces.
```

Every record binds representation, units, ghost number, Grassmann parity,
source/sink orientation, endpoint side, link order, boundary class,
holonomy class, resolution, owner, source roots, and no-default guards.

### Safe data-only programs

Create an immutable grammar for exact operations equivalent to:

```text
LOAD_C175_ENDPOINT_GHOST_LINK;
LOAD_C181_BOUNDARY_PULLBACK;
LOAD_C182_RESIDUAL_LINK_ENDPOINT;
LOAD_C183_CUT_HOLONOMY_CAPSULE;
APPLY_C203_BRST_DIFFERENTIAL;
APPLY_GRADED_LEIBNIZ_RULE;
APPLY_ENDPOINT_ORIENTATION;
APPLY_LINK_ENDPOINT_TRANSFORMATION;
APPLY_BOUNDARY_PULLBACK;
APPLY_CUT_TRANSITION;
APPLY_HOLONOMY_CONJUGATION;
TAKE_ENDPOINT_SOURCE_DERIVATIVE;
NORMAL_ORDER_GRADED_PRODUCT;
RETURN_TYPED_ENDPOINT_IDENTITY.
```

No callbacks, eval, pickle, dynamic imports, hidden network access, remembered
formulas, or implicit physical/boundary/holonomy defaults.

### Derive endpoint identities

Derive all transformations and residuals from C175/C181/C182/C183 plus the
C203 differential.  Do not infer signs or endpoint orientations from
continuum convention.  Construct separately:

```text
BRST variation before boundary pullback;
boundary pullback before BRST variation;
left-endpoint ghost/link identity;
right-endpoint ghost/link identity;
one-link and two-link identities;
source and sink orientations;
future/past/PV/cut-shift classes;
cut-transition covariance;
holonomy-conjugation remainder;
Q0/P0 interface remainder;
global-mode/gauge-volume remainder.
```

Require independent routes:

```text
BND-A direct endpoint BRST variation;
BND-B ghost-link source/operator route;
BND-C boundary-pullback commutator route;
BND-D residual-link endpoint covariance route;
BND-E reversed endpoint/link/source order;
BND-F cut and holonomy conjugation route;
BND-G free/tree and K11/K13 holdouts.
```

Any missing global object remains unavailable, never zero.

### Nilpotency, invariance, and descendants

Test endpoint-local second BRST variation, graded Jacobi/Leibniz identities,
boundary-pullback commutation, residual-link covariance, ghost number,
parity, units, complex/source-orientation conjugation, and all-eight-generator
covariance.  State exact local, boundary, holonomy, and global scopes.

Differentiate only the accepted endpoint source identity through committed
public APIs.  Consume C196-C203 read-only.  Bind parent identity, ordered
source slots, derivative order, endpoint/link orientation, projector/tensor,
resolution, scheme, holonomy/BC class, units, residual program, and missing
dependencies.  Do not recompute a field factor or proper vertex.

### Jacobian and ST replacement

Differentiate endpoint residuals with respect to the frozen six counterterm
directions, nine null coordinates, identified coordinates, endpoint/link
sources, ghost coordinates, boundary coordinates, and compatible holonomy
coordinates.  Publish dimensions, rank, nullity, left nullity, compatibility,
condition diagnostics, independent/dependent row bases, and closed/exact/
unconstrained directions without selecting a solution.

Replace only blocked `C197-ST-6` rows.  Preserve C197-ST-1 through ST-5 and
all unrelated rows byte-for-byte.  Publish old/new row crosswalks, updated
residual and Jacobian systems, rank/nullity/left-nullity, exact compatibility,
conditional solution-family dimension, and remaining ordered frontier.

### Topology and count once

Prove that bulk BRST terms, boundary pullbacks, endpoint ghost insertions,
link transformations, source definitions, identity constraints, cut
transitions, holonomy remainders, global-volume interfaces, counterterm/null
sensitivities, and later target/physical conditions are each counted once.
Do not count a source definition as an independent identity or a boundary
interface as a local matrix term.

## Public API

Implement under the contract-authorized package, preferably:

```text
src/deuteron_wigner/bridge/hqcdstboundary2/
data/runtime/c204_hqcdstboundary2/
```

Expose immutable equivalents of:

```python
load_verified_hqcd_stboundary2_authority()
verify_hqcd_stboundary2_authority()
stboundary2_plan_manifest()
frontier_manifest(object_id=None)
endpoint_inventory_manifest(record_id=None, endpoint=None, boundary_class=None)
endpoint_parameter_schema()
endpoint_fixture_manifest(fixture_id=None)
validate_endpoint_parameter_record(parameter_record)
endpoint_program_schema()
endpoint_program_manifest(program_id=None, owner_id=None)
endpoint_transformation_manifest(owner_id=None, endpoint=None, resolution_id=None)
apply_endpoint_transformation(parameter_record, owner_id, value)
endpoint_identity_manifest(identity_id=None, endpoint=None, link_order=None)
evaluate_endpoint_identity(parameter_record, identity_id)
boundary_pullback_commutator_manifest(owner_id=None, resolution_id=None)
endpoint_nilpotency_manifest(owner_id=None, endpoint=None, resolution_id=None)
cut_holonomy_remainder_manifest(record_id=None, holonomy_capsule_id=None)
descendant_manifest(descendant_id=None, parent_row_id=None)
jacobian_manifest(resolution_id=None, row_id=None, parameter_id=None)
st_replacement_manifest(old_row_id=None, new_row_id=None, system_id=None)
analyticity_manifest(resolution_id=None, fixture_id=None)
topology_manifest(graph_id=None)
count_once_manifest(request_id=None)
stboundary2_release_manifest()
request_resolution_manifest(request_id=None)
missing_endpoint_object_manifest(request_id=None)
next_st_handoff_contract()
dependency_frontier_manifest()
quantum_nonmutation_manifest()
stboundary2_completeness_certificate()
```

All NumPy loads use `allow_pickle=False`.  Reject unknown IDs, partial or
mixed records, hidden defaults, changed upstream roots, implicit identity
holonomy, endpoint averaging, resolution averaging, row dropping, missing
residuals encoded as zero, graph mutation, C158 values, and quantum changes.

## Root separation and deliverables

Publish distinct authenticated roots for input, provenance, plan, frontier,
inventory, parameter schema, fixtures, program schema/programs,
transformations, endpoint identities, pullback commutators, nilpotency,
cut/holonomy remainders, descendants, Jacobian, ST replacement, analyticity,
topology, count-once, release, request resolution, missing objects, next
handoff, dependency frontier, quantum nonmutation, scope, completeness, and
the package.  No root may bind physical values or claims.

Create complete `c204_*` contracts, manifests, validations, implementation
report, readiness report, runtime inventory, package-root manifest, safe-load
report, isolation report, nonmutation reports, test report, mutation report,
two-clean-build report, restart/sharding/paging/query-order reports, and one
prospective continuation contract.  Follow the established C203 naming and
source-authority style; do not generate hollow evidence merely to satisfy a
filename census.

## Validation

Use `/Users/dustin/miniforge3/bin/python3.9` and pytest 8.4.2.  Run:

```text
tracked authoritative C157 replacement;
C158 public regression without values;
C161-C203 targeted regressions;
C43/C130/C172/C174/C175/C181/C182/C183 gauge-boundary-link tests;
C196-C203 ST/vertex/BRST tests;
C204 focused tests and validators.
```

Require two deterministic clean builds and reversed-order builds across
endpoint side, source/sink orientation, link order, ghost/antighost order,
boundary class, cut side, holonomy capsule, resolution K9/K11/K13, scheme,
Jacobian row/column order, topology, requests, and handoff.  Test public
reload, safe loading, restart, sharding, paging, query order, source ownership,
units, graded Hermiticity/orientation, covariance, topology, and count-once.

Execute at least 384 focused live mutations over actual scientific roots,
inventory, programs, endpoint transformations, identities, orientations,
pullback commutators, nilpotency, cut/holonomy remainders, descendants,
Jacobians, ST replacements, topology, release, requests, and continuation.

A test failure requires diagnosis and repair.  It is not a blocker unless it
establishes one of the certified terminal conditions in the persistent master
instruction.

## Isolation and nonclaims

Require zero counts for remembered BRST/ST formulas, invented fields/sources,
proper-vertex or field-response recomputation, source/contact/higher-Fock
rediscovery, upstream mutation, graph mutation, C158 value use, quantum
mutation, hidden physical values, selected counterterms/null representatives,
boundary/local conflation, assumed holonomy invariance, absorbed global gauge
volume, missing-as-zero residuals, silent row drops, target/standard/physical
conditions, Hamiltonians, states, spectra, circuits, TMDs, and production
objects.

## Release and next continuation

A positive release must state either complete endpoint ghost/link authority
or finite-HO endpoint authority with the holonomy/global remainder explicit.
It must not claim `C197-ST-7` closure.

After release, read the committed ordered frontier.  Expected only if still
confirmed by public evidence:

```text
C197-ST-7 — global zero-mode/gauge-volume treatment
alias GLOBAL_GAUGE_VOLUME_IDENTITY
```

Create exactly one narrow prospective continuation for that exact object.
Do not jump to counterterm selection, target matching, physical input,
Hamiltonian construction, or PennyLane activation.

Then:

```text
commit C204 locally exactly once;
verify the commit and preserved worktree;
generate and hash the complete next prompt;
atomically update AUTOPILOT_STATE.json with revision incremented once;
continue immediately under PERSISTENT_YOLO_MASTER_PROMPT.md.
```

Stop only for `PENNYLANE_PHYSICAL_ACTIVE`, a certified
`REAL_MATH_PHYSICS_BLOCKER`, or a certified `INFRASTRUCTURE_BLOCKER`.

## Final job report

Record starting/final commits, plan/status, consumed contract and hash,
upstream-root verification, exact C197-ST-6 normalization, inventory and API
censuses, transformation/identity routes, nilpotency and remainder scopes,
descendants, Jacobian/system dimensions and ranks, ST-row replacements,
topology/count-once, release decision, test/mutation/determinism evidence,
package root, remaining frontier, successor contract/prompt, all nonclaims,
preserved user paths, and confirmation that nothing was pushed.
