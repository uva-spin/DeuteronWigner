# C211/HQCDMOMQEVAL1 Codex Work Package

## Mission

Evaluate the C210 guarded symbolic MOMq condition on authenticated, complete,
nonphysical caller-bound fixtures at K9/K11/K13 and compare independent enclosed
routes. Do not invent physical inputs, exact finite points, continuum values,
resolution averages, or hidden extrapolations. Validate, commit locally,
generate exactly one successor contract and full prompt, atomically update
persistent state, and continue immediately. Never push.

## Authority

```text
baseline: 08ac6fa2c63121c41c5d6647f9e8870e1b8dd5c6
C210 root: 4053af60153556d37c3fd045b3fd5d3a6d796494e0005b4fafaeb06d991ac756
C210 status: C210_C209_EXECUTABLE_ENCLOSED_TARGET_MOMQ_CONDITION_READY_NONPHYSICAL
contract: docs/next_level/c210_c211_hqcdmomqeval1_continuation_contract.json
contract SHA-256: 4796c7effce51e3a1cdb3c552ee39adc493fbc883a1139e580d42155dc512e33
```

Read C140, C157, C168, and C196-C210 authorities. Preserve user/untracked
work, C158 values, C166 graphs, and Q0/Q1/Q2.

## Exact object

Publish named nonphysical fixtures with every C209/C210 caller field explicit.
Evaluate all six channels and select channel 1 only after nonexceptional and
Gram guards. Propagate longitudinal-tail, omitted-HO-shell, quadrature,
invariant, and projector bounds. Compare direct inverse-Gram, dual-functional,
channel-reordered, and resolution-local routes by enclosure overlap. Reject
missing fields, resolution mismatch, zero-crossing guards, implicit defaults,
and malformed common-IR/boundary/holonomy records.

Choose exactly one plan: `MOMQEVAL1-A` evaluated enclosed fixtures ready;
`MOMQEVAL1-B` one smaller fixture/route object remains; `MOMQEVAL1-C` guard
failure; `MOMQEVAL1-D` enclosure propagation incomplete; `MOMQEVAL1-E`
common-IR/gauge/boundary mismatch; `MOMQEVAL1-F` lawful evaluation impossible;
`MOMQEVAL1-G` regression.

Implement preferably under `src/deuteron_wigner/bridge/hqcdmomqeval1/` with
immutable runtime metadata, strict fixture schema, safe data-only programs,
route results, overlap/residual certificates, release/handoff/isolation, and
completeness. All NumPy loads use `allow_pickle=False`; no eval, pickle,
callbacks, numerical defaults, or physical authority. Create complete `c211_*`
evidence, run focused and authoritative regressions, two clean builds, and at
least 384 live mutations. If evaluation closes, continue to the narrowest
remaining C197-ST-9 comparison/decision object. Stop only under the master
schema or at PENNYLANE_PHYSICAL_ACTIVE.
