# C265/HQCDC117NONLOCALTARGET1 Codex Work Package

## Mission

Evaluate or construct an exact executable evaluation program for the four C264
nonlocal continuum current wavepacket/projector targets. Bind Ward/ST
diagnostics, scale transport, standard/physical matching, and correlated
packet/regulator/scheme uncertainty while preserving K9/K11/K13 separation.

Do not solve finite-C43 C117 coefficients unless a later exact contract
authorizes the solve.

## Baseline and authority

Start from the C264 completion commit in AUTOPILOT_STATE.json. Frozen C264
package root:

```text
6aad37b4f5d278b249655c946529be10d159e5a2694c0bed09ef5f65df6c26dc
```

Read and hash-verify the C264→C265 contract; every C264 packet, functional,
distributional-pairing, channel, response, target, matching, residual,
release/runtime record; C263 locality proof; C254 Abel topology; C117 current
ancestry; and authenticated continuum/light-front sources.

## Frozen boundary

Use exactly the four `PROJECT_C117_NONLOCAL_PACKET_V1` functionals and their
normalized external packets. Preserve pair-first distributional semantics,
the C254 order of limits, CM-ground and U3 triplet channels, orientations,
normalizations, boundary/link/holonomy data, source/color/flavor order,
K9/K11/K13, C166, and Q0/Q1/Q2. Do not substitute local RI/SMOM insertions.

## Mutually exclusive plans

```text
C117NONLOCALTARGET1-A  all four continuum targets and physical matching ready
C117NONLOCALTARGET1-B  exact target-evaluation program ready; integrations remain
C117NONLOCALTARGET1-C  continuum current amplitude/source representation incomplete
C117NONLOCALTARGET1-D  Ward/ST or physical matching needs one additional condition
C117NONLOCALTARGET1-E  no lawful standard/physical matching path exists
C117NONLOCALTARGET1-F  source-qualified routes contradict
```

B/C/D are positive continuations. E/F require full blocker certification.

## Construction

For each functional define the exact continuum current amplitude, packet
measure and normalization, distributional pairing or channel projection,
regulator/test function, current renormalization, scale/flavor convention,
Ward/ST descendant, and standard/physical observable matching equation.

Implement two independent routes: direct continuum packet contraction and
spectral/finite-basis inverse reconstruction with controlled resolution and
Abel limits. Publish integrands/measures/prescriptions when numerical or
analytic evaluation remains. Never replace a missing target with zero or a
tree normalization.

Return four target values or exact unavailable records, covariance/enclosures,
packet/scale/Abel/scheme variations, Ward/ST residuals, physical matching
residuals, and separate K-resolution adapter inputs. Distinguish intermediate
scheme targets from physical observables.

## API, validation, continuation

Publish immutable safe-loading source, amplitude, integrand, pairing, channel,
target, Ward/ST, transport, physical-match, uncertainty, residual/release and
scope records with separate roots.

Run C250-C264 adjacent tests and C265 tests; two target routes; packet
normalization; distribution legality; Abel order; CM/color idempotence and
leakage; Ward/ST; scale/flavor transport; physical matching; uncertainty and
route parity; K separation; units/orientations/boundaries/holonomies; safe
loading; restart/sharding/paging/query order; two clean builds; protected
paths; and 384 live mutations.

Create one local commit, never push, issue exactly one evidence-driven next
contract and full prompt, atomically advance state, and continue. A complete
target proceeds to the finite-C43 adapter; an executable unevaluated program
gets the smallest evaluation continuation. Stop only for a certified real
blocker, exceptional infrastructure blocker, or PENNYLANE_PHYSICAL_ACTIVE.
