# C275/HQCDNONC117SLOT1 Codex Work Package

## Mission

Bind or rigorously reduce the composite non-C117 physical Hamiltonian input bundle required by the C274 K9/K11/K13 families: running coupling/masses, common-IR regulator, boundary/holonomy selection, non-C117 counterterms, finite-basis physical targets, and joint covariance. Preserve all four C117 coordinates unchanged.

## Baseline and authority

Start from the C274 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C274 package root:

```text
6bb3a76faaa0f38ca6943f59728762cf18a2a7182f6ce45d7ae317982f09f590
```

The sole prospective contract is `docs/next_level/c274_c275_hqcdnonc117slot1_continuation_contract.json`. Hash-verify it and audit C140–C219 physical-source, running, flavor, matching, boundary, counterterm, and covariance records; C220–C258 fixed-K operator evidence; C274 slot/family roots; and exact primary sources named by repository provenance.

## Boundary and plans

C274 closes symbolic Hermitian families `H_R(theta,c)=sum_owner H_owner,R(theta)+sum_i c_i O_C117,i,R`. Four `c_i` remain explicit by design. The composite `theta_nonC117` bundle is not numerically or physically bound.

Select exactly one:

```text
NONC117SLOT1-A  all non-C117 slots bind with authenticated joint covariance
NONC117SLOT1-B  maximal bundle closes with one exact slot class remaining
NONC117SLOT1-C  a narrower adapter/calculation is required
NONC117SLOT1-D  lawful physical-scheme alternatives remain separate
NONC117SLOT1-E  indispensable bundle is proven unavailable
NONC117SLOT1-F  source-qualified bindings contradict
```

A–D are positive continuations; E/F require blocker certification. No conditional record, marginal uncertainty, diagnostic fixture, or comparison value may be promoted to a physical default.

## Construction

For each slot publish source locator/hash, scheme/scale/flavor, running/threshold map, finite-basis adapter, units, K separation, boundary/link/holonomy class, counterterm ownership, target/holdout role, covariance and cross-correlations, availability, and roots. Demonstrate compatibility with every C274 owner and identify the maximal physically authenticated bundle. Keep absent values explicit and nonzero.

Use independent forward standard-to-finite-basis and reverse Hamiltonian-slot-to-source audits. Validate units, orientations, RG/threshold ordering, common-IR legality, topology/count-once, ST/BRST and boundary scope, covariance PSD semantics, K9/K11/K13 separation, and no circular calibration.

## API, validation, continuation

Publish immutable safe-loading slot, bundle, mapping, covariance, route, residual, release, mutation, and scope APIs with separate roots and JSON-only runtime. Run C250–C274 plus focused C275 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

Do not select C117 coefficients, construct a physical state prematurely, or claim activation. After one local commit, create exactly one next contract and prompt and atomically advance state. Return to Hamiltonian/state assembly only if the non-C117 bundle is sufficient; otherwise select the smallest missing slot adapter or physical input.

Report plan, slot closure and exact unavailable boundary, tests/mutations, root, commit, next job/object, and no-push/nonactivation status.
