# C210/HQCDMOMQCOND2 Codex Work Package

## Mission

Execute the authenticated C208 symmetric MOMq target condition over the C209
certified resolution-local wavepacket/projector adapter. Bind the symbolic
condition, guards, residuals, and error enclosures without asserting a physical
continuum value or an exact finite-C43 point. Validate, commit locally, generate
the single successor contract/prompt, atomically update persistent state, and
continue. Never push.

## Baseline and authority

```text
baseline: 653a452bee620d66f8323826237817f9539bace1
C209 package root: fb0abd5750ea3684ad44fc05512dbd4e0765c0da5e721bdc51da4735326d9654
C209 status: C209_C208_CERTIFIED_RESOLUTION_LOCAL_MOMQ_WAVEPACKET_LIMIT_ADAPTER_READY_NO_EXACT_FINITE_POINT
contract: docs/next_level/c209_c210_hqcdmomqcond2_continuation_contract.json
```

Read C43 finite-cell/HO conventions; C140 representability authority; C157
common-IR boundary; C168 adapter contract; C196-C209 qg/ST/MOMq authorities;
and the C208 source objects (2.1), (2.4)-(2.6), (3.5), (6.34), and (6.35).
Preserve all user/untracked work, C134/C157, C158 values, C166 graphs,
Q0/Q1/Q2, and C164-C209 authorities.

## Exact target condition

Bind without alteration the symmetric nonexceptional target
`p^2=q^2=r^2=-mu^2`, `r=-p-q`, `p.q=mu^2/2`; the six qg tensor channels;
inverse-Gram projection; channel 1; explicit linear-covariant-gauge alpha;
dimensional regularization; chiral limit; symbolic active Nf; and
`a=g^2/(16 pi^2)`.

Execute this condition separately for K9/K11/K13 using complete caller records.
Every result must retain the longitudinal-tail, omitted-HO-shell, quadrature,
invariant, and projector/Gram enclosures. Do not average resolutions, silently
extrapolate, insert a physical scale, default mu/alpha/Nf, or replace the
wavepacket condition by an invented exact point.

## Plans

Choose exactly one:

```text
MOMQCOND2-A — executable enclosed target MOMq condition ready;
MOMQCOND2-B — target evaluator ready but one smaller guard/certificate remains;
MOMQCOND2-C — Gram/projector execution incomplete;
MOMQCOND2-D — invariant/error propagation incomplete;
MOMQCOND2-E — common-IR/gauge/boundary compatibility incomplete;
MOMQCOND2-F — authenticated condition cannot lawfully act on the adapter;
MOMQCOND2-G — current-chain regression.
```

An underdetermined symbolic record is not a blocker while a stricter
caller-bound evaluator or enclosure remains constructible.

## Construction and validation

Implement preferably under `src/deuteron_wigner/bridge/hqcdmomqcond2/` with
immutable runtime metadata. Expose strict parameter validation, safe data-only
programs, invariant enclosures, nonexceptional and Gram guards, six-channel
projector action, channel-1 condition, gauge/scheme/common-IR checks,
resolution-local result records, convergence/nonexactness certificates,
release, handoff, isolation, mutations, and completeness. No eval, pickle,
callbacks, implicit defaults, or hidden numerical authority.

Require independent direct and reordered projector routes, channel/order
reversal, K9/K11/K13 holdouts, malformed-record rejection, boundary/holonomy
covariance, restart/order/sharding determinism, and error-widening monotonicity.
Use `allow_pickle=False` for all NumPy loads. Create complete `c210_*` evidence,
run focused and authoritative predecessor regressions, two clean builds, and at
least 384 live mutations over actual condition, guard, and enclosure roots.

## Continuation

If the enclosed target condition closes, return to the narrowest remaining
C197-ST-9 target-evaluation or comparison object. If one smaller executable
object remains, create exactly one continuation for it. Declare a blocker only
under the master schema after independent audits prove no lawful exact or
controlled-limit execution exists. Commit once locally, verify, generate/hash
the complete next prompt, increment state once, and continue immediately.
