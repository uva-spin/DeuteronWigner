# C268/HQCDC117STANDARDSIDE1 Codex Work Package

## Mission

Construct the source-qualified standard/physical-side nonlocal current amplitude and matching target for all four C267 packet functionals. Close the smallest missing side of the C267 matching residual without choosing unsupported packet coordinates or finite C43/C117 coefficients.

## Baseline and provenance

Start from the C267 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C267 package root:

```text
26bb80bdbf2e61983e70c581f53b21a172a5d812cf44279fca3071d0f18ed20e
```

The sole prospective contract is `docs/next_level/c267_c268_hqcdc117standardside1_continuation_contract.json`. Hash-verify it and read the complete C267 target/matching/uncertainty authority, C266 current and packet capsules, C264–C265 nonlocal functional records, C259–C263 scheme/tensor audits, C114–C117 source APIs, authenticated C43 sources, and applicable primary standard-current/matching sources. Never use model memory as scientific authority.

## Frozen authority and boundary

C267 closes four project-side functionals in the exact order `I2_density_projector`, `derivative_density`, `CM_ground`, `triplet_projected`, retaining ordered `J_qJ_q`, `J_qJ_g`, `J_gJ_q`, `J_gJ_g` terms, Q0, pair-first Abel limits, packet coordinates, K9/K11/K13, Ward/ST diagnostics, and symbolic PSD covariance. Its standard/physical side is unavailable, not zero. Preserve this boundary.

Select exactly one plan:

```text
C117STANDARDSIDE1-A  all four standard-side amplitudes and matching targets close executably
C117STANDARDSIDE1-B  a standard intermediate-scheme amplitude closes but physical matching data remain absent
C117STANDARDSIDE1-C  the required standard current/operator definition remains source-incomplete
C117STANDARDSIDE1-D  amplitudes close parametrically but a packet/scale prescription is the smallest next object
C117STANDARDSIDE1-E  the standard-side map is proven nonexistent
C117STANDARDSIDE1-F  independent source-qualified routes contradict
```

A–D are positive continuations. E/F require certified blocker procedure. Do not fabricate a physical state, target value, conversion, width, scale, or zero.

## Construction and independent routes

For each direction publish the standard operator/current source ID, scheme and scale, external-state definition, packet pairing, momentum measure and conservation, spin/helicity and ordered color tensors, regulator and limit ordering, boundary/link/holonomy data, Ward/ST descendant, standard-side amplitude, C267 project-side amplitude, exact matching residual, uncertainty ancestry, units, orientation, and roots. Keep source-side, intermediate-standard, and physical-data availability distinct.

Use two independent routes where authority permits: direct standard continuum current insertion paired with the C266 packet, and spectral/operator-basis reconstruction through the authenticated C259–C263 adapter. Check equality only on their common domain. Preserve parameterized algebraic or deterministic-quadrature results as such; do not claim numerical convergence without evidence.

Prove four-direction completeness, momentum/color/helicity closure, Hermitian reversal, packet normalization, distributional legality, pair-first Abel order, Ward/ST scope, scheme conversion boundaries, K separation, and correlated uncertainty without double counting.

## API, validation, and isolation

Publish an immutable safe-loading `deuteron_wigner.bridge.hqcdc117standardside1` API for standard amplitudes, matching targets/residuals, Ward/ST records, uncertainty, route comparison, residual frontier, release, isolation, mutations, verified loading, and separate roots. Runtime artifacts are JSON-only with `allow_pickle=false`.

Run C250–C267 adjacent tests, focused C268 tests, two deterministic builds, and at least 384 live mutations. Validate hashes, source ownership, orientations/units, all four directions/products, Hermiticity, BRST/ST, regulator and Abel order, boundaries/links/holonomies, K9/K11/K13, covariance semantics, safe loading, restart/sharding/paging/query order, protected paths, C166 nonmutation, Q0/Q1/Q2 nonmutation, and no push.

Do not mutate unrelated user work, C166, Q0/Q1/Q2, or quarantined evidence. Do not select finite coefficients or claim physical activation.

## Completion and continuation

Create one local completion commit, never push, and generate exactly one next prospective contract and full prompt. Advance to coefficient solution only if four full-rank matching targets are authenticated and executable; otherwise select the smallest missing standard-current, conversion, physical-input, or packet-prescription object. Atomically update `AUTOPILOT_STATE.json` and continue.

Report the selected plan, exact standard-side closure and unavailable boundary, tests/mutations, package root, local commit, next job/object, and no-push/nonactivation status.
