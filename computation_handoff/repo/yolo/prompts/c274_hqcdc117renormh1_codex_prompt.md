# C274/HQCDC117RENORMH1 Codex Work Package

## Mission

Assemble the complete Hermitian K9/K11/K13 renormalized Hamiltonian family over four explicit unresolved C117 coordinates, binding every non-C117 physical slot to authenticated authority or preserving it as an explicit missing slot. Do not select C117 coefficients or promote diagnostics.

## Baseline and authority

Start from the C273 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C273 package root:

```text
86e675df46233e9c19d9dc4f305c62d64104097feb7e471c71632548481a69b5
```

The sole prospective contract is `docs/next_level/c273_c274_hqcdc117renormh1_continuation_contract.json`. Hash-verify it and audit C128–C273, C166 ownership graphs, C213–C219 physical-input mappings, C242–C258 finite current contributions, C259–C273 C117 authority, and Q0/Q1/Q2 read-only boundaries.

## Boundary and plans

C273 proves that constructing a state with arbitrarily selected C117 coefficients and using that state to justify those coefficients is circular. It supplies only a conditional state/resolvent schema. C274 owns Hamiltonian-family assembly, not state calibration.

Select exactly one:

```text
C117RENORMH1-A  complete conditional K9/K11/K13 Hamiltonian family closes with only four C117 coordinates unresolved
C117RENORMH1-B  family closes but one authenticated non-C117 physical slot remains symbolic
C117RENORMH1-C  a narrower operator/component assembly remains missing
C117RENORMH1-D  multiple lawful scheme families require separate Hamiltonians
C117RENORMH1-E  required Hermitian assembly is proven nonexistent
C117RENORMH1-F  source-qualified assemblies contradict
```

A–D are positive continuations; E/F require blocker certification. Missing slots remain explicit and nonzero.

## Construction

For each resolution publish basis/root ancestry, every free/interaction/contact/counterterm/boundary owner, parameter and units, coefficient source, C117 insertion matrices, count-once keys, CM/color blocks, Hermitian reversals, boundary/link/holonomy classes, and the family `H_R(c)=H_nonC117,R+sum_i c_i O_i,R`. Separate authenticated physical slots, conditional slots, and absent slots. Provide sparse/matrix-free application, derivatives, serialization, and K holdout crosswalks.

Validate two independent assembly routes, Hermiticity, units, source ordering, topology/count-once, gauge/BRST/ST scope, boundary and zero-mode handling, CM/color leakage, K separation, parameter derivatives, safe loading, restart/sharding/paging/query order, and two deterministic builds.

## API and continuation

Publish immutable safe-loading family, slot audit, insertion, route, uncertainty, residual, release, mutation, and scope APIs with separate roots and JSON-only runtime. Run C250–C273 plus focused C274 tests and at least 384 mutations. Preserve C166 and Q0/Q1/Q2; never push.

Do not select C117 coefficients, diagonalize a conditional family as physical, or claim activation. After one local commit, generate exactly one next contract and prompt and atomically advance state. Return to state construction only if the Hamiltonian family is complete; otherwise select the smallest missing operator or physical slot.

Report plan, assembly/slot closure, tests/mutations, package root, commit, next job/object, and no-push/nonactivation status.
