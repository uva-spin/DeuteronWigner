# C258/HQCDRIQUARKFIXEDKV2CURRENTTARGETAUDIT1 Codex Work Package

Mission: perform the independent scientific and provenance audits required
after C257 found no authenticated current-specific subtraction target capsule.
Baseline `9954517b4b40d063632570cedc09c9edc4eb65a6`; frozen C257 root
`7f31c16923d082fb3bb4058ec07a066dd9a83542c5a64794b197618405e8204c`.
Contract:
`docs/next_level/c257_c258_hqcdriquarkfixedkv2currenttargetaudit1_continuation_contract.json`.

Verify all hashes and roots. Independently audit (A) the mathematical role of
the four C117 directions and whether any source-side identity can fix them,
(B) physical renormalization/matching conditions in C150-C168 and whether
they lawfully induce current-specific rows, and (C) provenance across the
repository, all Git refs, authenticated local archives, and any exact official
source locator already named by project authority. Audit whether the target
can be derived from the authenticated C43 action without importing a physical
choice. Preserve scheme, scale, gauge, regulator, test function, channels,
units, order of limits, and claim tier.

Choose exactly one plan: discover and authenticate a capsule; identify a
lawful exact derivation/acquisition job; certify the smallest indispensable
missing authority after all routes are exhausted; certify contradiction; or
regression. Absence, rank deficiency, and difficulty alone are not blockers.
Never invent a condition, source, coefficient, null representative, scheme,
scale, regulator, or physical value.

Implement immutable safe APIs under
`src/deuteron_wigner/bridge/hqcdriquarkfixedkv2currenttargetaudit1/` exposing
the two independent scientific audits, provenance audit, route-exhaustion
ledger, derivability decision, exact smallest missing object, compatibility
checks, residual/release records, and separated roots. If and only if the
master blocker criteria are satisfied, generate the schema-certified blocker
packet and terminal state; otherwise name exactly one narrower continuation.

Generate deterministic `c258_*` evidence, two clean builds, safe reload,
focused/adjacent regressions, and at least 384 mutations. Preserve C134,
C157, C166, and Q0/Q1/Q2. Commit locally once, never push, publish exactly one
successor contract/full prompt when continuing, atomically advance state, and
continue. Stop only for a certified blocker or `PENNYLANE_PHYSICAL_ACTIVE`.
