# C256/HQCDRIQUARKFIXEDKV2CURRENTSOURCE1 Codex Work Package

Mission: identify and authenticate a current-specific target observable,
projector, scheme, scale, and regulator capsule capable of supplying condition
rows for the four C117 instantaneous-current complement subtraction directions.
Baseline `dd85893317772e188b22d78bfde0a70f41036895`; frozen C255 root
`62997585de038f452232f4a9c90a7e7a8ce85375e10261d7f1a6dc92818157d1`.
The prospective contract is
`docs/next_level/c255_c256_hqcdriquarkfixedkv2currentsource1_continuation_contract.json`.

Verify baseline, contract hash, prompt hash, C255 root, and every upstream
root before construction. Read C117, C150-C158, C168, and C220-C255 through
their verified public APIs and authenticated provenance. Preserve graph
direction, finite-basis and target schemes, scale, gauge, common-IR family,
regulator/test-function capsule, flavor/color channel, source/sink order,
units, covariance, order of limits, and physical/nonphysical claim tier.

Choose exactly one mutually exclusive plan: an authenticated source supplies
a complete four-direction target capsule; it supplies a strict subset; it
supplies only a conditional caller-bound capsule; no applicable source is
present but a narrower exact acquisition/derivation frontier remains; an
exact contradiction exists; or regression. Never reinterpret the C150-C158
standard matching quantities as graph-specific conditions, select a preferred
scheme/scale/regulator, set an unresolved coefficient to zero, or fit from
unrelated fixtures. Rank deficiency is not a blocker while an exact frontier
remains.

Implement immutable safe APIs under
`src/deuteron_wigner/bridge/hqcdriquarkfixedkv2currentsource1/`. Expose source
inventory and provenance, target-capsule schema, source-qualified candidate
records, direction coverage, compatibility and rejection records, caller
validation, route parity, exact residual/release records, and separated roots.
Prove deterministic serialization, source ownership, units/orientations,
Hermiticity scope, restart/shard/page/query stability, protected nonmutation,
and fail-closed unavailable semantics.

Generate deterministic `c256_*` evidence, two clean builds, focused and
adjacent regressions, safe reload, and at least 384 live mutations. Preserve
C134, C157, C166, and Q0/Q1/Q2. Commit exactly once locally, never push,
publish exactly one successor contract and full prompt, atomically advance
state, and continue. Stop only for a schema-certified real blocker, an
exceptional infrastructure blocker, or `PENNYLANE_PHYSICAL_ACTIVE`.
