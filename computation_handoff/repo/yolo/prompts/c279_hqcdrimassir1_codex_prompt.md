# C279/HQCDRIMASSIR1 Codex Work Package

## Mission

Bind, evaluate, or rigorously reduce authenticated `C157-COMMON-IR-NUMERIC-RECORD-V1` records for `SIGNED_QUARK_MASS` in RI/SMOM, separately at K9, K11, and K13, for the C278 symbolic state family.

## Baseline and authority

Start from the C278 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C278 package root:

```text
61406fb610302b90150db398ab2d57c15acc4d0cfcab27c1305e5f56c32585ef
```

The sole prospective contract is `docs/next_level/c278_c279_hqcdrimassir1_continuation_contract.json`; hash-verify it. Audit C153–C161 common-IR schemas, finite-basis and target coefficient programs, matching grids, perturbative controls and flavor covariance; C164–C168 exact RI/SMOM signed-mass target locators/dependencies; C276–C278 conventions and state roots; and authenticated local source artifacts named by repository provenance.

## Boundary and plans

C278 closes symbolic state geometry but identifies C157 numerical common-IR authority as the first executable gate. Select exactly one:

```text
RIMASSIR1-A  authenticated numerical records close at all three K
RIMASSIR1-B  guarded record family closes pending one named coefficient/input
RIMASSIR1-C  a narrower source expression, regulator, or scale record is required
RIMASSIR1-D  lawful common-IR families or parameter branches remain separate
RIMASSIR1-E  indispensable numerical authority is proven unavailable
RIMASSIR1-F  authenticated common-IR records contradict
```

A–D are positive continuations; E/F require a newly schema-certified blocker. Never promote fixtures, invent scales, merge rho with mu, average K or flavor, or substitute absent coefficients by zero.

## Construction

For every record bind its exact schema/root, common-IR and family IDs, quantity, resolution, C153 matching record/root, finite-basis and RI/SMOM schemes, active-`N_f` record, external flavor, common external state and invariants, rho and mu with units and independence, gauge and light-front pole prescription, zero-mode/boundary/mass-regulator policies, order of limits, exactly one parameter-record or fixture branch, precision/solver and variation-family records, claim tier, provenance, and no-default/no-physical flags.

Validate direct finite-basis and continuum projections, common-IR cancellation, logarithmic rho derivative, variation family, inverse round trip, first omitted order, positive bracket, sign reversal, flavor covariance and K-separated uncertainty. If executable target expressions remain absent, isolate their smallest exact source object without fabricating numeric values.

## API, validation, continuation

Publish immutable safe-loading record, schema, coefficient, cancellation, variation, remainder, covariance, residual, release, mutation, and isolation APIs with JSON-only runtime. Run C250–C278 plus focused C279 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order checks, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

After exactly one local commit, create exactly one next prospective contract and full prompt and atomically advance `AUTOPILOT_STATE.json`. If common-IR records close, return to state binding/evaluation; otherwise continue with the smallest exact missing expression or input.

Report plan, record closure boundary, tests/mutations, root, commit, next job/object, and no-push/nonactivation status.
