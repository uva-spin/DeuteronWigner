# C273/HQCDC117PHYSSTATE1 Codex Work Package

## Mission

Construct or recover an authenticated renormalized K9 deuteron eigenstate and pole-subtracted reduced-resolvent response bundle, with K11/K13 holdouts, sufficient to evaluate the four C272 tensor-DIS `b1` sensitivities. Do not promote validation fixtures or choose unresolved C117 coefficients merely to create a state.

## Baseline and authority

Start from the C272 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C272 package root:

```text
cdebc5a5cfb44709b3f3009fa12b599c35e032700a55fd75aab0cfa5532c004c
```

The sole prospective contract is `docs/next_level/c272_c273_hqcdc117physstate1_continuation_contract.json`. Hash-verify it and audit C131–C272, all physical-input and Hamiltonian-slot records, C166 dependency graphs, C259–C272 renormalization authority, and Q0/Q1/Q2 read-only interfaces. Repository diagnostic states, synthetic fixtures, caller-supplied resolvents, and unrenormalized Hamiltonians are not physical authority.

## Frozen boundary and plans

C272 closes four exact direct/Feynman–Hellmann derivative programs. Their evaluation requires a renormalized Hamiltonian, normalized deuteron eigenstate, projected pole-removed resolvent, operator insertions, and cross-resolution holdouts. None was authenticated at C272.

Select exactly one:

```text
C117PHYSSTATE1-A  complete physical K9 state/response bundle and K11/K13 holdouts close
C117PHYSSTATE1-B  renormalized Hamiltonian closes but physical state calibration remains next
C117PHYSSTATE1-C  state construction is blocked by a narrower unresolved Hamiltonian/input slot
C117PHYSSTATE1-D  conditional state family closes over explicit unresolved coordinates
C117PHYSSTATE1-E  required physical state bundle is proven nonexistent
C117PHYSSTATE1-F  independent source-qualified constructions contradict
```

A–D are positive continuations; E/F require blocker certification. A conditional family is progress but cannot be labeled physical or used to select its own renormalization coordinates.

## Construction

Publish Hamiltonian ancestry and parameter ownership, Hermitian finite-basis operators at K9/K11/K13, state quantum numbers and normalization, spectral isolation and phase convention, pole projector, reduced resolvent, derivative-state equations, C117 insertion maps, boundary/link/holonomy classes, units, uncertainties, and roots. Prove count-once ownership, CM/color-sector purity, Hermiticity, state tracking, pole removal, resolvent identities, Feynman–Hellmann parity, and K holdout semantics.

Use independent direct diagonalization and matrix-free/Krylov or spectral-projector routes when values are authorized. Never infer physicality from numerical convergence alone.

## API, validation, continuation

Publish immutable safe-loading Hamiltonian/state/resolvent/response/holdout/uncertainty/residual/release/scope APIs with separate roots and JSON-only runtime. Run C250–C272 plus focused C273 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order, protected paths, C166 and Q0/Q1/Q2 nonmutation, and no push.

Do not select C117 coefficients or claim activation. After one local commit, create exactly one next contract and prompt, atomically advance state, and continue. Return to sensitivity evaluation only if the state bundle is genuinely authenticated; otherwise select the smallest missing Hamiltonian slot, physical input, coefficient, state-calibration, or response object.

Report plan, state-bundle closure and physical/conditional boundary, tests/mutations, package root, commit, next job/object, and no-push/nonactivation status.
