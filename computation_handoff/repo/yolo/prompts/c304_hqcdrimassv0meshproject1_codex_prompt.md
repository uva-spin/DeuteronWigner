# C304/HQCDRIMASSV0MESHPROJECT1 Codex Work Package

Reconstruct the clipped 41×41 C293 `potSU3.ps` vector mesh and perform the normalized C301 class-function Gram projection with digitization enclosure.

Start from C303 in `AUTOPILOT_STATE.json`; frozen root `56d663455a989e534f9a2072bfc46dd840a0bbc886b77478a6387ce70a5ca300`. Verify/load C303 and use its exact V0 AST, affine u/v/z axes, nominal grid, and hidden-line warning. Decode gnuplot 3.7 hidden-line polylines by projected grid connectivity, reconstruct visible samples, use source symmetries/interpolation only with explicit enclosure, and independently check against direct regulated AST sums over increasing cutoffs. Perform the weighted constant/CHI8/RE_TF3 Gram solve under the C295 normalized measure. Results are reduced-model benchmarks only, never C43 coefficients.

Choose one `RIMASSV0MESHPROJECT1-A` through `-F`; A-D continue and E/F require certified blocker protocol. Publish mesh decoder, axis parity, visibility map, AST convergence, Gram matrix, coefficient enclosure, residual, release and isolation APIs. Run cumulative tests, 384 mutations, deterministic builds, safe reload, protected paths and quantum nonmutation. Commit once, never push, generate one next contract/prompt, atomically advance state, and continue.
