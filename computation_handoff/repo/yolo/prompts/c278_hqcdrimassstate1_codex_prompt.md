# C278/HQCDRIMASSSTATE1 Codex Work Package

## Mission

Bind or rigorously reduce authenticated complete `PROJECT_RI_SMOM_SIGNED_MASS_COMMON_STATE_IR_V1` parameter capsules at K9, K11, and K13 for the C277 signed-mass-projected self-energy program.

## Baseline and authority

Start from the C277 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C277 package root:

```text
b57cd76303f9ba0a7002bc97baca44eabb8de68529920682103069bc66bca0f1
```

The sole prospective contract is `docs/next_level/c277_c278_hqcdrimassstate1_continuation_contract.json`; hash-verify it. Audit C140–C168 external-state, source/sink, projector, running, flavor, common-IR, matching and target records; C169–C215 boundary, holonomy and physical-input maps; C216–C277 quark-kernel and signed-mass conventions; and authenticated source artifacts named by repository provenance.

## Boundary and plans

C277 establishes that the reusable kernel program is ready but no complete authenticated state instance exists. Select exactly one:

```text
RIMASSSTATE1-A  complete three-resolution state capsules bind
RIMASSSTATE1-B  maximal state family binds with one named conditional coordinate
RIMASSSTATE1-C  a narrower kinematic, regulator, flavor, or boundary input is required
RIMASSSTATE1-D  lawful state/scheme branches remain separate
RIMASSSTATE1-E  indispensable state authority is proven unavailable
RIMASSSTATE1-F  authenticated state records contradict
```

A–D are positive continuations; E/F require a newly schema-certified blocker. Do not promote diagnostic fixtures, select C117 coefficients, infer a physical scale, or replace missing fields by zero.

## Construction

For each K bind record ID, incoming/outgoing momenta satisfying symmetric nonexceptional kinematics, virtuality and scale, finite-cell/HO regulator coordinates, C43 and target gauges, PV/Q0 prescription, boundary class, residual-link and holonomy capsule, active `N_f`, external flavor, signed `m_R`, independent `m_R^2`, source/sink order, common-IR ID, units, uncertainty/covariance, provenance, and no-default/nonphysical flags. Publish exact validation and compatibility with C149/C150/C153/C164/C276/C277.

Use independent forward source-to-projector and reverse self-energy-slot-to-source audits. Check dimensions, momentum identities, sign reversal, flavor/threshold ordering, gauge and pole compatibility, boundary/holonomy scope, common-IR legality, K separation, covariance semantics, and absence of circular calibration.

## API, validation, continuation

Publish immutable safe-loading state schema, capsule, kinematics, regulator, flavor, mapping, covariance, residual, release, mutation, and isolation APIs with JSON-only runtime. Run C250–C277 plus focused C278 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order checks, protected paths, C166/Q0/Q1/Q2 nonmutation, and no push.

After exactly one local commit, create exactly one next prospective contract and full prompt and atomically advance `AUTOPILOT_STATE.json`. If state capsules close, evaluate the C277 mass projection; otherwise continue with the smallest exact missing input.

Report plan, capsule closure boundary, tests/mutations, root, commit, next job/object, and no-push/nonactivation status.
