# C311/HQCDRIMASSEPSLIMIT1 Codex Work Package

Extrapolate the separately tail-subtracted `CHI8` and `RE_TF3` full-Gram finite remainders to epsilon zero with regulator covariance.

Start from exact baseline `c088c1ff4b37257ba695604af78e0a97968a9d10` and frozen C310 package root `b553660e9822e8af80b885a4e236eccbfc544ee45dbdec6d8c13107d8c4be499`. The prospective authority is `docs/next_level/c310_c311_hqcdrimassepslimit1_continuation_contract.json`, derived from the committed C310 residual frontier. Verify the contract and load C310 through its public safe-loading API before implementation.

The frozen authority is: C308 owns only the symmetric center-mode subtraction; C310 owns distinct, source-qualified outward logarithmic-tail enclosures for `CHI8` and `RE_TF3`; the three fixed-epsilon remainder intervals and their correlated tail covariance are authoritative. Preserve C303-C310 roots read-only. Do not refit or merge the two shape-tail owners merely to improve an epsilon extrapolation.

Choose exactly one plan `RIMASSEPSLIMIT1-A` through `-F`. Plans A-D are positive lawful routes using source-qualified regulator asymptotics, multiple epsilon windows, and interval/covariance propagation. Plans E/F are fail-closed outcomes requiring the master blocker protocol. A rank-deficient covariance or an initially insufficient epsilon range is not a blocker while lawful higher-resolution fixed-epsilon evaluations remain.

Construct at least one primary extrapolation and one independent lawful cross-check. Extend the fixed-epsilon scan toward the wall only with the authenticated C303 AST, corrected C295 measure, C309 full-Gram conventions, and C310 separate tail subtraction. Publish epsilon windows, regulator variables, fit families, resolution and quadrature settings, correlated covariance, route residuals, and outward zero-epsilon enclosures for each channel. Test leave-one-window-out stability, alternative source-qualified powers/logarithms, mode order, cutoff order, quadrature resolution, and covariance perturbations. Preserve the limit order: first subtract `N` tails at every fixed epsilon, then extrapolate epsilon. Never guess an exact coefficient, collapse an interval to a point, impose plot normalization on a shape channel, or claim C43 matching.

Publish immutable APIs for the input freeze, epsilon scan, extrapolation models, covariance, channel-specific limit enclosures, stability certificate, residual frontier, release, safe reload, roots, and isolation guard. The package must remain nonphysical: no physical mass coefficient, C117 coordinate choice, boundary ensemble, Hamiltonian, counterterm/null representative, spectrum, TMD, PennyLane activation, or production claim is authorized. Preserve the C134 quarantine, inherited C157 boundary, C166 graphs, protected quantum worktrees, Q0/Q1/Q2, and the user's `handoff/ROADMAP.md` modification.

Validate the exact baseline/upstream roots, public reload, safe loading, source ownership, units and orientations, independent-route parity, regulator and limit order, restart/query-order stability, two deterministic clean builds, protected paths, quantum nonmutation, cumulative C250-C311 tests, and at least 384 focused live mutations. Repair ordinary numerical or software failures autonomously. Never push.

Complete C311 with exactly one local commit. Read the committed residual frontier and create exactly one prospective continuation contract and one full next-job prompt. Hash them, atomically advance `/Users/dustin/work/DeuteronWigner-yolo/state/AUTOPILOT_STATE.json` by exactly one revision, and continue immediately under `/Users/dustin/work/DeuteronWigner-yolo/PERSISTENT_YOLO_MASTER_PROMPT.md`. Stop only for `REAL_MATH_PHYSICS_BLOCKER`, `INFRASTRUCTURE_BLOCKER`, or `PENNYLANE_PHYSICAL_ACTIVE` under the certified protocols.
