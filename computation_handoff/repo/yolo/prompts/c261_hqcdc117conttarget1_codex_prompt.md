# C261/HQCDC117CONTTARGET1 Codex Work Package

## Mission

Construct the continuum target and standard-conversion authority for the exact
C260 `PROJECT_C117_RI_SMOM_V1` scheme: projected renormalized amplitudes,
RI/SMOM-to-MSbar conversion/mixing, anomalous-dimension and step-scaling
semantics, scale/flavor labels, evanescent-scheme dependence, and uncertainty.

Do not solve any finite-C43 C117 coefficient in C261.

## Baseline and provenance

Start from the C260 completion commit recorded in AUTOPILOT_STATE.json. Frozen
C260 package root:

```text
a1bc4fa7bcf65695fcbec7bbfa89370180671d41343eb3e8d7442e0b1a5b4c83
```

Read completely and hash-verify the C260→C261 continuation contract, all C260
operator, kinematics, projector, tree-target, mixing/evanescent, conversion,
adapter, release and runtime manifests, and the C259 primary-source corpus.
Preserve the human-authorized recovery policy and historical C258 blocker.

## Frozen authority

Preserve exactly:

```text
scheme: PROJECT_C117_RI_SMOM_V1
gauge: Landau xi=0
kinematics: Euclidean p1^2=p2^2=(p1-p2)^2=mu^2 > 0
operator order: I2_density_projector, derivative_density, CM_ground,
                triplet_projected
tree response: I4
rank: 4
gamma5: NDR anticommuting, nonsinglet scope
standard target: MSbar-NDR with identical physical/evanescent ordering
```

Keep flavors, color/source order, EOM, BRST-exact, evanescent, boundary,
link/holonomy, P0/Q0, orientation, units, Abel topology, K9/K11/K13, C166 and
Q0/Q1/Q2 separate. Never push.

## Strict claim boundary

C261 may publish exact symbolic or perturbative continuum target and conversion
records only when derived from authenticated sources plus the C260 scheme
definition. It must distinguish published generic RI/SMOM architecture from
project-owned C117 calculations. No paper is claimed to tabulate the C117
finite-C43 coefficients.

Unsupported loop entries remain `UNAVAILABLE_NOT_ZERO`. Do not infer a zero
from absence, tree diagonality, symmetry of another operator basis, or a
related published scheme. No physical observable target, finite-C43 solve,
Hamiltonian, PennyLane object, spectrum, or TMD is created.

## Mutually exclusive plans

Select exactly one:

```text
C117CONTTARGET1-A  continuum target/conversion authority complete at declared order
C117CONTTARGET1-B  exact symbolic conversion program ready; numerical loop integration remains
C117CONTTARGET1-C  additional evanescent/EOM/BRST mixing partners required
C117CONTTARGET1-D  standard conversion unavailable but a source-qualified alternative scheme remains
C117CONTTARGET1-E  source-qualified continuum routes contradict after convention audit
C117CONTTARGET1-F  no lawful standard/physical matching path exists
```

Partial perturbative order or symbolic loop integrals are positive progress
when executable and source-qualified. They are not a blocker while an exact
calculation continuation remains.

## Primary-source and derivation requirements

Use the hash-locked C259 archives, especially hep-lat/9411010, 0901.2599,
1104.4948, 1109.1223, hep-lat/9902030, and the light-front counterterm sources.
Extract exact TeX equation locators for projector renormalization, symmetric
kinematics, mixing conversion, evanescent definitions, step scaling and Ward
comparisons. Acquire an additional official primary source only if an exact
missing convention or calculation requires it; hash every byte.

Define the regularized amputated C117 Green-function tensor and all physical,
EOM, BRST-exact and evanescent components. Construct the projected bare and
renormalized matrices in the exact C260 ordering. Publish tree and loop orders
separately.

Derive or implement:

```text
Z_RISMOM from four C260 projector conditions;
Z_MSbar in the identical physical/evanescent convention;
C_MSbar<-RISMOM = Z_MSbar Z_RISMOM^-1;
projected target vector/matrix at named mu and nf;
anomalous-dimension convention and RG equation;
step-scaling composition Sigma(mu3,mu2) Sigma(mu2,mu1)=Sigma(mu3,mu1);
scale reversal/inversion;
threshold-label interface;
perturbative truncation and scheme-variation uncertainty.
```

Perform two independent routes: direct projected diagrams/integrals and an
independent counterterm/RG or basis-transformation route. Audit signs, operator
orientation, D-dimensional algebra, gamma5, evanescent finite subtraction,
gauge parameter, color order, flavor routing, and normalization.

If exact C117 loop integrals are not yet evaluable, publish an executable
symbolic integral/reduction program with complete measure, denominators,
numerators, prescriptions, projectors, subtraction operator and output schema,
then select plan B and create the smallest calculation continuation. Never
fill an unevaluated integral from model memory.

## Public API and roots

Provide immutable safe-loading records for source locators, continuum tensor
basis, diagram/integral inventory, projected amplitudes, Z matrices,
conversion matrix or executable symbolic program, anomalous dimensions,
step-scaling, Ward/ST diagnostics, scheme variants, uncertainty, standard
target, release and residual frontier. Separate every root and reject stale
runtime data without pickle.

## Validation

Run all C250-C260 adjacent tests and C261 tests; primary-source hashes and TeX
locators; two diagram/integral inventories; two conversion routes; UV-pole
cancellation; gauge and Ward/ST scope; evanescent-basis changes; matrix
inversion and RG composition; scale reversal; flavor/threshold separation;
units/orientations; boundary/link/holonomy and Abel nonconflation; K9/K11/K13
nonconsumption; safe loading; restart/sharding/paging/query-order stability;
two deterministic clean builds; protected-path checks; and at least 384 live
mutations. Repair ordinary failures autonomously.

Create exactly one local completion commit. Never push.

## Continuation

If the continuum authority is complete, the next job is
`C262/HQCDC117C43ADAPTER1` to construct separate K9/K11/K13 finite-cell/HO
matrices and right-hand sides. If C261 produces an exact symbolic program but
not evaluated loop results, select the smallest evidence-driven C261-calculation
continuation first. Do not jump to coefficient solving.

After completion create exactly one prospective contract and full prompt,
atomically advance AUTOPILOT_STATE.json, and continue immediately. Stop only
for a newly schema-certified real math/physics blocker, exceptional
infrastructure blocker, or PENNYLANE_PHYSICAL_ACTIVE.
