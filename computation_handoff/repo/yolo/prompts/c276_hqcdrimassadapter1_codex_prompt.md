# C276/HQCDRIMASSADAPTER1 Codex Work Package

## Mission

Construct or rigorously reduce the exact C166 request `C166-ACQUIRE-C165-MISSING-C164-LOC-TGT-SIGNED_QUARK_MASS-RI_SMOM-2`: the RI/SMOM signed-quark-mass C43 gauge/regulator-changing finite-basis adapter at K9, K11, and K13. This is the first non-C117 adapter edge identified by C275; preserve all C117 coordinates and every later adapter request.

## Baseline and authority

Start from the C275 completion commit recorded in `AUTOPILOT_STATE.json`. Frozen C275 package root:

```text
6daab8eb3cdc5d6006047c1203e5d68ca9e151e31b3d14df7be4a35611aa01fc
```

The sole prospective contract is `docs/next_level/c275_c276_hqcdrimassadapter1_continuation_contract.json`. Hash-verify it. Audit C154–C168 running, signed-mass, local-target, scheme, gauge, regulator, flavor and uncertainty records; C215 ordered adapter authority; C250–C275 fixed-K and Hamiltonian-slot evidence; and exact primary sources already named by repository provenance. Do not infer an unavailable coefficient from labels or neighboring requests.

## Boundary and plans

C275 establishes that all six composite non-C117 slot classes remain conditional and that the ordered first missing non-C117 edge is request 2, signed quark mass in RI/SMOM. Select exactly one:

```text
RIMASSADAPTER1-A  exact authenticated adapter closes at all three K
RIMASSADAPTER1-B  symbolic executable adapter closes pending named numerical input
RIMASSADAPTER1-C  a narrower source/reduction calculation is required
RIMASSADAPTER1-D  lawful scheme or orientation branches remain separate
RIMASSADAPTER1-E  indispensable adapter authority is proven unavailable
RIMASSADAPTER1-F  source-qualified adapter records contradict
```

A–D are positive continuations. E/F require a newly schema-certified blocker. Never replace missing signed-mass, regulator, gauge, or covariance inputs by zero or by a comparison value.

## Construction

Publish the signed-mass convention and orientation, RI/SMOM projector and symmetric nonexceptional kinematics, source/target schemes and scales, gauge and regulator map, flavor/threshold ordering, C43 finite-basis coordinates, K9/K11/K13 adapters, units, uncertainty and cross-covariance semantics, provenance, and separate roots. Demonstrate the forward standard-mass-to-finite-basis map and an independent reverse C274 Hamiltonian-slot-to-source audit. Check signs, dimensions, normalization, RG ordering, finite-basis orientation, common-IR legality, and count-once ownership.

Keep the later MOMq quark-field, transverse-gluon, qg-vertex, and coupling requests ordered but untouched. Keep request 1's C117 frontier separate. Do not select finite C117 coefficients or claim a physical Hamiltonian/eigenstate.

## API, validation, continuation

Publish immutable safe-loading source, convention, projector, adapter, K-family, mapping-audit, covariance, residual, release, mutation, and isolation APIs with JSON-only runtime. Run C250–C275 plus focused C276 tests, at least 384 mutations, two deterministic builds, reload/restart/sharding/paging/query-order checks, protected-path checks, C166/Q0/Q1/Q2 nonmutation, and no push.

After exactly one local commit, create exactly one next prospective contract and full prompt and atomically advance `AUTOPILOT_STATE.json`. If the adapter closes, return to the ordered non-C117 frontier; otherwise continue with the smallest exact source or calculation edge.

Report selected plan, signed-mass convention and closure boundary, tests/mutations, package root, commit, next job/object, and no-push/nonactivation status.
