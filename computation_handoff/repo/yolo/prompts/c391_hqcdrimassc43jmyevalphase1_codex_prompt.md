# C391/HQCDRIMASSC43JMYEVALPHASE1 Codex Work Package

## Title

**Phase-level executable C43/JMY distribution and Laurent authority: exact
d-dimensional transverse Fourier–Bessel measurement kernels, source-owned
DRqq/FRqq regular-plus decomposition, first-node execution, all 16 grouped
real-emission Laurent coefficients, real/virtual and separator identities,
finite remainders and positive scale brackets, covariance and holdouts, and
fail-closed handoff to physical finite-basis/continuum matching**

## Human-authorized transition

This is a prospective phase-package replacement for the uncommitted narrow
C391 successor selected by C390.

Freeze the accepted scientific parent:

```text
C390 job:
C390/HQCDRIMASSC43JMYGROUPEVAL5

C390 completion commit:
34b002948c0a501c4d59a10d2d00bca292e227b8

C390 package root:
7748f90ead890c2a207e9117e95627104c33677213466a90f1eff14c4ba309ad

C390 status:
C390_GROUP_LAURENT_EVALUATION_FAIL_CLOSED_TRANSVERSE_FOURIER_AND_REGULAR_PLUS_EXECUTOR_REQUIRED

controller revision at checkpoint:
189

activation gate:
NOT_READY
```

The C390-generated narrow successor, its continuation contract, and its full
prompt remain immutable historical handoff evidence. Do not delete, amend, or
misrepresent them.

The new phase contract must state that its Stage A is exactly the C390
frontier:

```text
C390-C43-JMY-TRANSVERSE-FOURIER-REGULAR-PLUS-EVALUATOR
```

with required object:

```text
derive executable d-dimensional transverse Fourier-Bessel measurement kernels
and the source-owned DRqq FRqq regular-plus decomposition
```

No C391 scientific implementation or package root existed at the checkpoint.

Create one local phase completion commit. Never push.

---

# 1. Freeze C390 and the historical C391 handoff

Before changing tracked files:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "34b002948c0a501c4d59a10d2d00bca292e227b8"
```

Read and hash-verify:

```text
docs/next_level/c390_implementation_report.md
docs/next_level/c390_package_root_manifest.json
docs/next_level/c390_release_manifest.json
docs/next_level/c390_release_contract.json
docs/next_level/c390_evaluation_gate_manifest.json
docs/next_level/c390_residual_frontier_manifest.json
docs/next_level/c390_validation_manifest.json
docs/next_level/c390_mutation_report.json

the exact C390-generated C391 continuation contract in the archived
pre-phase controller state;

the exact C390-generated C391 prompt in the archived pre-phase controller
state;

all C389 source, group, corrected-dispatch, AST, cut, and ownership roots named
by C390.
```

Freeze:

```text
16 grouped terms;
first node DR.qq;
regulated_cut_phase_space_integral dispatch;
distribution_test_action measurement role;
transverse-kernel source argument i*bT_dot_kT;
0 accepted Laurent coefficients;
0 UV/IR/analytic/finite entries;
no separator cancellation;
no physical mass/coupling/scale/state;
384 C390 mutations;
C390 package root.
```

Preserve all unrelated user changes, protected worktrees, C134 quarantine,
inherited C157 boundary, C166 graphs, Q0/Q1/Q2, and no-push rules.

Create:

```text
docs/phases/c391_jmy_eval/c391_phase_input_freeze.json
docs/phases/c391_jmy_eval/c391_historical_c391_handoff.json
docs/phases/c391_jmy_eval/c391_authority_preservation.json
```

---

# 2. Strict phase claim boundary

A positive phase may publish:

```text
executable source-derived d-dimensional transverse measurement kernels;

transverse angular measures, Bessel orders, and Fourier normalizations with
exact convention provenance;

distribution-valued regular/plus decomposition for DR.qq and FR.qq;

a first-node executable distribution action;

all source-qualified 16-group Laurent coefficients;

UV, IR, analytic, and finite coefficient classifications;

real/virtual, DR/FR, and separator identities at the exact source scope;

finite remainders and positive scale brackets where established;

covariance and numerical/symbolic outward enclosures;

and an exact physical-matching continuation.
```

The phase must not publish:

```text
a guessed Fourier normalization;
a remembered Bessel order;
a guessed regular/plus coefficient;
a scalar replacement for a distribution;
an unsupported separator cancellation;
a physical mass or coupling;
a physical scale choice;
a physical finite-basis/continuum target;
a selected counterterm/null representative;
a physical Hamiltonian;
a PennyLane state or circuit;
a TMD/process result;
or a production object.
```

---

# 3. Phase stages

Complete these ordered stages inside one C391 phase package.

## Stage A — transverse Fourier–Bessel measurement authority

Construct exact executable kernels from the frozen C389/C390 source and
conventions.

Bind for every measurement kernel:

```text
kernel ID;
source AST node;
transverse dimension d_perp;
epsilon convention;
angular measure;
radial measure;
Fourier normalization;
Bessel order;
tensor/angular rank;
phase and sign;
b_T and k_T variables;
distribution test-function domain;
units;
branch conditions;
and source root.
```

Require independent source-qualified routes:

```text
A1:
direct derivation from the frozen d-dimensional transverse integral;

A2:
angular-harmonic/Bessel reduction;

A3:
small-b_T or test-function moment holdout;

A4:
direct numerical angular integration on named nonphysical fixtures;

A5:
dimension and normalization audit.
```

Do not insert a continuum formula from memory. When an exact source locator is
required and already named by C389, use the exact authenticated version. A
new acquisition must be narrow, official, hash locked, and recorded.

## Stage B — source-owned DRqq/FRqq regular-plus decomposition

Construct the exact regular/plus decomposition for every source-qualified
distributional factor needed by `DR.qq` and `FR.qq`.

Keep separate:

```text
delta support;
plus distributions;
regular functions;
endpoint distributions;
epsilon-dependent coefficients;
scale logarithms;
test-function subtraction terms;
and scheme/source labels.
```

Every decomposition must provide:

```text
source expression;
distribution domain;
subtraction point;
test action;
coefficient enclosure;
normalization;
orientation;
and count-once owner.
```

Require:

```text
B1:
direct source algebra;

B2:
test-function action;

B3:
moment identities;

B4:
endpoint support audit;

B5:
DR/FR ownership crosswalk.
```

A zero coefficient requires an exact proof.

## Stage C — first-node executable gate

Make `DR.qq` executable before group-wide evaluation.

The first-node record must close:

```text
transverse Fourier-Bessel kernel;
distribution test action;
regular/plus decomposition;
phase-space measure;
epsilon/Laurent order;
UV/IR labels;
units;
and outward enclosure.
```

Require symbolic and independent numerical fixture agreement.

Do not accept the group evaluator if `DR.qq` remains only descriptively typed.

## Stage D — all 16 grouped Laurent evaluations

Evaluate every C389 group through its required finite-epsilon Laurent order.

For every term and group publish:

```text
group and term IDs;
source owners;
cut/virtual class;
distribution support;
epsilon powers;
UV coefficients;
IR coefficients;
analytic coefficients;
finite coefficients;
scale logarithms;
imaginary/real structure;
enclosures;
and unresolved remainder.
```

Keep term-level records available even when a group sum closes.

Require:

```text
D1:
term-first evaluation;

D2:
group-first factorized evaluation;

D3:
epsilon-order reversal;

D4:
source-order reversal;

D5:
high-precision numerical holdouts;

D6:
distribution-test-function holdouts.
```

## Stage E — real/virtual and separator identities

Construct only source-qualified combinations.

Keep separate:

```text
direct real DR;
finite real FR;
virtual;
UV counterterm-side pieces;
IR separator pieces;
soft/collinear distribution supports;
and finite remainder.
```

Test:

```text
real/virtual compatibility;
separator cancellation;
UV cancellation or explicit standard-side remainder;
IR cancellation or explicit common-IR remainder;
distribution support matching;
scale-log matching;
and cut/orientation consistency.
```

No cancellation may be inferred from equal floating-point totals alone.

## Stage F — finite remainder, bracket, and covariance

Where Stages A–E close, publish:

```text
finite source-side remainder;
positive scale bracket or exact bracket blocker;
symbolic/numerical covariance;
regular/plus coefficient covariance;
Laurent-order covariance;
K9/K11/K13 records when the finite-basis adapter enters;
scheme/source labels;
and unresolved target-side remainder.
```

Do not select a physical scale merely to obtain a positive bracket.

## Stage G — phase release and next authority discontinuity

Release exactly one of:

```text
JMY_DISTRIBUTION_LAURENT_REAL_VIRTUAL_FINITE_REMAINDER_AUTHORITY_READY_PHYSICAL_MATCHING_NEXT;

JMY_DISTRIBUTION_LAURENT_AUTHORITY_READY_SEPARATOR_OR_TARGET_REMAINDER_EXPLICIT;

JMY_NOT_RELEASED_FOURIER_BESSEL_AUTHORITY_INCOMPLETE;

JMY_NOT_RELEASED_REGULAR_PLUS_DECOMPOSITION_INCOMPLETE;

JMY_NOT_RELEASED_FIRST_NODE_EXECUTION_INCOMPLETE;

JMY_NOT_RELEASED_GROUP_LAURENT_EVALUATION_INCOMPLETE;

JMY_NOT_RELEASED_REAL_VIRTUAL_SEPARATOR_INCONSISTENT;

JMY_NOT_RELEASED_FINITE_REMAINDER_OR_BRACKET_INCOMPLETE;

or ROUTE_MISMATCH.
```

If positive through the source-side finite remainder, the next top-level
package should normally be a physical finite-basis/continuum matching phase.
Use the exact first target/common-IR/running object exposed by the completed
phase.

---

# 4. Internal checkpoint protocol

Use external restartable checkpoints under:

```text
/Users/dustin/work/DeuteronWigner-yolo/state/phases/c391_jmy_eval/
```

Each checkpoint binds:

```text
phase ID;
stage ID;
baseline commit;
input roots;
output root;
test status;
mutation count;
worktree fingerprint;
and next internal stage.
```

Checkpoint writes are atomic. They are not scientific commits.

Do not create C392, C393, and so on for Stages B–F.

---

# 5. Public package and evidence

Implement under:

```text
src/deuteron_wigner/bridge/hqcdrimassc43jmyevalphase1/
data/runtime/c391_hqcdrimassc43jmyevalphase1/
docs/phases/c391_jmy_eval/
tests/test_c391_hqcdrimassc43jmyevalphase1.py
```

Expose immutable APIs equivalent to:

```python
load_verified_jmy_eval_phase1_authority()
verify_jmy_eval_phase1_authority()
phase_plan_manifest()
phase_stage_manifest(stage_id=None)
fourier_bessel_manifest(kernel_id=None)
regular_plus_manifest(owner_id=None, distribution_id=None)
apply_distribution_test_action(record, test_function_record)
first_node_manifest()
evaluate_first_node(parameter_record)
group_laurent_manifest(group_id=None, term_id=None)
evaluate_group_laurent(parameter_record, group_id)
separator_manifest(identity_id=None)
finite_remainder_manifest(record_id=None)
covariance_manifest(record_id=None)
phase_release_manifest()
next_phase_handoff_contract()
phase_completeness_certificate()
```

No public call may mutate upstream records or perform hidden network access.

Every NumPy load uses:

```python
allow_pickle=False
```

---

# 6. Validation

Internal stage validation:

```text
focused tests;
independent route parity;
source/root checks;
32-64 focused mutations;
checkpoint hash;
and restart test.
```

Phase completion validation:

```text
all C389/C390 adjacent tests;
C391 full focused suite;
at least 384 focused live mutations;
two deterministic clean builds;
public reload;
safe loading;
stage-order reversal;
term/group-order reversal;
epsilon-order reversal;
distribution-test-function holdouts;
high-precision numerical holdouts;
topology/count-once;
source/unit/orientation audit;
and no-recomputation checks.
```

Use immutable upstream hashes instead of recalculating unrelated earlier
packages.

---

# 7. Isolation and nonclaims

Require zero:

```text
C389/C390 source or dispatch mutation;
coefficient invention;
remembered Fourier/Bessel formula used as authority;
distribution replaced by scalar;
unsupported separator cancellation;
mass-IR physical import;
C356 backsolve;
C43 unrelated import;
physical mass/coupling/scale selection;
counterterm/null selection;
C166 graph mutation;
Q0/Q1/Q2 mutation;
PennyLane use;
state/spectrum/TMD creation;
resolution averaging;
continuum extrapolation;
or push.
```

---

# 8. Phase completion

C391 completes only when:

```text
the C390 baseline and package root verify;
the historical narrow C391 handoff is preserved;
Stages A–G have terminal stage records;
the first-node gate is explicit;
all accepted group coefficients have source provenance;
all cancellation claims are identity based;
finite remainders/brackets have enclosures or exact blockers;
one phase package root exists;
one local completion commit exists;
one exact next phase contract exists;
the external controller state advances once;
and nothing is pushed.
```
