# C103/IFEQUIV10 Codex Work Package

## Title

**Authoritative exhaustive public-only historical-versus-current-descendant C82 factorized-semantic equivalence: independently frozen descendant compilation, complete C98/C100 primitive crosswalk, unchanged C102 theorem application to 154,830 pairs, compact equivalence certificates, bounded mismatch diagnostics, and C104 canonical-persistence handoff**

## Authoritative baseline

Start from the clean local C102 completion commit:

```text
95c0acb61cb0f4cf528938266257ab78a9ef6a6a
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "95c0acb61cb0f4cf528938266257ab78a9ef6a6a"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, consume, or use either protected path as scientific authority.

Read completely and execute the sole committed C102-to-C103 import contract:

```text
docs/next_level/c102_c103_ifequiv10_import_contract.json
```

If the sole tracked C103 import contract has a different exact filename or extension, discover it, record its path and SHA-256, and do not create or use a competing contract.

If the import contract freezes exact status names, schemas, public signatures, package names, or a narrower boundary, preserve them. This work package supplies the complete comparison. It does not authorize changing either scientific side to obtain agreement.

Create one local completion commit. Do not push.

---

# 1. Preserve all prior decisions and claim boundaries

Preserve unchanged:

```text
C98_HISTORICAL_PUBLIC_THEOREM_INPUT_READY

C99 diagnostic decision:
SCIENTIFIC_EQUIVALENCE_UNRESOLVED

C100_C98_AUTHENTICATED_PRIMITIVE_ENUMERATION_READY

C101 diagnostic status:
C101_IFEQUIV9_EXPANSION_PROOF_INCOMPLETE

C102_C94_PUBLIC_FACTORIZED_SEMANTIC_CHECKER_READY
```

Interpret these precisely:

```text
C99:
    primitive-domain enumeration interface incomplete;
    no scientific mismatch claimed.

C100:
    primitive identities publicly enumerable;
    no historical-versus-descendant comparison performed.

C101:
    descendant domain and public primitive traversal completed;
    theorem call unavailable through the permitted facade;
    no scientific mismatch claimed.

C102:
    the unchanged accepted C94 factorized-semantic checker is now
    publicly callable through an immutable authenticated adapter;
    154,830 historical self-regressions are positive;
    no historical-versus-current-descendant comparison performed.
```

Do not issue C99 or C101 equivalence retroactively.

Retain the following prior results only as independent holdouts:

```text
C99 K9 public pair-program diagnostic:
    pairs = 16,224
    scientific identity/order/root/content/count mismatches = 0
    instance-only metadata adapter present

C101 historical-blind descendant freeze:
    pairs = 154,830
    logical records = 891,992,018

C101 resolution pair census:
    16,224 / 43,350 / 95,256

C101 resolution logical-record census:
    28,606,464 / 165,991,250 / 697,394,304

C101 C100 public primitive traversal:
    families = 5
    records = 35
    page/direct-content mismatches = 0
```

C103 must produce its own complete authoritative comparison from the final public chain.

Retain these claim boundaries:

```text
historical_environment =
    HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY

exact_historical_C72_runtime_instance =
    UNKNOWN_NOT_CLAIMED

C93_payload_relation =
    RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY

C97_operand_relation =
    DESCENDANT_RECOVERED_CANONICAL_OPERAND_RECORD_FOR_FROZEN_C90_PROOF

C100_scope =
    AUTHENTICATED_PRIMITIVE_IDENTITY_ENUMERATION_ONLY

C102_scope =
    UNCHANGED_C94_THEOREM_AND_CHECKER_PUBLIC_EXPORT_ONLY
```

---

# 2. Frozen historical public authority

Consume historical science only through the following public methods.

## C98 historical inputs and scientific content

```python
historical_pair_normal_form(pair_id, resolution)

historical_pair_proof_inputs(pair_id, resolution)

historical_primitive_record(family_id, record_id)
```

## C100 primitive-domain identity enumeration

```python
historical_primitive_domain_manifest()

historical_primitive_record_page(
    *,
    family_id=None,
    cursor=None,
    limit=...,
)
```

## C102 unchanged theorem/checker authority

Use the exact contract-frozen equivalents of:

```python
load_verified_factorized_semantic_theorem_authority()

factorized_expansion_theorem_specification()

factorized_expansion_checker_contract()

verify_factorized_expansion_equivalence(
    historical_program,
    descendant_program,
    primitive_equivalence_certificates,
    *,
    scientific_schema,
    canonical_order,
)

verify_factorized_expansion_invocation(
    invocation_certificate,
)
```

Preserve all C90-C102 scientific and package roots unchanged. Verify their complete values from committed artifacts. At minimum verify and freeze:

```text
C90 aggregate:
efee3c97dff6310349f39c52c52efe8fe5a3691dff033b783c4ce0be61366a34

C93 capsule:
65173c9f3a623f87ec06f2f6b1a82d600db7e26526a08ea26851c47c8e8950f6

C94 package:
3792f56e5ecad966c32d238efe0d8fe580da349900d908083de9dbc528ed1e75

C97 operand:
99a404f149321009fdf0a8f29c6a68b164ef392b7247179188509bfef122abad

C97 regression result:
4b9e5ad15a6367cc8b92a85e25ef6d87f1b2fea4f9a26087fb6662cbe56a15ab

C97 capsule:
916cb84f4ed617a09662fd35e3a5cc16708da765df6e8c1a8d599d291d4474b8

C98 package:
6981b0e72fb72c80313a2bf773eff1818994969e9ba1c6c753043eada1b1e8ef
```

Also verify and freeze:

```text
C100 primitive-domain enumeration root;
C100 package root;
C102 theorem-authority root;
C102 checker-API root;
C102 historical-self-regression root;
C102 package root;
C88 scientific schema/order roots;
C90 semantic-IR and normal-form roots;
C94 theorem and checker source/API roots.
```

Create:

```text
docs/next_level/c103_derivation_authority_manifest.json
docs/next_level/c103_input_fidelity_audit.json
docs/next_level/c103_historical_public_authority_freeze.json
```

Issue:

```text
C103_HISTORICAL_PUBLIC_AUTHORITY_FROZEN_COMPLETE
```

---

# 3. Strict public-only historical boundary

During C103, poison and forbid:

```text
private C90 ledgers, normalizers, proof builders, or historical records;

private C93 capsule fields, recovery routes, and source indices;

private C94 pair ledgers, primitive indices, payload constructors,
and checker internals outside the public C102 route;

private C97 Route-A/Route-B recovery, capsule internals, and transport
fields not exposed through C98;

private C98 location indices and package internals;

private C100 builder indices and private enumeration tables;

private C102 checker implementation details outside its authenticated
public callable and manifests;

C99/C101 expected comparison outputs as construction inputs;

historical worktree staging;

build-if-missing;

repair-if-missing;

network access.
```

Every historical normal form, proof input, primitive identity, primitive content record, theorem specification, and checker invocation must enter through C98, C100, or C102.

Create:

```text
docs/next_level/c103_historical_public_only_contract.json
docs/next_level/c103_historical_public_only_validation.json
```

Any forbidden access is a fail-closed public-boundary defect, not a scientific mismatch.

---

# 4. Scientific question

Determine whether the current descendant C77/C78/C80/C82 construction defines exactly the same ordered C88 scientific-record sequence as the frozen historical C82 construction exposed through C98/C100 and checked through C102.

For every supported physical pair \(p\), compare:

\[
\mathcal P^{\mathrm{hist}}_p
\qquad\text{and}\qquad
\mathcal P^{\mathrm{desc}}_p.
\]

The required favorable theorem statement is:

\[
\llbracket \mathcal P^{\mathrm{hist}}_p \rrbracket
=
\llbracket \mathcal P^{\mathrm{desc}}_p \rrbracket.
\]

This is equality of the complete ordered factorized scientific semantics. It is not:

```text
a count-only check;
a root-only shortcut;
a sampled record comparison;
a package-layout comparison;
a claim that 891,992,018 records were serialized;
or recovery of the original C72 runtime.
```

---

# 5. Descendant independence gate

The current descendant side must be compiled before any historical expected output becomes visible.

Use this exact phase order.

## Phase D1 — freeze current source and scientific inputs

Record:

```text
C103 starting commit;
current source/API fingerprints;
C77/C78/C80/C82 package and scientific roots;
semantic schemas;
supported-pair constructor identity;
primitive-record constructors;
normal-form compiler;
normalizer;
C88 record schema/order.
```

## Phase D2 — poison historical expected objects

Before descendant compilation, poison:

```text
C98 historical normal forms;
C98 historical proof inputs;
C98 historical primitive values;
C100 family/record identities and canonical order;
C102 positive proof results and invocation certificates;
historical pair roots and summaries;
historical proof results;
C99/C101 diagnostic pair roots and comparison decisions;
C103 expected status or equivalence roots.
```

## Phase D3 — compile and normalize the complete descendant domain

Independently compile all current supported C77/C78/C80/C82 pairs into the unchanged C90 typed semantic IR.

For every pair freeze:

```text
resolution and canonical pair identity;
global and resolution-local sequence;
physical bra/ket identities;
support status;
typed normal-form DAG;
ordered child graph;
primitive dependencies;
logical cardinality;
mixed-radix order;
record-expression root;
coefficient-expression root;
bound-rule root;
status-rule root;
multiplicity-rule root;
ancestry-rule root;
first/last logical identities;
closed-form summary;
descendant semantic-program root.
```

## Phase D4 — freeze descendant roots

Only after all descendant records and roots are persisted and authenticated may C98/C100/C102 historical access be enabled.

Create:

```text
docs/next_level/c103_descendant_input_freeze.json
docs/next_level/c103_descendant_independence_audit.json
docs/next_level/c103_descendant_program_manifest.json
```

Issue:

```text
C103_DESCENDANT_DOMAIN_FROZEN_WITH_HISTORICAL_OUTPUTS_POISONED
```

Any historical access during descendant construction is a failure.

---

# 6. Descendant compiler integrity and census

Compare fresh C103 descendant output against the accepted C91/C99/C101 descendant holdouts without consuming historical scientific outputs.

Require:

```text
supported pairs = 154,830
logical records = 891,992,018

K9 pairs = 16,224
K11 pairs = 43,350
K13 pairs = 95,256

K9 records = 28,606,464
K11 records = 165,991,250
K13 records = 697,394,304
```

Compare prior and fresh descendant:

```text
pair identities and canonical order;
primitive identities and roots;
normal-form roots and complete normalized content;
logical counts;
record/coefficient/bound/status/multiplicity/ancestry roots;
first/last identities;
summaries;
resolution and aggregate descendant roots.
```

Require zero descendant-science mismatch.

Create:

```text
docs/next_level/c103_descendant_compiler_integrity.json
docs/next_level/c103_descendant_recompilation_report.json
docs/next_level/c103_logical_census_precomparison.json
```

---

# 7. Freeze a schema-level adapter registry before content comparison

C99 identified an instance-only metadata adapter in K9. C103 may use an adapter only when it is frozen from schema/API differences before inspecting pair scientific values.

Create a finite adapter registry whose entries may cover only:

```text
public-wrapper type names;
field aliases with one-to-one semantics;
package path or source-API metadata;
serialization-layout metadata;
historical-versus-descendant instance roots;
canonical immutable wrapper conversion.
```

An adapter must not change:

```text
scientific value or exact expression;
primitive identity;
normal-form node type or child order;
logical count;
mixed-radix order;
coefficient;
bound;
status;
multiplicity;
ancestry;
first/last logical identity;
summary scientific content;
semantic-program root after canonicalization.
```

Every adapter requires:

```text
schema-level justification;
canonical forward map;
inverse or round-trip proof;
field-completeness proof;
value-preservation proof;
mutation controls;
declared instance-only classification.
```

Freeze the adapter registry before the full primitive and pair comparison.

Create:

```text
docs/next_level/c103_instance_adapter_registry.json
docs/next_level/c103_instance_adapter_validation.json
```

Do not add a new adapter after discovering a scientific mismatch unless C103 fails closed and the adapter is reviewed in a separate continuation package.

---

# 8. Exhaustive public primitive-domain census

Enumerate the historical primitive domain only through C100.

Traverse all allowed page sizes required by the C100 contract and verify:

```text
families = 5
records = 35

missing families = 0
extra families = 0
duplicate family IDs = 0
family-order mismatch = 0

missing records = 0
extra records = 0
duplicate record IDs = 0
record-order mismatch = 0
cursor/page gap = 0
cursor/page overlap = 0
inclusion failure = 0
```

Retrieve each enumerated scientific record only through C98:

```python
historical_primitive_record(family_id, record_id)
```

Create:

```text
docs/next_level/c103_historical_primitive_domain_census.json
```

---

# 9. Complete primitive scientific crosswalk

For each of the 35 historical primitive records:

1. Enumerate the identity through C100.
2. Retrieve the historical content through C98.
3. Locate the independently frozen descendant primitive record.
4. Canonicalize only through the pre-frozen adapter registry.
5. Compare complete scientific content.
6. Construct one primitive-equivalence certificate accepted by C102.

Compare:

```text
family and record identity;
schema and version;
canonical sequence;
exact expression or symbolic identity;
numerical value when present;
certified bound;
terminal status;
multiplicity;
ancestry;
factor ownership;
source/provenance identity;
scientific family root.
```

Classify each relation exactly once:

```text
BYTE_IDENTICAL_SCIENTIFIC_RECORD

CANONICALLY_IDENTICAL_SCIENTIFIC_RECORD

SCIENTIFICALLY_IDENTICAL_WITH_EXPLICIT_ADAPTER

INSTANCE_ONLY_DIFFERENCE

SCIENTIFIC_PRIMITIVE_DIFFERENCE

UNRESOLVED
```

A favorable result permits only the first four.

Require:

```text
historical primitive records = 35
descendant primitive records crosswalked = 35
missing descendant primitives = 0
extra descendant primitives in the compared domain = 0
scientific primitive differences = 0
unresolved primitive relations = 0
```

Create:

```text
docs/next_level/c103_primitive_domain_crosswalk.json
docs/next_level/c103_primitive_record_equivalence.json
docs/next_level/c103_primitive_equivalence_certificate_manifest.json
```

---

# 10. Complete color-authority crosswalk

Compare the historical color records exposed through C98/C100 with the frozen descendant color authority.

Include the repository-equivalent of:

```text
24 ordered product-color rows;
3 retained-triplet columns;
72 ordered pair records;
exact expressions and zero certificates;
per-entry values and certified bounds;
terminal statuses;
E_src;
Gram;
U3 and U3-dagger;
P3;
basis permutations;
all-eight-generator intertwining;
rank/trace/projector identities;
anti-sextet leakage;
15-channel leakage.
```

Select exactly one:

```text
DESCENDANT_COLOR_SCIENCE_IDENTICAL_TO_HISTORICAL_CANONICAL

DESCENDANT_COLOR_INSTANCE_DIFFERS_SCIENCE_IDENTICAL

DESCENDANT_COLOR_SCIENCE_DIFFERS

UNRESOLVED
```

A favorable result requires one of the first two.

Create:

```text
docs/next_level/c103_color_authority_crosswalk.json
```

---

# 11. Complete supported-pair crosswalk

For all 154,830 pairs compare:

```text
resolution;
global pair sequence;
resolution-local sequence;
canonical pair ID;
physical bra/ket identities;
row/column identities;
support status;
exact logical cardinality;
canonical mixed-radix/order identity.
```

Require:

```text
historical pairs = 154,830
descendant pairs = 154,830

missing = 0
extra = 0
duplicates = 0
pair-ID mismatch = 0
pair-order mismatch = 0
resolution mismatch = 0
support mismatch = 0
logical-count mismatch = 0
```

Create:

```text
docs/next_level/c103_supported_pair_crosswalk.json
docs/next_level/c103_supported_pair_order_validation.json
```

---

# 12. Exhaustive pair-program scientific comparison

For every pair, load the historical normalized program through C98 and compare it with the independently frozen descendant program.

Compare separately:

```text
pair identity;
pair order;
normal-form root;
complete typed node content;
node graph and ordered children;
primitive dependencies under approved certificates;
logical cardinality;
mixed-radix ordering;
record-construction expression;
projected-coefficient expression;
bound rule;
terminal-status rule;
witness-multiplicity rule;
ancestry rule;
first/last logical identities;
closed-form summary;
semantic-program root.
```

Record mismatch counts for:

```text
MISSING_PAIR
EXTRA_PAIR
PAIR_IDENTITY
PAIR_ORDER
NORMAL_FORM_ROOT
NORMAL_FORM_CONTENT
NODE_GRAPH
PRIMITIVE_DEPENDENCY
LOGICAL_COUNT
ORDER
RECORD_EXPRESSION
COEFFICIENT_EXPRESSION
BOUND_RULE
STATUS_RULE
MULTIPLICITY_RULE
ANCESTRY_RULE
FIRST_LAST_IDENTITY
SUMMARY
PAIR_ROOT
```

A favorable result requires zero scientific mismatches in every category.

Rerun K9 through the final C98/C100/C102 route. Compare its complete result with both the C99 and C101 holdouts. Require zero scientific discrepancy.

Create:

```text
docs/next_level/c103_exhaustive_pair_semantic_comparison.json
docs/next_level/c103_k9_diagnostic_holdout_comparison.json
```

---

# 13. Apply the unchanged public theorem to all pairs

For every pair with a complete primitive certificate and complete programs, call only C102:

```python
verify_factorized_expansion_equivalence(
    historical_program,
    descendant_program,
    primitive_equivalence_certificates,
    scientific_schema=...,
    canonical_order=...,
)
```

The checker inputs must bind:

```text
historical program root;
descendant program root;
primitive-equivalence-certificate root;
C88 schema/order roots;
C90 semantic-IR and normal-form roots;
C94 theorem/checker roots;
C102 checker authority/API roots;
exact logical cardinality.
```

The required favorable result is the exact accepted C102/C94 positive status, semantically equivalent to:

```text
EXPANDED_C88_SEQUENCE_IDENTICAL_BY_FACTORIZED_SEMANTIC_PROOF
```

Create:

```text
one C103 theorem-input root per pair;
one C103 invocation/equivalence certificate root per pair;
one descendant semantic root per resolution;
one equivalence-certificate root per resolution;
one aggregate descendant semantic root;
one aggregate historical-descendant equivalence-certificate root.
```

Label the equivalence roots explicitly:

```text
FACTORIZED_SEMANTIC_EQUIVALENCE_CERTIFICATE_ROOT
```

They are not expanded record-stream hashes and not unavailable historical C90 certificate identities.

Require:

```text
theorem executions = 154,830
theorem successes = 154,830
theorem failures = 0
theorem unresolved = 0
checker-source/API mismatches = 0
theorem-root mismatches = 0
schema/order mismatches = 0
cardinality mismatches = 0
```

Create:

```text
docs/next_level/c103_expansion_equivalence_application.json
docs/next_level/c103_equivalence_certificate_manifest.json
docs/next_level/c103_equivalence_root_manifest.json
```

---

# 14. Exact logical-census closure

Require theorem-certified closure:

```text
K9_2_N8_b0.40
    pairs = 16,224
    logical records = 28,606,464

K11_2_N10_b0.45
    pairs = 43,350
    logical records = 165,991,250

K13_2_N12_b0.50
    pairs = 95,256
    logical records = 697,394,304

total
    pairs = 154,830
    logical records = 891,992,018
```

Create:

```text
docs/next_level/c103_logical_census_closure.json
```

Do not serialize or exhaustively materialize the leaf domain.

---

# 15. Bounded mismatch localization

If any pair differs, do not expand the global domain and do not modify either side.

For each mismatched pair:

1. Produce a typed normal-form DAG diff.
2. Identify the first differing node, primitive, expression, count, or order rule.
3. Derive affected ordinal intervals through exact mixed-radix algebra.
4. Materialize only a byte-capped record subset around:
   - first difference;
   - first and last ordinals;
   - carry boundaries;
   - expression boundaries;
   - bound/status/multiplicity/ancestry transitions.
5. Compare every C88 scientific field.
6. Preserve complete reversible ancestry.
7. Run the C102 checker negative diagnostic on the localized mutation.

Provide bounded operations equivalent to:

```python
diagnose_pair_semantic_difference(
    pair_id,
    resolution,
    *,
    max_bytes,
)

materialize_pair_record_difference(
    pair_id,
    resolution,
    ordinal_ranges,
    *,
    max_bytes,
)
```

Exercise this path through mutations even if production comparison is fully positive.

Create:

```text
docs/next_level/c103_mismatch_localization_contract.json
docs/next_level/c103_mismatch_diagnostic_report.json
```

---

# 16. Difference classification and final scientific decision

Classify every observed difference exactly once:

```text
SCIENTIFIC_PRIMITIVE_DIFFERENCE
SCIENTIFIC_PROGRAM_DIFFERENCE
SCIENTIFIC_ORDER_OR_MULTIPLICITY_DIFFERENCE
SCIENTIFIC_COEFFICIENT_OR_BOUND_DIFFERENCE
SCIENTIFIC_STATUS_OR_ANCESTRY_DIFFERENCE
SCIENTIFIC_SCHEMA_DIFFERENCE
INSTANCE_ONLY_PACKAGE_ROOT_DIFFERENCE
INSTANCE_ONLY_PATH_OR_LAYOUT_DIFFERENCE
INSTANCE_ONLY_SOURCE_API_FINGERPRINT_DIFFERENCE
DOCUMENTATION_ONLY_DIFFERENCE
UNRESOLVED_DIFFERENCE
```

For every instance-only difference, require:

```text
canonical scientific fields equal;
adapter round-trip closes;
primitive certificate succeeds;
C102 theorem proof succeeds.
```

Select exactly one final decision:

```text
COMPLETE_FACTORIZED_SEMANTIC_EQUIVALENCE

SCIENTIFICALLY_EQUIVALENT_WITH_INSTANCE_ONLY_DIFFERENCES

SCIENTIFIC_PAYLOAD_MISMATCH

SCIENTIFIC_EQUIVALENCE_UNRESOLVED
```

Definitions:

```text
COMPLETE_FACTORIZED_SEMANTIC_EQUIVALENCE:
    canonical primitives and programs are identical without a nontrivial
    scientific adapter.

SCIENTIFICALLY_EQUIVALENT_WITH_INSTANCE_ONLY_DIFFERENCES:
    package/API/layout instances differ, but all canonical science is
    proven equal by explicit adapters and C102 theorem certificates.

SCIENTIFIC_PAYLOAD_MISMATCH:
    at least one primitive, program, count, order, expression,
    coefficient, bound, status, multiplicity, ancestry, or schema differs.

SCIENTIFIC_EQUIVALENCE_UNRESOLVED:
    at least one required public comparison, certificate, adapter, or
    diagnostic remains incomplete.
```

Either of the first two may support the positive C103 package status.

Create:

```text
docs/next_level/c103_difference_classification.json
docs/next_level/c103_instance_science_separation_report.md
docs/next_level/c103_scientific_equivalence_decision.json
```

---

# 17. Compact runtime and public C103 API

Implement under:

```text
src/deuteron_wigner/bridge/ifequiv10/
```

or the exact contract-frozen equivalent.

Use compact metadata under:

```text
data/runtime/c103_ifequiv10/
```

Persist one compact ledger entry per pair containing:

```text
pair identity and sequence;
historical program root;
descendant program root;
primitive-certificate root;
logical count;
comparison status;
C102 theorem-input root;
C103 equivalence-certificate root;
summary;
difference classification.
```

Do not copy complete historical/descendant program payloads or expanded records.

Expose immutable operations equivalent to:

```python
load_verified_historical_descendant_equivalence()

verify_historical_descendant_equivalence_root()

scientific_equivalence_decision()

pair_equivalence(pair_id, resolution)

historical_pair_attestation(pair_id, resolution)

descendant_pair_attestation(pair_id, resolution)

pair_equivalence_proof(pair_id, resolution)

primitive_equivalence(family_id, record_id)

diagnose_pair_difference(
    pair_id,
    resolution,
    *,
    max_bytes,
)
```

One-pair lookup must not rerun the complete comparison.

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
unsafe paths or symlinks;
unindexed files;
mutable arrays or records;
unknown schemas;
root mismatches;
hidden build-if-missing;
hidden repair behavior;
network access.
```

Construct separately:

```text
C103_DESCENDANT_AGGREGATE_SEMANTIC_ROOT

C103_HISTORICAL_DESCENDANT_EQUIVALENCE_CERTIFICATE_ROOT

C103_PACKAGE_ROOT
```

Create:

```text
docs/next_level/c103_descendant_semantic_root_manifest.json
docs/next_level/c103_historical_descendant_equivalence_root.json
docs/next_level/c103_api_contract.json
docs/next_level/c103_api_validation.json
docs/next_level/c103_runtime_inventory.json
```

---

# 18. Determinism, restart, serial, and parallel execution

Run:

```text
two complete clean descendant compilations;

two complete historical-versus-descendant comparisons;

one serial comparison;

one deterministic pair-range parallel comparison with canonical commit order;

one controlled interrupted/resumed comparison;

two historical traversals using different allowed C98/C100 page/query orders.
```

Use pair-atomic checkpoints binding:

```text
all C90-C102 historical roots;
current descendant source/scientific roots;
C103 adapter-registry root;
primitive-crosswalk root;
C102 theorem/checker roots;
semantic-IR and schema versions;
next pair sequence;
closed shard hashes;
rolling descendant root;
rolling equivalence root;
compiler/checker fingerprints.
```

Restart must reject any changed root, schema, compiler, checker, adapter, crosswalk, or closed shard.

Require identical:

```text
descendant programs and roots;
primitive crosswalk and certificates;
pair comparison decisions;
C102 invocation results;
C103 pair certificate roots;
resolution and aggregate descendant roots;
resolution and aggregate equivalence roots;
final scientific decision;
C103 package root.
```

Create:

```text
docs/next_level/c103_restart_contract.json
docs/next_level/c103_restart_validation.json
docs/next_level/c103_two_clean_pass_determinism.json
docs/next_level/c103_parallel_comparison_report.json
docs/next_level/c103_resource_and_scaling_report.json
```

---

# 19. Isolation and mutation controls

Execute at least **384 focused live mutations** across actual:

```text
C98 historical normal forms and proof inputs;
C100 family/record identities, pages, and cursors;
C102 theorem/checker manifests and invocation schema;
descendant compiler inputs;
adapter-registry entries;
primitive records and certificates;
pair identities and ordering;
normal-form nodes and child order;
logical counts;
record/coefficient/bound/status/multiplicity/ancestry rules;
first/last identities;
summaries;
theorem inputs and outputs;
equivalence certificates;
compact ledgers;
checkpoint roots;
C103 public API roots;
C104 import-contract fields.
```

Required failures include every scientific field mutation.

Required invariance includes:

```text
temporary path;
page size;
query order;
parallel shard layout;
process ID;
wall-clock timestamp;
documentation path;
package-instance metadata explicitly proved scientifically irrelevant.
```

Poison and verify independence from:

```text
C80 numerical kernel values;
C53 values and propagation;
C58 values;
physical coupling;
counterterm interfaces;
contact-matrix APIs;
downstream matching/inference/production APIs.
```

Create:

```text
docs/next_level/c103_isolation_report.json
docs/next_level/c103_regression_report.json
```

---

# 20. Supersede C101 only at the checker boundary

Create:

```text
C101_IFEQUIV9_EXPANSION_PROOF_INCOMPLETE
    -> C102 exposes the unchanged checker;
       C103 executes the complete independent comparison.
```

Preserve the C101 diagnostic report and its descendant/primitive holdouts.

Record:

```text
whether C103 descendant freeze agrees with C101;
whether C103 primitive traversal agrees with C101;
whether C103 K9 result agrees with C99 and C101 holdouts.
```

Create:

```text
docs/next_level/c103_c101_blocker_supersession_report.json
```

---

# 21. C104 canonical-persistence import contract

On favorable closure create the sole next contract:

```text
docs/next_level/c103_c104_ifpersist4_import_contract.json
```

C104 must consume:

```text
positive C103 scientific decision;
C98 historical theorem-input authority;
C100 primitive enumeration authority;
C102 unchanged theorem/checker authority;
approved canonical descendant primitive records;
C88 scientific-record schema/order;
complete supported-pair order;
C103 descendant pair-program ledger;
primitive-equivalence certificates;
pair equivalence certificates;
descendant and equivalence roots.
```

C104 must create an authenticated, upstream-free factorized package capable of returning, for any supported pair and pair-local ordinal:

```text
complete canonical C82 scientific record;
physical bra/ket identities;
canonical C80 coordinate identity;
projected coefficient exact expression or value;
certified bound;
terminal status;
factor-ownership identity;
witness multiplicity;
complete reversible ancestry;
historical-descendant equivalence certificate.
```

C104 must not:

```text
materialize all 891,992,018 records;
call historical or current scientific builders at runtime;
multiply projected coefficients by a C80 numerical kernel.
```

Do not create the C104 package inside C103.

---

# 22. Readiness branches

Select exactly one branch.

## Favorable

Issue the exact contract-frozen positive status, with preferred semantic identity:

```text
C103_C82_HISTORICAL_DESCENDANT_FACTORIZED_SEMANTIC_EQUIVALENCE_READY
```

Required:

```text
C98/C100/C102 historical public authorities verify;

current descendant compilation is frozen with historical expected outputs
poisoned;

fresh descendant science reproduces accepted prior descendant holdouts;

all 5 primitive families and 35 records are exhaustively enumerated,
retrieved, crosswalked, and certified;

the complete color authority is scientifically reconciled;

all 154,830 pair identities and order records agree;

all descendant programs compile and normalize;

every scientific mismatch count is zero;

all 154,830 C102 theorem invocations succeed;

the 891,992,018-record census closes;

every instance-only difference has an explicit pre-frozen adapter and
successful theorem certificate;

no unresolved item remains;

clean, restart, serial, parallel, API, safe-loading, and mutation gates pass;

the C104 import contract is complete.
```

Next:

> **C104/IFPERSIST4 — authenticated upstream-free canonical factorized pair-coordinate coefficient package**

## Historical public input incomplete

```text
C103_IFEQUIV10_HISTORICAL_PUBLIC_INPUT_INCOMPLETE
```

## Descendant compiler incomplete

```text
C103_IFEQUIV10_DESCENDANT_COMPILER_INCOMPLETE
```

## Primitive scientific mismatch

```text
C103_IFEQUIV10_PRIMITIVE_SCIENTIFIC_MISMATCH
```

## Pair-program scientific mismatch

```text
C103_IFEQUIV10_PAIR_PROGRAM_MISMATCH
```

## Theorem proof incomplete

```text
C103_IFEQUIV10_EXPANSION_PROOF_INCOMPLETE
```

## Instance/science classification unresolved

```text
C103_IFEQUIV10_INSTANCE_DIFFERENCE_UNRESOLVED
```

## Public equivalence package incomplete

```text
C103_IFEQUIV10_PUBLIC_EQUIVALENCE_INCOMPLETE
```

Use only the exact branch authorized by the import contract. Localize the precise failing object. Do not modify either side to manufacture agreement.

Do not issue coefficient-persistence, kernel-product, contact-matrix, complete-instantaneous-fermion, TMD, matching, inference, or production status.

---

# 23. Essential deliverables

Create at least:

```text
docs/next_level/c103_implementation_report.md
docs/next_level/c103_derivation_authority_manifest.json
docs/next_level/c103_input_fidelity_audit.json
docs/next_level/c103_historical_public_authority_freeze.json
docs/next_level/c103_historical_public_only_contract.json
docs/next_level/c103_historical_public_only_validation.json

docs/next_level/c103_descendant_input_freeze.json
docs/next_level/c103_descendant_independence_audit.json
docs/next_level/c103_descendant_program_manifest.json
docs/next_level/c103_descendant_compiler_integrity.json
docs/next_level/c103_descendant_recompilation_report.json
docs/next_level/c103_logical_census_precomparison.json

docs/next_level/c103_instance_adapter_registry.json
docs/next_level/c103_instance_adapter_validation.json

docs/next_level/c103_historical_primitive_domain_census.json
docs/next_level/c103_primitive_domain_crosswalk.json
docs/next_level/c103_primitive_record_equivalence.json
docs/next_level/c103_primitive_equivalence_certificate_manifest.json
docs/next_level/c103_color_authority_crosswalk.json

docs/next_level/c103_supported_pair_crosswalk.json
docs/next_level/c103_supported_pair_order_validation.json
docs/next_level/c103_exhaustive_pair_semantic_comparison.json
docs/next_level/c103_k9_diagnostic_holdout_comparison.json

docs/next_level/c103_expansion_equivalence_application.json
docs/next_level/c103_equivalence_certificate_manifest.json
docs/next_level/c103_equivalence_root_manifest.json
docs/next_level/c103_logical_census_closure.json

docs/next_level/c103_mismatch_localization_contract.json
docs/next_level/c103_mismatch_diagnostic_report.json
docs/next_level/c103_difference_classification.json
docs/next_level/c103_instance_science_separation_report.md
docs/next_level/c103_scientific_equivalence_decision.json

docs/next_level/c103_descendant_semantic_root_manifest.json
docs/next_level/c103_historical_descendant_equivalence_root.json
docs/next_level/c103_api_contract.json
docs/next_level/c103_api_validation.json
docs/next_level/c103_runtime_inventory.json

docs/next_level/c103_restart_contract.json
docs/next_level/c103_restart_validation.json
docs/next_level/c103_two_clean_pass_determinism.json
docs/next_level/c103_parallel_comparison_report.json
docs/next_level/c103_resource_and_scaling_report.json

docs/next_level/c103_c101_blocker_supersession_report.json
docs/next_level/c103_c104_ifpersist4_import_contract.json
docs/next_level/c103_isolation_report.json
docs/next_level/c103_readiness_report.json
docs/next_level/c103_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifequiv10/
```

Use compact runtime metadata under:

```text
data/runtime/c103_ifequiv10/
```

Create exactly one next-package contract corresponding to the selected branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not modify the protected C69 prompt.

---

# 24. Acceptance criteria

C103 is complete only when:

1. Baseline `95c0acb61cb0f4cf528938266257ab78a9ef6a6a` reproduces.
2. Both protected paths remain untouched and untracked.
3. C90-C102 tracked scientific artifacts remain unchanged.
4. The sole C102-to-C103 import contract is consumed and preserved.
5. Historical access uses only C98, C100, and C102 public methods.
6. Private historical/index/builder access is zero.
7. Descendant compilation occurs with historical outputs and identities poisoned.
8. Fresh descendant science reproduces the accepted prior descendant freeze.
9. The adapter registry is frozen before content comparison.
10. No adapter changes a scientific field.
11. All five historical primitive families are publicly enumerated.
12. All 35 primitive records are publicly enumerated exactly once.
13. Every primitive content record is retrieved through C98.
14. Every historical primitive has a complete descendant crosswalk.
15. No primitive scientific difference or unresolved relation remains in the favorable branch.
16. The complete color authority is reconciled scientifically.
17. All 154,830 pair identities and order records agree.
18. All 154,830 descendant programs compile and normalize.
19. Every pair scientific field is compared.
20. Every scientific mismatch category is zero in the favorable branch.
21. All 154,830 C102 theorem invocations execute.
22. All 154,830 theorem results are positive.
23. Theorem failures and unresolved results are zero.
24. The exact 891,992,018 logical-record census closes.
25. No expanded full-record stream is written.
26. Every instance-only difference is justified by field equality, adapter round trip, and theorem proof.
27. Mismatch diagnostics are operational through mutations.
28. Two clean descendant compilations and comparisons agree.
29. Interrupted/resumed execution agrees.
30. Serial and deterministic parallel execution agree.
31. The public C103 API is immutable, bounded, and safe.
32. One-pair lookup does not rerun the full comparison.
33. At least 384 focused live mutations pass.
34. C99 and C101 diagnostics are superseded honestly and retained.
35. The C104 import contract is complete.
36. No canonical coefficient package is created.
37. No C80 numerical kernel is evaluated as a physical composition.
38. No coefficient-times-kernel product, contact matrix/action, coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object is created.
39. `NO_JOINT_MEASURE`, 216 production routes, all 642 ART25 identities/covariance, and all authoritative artifacts remain unchanged.
40. The working tree is clean except for the two protected untracked paths.
41. One local completion commit is created and not pushed.

Do not weaken primitive completeness, descendant independence, adapter discipline, pair ordering, theorem application, or scientific-versus-instance classification to obtain a positive result.

---

# 25. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed import-contract path and hash;
- complete C90-C102 historical roots;
- current descendant source/scientific roots;
- descendant compiler-integrity result;
- adapter-registry contents and validation;
- primitive family/record enumeration counts;
- primitive crosswalk counts by classification;
- primitive scientific mismatch counts;
- color-authority relation;
- supported-pair counts and order results;
- mismatch count in every semantic category;
- C102 theorem executions, successes, failures, and unresolved counts;
- logical-record census by resolution and total;
- descendant resolution/aggregate roots;
- equivalence resolution/aggregate roots;
- scientific versus instance-only difference counts;
- final scientific decision;
- C99/C101 K9 and descendant holdout comparisons;
- clean/restart/serial/parallel results;
- compact runtime size and peak memory;
- API, safe-loading, and no-recomputation results;
- mutation and test results;
- C101 supersession result;
- C104 import-contract result;
- exact readiness/no-go status;
- exact next branch;
- confirmation that historical data were consumed only through C98/C100/C102;
- confirmation that historical expected outputs did not enter descendant compilation;
- confirmation that no expanded stream, coefficient package, kernel product, contact matrix, or downstream physics object was created;
- confirmation that nothing was pushed.
