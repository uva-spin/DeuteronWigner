# C76/QGTM4 Codex Work Package

## Title

**Authenticated immutable public basis-crosswalk completion for the C64 exact Talmi–Moshinsky package: block-local/global raw–relative/CM identities, orientation records, CM-ground query API, strict no-regeneration loading, and C77 physical-embedding preflight**

## Authoritative baseline

Start from the clean local C75 fail-closed completion commit:

```text
7416778a9554fa325d267bf8f4a0e6b1ae012249
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "7416778a9554fa325d267bf8f4a0e6b1ae012249"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

The baseline is authoritative only when it contains and reproduces:

```text
C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY

C74_SOURCE_DERIVED_TRIPLET_PUBLIC_IMPORT_READY

C75_C64_CROSSWALK_PUBLIC_IMPORT_INCOMPLETE
```

and the exact C75 finding:

```text
C74 public color import:
    passes through its authenticated immutable public API;

C64 numerical/exact TM package:
    retains 733 exact TM blocks;
    retains 171,153 exact coefficient-status records;
    retains 67,920 exact cross-m residue certificates;
    retains threshold-free support, canonical expressions,
    certified arrays and bounds, hashes, and deterministic paths;

C64 public import limitation:
    no authenticated immutable public loader exposes the
    block-local/global basis crosswalk required to identify raw
    product states, relative/CM states, global offsets, and exact
    CM-ground columns;

current crosswalk location:
    documentation artifact only;

C75 consequence:
    C75 cannot read the documentation artifact directly while
    claiming that construction consumes C64 only through its public
    immutable API;
    no low-shell pilot, CM-ground map, physical qg embedding,
    descendant-impact audit, or contact object was constructed.
```

Verify every statement from the actual C64 package, C64 crosswalk documentation, C64 public implementation, and C75 import audit. This prompt is not numerical authority.

The exact C62/C64 transverse convention remains immutable:

\[
|n,m\rangle_{\rm polar}
=
(-1)^n
\left|
n+\max(m,0),\,
n+\max(-m,0)
\right\rangle_{\rm circ},
\qquad
L_z=N_+-N_- .
\]

C76 must not change:

```text
any of the 733 C64 block identities;

any of the 171,153 coefficient statuses;

any exact expression or exact-zero classification;

any of the 67,920 residue certificates;

any certified midpoint or bound;

any block row or column ordering;

the C62 analytic phase convention;

the threshold-free support semantics;

the historical C64 package root or tracked artifacts.
```

C74 remains read-only and unchanged.

Create a local completion commit. Do not push.

---

# 1. Descendant correction and ownership

C64 completed the exact TM scientific payload and a read-only numerical import layer, but C75 has demonstrated that its **basis-crosswalk authority is not publicly consumable**.

Do not alter historical C64 files. Create an explicit descendant correction:

```text
historical status:
    C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY

descendant qualification:
    C64_EXACT_TM_CONTENT_COMPLETE_CROSSWALK_PUBLIC_API_INCOMPLETE

reason:
    exact blocks, statuses, expressions, residue certificates,
    certified arrays, and hashes exist, but the block-local/global
    raw–relative/CM crosswalk is available only as documentation
    and has no authenticated immutable public loader.
```

Ownership is:

```text
C62:
    exact circular-ladder TM algebra and phase convention;

C64:
    immutable exact/certified TM payload;

C76:
    authenticated public crosswalk and orientation interface bound
    to the unchanged C64 package.
```

C76 is an interface and authority-completion descendant. It must not rederive a TM coefficient or create a new TM basis.

---

# 2. Execution mandate

C76 must close the exact C75 blocker in this package:

1. authenticate the existing C64 crosswalk authority;
2. materialize a machine-readable runtime crosswalk when the current authority is documentation-only;
3. expose immutable public loaders for block, basis, crosswalk, orientation, and CM-ground identities;
4. bind every crosswalk entry to the unchanged C64 block/status/expression/array identities;
5. enforce safe loading, immutability, no-regeneration, and no-inference;
6. provide one complete public TM authority package;
7. run a public-only C77 preflight using C76 and C74.

Do not create another numerical TM package, another exact-algebra package, or another color package.

A fail-closed result is valid only when the existing C64 crosswalk documentation is internally inconsistent with the authenticated C64 block and basis identities, or when a safe immutable public representation cannot be created without changing the science.

A preferred filename, alternate schema, denser representation, or additional wrapper is not a valid no-go.

---

# 3. Exact purpose

C76 must create:

```text
a complete audit of C64's existing crosswalk documentation and
public package;

an authenticated fingerprint of the exact crosswalk source;

an authenticated fingerprint of the C76 public API;

explicit immutable resolution identities;

explicit immutable identities for all 733 TM blocks;

explicit immutable raw-product transverse-basis records;

explicit immutable relative/CM transverse-basis records;

complete block-local row and column basis identities;

complete block-local-to-global basis crosswalks;

explicit global offsets and global basis IDs;

exact block orientation records;

public exact CM-ground query records for n_CM=0,m_CM=0;

links from every crosswalk entry to the corresponding C64
status/expression/support/certified-array identities;

one minimal authenticated C76 interface/runtime root bound to the
unchanged C64 package root and crosswalk-source hash;

one frozen public TMAuthorityPackage;

safe immutable public loaders;

complete C64/C76 equivalence and crosswalk-closure tests;

a C77/QGEMBED9 import contract;

and a dry C77 preflight using only the C76 and C74 public APIs.
```

C76 must not construct:

```text
a kinematic CM-ground injection J_kin;

a raw-to-physical projection P_kin;

a physical qg embedding;

a CM-ground image projector;

a color-composed map;

a C47/C52/C53/C57/C58 impact result;

direct-contact support or a contact matrix.
```

The strongest positive status is:

```text
C76_SOURCE_DERIVED_TM_CROSSWALK_PUBLIC_IMPORT_READY
```

The exact positive continuation is:

> **C77/QGEMBED9 — construct the exact CM-ground and color-triplet physical \(qg\) embedding and close descendant impact using the immutable C76 and C74 public packages**

---

# 4. Mandatory repository audit

Inventory the actual C62/C64/C75 files and public exports:

```bash
git ls-files 'docs/next_level/c62*'
git ls-files 'docs/next_level/c64*'
git ls-files 'docs/next_level/c75*'
git ls-files 'src/deuteron_wigner/**/qgtm*'
git ls-files 'tests/*c62*'
git ls-files 'tests/*c64*'
git ls-files 'tests/*c75*'
find data/runtime -maxdepth 3 -type f | sort
```

Read completely the actual repository equivalents of:

```text
C62 exact phase and coefficient construction;

C64 derivation-authority and input-fidelity records;

C64 block census and block identities;

C64 basis-order manifests;

C64 expression, status, and support hashes;

C64 certified arrays and bounds;

C64 residue certificates;

C64 crosswalk documentation;

C64 public API and implementation;

C64 package/inventory/readiness/determinism records;

C75 C64-import report, implementation report,
missing-calculation specification, readiness report, and
regression report.
```

Use actual filenames. Do not manufacture an obstruction from a naming difference.

Create:

```text
docs/next_level/c76_derivation_authority_manifest.json
docs/next_level/c76_input_fidelity_audit.json
```

---

# 5. Authenticate the existing crosswalk source

Identify the exact tracked C64 document or manifest that currently defines the basis crosswalk.

Record:

```text
repository-relative path;

SHA-256;

schema or prose/table structure;

scientific role;

C64 block and basis identities it references;

whether it is canonical machine-readable data or documentation
requiring a one-time deterministic transcription.
```

When the current crosswalk is documentation-only:

```text
parse it through one deterministic, explicitly tested translator;

do not infer missing fields;

fail on ambiguous or duplicate entries;

compare all parsed identities against C64 basis and block records;

preserve the source document unchanged.
```

Compute:

```text
c64_crosswalk_source_sha256;
c76_crosswalk_translation_schema_sha256.
```

Create:

```text
docs/next_level/c76_crosswalk_source_fingerprint.json
docs/next_level/c76_crosswalk_source_validation.json
docs/next_level/c76_crosswalk_translation_contract.json
```

The translator is construction authority only for faithfully materializing the existing crosswalk, not for inventing a new one.

---

# 6. Preserve and verify the C64 scientific package

Before creating the C76 interface, verify through the existing C64 public operations everything they currently expose:

```text
733 block identities;

171,153 coefficient statuses;

67,920 residue certificates;

all exact expression and support identities;

all certified arrays and bounds;

all block dimensions and hashes;

all runtime paths;

all package/inventory identities;

immutability;

no C62 regeneration.
```

Create:

```text
docs/next_level/c76_c64_payload_import_report.json
docs/next_level/c76_c64_payload_integrity_validation.json
```

Any mismatch blocks C76. Do not repair, rebuild, or regenerate C64.

---

# 7. Resolution identities

Materialize immutable resolution records using the exact C64 trajectory.

Each record must contain the actual inherited fields, including at minimum:

```text
resolution ID;

K;

Nmax;

bHO;

crosswalk namespace;

block-ID domain;

raw-basis namespace;

relative/CM-basis namespace;

C64 package identity;

basis-order hashes.
```

Do not add a derived physical interpretation that C64 does not support.

Create:

```text
docs/next_level/c76_tm_resolution_manifest.json
docs/next_level/c76_tm_resolution_validation.json
```

---

# 8. Complete block authority

Materialize exactly 733 immutable block records.

Each block record must retain every authoritative C64 field and at minimum:

```text
block ID;

resolution ID;

block quantum-number labels;

raw-side dimension;

relative/CM-side dimension;

raw local-basis hash;

relative/CM local-basis hash;

stored matrix orientation;

coefficient-status-domain identity;

expression-domain identity;

support-domain identity;

certified-array and bound identities;

residue-certificate identities;

runtime path identity.
```

Do not infer block ordering from filesystem order.

Create:

```text
docs/next_level/c76_tm_block_authority_manifest.json
docs/next_level/c76_tm_block_authority_validation.json
```

---

# 9. Raw-product transverse-basis authority

Materialize every raw-product transverse-basis state used by C64.

Each record must preserve the actual C64/C62 fields. Expected examples include, only when present in the source:

```text
single-particle q and g radial/circular/polar labels;

single-particle angular labels;

total shell;

total m or Lz;

longitudinal-partition/block identity;

local raw index;

global raw-transverse index;

resolution;

basis phase convention;

basis record hash.
```

Do not invent helicity, longitudinal, color, or physical-Fock fields that are not part of the C64 transverse crosswalk.

Create:

```text
docs/next_level/c76_raw_transverse_basis_manifest.json
docs/next_level/c76_raw_transverse_basis_validation.json
```

---

# 10. Relative/CM transverse-basis authority

Materialize every relative/CM transverse-basis state used by C64.

Each record must preserve the actual inherited fields, including when present:

```text
n_rel;

m_rel;

n_CM;

m_CM;

relative shell;

CM shell;

total shell and m;

local relative/CM index;

global relative/CM index;

resolution;

block ID;

basis phase convention;

basis record hash.
```

The CM-ground identity must be explicit data:

```text
n_CM = 0;
m_CM = 0.
```

It must not be inferred from array sparsity or an index position.

Create:

```text
docs/next_level/c76_relcm_transverse_basis_manifest.json
docs/next_level/c76_relcm_transverse_basis_validation.json
```

---

# 11. Complete block-local/global crosswalk

For every block, materialize an immutable crosswalk that identifies:

```text
block ID and resolution;

every local raw index and canonical raw basis ID;

every local relative/CM index and canonical relative/CM basis ID;

every global raw-transverse index;

every global relative/CM index;

the exact block row/column role;

global offsets or explicit global IDs;

basis-order hashes;

orientation identity.
```

The crosswalk must not assume that:

```text
local rows are raw rather than relative/CM;

local columns are relative/CM rather than raw;

global IDs are contiguous by filename order;

a matrix should be transposed or conjugated;

or a zero coefficient removes a basis state.
```

Verify:

```text
every local index appears exactly once;

every expected global basis state appears exactly once in the
declared decomposition;

no local/global collision exists;

no missing state exists;

block dimensions match crosswalk cardinalities;

all basis hashes match C64.
```

Create:

```text
docs/next_level/c76_tm_block_crosswalk_manifest.json
docs/next_level/c76_tm_global_crosswalk_manifest.json
docs/next_level/c76_tm_crosswalk_validation.json
```

---

# 12. Exact orientation records

For every block, expose an immutable orientation record stating the actual C64 meaning, for example:

\[
T_{\mathrm{relCM}\leftarrow\mathrm{raw}}
=
\langle \mathrm{rel},\mathrm{CM}|\mathrm{raw}\rangle,
\]

or the exact alternative used by C64.

Each record must specify:

```text
stored row space;

stored column space;

bra/ket interpretation;

conjugation convention;

forward action;

adjoint/inverse action;

basis metrics;

source identity;

low-shell validation identity.
```

Do not select orientation by whichever option yields a smaller residual.

Create:

```text
docs/next_level/c76_tm_orientation_manifest.json
docs/next_level/c76_tm_orientation_validation.json
```

---

# 13. Link crosswalk entries to exact C64 coefficient authority

For every candidate coefficient identity in the 171,153-entry domain, expose or derive an immutable link containing:

```text
block ID;

local row ID;

local column ID;

global raw basis ID;

global relative/CM basis ID;

terminal exact status;

exact-expression or zero-certificate identity;

support identity;

certified midpoint identity;

certified bound identity;

residue-certificate identity when applicable.
```

This is a linkage layer. Do not duplicate numerical payloads unless the existing C64 public interface cannot safely reference them.

Verify:

```text
every C64 coefficient identity links to exactly one crosswalk pair;

no crosswalk pair has multiple coefficient identities unless the
C64 schema explicitly declares multiplicity;

all 171,153 statuses are covered;

all 67,920 residue certificates point to the correct exact-zero
crosswalk entries.
```

Create:

```text
docs/next_level/c76_tm_coefficient_crosswalk_linkage.json
docs/next_level/c76_tm_coefficient_crosswalk_validation.json
```

---

# 14. Public CM-ground query authority

Create an immutable public query that returns all relative/CM basis states satisfying:

```text
n_CM = 0;
m_CM = 0;
```

for a selected resolution and optional block.

The result must expose:

```text
relative/CM basis ID;

block-local index;

global relative/CM index;

block ID;

resolution;

orientation;

all raw components and coefficient identities available through
the linked C64 block;

exact support identities;

certified bounds.
```

The query must not:

```text
construct J_kin;

conjugate or transpose the block on behalf of the consumer;

apply a numerical threshold;

drop exact-zero components from the authority domain;

or extend the state over helicity, longitudinal, or color spaces.
```

Create:

```text
docs/next_level/c76_cm_ground_query_contract.json
docs/next_level/c76_cm_ground_query_validation.json
```

---

# 15. Safe immutable loading

Create one internal safe-loading path for every C76 numerical access.

Every NumPy load must explicitly use:

```python
numpy.load(path, allow_pickle=False)
```

Reject:

```text
object dtype;

pickle-dependent payloads;

path traversal;

unexpected symlinks;

unindexed files;

shape/dtype/endianness mismatches;

writable returned arrays.
```

All public records must be frozen and all arrays non-writeable.

Create:

```text
docs/next_level/c76_safe_loading_contract.json
docs/next_level/c76_safe_loading_validation.json
docs/next_level/c76_immutable_record_contract.json
docs/next_level/c76_immutable_record_validation.json
```

---

# 16. Minimal authenticated C76 interface root

Use:

```text
data/runtime/c76_qgtm4/
```

The interface root must not duplicate the heavy C64 numerical payload.

Create deterministic control and crosswalk payloads:

```text
interface.json;

interface.sha256;

bound C64 package/root identity;

crosswalk-source fingerprint;

translation-schema identity;

C76 loader-source fingerprint;

C76 public-API fingerprint;

resolution manifest;

block authority manifest;

raw basis manifest;

relative/CM basis manifest;

block/global crosswalk manifests;

orientation manifest;

coefficient-linkage manifest;

CM-ground query metadata;

expected counts and inventory.
```

The authenticated `interface.json` hash is the C76 interface-root identity.

Create:

```text
docs/next_level/c76_interface_schema_contract.json
docs/next_level/c76_interface_root_manifest.json
docs/next_level/c76_interface_authentication_report.json
docs/next_level/c76_runtime_inventory_report.json
docs/next_level/c76_runtime_inventory_validation.json
```

The top-level public loader must verify both the C76 interface root and the bound C64 package root.

---

# 17. Public immutable TM authority package

Implement under:

```text
src/deuteron_wigner/bridge/qgtm4/
```

or the repository-equivalent public package.

Provide a frozen top-level operation equivalent to:

```python
load_tm_authority_package() -> TMAuthorityPackage
```

The returned package must expose publicly:

```text
C64 package/root identity;

C76 interface-root identity;

resolution records;

733 block records;

raw transverse basis;

relative/CM transverse basis;

block-local/global crosswalks;

orientation records;

coefficient linkage;

CM-ground query;

certified C64 block/status/expression/bound access through the
existing public payload interface.
```

Provide subordinate public operations equivalent to:

```python
load_tm_resolutions()

load_tm_blocks()

load_raw_transverse_basis(resolution)

load_relcm_transverse_basis(resolution)

load_tm_block_crosswalk(block_id)

load_tm_global_crosswalk(resolution)

load_tm_orientation(block_id)

load_tm_coefficient_linkage(block_id=None, resolution=None)

load_cm_ground_states(resolution, block_id=None)

load_certified_tm_block(block_id)
```

No consumer may need to read a documentation file, private C64 field, or unindexed runtime path.

Create:

```text
docs/next_level/c76_api_contract.json
docs/next_level/c76_api_validation.json
docs/next_level/c76_package_object_contract.json
docs/next_level/c76_package_object_validation.json
```

---

# 18. No-regeneration and no-inference guards

The C76 public implementation must be unable to call:

```text
C62 coefficient builders;

C64 builders;

a crosswalk repair fallback;

a basis re-enumerator;

a phase-fitting route;

a threshold-support constructor;

a build-if-missing path.
```

It must never infer:

```text
basis identities from array positions alone;

global offsets from sorted filenames;

CM-ground identity from a fixed column number;

orientation from numerical residual minimization;

support from magnitude;

zero from subthreshold value;

or a crosswalk entry from a C47 quadrature map.
```

Create:

```text
docs/next_level/c76_no_regeneration_no_inference_report.json
```

---

# 19. Complete equivalence and closure

Compare the C76 public return domain with the unchanged C64 authority.

Require:

```text
733/733 block identity equivalence;

complete local-basis identity equivalence;

complete global-crosswalk equivalence;

complete orientation equivalence;

171,153/171,153 coefficient-link coverage;

67,920/67,920 residue-certificate linkage;

exact basis-order hash agreement;

certified block/array identity agreement;

CM-ground query agreement with the authenticated source crosswalk.
```

Run package-only checks using only the public C76 object:

```text
crosswalk bijection;

block dimension closure;

global count-once closure;

orientation consistency;

low-shell exact analytic holdouts;

residue exact-zero consistency;

immutable return behavior.
```

Create:

```text
docs/next_level/c76_public_loader_equivalence_report.json
docs/next_level/c76_crosswalk_closure_report.json
```

A sampled comparison is insufficient.

---

# 20. C77/QGEMBED9 import contract

Define the exact contract by which C77 consumes:

```text
the C76 interface root;

the bound C64 package root;

the complete public TM authority package;

resolution and block identities;

raw and relative/CM basis identities;

block-local/global crosswalks;

orientation records;

CM-ground query records;

coefficient/status/expression/support/bound linkage;

certified block access;

the public C74 TripletAuthorityPackage.
```

C77 may use only public C76 and C74 symbols.

C77 may not:

```text
open C64 documentation directly;

inspect private C64 fields;

call C62/C64/C76 builders;

infer CM-ground columns;

infer orientation;

infer support from magnitude;

or infer basis ordering from array shapes.
```

Create:

```text
docs/next_level/c76_c77_qgembed9_import_contract.json
```

---

# 21. Dry C77 public-only preflight

Run a dry preflight using only:

```text
load_tm_authority_package();

load_triplet_authority_package();
```

or the exact public equivalents.

Verify:

```text
both interface/package roots;

733 blocks;

171,153 linked statuses;

67,920 linked residue certificates;

complete raw and relative/CM basis authority;

complete block/global crosswalks;

all orientation records;

public CM-ground queries;

24/3/72 color authority;

all arrays and bounds;

immutability;

safe loading;

no-regeneration/no-inference guards;

the metadata required for future kinematic/color composition.
```

Do not construct \(J_{\rm kin}\), \(P_{\rm kin}\), \(J_{\rm phys}\), or any projector in C76.

Create:

```text
docs/next_level/c76_c75_blocker_supersession_report.json
docs/next_level/c76_c77_import_preflight.json
```

The report must explicitly establish that:

```text
C75_C64_CROSSWALK_PUBLIC_IMPORT_INCOMPLETE
```

is superseded by C76.

---

# 22. Deterministic reconstruction

Run:

```text
two consecutive complete C76 interface builds;

one clean temporary build;

one serial build;

one supported parallel/sharded build where supported;

one restart/resume build.
```

Require byte-identical:

```text
all crosswalk/runtime payloads;

interface.json;

interface.sha256;

source/API fingerprints;

inventory;

all tracked manifests and reports.
```

The C64 payload must remain byte-identical.

Create:

```text
docs/next_level/c76_deterministic_reconstruction_report.json
docs/next_level/c76_restart_parallel_report.json
docs/next_level/c76_environment_manifest.json
```

---

# 23. Count-once, isolation, and mutation controls

Report:

```text
resolution records;

block records = 733;

raw transverse-basis records;

relative/CM transverse-basis records;

block-local crosswalk entries;

global crosswalk entries;

orientation records = 733;

coefficient links = 171,153;

residue links = 67,920;

CM-ground query records by resolution;

missing IDs;

duplicate IDs;

basis collisions;

unlinked coefficients;

unlinked residues;

unsafe loads;

mutable records;

writable arrays;

builder calls;

inferred identities;

hash mismatches.
```

A positive gate requires every error count to be zero.

Prove C76 construction and public loading are independent of:

```text
C47 quadrature values and argmax phases;

the historical 1e-12 threshold;

C52/C53/C57/C58 numerical objects;

the C74 color arrays except during C77 preflight;

ART25 files.
```

Create at least **384 focused live mutations** including:

```text
crosswalk-source hash;

translation schema;

C64 root;

C76 interface root;

resolution ID;

block ID;

local raw index;

local relative/CM index;

global raw ID;

global relative/CM ID;

basis order;

basis hash;

orientation;

n_CM or m_CM;

coefficient linkage;

status/expression/bound linkage;

residue-certificate linkage;

allow_pickle flag;

object dtype;

path traversal;

symlink escape;

immutability flag;

builder guard;

C77 import-contract field.
```

Create:

```text
docs/next_level/c76_count_once_report.json
docs/next_level/c76_isolation_report.json
docs/next_level/c76_regression_report.json
```

---

# 24. Readiness gate

Issue:

```text
C76_SOURCE_DERIVED_TM_CROSSWALK_PUBLIC_IMPORT_READY
```

only when:

```text
the full C75 baseline reproduces;

both required untracked paths remain untouched;

the C64 scientific payload remains byte-identical;

the C64 public-crosswalk limitation is recorded as a descendant
qualification;

the crosswalk source is fingerprinted and unambiguous;

the deterministic translation, when needed, is complete;

all resolution identities are public and immutable;

all 733 block identities are public and immutable;

the raw transverse basis is public and immutable;

the relative/CM transverse basis is public and immutable;

all block-local/global crosswalks are public and immutable;

all 733 orientations are public and immutable;

all 171,153 coefficient identities link to the crosswalk;

all 67,920 residue certificates link to exact-zero crosswalk pairs;

the CM-ground query returns explicit n_CM=0,m_CM=0 identities;

the minimal C76 interface root binds the exact C64 root;

the top-level TMAuthorityPackage verifies both roots;

all public loaders are safe, immutable, and no-regeneration;

complete public equivalence and closure pass;

the C77 import contract is complete;

the C77 public-only preflight with C76 and C74 passes;

the C75 blocker supersession is explicit;

deterministic reconstruction passes;

count-once, isolation, and mutation tests pass.
```

Do not issue:

```text
C76_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY;

C76_QG_CM_GROUND_EMBEDDING_READY;

C76_IFERM_CONTACT_SUPPORT_READY;

C76_SOURCE_DERIVED_INSTANTANEOUS_FERMION_READY.
```

---

# 25. Exact no-go branches

## A. Existing C64 crosswalk source is inconsistent

```text
C76_QGTM4_CROSSWALK_SOURCE_INCONSISTENT
```

Next:

> **C77/QGTM-SRC — repair only the specifically identified C64 crosswalk-source inconsistency**

## B. Basis or block authority is incomplete

```text
C76_QGTM4_BASIS_AUTHORITY_INCOMPLETE
```

Next:

> **C77/QGTM-BASIS — close only the block/raw/relative-CM basis identity defect**

## C. Local/global crosswalk is incomplete

```text
C76_QGTM4_CROSSWALK_AUTHORITY_INCOMPLETE
```

Next:

> **C77/QGTM-XWALK — close only the local/global mapping, offset, count-once, or linkage defect**

## D. Orientation or CM-ground authority is incomplete

```text
C76_QGTM4_ORIENTATION_CM_AUTHORITY_INCOMPLETE
```

Next:

> **C77/QGTM-ORIENT — close only the orientation, basis metric, or explicit CM-ground identity defect**

## E. Public API or safe loading remains incomplete

```text
C76_QGTM4_PUBLIC_IMPORT_INCOMPLETE
```

Next:

> **C77/QGTM-API — close only the concrete root, loader, immutability, safe-loading, or no-regeneration defect**

## F. Public crosswalk closure succeeds

```text
C76_SOURCE_DERIVED_TM_CROSSWALK_PUBLIC_IMPORT_READY
```

Next:

> **C77/QGEMBED9 — exact physical \(qg\) embedding and descendant-impact closure**

---

# 26. Required deliverables

Create at least:

```text
docs/next_level/c76_implementation_report.md
docs/next_level/c76_api.md
docs/next_level/c76_derivation_authority_manifest.json
docs/next_level/c76_input_fidelity_audit.json

docs/next_level/c76_c64_descendant_correction.json
docs/next_level/c76_crosswalk_source_fingerprint.json
docs/next_level/c76_crosswalk_source_validation.json
docs/next_level/c76_crosswalk_translation_contract.json

docs/next_level/c76_c64_payload_import_report.json
docs/next_level/c76_c64_payload_integrity_validation.json

docs/next_level/c76_tm_resolution_manifest.json
docs/next_level/c76_tm_resolution_validation.json
docs/next_level/c76_tm_block_authority_manifest.json
docs/next_level/c76_tm_block_authority_validation.json

docs/next_level/c76_raw_transverse_basis_manifest.json
docs/next_level/c76_raw_transverse_basis_validation.json
docs/next_level/c76_relcm_transverse_basis_manifest.json
docs/next_level/c76_relcm_transverse_basis_validation.json

docs/next_level/c76_tm_block_crosswalk_manifest.json
docs/next_level/c76_tm_global_crosswalk_manifest.json
docs/next_level/c76_tm_crosswalk_validation.json

docs/next_level/c76_tm_orientation_manifest.json
docs/next_level/c76_tm_orientation_validation.json

docs/next_level/c76_tm_coefficient_crosswalk_linkage.json
docs/next_level/c76_tm_coefficient_crosswalk_validation.json

docs/next_level/c76_cm_ground_query_contract.json
docs/next_level/c76_cm_ground_query_validation.json

docs/next_level/c76_safe_loading_contract.json
docs/next_level/c76_safe_loading_validation.json
docs/next_level/c76_immutable_record_contract.json
docs/next_level/c76_immutable_record_validation.json

docs/next_level/c76_interface_schema_contract.json
docs/next_level/c76_interface_root_manifest.json
docs/next_level/c76_interface_authentication_report.json
docs/next_level/c76_runtime_inventory_report.json
docs/next_level/c76_runtime_inventory_validation.json

docs/next_level/c76_api_contract.json
docs/next_level/c76_api_validation.json
docs/next_level/c76_package_object_contract.json
docs/next_level/c76_package_object_validation.json
docs/next_level/c76_no_regeneration_no_inference_report.json

docs/next_level/c76_public_loader_equivalence_report.json
docs/next_level/c76_crosswalk_closure_report.json

docs/next_level/c76_c77_qgembed9_import_contract.json
docs/next_level/c76_c75_blocker_supersession_report.json
docs/next_level/c76_c77_import_preflight.json

docs/next_level/c76_deterministic_reconstruction_report.json
docs/next_level/c76_restart_parallel_report.json
docs/next_level/c76_environment_manifest.json
docs/next_level/c76_count_once_report.json
docs/next_level/c76_isolation_report.json

docs/next_level/c76_readiness_report.json
docs/next_level/c76_source_sufficiency_decision.json
docs/next_level/c76_no_go_decision_tree.json
docs/next_level/c76_missing_calculation_specification.md
docs/next_level/c76_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/qgtm4/
```

or the repository-equivalent package.

Use:

```text
data/runtime/c76_qgtm4/
```

for the minimal authenticated interface and crosswalk payload.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 27. Acceptance criteria

C76 is complete only when:

1. Baseline `7416778a9554fa325d267bf8f4a0e6b1ae012249` reproduces.
2. Both required untracked paths remain untouched.
3. C64 and C74 historical artifacts remain unchanged.
4. The C64 exact scientific payload remains byte-identical.
5. The C64 public-crosswalk limitation is recorded only in a descendant record.
6. The existing crosswalk source is fingerprinted.
7. Any documentation-to-runtime translation is deterministic and complete.
8. Every parsed field is source-supported.
9. All resolution records are explicit and immutable.
10. Exactly 733 block records are explicit and immutable.
11. Every block dimension and basis hash agrees with C64.
12. Every raw transverse-basis identity is explicit.
13. Every relative/CM basis identity is explicit.
14. \(n_{\rm CM}\) and \(m_{\rm CM}\) are explicit fields.
15. Every local raw index has one canonical basis ID.
16. Every local relative/CM index has one canonical basis ID.
17. Every local index maps to one declared global ID.
18. No missing, duplicate, or colliding crosswalk entry exists.
19. Every block orientation is explicit.
20. Orientation is not inferred from numerical residual minimization.
21. All 171,153 coefficient identities link to crosswalk pairs.
22. All 67,920 residue certificates link to exact-zero crosswalk pairs.
23. The CM-ground query uses explicit \(n_{\rm CM}=m_{\rm CM}=0\).
24. The CM-ground query does not construct the physical embedding.
25. The C76 interface root binds the exact C64 root and crosswalk source.
26. All NumPy loads explicitly use `allow_pickle=False`.
27. Object-dtype, unsafe paths, unindexed files, mutable records, and writable arrays are rejected.
28. The public TMAuthorityPackage verifies both roots before returning data.
29. No public loader calls a builder or inference fallback.
30. Complete public equivalence and crosswalk closure pass.
31. The C77 import contract is complete.
32. The C77 public-only preflight with C76 and C74 passes.
33. The C75 blocker is explicitly superseded.
34. Repeated, clean, serial, parallel, and restart reconstruction is deterministic.
35. All count-once and isolation error counts are zero.
36. At least 384 focused live mutations are detected.
37. No \(J_{\rm kin}\), \(P_{\rm kin}\), physical embedding, projector, descendant impact, endpoint/witness support, contact value/matrix, instantaneous-fermion completion, TMD/matching, inference, or production object is created.
38. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
39. `MSHT20_REP/` remains untouched.
40. The working tree is clean except for the two pre-existing untracked paths.
41. A local completion commit is created and not pushed.

Do not weaken crosswalk-source authority, basis identity, local/global count-once closure, orientation, explicit CM labels, coefficient linkage, safe loading, immutability, or no-regeneration/no-inference semantics to open the gate.

---

# 28. Final Codex response

Report:

- starting and final commits;
- untouched untracked paths;
- descendant correction applied to the C64 readiness claim;
- unchanged C64 package/scientific root;
- crosswalk-source and translation-schema hashes;
- C76 interface-root hash;
- public API/source fingerprints;
- resolution count and identities;
- block count = 733 and aggregate block hash;
- raw and relative/CM basis counts by resolution;
- local/global crosswalk counts and hashes;
- orientation count = 733 and orientation-class counts;
- linked coefficient count = 171,153;
- linked residue count = 67,920;
- CM-ground query counts by resolution and hashes;
- safe-loading and immutability results;
- public TMAuthorityPackage validation;
- no-regeneration/no-inference results;
- complete C64/C76 equivalence and crosswalk closure;
- C75 blocker-supersession result;
- C77 public-only preflight result;
- deterministic reconstruction;
- count-once, isolation, and mutation results;
- exact readiness or no-go status;
- exact next branch;
- confirmation that no kinematic or physical embedding, projector, descendant-impact promotion, endpoint/witness relation, contact value/matrix, complete instantaneous-fermion operator, TMD/matching, inference, or production object was created;
- confirmation that nothing was pushed.

Do not describe a documentation-only crosswalk, private-field access, inferred global offsets, guessed orientation, fixed CM column number, thresholded support, mutable record, unsafe NumPy load, or a loader that calls a builder as a complete public TM crosswalk authority.
