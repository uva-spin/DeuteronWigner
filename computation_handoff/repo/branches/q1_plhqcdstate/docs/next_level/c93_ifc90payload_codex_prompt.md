# C93/IFC90PAYLOAD Codex Work Package

## Title

**Commit-pinned recovery and authenticated materialization of the missing C90 semantic payload: complete normal-form DAG, primitive-family records, expression library, theorem/checker corpus, pair proof inputs, exact root-preimage closure, and C94 public-facade handoff**

## Authoritative baseline

Start from the clean local C92 completion commit:

```text
aeec9cd193f05481295e146e4575ddbdf4943ab5
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "aeec9cd193f05481295e146e4575ddbdf4943ab5"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Consume the sole committed C93 continuation contract completely. The expected path is:

```text
docs/next_level/c93_ifc90payload_contract.json
```

If the sole tracked continuation contract has a different exact filename or extension, discover it, record its path and SHA-256, and do not create or use a competing contract.

If the contract freezes exact statuses, schemas, APIs, or a narrower boundary, preserve them. This work package supplies the authorized commit-pinned recovery, root-preimage validation, payload materialization, and C94 handoff plan; it does not authorize changing C90 science.

The historical C90 completion commit is:

```text
ac622ab358b83f090717d7e7fa179b58f18f526d
```

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical execution environment remains:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

The baseline is authoritative only when it contains and reproduces:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE

C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE
```

with the exact C92 audit result:

```text
four deterministic C90 runtime ledgers:
    byte-identical compact semantic-root ledgers;

C90 public aggregate:
    verifies;

persisted per-pair content:
    pair identity;
    normal-form root;
    primitive-root bundle;
    summary;
    proof result;

not persisted:
    normal-form content;
    primitive-family records;
    record/coefficient/bound/status/ancestry expression content
    and roots;
    theorem specification;
    proof-checker inputs;

C92 decision:
    Route C;

C92 prohibition:
    no facade or snapshot may be fabricated from roots alone;
    no C77/C78/C82/C89/C90 scientific rebuild was authorized
    inside C92.
```

Verify and record the complete C90 aggregate semantic root, all C90 ledger roots, the C90 runtime root/index hashes, C90 source/API fingerprints, C87 roots, C88 schema root, C89 program roots, and the C92 audit hashes from the repository. Prefixes in prose are not authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification

C93 is an **authorized descendant recovery of omitted scientific payload preimages**.

It is not:

```text
a public facade over incomplete data;
an inference of content from a cryptographic hash;
a new C82 or C90 scientific calculation;
a change to the C90 normal forms;
a replacement theorem;
or a claim that the recovered payload existed in the original C90
runtime directory.
```

C92 correctly prohibited rebuilding omitted objects while claiming to
provide a read-only facade. C93 is the sole continuation explicitly
authorized to reconstruct the missing objects from:

```text
the exact C90 completion source;
the exact frozen C90 scientific inputs;
the historical C87-qualified execution environment;
and an independent source-chain route.
```

Every reconstructed object must validate as a preimage of an already
frozen C90 scientific root or proof result. C93 may not change a root to
fit newly produced content.

No external publication is required for:

```text
materializing a typed normal form;
serializing a primitive table;
recovering an expression AST from exact source;
packaging a theorem specification;
or binding proof inputs to an existing proof result.
```

Fail closed when a missing object cannot be reconstructed uniquely and
deterministically from the frozen C90 source chain, or when its recovered
content does not reproduce the corresponding C90 root/result.

---

# 2. Claim boundary and descendant qualifications

Do not alter historical C90, C91, or C92 artifacts.

Create explicit descendant qualifications:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY
    -> C90_SCIENCE_COMPLETE_ORIGINAL_RUNTIME_PAYLOAD_CONTENT_INCOMPLETE

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
    -> descendant compiler complete;
       historical comparison blocked by absent public historical
       semantic content;

C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE
    -> Route C correctly establishes that roots alone cannot support
       a legal no-recomputation facade.
```

The new C93 payload must carry:

```text
payload_provenance =
    DESCENDANT_RECOVERED_PREIMAGE_OF_C90_FROZEN_SCIENTIFIC_ROOTS

original_c90_runtime_payload_claim =
    NOT_CLAIMED
```

Create:

```text
docs/next_level/c93_descendant_qualification.json
docs/next_level/c93_claim_boundary.json
```

---

# 3. Exact purpose and stopping point

C93 must produce:

```text
a clean commit-pinned reconstruction environment at the exact C90
completion commit;

a complete census of every omitted scientific payload class;

two independent recovery routes;

a canonical content-addressed normal-form DAG for all 154,830 pairs;

complete authenticated primitive-family records;

a complete canonical expression library for record, coefficient,
bound, status, and ancestry semantics;

the immutable C90 theorem specification;

the immutable C90 proof-checker source/semantic corpus and all
required proof-input records;

pair-level proof-certificate inputs and recovered proof results;

exact pair-by-pair root-preimage closure against the existing C90
compact ledger;

exact primitive-root closure;

exact theorem/checker regression closure;

reproduction of the unchanged C90 aggregate semantic authority;

a new C93-owned authenticated semantic-payload capsule;

a strict capsule-only immutable loader for C94;

and a C94/IFEQUIVAPI2 import contract.
```

C93 must not:

```text
perform the historical-versus-descendant C91 comparison;

issue scientific equivalence between historical and descendant C82;

present the recovered capsule as the original C90 runtime;

write 891,992,018 expanded records;

form a projected-coefficient times C80-kernel product;

construct a contact matrix or action;

choose a physical coupling or counterterm;

add C53 or C58;

or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C93_C90_AUTHENTICATED_SEMANTIC_PAYLOAD_READY
```

If the sole C93 contract freezes another exact spelling, preserve it and
record an explicit semantic alias.

The favorable continuation is:

> **C94/IFEQUIVAPI2 — immutable public pair/primitive/theorem facade over the authenticated C93 payload, with complete pagination, exported proof checking, and public-only exhaustive-equivalence preflight**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C87:
    canonical color scientific/capsule roots;
    historical environment staging;
    claim boundary;

C88:
    complete scientific-record schema;
    field classification;
    canonical record order;
    exact logical census;

C89:
    immutable factorized pair programs;
    mixed-radix rank/unrank;
    exact pair counts;
    record reconstruction semantics;

C90:
    typed semantic IR;
    primitive-table definitions;
    Route-A historical compiler;
    Route-B C89 normalizer;
    canonical normal-form rules;
    expression representations;
    pair summaries;
    expansion-equivalence theorem;
    proof checker;
    record audits;
    compact pair ledger;
    deterministic passes;
    runtime root/index;
    source/API fingerprints;
    tests and validator;

C91:
    independent descendant compiler and domain freeze;
    public-equivalence no-go;

C92:
    actual C90 payload audit;
    Route-C decision;
    exact omitted-object list;
    C93 continuation contract.
```

Create:

```text
docs/next_level/c93_derivation_authority_manifest.json
docs/next_level/c93_input_fidelity_audit.json
```

---

# 5. Freeze the existing C90 compact authority

Consume C90 through its existing immutable public aggregate API and direct
authenticated runtime audit.

Freeze:

```text
historical C90 completion commit;
historical C82 commit and environment qualification;
C87 scientific/capsule roots;
C88 schema and order roots;
C89 pair-program roots;
C90 semantic-IR and normal-form versions;
all three resolution identities;
154,830 pair identities and their compact ledger records;
all pair normal-form roots;
all primitive-root bundles;
all pair summaries;
all pair proof results;
resolution roots;
aggregate C90 semantic root;
C90 runtime root/index;
C90 compiler, normalizer, theorem, and checker source hashes.
```

Issue:

```text
C93_C90_COMPACT_AUTHORITY_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c93_input_freeze.json
```

After this freeze, no existing C90 root, summary, or proof result may be
changed or reissued under a modified value.

---

# 6. Complete omitted-payload census

Derive the exact missing-object census before reconstruction.

At minimum inventory:

```text
unique normalized IR nodes;
pair-to-normal-form-root bindings;
normal-form node graphs;
unique primitive families;
primitive-family record counts;
primitive records;
record-construction expressions;
projected-coefficient expressions;
bound expressions/rules;
status expressions/rules;
ancestry expressions/templates;
first/last logical-record identity expressions;
theorem-specification records;
proof-checker semantic rules;
pair proof inputs;
pair proof certificates/results;
serializer and canonicalization versions.
```

For each class record:

```text
scientific role;
expected C90 root/result;
historical producer function;
historical source path and hash;
required upstream inputs;
estimated record count;
estimated bytes;
deduplication opportunity;
reconstruction Route-A availability;
independent Route-B availability;
public-facade requirement in C94.
```

Create:

```text
docs/next_level/c93_missing_payload_census.json
docs/next_level/c93_missing_payload_dependency_graph.json
```

A favorable result requires no unknown payload class.

---

# 7. Capacity and canonical representation decision

Before materializing content, estimate the persistent footprint of:

```text
flat per-pair JSON;
deduplicated typed-node DAG;
factorized primitive tables;
content-addressed expression store;
proof-input/certificate store.
```

Select a representation that fits the available filesystem with a declared
safety reserve.

The preferred representation is:

```text
one deduplicated normal-form node store keyed by scientific hash;

one pair-binding table mapping each pair to a normal-form root;

one content-addressed expression store;

paged primitive-family stores;

one theorem/checker corpus;

one proof-input/certificate store;

compact indices and Merkle/inclusion structures.
```

Do not write expanded C88 logical records.

Create:

```text
docs/next_level/c93_representation_decision.json
docs/next_level/c93_capacity_and_reserve_report.json
```

---

# 8. Controlled commit-pinned reconstruction environment

Create clean detached worktrees or immutable Git archives for:

```text
C90 completion:
    ac622ab358b83f090717d7e7fa179b58f18f526d;

historical C82 source:
    8e47231ab565f0f729d335b39aa98881176ba166;

C89 completion source and runtime authority;
C87 capsule authority.
```

Use a controlled environment:

```text
TZ=UTC;
LC_ALL=C;
PYTHONHASHSEED=0;
fixed SOURCE_DATE_EPOCH where supported;
network disabled;
current descendant runtime hidden;
build-if-missing and repair-if-missing fallbacks poisoned.
```

Historical tracked source must remain byte-identical and clean.

Create:

```text
docs/next_level/c93_reconstruction_environment_manifest.json
docs/next_level/c93_historical_source_lock_manifest.json
```

---

# 9. Route A — exact C90 replay and extraction

Route A must execute the exact C90 completion source in the frozen
historical environment.

It may use a C93-owned extraction adapter only to serialize returned typed
objects that the original C90 run held in memory. The adapter must not
alter:

```text
compiler inputs;
program nodes;
normalization rules;
primitive values;
expression rules;
proof inputs;
proof results;
canonical ordering;
or scientific hashes.
```

For every supported pair, capture:

```text
Route-A normalized program content;
normal-form node graph;
primitive references;
record/coefficient/bound/status/ancestry expressions;
exact logical count;
first/last identities;
summary;
proof-checker inputs;
proof result.
```

Capture all primitive tables and the theorem/checker corpus.

Run Route A twice in separate clean worktrees.

Create:

```text
docs/next_level/c93_route_a_c90_replay.json
docs/next_level/c93_route_a_determinism.json
```

A positive gate requires byte-identical canonical recovered content across
both Route-A runs.

---

# 10. Route B — independent source-chain recovery

Implement a separate C93 recovery route.

Route B must not consume Route-A materialized objects.

It must independently reconstruct the same payload from:

```text
the immutable C89 pair programs;
the exact C90 semantic-IR schema;
the exact C90 normal-form specification;
the frozen C77/C78/C80/C82 primitive authorities;
the C87 color authority;
the exact C90 theorem source.
```

Route B may use independently implemented canonical serializers and AST
walkers, but its scientific content must be compared field by field with
Route A before historical C90 serialization is applied.

Route B must recover:

```text
the same typed normal-form DAG;
the same pair bindings;
the same primitive records;
the same expressions;
the same theorem specification;
the same proof inputs;
and the same proof results.
```

Create:

```text
docs/next_level/c93_route_b_independent_recovery.json
docs/next_level/c93_route_a_b_equivalence.json
```

A sampled comparison is insufficient.

---

# 11. Canonical normal-form DAG

Define one immutable node record equivalent to:

```python
RecoveredC90NormalFormNode
```

Each node must retain:

```text
semantic-IR version;
normal-form version;
node type;
canonical ordered children;
primitive references;
exact cardinality;
ordering/mixed-radix semantics;
filter or permutation identity;
record-map identity;
expression dependencies;
scientific node hash.
```

Supported node types and semantics must match C90 exactly.

Requirements:

```text
all nodes referenced by pair roots exist;
no orphan node exists;
no cycle exists unless explicitly allowed and proved by C90;
child order is preserved;
all cardinalities reproduce;
all pair root hashes recompute exactly.
```

Create:

```text
docs/next_level/c93_normal_form_dag_schema.json
docs/next_level/c93_normal_form_dag_validation.json
```

---

# 12. Primitive scientific-family recovery

Recover every primitive family required by C90.

Each family must expose:

```text
family ID;
schema;
canonical order;
record count;
complete canonical records;
scientific family root;
producer/source identity;
safe immutable encoding.
```

Required families include the actual repository equivalents of:

```text
supported-pair records;
physical bra/ket identities;
C77 projection amplitudes and certified bounds;
C78 witnesses, endpoints, retained-q intermediates, and signs;
C87 color records;
C80 coordinate templates and equivalence identities;
metric/conjugation records;
coefficient-expression primitives;
bound primitives;
status primitives;
ancestry primitives.
```

For every family require:

```text
Route-A/Route-B record equality;
record-count equality;
canonical-order equality;
family-root equality to C90.
```

Create:

```text
docs/next_level/c93_primitive_family_manifest.json
docs/next_level/c93_primitive_family_root_closure.json
```

A root without recovered record content is insufficient.

---

# 13. Expression library recovery

Recover canonical typed expression records for:

```text
C88 scientific-record construction;
C80 coordinate construction references;
projected-coefficient construction;
bra conjugation and metric factors;
bound composition;
terminal status selection;
witness multiplicity;
ancestry construction;
first/last identity construction;
closed-form summaries.
```

No expression may contain:

```text
an opaque Python callable;
an unresolved closure;
filesystem iteration;
unordered maps;
floating-value equality;
or C80 numerical kernel values.
```

Each expression record must have:

```text
typed AST or bytecode;
input schema;
output schema;
canonical serialization;
scientific expression root;
source/function provenance.
```

Create:

```text
docs/next_level/c93_expression_library_schema.json
docs/next_level/c93_expression_library_validation.json
```

Require exact pair expression-root closure against every available C90
ledger field and proof input.

---

# 14. Theorem specification and checker corpus

Recover the exact C90 expansion theorem as a machine-readable public-ready
scientific object.

The theorem corpus must bind:

```text
semantic-IR node definitions;
node expansion semantics;
normal-form rules;
primitive-root requirements;
cardinality and order rules;
record-map requirements;
coefficient/bound/status/ancestry rules;
the theorem statement;
proof-certificate schema;
theorem version;
checker source hash;
checker API hash.
```

Recover the proof checker from exact C90 source without semantic
modification.

Create:

```text
docs/next_level/c93_theorem_specification.json
docs/next_level/c93_proof_checker_corpus.json
docs/next_level/c93_theorem_checker_source_lock.json
```

Do not yet expose the full public facade; C94 will do so.

---

# 15. Pair proof-input and certificate recovery

For all 154,830 historical pairs, recover the complete proof input:

```text
Route-A normal form;
Route-B normal form;
primitive-equivalence bindings;
schema and theorem versions;
exact cardinality;
ordering identity;
record/coefficient/bound/status/ancestry expression identities.
```

Run the recovered exact checker and require its result to equal the compact
C90 proof result for every pair.

Record:

```text
proof-input root;
proof-certificate root;
proof result;
failure diagnostics.
```

Create:

```text
docs/next_level/c93_pair_proof_input_manifest.json
docs/next_level/c93_pair_proof_regression.json
```

A favorable result requires:

```text
154,830 proof inputs recovered;
154,830 checker results reproduced;
proof-result mismatches = 0.
```

---

# 16. Exact root-preimage closure

For every historical pair, verify:

```text
hash(recovered normal-form content)
    == C90 normal-form root;

hash(recovered primitive dependency bundle)
    == C90 primitive-root bundle;

recovered summary
    == C90 summary;

checker(recovered proof inputs)
    == C90 proof result.
```

For every primitive family, verify its recovered root equals the frozen C90
root.

Recompute the C90 resolution and aggregate semantic roots from the unchanged
compact ledger and recovered content bindings.

Require:

```text
pair normal-form mismatches = 0;
primitive-bundle mismatches = 0;
summary mismatches = 0;
proof-result mismatches = 0;
resolution-root mismatches = 0;
aggregate-root mismatch = 0.
```

Create:

```text
docs/next_level/c93_root_preimage_closure_report.json
docs/next_level/c93_c90_aggregate_root_reproduction.json
```

Do not alter or replace an expected root when closure fails.

---

# 17. New C93-owned authenticated payload capsule

Build a new descendant capsule under a repository-equivalent path such as:

```text
data/authority/c93_c90_semantic_payload/
```

The capsule must contain or authenticate:

```text
normal-form node DAG;
pair-to-root bindings;
primitive-family records;
expression library;
theorem specification;
proof-checker corpus;
pair proof inputs/certificates;
summaries;
source/provenance locks;
root-preimage closure records;
package index and root.
```

The capsule root must bind:

```text
the unchanged C90 aggregate scientific root;
the exact C90 completion commit;
all recovered payload roots;
the representation schemas;
the complete file inventory;
the claim boundary.
```

The capsule must explicitly state:

```text
scientific_relation =
    RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY;

original_runtime_identity =
    NOT_CLAIMED;
```

Create:

```text
docs/next_level/c93_capsule_schema_contract.json
docs/next_level/c93_capsule_root_manifest.json
docs/next_level/c93_capsule_inventory.json
docs/next_level/c93_capsule_claim_boundary.json
```

---

# 18. Safe immutable payloads

Every numerical load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payloads;
path traversal;
unexpected symlinks;
unindexed files;
shape/dtype/endianness mismatch;
unknown node or expression type;
writable arrays;
mutable returned records.
```

Use canonical JSON or another deterministic safe representation for typed
nodes, expressions, theorem records, and proof inputs.

Create:

```text
docs/next_level/c93_safe_loading_contract.json
docs/next_level/c93_safe_loading_validation.json
```

---

# 19. Strict capsule-only loader

Implement under:

```text
src/deuteron_wigner/bridge/ifc90payload/
```

or the exact repository-equivalent package.

Provide builder/validation operations equivalent to:

```python
load_verified_c90_semantic_payload_capsule()

verify_c90_semantic_payload_capsule()

recovered_pair_binding(pair_id, resolution)

recovered_normal_form(root_id)

recovered_primitive_family(family_id)

recovered_primitive_record(family_id, record_id)

recovered_expression(expression_id)

recovered_theorem_specification()

recovered_pair_proof_inputs(pair_id, resolution)

verify_recovered_pair_against_c90(pair_id, resolution)
```

After capsule creation, poison:

```text
C77/C78/C82 builders;
C89 pair-program builder;
C90 compilers and normalizers;
historical worktree staging;
build-if-missing;
repair-if-missing;
network access.
```

The loader must continue to operate entirely from authenticated C93
payloads.

Create:

```text
docs/next_level/c93_loader_contract.json
docs/next_level/c93_loader_validation.json
docs/next_level/c93_no_recomputation_report.json
```

This is a payload loader for C94, not yet the final paged public-equivalence
facade.

---

# 20. Deterministic reconstruction

Run:

```text
two complete Route-A recoveries;
two complete Route-B recoveries;
two complete capsule builds;
one clean build;
one serial build;
one deterministic sharded build;
one interrupted/resumed build.
```

Require byte-identical:

```text
normal-form DAG;
primitive stores;
expression store;
theorem/checker corpus;
proof inputs/certificates;
pair bindings;
capsule root/index;
tracked reports.
```

Use pair-atomic or content-addressed checkpointing.

Create:

```text
docs/next_level/c93_deterministic_reconstruction_report.json
docs/next_level/c93_restart_validation.json
docs/next_level/c93_resource_and_scaling_report.json
```

---

# 21. C94/IFEQUIVAPI2 import contract

Define the exact contract by which C94 consumes:

```text
the C93 capsule root and claim boundary;
the bound C90 scientific root;
the complete pair-binding table;
the normal-form DAG;
all primitive-family stores;
the expression library;
the theorem specification;
the checker corpus;
the pair proof-input/certificate store;
the safe immutable capsule loader.
```

C94 must create:

```text
complete 154,830-pair authenticated enumeration;
bounded pagination;
direct pair lookup and inclusion proofs;
primitive-family public paging;
public theorem/checker operations;
strict no-recomputation loading;
public-only C95 exhaustive-equivalence preflight.
```

C94 must not reconstruct C90 science again.

Create:

```text
docs/next_level/c93_c94_ifequivapi2_import_contract.json
```

---

# 22. Isolation and mutation controls

Required failures include mutations of:

```text
C90 completion commit;
C90 aggregate root;
C90 ledger pair identity;
expected normal-form root;
normal-form node type;
child order;
primitive reference;
cardinality;
filter or permutation identity;
primitive record;
primitive family order;
expression AST;
coefficient expression;
bound/status/ancestry rule;
theorem statement;
node semantics;
proof-checker source/API fingerprint;
proof input;
proof result;
pair summary;
pair binding;
capsule root/index;
payload file hash;
safe-loader policy;
no-recomputation guard;
C94 import-contract field.
```

The recovered scientific content must remain invariant under mutations of:

```text
temporary worktree path;
host/process ID;
wall-clock timestamp;
capsule shard layout;
checkpoint frequency;
narrative report path.
```

Poison:

```text
C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical coupling;
counterterm interfaces.
```

C93 reconstruction and capsule loading must remain independent of those
values.

Create at least **384 focused live mutations** of actual nodes, primitive
records, expressions, theorem/checker objects, proof inputs, pair bindings,
capsule files, loaders, and roots.

Create:

```text
docs/next_level/c93_isolation_report.json
docs/next_level/c93_regression_report.json
```

---

# 23. Readiness and continuation decisions

Select exactly one branch.

## 23.1 Favorable branch

Issue the exact positive status frozen by the sole C93 contract, with
preferred semantic identity:

```text
C93_C90_AUTHENTICATED_SEMANTIC_PAYLOAD_READY
```

Required:

```text
the C90 compact authority freezes;
the omitted-payload census is complete;
the selected representation fits within reserve;
Route A reconstructs deterministically from exact C90 source;
Route B reconstructs independently;
Route A and Route B agree exhaustively;
all 154,830 normal-form contents reproduce their C90 roots;
all primitive families reproduce their C90 roots;
all expressions are recovered and typed;
the theorem specification and checker corpus are recovered;
all 154,830 proof results reproduce;
all summaries reproduce;
all resolution and aggregate C90 roots reproduce;
the new C93 capsule is complete and authenticated;
the claim boundary denies original-runtime identity;
the capsule-only loader performs no upstream recomputation;
determinism, restart, and mutations pass.
```

Next:

> **C94/IFEQUIVAPI2 — authenticated immutable public facade over the C93 semantic payload**

## 23.2 Normal-form recovery incomplete

```text
C93_IFC90PAYLOAD_NORMAL_FORM_RECOVERY_INCOMPLETE
```

Next:

> **C94/IFNFRECOVER — repair only the specifically identified missing node, pair binding, ordering, serializer, or normal-form root-preimage defect**

## 23.3 Primitive payload recovery incomplete

```text
C93_IFC90PAYLOAD_PRIMITIVE_RECOVERY_INCOMPLETE
```

Next:

> **C94/IFPRIMRECOVER — repair only the specifically identified primitive family, record, order, provenance, or root-preimage defect**

## 23.4 Expression payload recovery incomplete

```text
C93_IFC90PAYLOAD_EXPRESSION_RECOVERY_INCOMPLETE
```

Next:

> **C94/IFEXPRRECOVER — repair only the record, coefficient, bound, status, ancestry, or summary expression-recovery defect**

## 23.5 Theorem/checker recovery incomplete

```text
C93_IFC90PAYLOAD_THEOREM_RECOVERY_INCOMPLETE
```

Next:

> **C94/IFTHEOREMRECOVER — repair only the theorem specification, checker source, proof-input, or proof-regression defect**

## 23.6 Root-preimage mismatch

```text
C93_IFC90PAYLOAD_ROOT_PREIMAGE_MISMATCH
```

Next:

> **C94/IFROOTPREIMAGE — localize only the recovered-content versus frozen-C90 root or proof-result mismatch**

## 23.7 Capsule/loader incomplete

```text
C93_IFC90PAYLOAD_CAPSULE_INCOMPLETE
```

Next:

> **C94/IFCAPSULE2 — repair only the capsule root/index, safe encoding, immutable loader, no-recomputation, or deterministic-build defect**

Do not issue public-equivalence-import, historical-versus-descendant
equivalence, final coefficient persistence, coefficient-times-kernel,
contact-matrix, complete-instantaneous-fermion, counterterm-solved,
local-QCD, TMD, matching, inference, or production status.

---

# 24. Essential deliverables

Create at least:

```text
docs/next_level/c93_implementation_report.md
docs/next_level/c93_derivation_authority_manifest.json
docs/next_level/c93_input_fidelity_audit.json
docs/next_level/c93_descendant_qualification.json
docs/next_level/c93_claim_boundary.json
docs/next_level/c93_input_freeze.json

docs/next_level/c93_missing_payload_census.json
docs/next_level/c93_missing_payload_dependency_graph.json
docs/next_level/c93_representation_decision.json
docs/next_level/c93_capacity_and_reserve_report.json
docs/next_level/c93_reconstruction_environment_manifest.json
docs/next_level/c93_historical_source_lock_manifest.json

docs/next_level/c93_route_a_c90_replay.json
docs/next_level/c93_route_a_determinism.json
docs/next_level/c93_route_b_independent_recovery.json
docs/next_level/c93_route_a_b_equivalence.json

docs/next_level/c93_normal_form_dag_schema.json
docs/next_level/c93_normal_form_dag_validation.json
docs/next_level/c93_primitive_family_manifest.json
docs/next_level/c93_primitive_family_root_closure.json
docs/next_level/c93_expression_library_schema.json
docs/next_level/c93_expression_library_validation.json

docs/next_level/c93_theorem_specification.json
docs/next_level/c93_proof_checker_corpus.json
docs/next_level/c93_theorem_checker_source_lock.json
docs/next_level/c93_pair_proof_input_manifest.json
docs/next_level/c93_pair_proof_regression.json

docs/next_level/c93_root_preimage_closure_report.json
docs/next_level/c93_c90_aggregate_root_reproduction.json

docs/next_level/c93_capsule_schema_contract.json
docs/next_level/c93_capsule_root_manifest.json
docs/next_level/c93_capsule_inventory.json
docs/next_level/c93_capsule_claim_boundary.json
docs/next_level/c93_safe_loading_contract.json
docs/next_level/c93_safe_loading_validation.json

docs/next_level/c93_loader_contract.json
docs/next_level/c93_loader_validation.json
docs/next_level/c93_no_recomputation_report.json
docs/next_level/c93_deterministic_reconstruction_report.json
docs/next_level/c93_restart_validation.json
docs/next_level/c93_resource_and_scaling_report.json
docs/next_level/c93_c94_ifequivapi2_import_contract.json

docs/next_level/c93_isolation_report.json
docs/next_level/c93_readiness_report.json
docs/next_level/c93_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifc90payload/
```

Use a new retained authority root equivalent to:

```text
data/authority/c93_c90_semantic_payload/
```

and temporary runtime space equivalent to:

```text
data/runtime/c93_ifc90payload/
```

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the protected C69 prompt.

---

# 25. Acceptance criteria

C93 is complete only when:

1. Baseline `aeec9cd193f05481295e146e4575ddbdf4943ab5` reproduces.
2. Both protected untracked paths remain untouched.
3. C87/C88/C89/C90/C91/C92 historical artifacts remain unchanged.
4. The sole committed C93 continuation contract is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. The recovered payload is not called the original C90 runtime payload.
7. The C90 compact authority verifies before reconstruction.
8. The complete omitted-payload census has no unknown class.
9. The selected representation fits within a declared filesystem reserve.
10. No expanded 891,992,018-record stream is written.
11. Route A uses the exact C90 completion source.
12. Historical source remains tracked-clean.
13. Route B is independent of Route-A materialized output.
14. Route A and Route B agree exhaustively.
15. Every one of the 154,830 pair normal forms is recovered.
16. Every recovered normal form reproduces its C90 root.
17. Every normal-form node is typed, ordered, content addressed, and reachable.
18. Every required primitive family is recovered completely.
19. Every primitive family reproduces its C90 root.
20. Every record/coefficient/bound/status/ancestry expression is recovered in typed form.
21. No opaque callable enters scientific identity.
22. The exact C90 theorem specification is recovered.
23. The exact checker source/API identity is frozen.
24. All 154,830 proof-input records are recovered.
25. All 154,830 C90 proof results reproduce.
26. All pair summaries reproduce.
27. All resolution roots reproduce.
28. The C90 aggregate semantic root reproduces.
29. The C93 capsule binds all payload files and the unchanged C90 root.
30. Every NumPy load explicitly uses `allow_pickle=False`.
31. Object dtype, unsafe paths, unindexed files, writable arrays, and mutable records are rejected.
32. The capsule-only loader uses no C77/C78/C82/C89/C90 scientific builder.
33. Two complete reconstructions agree.
34. Clean, serial, sharded, and interrupted/resumed builds agree.
35. The C94 import contract is complete.
36. At least 384 focused live mutations pass.
37. No public-equivalence facade is claimed in C93.
38. No historical-versus-descendant equivalence is claimed.
39. No final coefficient persistence package is claimed.
40. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
41. `NO_JOINT_MEASURE`, the 216-route registry, all 642 ART25 identities, and authoritative artifacts remain unchanged.
42. The working tree is clean except for the two protected untracked paths.
43. A local completion commit is created and not pushed.

Do not weaken root-preimage closure, Route-A/Route-B independence, primitive
record completeness, expression typing, theorem/checker recovery, or the
new-versus-original payload claim boundary to open the gate.

---

# 26. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed C93 contract path and hash;
- historical C90 and C82 commits;
- historical environment qualification;
- full frozen C90 runtime and scientific roots;
- omitted-payload census by class;
- representation decision and capacity reserve;
- Route-A and Route-B source/implementation identities;
- normal-form node and pair-binding counts;
- primitive-family and primitive-record counts;
- expression-record counts by class;
- theorem-specification and checker hashes;
- pair proof-input and reproduced-proof counts;
- mismatch counts for every recovered payload class;
- pair, primitive, summary, proof, resolution, and aggregate root-preimage closure;
- C93 capsule path, root, index, file count, and size;
- capsule claim boundary;
- safe-loading and no-recomputation results;
- deterministic/restart/sharded results;
- C94 import-contract result;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime recovery remains unclaimed;
- confirmation that the C93 capsule is a descendant recovered preimage of frozen C90 scientific roots, not the original C90 runtime payload;
- confirmation that roots remain factorized-semantic roots, not expanded serialized-record hashes;
- confirmation that no public equivalence facade, historical-versus-descendant equivalence, expanded full-record stream, final coefficient package, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
