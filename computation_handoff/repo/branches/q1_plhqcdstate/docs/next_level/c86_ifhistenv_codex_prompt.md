# C86/IFHISTENV Codex Work Package

## Title

**Historical C82 runtime-environment recovery: complete ignored-dependency closure, authenticated retained C72/C74 color-authority capsule, clean read-only staging, historical scientific-stream reconstruction, and C87 equivalence handoff**

## Authoritative baseline

Start from the clean local C85 completion commit:

```text
c66d17383e9a344ced10b61d393ce96e540c570e
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "c66d17383e9a344ced10b61d393ce96e540c570e"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c86_ifhistenv_contract.md
```

If that contract freezes exact status names, schemas, APIs, or a narrower
scientific boundary, preserve them. This work package supplies the
dependency-recovery, capsule, staging, and historical-reconstruction plan;
it does not authorize weakening the committed contract.

The historical C82 completion commit is:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The known first missing historical dependency is:

```text
data/runtime/c72_qgcolor5/root.json
```

The relevant completion commits include:

```text
C72/QGCOLOR5:
    369e95e1004c2f896f8a41e5c3270f7644e3391d

C74/QGCOLOR6:
    56b7e6c2bf68abc46493990afbdf1d8a2ce86415

C77/QGEMBED9:
    5a117fa29442195a52f67da6fa00368a28a13e8f

C78/IFSUPPORT2:
    1ddf21c230d3a16ee7e52ed09d84140f43781bb8

C80/IFKERNEL2:
    0868a88422380339d9d5e0631830ba7528bd776f

C82/IFAGG:
    8e47231ab565f0f729d335b39aa98881176ba166
```

Verify all commits, roots, hashes, paths, schemas, and dependency roles from
the repository. This prompt is not numerical authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification

C86 addresses a **historical execution-environment dependency**, not a new
physics calculation and not an external-authority gap.

The C85 no-go establishes:

```text
the exact C82 detached worktree cannot instantiate its color
authority because an ignored runtime dependency is absent;

therefore no historical C82 scientific record stream exists yet
for equivalence comparison.
```

The absence of an ignored generated file from Git does not itself invalidate
the C72, C74, or C82 science. It does mean that the historical environment
must be recovered and authenticated before scientific equivalence can be
tested.

No external paper is required for:

```text
recovering project-generated runtime files;
rebuilding them from exact historical source;
verifying them against tracked manifests;
creating a retained content-addressed capsule;
or staging that capsule into a detached worktree.
```

Fail closed only when:

```text
a required runtime dependency cannot be recovered or rebuilt;
the recovered payload contradicts its tracked scientific manifest;
multiple candidates with different scientific content remain;
the complete dependency closure cannot be established;
or the reconstructed historical C82 stream is nondeterministic.
```

Do not classify an absent ignored file as an absent physical authority.

---

# 2. Descendant qualification

Do not alter historical C72, C74, C82, C84, or C85 artifacts.

Create an explicit descendant qualification:

```text
C85_C82_SCIENTIFIC_PAYLOAD_EQUIVALENCE_INCOMPLETE
    -> cause:
       HISTORICAL_RUNTIME_DEPENDENCY_CLOSURE_INCOMPLETE

known first missing node:
    data/runtime/c72_qgcolor5/root.json

scientific interpretation:
    historical C82 science is not rejected;
    equivalence is untested because the historical execution
    environment is incomplete.
```

Create:

```text
docs/next_level/c86_descendant_qualification.json
```

---

# 3. Exact purpose and stopping point

C86 must produce:

```text
a complete ignored-runtime dependency graph for historical C82;

a typed inventory of every required dependency path;

a recovery decision and provenance record for every dependency;

a complete self-contained retained dependency capsule for all
scientifically required runtime objects that cannot be rebuilt
inside the detached C82 worktree without external local state;

an authenticated capsule root and index;

a strict read-only staging mechanism into a clean detached C82
worktree;

a successfully instantiated historical C72/C74 color authority;

a successfully instantiated complete historical C82 dependency
closure;

a deterministic historical C82 scientific-stream reconstruction;

and a C87/IFEQUIV2 import contract.
```

C86 must not:

```text
compare the historical stream exhaustively with the descendant
stream beyond basic reconstruction holdouts;

declare scientific payload equivalence;

persist the final C82 pair-coordinate public snapshot;

form coefficient-times-kernel products;

construct a contact matrix or action;

choose a physical coupling or counterterm;

add C53 or C58;

or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C86_C82_HISTORICAL_RUNTIME_ENVIRONMENT_READY
```

If `c86_ifhistenv_contract.md` freezes a different exact spelling, preserve
it and record a semantic alias.

The favorable continuation is:

> **C87/IFEQUIV2 — exhaustive historical-versus-descendant C82 scientific-record equivalence using the immutable C86 recovered environment**

---

# 4. Mandatory repository and filesystem audit

Read completely the actual repository equivalents of:

```text
C72:
    implementation report;
    runtime root/index schema;
    exact 24-row/3-column/72-record authority;
    source/API fingerprints;
    numerical arrays and bounds;
    deterministic reconstruction;
    public-loader expectations;

C74:
    descendant qualification of C72;
    safe public import;
    bound C72 root;
    public API and interface root;
    no-regeneration contract;

C77/C78/C80/C82:
    public package dependencies;
    package/root expectations;
    runtime paths;
    builder and loader behavior;
    deterministic reconstruction records;

C83/C84/C85:
    import failure;
    materialization failure;
    historical reconstruction failure;
    c86_ifhistenv_contract.md.
```

Inventory all candidate local runtime copies without changing them:

```bash
find data/runtime -maxdepth 4 -type f | sort
find . -path '*/data/runtime/*' -type f | sort
git worktree list --porcelain
```

Also audit documented local paths, temporary worktrees, retained caches, and
content-addressed copies mentioned in tracked reports. Do not crawl
unrelated user directories.

Create:

```text
docs/next_level/c86_derivation_authority_manifest.json
docs/next_level/c86_input_fidelity_audit.json
docs/next_level/c86_runtime_candidate_inventory.json
```

---

# 5. Build the complete historical C82 runtime-dependency graph

In a clean detached worktree at the exact C82 commit, attempt to instantiate
the C82 scientific route with no copied runtime data.

Instrument the loader at the filesystem/public-API boundary and record each
missing required path in dependency order.

For every missing node record:

```text
runtime path;
consumer package and public operation;
producer package;
producer completion commit;
scientific role;
expected root/index/object hash;
tracked manifest or report that constrains it;
whether it can be rebuilt from tracked historical source alone;
whether a local candidate exists;
whether redistribution is permitted;
whether the object is scientifically essential or instance-only.
```

Continue the audit after staging each independently verified node until the
full C82 dependency closure is known. Do not stop permanently after the
first C72 error if additional ignored dependencies become visible.

Create:

```text
docs/next_level/c86_historical_dependency_graph.json
docs/next_level/c86_historical_dependency_closure_report.json
```

A favorable result requires no unknown dependency node.

---

# 6. Recovery routes

Evaluate the following mutually exclusive recovery routes for each required
runtime object.

## Route A — exact local content-addressed candidate

Use only when an existing local candidate:

```text
verifies against its own root/index;
matches every tracked scientific manifest;
matches the expected producer commit and API schema;
contains all indexed payloads;
and has no unexplained scientific-field difference.
```

## Route B — clean historical rebuild

Rebuild in a detached worktree at the exact producer completion commit,
using only:

```text
tracked source and manifests;
already verified lower-level dependency capsules;
the controlled C86 environment;
and the historical builder/validator commands.
```

Require repeated deterministic output.

## Route C — Git-object/content-addressed recovery

Recover project-generated bytes from:

```text
another exact historical worktree;
a content-addressed retained local cache;
a documented archive;
or Git-object-backed retained source/payload records.
```

Require complete provenance and hash validation.

## Route D — unavailable

Select only when no candidate or deterministic rebuild can reproduce the
required scientific payload.

Do not choose the newest runtime merely because it is available.

Create:

```text
docs/next_level/c86_runtime_recovery_decision.json
docs/next_level/c86_runtime_recovery_validation.json
```

---

# 7. C72/C74 color-authority recovery

The first required recovery target is the complete runtime package rooted
at:

```text
data/runtime/c72_qgcolor5/root.json
```

Recover the complete indexed payload, not only `root.json`.

Verify through the historical C72/C74 contracts:

```text
exact C72 scientific root;
24 ordered product-color rows;
3 ordered retained-triplet columns;
72 ordered pair identities;
72 terminal statuses;
72 exact records;
all exact expressions;
all exact-zero certificates;
72 per-entry bounds;
E_src, Gram, triplet adapter, U3, U3-dagger, P3;
source/API fingerprints;
safe non-pickle array formats;
complete inventory;
all-eight-generator intertwining;
rank-three projector closure;
zero anti-sextet and 15 leakage.
```

Run the historical C74 public loader against the recovered C72 package.

Create:

```text
docs/next_level/c86_c72_color_authority_recovery.json
docs/next_level/c86_c74_public_import_recovery_validation.json
```

Do not regenerate or renormalize \(U_3\) inside the public staging route.

---

# 8. Complete dependency capsule

Create a self-contained retained capsule containing every runtime dependency
required for historical C82 reconstruction that is not already present as
tracked immutable historical data.

Prefer a tracked, content-addressed capsule for compact project-generated
authority payloads. Use a repository location equivalent to:

```text
data/authority/c86_c82_historical_environment/
```

If repository policy requires a different retained-data root, document it.

The capsule must contain:

```text
all required runtime roots and indices;
all indexed payload files;
all safe numerical arrays and bounds;
all exact/status/record payloads;
the dependency graph;
producer commits and source hashes;
builder/validator commands;
schema and serializer identities;
per-file SHA-256 hashes;
package-level roots;
license/redistribution status;
and a capsule root/index.
```

The capsule must not contain:

```text
MSHT20_REP;
external restricted data;
machine-local absolute paths;
temporary-worktree names;
timestamps as scientific identity;
unrelated runtime outputs;
or hidden build fallbacks.
```

When a required payload is too large or repository policy forbids tracking
it, create a content-addressed retained local capsule plus a tracked
reconstruction manifest and prove that the exact bytes are reproducible
from tracked historical source. A positive C86 gate still requires the
historical reconstruction to work in a clean controlled environment.

Create:

```text
docs/next_level/c86_capsule_schema_contract.json
docs/next_level/c86_capsule_root_manifest.json
docs/next_level/c86_capsule_inventory.json
docs/next_level/c86_capsule_validation.json
```

---

# 9. Safe and immutable capsule format

All numerical loads must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payloads;
path traversal;
unexpected symlinks;
unindexed files;
shape, dtype, or endianness mismatch;
writable returned arrays.
```

All JSON/exact records must be canonical and immutable.

Create:

```text
docs/next_level/c86_safe_capsule_loading_contract.json
docs/next_level/c86_safe_capsule_loading_validation.json
```

---

# 10. Read-only historical staging

Implement under:

```text
src/deuteron_wigner/bridge/ifhistenv/
```

or the exact repository-equivalent package.

Provide builder/validation operations equivalent to:

```python
load_historical_c82_environment_capsule()

verify_historical_c82_environment_capsule()

stage_historical_c82_environment(
    detached_worktree: Path,
    *,
    read_only: bool = True,
)

verify_staged_historical_c82_environment(detached_worktree: Path)

remove_staged_historical_c82_environment(detached_worktree: Path)
```

The staging operation must:

```text
verify the capsule before copying or linking;
stage only expected runtime paths;
preserve exact relative paths expected by historical code;
make staged files read-only;
record every staged path and hash;
refuse to overwrite an existing mismatched file;
create no tracked modification in the historical worktree;
and cleanly remove only files it staged.
```

The historical C82 code must remain byte-identical.

Create:

```text
docs/next_level/c86_staging_contract.json
docs/next_level/c86_staging_validation.json
```

---

# 11. Strict no-current-runtime fallback

During historical reconstruction, poison or hide:

```text
the current main-worktree runtime directories;
current descendant builders;
current private package fields;
build-if-missing routes;
repair-if-missing routes;
network access;
and unindexed filesystem searches.
```

The historical worktree may consume only:

```text
its tracked historical source;
the verified staged C86 capsule;
the controlled Python/dependency environment.
```

Create:

```text
docs/next_level/c86_no_current_runtime_fallback_report.json
```

---

# 12. Historical C82 scientific-stream reconstruction

With the complete capsule staged into a clean detached C82 worktree:

1. instantiate the historical C72/C74 color authority;
2. instantiate every remaining required historical package;
3. execute the historical C82 tests, builder, and validator;
4. iterate the complete C82 scientific pair-coordinate record stream;
5. compute deterministic logical counts and a provisional stream digest;
6. repeat the reconstruction in a second clean worktree;
7. compare all logical records and digests.

At minimum report:

```text
supported-pair count;
logical pair-coordinate record counts by resolution;
coordinate-ID counts;
coefficient-status counts;
bound and ancestry availability;
stream digest;
historical runtime root/index produced in each run;
all loader and validator results.
```

This establishes reconstruction availability and determinism. The
scientific comparison with the current descendant route belongs to C87.

Create:

```text
docs/next_level/c86_historical_c82_stream_reconstruction.json
docs/next_level/c86_historical_c82_stream_determinism.json
```

A positive gate requires the two historical streams to be identical.

---

# 13. Historical recorded-hash handling

Compare the newly reconstructed historical runtime hashes with every C82
hash frozen in tracked historical reports.

Classify:

```text
HISTORICAL_RUNTIME_HASH_REPRODUCED;

HISTORICAL_RUNTIME_INSTANCE_HASH_DIFFERS_STREAM_STABLE;

HISTORICAL_RECONSTRUCTION_NONDETERMINISTIC;

HISTORICAL_RECORDED_HASH_UNRESOLVED.
```

Do not classify a difference as metadata-only in C86 unless its exact
fields are already proved instance-only and the reconstructed logical
stream is stable. Full historical-versus-descendant scientific equivalence
still belongs to C87.

Create:

```text
docs/next_level/c86_historical_hash_recovery_report.json
```

---

# 14. C87/IFEQUIV2 import contract

Define the exact contract by which C87 consumes:

```text
the C86 capsule root;
the complete historical dependency graph;
the staging API;
the exact C82 historical worktree commit;
the historical scientific-stream iterator;
the historical logical counts;
the historical record ordering;
the historical pair and stream digests;
the reconstructed historical runtime hashes;
and the recovery classifications.
```

C87 must compare the historical stream with the current descendant stream
exhaustively.

C87 must not:

```text
reopen dependency recovery;
read unverified current runtime files;
substitute a different C72 package;
or decide that a runtime difference is metadata-only without
field-level and scientific-record comparison.
```

Create:

```text
docs/next_level/c86_c87_ifequiv2_import_contract.json
```

---

# 15. Determinism and resource checks

Run:

```text
two complete capsule builds;
one clean capsule build;
one serial build;
one supported deterministic sharded build where applicable;
two independent historical worktree stagings;
two complete historical C82 reconstructions;
one interrupted/restart reconstruction.
```

Require:

```text
byte-identical capsule payloads and root;
identical staged path inventories;
identical historical logical record streams;
identical logical counts and digests;
no tracked historical-worktree changes.
```

Create:

```text
docs/next_level/c86_deterministic_reconstruction_report.json
docs/next_level/c86_resource_and_scaling_report.json
```

---

# 16. Isolation and mutation controls

Required failures include mutations of:

```text
capsule root or index;
dependency graph;
producer commit;
source hash;
runtime path;
payload hash;
C72 row/column/pair identity;
exact record;
per-entry bound;
U3 or P3 array;
staged path;
staged permissions;
historical worktree commit;
current-runtime fallback;
build-if-missing fallback;
logical record ID;
historical stream digest;
C87 import-contract field.
```

Poison:

```text
C80 kernel numerical values;
C53 values and propagators;
C58 values;
physical coupling;
counterterm interfaces.
```

Historical C82 scientific-stream reconstruction must remain independent of
those values except for the exact coordinate schema already frozen by C80.

Create at least **384 focused live mutations**.

Create:

```text
docs/next_level/c86_isolation_report.json
docs/next_level/c86_regression_report.json
```

---

# 17. Readiness and continuation decisions

Select exactly one branch.

## 17.1 Favorable branch

Issue the exact positive status frozen by the C86 contract, with preferred
semantic identity:

```text
C86_C82_HISTORICAL_RUNTIME_ENVIRONMENT_READY
```

Required:

```text
the complete historical C82 dependency graph is known;
every required dependency has a verified recovery route;
the complete C72/C74 color authority is recovered;
the retained capsule is complete and authenticated;
staging is safe, read-only, and leaves tracked files unchanged;
no current-runtime or builder fallback is used;
the historical C82 route instantiates completely;
two clean historical scientific streams are identical;
historical hash status is typed;
the C87 import contract is complete;
determinism, isolation, and mutations pass.
```

Next:

> **C87/IFEQUIV2 — exhaustive C82 historical-versus-descendant scientific-payload equivalence**

## 17.2 Required historical dependency unavailable

```text
C86_IFHISTENV_DEPENDENCY_UNAVAILABLE
```

Next:

> **C87/IFRECOVER — recover only the specifically identified missing project-generated runtime authority**

## 17.3 Recovered payload conflicts with tracked authority

```text
C86_IFHISTENV_PAYLOAD_IDENTITY_MISMATCH
```

Next:

> **C87/IFPAYLOADDIFF — resolve only the specifically identified root, record, array, bound, or source-manifest mismatch**

## 17.4 Dependency closure incomplete

```text
C86_IFHISTENV_DEPENDENCY_CLOSURE_INCOMPLETE
```

Next:

> **C87/IFDEPCLOSE — close only the specifically identified remaining ignored-runtime dependency nodes**

## 17.5 Historical stream nondeterministic

```text
C86_IFHISTENV_HISTORICAL_STREAM_NONDETERMINISTIC
```

Next:

> **C87/IFHISTDET — localize only the historical builder, serializer, ordering, environment, or runtime nondeterminism**

## 17.6 Capsule/staging incomplete

```text
C86_IFHISTENV_CAPSULE_IMPORT_INCOMPLETE
```

Next:

> **C87/IFCAPSULE — repair only the capsule root/index, safe loader, staging, permissions, or no-fallback defect**

Do not issue scientific-equivalence, persisted-pair-import,
coefficient-times-kernel, contact-matrix, complete-instantaneous-fermion,
counterterm-solved, local-QCD, TMD, matching, inference, or production
status.

---

# 18. Essential deliverables

Create at least:

```text
docs/next_level/c86_implementation_report.md
docs/next_level/c86_derivation_authority_manifest.json
docs/next_level/c86_input_fidelity_audit.json
docs/next_level/c86_descendant_qualification.json
docs/next_level/c86_runtime_candidate_inventory.json

docs/next_level/c86_historical_dependency_graph.json
docs/next_level/c86_historical_dependency_closure_report.json
docs/next_level/c86_runtime_recovery_decision.json
docs/next_level/c86_runtime_recovery_validation.json

docs/next_level/c86_c72_color_authority_recovery.json
docs/next_level/c86_c74_public_import_recovery_validation.json

docs/next_level/c86_capsule_schema_contract.json
docs/next_level/c86_capsule_root_manifest.json
docs/next_level/c86_capsule_inventory.json
docs/next_level/c86_capsule_validation.json
docs/next_level/c86_safe_capsule_loading_contract.json
docs/next_level/c86_safe_capsule_loading_validation.json

docs/next_level/c86_staging_contract.json
docs/next_level/c86_staging_validation.json
docs/next_level/c86_no_current_runtime_fallback_report.json

docs/next_level/c86_historical_c82_stream_reconstruction.json
docs/next_level/c86_historical_c82_stream_determinism.json
docs/next_level/c86_historical_hash_recovery_report.json
docs/next_level/c86_c87_ifequiv2_import_contract.json

docs/next_level/c86_deterministic_reconstruction_report.json
docs/next_level/c86_resource_and_scaling_report.json
docs/next_level/c86_isolation_report.json
docs/next_level/c86_readiness_report.json
docs/next_level/c86_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifhistenv/
```

Use a retained authority capsule root equivalent to:

```text
data/authority/c86_c82_historical_environment/
```

and temporary runtime space equivalent to:

```text
data/runtime/c86_ifhistenv/
```

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 19. Acceptance criteria

C86 is complete only when:

1. Baseline `c66d17383e9a344ced10b61d393ce96e540c570e` reproduces.
2. Both protected untracked paths remain untouched.
3. C72/C74/C77/C78/C80/C82/C83/C84/C85 historical artifacts remain unchanged.
4. `c86_ifhistenv_contract.md` is consumed and preserved.
5. The C85 no-go is qualified as an incomplete historical environment, not a rejected scientific result.
6. A clean detached C82 worktree is used.
7. The complete ignored-runtime dependency graph is derived.
8. The audit continues beyond the first missing C72 path.
9. Every dependency has a typed producer, role, expected identity, and recovery route.
10. No unknown dependency remains in the favorable branch.
11. The complete C72 runtime package is recovered, not just `root.json`.
12. The C72 scientific root and all 24/3/72 authority domains verify.
13. The historical C74 public loader passes against the recovered C72 package.
14. Every additional required runtime dependency is recovered and verified.
15. The retained capsule is self-contained for historical C82 reconstruction.
16. Restricted external data are not copied into the capsule.
17. Every capsule payload is content-addressed and indexed.
18. Every NumPy load explicitly uses `allow_pickle=False`.
19. Object dtype, unsafe paths, unindexed files, writable arrays, and mutable records are rejected.
20. Staging writes only expected runtime paths.
21. Staging refuses mismatched pre-existing files.
22. Staged files are read-only.
23. The detached historical source remains byte-identical and clean.
24. No current-runtime or current-builder fallback is used.
25. The complete historical C82 scientific route instantiates.
26. Two clean historical scientific record streams are identical.
27. Historical logical counts and stream digests are derived.
28. The historical recorded-runtime hash status is typed.
29. Scientific equivalence with the descendant route is not overclaimed.
30. The C87 import contract is complete.
31. Repeated capsule, staging, serial, sharded, and restart tests pass.
32. At least 384 focused live mutations pass.
33. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
34. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
35. The working tree is clean except for the two protected untracked paths.
36. A local completion commit is created and not pushed.

Do not make a current runtime authoritative merely because it exists. Do
not copy only a root file without its complete indexed payload. Do not
classify runtime differences as metadata-only before C87's exhaustive
scientific comparison.

---

# 20. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C86 contract identity;
- C82 historical completion commit;
- complete dependency-node count;
- dependency graph and closure hashes;
- each dependency's producer commit, recovery route, root, and payload count;
- exact C72 root and recovered 24/3/72 authority counts;
- historical C74 public-loader validation;
- capsule path, root, index, file count, and total size;
- whether the capsule is tracked, retained local, or mixed, with justification;
- staging path count, permission result, and tracked-worktree cleanliness;
- no-current-runtime-fallback result;
- historical C82 supported-pair and logical-record counts;
- both historical stream digests and equality result;
- historical runtime/index hashes and their typed status;
- C87 import-contract result;
- deterministic and restart results;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that no scientific-equivalence claim, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
