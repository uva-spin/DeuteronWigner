# C87/IFCAPSULE Codex Work Package

## Title

**Canonical source-chain-equivalent C72 color-authority capsule: independent reconstruction of the complete 24×3/72-record scientific payload, exhaustive candidate comparison, historical C74 compatibility staging, deterministic C82 stream recovery, and C88 equivalence handoff**

## Authoritative baseline

Start from the clean local C86 completion commit:

```text
2994941a4344ba90aa1ed78d6d98fc15e03c6ad8
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "2994941a4344ba90aa1ed78d6d98fc15e03c6ad8"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c87_ifcapsule_contract.md
```

If that contract freezes exact status names, schemas, APIs, or a narrower scientific boundary, preserve them. This work package supplies the canonical scientific-payload reconstruction, compatibility-capsule, staging, and validation plan; it does not authorize weakening the committed contract.

The historical C82 completion commit is:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The relevant color-authority completion commits are:

```text
C66/QGCOLOR2:
    8f8240ff2c5cb2615ee68ba10331b9732dd84ca6

C68/QGCOLOR-RUNTIME:
    031ac09fe3aeac19f281255ae2a0f014092485e8

C70/QGCOLOR4:
    ac61aecfdc8896f66d3b42204b1e53481eb79cc1

C72/QGCOLOR5:
    369e95e1004c2f896f8a41e5c3270f7644e3391d

C74/QGCOLOR6:
    56b7e6c2bf68abc46493990afbdf1d8a2ce86415
```

Verify every actual commit, path, source lock, schema, record identity, and package dependency from the repository. This prompt is not numerical authority.

The baseline is authoritative only when it reproduces:

```text
C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY

C74_SOURCE_DERIVED_TRIPLET_PUBLIC_IMPORT_READY

C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY

C85_C82_SCIENTIFIC_PAYLOAD_EQUIVALENCE_INCOMPLETE

C86_C82_HISTORICAL_RUNTIME_ENVIRONMENT_INCOMPLETE
```

and the exact C86 finding:

```text
historical dependency graph:
    C82 -> C77, C78, C80, C74 -> C72;

a local C72 runtime candidate exists;

historical C82 froze only C74 color-record counts, not the C72
root, index, or payload hashes;

therefore the local candidate cannot be declared the exact
historical C72 runtime instance;

no authenticated historical capsule or historical C82 scientific
stream was created.
```

Create a local completion commit. Do not push.

---

# 1. Scientific interpretation of the blocker

C87 must explicitly separate:

```text
A. historical runtime-instance identity:
       the exact ignored C72 directory that happened to exist when
       C82 originally ran;

B. source-chain scientific identity:
       the complete color basis, exact records, exact expressions,
       certified values and bounds, and operator/projector
       identities determined by the C53/C66/C68/C70/C72 source
       chain;

C. compatibility-package identity:
       a concrete root/index/file layout accepted by the historical
       C74 public loader.
```

Object A is not recoverable merely from C82 because C82 did not freeze its
hashes.

That does **not** imply that object B is unknowable.

C87 must not forge, copy, or backdate a C72 runtime root and call it the
historical instance. Instead, it must determine whether the exact producer
source chain defines a unique scientific payload. When it does, C87 may
construct a new, explicitly descendant-owned compatibility capsule
representing that payload.

The permitted claim is:

```text
CANONICAL_SOURCE_CHAIN_EQUIVALENT_C72_AUTHORITY
```

The forbidden claim is:

```text
EXACT_HISTORICAL_C72_RUNTIME_INSTANCE_RECOVERED
```

unless an independently hash-verified historical copy is actually found.

No external publication is required for this project-generated authority.
External papers remain part of the already frozen source chain; the
runtime record and capsule format are project-owned consequences.

---

# 2. Descendant qualification

Do not alter historical C72, C74, C82, C85, or C86 artifacts.

Create the descendant qualification:

```text
C86_C82_HISTORICAL_RUNTIME_ENVIRONMENT_INCOMPLETE
    -> exact historical C72 runtime instance not identifiable from
       C82's frozen record;

C72 scientific payload:
    not rejected;

authorized C87 task:
    reconstruct and prove a unique canonical source-chain
    scientific payload, then package it under a new C87 identity
    for historical compatibility testing.
```

Create:

```text
docs/next_level/c87_descendant_qualification.json
```

---

# 3. Exact purpose and stopping point

C87 must produce:

```text
a complete scientific-field schema for the C72 color authority;

a complete instance/compatibility-field schema;

two independent source-chain reconstructions of the C72
scientific payload;

a canonical scientific root over the complete color-authority
record stream;

an exhaustive comparison of every available local C72 candidate
against that canonical payload;

a typed uniqueness decision;

a new C87-owned, content-addressed compatibility capsule carrying
the canonical payload in the historical C72/C74 public schema;

a safe immutable loader and read-only staging route;

a passing historical C74 public import using the C87 capsule;

a deterministic historical C82 scientific-stream reconstruction
under the explicitly qualified canonical-source-chain-equivalent
environment;

and a C88/IFEQUIV3 import contract.
```

C87 must not:

```text
claim recovery of the exact historical C72 runtime instance;

declare historical-versus-descendant C82 scientific equivalence;

persist the final C82 pair-coordinate public snapshot;

form coefficient-times-kernel products;

construct a contact matrix or action;

choose a physical coupling or counterterm;

add C53 or C58 contributions;

or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C87_CANONICAL_SOURCE_CHAIN_C72_AUTHORITY_CAPSULE_READY
```

If `c87_ifcapsule_contract.md` freezes another exact spelling, preserve it
and record a semantic alias.

The favorable continuation is:

> **C88/IFEQUIV3 — exhaustive C82 historical-canonical-environment versus descendant scientific-record equivalence**

---

# 4. Mandatory source and runtime audit

Read completely the actual repository equivalents of:

```text
C53:
    exact SU(3) emission map and triplet image;

C66:
    U3 = E_src/sqrt(C_F);
    exact Gram normalization;
    all-eight-generator intertwining;
    rank-three projector validation;
    zero leakage;

C68:
    first runtime materialization;
    72 statuses;
    arrays, bounds, hashes, inventory, loader;

C70:
    partial package authentication;

C72:
    24 rows, 3 columns, 72 exact records;
    exact expressions and zero certificates;
    per-entry bounds;
    source/API fingerprints;
    runtime root/index and package object;

C74:
    public immutable loaders;
    safe-loading contract;
    complete public-package equivalence and invariants;

C82:
    exact color fields actually consumed by the aggregation bridge;

C85/C86:
    historical-environment failures and exact missing authority.
```

Inventory every local candidate without modifying it:

```bash
find . -path '*/data/runtime/c72_qgcolor5/*' -type f | sort
find . -path '*/data/runtime/c70_qgcolor4/*' -type f | sort
find . -path '*/data/runtime/c68_qgcolor_runtime/*' -type f | sort
git worktree list --porcelain
```

Also inspect candidate locations explicitly named in tracked historical
reports. Do not crawl unrelated user directories.

Create:

```text
docs/next_level/c87_derivation_authority_manifest.json
docs/next_level/c87_input_fidelity_audit.json
docs/next_level/c87_local_candidate_inventory.json
```

---

# 5. Freeze the scientific-field schema

Define the complete canonical C72 scientific record.

It must bind every scientifically meaningful field, including the actual
repository equivalents of:

```text
24 ordered product-color row identities and basis tuples;

3 ordered retained-triplet column identities and basis tuples;

72 ordered row-column pair identities;

72 terminal exact statuses;

72 exact-record identities;

all exact nonzero expressions;

all exact-zero certificates;

72 per-entry midpoint and bound identities;

E_src;

Gram matrix;

triplet adapter;

U3;

U3-dagger;

P3;

basis permutations;

C_F and normalization identity;

all-eight-generator intertwining records;

projector rank/trace records;

anti-sextet and 15 leakage records;

C53/C66 scientific ancestry;

exact source-lock identities required to derive the object.
```

The scientific schema must exclude, unless proven to alter the above:

```text
absolute runtime path;

temporary worktree path;

wall-clock timestamp;

host or process ID;

file shard order;

current descendant HEAD;

narrative report path;

runtime-instance root hash itself;

API convenience names that do not alter returned records.
```

Classify separately:

```text
SCIENTIFIC_FIELD;

SCIENTIFIC_PROVENANCE_FIELD;

COMPATIBILITY_SCHEMA_FIELD;

INSTANCE_ONLY_FIELD.
```

Create:

```text
docs/next_level/c87_c72_scientific_schema.json
docs/next_level/c87_c72_field_classification.json
docs/next_level/c87_c72_scientific_schema_validation.json
```

---

# 6. Reconstruction Route A — exact producer-commit build

Create a clean detached worktree at the exact C72 completion commit:

```text
369e95e1004c2f896f8a41e5c3270f7644e3391d
```

Reconstruct the complete C72 scientific payload from the exact producer
source.

When lower ignored dependencies are required:

```text
reconstruct them recursively at their exact producer commits;

or stage a separately verified lower-level canonical source-chain
payload;

never substitute a current runtime merely because it exists.
```

Use a controlled environment:

```text
TZ=UTC;
LC_ALL=C;
PYTHONHASHSEED=0;
fixed SOURCE_DATE_EPOCH where supported;
clean runtime roots;
network disabled after required public source locks are present.
```

Run the historical C72 tests, builder, and validator at least twice.

Stream the complete canonical scientific record domain and compute:

```text
per-record digests;
row-domain digest;
column-domain digest;
pair-domain digest;
exact-record digest;
bound-domain digest;
array-domain digest;
aggregate Route-A scientific root.
```

Create:

```text
docs/next_level/c87_route_a_producer_reconstruction.json
```

A runtime-instance hash mismatch does not fail Route A when the canonical
scientific record stream is deterministic.

---

# 7. Reconstruction Route B — independent source-chain derivation

Implement a second route that does not consume Route A's generated C72
payload or root.

Route B must reconstruct the scientific object from the earlier frozen
operator chain:

```text
C53 exact emission map and color basis;

C66 relation U3 = E_src/sqrt(C_F);

C66 exact Gram/intertwiner/projector identities;

C68 exact status/expression/bound semantics;

C72 basis and exact-record schema.
```

It may use:

```text
exact symbolic SU(3) matrices;

canonical basis tuples;

exact normalization identities;

independent array evaluation;

independent serialization of the scientific record.
```

It must not call the Route-A C72 builder or load Route-A arrays.

Compute the same per-domain and aggregate scientific roots.

Create:

```text
docs/next_level/c87_route_b_independent_derivation.json
```

A favorable result requires exact equality of the complete Route-A and
Route-B scientific records under the canonical schema.

---

# 8. Optional Route C — available local candidate

For each local C72 runtime candidate:

1. verify its own root/index and complete payload;
2. load it through the compatible C74 public loader where possible;
3. stream its complete canonical scientific records;
4. compare every field against the Route-A/Route-B canonical result.

Classify each candidate:

```text
SCIENTIFICALLY_IDENTICAL_COMPATIBLE_INSTANCE;

SCIENTIFICALLY_IDENTICAL_DIFFERENT_INSTANCE_METADATA;

SCIENTIFIC_PAYLOAD_MISMATCH;

INCOMPLETE_OR_UNAUTHENTICATED_CANDIDATE.
```

A local candidate is never selected merely because it exists.

A candidate mismatch does not block a positive C87 result when Routes A and
B agree and C87 builds a new canonical capsule from those routes. The
mismatched candidate must be excluded explicitly.

Create:

```text
docs/next_level/c87_local_candidate_comparison.json
```

---

# 9. Canonical scientific root and uniqueness decision

Compute one aggregate canonical scientific root binding:

```text
the C87 scientific schema;

the exact producer source/provenance identities;

all 24 row records;

all 3 column records;

all 72 pair records;

all exact statuses, expressions, zero certificates, and bounds;

all numerical/scientific arrays;

all invariant and leakage records;

the complete canonical ordering.
```

Require:

```text
Route-A root = Route-B root;

all record-level mismatch counts = 0;

all invariant residuals close under the declared exact/certified
semantics.
```

Then issue one uniqueness decision:

```text
UNIQUE_CANONICAL_SOURCE_CHAIN_PAYLOAD;

MULTIPLE_SOURCE_CHAIN_PAYLOADS_SCIENTIFICALLY_IDENTICAL;

SOURCE_CHAIN_RECONSTRUCTION_MISMATCH;

SCIENTIFIC_PAYLOAD_UNDERDETERMINED.
```

The first two may support a positive C87 gate.

Create:

```text
docs/next_level/c87_canonical_scientific_root.json
docs/next_level/c87_scientific_uniqueness_decision.json
docs/next_level/c87_exhaustive_route_equivalence.json
```

---

# 10. New C87-owned compatibility capsule

Build a new capsule under a repository-equivalent location such as:

```text
data/authority/c87_canonical_c72_color_authority/
```

The capsule is a **new descendant artifact**. It must never copy or claim
the historical C72 runtime root as its own identity.

The capsule must contain:

```text
the complete canonical scientific payload;

a historical-C72/C74-compatible root/index and path layout;

all indexed exact/status/bound payloads;

safe numerical arrays;

the C87 canonical scientific root;

producer source/provenance identities;

compatibility schema and adapter identity;

per-file hashes;

package inventory;

explicit claim boundary:
    canonical source-chain equivalent,
    not exact historical runtime instance.
```

The capsule root must bind both:

```text
the scientific root;

the compatibility-package schema and file inventory.
```

Create:

```text
docs/next_level/c87_capsule_schema_contract.json
docs/next_level/c87_capsule_root_manifest.json
docs/next_level/c87_capsule_inventory.json
docs/next_level/c87_capsule_claim_boundary.json
```

---

# 11. Safe immutable capsule loader

Implement under:

```text
src/deuteron_wigner/bridge/ifcapsule/
```

or the exact repository-equivalent package.

Provide operations equivalent to:

```python
load_canonical_c72_authority_capsule()

verify_canonical_c72_authority_capsule()

stage_canonical_c72_for_historical_c74(
    detached_worktree: Path,
    *,
    read_only: bool = True,
)

verify_staged_canonical_c72(detached_worktree: Path)

remove_staged_canonical_c72(detached_worktree: Path)
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;

pickle-dependent payload;

path traversal;

unexpected symlink;

unindexed file;

shape/dtype/endianness mismatch;

writable returned array;

mutable authority record.
```

The loader must verify both capsule and scientific roots.

Create:

```text
docs/next_level/c87_capsule_api_contract.json
docs/next_level/c87_capsule_api_validation.json
docs/next_level/c87_safe_loading_validation.json
```

---

# 12. Historical C74 compatibility validation

Create a clean detached historical C82 worktree.

Stage the C87 capsule at exactly the runtime path expected by its historical
C74 loader.

Require:

```text
historical C74 imports successfully;

all 24/3/72 counts close;

all exact records and bounds load;

U3/Gram/projector/intertwining/leakage invariants close;

the public loader performs no regeneration;

the staged source remains read-only;

no tracked historical file changes.
```

Record explicitly:

```text
environment type:
    CANONICAL_SOURCE_CHAIN_EQUIVALENT;

exact historical runtime-instance identity:
    UNKNOWN_NOT_CLAIMED.
```

Create:

```text
docs/next_level/c87_historical_c74_compatibility_report.json
docs/next_level/c87_staging_validation.json
```

---

# 13. Historical C82 scientific-stream reconstruction

With the C87 capsule staged, recover every other already verified
historical C82 dependency through the C86 dependency graph.

Run:

```text
historical C82 tests;

historical C82 builder;

historical C82 validator;

complete scientific pair-coordinate stream.
```

Execute in two separate clean worktrees.

Record:

```text
supported-pair counts;

logical pair-coordinate record counts;

coordinate identities;

coefficient statuses and bounds;

ancestry availability;

per-resolution and aggregate historical-canonical stream digests;

runtime root/index hashes.
```

Require the two historical-canonical streams to be identical.

This proves that a deterministic historical-source scientific stream exists
under the canonical source-chain-equivalent environment.

It does **not** prove that the unknowable original C72 runtime instance was
the same, and it does not yet prove equality with the current descendant
C82 stream.

Create:

```text
docs/next_level/c87_historical_c82_canonical_environment_stream.json
docs/next_level/c87_historical_c82_canonical_stream_determinism.json
```

---

# 14. Scope-sensitivity test

Because the original C72 runtime hash is unknown, test whether the C82
scientific stream depends only on the canonical C72 scientific fields.

Construct controlled compatibility-package variants that change only
instance fields, such as:

```text
file ordering;

shard naming;

non-scientific package metadata;

temporary staging path;

runtime-instance root identity.
```

Keep the canonical scientific root fixed.

Require the historical C82 scientific stream to remain identical.

Mutations of any scientific field must fail or change the stream.

Create:

```text
docs/next_level/c87_c82_color_authority_scope_sensitivity.json
```

This does not prove what historical bytes existed. It proves that C82's
scientific result is a function of the canonical scientific authority
rather than incidental package-instance metadata.

---

# 15. C88/IFEQUIV3 import contract

Define the exact contract by which C88 consumes:

```text
the C87 canonical C72 scientific root;

the C87 compatibility capsule root;

the historical C74 compatibility result;

the deterministic historical-canonical C82 record stream;

the exact record schema and ordering;

the historical-canonical logical counts and digests;

the claim boundary that the exact historical runtime instance
remains unknown.
```

C88 must compare the complete historical-canonical stream with the current
descendant C82 stream exhaustively.

C88 must not:

```text
claim exact historical runtime recovery;

reopen C72 payload reconstruction;

accept equal top-level counts as scientific equivalence;

or classify a difference as metadata-only without record-level
comparison.
```

Create:

```text
docs/next_level/c87_c88_ifequiv3_import_contract.json
```

---

# 16. Deterministic reconstruction

Run:

```text
two Route-A reconstructions;

two Route-B reconstructions;

two capsule builds;

one clean capsule build;

serial and deterministic sharded capsule construction;

two historical C74 stagings;

two historical-canonical C82 stream reconstructions;

one interrupted/restart reconstruction.
```

Require:

```text
identical canonical scientific roots;

identical capsule roots and files;

identical staged inventories;

identical historical-canonical logical records and digests;

clean historical tracked worktrees.
```

Create:

```text
docs/next_level/c87_deterministic_reconstruction_report.json
docs/next_level/c87_resource_and_scaling_report.json
```

---

# 17. Isolation and mutation controls

Required failures include mutations of:

```text
producer commit;

source-lock identity;

row or column basis tuple;

pair identity;

terminal exact status;

exact expression;

zero certificate;

per-entry midpoint or bound;

E_src;

Gram;

U3;

P3;

intertwiner identity;

leakage record;

canonical ordering;

scientific schema;

scientific root;

capsule root/index;

compatibility adapter;

staged path or permission;

historical C74 loader result;

historical C82 stream digest;

C88 import-contract field.
```

The canonical scientific root must remain invariant under mutations of:

```text
temporary path;

host/process ID;

file shard naming;

instance-only metadata;

current descendant HEAD;

narrative documentation path.
```

Create at least **384 focused live mutations**.

Create:

```text
docs/next_level/c87_isolation_report.json
docs/next_level/c87_regression_report.json
```

---

# 18. Readiness and continuation decisions

Select exactly one branch.

## 18.1 Favorable branch

Issue the exact positive status frozen by the C87 contract, with preferred
semantic identity:

```text
C87_CANONICAL_SOURCE_CHAIN_C72_AUTHORITY_CAPSULE_READY
```

Required:

```text
the complete C72 scientific schema is frozen;

Route A and Route B reconstruct the complete payload independently;

all scientific record mismatch counts are zero;

a unique canonical scientific root is established;

every local candidate is typed and only identical candidates are
accepted;

the C87 capsule is complete and authenticated;

the claim boundary explicitly denies exact historical-runtime
recovery;

historical C74 imports and validates the capsule;

two historical-canonical C82 streams are identical;

instance-only package variants do not change the C82 scientific
stream;

scientific-field mutations fail;

the C88 import contract is complete;

determinism, isolation, and mutations pass.
```

Next:

> **C88/IFEQUIV3 — exhaustive historical-canonical versus descendant C82 scientific-payload equivalence**

## 18.2 Producer reconstruction incomplete

```text
C87_IFCAPSULE_PRODUCER_RECONSTRUCTION_INCOMPLETE
```

Next:

> **C88/IFPRODUCER — repair only the specifically identified C66/C68/C70/C72 source-chain reconstruction dependency**

## 18.3 Independent source-chain routes disagree

```text
C87_IFCAPSULE_SCIENTIFIC_PAYLOAD_MISMATCH
```

Next:

> **C88/IFCOLORDIFF — resolve only the specifically identified basis, exact-record, array, bound, invariant, or provenance mismatch**

## 18.4 Scientific payload underdetermined

```text
C87_IFCAPSULE_SCIENTIFIC_PAYLOAD_UNDERDETERMINED
```

Use only when multiple scientifically different payloads satisfy all frozen
producer source constraints.

Next:

> **C88/IFCOLORAUTH — identify the missing scientific convention or authority that distinguishes the candidate payloads**

## 18.5 Compatibility capsule incomplete

```text
C87_IFCAPSULE_COMPATIBILITY_IMPORT_INCOMPLETE
```

Next:

> **C88/IFCOMPAT — repair only the root/index, schema adapter, safe loading, staging, or historical C74 compatibility defect**

## 18.6 Historical-canonical C82 stream incomplete

```text
C87_IFCAPSULE_HISTORICAL_STREAM_INCOMPLETE
```

Next:

> **C88/IFHISTSTREAM — repair only the remaining historical dependency, loader, builder, ordering, or determinism defect**

Do not issue exact-historical-runtime, final scientific-equivalence,
persisted-C82-import, coefficient-times-kernel, contact-matrix,
complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD,
matching, inference, or production status.

---

# 19. Essential deliverables

Create at least:

```text
docs/next_level/c87_implementation_report.md
docs/next_level/c87_derivation_authority_manifest.json
docs/next_level/c87_input_fidelity_audit.json
docs/next_level/c87_descendant_qualification.json
docs/next_level/c87_local_candidate_inventory.json

docs/next_level/c87_c72_scientific_schema.json
docs/next_level/c87_c72_field_classification.json
docs/next_level/c87_c72_scientific_schema_validation.json

docs/next_level/c87_route_a_producer_reconstruction.json
docs/next_level/c87_route_b_independent_derivation.json
docs/next_level/c87_local_candidate_comparison.json
docs/next_level/c87_canonical_scientific_root.json
docs/next_level/c87_scientific_uniqueness_decision.json
docs/next_level/c87_exhaustive_route_equivalence.json

docs/next_level/c87_capsule_schema_contract.json
docs/next_level/c87_capsule_root_manifest.json
docs/next_level/c87_capsule_inventory.json
docs/next_level/c87_capsule_claim_boundary.json
docs/next_level/c87_capsule_api_contract.json
docs/next_level/c87_capsule_api_validation.json
docs/next_level/c87_safe_loading_validation.json

docs/next_level/c87_historical_c74_compatibility_report.json
docs/next_level/c87_staging_validation.json
docs/next_level/c87_historical_c82_canonical_environment_stream.json
docs/next_level/c87_historical_c82_canonical_stream_determinism.json
docs/next_level/c87_c82_color_authority_scope_sensitivity.json
docs/next_level/c87_c88_ifequiv3_import_contract.json

docs/next_level/c87_deterministic_reconstruction_report.json
docs/next_level/c87_resource_and_scaling_report.json
docs/next_level/c87_isolation_report.json
docs/next_level/c87_readiness_report.json
docs/next_level/c87_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifcapsule/
```

Use a retained authority root equivalent to:

```text
data/authority/c87_canonical_c72_color_authority/
```

and temporary runtime space equivalent to:

```text
data/runtime/c87_ifcapsule/
```

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 20. Acceptance criteria

C87 is complete only when:

1. Baseline `2994941a4344ba90aa1ed78d6d98fc15e03c6ad8` reproduces.
2. Both protected untracked paths remain untouched.
3. C53/C66/C68/C70/C72/C74/C82/C85/C86 historical artifacts remain unchanged.
4. `c87_ifcapsule_contract.md` is consumed and preserved.
5. The exact historical C72 runtime instance remains explicitly unknown unless a hash-verified copy is found.
6. No historical root is copied or forged.
7. The complete scientific-field schema is frozen.
8. All 24 rows, 3 columns, and 72 pair records are included.
9. All exact expressions, zero certificates, and per-entry bounds are included.
10. E_src, Gram, adapter, U3, U3-dagger, P3, and permutations are included.
11. All intertwining, projector, and leakage records are included.
12. Route A reconstructs from exact producer commits.
13. Route B reconstructs independently from the source chain.
14. Route A and Route B scientific records agree exhaustively.
15. All scientific mismatch counts are zero in the favorable branch.
16. A canonical scientific root is established.
17. Every local candidate is exhaustively classified.
18. A mismatched local candidate is excluded, not silently repaired.
19. The C87 capsule has its own new descendant identity.
20. The capsule claim boundary states source-chain equivalence, not exact historical recovery.
21. All capsule payloads are indexed and content addressed.
22. Every NumPy load explicitly uses `allow_pickle=False`.
23. Object dtype, unsafe paths, unindexed files, writable arrays, and mutable records are rejected.
24. Historical C74 imports and validates the staged capsule.
25. Staging is read-only and leaves tracked historical files clean.
26. Two historical-canonical C82 scientific streams are identical.
27. Instance-only compatibility-package variations leave the C82 scientific stream unchanged.
28. Scientific-field mutations fail.
29. Scientific equivalence with the current descendant C82 stream is not yet claimed.
30. The C88 import contract is complete.
31. Repeated, clean, serial, sharded, and restart reconstruction passes.
32. At least 384 focused live mutations pass.
33. No final C82 persisted snapshot, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
34. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
35. The working tree is clean except for the two protected untracked paths.
36. A local completion commit is created and not pushed.

Do not require the unknowable historical runtime-instance hash to prove a
source-chain scientific payload. Do not weaken scientific-record coverage
to force agreement. Do not call a current local candidate authoritative
without independent source-chain reconstruction.

---

# 21. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C87 contract identity;
- historical C82 and producer completion commits;
- exact-historical-runtime claim status;
- scientific schema and field-classification hashes;
- Route-A and Route-B roots and record counts;
- exhaustive mismatch counts;
- canonical scientific root;
- uniqueness decision;
- every local candidate's root and classification;
- capsule path, root, index, file count, and total size;
- capsule claim boundary;
- historical C74 public-loader and invariant results;
- staging path count, permissions, and historical-worktree cleanliness;
- historical-canonical C82 supported-pair and logical-record counts;
- both historical-canonical stream digests;
- instance-only scope-sensitivity result;
- C88 import-contract result;
- deterministic and restart results;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime-instance recovery is not claimed unless independently proven;
- confirmation that no descendant scientific-equivalence claim, final C82 persisted snapshot, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
