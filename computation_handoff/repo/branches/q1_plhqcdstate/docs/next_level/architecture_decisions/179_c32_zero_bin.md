# ADR 179: Place the overlap subtraction exactly once

The zero-bin is defined before inverse-square-root soft allocation. Missing and
duplicate routes remain explicit until the microscopic soft region exists.
