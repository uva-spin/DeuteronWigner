# ADR 119: `MSHT20_REP` is a custom source identity

Accepted. ART25 requires indices through 999. The public 65-member `MSHT20nnlo_as118` Hessian set is not the requested ensemble and cannot satisfy member identity.
