# C88/IFSTREAM Codex Work Package

## Title

**Bounded, restartable, complete C82 scientific-stream exporter: canonical pair-coordinate records, deterministic sharding and checkpoints, exact count-once closure, historical C87-capsule execution, and C89 exhaustive-equivalence handoff**

## Authoritative baseline

Start from the clean local C87 completion commit:

```text
cb6332f525ab4a26e8ac16e5173479aeac69753c
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "cb6332f525ab4a26e8ac16e5173479aeac69753c"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c88_ifstream_contract.md
```

If that contract freezes exact status names, schemas, APIs, or a narrower scientific boundary, preserve them. This work package supplies the executable stream-export, boundedness, restart, completeness, and validation plan; it does not authorize weakening the committed contract.

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The C87 capsule is explicitly:

```text
CANONICAL_SOURCE_CHAIN_EQUIVALENT
```

and is explicitly **not**:

```text
EXACT_HISTORICAL_C72_RUNTIME_INSTANCE_RECOVERED
```

The baseline is authoritative only when it contains and reproduces:

```text
C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY

C74_SOURCE_DERIVED_TRIPLET_PUBLIC_IMPORT_READY

C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY

C87 canonical C72 scientific payload:
    Route-A producer reconstruction;
    independent C53/C66 source-chain reconstruction;
    equal scientific roots;
    new immutable C87-owned compatibility capsule;
    passing historical C74 public import;

C87 historical C82 result:
    historical C82 materialization succeeds with the staged
    canonical capsule;

C87 fail-closed boundary:
    no bounded, restartable, complete scientific-stream exporter;
    therefore no two exhaustive historical streams and no complete
    stream mutation suite can be claimed.
```

Verify all actual package roots, counts, record schemas, pair orders,
coefficient statuses, bounds, capsule identities, loader behavior, and
historical commands from the repository. This prompt is not numerical
authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification

C88 is an execution and scientific-record export package.

It does not introduce:

```text
a new operator;
a new regulator;
a new color authority;
a new projected coefficient;
a new physical parameter;
or a new external source.
```

The C82 scientific bridge already exists. The C87 capsule already supplies
the qualified color authority needed to instantiate historical C82.

The missing object is a complete exporter that can traverse the C82 logical
pair-coordinate domain:

```text
with bounded memory;
in a canonical order;
with exact count-once semantics;
with deterministic shards and digests;
with safe interruption and restart;
without modifying historical source;
and without materializing the complete domain in memory.
```

No external paper is required for a repository-owned iterator, checkpoint,
shard schema, sequence number, pair digest, or stream root.

A fail-closed result is required only when:

```text
the historical C82 route cannot expose every logical scientific
record;

a canonical record or ordering cannot be derived from the frozen
C82 semantics;

bounded execution cannot be achieved;

restart changes or duplicates the stream;

two clean exports differ;

or the exporter cannot prove complete coverage.
```

---

# 2. Claim boundary and descendant qualification

Do not alter historical C72, C74, C82, or C87 artifacts.

Create the descendant qualification:

```text
C87 canonical color-authority science:
    complete and unchanged;

C87 historical C82 materialization:
    succeeds in the canonical source-chain-equivalent environment;

remaining blocker:
    C82_COMPLETE_SCIENTIFIC_STREAM_EXPORT_UNAVAILABLE;

authorized C88 task:
    build the complete bounded/restartable exporter and prove its
    deterministic record-level closure.
```

Create:

```text
docs/next_level/c88_descendant_qualification.json
```

Every exported historical record must retain the environment label:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

It must not be labeled as output from the unknowable exact historical C72
runtime instance.

---

# 3. Exact purpose and stopping point

C88 must produce:

```text
a complete canonical C82 scientific-record schema;

a complete historical pair-order and pair-coordinate ordering;

a bounded-memory record iterator;

a deterministic pair-atomic shard exporter;

a restart/checkpoint protocol with exactly-once semantics;

per-record, per-pair, per-shard, per-resolution, and aggregate
scientific digests;

two independent complete historical exports;

one interrupted-and-resumed complete historical export;

an exhaustive exporter-versus-lazy-C82 equivalence check;

a complete mutation suite operating on live stream artifacts;

a builder/validation API for C89;

and a C89/IFEQUIV4 import contract.
```

C88 may create content-addressed runtime stream shards for validation, but
it must not claim the final upstream-free public persisted C82 coefficient
snapshot. That later snapshot must be built only after historical-versus-
descendant scientific equivalence closes.

C88 must not:

```text
compare the complete historical stream with the complete current
descendant stream;

declare historical-versus-descendant scientific equivalence;

multiply projected coefficients by C80 kernels;

construct a P-minus or M-squared contact matrix;

construct a contact matrix-free action;

choose a physical coupling or counterterm;

add C53 or C58 contributions;

or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C88_C82_BOUNDED_RESTARTABLE_SCIENTIFIC_STREAM_EXPORT_READY
```

If `c88_ifstream_contract.md` freezes another exact spelling, preserve it
and record an explicit semantic alias.

The favorable continuation is:

> **C89/IFEQUIV4 — exhaustive record-level equivalence between the C88 historical-canonical stream and the current descendant C82 stream**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C77:
    physical qg basis order;
    raw/physical projection identities;
    component values and bounds;

C78:
    all supported physical pairs;
    endpoint/witness order;
    structural-support identities;

C80:
    canonical raw-coordinate schema and equivalence identity;
    no kernel-value ownership inside the C82 scientific stream;

C82:
    factor-ownership certificate;
    lazy witness-leaf to C80 coordinate route;
    projected-coefficient construction;
    pair-local grouping;
    coefficient statuses and bounds;
    reverse ancestry;
    public/scientific constructor;
    logical count semantics;

C87:
    canonical C72 scientific root;
    capsule root and loader;
    historical C74 compatibility;
    historical C82 materialization commands;
    claim boundary;
    C88 contract.
```

Read C53 and C58 only for negative controls after the exporter is frozen.

Create:

```text
docs/next_level/c88_derivation_authority_manifest.json
docs/next_level/c88_input_fidelity_audit.json
```

---

# 5. Freeze the historical execution environment

Create a clean detached worktree at:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

Stage and verify the immutable C87 capsule read-only.

Freeze:

```text
historical C82 source commit;
C87 scientific and capsule roots;
historical C74 compatibility result;
all other historical package roots consumed by C82;
the controlled Python/dependency environment;
the C82 supported-pair order;
the C82 pair-coordinate construction semantics;
the stream schema and canonical order.
```

Issue:

```text
C88_HISTORICAL_ENVIRONMENT_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c88_input_freeze.json
```

Poison current runtime directories, current descendant builders,
build-if-missing fallbacks, private current fields, and network access
during historical export.

---

# 6. Canonical scientific-record schema

Define one immutable record equivalent to:

```python
C82ScientificPairCoordinateRecord
```

Each record must include every scientifically meaningful field required for
later equivalence and persistence, including the actual repository
equivalents of:

```text
scientific schema version;

environment qualification;

resolution ID;

supported-pair ID;

physical bra ID;

physical ket ID;

physical row and column indices;

canonical C80 coordinate ID;

C80 exact coordinate-equivalence ID;

projected-coefficient exact-record or expression identity;

projected-coefficient real and imaginary midpoint;

certified absolute bound;

precision and interval convention;

terminal projected-coefficient status;

factor-ownership identity;

witness multiplicity;

canonical ordered witness/endpoint ancestry digest;

C77/C78/C80/C82 scientific-root identities;

canonical record ID.
```

The scientific record must explicitly state:

```text
contains_no_C80_kernel_value = true;

contains_no_g_s_squared = true;

contains_no_coefficient_times_kernel_product = true.
```

Exclude instance-only fields such as:

```text
temporary worktree path;

wall-clock timestamp;

host/process ID;

current descendant HEAD;

shard filename;

checkpoint filename;

execution timing;

narrative report path.
```

Classify every field as:

```text
SCIENTIFIC;

SCIENTIFIC_PROVENANCE;

STREAM_CONTROL;

INSTANCE_ONLY.
```

Create:

```text
docs/next_level/c88_scientific_record_schema.json
docs/next_level/c88_record_field_classification.json
docs/next_level/c88_scientific_record_schema_validation.json
```

---

# 7. Canonical logical ordering

Freeze one total order:

```text
resolution order from the historical C82 authority;

within a resolution:
    C78/C82 supported-pair order;

within a supported pair:
    canonical C80 coordinate-equivalence order;
    canonical C80 coordinate ID;
    canonical record ID.
```

Do not use:

```text
filesystem order;

dictionary insertion order;

parallel completion order;

floating kernel value;

or a current descendant ordering.
```

For every pair derive:

```text
pair sequence number;

first global record sequence number;

pair-local record count;

ordered record IDs;

pair digest.
```

Create:

```text
docs/next_level/c88_canonical_order_contract.json
docs/next_level/c88_canonical_order_validation.json
```

---

# 8. Bounded historical record iterator

Implement under:

```text
src/deuteron_wigner/bridge/ifstream/
```

or the exact repository-equivalent package.

Provide a builder/validation iterator equivalent to:

```python
iterate_historical_c82_scientific_records(
    *,
    resolution: str | None = None,
    pair_start: int | None = None,
    pair_stop: int | None = None,
    max_pairs_in_memory: int = ...,
    max_records_in_memory: int = ...,
)
```

Requirements:

```text
records are yielded in canonical order;

the iterator never materializes the complete supported-pair or
logical record domain;

the iterator uses bounded pair-local/lazy C82 construction;

all records are frozen;

no kernel value is queried;

no coefficient-times-kernel product is formed;

the historical worktree remains tracked-clean.
```

The implementation must enforce configurable hard limits:

```text
maximum pairs in memory;

maximum logical records in memory;

maximum pending serialized bytes;

maximum shard bytes.
```

If a limit would be exceeded, close the current unit deterministically or
fail before unbounded allocation.

Create:

```text
docs/next_level/c88_bounded_iterator_contract.json
docs/next_level/c88_bounded_iterator_validation.json
```

---

# 9. Pair-atomic export semantics

The primary export unit is one supported physical pair.

A pair should not be split across shards unless one pair alone exceeds the
declared shard limit. When a split is required, use explicit deterministic
pair-subspan identities:

```text
pair ID;

subspan index;

first/last pair-local coordinate index;

subspan record count;

subspan digest.
```

The complete ordered set of subspans must reconstruct one pair digest.

Require:

```text
every supported pair begins exactly once;

every supported pair ends exactly once;

every logical record is emitted exactly once;

no pair is omitted;

no pair is duplicated;

no record is duplicated across subspans or shards.
```

Create:

```text
docs/next_level/c88_pair_atomicity_contract.json
docs/next_level/c88_count_once_validation.json
```

---

# 10. Safe deterministic stream format

Use a canonical safe format such as:

```text
canonical UTF-8 JSON Lines with sorted keys and fixed separators;

or safe fixed-schema tables with a canonical non-pickle exact-
record side table.
```

Requirements:

```text
no Python pickle;

no object-dtype NumPy arrays;

no NaN or Infinity;

complex values encoded canonically;

bounds encoded canonically;

exact-expression IDs preserved;

line endings fixed to LF;

optional compression must be deterministic and bind its settings.
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Create:

```text
docs/next_level/c88_stream_serialization_contract.json
docs/next_level/c88_stream_serialization_validation.json
```

---

# 11. Deterministic shard format

Use runtime space equivalent to:

```text
data/runtime/c88_ifstream/
```

Each shard must contain or bind:

```text
stream schema hash;

environment qualification;

historical C82 commit;

C87 scientific/capsule roots;

resolution;

pair-sequence start and stop;

global-record-sequence start and stop;

pair count;

record count;

ordered record payload;

record digest root;

pair digest root;

previous-shard digest;

shard SHA-256;

serializer/compression identity.
```

Create one stream index and aggregate root binding all shards in order.

The aggregate scientific stream root must depend only on:

```text
scientific schema;

frozen scientific input roots;

canonical record stream;

canonical ordering.
```

It must not depend on:

```text
shard size;

temporary path;

host;

timestamp;

parallel scheduling;

or restart history.
```

Create:

```text
docs/next_level/c88_stream_shard_schema.json
docs/next_level/c88_stream_root_manifest.json
docs/next_level/c88_stream_inventory.json
```

---

# 12. Restart and checkpoint protocol

Implement operations equivalent to:

```python
export_historical_c82_scientific_stream(
    output_dir,
    *,
    checkpoint=None,
    pair_start=None,
    pair_stop=None,
    max_pairs_in_memory=...,
    max_records_in_memory=...,
    max_shard_bytes=...,
)

resume_historical_c82_scientific_stream(
    output_dir,
    checkpoint,
)

verify_historical_c82_scientific_stream(output_dir)
```

A checkpoint must bind:

```text
scientific schema hash;

all frozen input roots;

historical commit;

canonical-order version;

next pair sequence;

next global record sequence;

closed shard hashes;

rolling pair root;

rolling record root;

current pair/subspan state, if any;

exporter source/API fingerprints.
```

Restart must reject:

```text
changed input root;

changed schema;

changed ordering;

changed exporter fingerprint;

mismatched closed shard;

mismatched cursor;

a checkpoint from another output directory;

or a partially written unverified shard.
```

Use atomic close/rename semantics for shards and checkpoints.

Create:

```text
docs/next_level/c88_restart_checkpoint_contract.json
docs/next_level/c88_restart_checkpoint_validation.json
```

---

# 13. Complete historical-domain census

Derive and report:

```text
supported-pair counts by resolution;

actual logical pair-coordinate record counts by resolution;

unique C80 coordinate-ID counts;

unique coordinate-equivalence counts;

coefficient-status counts;

exact-expression versus numerical-only record counts;

witness multiplicity distribution;

ancestry-link counts;

maximum records in one pair;

maximum serialized pair size.
```

The inherited supported-pair total is expected to be:

```text
154,830
```

but the repository is authoritative.

Create:

```text
docs/next_level/c88_historical_stream_census.json
docs/next_level/c88_historical_stream_census_validation.json
```

---

# 14. Exporter-versus-lazy-C82 exhaustive equivalence

During validation, compare every exported pair and record against the
historical C82 lazy scientific route from which it was derived.

For every pair compare:

```text
pair identity;

record count;

ordered coordinate IDs;

equivalence IDs;

coefficient exact/value identities;

midpoints;

bounds;

statuses;

factor ownership;

witness multiplicities;

ancestry digests;

pair digest.
```

Require:

```text
missing pairs = 0;

extra pairs = 0;

missing records = 0;

extra records = 0;

order mismatches = 0;

scientific-field mismatches = 0.
```

The equivalence check must itself be streaming and bounded.

Create:

```text
docs/next_level/c88_exporter_lazy_equivalence_report.json
```

A sampled comparison is insufficient.

---

# 15. Two complete clean historical exports

Run two complete exports in separate clean detached worktrees, with the
same C87 capsule and controlled environment.

Require equality of:

```text
scientific schema;

logical census;

all record IDs;

all pair digests;

all resolution roots;

aggregate scientific stream root.
```

Shard sizes may differ only in a dedicated shard-layout sensitivity test.
For the primary deterministic test, keep identical shard settings and
require byte-identical output.

Create:

```text
docs/next_level/c88_two_clean_export_determinism.json
```

---

# 16. Interrupted-and-resumed full export

Perform one controlled interruption after multiple closed shards and, when
possible, one interruption during an open pair/subspan.

Resume from the last verified checkpoint.

Compare the final result with the uninterrupted export:

```text
record stream equality;

pair digest equality;

resolution root equality;

aggregate root equality;

no duplicate or skipped sequence numbers;

no changed scientific record.
```

Create:

```text
docs/next_level/c88_restart_full_export_report.json
```

---

# 17. Serial/sharded layout sensitivity

Run:

```text
one serial small-shard export;

one deterministic larger-shard export;

one supported parallel producer with canonical ordered commit of
shards.
```

The physical shard bytes may differ when layout settings differ, but the
canonical pair digests, resolution roots, and aggregate scientific root
must remain identical.

Parallel workers may compute disjoint pair ranges, but only the canonical
coordinator may commit shards and update the rolling root.

Create:

```text
docs/next_level/c88_shard_layout_sensitivity_report.json
docs/next_level/c88_resource_and_scaling_report.json
```

Report measured peak memory and prove it remains bounded independently of
the total stream size.

---

# 18. Builder/validation API for C89

Provide operations equivalent to:

```python
load_verified_historical_c82_stream_index(path)

iterate_verified_historical_c82_stream(
    path,
    *,
    resolution=None,
    pair_start=None,
    pair_stop=None,
)

verify_historical_c82_stream_root(path)

historical_c82_pair_digest(path, bra_id, ket_id, resolution)
```

This is not yet the final public persisted coefficient package. It is a
verified builder/validation stream for C89.

The loader must:

```text
verify index and every accessed shard;

verify record and pair sequence continuity;

return frozen records;

perform no upstream C82 recomputation;

query no C80 kernel values;

form no contact product.
```

Create:

```text
docs/next_level/c88_stream_api_contract.json
docs/next_level/c88_stream_api_validation.json
```

---

# 19. Current descendant source preflight

Prepare—but do not execute the complete scientific comparison—a current
descendant C82 record-source adapter using the same C88 scientific schema.

The preflight must verify on frozen beginning, middle, and ending pair
ranges that the descendant route can provide:

```text
the same field vocabulary;

canonical IDs;

coefficient values and bounds;

statuses;

factor ownership;

witness multiplicity;

ancestry.
```

Do not compare the complete historical and descendant domains in C88.

Create:

```text
docs/next_level/c88_descendant_stream_adapter_preflight.json
```

---

# 20. C89/IFEQUIV4 import contract

Define the exact contract by which C89 consumes:

```text
the C88 historical stream schema;

the C88 verified historical stream index/root;

the historical environment qualification;

the pair and record ordering;

the logical census;

the verified stream iterator;

the descendant record-source adapter;

the exact scientific-field and instance-field classification.
```

C89 must:

```text
export or stream the complete current descendant C82 domain under
the same schema;

compare every pair and record exhaustively;

type every scientific and instance-only difference;

and issue the scientific-equivalence decision.
```

C89 must not reopen the C72 capsule reconstruction unless the C88 stream
root or capsule fails verification.

Create:

```text
docs/next_level/c88_c89_ifequiv4_import_contract.json
```

---

# 21. Isolation and mutation controls

Required failures include mutations of:

```text
historical C82 commit;

C87 scientific or capsule root;

scientific schema;

canonical ordering;

resolution order;

pair sequence;

record sequence;

pair ID;

coordinate ID;

equivalence ID;

coefficient exact record;

coefficient midpoint;

coefficient bound;

terminal status;

factor ownership;

witness multiplicity;

ancestry digest;

pair digest;

record digest;

shard hash;

previous-shard link;

checkpoint cursor;

rolling root;

checkpoint input root;

serializer identity;

partial-shard atomicity;

duplicate pair;

missing pair;

duplicate record;

unsafe loading;

mutable return;

C89 import-contract field.
```

The aggregate scientific root must remain invariant under mutations of:

```text
temporary output path;

host/process ID;

wall-clock timing;

shard size;

parallel scheduling;

checkpoint frequency;

narrative report path.
```

Poison:

```text
C80 kernel numerical values;

C53 values/propagators;

C58 values;

physical coupling;

counterterm interfaces.
```

The C88 stream must remain unchanged.

Create at least **384 focused live mutations** of actual records, shards,
checkpoints, indices, iterators, and roots.

Create:

```text
docs/next_level/c88_isolation_report.json
docs/next_level/c88_regression_report.json
```

---

# 22. Readiness and continuation decisions

Select exactly one branch.

## 22.1 Favorable branch

Issue the exact positive status frozen by the C88 contract, with preferred
semantic identity:

```text
C88_C82_BOUNDED_RESTARTABLE_SCIENTIFIC_STREAM_EXPORT_READY
```

Required:

```text
the historical environment freezes successfully;

the canonical scientific schema and order are complete;

the iterator is demonstrably memory bounded;

all supported pairs and logical records are exported exactly once;

the complete logical census is known;

the stream format is safe and deterministic;

per-record, pair, shard, resolution, and aggregate roots close;

exporter-versus-lazy-C82 exhaustive equivalence has zero
mismatches;

two clean full historical exports agree;

the interrupted/resumed export agrees with the uninterrupted
export;

serial/sharded layout sensitivity preserves the scientific root;

the C89 stream API and import contract are complete;

the complete mutation suite passes.
```

Next:

> **C89/IFEQUIV4 — exhaustive historical-canonical versus descendant C82 scientific-record equivalence**

## 22.2 Scientific schema incomplete

```text
C88_IFSTREAM_SCIENTIFIC_SCHEMA_INCOMPLETE
```

Next:

> **C89/IFSCHEMA — repair only the specifically identified missing scientific field, identity, bound, status, ownership, or ancestry record**

## 22.3 Complete coverage incomplete

```text
C88_IFSTREAM_COMPLETE_COVERAGE_INCOMPLETE
```

Next:

> **C89/IFCOVERAGE — repair only the specifically identified missing/duplicate pair, record, coordinate, or count-once defect**

## 22.4 Bounded exporter incomplete

```text
C88_IFSTREAM_BOUNDED_EXPORT_INCOMPLETE
```

Next:

> **C89/IFBOUNDSTREAM — repair only the specifically identified unbounded allocation, pair atomicity, shard, or resource-control defect**

## 22.5 Restart/determinism incomplete

```text
C88_IFSTREAM_RESTART_DETERMINISM_INCOMPLETE
```

Next:

> **C89/IFRESTART — repair only the specifically identified checkpoint, cursor, rolling-root, shard-commit, or nondeterminism defect**

## 22.6 Exported-versus-lazy mismatch

```text
C88_IFSTREAM_SCIENTIFIC_PAYLOAD_MISMATCH
```

Next:

> **C89/IFSTREAMDIFF — resolve only the specifically identified record, order, coefficient, bound, status, ownership, or ancestry mismatch**

## 22.7 Builder/validation API incomplete

```text
C88_IFSTREAM_API_INCOMPLETE
```

Next:

> **C89/IFSTREAMAPI — repair only the index, shard verification, immutable iterator, safe-loading, or descendant-adapter defect**

Do not issue historical-versus-descendant scientific equivalence,
persisted-public-import, coefficient-times-kernel, contact-matrix,
complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD,
matching, inference, or production status.

---

# 23. Essential deliverables

Create at least:

```text
docs/next_level/c88_implementation_report.md
docs/next_level/c88_derivation_authority_manifest.json
docs/next_level/c88_input_fidelity_audit.json
docs/next_level/c88_descendant_qualification.json
docs/next_level/c88_input_freeze.json

docs/next_level/c88_scientific_record_schema.json
docs/next_level/c88_record_field_classification.json
docs/next_level/c88_scientific_record_schema_validation.json
docs/next_level/c88_canonical_order_contract.json
docs/next_level/c88_canonical_order_validation.json

docs/next_level/c88_bounded_iterator_contract.json
docs/next_level/c88_bounded_iterator_validation.json
docs/next_level/c88_pair_atomicity_contract.json
docs/next_level/c88_count_once_validation.json
docs/next_level/c88_stream_serialization_contract.json
docs/next_level/c88_stream_serialization_validation.json

docs/next_level/c88_stream_shard_schema.json
docs/next_level/c88_stream_root_manifest.json
docs/next_level/c88_stream_inventory.json
docs/next_level/c88_restart_checkpoint_contract.json
docs/next_level/c88_restart_checkpoint_validation.json

docs/next_level/c88_historical_stream_census.json
docs/next_level/c88_historical_stream_census_validation.json
docs/next_level/c88_exporter_lazy_equivalence_report.json
docs/next_level/c88_two_clean_export_determinism.json
docs/next_level/c88_restart_full_export_report.json
docs/next_level/c88_shard_layout_sensitivity_report.json
docs/next_level/c88_resource_and_scaling_report.json

docs/next_level/c88_stream_api_contract.json
docs/next_level/c88_stream_api_validation.json
docs/next_level/c88_descendant_stream_adapter_preflight.json
docs/next_level/c88_c89_ifequiv4_import_contract.json

docs/next_level/c88_isolation_report.json
docs/next_level/c88_readiness_report.json
docs/next_level/c88_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifstream/
```

Use runtime space equivalent to:

```text
data/runtime/c88_ifstream/
```

The full stream shards may remain ignored runtime artifacts when
reconstructible; all roots, schemas, counts, commands, and validation
digests must be tracked.

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 24. Acceptance criteria

C88 is complete only when:

1. Baseline `cb6332f525ab4a26e8ac16e5173479aeac69753c` reproduces.
2. Both protected untracked paths remain untouched.
3. C72/C74/C77/C78/C80/C82/C87 historical artifacts remain unchanged.
4. `c88_ifstream_contract.md` is consumed and preserved.
5. The exact historical C72 runtime-instance claim remains unknown and unclaimed.
6. The C87 capsule and scientific roots verify before export.
7. The historical C82 worktree remains tracked-clean.
8. Current runtime and build fallbacks are poisoned.
9. The complete scientific-record schema is frozen.
10. Every scientific field needed for later equivalence is present.
11. Instance-only fields are excluded explicitly.
12. Canonical pair and record ordering is deterministic.
13. The iterator enforces bounded pairs, records, bytes, and shard size.
14. The complete domain is never materialized in memory.
15. Every supported pair is exported exactly once.
16. Every logical pair-coordinate record is exported exactly once.
17. No unsupported pair is introduced.
18. Pair-atomic or explicit pair-subspan semantics close.
19. The complete logical census is derived.
20. Per-record, pair, shard, resolution, and aggregate roots are computed.
21. The scientific root is independent of shard layout and checkpoint history.
22. Every payload uses a safe non-pickle format.
23. Every NumPy load explicitly uses `allow_pickle=False`.
24. Restart rejects changed roots, schema, order, exporter, or closed shards.
25. Restart produces no missing or duplicate sequence.
26. Exporter-versus-lazy-C82 exhaustive equivalence has zero mismatches.
27. Two clean historical exports have identical scientific streams.
28. Interrupted/resumed and uninterrupted exports agree.
29. Serial and sharded exports preserve the scientific root.
30. Peak memory is measured and bounded independently of total stream size.
31. The verified stream API performs no upstream recomputation.
32. The descendant source adapter preflight passes.
33. The C89 import contract is complete.
34. At least 384 focused live mutations pass.
35. No complete historical-versus-descendant equivalence claim is made.
36. No final public persisted C82 snapshot is claimed.
37. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
38. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
39. The working tree is clean except for the two protected untracked paths.
40. A local completion commit is created and not pushed.

Do not weaken complete record coverage, canonical ordering, boundedness,
restart exactly-once semantics, record-level equivalence, or scientific
root independence to open the gate.

---

# 25. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C88 contract identity;
- historical C82 commit;
- C87 scientific/capsule roots and environment qualification;
- scientific-record schema and canonical-order hashes;
- supported-pair counts by resolution;
- actual logical pair-coordinate record counts by resolution;
- unique coordinate/equivalence counts;
- coefficient-status and witness-multiplicity counts;
- maximum records and bytes in one pair;
- shard count and layout;
- record, pair, resolution, and aggregate scientific roots;
- missing, duplicate, extra, order-mismatch, and scientific-field-mismatch counts;
- measured peak memory and configured bounds;
- two-clean-export equality;
- interrupted/resumed equality;
- serial/sharded scientific-root equality;
- checkpoint and restart validation;
- verified stream API result;
- descendant adapter preflight;
- C89 import-contract result;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime-instance recovery is not claimed;
- confirmation that no historical-versus-descendant equivalence claim, final public persisted snapshot, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
