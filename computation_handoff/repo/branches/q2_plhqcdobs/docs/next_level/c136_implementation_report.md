# C136/HQCDID3 implementation report

Plan ID3-B is selected. The exact C132/C131 public registry yields eleven
directions. Three exact reductions are retained: `m_q^2=(m_q)^2`, vacuum
projection exclusion, and nonmatrix Q_R boundary/truncation interfaces.
The admissible condition ledger has twelve rows with deterministic selectors.
Mass and vertex families retain the C135 external-input semantics; gluon
factorization remains spectator-dependent in all three routes; Ward/current
rows have zero calibration sensitivity. The two independent evaluator and
Jacobian routes agree. Generic and diagnostic rank are 2, with an explicit
nine-dimensional nullspace. Only the reduced maximal identifiable subspace
may be solved in a later package; C136 solves nothing.
