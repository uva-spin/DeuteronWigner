# C91/IFEQUIV6 Codex Work Package

## Title

**Exhaustive historical-versus-descendant C82 factorized-semantic equivalence: current-descendant compiler, 154,830-pair normal-form and primitive-root comparison, theorem-backed expanded-sequence identity, mismatch-localized record diagnostics, and C92 canonical persistence handoff**

## Authoritative baseline

Start from the clean local C90 completion commit:

```text
ac622ab358b83f090717d7e7fa179b58f18f526d
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "ac622ab358b83f090717d7e7fa179b58f18f526d"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the C90-to-C91 import contract:

```text
docs/next_level/c90_c91_ifequiv6_import_contract.json
```

If the committed contract freezes exact status names, schemas, APIs, or a narrower scientific boundary, preserve them. This work package supplies the descendant compiler, exhaustive pair comparison, mismatch localization, equivalence decision, and C92 handoff plan; it does not authorize weakening the committed contract.

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical environment remains qualified as:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

The baseline is authoritative only when it contains and reproduces:

```text
C87 canonical color scientific authority:
    canonical source-chain equivalent;
    exact historical runtime instance not claimed;

C88 exact logical census:
    28,606,464;
    165,991,250;
    697,394,304;
    891,992,018 total logical records;

C89 exact factorized pair programs and mixed-radix rank/unrank;

C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY:
    Route A compiled independently from historical C78/C82 semantics;
    Route B normalized immutable C89 pair programs;
    identical typed normal forms for all 154,830 pairs;
    authenticated primitive scientific roots;
    structural expansion-equivalence checker;
    compact 154,830-entry FACTORIZED_SEMANTIC_PROGRAM_ROOT ledger;
    two clean, pair-atomic restart, and deterministic parallel
    passes sharing one historical semantic root;
    no expanded serialized-record traversal or stream hash.
```

The reported aggregate semantic-root prefix is:

```text
efee3c...6a34
```

Verify and record the complete root from the committed C90 runtime and manifests. The prefix in this prompt is not authority.

Create a local completion commit. Do not push.

---

# 1. Scientific meaning of C90 and the C91 question

C90 has established the complete semantics of the historical C82
pair-coordinate domain without traversing all 891,992,018 Python records.

For each supported physical pair \(p\), C90 has a normalized typed program

\[
\mathcal P^{\rm hist}_p
\]

and authenticated primitive tables such that the C90 expansion theorem
establishes a unique ordered C88 scientific-record sequence

\[
\llbracket \mathcal P^{\rm hist}_p \rrbracket.
\]

C91 must now answer one question:

> Does the current descendant C82 scientific construction define the same
> complete ordered scientific-record sequence for every one of the 154,830
> supported pairs?

C91 must construct an independent descendant program

\[
\mathcal P^{\rm desc}_p,
\]

normalize it under the unchanged C90 semantic IR, and compare it with the
historical program.

When:

```text
normal forms are identical;
primitive scientific records are identical under declared adapters;
logical counts and orderings are identical;
and the C90 expansion theorem validates every node,
```

C91 may conclude:

\[
\llbracket \mathcal P^{\rm hist}_p \rrbracket
=
\llbracket \mathcal P^{\rm desc}_p \rrbracket
\]

without expanding all leaves.

This is exact factorized-semantic equivalence, not sampling, approximate
hash agreement, or runtime-instance identity.

---

# 2. Claim boundary

C91 may establish:

```text
historical-canonical versus current-descendant C82 scientific
payload equivalence at the complete factorized-semantic level.
```

C91 must not claim:

```text
that the unknowable original C72 runtime directory was recovered;

that the C90 semantic root is an expanded serialized-record hash;

that a final upstream-free public pair-coordinate coefficient
package already exists;

that any coefficient has been multiplied by a C80 kernel;

that a contact matrix, physical coupling, counterterm, complete
instantaneous-fermion operator, TMD, fit, inference, or production
object exists.
```

The favorable equivalence statement must retain the historical environment
qualification and must distinguish scientific identity from package-
instance identity.

---

# 3. Exact purpose and stopping point

C91 must produce:

```text
a frozen current-descendant C82 scientific environment;

an independent descendant semantic compiler into the unchanged C90
typed IR;

a scientific primitive-root crosswalk between historical and
descendant objects;

a complete supported-pair identity and ordering crosswalk;

a normalized descendant program for every supported pair;

an exhaustive 154,830-pair comparison of normal forms, primitive
roots, counts, orderings, expressions, bounds, statuses, and
ancestry;

application of the C90 expansion-equivalence theorem to every
matched pair;

bounded record-level diagnostics for every mismatched pair;

a typed classification of every scientific and instance-only
difference;

historical, descendant, and equivalence-certificate roots;

two clean, restart, serial, and deterministic parallel comparison
passes;

an immutable equivalence API;

and a C92 canonical factorized-persistence import contract.
```

C91 must not persist 891,992,018 expanded rows.

The preferred positive semantic status is:

```text
C91_C82_HISTORICAL_DESCENDANT_FACTORIZED_SEMANTIC_EQUIVALENCE_READY
```

If the C90 import contract freezes another exact spelling, preserve that
spelling and record an explicit semantic alias.

The favorable continuation is:

> **C92/IFPERSIST3 — create an authenticated, upstream-free canonical factorized pair-coordinate coefficient package from the C91-equivalent semantic authority, with exact on-demand record reconstruction for later C80 kernel multiplication**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C77:
    physical qg basis order;
    raw/physical projection identities;
    component exact/value/bound records;
    metric and conjugation conventions;

C78:
    all supported-pair identities;
    endpoints, witnesses, retained-q intermediates;
    source order and structural support;
    logical coordinate domains;

C80:
    canonical ContactKernelCoordinate schema;
    exact coordinate-equivalence semantics;
    factor ownership;
    no numerical kernel values inside the C82 scientific payload;

C82:
    witness-leaf-to-C80-coordinate construction;
    projected-coefficient construction;
    pair-local grouping;
    coefficient expressions, values, bounds, and statuses;
    factor ownership and reversible ancestry;

C87:
    canonical C72 color scientific root and compatibility capsule;
    exact claim boundary;

C88:
    scientific record schema and field classification;
    canonical record ordering;
    exact logical census;

C89:
    immutable pair-program and rank/unrank semantics;

C90:
    semantic IR;
    normal-form rules;
    primitive scientific tables;
    Route-A historical compiler;
    Route-B C89 normalizer;
    expansion-equivalence theorem and proof checker;
    historical pair-program ledger;
    historical summaries;
    historical semantic roots;
    immutable historical-attestation API;
    descendant adapter preflight;
    C91 import contract.
```

Create:

```text
docs/next_level/c91_derivation_authority_manifest.json
docs/next_level/c91_input_fidelity_audit.json
```

---

# 5. Freeze the historical C90 authority

Consume the historical C90 package only through its immutable public API.

Freeze and verify:

```text
historical C82 commit;
historical environment qualification;
C87 scientific and capsule roots;
C88 scientific schema and canonical order;
C90 semantic-IR schema;
C90 normal-form version;
C90 expansion-theorem/proof-checker version;
all primitive-table roots;
all 154,830 historical pair IDs and sequence numbers;
all historical pair normal-form roots;
all historical pair summaries;
all resolution roots;
the complete aggregate historical semantic root.
```

Issue:

```text
C91_HISTORICAL_SEMANTIC_AUTHORITY_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c91_historical_input_freeze.json
```

Do not rebuild or reinterpret the historical ledger through current private
fields.

---

# 6. Freeze the current descendant scientific environment

Define the current descendant scientific route at the C91 baseline.

Freeze:

```text
current descendant source commit;
current public C77/C78/C80/C82 scientific package roots;
current color scientific payload;
current supported-pair domain;
current coordinate schema;
current coefficient/status/bound/ancestry semantics;
current source/API fingerprints needed for reconstruction.
```

The current descendant environment may contain runtime and API instance
changes. These are not automatically scientific differences.

Issue:

```text
C91_DESCENDANT_SCIENTIFIC_ENVIRONMENT_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c91_descendant_input_freeze.json
```

Poison:

```text
historical private builders during descendant compilation;
current C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical-coupling and counterterm interfaces.
```

---

# 7. Current descendant semantic compiler

Implement a descendant compiler that reads the current C77/C78/C80/C82
scientific semantics and emits the unchanged C90 semantic IR.

It must not:

```text
load the historical pair normal form and return it as descendant
output;

copy the C90 historical pair root;

call the historical Route-A compiler;

call the C89 historical program constructor;

or define equality from a pre-existing expected hash.
```

The compiler must independently reconstruct:

```text
pair identity and order;

witness and endpoint axes;

projection-amplitude primitive references;

coordinate templates and exact equivalence identities;

coefficient expressions;

bound composition;

terminal status rules;

witness multiplicity;

reversible ancestry;

complete C88 scientific-record map.
```

Create:

```text
docs/next_level/c91_descendant_semantic_compiler.json
docs/next_level/c91_descendant_semantic_compiler_validation.json
```

---

# 8. Scientific primitive-root crosswalk

Historical and descendant package-instance roots may differ even when the
scientific records are identical.

Construct a typed crosswalk for every primitive scientific table:

```text
historical primitive table/root;
descendant primitive table/root;
scientific schema;
record count;
canonical order;
record-level comparison method;
declared identity adapter, if any;
scientific equivalence status;
instance-metadata differences.
```

Required primitive families include the actual repository equivalents of:

```text
supported-pair identities;

physical bra/ket identities;

C77 projection amplitudes and bounds;

C78 witnesses, endpoints, retained-q intermediates, and signs;

C87/current color authority records;

C80 coordinate templates and equivalence identities;

metric and conjugation records;

coefficient-expression primitives;

bound primitives;

status rules;

ancestry primitives.
```

Classify each relation:

```text
BYTE_IDENTICAL_SCIENTIFIC_TABLE;

CANONICALLY_IDENTICAL_SCIENTIFIC_TABLE;

SCIENTIFICALLY_IDENTICAL_WITH_EXPLICIT_ADAPTER;

SCIENTIFIC_TABLE_DIFFERENCE;

INSTANCE_ONLY_DIFFERENCE;

UNRESOLVED.
```

A favorable C91 result requires no `SCIENTIFIC_TABLE_DIFFERENCE` and no
`UNRESOLVED`.

Create:

```text
docs/next_level/c91_primitive_root_crosswalk.json
docs/next_level/c91_primitive_record_equivalence.json
```

Do not treat equal top-level counts as primitive equivalence. Compare the
complete canonical record domain or prove equality through the relevant
source-chain theorem.

---

# 9. Color-authority crosswalk

The historical route is explicitly based on:

```text
C87_CANONICAL_SOURCE_CHAIN_EQUIVALENT color authority.
```

The descendant route may use a later public color package or a different
package-instance root.

Compare the complete scientific color payload under the C87 schema:

```text
24 row identities;
3 column identities;
72 pair records;
exact expressions and zero certificates;
per-entry values and bounds;
E_src, Gram, U3, U3-dagger, P3;
intertwining, rank, projector, and leakage identities.
```

Issue one relation:

```text
DESCENDANT_COLOR_SCIENCE_IDENTICAL_TO_C87_CANONICAL;

DESCENDANT_COLOR_SCIENCE_DIFFERS_FROM_C87_CANONICAL;

DESCENDANT_COLOR_INSTANCE_DIFFERS_SCIENCE_IDENTICAL;

UNRESOLVED.
```

Create:

```text
docs/next_level/c91_color_authority_crosswalk.json
```

The exact original historical C72 runtime instance remains unknown
regardless of this result.

---

# 10. Supported-pair identity and ordering crosswalk

Compare all supported pairs between historical and descendant routes.

Require identity of:

```text
resolution order;
pair sequence;
physical bra/ket IDs;
row/column indices;
structural-support status;
pair-local scientific domain;
canonical pair ID.
```

Report:

```text
missing historical pairs;
extra descendant pairs;
pair identity mismatches;
pair order mismatches;
resolution mismatches;
support-status mismatches.
```

A favorable result requires all counts to be zero.

Create:

```text
docs/next_level/c91_supported_pair_crosswalk.json
```

The authoritative total remains:

```text
154,830 supported pairs.
```

---

# 11. Normalize every descendant pair program

For every current descendant supported pair:

1. compile the pair into the C90 semantic IR;
2. bind descendant primitive scientific roots through the approved crosswalk;
3. apply the unchanged C90 normal-form rules;
4. compute exact cardinality and summaries;
5. compute the descendant pair semantic-program root.

The descendant normal form must expose:

```text
node graph;
primitive references;
logical count;
mixed-radix order;
record-construction expression;
coefficient expression;
bound rule;
status rule;
ancestry rule;
first/last logical record identities;
closed-form summary;
normal-form root.
```

Create:

```text
docs/next_level/c91_descendant_pair_program_manifest.json
docs/next_level/c91_descendant_pair_program_validation.json
```

Do not persist expanded records.

---

# 12. Exhaustive 154,830-pair semantic comparison

For every pair compare historical and descendant:

```text
pair identity;
normal-form IR;
node graph;
primitive scientific roots after approved adapters;
exact logical count;
mixed-radix order;
record-construction expression;
coefficient expression;
bound rule;
status rule;
ancestry rule;
first/last record identities;
closed-form summary;
pair semantic-program root.
```

Report exact mismatch counts by category:

```text
MISSING_PAIR;
EXTRA_PAIR;
PAIR_IDENTITY;
NORMAL_FORM;
NODE_GRAPH;
PRIMITIVE_ROOT;
LOGICAL_COUNT;
ORDER;
RECORD_EXPRESSION;
COEFFICIENT_EXPRESSION;
BOUND_RULE;
STATUS_RULE;
ANCESTRY_RULE;
FIRST_LAST_IDENTITY;
SUMMARY;
PAIR_ROOT.
```

Create:

```text
docs/next_level/c91_exhaustive_pair_semantic_comparison.json
```

A favorable result requires all mismatch counts to be zero.

---

# 13. Apply the C90 expansion-equivalence theorem

For every matched pair, run the immutable C90 proof checker using:

```text
historical normal form;
descendant normal form;
approved primitive-root equivalence certificates;
C88 scientific schema;
canonical order;
exact cardinality;
all map/bound/status/ancestry expressions.
```

The pair result must state:

```text
EXPANDED_C88_SEQUENCE_IDENTICAL_BY_FACTORIZED_SEMANTIC_PROOF
```

and must cite the exact theorem/proof-checker version.

Construct:

```text
per-pair proof-certificate root;
per-resolution proof root;
aggregate historical-descendant equivalence-certificate root.
```

Create:

```text
docs/next_level/c91_expansion_equivalence_application.json
docs/next_level/c91_equivalence_certificate_manifest.json
```

Do not describe this certificate as an expanded serialized-record hash.

---

# 14. Bounded diagnostics for mismatched pairs

When any pair differs, do not expand the entire global domain.

For each mismatched pair:

```text
produce a structural tree diff;

identify the first differing node and primitive dependency;

compare exact counts and axis cardinalities;

locate differing ordinal ranges through mixed-radix algebra;

materialize bounded records at:
    first difference;
    surrounding semantic boundary;
    first/last ordinal;
    deterministic root-seeded interior ordinals;

compare every C88 scientific field;

write one byte-capped diagnostic artifact.
```

When a pair is too large, use recursive interval localization until the
first differing ordinal range is bounded.

Provide operations equivalent to:

```python
diagnose_pair_semantic_difference(pair_id, resolution, max_bytes)

materialize_historical_descendant_record_diff(
    pair_id,
    ordinal_ranges,
    resolution,
    max_bytes,
)
```

Create:

```text
docs/next_level/c91_mismatch_localization_contract.json
docs/next_level/c91_mismatch_diagnostic_report.json
```

For the favorable branch, this report records zero mismatched pairs and
demonstrates the diagnostic route on mutation controls only.

---

# 15. Difference classification

Every observed difference must be classified as exactly one of:

```text
SCIENTIFIC_PRIMITIVE_DIFFERENCE;

SCIENTIFIC_PROGRAM_DIFFERENCE;

SCIENTIFIC_ORDER_OR_MULTIPLICITY_DIFFERENCE;

SCIENTIFIC_COEFFICIENT_OR_BOUND_DIFFERENCE;

SCIENTIFIC_STATUS_OR_ANCESTRY_DIFFERENCE;

SCIENTIFIC_SCHEMA_DIFFERENCE;

INSTANCE_ONLY_PACKAGE_ROOT_DIFFERENCE;

INSTANCE_ONLY_PATH_LAYOUT_DIFFERENCE;

INSTANCE_ONLY_SOURCE_API_FINGERPRINT_DIFFERENCE;

DESCENDANT_DOCUMENTATION_DIFFERENCE;

UNRESOLVED_DIFFERENCE.
```

For every instance-only classification, prove that the canonical primitive
records and normalized pair programs are unchanged.

Create:

```text
docs/next_level/c91_difference_classification.json
docs/next_level/c91_instance_science_separation_report.md
```

Do not use “metadata-only” without field-level evidence.

---

# 16. Descendant and equivalence roots

Construct a compact current-descendant ledger with one entry per pair.

Each entry binds:

```text
pair identity;
descendant normal-form root;
descendant primitive roots;
logical count;
closed-form summary;
proof-certificate identity;
comparison status.
```

Construct:

```text
descendant resolution roots;
descendant aggregate semantic root;
historical-descendant equivalence-certificate root.
```

The descendant aggregate root may equal or differ from the historical root
depending on whether instance-independent canonicalization is used. The
equivalence decision must be based on the normalized scientific records and
proof certificates, not on an unexplained top-level root coincidence.

Create:

```text
docs/next_level/c91_descendant_semantic_root_manifest.json
docs/next_level/c91_historical_descendant_equivalence_root.json
```

---

# 17. Scientific equivalence decision

Select exactly one scientific relation:

```text
COMPLETE_FACTORIZED_SEMANTIC_EQUIVALENCE;

SCIENTIFICALLY_EQUIVALENT_WITH_INSTANCE_ONLY_DIFFERENCES;

SCIENTIFIC_PAYLOAD_MISMATCH;

SCIENTIFIC_EQUIVALENCE_UNRESOLVED.
```

Definitions:

```text
COMPLETE_FACTORIZED_SEMANTIC_EQUIVALENCE:
    canonical scientific primitive records and every normalized
    pair program are identical;

SCIENTIFICALLY_EQUIVALENT_WITH_INSTANCE_ONLY_DIFFERENCES:
    package instances differ, but complete primitive and program
    equivalence is proved under explicit adapters;

SCIENTIFIC_PAYLOAD_MISMATCH:
    at least one scientific primitive or pair program differs;

SCIENTIFIC_EQUIVALENCE_UNRESOLVED:
    a required comparison or proof cannot be completed.
```

Create:

```text
docs/next_level/c91_scientific_equivalence_decision.json
```

A positive readiness status may be issued for either of the first two
relations, provided every instance-only adapter is explicit and verified.

---

# 18. Two clean passes, restart, and deterministic parallel comparison

Run:

```text
two complete clean C91 comparison passes;

one interrupted/resumed pass;

one serial pass;

one deterministic pair-range parallel pass with canonical commit
order.
```

Require equality of:

```text
primitive crosswalk decisions;
all pair comparison results;
all proof-certificate roots;
descendant resolution and aggregate roots;
equivalence-certificate root;
scientific equivalence decision.
```

Use pair-atomic checkpoints.

A checkpoint must bind:

```text
historical C90 root;
descendant scientific input roots;
semantic-IR and normal-form versions;
primitive-crosswalk root;
next pair sequence;
closed ledger-shard hashes;
rolling descendant and equivalence roots;
compiler/proof-checker fingerprints.
```

Create:

```text
docs/next_level/c91_restart_contract.json
docs/next_level/c91_restart_validation.json
docs/next_level/c91_two_clean_pass_determinism.json
docs/next_level/c91_parallel_comparison_report.json
docs/next_level/c91_resource_and_scaling_report.json
```

---

# 19. Immutable equivalence API

Implement under:

```text
src/deuteron_wigner/bridge/ifequiv6/
```

or the exact repository-equivalent package.

Use compact runtime space equivalent to:

```text
data/runtime/c91_ifequiv6/
```

Provide immutable operations equivalent to:

```python
load_verified_c82_semantic_equivalence()

verify_c82_semantic_equivalence_root()

c82_pair_equivalence(pair_id, resolution)

historical_pair_normal_form(pair_id, resolution)

descendant_pair_normal_form(pair_id, resolution)

c82_pair_equivalence_proof(pair_id, resolution)

diagnose_c82_pair_difference(
    pair_id,
    resolution,
    *,
    max_bytes,
)

current_scientific_equivalence_decision()
```

The API must:

```text
verify all compact ledgers and roots;

return frozen records and non-writeable arrays;

answer a pair query without comparing all pairs again;

query no C80 numerical kernel values;

form no coefficient-times-kernel product;

perform no hidden rebuild-if-missing operation.
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Create:

```text
docs/next_level/c91_api_contract.json
docs/next_level/c91_api_validation.json
docs/next_level/c91_runtime_inventory.json
docs/next_level/c91_deterministic_reconstruction_report.json
```

---

# 20. C92 canonical factorized persistence contract

Define the exact contract by which C92 consumes:

```text
the positive C91 scientific equivalence decision;

the canonical C88 scientific-record schema;

the C90/C91 semantic IR and normal-form versions;

the approved canonical primitive scientific tables;

the supported-pair order;

the descendant pair-program ledger;

the expansion-equivalence theorem;

the exact rank/unrank and on-demand reconstruction semantics;

the pair summaries, roots, and proof certificates.
```

C92 must create an authenticated, upstream-free factorized public package
that can return, for any supported pair and pair-local ordinal:

```text
the complete canonical C82 pair-coordinate scientific record;

the projected coefficient expression/value;

the certified bound;

the terminal coefficient status;

the canonical C80 coordinate identity;

the complete reversible ancestry.
```

C92 public loading must not call historical or current C77/C78/C82 builders.

C92 must not materialize all 891,992,018 records.

C92 must not multiply the coefficient by the C80 kernel.

Create:

```text
docs/next_level/c91_c92_ifpersist3_import_contract.json
```

---

# 21. Isolation and mutation controls

Required failures include mutations of:

```text
historical C90 root;

descendant source commit;

C88 scientific schema;

semantic-IR version;

normal-form rule;

expansion theorem or proof checker;

historical pair identity;

descendant pair identity;

pair order;

primitive-table record;

primitive root adapter;

color authority record;

logical count;

node graph;

record expression;

coefficient expression;

bound rule;

status rule;

ancestry rule;

first/last identity;

pair summary;

pair root;

proof certificate;

equivalence decision;

checkpoint cursor;

closed ledger shard;

unsafe loading;

mutable return;

C92 import-contract field.
```

The equivalence decision must remain invariant under mutations of proven
instance-only fields:

```text
temporary paths;

runtime shard names;

host/process IDs;

wall-clock timestamps;

current roadmap bytes;

narrative report locations;

package-instance roots whose scientific records are unchanged.
```

Poison:

```text
C80 numerical kernel values;

C53 values and propagators;

C58 values;

physical coupling;

counterterm interfaces.
```

The C91 comparison must remain unchanged.

Create at least **384 focused live mutations** of actual compilers, primitive
crosswalks, normal forms, proof certificates, ledgers, checkpoints, APIs,
and decisions.

Create:

```text
docs/next_level/c91_isolation_report.json
docs/next_level/c91_regression_report.json
```

---

# 22. Readiness and continuation decisions

Select exactly one branch.

## 22.1 Favorable branch

Issue the exact positive status frozen by the C90 import contract, with
preferred semantic identity:

```text
C91_C82_HISTORICAL_DESCENDANT_FACTORIZED_SEMANTIC_EQUIVALENCE_READY
```

Required:

```text
historical C90 authority verifies;

the descendant scientific environment freezes;

the descendant compiler is independent;

all primitive scientific tables are crosswalked completely;

the color authority is scientifically reconciled;

all 154,830 supported pairs match by identity and order;

all 154,830 descendant normal forms are constructed;

all pair normal forms, primitive roots, counts, orderings,
expressions, bounds, statuses, ancestry, summaries, and roots
match under approved adapters;

the C90 expansion theorem validates every matched pair;

all scientific mismatch counts are zero;

no unresolved difference remains;

the equivalence decision is positive;

two clean, restart, serial, and parallel comparison passes agree;

the immutable API passes;

the C92 import contract is complete;

the mutation suite passes.
```

Next:

> **C92/IFPERSIST3 — authenticated upstream-free canonical factorized pair-coordinate coefficient package**

## 22.2 Descendant compiler incomplete

```text
C91_IFEQUIV6_DESCENDANT_COMPILER_INCOMPLETE
```

Next:

> **C92/IFDESCCOMP — repair only the specifically identified current C77/C78/C80/C82 semantic compiler, field map, ordering, or primitive-reference defect**

## 22.3 Primitive authority unresolved

```text
C91_IFEQUIV6_PRIMITIVE_AUTHORITY_INCOMPLETE
```

Next:

> **C92/IFPRIMX — repair only the specifically identified primitive table, color-authority crosswalk, adapter, record comparison, or root-equivalence defect**

## 22.4 Scientific payload mismatch

```text
C91_IFEQUIV6_SCIENTIFIC_PAYLOAD_MISMATCH
```

Next:

> **C92/IFSCIDIFF2 — resolve only the specifically identified pair, program node, primitive, count, ordering, coefficient, bound, status, or ancestry difference**

## 22.5 Expansion-proof application incomplete

```text
C91_IFEQUIV6_EXPANSION_PROOF_INCOMPLETE
```

Next:

> **C92/IFPROOF2 — repair only the specifically identified proof-certificate, primitive adapter, node semantics, or theorem-application defect**

## 22.6 Instance/science classification unresolved

```text
C91_IFEQUIV6_INSTANCE_DIFFERENCE_UNRESOLVED
```

Next:

> **C92/IFINSTDIFF — localize only the unresolved runtime, API, path, serializer, or scientific-field classification**

## 22.7 Public equivalence API incomplete

```text
C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
```

Next:

> **C92/IFEQUIVAPI — repair only the compact ledger, proof-root verification, pair query, diagnostic materializer, immutability, or safe-loading defect**

Do not issue final-public-persisted-import, coefficient-times-kernel,
contact-matrix, complete-instantaneous-fermion, counterterm-solved,
local-QCD, TMD, matching, inference, or production status.

---

# 23. Essential deliverables

Create at least:

```text
docs/next_level/c91_implementation_report.md
docs/next_level/c91_derivation_authority_manifest.json
docs/next_level/c91_input_fidelity_audit.json
docs/next_level/c91_historical_input_freeze.json
docs/next_level/c91_descendant_input_freeze.json

docs/next_level/c91_descendant_semantic_compiler.json
docs/next_level/c91_descendant_semantic_compiler_validation.json
docs/next_level/c91_primitive_root_crosswalk.json
docs/next_level/c91_primitive_record_equivalence.json
docs/next_level/c91_color_authority_crosswalk.json
docs/next_level/c91_supported_pair_crosswalk.json

docs/next_level/c91_descendant_pair_program_manifest.json
docs/next_level/c91_descendant_pair_program_validation.json
docs/next_level/c91_exhaustive_pair_semantic_comparison.json
docs/next_level/c91_expansion_equivalence_application.json
docs/next_level/c91_equivalence_certificate_manifest.json

docs/next_level/c91_mismatch_localization_contract.json
docs/next_level/c91_mismatch_diagnostic_report.json
docs/next_level/c91_difference_classification.json
docs/next_level/c91_instance_science_separation_report.md

docs/next_level/c91_descendant_semantic_root_manifest.json
docs/next_level/c91_historical_descendant_equivalence_root.json
docs/next_level/c91_scientific_equivalence_decision.json

docs/next_level/c91_restart_contract.json
docs/next_level/c91_restart_validation.json
docs/next_level/c91_two_clean_pass_determinism.json
docs/next_level/c91_parallel_comparison_report.json
docs/next_level/c91_resource_and_scaling_report.json

docs/next_level/c91_api_contract.json
docs/next_level/c91_api_validation.json
docs/next_level/c91_runtime_inventory.json
docs/next_level/c91_deterministic_reconstruction_report.json
docs/next_level/c91_c92_ifpersist3_import_contract.json

docs/next_level/c91_isolation_report.json
docs/next_level/c91_readiness_report.json
docs/next_level/c91_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifequiv6/
```

Use only compact runtime payloads under:

```text
data/runtime/c91_ifequiv6/
```

No expanded full-record stream may be written.

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 24. Acceptance criteria

C91 is complete only when:

1. Baseline `ac622ab358b83f090717d7e7fa179b58f18f526d` reproduces.
2. Both protected untracked paths remain untouched.
3. C77/C78/C80/C82/C87/C88/C89/C90 historical artifacts remain unchanged.
4. `c90_c91_ifequiv6_import_contract.json` is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. The historical C90 attestation verifies through its public API.
7. The current descendant scientific environment is frozen explicitly.
8. The descendant compiler is independent of historical expected outputs.
9. Every primitive scientific family is crosswalked.
10. Primitive equivalence is record-complete or theorem-proved, not count-only.
11. The historical and descendant color sciences are compared completely.
12. All 154,830 supported pair identities are present in both routes.
13. No extra or missing pair exists.
14. Pair ordering agrees exactly.
15. Every descendant pair compiles into the unchanged C90 semantic IR.
16. All 154,830 descendant pair normal forms are constructed.
17. Historical and descendant normal forms compare exhaustively.
18. Logical counts reproduce 891,992,018 records.
19. All record/coefficient/bound/status/ancestry expressions agree.
20. Every matched pair receives an expansion-equivalence proof certificate.
21. All scientific mismatch counts are zero in the favorable branch.
22. Every instance-only difference is field-level justified.
23. No unresolved difference remains in the favorable branch.
24. Mismatch diagnostics are bounded and operational through mutation tests.
25. The scientific equivalence decision is explicit.
26. Two clean passes agree.
27. Interrupted/resumed execution agrees.
28. Serial and deterministic parallel comparison agree.
29. The public API returns immutable verified objects.
30. A pair query does not compare the full domain again.
31. The C92 canonical-persistence contract is complete.
32. At least 384 focused live mutations pass.
33. No expanded full-record stream is written.
34. The C90/C91 roots are labeled factorized semantic and equivalence roots, not expanded stream hashes.
35. No final public C82 persisted coefficient package is claimed.
36. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
37. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
38. The working tree is clean except for the two protected untracked paths.
39. A local completion commit is created and not pushed.

Do not weaken primitive coverage, supported-pair identity, normal-form
comparison, theorem application, scientific/instance classification, or
bounded mismatch diagnostics to obtain a positive equivalence decision.

---

# 25. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C90-to-C91 contract identity;
- historical C82 commit and environment qualification;
- full C90 historical semantic root;
- descendant source/scientific roots;
- semantic-IR, normal-form, and proof-checker hashes;
- primitive-table crosswalk counts and statuses;
- historical/descendant color-authority relation;
- supported-pair counts and ordering result;
- descendant pair-program count;
- mismatch counts for every semantic category;
- proof-certificate count and aggregate root;
- logical record counts by resolution and total;
- historical and descendant resolution/aggregate roots;
- scientific versus instance-only difference counts;
- mismatch diagnostic results;
- final scientific equivalence decision;
- two-clean-pass equality;
- interrupted/resumed equality;
- serial/parallel equality;
- compact runtime size and peak memory;
- public API result;
- C92 import-contract result;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime-instance recovery remains unclaimed;
- confirmation that roots are factorized-semantic/equivalence roots, not expanded serialized-record hashes;
- confirmation that no expanded full-record stream, final public persisted coefficient package, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
