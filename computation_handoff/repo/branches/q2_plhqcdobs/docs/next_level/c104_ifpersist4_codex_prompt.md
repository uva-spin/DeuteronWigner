# C104/IFPERSIST4 Codex Work Package

## Title

**Authenticated upstream-free canonical factorized C82 coefficient authority: compact persistence of the C103-proven descendant object, exact pair-local rank/unrank reconstruction across 891,992,018 logical records, immutable public record APIs, strict coefficient/kernel ownership separation, and C105 direct-contact assembly handoff**

## Authoritative baseline

Start from the clean local C103 completion commit:

```text
45307763a1b135e95c554681015206ac2d9a6035
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "45307763a1b135e95c554681015206ac2d9a6035"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, consume, or use either protected path as scientific authority.

Read completely and execute the sole committed C103-to-C104 import contract:

```text
docs/next_level/c103_c104_ifpersist4_import_contract.json
```

If the sole tracked C104 import contract has a different exact filename or extension, discover it, record its path and SHA-256, and do not create or use a competing contract.

If the import contract freezes exact status names, schemas, public signatures, package names, factor-ownership names, or a narrower boundary, preserve them. This work package supplies canonical persistence and on-demand reconstruction only. It does not authorize C80-kernel evaluation, coefficient-times-kernel multiplication, or contact-matrix assembly.

Create one local completion commit. Do not push.

---

# 1. Preserve the positive C103 equivalence result

Preserve unchanged:

```text
C103_C82_HISTORICAL_DESCENDANT_FACTORIZED_SEMANTIC_EQUIVALENCE_READY
```

C103 roots:

```text
C103 package root
f878c247ce548cd3bd045afe93224a3c5fef305ebc48b5a432a46196907a54d

C103 descendant semantic root
a551004d171c430d262fac64c095807d4cfe78aa0be6290ce2b8cb6a177162dd

C103 historical-descendant equivalence-certificate root
ba020671f51c3072f4086e15a474ba4ccd1efc12a598531a454dcdbf65e260f0
```

Preserve the C103 scientific result:

```text
supported pairs = 154,830
C102 positive proofs = 154,830
proof failures = 0
proof unresolved = 0
all scientific mismatch categories = 0

primitive families = 5
primitive records = 35
primitive scientific mismatches = 0

logical records = 891,992,018

K9:
    pairs = 16,224
    logical records = 28,606,464

K11:
    pairs = 43,350
    logical records = 165,991,250

K13:
    pairs = 95,256
    logical records = 697,394,304

final scientific decision:
    use the exact C103 decision from the committed report

approved differences:
    instance-only metadata adapter(s) frozen and verified by C103
```

Do not rerun the historical-versus-descendant comparison merely to create C104. Verify the C103 package, pair certificates, primitive certificates, descendant roots, and equivalence roots read-only.

Retain all C90-C102 historical authorities and roots unchanged.

Retain these claim boundaries:

```text
exact_historical_C72_runtime_instance =
    UNKNOWN_NOT_CLAIMED

C93_payload_relation =
    RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY

C97_operand_relation =
    DESCENDANT_RECOVERED_CANONICAL_OPERAND_RECORD_FOR_FROZEN_C90_PROOF

C103_equivalence_relation =
    FACTORIZED_SEMANTIC_EQUIVALENCE_CERTIFIED_FOR_ALL_SUPPORTED_PAIRS
```

---

# 2. Scientific classification

C104 is an **upstream-free canonical persistence package** for the coefficient side of the C82 direct-contact construction.

It is not:

```text
a new historical recovery;
a new descendant compilation;
a new equivalence proof;
a new primitive model;
a C80 kernel evaluation;
a coefficient-times-kernel multiplication;
a direct-contact matrix;
a complete instantaneous-fermion operator;
or a downstream TMD/matching/inference calculation.
```

C104 must persist the C103-proven canonical scientific object in a form that can reconstruct any supported C82 logical record on demand without calling historical or current scientific builders.

The central ownership boundary is immutable:

```text
C104 / canonical C82 coefficient authority owns:
    pair identity and order;
    witness/leaf identity;
    projected embedding coefficient;
    bra conjugation;
    coefficient expression/value;
    coefficient bound;
    terminal status;
    witness multiplicity;
    factor ownership;
    reversible ancestry;
    canonical C80 coordinate identity;
    C103 equivalence certificate.

C80 owns:
    W3 kernel factors;
    direct-contact kernel evaluation;
    explicit factored g_s^2;
    P-minus to M-squared kernel conversion where defined;
    kernel numerical/error bounds.

C104 must not multiply these two ownership domains.
```

No external source is required to persist the already proven project-owned canonical semantics.

---

# 3. Exact purpose and stopping point

C104 must produce:

```text
a frozen canonical-source decision derived from C103;

a complete compact 154,830-pair canonical program ledger;

a compact primitive/expression/schema authority sufficient for
upstream-free record reconstruction;

a deterministic mixed-radix rank/unrank implementation for every pair;

an immutable on-demand API returning one complete canonical C82
scientific record for any supported pair and pair-local ordinal;

bounded pagination over records without materializing the full domain;

complete pair, primitive, expression, ownership, ancestry, and
equivalence-certificate authentication;

exhaustive pair-program/root validation against C103;

factorized proof that the package represents exactly 891,992,018
ordered logical records;

bounded record-level holdouts and rank/unrank closure tests;

strict no-builder/no-kernel/no-private-fallback behavior;

deterministic, restartable, serial/sharded package construction;

at least 384 focused live mutations;

and exactly one C105 direct-contact-assembly import contract.
```

C104 must not:

```text
materialize all 891,992,018 records;

evaluate a C80 kernel value;

read a C80 kernel cache as a scientific coefficient input;

multiply c_kappa by K_kappa;

aggregate pair records into sparse or dense matrix entries;

construct a matrix-free contact action;

select a physical g_s or alpha_s;

solve or apply a counterterm coefficient;

combine the C58 self-induced-inertia contribution;

claim the complete instantaneous-fermion operator;

or create any TMD, matching, fit, inference, process, or production object.
```

The preferred positive status is:

```text
C104_C82_UPSTREAM_FREE_CANONICAL_FACTORIZED_COEFFICIENT_PACKAGE_READY
```

If the committed import contract freezes another exact status, use that exact status and record the preferred name only as a semantic alias.

The preferred favorable continuation is:

> **C105/IFCONTACT5 — authenticated coefficient-times-C80-kernel aggregation and direct qg contact matrix/action assembly with explicit g_s^2 factored**

---

# 4. Mandatory C103 inputs

Read and verify the committed C103 public package and reports.

Freeze:

```text
C103 package root;
C103 descendant semantic root;
C103 equivalence-certificate root;
all resolution roots;
all 154,830 pair identities and order records;
all descendant pair-program roots;
all pair equivalence-certificate roots;
all primitive-equivalence certificate roots;
all five primitive-family roots and 35 canonical primitive records;
the approved instance-adapter registry;
C88 scientific-record schema and canonical order;
C90 semantic-IR and normal-form identities;
C80 coordinate schema and identity contract only;
C82 coefficient/factor-ownership schema;
C103 public API fingerprints.
```

Create:

```text
docs/next_level/c104_derivation_authority_manifest.json
docs/next_level/c104_input_fidelity_audit.json
docs/next_level/c104_canonical_input_freeze.json
```

Issue:

```text
C104_C103_CANONICAL_INPUT_AUTHORITY_FROZEN_COMPLETE
```

No C103 scientific root may be altered.

---

# 5. Select the canonical scientific representation

Select exactly one canonical representation for persistence.

The preferred selection is:

```text
CANONICAL_CURRENT_DESCENDANT_C82_REPRESENTATION
```

because C103 independently froze the current descendant before historical access and proved it scientifically equivalent to the historical object.

If C103’s committed contract selects another canonical side or canonicalized adapter representation, preserve that decision.

Record:

```text
selected canonical representation;
source pair-program roots;
primitive-record roots;
adapter registry root;
equivalence-certificate root;
reason the selected instance is scientifically authoritative;
fields that are instance-only and excluded from canonical scientific identity.
```

Create:

```text
docs/next_level/c104_canonical_representation_decision.json
docs/next_level/c104_instance_metadata_exclusion_contract.json
```

Do not average, merge, or combine historical and descendant values. Equivalence is a proof relation, not an additive model.

---

# 6. Define the complete canonical C82 logical-record schema

Define one immutable canonical record equivalent to:

```python
CanonicalC82CoefficientRecord
```

It must include the actual repository equivalents of:

## Pair and ordering identity

```text
resolution ID;
global pair sequence;
resolution-local pair sequence;
canonical pair ID;
physical qg bra identity;
physical qg ket identity;
physical retained-q intermediate identity where applicable;
row/column or bra/ket indices;
support status;
pair logical cardinality;
pair-local ordinal;
canonical global logical ordinal or a reversible global locator;
mixed-radix digits and radix identities;
canonical record ID.
```

## Witness and coordinate identity

```text
ordered witness identity;
endpoint-path identities;
raw source-leaf identities;
canonical C80 raw-coordinate identity;
C80 coordinate-schema version;
generator/color order;
spin/helicity labels;
longitudinal mode labels;
four-HO mode labels;
resolution and basis identities.
```

## Coefficient-side science

```text
projected coefficient exact expression or canonical value;
bra-conjugation identity;
coefficient real/imaginary representation;
certified coefficient bound or interval;
terminal coefficient status;
witness multiplicity;
factor-ownership identity;
zero/nonzero/exact/interval classification;
record scientific digest.
```

## Provenance and proof

```text
complete reversible ancestry;
primitive-record references;
expression references;
C103 descendant pair-program root;
C103 primitive-equivalence certificate root;
C103 pair-equivalence certificate root;
C103 aggregate equivalence-certificate root;
C103 descendant semantic root;
C104 package and schema roots.
```

## Explicit exclusions

The record must not contain:

```text
C80 kernel value;
C80 kernel numerical error;
g_s^2 value;
coefficient-times-kernel product;
matrix row contribution;
aggregated matrix entry;
counterterm value;
C58 contribution;
physical coupling choice.
```

Create:

```text
docs/next_level/c104_canonical_record_schema.json
docs/next_level/c104_canonical_record_schema_validation.json
docs/next_level/c104_factor_ownership_contract.json
```

---

# 7. Compact upstream-free package design

Create a compact package under:

```text
data/runtime/c104_ifpersist4/
```

It may contain:

```text
pair-order ledger;
pair factorized-program records;
primitive dictionary;
typed expression DAG/library;
mixed-radix axis definitions;
coordinate-constructor schema;
coefficient/bound/status/multiplicity/ancestry rules;
compact record-ID index;
pair-equivalence certificate references;
safe-loading and no-recomputation policies;
complete file inventory.
```

It must not contain:

```text
expanded per-record JSONL for all 891,992,018 records;
C80 kernel arrays or values;
C80 numerical caches;
contact matrix entries;
duplicated historical C93/C97 payloads;
private builder state.
```

Construct and distinguish:

```text
C104_CANONICAL_PRIMITIVE_ROOT

C104_CANONICAL_PAIR_PROGRAM_ROOT

C104_CANONICAL_LOGICAL_DOMAIN_ROOT

C104_PACKAGE_ROOT
```

The logical-domain root is a factorized semantic root, not an expanded record-stream hash.

Create:

```text
docs/next_level/c104_package_schema_contract.json
docs/next_level/c104_package_root_manifest.json
docs/next_level/c104_runtime_inventory.json
docs/next_level/c104_root_semantics.json
docs/next_level/c104_no_expanded_stream_declaration.json
```

---

# 8. Upstream-free reconstruction compiler

Implement under:

```text
src/deuteron_wigner/bridge/ifpersist4/
```

or the exact contract-frozen equivalent.

The runtime reconstruction engine may read only the persisted C104 package and its authenticated public metadata.

It must not call:

```text
C77 embedding constructors;
C78 support/witness constructors;
C80 kernel evaluator;
C82 bridge constructors;
C89/C90 historical compilers;
C93/C97 recovery;
C98/C100 historical retrieval;
C102 theorem checker for routine record reconstruction;
C103 descendant compiler or full comparison;
build-if-missing;
repair-if-missing;
network access.
```

Provide an exact deterministic evaluator that reconstructs a record from:

```text
pair factorized program;
pair-local ordinal;
mixed-radix rank/unrank rules;
primitive records;
expression rules;
coordinate constructor;
ownership/ancestry rules.
```

No full-pair leaf list may be constructed merely to answer one ordinal query.

Create:

```text
docs/next_level/c104_upstream_free_reconstruction_contract.json
docs/next_level/c104_upstream_free_reconstruction_validation.json
docs/next_level/c104_no_recomputation_report.json
```

---

# 9. Exact rank/unrank and ordinal semantics

Implement operations equivalent to:

```python
pair_logical_count(pair_id, resolution)

unrank_pair_ordinal(pair_id, resolution, ordinal)

rank_pair_record_identity(pair_id, resolution, record_identity)

canonical_record_id(pair_id, resolution, ordinal)
```

Require for every pair:

```text
ordinal domain = [0, logical_count);

rank(unrank(i)) = i for all algebraically represented i;

unrank(rank(record_identity)) = canonical record identity;

first and last ordinals reproduce frozen identities;

mixed-radix carry rules are exact;

no ordinal aliases;

no omitted ordinal;

no out-of-range acceptance.
```

Do not iterate every ordinal when an exact mixed-radix proof closes the domain.

Create:

```text
docs/next_level/c104_rank_unrank_contract.json
docs/next_level/c104_rank_unrank_validation.json
docs/next_level/c104_ordinal_identity_manifest.json
```

---

# 10. Exhaustive pair-program persistence validation

For all 154,830 pairs:

```text
load the C104 persisted pair program;
compare identity and order with C103;
compare logical cardinality;
compare primitive dependencies;
compare expression roots;
compare mixed-radix/order identity;
compare coefficient/bound/status/multiplicity/ancestry rules;
compare first/last identities;
compare summary;
compare canonical pair-program root;
verify the C103 pair-equivalence certificate reference.
```

Require:

```text
pairs persisted = 154,830
missing = 0
extra = 0
duplicates = 0
pair-order mismatches = 0
program-root mismatches = 0
primitive-dependency mismatches = 0
logical-count mismatches = 0
order mismatches = 0
expression/rule mismatches = 0
equivalence-certificate-reference failures = 0
```

Create:

```text
docs/next_level/c104_exhaustive_pair_program_persistence_validation.json
```

---

# 11. Primitive and expression closure

Persist or reference compact canonical records for every required primitive and expression family.

At minimum require:

```text
five C103-approved primitive families;
35 canonical primitive records;
all expression nodes reachable from the 154,830 pair programs;
all coordinate-constructor primitives;
all coefficient, conjugation, bound, status, multiplicity, ownership,
and ancestry rules.
```

Require:

```text
missing primitive = 0
extra primitive in declared canonical domain = 0
duplicate primitive ID = 0
primitive scientific mismatch = 0
unreachable required expression = 0
unknown expression node = 0
expression-cycle defect = 0
```

Create:

```text
docs/next_level/c104_primitive_expression_inventory.json
docs/next_level/c104_primitive_expression_validation.json
```

---

# 12. On-demand canonical record API

Expose immutable public operations equivalent to:

```python
load_verified_canonical_c82_coefficient_authority()

verify_canonical_c82_coefficient_authority()

supported_pair_count(resolution=None)

logical_record_count(resolution=None)

pair_attestation(pair_id, resolution)

pair_factorized_program(pair_id, resolution)

pair_equivalence_certificate(pair_id, resolution)

canonical_record(
    pair_id,
    resolution,
    ordinal,
)

canonical_record_by_id(record_id)

canonical_record_page(
    pair_id,
    resolution,
    *,
    cursor=None,
    limit=...,
)

rank_record_identity(
    pair_id,
    resolution,
    record_identity,
)
```

One-record lookup must not:

```text
scan all pairs;
expand the complete pair;
call any upstream builder;
call C80;
rerun C103 equivalence.
```

Every returned object must be immutable and expose relevant C103/C104 roots.

Create:

```text
docs/next_level/c104_api_contract.json
docs/next_level/c104_api_validation.json
```

---

# 13. Bounded pagination and cursors

The record-page API must paginate one pair’s logical domain without pre-expanding it.

Cursors must bind:

```text
C104 package root;
pair ID and resolution;
next pair-local ordinal;
page limit;
schema/API version;
canonical order identity.
```

Require:

```text
no gaps;
no overlaps;
no duplicates;
explicit terminal page;
stable traversal at multiple allowed limits;
same record identities independent of page size;
cursor corruption rejection;
cross-pair and cross-root cursor rejection.
```

Do not traverse all 891,992,018 records as an acceptance requirement.

Create:

```text
docs/next_level/c104_pagination_contract.json
docs/next_level/c104_pagination_validation.json
```

---

# 14. Record-level validation without global expansion

Use a deterministic bounded validation set derived before evaluating record values.

For every pair validate at least:

```text
ordinal 0;
ordinal logical_count - 1;
every distinct mixed-radix carry-boundary class where algebraically unique;
every status-transition class;
every coefficient/bound/multiplicity/ancestry branch class;
root-seeded deterministic ordinal holdouts subject to a global resource cap.
```

Also include intensive validation for:

```text
smallest and largest pairs;
first/middle/final pair of each resolution;
largest-cardinality pair;
primitive-family boundaries;
coordinate-schema boundaries;
zero/nonzero and exact/interval status boundaries.
```

Compare the reconstructed C104 record with the canonical current-descendant C82 route in an isolated build-time validation process and, where practical, with the equivalent historical record implied by the C103 proof.

The production runtime must not depend on either route.

Require zero mismatch in:

```text
record identity;
coordinate identity;
projected coefficient;
conjugation;
bound;
status;
multiplicity;
ownership;
ancestry;
equivalence-certificate reference.
```

Create:

```text
docs/next_level/c104_record_holdout_plan.json
docs/next_level/c104_record_reconstruction_validation.json
```

Do not claim all 891,992,018 records were directly materialized.

---

# 15. Factorized logical-domain proof

Use the C103-proven pair programs and C104 rank/unrank implementation to prove:

```text
K9:
    pairs = 16,224
    logical records = 28,606,464

K11:
    pairs = 43,350
    logical records = 165,991,250

K13:
    pairs = 95,256
    logical records = 697,394,304

total:
    pairs = 154,830
    logical records = 891,992,018
```

Construct one factorized domain certificate per resolution and one aggregate certificate.

Label these:

```text
C104_FACTORIZED_LOGICAL_DOMAIN_CERTIFICATE_ROOT
```

They are not expanded stream hashes.

Create:

```text
docs/next_level/c104_logical_domain_certificate.json
docs/next_level/c104_logical_census_closure.json
```

---

# 16. C80 coordinate identity boundary

C104 must preserve the exact canonical C80 coordinate identity required for later kernel evaluation.

For every reconstructed record validate:

```text
coordinate schema version;
longitudinal mode identities;
spin/polarization labels;
ordered generator/color labels;
four-HO mode labels;
resolution/basis labels;
raw coordinate orientation;
all exact integer/rational fields;
coordinate canonical digest.
```

Do not call the C80 evaluator.

Poison the C80 numerical evaluator and all kernel-value files during C104 build and runtime tests.

Create:

```text
docs/next_level/c104_c80_coordinate_identity_contract.json
docs/next_level/c104_c80_coordinate_identity_validation.json
docs/next_level/c104_kernel_nonconsumption_report.json
```

Require:

```text
C80 numerical evaluator calls = 0
kernel values loaded = 0
kernel products formed = 0
```

---

# 17. Coefficient and factor-ownership integrity

For every expression/rule family prove:

```text
projected coefficient belongs to C104/C82 coefficient authority;

C80 kernel factors are external references only;

g_s^2 remains explicitly factored and absent from coefficient identity
unless the inherited coefficient schema explicitly contains a symbolic
ownership marker rather than a numerical value;

bra conjugation is applied exactly once;

witness multiplicity is not absorbed into the kernel;

bounds remain coefficient-side bounds and are not relabeled as product
bounds;

C58 self-induced-inertia remains separate;

no counterterm value is introduced.
```

Create:

```text
docs/next_level/c104_coefficient_ownership_validation.json
docs/next_level/c104_no_double_counting_ledger.json
```

---

# 18. Safe immutable loading

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payload;
unsafe paths or unexpected symlinks;
unindexed files;
unknown primitive/expression/rule types;
unknown schema or coordinate version;
shape, width, dtype, or endianness mismatch;
writable arrays;
mutable returned records;
root mismatch;
hidden build-if-missing or repair behavior.
```

Create:

```text
docs/next_level/c104_safe_loading_contract.json
docs/next_level/c104_safe_loading_validation.json
```

---

# 19. Determinism, restart, serial, and sharded construction

Run:

```text
two complete clean C104 package builds;

one serial build;

one deterministic pair-range sharded build with canonical merge order;

one controlled interrupted/resumed build;

two complete public pair-ledger traversals in different query orders;

record-page traversals with multiple permitted limits.
```

All gzip output must use deterministic headers:

```text
mtime = 0;
stable or omitted embedded filename;
fixed compression level;
canonical file and record order.
```

Require byte-identical or canonically identical:

```text
primitive and expression records;
pair program ledger;
rank/unrank schemas;
factorized domain certificates;
all package files;
all C104 roots;
public query results;
record holdouts.
```

Use checkpoints bound to:

```text
C103 roots;
canonical representation decision;
primitive/expression roots;
schema and API fingerprints;
next pair sequence;
closed shard hashes;
rolling pair-program root;
package-builder fingerprint.
```

Create:

```text
docs/next_level/c104_restart_contract.json
docs/next_level/c104_restart_validation.json
docs/next_level/c104_two_clean_build_determinism.json
docs/next_level/c104_sharded_build_report.json
docs/next_level/c104_resource_and_scaling_report.json
```

---

# 20. Isolation and mutation controls

Execute at least **384 focused live mutations** across actual:

```text
C103 package, descendant, and equivalence roots;
canonical-representation decision;
pair identities and ordering;
pair logical counts;
radices and mixed-radix digits;
first/last identities;
primitive records;
expression nodes;
coordinate identities;
projected coefficients;
conjugation rules;
bounds;
statuses;
multiplicities;
ownership records;
ancestry;
equivalence-certificate references;
record IDs;
pagination cursors;
safe-loader policies;
checkpoints;
C105 import-contract fields.
```

Required failures include mutation of every scientific field.

Required invariance includes:

```text
temporary path;
query order;
page size;
shard layout;
process ID;
wall-clock timestamp;
documentation path;
excluded instance-only metadata.
```

Poison and verify independence from:

```text
C80 numerical kernel values and evaluator;
C53 propagation values;
C58 contribution values;
physical coupling;
counterterm coefficients;
contact-matrix APIs;
downstream TMD/matching/inference/production APIs.
```

Create:

```text
docs/next_level/c104_isolation_report.json
docs/next_level/c104_regression_report.json
```

---

# 21. C103 consumption and supersession boundary

Create an explicit consumption record:

```text
C103 positive scientific equivalence
    -> canonical current-descendant C82 representation selected for C104
       persistence;
       historical equivalence certificates retained per pair;
       no scientific comparison reopened.
```

State explicitly:

```text
C104 does not improve or reinterpret the C103 equivalence result;

C104 creates no new historical/current scientific values;

C104 package roots are persistence roots, not new equivalence roots.
```

Create:

```text
docs/next_level/c104_c103_consumption_report.json
```

---

# 22. C105 direct-contact assembly import contract

On favorable closure create the sole next contract:

```text
docs/next_level/c104_c105_ifcontact5_import_contract.json
```

C105 must consume:

```text
C104 canonical coefficient authority;
C104 record and pagination APIs;
C104 C80-coordinate identities;
C104 factor-ownership contract;
C80 authenticated kernel evaluator and kernel bounds;
C103 pair equivalence certificates;
C58 self-induced-inertia status as a separate contribution;
the frozen physical qg basis and pair order.
```

C105 must:

```text
evaluate C80 kernels only after C104 coefficients are frozen;

form coefficient-times-kernel products with explicit g_s^2 factored;

aggregate source-ordered witness contributions into sparse and
matrix-free qg direct-contact actions;

derive certified product and aggregation bounds;

validate Hermiticity from source ordering rather than post-hoc averaging;

keep C58 self-induced inertia separate before an explicit later assembly
decision;

create no physical coupling value or counterterm unless separately
authorized.
```

C105 must not silently combine C58, C53 propagation, or counterterms.

---

# 23. Readiness branches

Select exactly one branch.

## Favorable

Issue the exact contract-frozen positive status, with preferred semantic identity:

```text
C104_C82_UPSTREAM_FREE_CANONICAL_FACTORIZED_COEFFICIENT_PACKAGE_READY
```

Required:

```text
C103 positive equivalence authority verifies;

one canonical representation is selected without changing science;

all five primitive families and 35 primitive records persist canonically;

all 154,830 pair programs persist and reproduce C103 roots;

rank/unrank closes exactly for every pair;

the factorized 891,992,018-record census closes;

on-demand canonical records reproduce all bounded holdouts;

every record carries complete coefficient, coordinate, ownership,
multiplicity, status, bound, ancestry, and equivalence identities;

runtime calls no upstream builder and no C80 evaluator;

no coefficient-times-kernel product exists;

safe-loading, determinism, restart, sharding, pagination, API, and
mutation gates pass;

the C105 import contract is complete.
```

Next:

> **C105/IFCONTACT5 — coefficient-times-C80-kernel aggregation and direct qg contact matrix/action assembly**

## Canonical representation unresolved

```text
C104_IFPERSIST4_CANONICAL_REPRESENTATION_UNRESOLVED
```

## Pair-program persistence incomplete

```text
C104_IFPERSIST4_PAIR_PROGRAM_INCOMPLETE
```

## Rank/unrank incomplete

```text
C104_IFPERSIST4_RANK_UNRANK_INCOMPLETE
```

## Record reconstruction mismatch

```text
C104_IFPERSIST4_RECORD_RECONSTRUCTION_MISMATCH
```

## Factor ownership incomplete

```text
C104_IFPERSIST4_FACTOR_OWNERSHIP_INCOMPLETE
```

## Public package incomplete

```text
C104_IFPERSIST4_PUBLIC_PACKAGE_INCOMPLETE
```

Use only the exact branch authorized by the import contract. Localize the precise object. Do not evaluate the C80 kernel to bypass a coefficient-package defect.

Do not issue contact-matrix, complete-instantaneous-fermion, TMD, matching, inference, or production status.

---

# 24. Essential deliverables

Create at least:

```text
docs/next_level/c104_implementation_report.md
docs/next_level/c104_derivation_authority_manifest.json
docs/next_level/c104_input_fidelity_audit.json
docs/next_level/c104_canonical_input_freeze.json

docs/next_level/c104_canonical_representation_decision.json
docs/next_level/c104_instance_metadata_exclusion_contract.json

docs/next_level/c104_canonical_record_schema.json
docs/next_level/c104_canonical_record_schema_validation.json
docs/next_level/c104_factor_ownership_contract.json

docs/next_level/c104_package_schema_contract.json
docs/next_level/c104_package_root_manifest.json
docs/next_level/c104_runtime_inventory.json
docs/next_level/c104_root_semantics.json
docs/next_level/c104_no_expanded_stream_declaration.json

docs/next_level/c104_upstream_free_reconstruction_contract.json
docs/next_level/c104_upstream_free_reconstruction_validation.json
docs/next_level/c104_no_recomputation_report.json

docs/next_level/c104_rank_unrank_contract.json
docs/next_level/c104_rank_unrank_validation.json
docs/next_level/c104_ordinal_identity_manifest.json

docs/next_level/c104_exhaustive_pair_program_persistence_validation.json
docs/next_level/c104_primitive_expression_inventory.json
docs/next_level/c104_primitive_expression_validation.json

docs/next_level/c104_api_contract.json
docs/next_level/c104_api_validation.json
docs/next_level/c104_pagination_contract.json
docs/next_level/c104_pagination_validation.json

docs/next_level/c104_record_holdout_plan.json
docs/next_level/c104_record_reconstruction_validation.json
docs/next_level/c104_logical_domain_certificate.json
docs/next_level/c104_logical_census_closure.json

docs/next_level/c104_c80_coordinate_identity_contract.json
docs/next_level/c104_c80_coordinate_identity_validation.json
docs/next_level/c104_kernel_nonconsumption_report.json
docs/next_level/c104_coefficient_ownership_validation.json
docs/next_level/c104_no_double_counting_ledger.json

docs/next_level/c104_safe_loading_contract.json
docs/next_level/c104_safe_loading_validation.json
docs/next_level/c104_restart_contract.json
docs/next_level/c104_restart_validation.json
docs/next_level/c104_two_clean_build_determinism.json
docs/next_level/c104_sharded_build_report.json
docs/next_level/c104_resource_and_scaling_report.json

docs/next_level/c104_c103_consumption_report.json
docs/next_level/c104_c105_ifcontact5_import_contract.json
docs/next_level/c104_isolation_report.json
docs/next_level/c104_readiness_report.json
docs/next_level/c104_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifpersist4/
```

Use compact runtime metadata under:

```text
data/runtime/c104_ifpersist4/
```

Create exactly one next-package contract corresponding to the selected branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not modify the protected C69 prompt.

---

# 25. Acceptance criteria

C104 is complete only when:

1. Baseline `45307763a1b135e95c554681015206ac2d9a6035` reproduces.
2. Both protected paths remain untouched and untracked.
3. C90-C103 tracked scientific artifacts remain unchanged.
4. The sole C103-to-C104 import contract is consumed and preserved.
5. C103 positive status and all three C103 roots verify.
6. The canonical scientific representation is selected explicitly.
7. Instance-only metadata is excluded from scientific identity through the C103-approved adapter contract.
8. All five primitive families and 35 primitive records are represented canonically.
9. Every required expression and rule is represented exactly once.
10. All 154,830 pair programs persist.
11. Pair order and pair roots reproduce C103.
12. Every pair logical count reproduces C103.
13. Rank/unrank closes for every pair.
14. The exact 891,992,018-record factorized census closes.
15. No expanded complete record stream is written.
16. On-demand reconstruction returns the complete canonical record schema.
17. Every record contains the canonical C80 coordinate identity.
18. Every record contains coefficient value/expression, bound, status, multiplicity, ownership, ancestry, and equivalence certificate.
19. No record contains a C80 kernel value or coefficient-times-kernel product.
20. C80 evaluator calls are zero.
21. Upstream scientific builder calls at runtime are zero.
22. One-record lookup does not scan the complete pair or global domain.
23. Pagination is gap-free, overlap-free, and root-bound.
24. All bounded record holdouts reproduce the canonical descendant route.
25. Safe immutable loading passes.
26. Two clean builds agree.
27. Serial and deterministic sharded builds agree.
28. Interrupted/resumed construction agrees.
29. Query-order and page-size invariance close.
30. At least 384 focused live mutations pass.
31. C103 equivalence certificates remain linked without being recomputed.
32. The C105 import contract is complete.
33. No C80 kernel value is evaluated as physical composition.
34. No coefficient-times-kernel product, contact matrix/action, coupling, counterterm, C53/C58 combination, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object is created.
35. `NO_JOINT_MEASURE`, 216 production routes, all 642 ART25 identities/covariance, and all authoritative artifacts remain unchanged.
36. The working tree is clean except for the two protected untracked paths.
37. One local completion commit is created and not pushed.

Do not weaken upstream freedom, rank/unrank exactness, record completeness, factor ownership, or kernel nonconsumption to obtain a positive result.

---

# 26. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed import-contract path and hash;
- complete C90-C103 authority roots;
- exact C103 package, descendant, and equivalence roots;
- canonical representation decision;
- C104 primitive, pair-program, logical-domain, and package roots;
- compact package path, file count, and bytes;
- primitive-family and record counts;
- expression/rule counts;
- pair counts and order results;
- rank/unrank validation counts;
- logical-record census by resolution and total;
- record-holdout counts and mismatch counts;
- C80 coordinate-identity validation;
- C80 evaluator call count;
- coefficient-times-kernel product count;
- upstream-builder call count;
- public API signatures;
- pagination and cursor results;
- safe-loading/no-recomputation results;
- deterministic/restart/sharded/query-order results;
- mutation and test results;
- C103 consumption result;
- C105 import-contract result;
- exact readiness/no-go status;
- exact next branch;
- confirmation that no expanded stream, kernel product, contact matrix, or downstream physics object was created;
- confirmation that nothing was pushed.
