# C138 external-input request

No numerical capsule is present. Supply exactly two authenticated
C135-compatible capsules: `M_R2_FB` (GeV², `C136_MASS_K9`) and
`g_R_FB(K_R)` (dimensionless, `C136_VERTEX_LONGITUDINAL`). Each must include
scheme, finite-resolution open-triplet domain, kinematics, provenance, claim
tier, uncertainty semantics, signature, and `no_default: true`. No numerical
placeholder is supplied.
