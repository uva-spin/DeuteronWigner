# C20/M1 validation API

`deuteron_wigner.matching.m1` adds immutable coefficient records, typed delta,
plus, and regular distributions, source/transcription provenance, synthetic
external constraint bundles, shared overconstrained matching, decomposed step
scaling, complete-block scheme conversion, audited small-b transport,
holdouts, and a component-resolved uncertainty ledger. It preserves the C19
540-entry identities and remains disconnected from production.
