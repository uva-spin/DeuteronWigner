# C84/IFCOORD2 Codex Work Package

## Title

**Authenticated persisted pair-coordinate import package for the C82 aggregation bridge: factorized materialization, runtime-root/index verification, immutable coefficient-record API, strict no-upstream-recomputation, and C85 contact-matrix preflight**

## Authoritative baseline

Start from the clean local C83 completion commit:

```text
d99af82754561bcf5efc3944ebdef72790d362bc
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "d99af82754561bcf5efc3944ebdef72790d362bc"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c84_ifcoord2_contract.md
```

If that contract freezes exact status names, schemas, APIs, or a narrower
scientific boundary, preserve them. This work package supplies the
executable materialization and verification plan; it does not authorize
weakening the committed contract.

The baseline is authoritative only when it contains and reproduces:

```text
C77_EXACT_SOURCE_CHAIN_DERIVED_QG_EMBEDDING_READY

C78_SOURCE_DERIVED_IFERM_CONTACT_SUPPORT_READY

C80_EXACT_SOURCE_CHAIN_DERIVED_IFCONTACT_KERNEL_EVALUATOR_READY

C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY

C83_IFCONTACT_SHARED_COORDINATE_INCOMPLETE
```

and the exact C83 finding:

```text
C80:
    its public runtime verifies correctly;

C82 scientific bridge:
    total deterministic reversible witness-leaf -> C80 coordinate
    construction;
    projected physical coefficients and bounds;
    exact factor ownership;
    zero unmapped or ambiguous leaves;

C82 public-import defect:
    the public constructor recomputes lazy bridge state from C77/C74;
    it does not authenticate and verify C82's own runtime root/index;
    it does not expose an authenticated persisted pair-coordinate
    record package that C83 can freeze before multiplication;

consequence:
    no legal immutable coefficient record can be frozen;
    no coefficient-times-kernel product or contact matrix is legal.
```

Verify every actual package root, index, schema, count, coefficient status,
bound, source/API fingerprint, and public operation from the repository.
This prompt is not numerical authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification of the blocker

C84 is an **artifact-persistence and public-import completion**, not a new
physics calculation.

External authority is not required for:

```text
persisting an already derived C82 coefficient record;
assigning deterministic shard and record IDs;
binding records to the C82 root and upstream roots;
constructing an authenticated index;
loading immutable records safely;
or exposing a public iterator/query API.
```

The C82 scientific result remains unchanged:

```text
the total coordinate map remains source-chain derived;
the projected coefficients remain owned by C82;
all W3 kernel factors and explicit g_s^2 remain owned by C80;
C78 structural support remains unchanged.
```

A fail-closed C84 result is valid only if:

```text
the C82 lazy output cannot be materialized deterministically;
the materialized records do not reproduce the C82 bridge;
a record cannot be bound to an authenticated pair/coordinate ID;
the runtime root/index cannot cover the complete logical domain;
or the public loader cannot return immutable verified records
without rebuilding upstream state.
```

Do not seek a paper that publishes the repository's pair-coordinate file
format or shard layout.

---

# 2. Descendant qualification and ownership

Do not alter historical C82 or C83 artifacts.

Create an explicit descendant relation:

```text
historical C82 status:
    C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY

descendant qualification:
    C82_AGGREGATION_SCIENCE_COMPLETE_PERSISTED_PUBLIC_IMPORT_INCOMPLETE

reason:
    C82's total lazy bridge and factor ownership are valid, but its
    consumer-facing constructor recomputes upstream state and does
    not verify an authenticated persisted pair-coordinate package.
```

Ownership is:

```text
C77/C74:
    immutable physical projection amplitudes and color authority;

C78:
    immutable structural support, endpoint, and witness ancestry;

C80:
    immutable raw-coordinate W3 kernel and explicit factored g_s^2;

C82:
    scientific coordinate bridge and projected-coefficient logic;

C84:
    authenticated persisted representation and public import of
    the unchanged C82 pair-coordinate coefficient records.
```

C84 must not alter a C82 coefficient, coordinate, status, bound, factor
owner, or ancestry relation.

Create:

```text
docs/next_level/c84_c82_descendant_qualification.json
```

---

# 3. Exact purpose and stopping point

C84 must materialize and authenticate a complete persisted representation
of the logical C82 record domain:

\[
\mathcal R_R
=
\left\{
(b,k,\xi,C_{bk}^{[\xi]},\delta C_{bk}^{[\xi]},
\text{status},\text{ancestry})
\right\}.
\]

For every logical pair-coordinate contribution, the persisted
representation must contain or reconstruct from authenticated local tables:

```text
resolution;
physical bra ID;
physical ket ID;
C78 supported-pair ID;
canonical C80 coordinate ID;
exact C80 coordinate-equivalence-class ID;
C82 projected-coefficient exact expression or numerical value;
certified coefficient bound;
terminal coefficient status;
factor-ownership identity;
witness count;
reversible C77/C78/C82 ancestry;
canonical pair-coordinate record ID.
```

C84 must produce:

```text
one authenticated persisted package root;
one complete package index;
deterministic factorized/sharded payloads;
a complete immutable public import API;
exhaustive equivalence to C82's lazy scientific route;
strict no-upstream-recomputation behavior;
and a C85 public-only matrix-assembly preflight.
```

C84 must not:

```text
query or multiply C80 kernel values as part of a matrix element;
form a coefficient-times-kernel product;
construct a P-minus or M-squared contact matrix;
construct a matrix-free contact action;
choose a physical coupling;
solve a counterterm;
add C53 or C58;
or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C84_SOURCE_DERIVED_IFCONTACT_PERSISTED_PAIR_COORDINATE_IMPORT_READY
```

If `c84_ifcoord2_contract.md` freezes a different exact status spelling,
preserve it and record a semantic alias.

The favorable continuation is:

> **C85/IFCONTACT5 — consume immutable C84 coefficient records and immutable C80 kernels to assemble the certified sparse and matrix-free bare direct-contact matrices**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C77:
    physical/raw basis identities;
    projection coefficients and bounds;
    basis order, metric, and conjugation conventions;

C78:
    all 154,830 supported pairs;
    endpoint and witness identities;
    structural-support statuses;
    factorized symbolic coordinate domains;

C80:
    canonical raw-coordinate schema and equivalence IDs;
    public coordinate validation and kernel API;
    runtime root and index;

C82:
    factor-ownership certificate;
    witness-leaf-to-C80 coordinate constructor;
    projected-coefficient constructor;
    pair-local grouping logic;
    reverse ancestry;
    runtime metadata/root/index;
    public constructor and API;
    deterministic reconstruction and mutation records;

C83:
    exact import audit;
    public-loader failure;
    c84_ifcoord2_contract.md.
```

Read C53 and C58 only for post-construction poisoning controls.

Create:

```text
docs/next_level/c84_derivation_authority_manifest.json
docs/next_level/c84_input_fidelity_audit.json
```

---

# 5. Freeze the scientific bridge before materialization

Use the authenticated C82 builder/scientific constructor only inside the
explicit C84 build and equivalence layer.

Freeze:

```text
C77/C74/C78/C80/C82 package and source identities;
all 154,830 supported-pair IDs;
all C82 coordinate-construction identities;
all pair-local coordinate-equivalence IDs;
all projected coefficient expressions/values/statuses/bounds;
all factor-ownership and reverse-ancestry identities;
all resolution and basis orders.
```

Issue:

```text
C84_SCIENTIFIC_INPUTS_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c84_input_freeze.json
```

The C84 builder may consume the frozen C82 lazy route to create the
snapshot. The C84 **public loader may not** call that route.

---

# 6. Complete logical-domain census

Before choosing a physical file layout, derive and freeze the actual
logical counts by resolution:

```text
supported physical pairs;
pair-coordinate contribution records;
unique C80 coordinate IDs;
unique projected-coefficient identities;
exact-expression records;
numerical-only coefficient records;
coefficient-status counts;
witness-ancestry links;
reverse-ancestry links.
```

The supported-pair total must remain:

```text
154,830
```

with the inherited resolution split taken from C78.

Do not assume that one supported pair has one coordinate record. Derive the
actual pair-coordinate multiplicity.

Create:

```text
docs/next_level/c84_logical_domain_census.json
docs/next_level/c84_logical_domain_validation.json
```

Require:

```text
no missing supported pair;
no duplicate pair-coordinate record;
no pair-coordinate record outside C78 support;
every supported pair has at least one terminal coefficient record;
every coefficient record has one canonical C80 coordinate.
```

---

# 7. Persisted representation requirements

C84 may select either:

```text
A. directly sharded pair-coordinate records; or

B. a factorized persisted representation consisting of immutable
   pair tables, coordinate tables, coefficient primitive tables,
   pair-to-coordinate spans, and ancestry tables.
```

A factorized representation is preferred when direct expansion would be
unreasonably large.

Regardless of representation, every logical record must be:

```text
fully recoverable without C77/C74/C78/C82 recomputation;
deterministically ordered;
content-addressed;
queryable by pair ID and record ID;
coupled to its coefficient value/expression, status, and bound;
coupled to its canonical C80 coordinate and equivalence ID;
coupled to reversible ancestry;
covered by the authenticated package root.
```

A metadata-only count/hash record is insufficient.

A decoder may expand authenticated local C84 tables lazily. This is
permitted only when:

```text
the decoder reads exclusively from verified C84 payloads;
the canonical logical record and pair digest are verified before
return;
and no upstream constructor or private runtime is called.
```

Create:

```text
docs/next_level/c84_persisted_representation_contract.json
docs/next_level/c84_persisted_representation_validation.json
```

---

# 8. Canonical pair-coordinate record schema

Define one frozen immutable record type, equivalent to:

```python
PersistedPairCoordinateContribution
```

with fields sufficient to preserve:

```text
schema version;
resolution ID;
pair ID;
physical bra/ket IDs;
row/column indices;
canonical C80 coordinate ID;
C80 equivalence-class ID;
C82 coefficient exact-record ID;
exact expression ID when available;
real and imaginary midpoint;
certified absolute bound;
precision and interval convention;
terminal coefficient status;
factor-ownership ID;
witness multiplicity;
ancestry span/digest;
record ID;
bound C77/C78/C80/C82 root identities.
```

The record must explicitly state:

```text
contains_no_C80_kernel_value = true;
contains_no_g_s_squared = true;
contains_no_coefficient_times_kernel_product = true.
```

Create:

```text
docs/next_level/c84_pair_coordinate_record_schema.json
docs/next_level/c84_pair_coordinate_record_validation.json
```

---

# 9. Deterministic materialization

Implement a dedicated builder that:

1. fully verifies all frozen upstream roots;
2. executes the C82 scientific route in deterministic pair order;
3. emits canonical pair-coordinate records or factorized local tables;
4. writes deterministic shards;
5. computes per-record, per-pair, per-shard, and aggregate digests;
6. closes the runtime package;
7. exits without making the public loader depend on the builder.

The builder must not:

```text
inspect C80 numerical kernel values;
merge coordinates by floating equality;
change a C82 coefficient after seeing storage size;
drop an interval-containing-zero record;
silently prune exact-zero coefficient records;
or create a matrix entry.
```

Create:

```text
docs/next_level/c84_materialization_plan.json
docs/next_level/c84_materialization_report.json
```

---

# 10. Pair and record digests

For every supported pair compute a canonical digest over the ordered
logical record sequence:

```text
pair ID;
ordered coordinate IDs;
ordered equivalence IDs;
coefficient exact/value identities;
bounds;
statuses;
factor-ownership IDs;
ancestry digests.
```

For every logical record compute a canonical record digest.

The public API must verify:

```text
the package root and index;
the requested shard;
the requested record digest;
the enclosing pair digest.
```

before returning the record.

Create:

```text
docs/next_level/c84_pair_digest_manifest.json
docs/next_level/c84_record_digest_validation.json
```

---

# 11. Reversible ancestry persistence

Persist enough information to reconstruct, for every pair-coordinate
record:

```text
the originating C78 pair;
the contributing C78 witness IDs;
the raw endpoint IDs;
the C77/C74 projection-amplitude IDs;
the C82 sign/conjugation/metric records;
the C80 coordinate-construction identity.
```

The ancestry may be stored as:

```text
direct record lists;
content-addressed ancestry groups;
or factorized span tables.
```

Require:

```text
forward record -> ancestry closure;
reverse witness -> pair-coordinate record closure;
no orphan ancestry;
no ambiguous ancestry;
no ancestry obtained by rerunning C82.
```

Create:

```text
docs/next_level/c84_ancestry_index_manifest.json
docs/next_level/c84_ancestry_reverse_closure_report.json
```

---

# 12. Authenticated runtime root and index

Use:

```text
data/runtime/c84_ifcoord2/
```

Create a canonical authenticated index, for example:

```text
index.json
index.sha256
root.json
```

following the repository's actual runtime convention.

The index/root must cover:

```text
schema and serializer versions;
bound C77/C74/C78/C80/C82 identities;
C84 loader-source fingerprint;
C84 public-API fingerprint;
logical-domain counts;
pair and record digest roots;
coordinate/equivalence domain hashes;
coefficient/status/bound domain hashes;
ancestry roots;
ordered shard inventory;
for each shard:
    path;
    role;
    pair and record range;
    size;
    SHA-256;
    shapes/counts;
    dtype/endianness;
    compression/encoding identity;
expected payload/control-file counts;
safe-loading and immutability policies.
```

The index must not authenticate itself circularly. Use an external
sidecar/root manifest under the repository's established convention.

Create:

```text
docs/next_level/c84_runtime_root_manifest.json
docs/next_level/c84_runtime_index_validation.json
docs/next_level/c84_runtime_inventory.json
```

---

# 13. Safe payload formats

All numerical payloads must be safe without pickle.

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payloads;
path traversal;
unexpected symlinks;
unindexed files;
shape, dtype, or endianness mismatch;
writable returned arrays.
```

String and exact-expression identities may be stored in canonical JSON,
fixed-width Unicode arrays, or another explicitly authenticated
non-pickle representation.

Create:

```text
docs/next_level/c84_safe_loading_contract.json
docs/next_level/c84_safe_loading_validation.json
```

---

# 14. Immutable public import API

Implement under:

```text
src/deuteron_wigner/bridge/ifcoord2/
```

or the exact repository-equivalent package.

Provide a frozen package loader equivalent to:

```python
load_persisted_ifcontact_pair_coordinate_package(
    resolution: str,
) -> PersistedPairCoordinatePackage
```

and public operations equivalent to:

```python
verify_complete_pair_coordinate_package(resolution)

pair_coordinate_contribution(record_id, resolution)

pair_coordinate_contributions(bra_id, ket_id, resolution)

iterate_pair_coordinate_contributions(
    resolution,
    pair_start=None,
    pair_stop=None,
    shard_id=None,
)

pair_coordinate_ancestry(record_id, resolution)

pair_coordinate_digest(bra_id, ket_id, resolution)
```

The public package must:

```text
verify the C84 root/index before returning data;
verify each accessed shard before returning its records;
verify record and pair digests;
return frozen records and non-writeable arrays;
expose all 154,830 supported pairs;
call no C77/C74/C78/C82 constructor or builder;
inspect no private upstream field;
perform no coefficient-times-kernel product.
```

Create:

```text
docs/next_level/c84_api_contract.json
docs/next_level/c84_api_validation.json
docs/next_level/c84_package_object_validation.json
```

---

# 15. Strict no-upstream-recomputation guard

The public import path must fail if it attempts to call:

```text
a C77 embedding builder;
a C74 color constructor;
a C78 support/witness builder;
the C82 witness-leaf constructor;
the C82 projected-coefficient constructor;
the C82 lazy package constructor;
a build-if-missing or repair-if-missing route.
```

The public loader may only decode and verify authenticated C84 payloads.

Poison all listed upstream constructors during public-loader tests.

Create:

```text
docs/next_level/c84_no_upstream_recomputation_report.json
```

---

# 16. Exhaustive C82 equivalence

During C84 validation—not public loading—compare the persisted C84 domain
against the frozen C82 lazy scientific route.

For every one of the 154,830 supported pairs compare the complete canonical
logical sequence:

```text
pair ID;
record count;
coordinate IDs;
equivalence IDs;
coefficient exact/value identities;
midpoints;
bounds;
statuses;
factor-ownership IDs;
witness counts;
ancestry digests;
pair digest.
```

Require:

```text
missing pairs = 0;
extra pairs = 0;
missing records = 0;
extra records = 0;
identity mismatches = 0;
coefficient mismatches = 0;
bound mismatches = 0;
status mismatches = 0;
ancestry mismatches = 0.
```

Create:

```text
docs/next_level/c84_c82_exhaustive_equivalence_report.json
```

A sampled comparison is insufficient for the readiness gate.

---

# 17. Public-only C85 preflight

After the C84 package is frozen, run a dry preflight using only:

```text
the C84 persisted pair-coordinate public API;
the C80 public kernel API.
```

Verify:

```text
all roots and inventories;
all 154,830 pair IDs;
every pair-coordinate record has a canonical C80 coordinate;
every required C80 coordinate validates and has a terminal kernel
status;
shared coordinate schema/equivalence IDs match;
coefficient and kernel factor ownership is compatible;
all values and bounds are immutable;
no upstream constructor is called.
```

Do not multiply a coefficient by a kernel in C84.

Create:

```text
docs/next_level/c84_c83_blocker_supersession_report.json
docs/next_level/c84_c85_ifcontact5_preflight.json
docs/next_level/c84_c85_ifcontact5_import_contract.json
```

The supersession report must establish that:

```text
C83_IFCONTACT_SHARED_COORDINATE_INCOMPLETE
```

is closed by the persisted authenticated C84 public package.

---

# 18. Deterministic, sharded, and restart reconstruction

Run:

```text
two consecutive complete materializations;
one clean temporary materialization;
one serial materialization;
one supported deterministic sharded materialization;
one interrupted/restart materialization.
```

Require byte-identical:

```text
logical counts;
payload shards;
pair/record digests;
index;
root;
tracked manifests and reports.
```

Record:

```text
logical contribution count;
materialized row/table counts;
shard count;
compression ratio;
peak memory;
serial/sharded/restart timing;
checkpoint identities.
```

Create:

```text
docs/next_level/c84_deterministic_reconstruction_report.json
docs/next_level/c84_resource_and_scaling_report.json
```

---

# 19. C53/C58 and kernel-value isolation

After the C84 package and hashes are frozen, poison:

```text
C53 numerical values, adjoints, propagators, and resolvents;
C58 numerical values;
C80 numerical kernel values.
```

C84 reconstruction must remain byte-identical because C84 persists only
C82 projected coefficients and coordinate identities—not kernel values.

Confirm:

```text
C53 supplies no record;
C58 supplies no record;
C80 supplies schema/coordinate authority only, not a kernel value
inside C84;
g_s^2 is absent.
```

Create:

```text
docs/next_level/c84_non_substitution_and_value_isolation_report.json
```

---

# 20. Mutation controls

Required failures include mutations of:

```text
C84 root or index;
bound C82 root;
pair ID;
record ID;
coordinate ID;
equivalence ID;
coefficient expression/value;
coefficient bound;
terminal status;
factor-ownership ID;
witness count;
ancestry span;
pair digest;
record digest;
shard hash;
shard range;
dtype/endianness;
allow_pickle flag;
path traversal;
symlink escape;
array writeability;
record mutability;
upstream-constructor call;
build-if-missing fallback;
C85 import-contract field.
```

Create at least **384 focused live mutations** of actual persisted records,
shards, indices, loaders, and package objects.

Create:

```text
docs/next_level/c84_isolation_report.json
docs/next_level/c84_regression_report.json
```

---

# 21. Readiness and continuation decisions

Select exactly one branch.

## 21.1 Favorable branch

Issue the exact positive status frozen by the committed C84 contract, with
preferred semantic identity:

```text
C84_SOURCE_DERIVED_IFCONTACT_PERSISTED_PAIR_COORDINATE_IMPORT_READY
```

Required:

```text
the C82 descendant qualification is explicit;
the complete logical domain is counted;
every C82 logical record is recoverable from persisted C84 data;
all 154,830 supported pairs are present;
all pair-coordinate coefficients, statuses, and bounds are
authenticated;
pair and record digests close;
forward and reverse ancestry close;
the runtime root/index covers all payloads;
the public loader verifies root, shard, record, and pair identity;
the public loader performs no upstream recomputation;
exhaustive C82 equivalence passes;
the C85 public-only preflight passes;
deterministic materialization and all mutations pass.
```

Next:

> **C85/IFCONTACT5 — certified bare direct-contact matrix multiplication, pair aggregation, sparse/matrix-free assembly, and Hermiticity closure**

## 21.2 Materialization incomplete

```text
C84_IFCOORD2_MATERIALIZATION_INCOMPLETE
```

Next:

> **C85/IFPERSIST — repair only the specifically identified logical-record, factorized-table, shard, or coefficient-persistence defect**

## 21.3 Root/index integrity incomplete

```text
C84_IFCOORD2_RUNTIME_AUTHENTICATION_INCOMPLETE
```

Next:

> **C85/IFROOT — repair only the specifically identified package-root, index, inventory, digest, or shard-authentication defect**

## 21.4 Public import incomplete

```text
C84_IFCOORD2_PUBLIC_IMPORT_INCOMPLETE
```

Next:

> **C85/IFIMPORT — repair only the specifically identified loader, immutability, safe-loading, query, iterator, or no-recomputation defect**

## 21.5 C82 equivalence or ancestry incomplete

```text
C84_IFCOORD2_EQUIVALENCE_INCOMPLETE
```

Next:

> **C85/IFEQUIV — repair only the specifically identified coefficient, status, bound, pair/record identity, or reverse-ancestry mismatch**

Do not issue a coefficient-times-kernel, contact-matrix,
complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD,
matching, inference, or production status.

---

# 22. Essential deliverables

Create at least:

```text
docs/next_level/c84_implementation_report.md
docs/next_level/c84_derivation_authority_manifest.json
docs/next_level/c84_input_fidelity_audit.json
docs/next_level/c84_c82_descendant_qualification.json
docs/next_level/c84_input_freeze.json

docs/next_level/c84_logical_domain_census.json
docs/next_level/c84_logical_domain_validation.json
docs/next_level/c84_persisted_representation_contract.json
docs/next_level/c84_persisted_representation_validation.json
docs/next_level/c84_pair_coordinate_record_schema.json
docs/next_level/c84_pair_coordinate_record_validation.json

docs/next_level/c84_materialization_plan.json
docs/next_level/c84_materialization_report.json
docs/next_level/c84_pair_digest_manifest.json
docs/next_level/c84_record_digest_validation.json
docs/next_level/c84_ancestry_index_manifest.json
docs/next_level/c84_ancestry_reverse_closure_report.json

docs/next_level/c84_runtime_root_manifest.json
docs/next_level/c84_runtime_index_validation.json
docs/next_level/c84_runtime_inventory.json
docs/next_level/c84_safe_loading_contract.json
docs/next_level/c84_safe_loading_validation.json

docs/next_level/c84_api_contract.json
docs/next_level/c84_api_validation.json
docs/next_level/c84_package_object_validation.json
docs/next_level/c84_no_upstream_recomputation_report.json
docs/next_level/c84_c82_exhaustive_equivalence_report.json

docs/next_level/c84_c83_blocker_supersession_report.json
docs/next_level/c84_c85_ifcontact5_preflight.json
docs/next_level/c84_c85_ifcontact5_import_contract.json

docs/next_level/c84_deterministic_reconstruction_report.json
docs/next_level/c84_resource_and_scaling_report.json
docs/next_level/c84_non_substitution_and_value_isolation_report.json
docs/next_level/c84_isolation_report.json
docs/next_level/c84_readiness_report.json
docs/next_level/c84_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifcoord2/
```

and runtime payloads under:

```text
data/runtime/c84_ifcoord2/
```

or the exact repository-equivalent paths.

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 23. Acceptance criteria

C84 is complete only when:

1. Baseline `d99af82754561bcf5efc3944ebdef72790d362bc` reproduces.
2. Both protected untracked paths remain untouched.
3. C77/C78/C80/C82/C83 historical artifacts remain unchanged.
4. `c84_ifcoord2_contract.md` is consumed and preserved.
5. The C82 scientific result is qualified, not overwritten.
6. Upstream scientific roots verify before materialization.
7. The actual logical pair-coordinate count is derived.
8. All 154,830 supported pairs are represented.
9. Every supported pair has at least one terminal coefficient record.
10. No unsupported pair is introduced.
11. Every record has one canonical C80 coordinate and equivalence ID.
12. Every record has an authenticated coefficient expression/value, status, and bound.
13. Every record has reversible ancestry.
14. No C80 kernel value or \(g_s^2\) enters C84.
15. The persisted representation is more than metadata-only counts and hashes.
16. Every logical record is recoverable without upstream recomputation.
17. Pair and record digests verify.
18. Every payload is covered by the authenticated index/root.
19. Public access verifies the selected shard before returning data.
20. Every NumPy load explicitly uses `allow_pickle=False`.
21. Object dtype, unsafe paths, unindexed files, writable arrays, and mutable records are rejected.
22. The public loader calls no C77/C74/C78/C82 constructor or builder.
23. Exhaustive C82 equivalence closes with zero mismatch.
24. Forward and reverse ancestry close.
25. The C85 public-only preflight passes.
26. No coefficient-times-kernel product is formed.
27. No contact matrix or matrix-free contact action is created.
28. Deterministic repeated, clean, serial, sharded, and restart materialization closes.
29. C53/C58/C80-value poisoning leaves C84 byte-identical.
30. At least 384 focused live mutations pass.
31. No physical coupling, counterterm, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
32. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
33. The working tree is clean except for the two protected untracked paths.
34. A local completion commit is created and not pushed.

Do not weaken persisted-record completeness, root/index coverage,
coefficient/bound identity, ancestry reversibility, safe loading, or strict
no-upstream-recomputation behavior to open the gate.

---

# 24. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C84 contract identity;
- descendant qualification applied to C82;
- bound C77/C74/C78/C80/C82 root identities;
- supported-pair count and resolution split;
- actual logical pair-coordinate contribution counts by resolution;
- unique coordinate, coefficient, expression, and ancestry counts;
- coefficient-status counts;
- materialized table/row and shard counts;
- pair/record/root/index hashes;
- missing, duplicate, extra, and mismatch counts;
- forward/reverse ancestry closure;
- public-loader root/shard/record/pair verification;
- explicit no-upstream-recomputation results;
- exhaustive C82 equivalence;
- C83 blocker supersession;
- C85 public-only preflight;
- compression ratio, peak memory, and deterministic shard/restart results;
- C53/C58/C80-value isolation;
- mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that no coefficient-times-kernel product, contact matrix, matrix-free contact action, physical coupling, counterterm, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
