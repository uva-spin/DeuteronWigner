# C94/IFEQUIVAPI2 Codex Work Package

## Title

**Authenticated immutable public facade over the C93 recovered C90 semantic payload: complete pair, normal-form, primitive, expression, theorem, and proof-input access; bounded pagination and inclusion proofs; exported proof checking; strict capsule-only loading; and C95 exhaustive-equivalence handoff**

## Authoritative baseline

Start from the clean local C93 completion commit:

```text
d2aca5f4494d25ca824fc3d152840e5a6e9e9b88
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "d2aca5f4494d25ca824fc3d152840e5a6e9e9b88"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either protected path as scientific authority.

Consume completely the committed C93-to-C94 import contract:

```text
docs/next_level/c93_c94_ifequivapi2_import_contract.json
```

If the exact tracked filename or extension differs, discover the sole C94 import contract, record its path and SHA-256, and do not create or use a competing contract.

If the import contract freezes exact statuses, schemas, API names, or a narrower scientific boundary, preserve them. This work package supplies the immutable public packaging, pagination, authentication, theorem-checker export, and C95 preflight plan; it does not authorize any change to C90 or C93 science.

The historical C90 completion commit is:

```text
ac622ab358b83f090717d7e7fa179b58f18f526d
```

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical environment remains qualified as:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

The baseline is authoritative only when it contains and reproduces:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE

C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE

C93_C90_AUTHENTICATED_SEMANTIC_PAYLOAD_READY
```

with the exact C93 scientific result:

```text
Route A:
    exact pinned C90 replay;

Route B:
    independent C89-backed recovery;

pair-domain closure:
    154,830 recovered normalized programs agree;

logical-domain closure:
    891,992,018 logical records remain represented factorwise;

root/result closure:
    every frozen C90 normal-form root;
    every primitive-root bundle;
    every pair summary;
    every pair proof result;
    every resolution root;
    aggregate historical root efee3c...6a34
    reproduce exactly;

C93 capsule:
    data/runtime/c93_ifc90payload/capsule/;
    approximately 225 MB;
    authenticated descendant recovered preimage of frozen C90
    scientific roots;
    not the original C90 runtime payload;

C93 public boundary:
    capsule-only internal loader exists;
    complete public pair/primitive/theorem facade does not yet exist.
```

Verify and record the complete hashes, counts, capsule size, index, file inventory, and source/API fingerprints from the repository. Prefixes in this prompt are not authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification

C94 is a **public import and authentication layer** over the already complete C93 payload.

It is not:

```text
a third reconstruction route;
a new historical authority;
a change to any C90 normal form;
a change to any primitive record;
a replacement theorem or proof checker;
a new C82 coefficient calculation;
or a historical-versus-descendant equivalence result.
```

C93 already reconstructed the missing scientific content and proved that it is a preimage of the frozen C90 roots and proof results. C94 must expose those exact C93 objects safely and completely.

No external publication is required for:

```text
public record schemas;
bounded pagination;
cursor tokens;
Merkle or inclusion proofs;
direct content-addressed lookup;
immutable loaders;
or public wrapper APIs.
```

Fail closed only when:

```text
the C93 capsule does not contain a required scientific object;
the public representation cannot be made exactly equivalent to C93;
complete pair or primitive enumeration cannot be authenticated;
the proof checker cannot be exposed without semantic modification;
or a public query requires rerunning C90/C93 reconstruction.
```

---

# 2. Claim boundary and descendant qualifications

Do not alter historical C90, C91, C92, or C93 artifacts.

Create explicit descendant qualifications:

```text
C93_C90_AUTHENTICATED_SEMANTIC_PAYLOAD_READY
    -> C93_SCIENCE_AND_PAYLOAD_COMPLETE_PUBLIC_FACADE_INCOMPLETE

C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE
    -> superseded at the payload level by C93 recovery;
       public import still awaits C94

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
    -> descendant compiler remains complete;
       historical public import remains the sole blocker until C94
```

Every public C94 object must retain:

```text
scientific_relation =
    RECOVERED_CONTENT_PREIMAGE_OF_FROZEN_C90_AUTHORITY

original_c90_runtime_identity =
    NOT_CLAIMED

exact_historical_c72_runtime_instance =
    UNKNOWN_NOT_CLAIMED
```

Create:

```text
docs/next_level/c94_descendant_qualification.json
docs/next_level/c94_claim_boundary.json
```

---

# 3. Exact purpose and stopping point

C94 must produce:

```text
a complete audit of the C93 capsule and internal loader;

a selected public-packaging route;

one authenticated public package root bound to the unchanged C93
capsule root and frozen C90 aggregate root;

complete enumeration of all 154,830 historical pair bindings;

bounded deterministic pagination and root-bound cursors;

direct immutable pair lookup and inclusion proofs;

public normal-form DAG and node access;

public primitive-family enumeration, paging, direct lookup, and
inclusion proofs;

public typed-expression-library access;

public theorem specification and proof-checker access;

public pair proof-input and proof-certificate access;

strict capsule-only, no-recomputation loading;

exhaustive C93-to-C94 content equivalence;

public proof-checker regression over all 154,830 historical pair
proofs;

a public-only C95 preflight using the frozen descendant compiler;

and a C95/IFEQUIV7 import contract.
```

C94 must not:

```text
replay C90;
reconstruct C93 content from C77/C78/C82/C89;
compare the complete historical and descendant domains;
issue historical-versus-descendant scientific equivalence;
write 891,992,018 expanded records;
create the final canonical descendant coefficient package;
multiply a projected coefficient by a C80 kernel;
construct a contact matrix or action;
choose a coupling or counterterm;
add C53 or C58;
or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C94_C93_PUBLIC_EQUIVALENCE_IMPORT_READY
```

If the committed C93-to-C94 contract freezes another exact spelling, preserve it and record an explicit semantic alias.

The favorable continuation is:

> **C95/IFEQUIV7 — exhaustive historical-versus-descendant factorized-semantic equivalence using only the immutable C94 historical public API and the independently frozen descendant compiler**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C87:
    canonical color scientific/capsule roots and claim boundary;

C88:
    scientific-record schema and exact logical census;

C89:
    factorized pair programs and rank/unrank semantics;

C90:
    semantic IR, normal-form rules, theorem, checker, compact
    historical ledger, and aggregate authority;

C91:
    independently compiled current descendant domain and public
    import blocker;

C92:
    actual C90 payload audit and Route-C no-go;

C93:
    omitted-payload census;
    Route-A and Route-B recovery;
    normal-form DAG;
    pair bindings;
    primitive stores;
    expression library;
    theorem/checker corpus;
    pair proof inputs and proof results;
    root-preimage closure;
    capsule root/index and internal loader;
    C94 import contract.
```

Create:

```text
docs/next_level/c94_derivation_authority_manifest.json
docs/next_level/c94_input_fidelity_audit.json
```

---

# 5. Freeze the C93 payload authority

Consume C93 only through its authenticated capsule loader and index.

Freeze:

```text
C93 capsule root and index;
bound C90 aggregate scientific root;
historical C90 and C82 commits;
historical environment qualification;
C87 scientific/capsule roots;
C88 schema and order;
C93 normal-form DAG root;
pair-binding root;
all primitive-family roots;
expression-library root;
theorem-specification root;
proof-checker source/API roots;
proof-input and proof-certificate roots;
all resolution roots;
all 154,830 pair identities and bindings.
```

Issue:

```text
C94_C93_PAYLOAD_AUTHORITY_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c94_input_freeze.json
```

No scientific object may be changed to simplify the public API.

---

# 6. Audit the C93 capsule and loader

Inventory every C93 payload object.

For each file or logical store record:

```text
path;
role;
schema/version;
count or shape;
size;
SHA-256;
capsule-index coverage;
scientific-root coverage;
current internal-loader operation;
required C94 public operation;
safe-loading status;
immutability status.
```

At minimum audit:

```text
capsule root/index;
pair-binding table;
normal-form DAG;
normal-form node index;
primitive-family manifests and records;
expression store;
theorem specification;
checker corpus;
proof-input store;
proof-certificate/result store;
summary store;
provenance/source-lock records;
root-preimage closure records.
```

Create:

```text
docs/next_level/c94_c93_capsule_inventory.json
docs/next_level/c94_public_surface_gap_matrix.json
```

---

# 7. Select one public packaging route

Select exactly one route.

## Route A — direct authenticated facade over C93

Use when the C93 capsule already provides complete safe immutable payloads and only public enumeration, authentication, and method surfaces are missing.

This is the preferred route.

## Route B — C94 public index and adapter package

Use when C93 content is complete but the internal layout cannot directly support bounded public lookup.

C94 may create:

```text
public pair-order and page indices;
inclusion-proof trees;
primitive-page indices;
safe adapter records;
API fingerprints.
```

All scientific content must remain referenced from unchanged C93 payload objects.

## Route C — unavailable

Use only when a required public scientific object is absent from the authenticated C93 capsule and would require scientific reconstruction.

Create:

```text
docs/next_level/c94_packaging_route_decision.json
```

C94 must not create new normal forms or primitive records under Route B.

---

# 8. Public package root and index

Create a public package under a repository-equivalent root such as:

```text
data/runtime/c94_ifequivapi2/
```

The package root must bind:

```text
C93 capsule root;
frozen C90 aggregate root;
C94 public schema and API versions;
pair-enumeration root;
page/index roots;
normal-form DAG root;
primitive-family roots;
expression-library root;
theorem/checker roots;
proof-input/certificate roots;
complete file inventory;
safe-loading and no-recomputation policies.
```

The C94 root is a public-package identity, not a new scientific-normal-form root.

Create:

```text
docs/next_level/c94_package_root_manifest.json
docs/next_level/c94_runtime_inventory.json
docs/next_level/c94_package_root_validation.json
```

---

# 9. Public pair-attestation schema

Define one immutable record equivalent to:

```python
PublicHistoricalPairAttestation
```

Each record must expose or bind:

```text
global pair sequence;
resolution ID;
resolution-local sequence;
canonical pair ID;
physical bra/ket IDs and row/column indices;
normal-form root;
normal-form content reference;
primitive-root bundle;
primitive dependency references;
exact logical count;
mixed-radix/order identity;
record-expression root;
coefficient-expression root;
bound-rule root;
status-rule root;
ancestry-rule root;
first and last logical-record identities;
closed-form summary;
proof-input root;
proof-certificate root;
proof result;
historical environment qualification;
C90 aggregate root;
C93 capsule root;
C94 package root;
canonical public-record ID.
```

Every record must state:

```text
root_semantics = FACTORIZED_SEMANTIC_PROGRAM_ROOT;
expanded_serialized_record_stream_hash = false;
original_c90_runtime_payload = false;
```

Create:

```text
docs/next_level/c94_pair_attestation_schema.json
docs/next_level/c94_pair_attestation_schema_validation.json
```

---

# 10. Complete pair enumeration and ordering

Expose the exact historical order:

```text
global pair sequence 0 ... 154,829;
resolution order;
resolution-local sequence;
canonical pair ID;
physical bra/ket IDs.
```

Require:

```text
total = 154,830;
missing sequence = 0;
duplicate sequence = 0;
missing pair = 0;
extra pair = 0;
order mismatch = 0;
resolution mismatch = 0;
binding mismatch = 0.
```

Create:

```text
docs/next_level/c94_pair_enumeration_manifest.json
docs/next_level/c94_pair_enumeration_validation.json
```

---

# 11. Bounded pagination and root-bound cursors

Provide deterministic pair pagination.

A page must bind:

```text
C94 package root;
C93 capsule root;
query/filter identity;
global sequence start/stop;
record count;
ordered pair IDs;
ordered record digests;
previous/next-page identity where used;
page digest;
next cursor.
```

A cursor must bind:

```text
package root;
query/filter;
next global sequence;
page-size policy;
schema/API version.
```

Require:

```text
safe declared maximum page size;
stable repeated pagination;
no gaps;
no overlaps;
no duplicate pairs;
explicit end-of-sequence;
root mismatch rejection;
query mismatch rejection;
cursor tamper rejection.
```

Create:

```text
docs/next_level/c94_pagination_contract.json
docs/next_level/c94_pagination_validation.json
```

---

# 12. Pair and page inclusion proofs

Provide efficient authentication for:

```text
one pair record;
one page;
one resolution;
the complete pair domain.
```

A returned pair must verify through:

\[
\text{pair record}
\rightarrow
\text{page}
\rightarrow
\text{resolution root}
\rightarrow
\text{C94 package root}
\rightarrow
\text{C93 capsule root}
\rightarrow
\text{C90 aggregate scientific root}.
\]

Create:

```text
docs/next_level/c94_pair_inclusion_proof_contract.json
docs/next_level/c94_pair_inclusion_proof_validation.json
```

Inclusion proves package membership; C90/C93 semantics prove scientific content.

---

# 13. Public normal-form DAG access

Expose immutable operations for:

```text
pair ID -> normal-form root;
normal-form root -> root node;
node ID -> typed node;
node -> ordered child IDs;
node -> primitive and expression dependencies;
bounded DAG traversal;
normal-form root verification.
```

The public API must preserve:

```text
node type;
ordered children;
exact cardinality;
mixed-radix semantics;
filter/permutation identity;
primitive references;
expression dependencies;
scientific node hash.
```

Reject:

```text
unknown node types;
cycles not allowed by the C90 schema;
missing child;
orphan reference;
child-order mutation;
hash mismatch.
```

Create:

```text
docs/next_level/c94_normal_form_public_contract.json
docs/next_level/c94_normal_form_public_validation.json
```

---

# 14. Public primitive-family access

For every primitive family expose:

```text
family ID;
schema;
canonical order;
record count;
scientific family root;
producer/provenance identity;
bounded pagination;
direct record lookup;
record inclusion proof.
```

Required families include the actual C93 equivalents of:

```text
supported-pair records;
physical bra/ket identities;
C77 projection amplitudes and bounds;
C78 witnesses, endpoints, retained-q intermediates, and signs;
C87 color records;
C80 coordinate templates and equivalence identities;
metric and conjugation records;
coefficient primitives;
bound primitives;
status primitives;
ancestry primitives.
```

Create:

```text
docs/next_level/c94_primitive_family_manifest.json
docs/next_level/c94_primitive_public_access_validation.json
```

A family root and count without record-level access is insufficient.

---

# 15. Public expression-library access

Expose immutable typed expression records for:

```text
scientific-record construction;
C80 coordinate construction references;
projected-coefficient construction;
bra conjugation and metric factors;
bound composition;
terminal status selection;
witness multiplicity;
ancestry construction;
first/last record identities;
closed-form summaries.
```

Provide:

```text
expression enumeration;
direct lookup by expression ID;
typed AST/bytecode access;
input/output schema;
source/provenance identity;
scientific expression root;
inclusion proof.
```

Reject opaque callables and unknown expression opcodes.

Create:

```text
docs/next_level/c94_expression_public_contract.json
docs/next_level/c94_expression_public_validation.json
```

---

# 16. Public theorem specification

Expose the immutable C93-recovered C90 theorem specification.

It must include:

```text
semantic-IR node types;
node expansion semantics;
normal-form rules;
primitive-root requirements;
cardinality rules;
ordering rules;
record-map requirements;
coefficient/bound/status/ancestry requirements;
the theorem statement;
proof-input schema;
proof-certificate schema;
theorem version and scientific root.
```

Create:

```text
docs/next_level/c94_theorem_public_specification.json
docs/next_level/c94_theorem_public_validation.json
```

C94 may wrap the theorem object but may not alter its semantics.

---

# 17. Public proof checker

Expose a safe immutable operation equivalent to:

```python
verify_factorized_expansion_equivalence(
    historical_normal_form,
    descendant_normal_form,
    primitive_equivalence_certificates,
    *,
    theorem_version,
) -> ExpansionEquivalenceResult
```

The checker must:

```text
use the exact recovered C90 checker semantics;
accept only typed C90 IR;
verify schemas and roots;
verify primitive-equivalence certificates;
verify node semantics, count, and order;
verify record/coefficient/bound/status/ancestry expressions;
return a frozen result and proof-certificate root;
perform no global leaf expansion;
query no C80 numerical kernel values.
```

Create:

```text
docs/next_level/c94_expansion_checker_contract.json
docs/next_level/c94_expansion_checker_validation.json
```

Run the public checker over all 154,830 recovered historical Route-A/Route-B proof inputs and require exact reproduction of the C93/C90 proof results.

---

# 18. Public proof-input and certificate access

For every pair expose:

```text
historical Route-A normal form;
historical Route-B normal form;
primitive-equivalence bindings;
schema/theorem versions;
exact cardinality;
order identity;
record/coefficient/bound/status/ancestry expression identities;
proof result;
proof-certificate root.
```

Provide direct pair lookup and bounded proof-input pagination.

Create:

```text
docs/next_level/c94_pair_proof_public_contract.json
docs/next_level/c94_pair_proof_public_validation.json
```

---

# 19. Strict capsule-only no-recomputation loading

Implement under:

```text
src/deuteron_wigner/bridge/ifequivapi2/
```

or the exact repository-equivalent package.

Poison and forbid:

```text
C77 builders;
C78 builders;
C82 constructors;
C89 pair-program construction;
C90 compilers and normalizers;
C93 recovery routes;
historical worktree staging;
build-if-missing;
repair-if-missing;
network access.
```

The public facade may read and verify only C93/C94 payloads and execute the exported checker.

Create:

```text
docs/next_level/c94_no_recomputation_contract.json
docs/next_level/c94_no_recomputation_validation.json
```

---

# 20. Safe immutable loading

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payload;
path traversal;
unexpected symlink;
unindexed file;
shape/dtype/endianness mismatch;
unknown node/expression type;
writable arrays;
mutable records;
unknown schema version.
```

Create:

```text
docs/next_level/c94_safe_loading_contract.json
docs/next_level/c94_safe_loading_validation.json
```

---

# 21. Public API

Provide immutable operations equivalent to:

```python
load_verified_c90_public_semantic_authority()

verify_c90_public_semantic_authority()

historical_pair_count(resolution=None)

historical_pair_page(
    *,
    resolution=None,
    cursor=None,
    limit=...,
)

historical_pair_by_sequence(global_sequence)

historical_pair_attestation(pair_id, resolution)

historical_pair_normal_form(pair_id, resolution)

historical_normal_form_node(node_id)

historical_pair_summary(pair_id, resolution)

historical_primitive_family(family_id)

historical_primitive_page(
    family_id,
    *,
    cursor=None,
    limit=...,
)

historical_primitive_record(family_id, record_id)

historical_expression(expression_id)

historical_pair_proof_inputs(pair_id, resolution)

historical_pair_proof_certificate(pair_id, resolution)

expansion_theorem_specification()

verify_factorized_expansion_equivalence(...)
```

Every returned object must expose its public-package, capsule, and scientific-root identities.

Create:

```text
docs/next_level/c94_api_contract.json
docs/next_level/c94_api_validation.json
```

---

# 22. Exhaustive C93-to-C94 equivalence

Compare the complete public C94 domain against the authenticated C93 capsule.

For all 154,830 pairs compare:

```text
sequence and identity;
normal-form root and reachable DAG;
primitive dependencies;
logical count;
ordering;
expression roots;
first/last identities;
summary;
proof inputs;
proof result;
proof-certificate root.
```

For all primitive and expression families compare all records and canonical order.

Require:

```text
missing pair = 0;
extra pair = 0;
order mismatch = 0;
DAG mismatch = 0;
primitive mismatch = 0;
expression mismatch = 0;
summary mismatch = 0;
proof-input mismatch = 0;
proof-result mismatch = 0;
checker-regression mismatch = 0;
inclusion-proof failure = 0.
```

Create:

```text
docs/next_level/c94_exhaustive_c93_public_equivalence.json
```

A sampled comparison is insufficient.

---

# 23. C91/C92 blocker supersession

Create an explicit descendant supersession record:

```text
C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE
    -> payload omission closed by C93 recovery;

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
    -> historical pair, primitive, expression, theorem, and checker
       public import closed by C94.
```

Do not claim that the historical-versus-descendant comparison has already been executed.

Create:

```text
docs/next_level/c94_c91_c92_blocker_supersession_report.json
```

---

# 24. Public-only C95 preflight

Using only:

```text
the C94 historical public API;
the frozen independent descendant compiler/artifacts from C91 or
their exact current continuation interface;
```

verify:

```text
all 154,830 historical pair IDs are enumerable exactly once;
all pair normal forms and summaries load publicly;
all primitive families required for crosswalk are accessible;
all expressions required for comparison are accessible;
all proof inputs and certificates are accessible;
the public checker executes;
the descendant compiler emits the compatible C90 IR;
beginning, middle, ending, maximum-cardinality, and semantic-boundary
pair checks pass;
no private C90 or C93 recovery access occurs.
```

C94 must not execute the full historical-versus-descendant comparison.

Create:

```text
docs/next_level/c94_c95_ifequiv7_preflight.json
docs/next_level/c94_c95_ifequiv7_import_contract.json
```

---

# 25. Determinism, pagination invariance, and restart

Run:

```text
two complete facade/index builds;
one clean build;
one serial build;
one deterministic sharded build;
one interrupted/resumed build;
two complete pair traversals with different permitted page sizes;
two complete primitive-family traversals with different page sizes.
```

Require:

```text
identical scientific records;
identical pair and primitive order;
identical package root;
identical inclusion roots;
identical checker results;
identical C95 preflight.
```

Page boundaries may differ, but package scientific identity may not.

Create:

```text
docs/next_level/c94_deterministic_reconstruction_report.json
docs/next_level/c94_restart_validation.json
docs/next_level/c94_resource_and_scaling_report.json
```

---

# 26. Isolation and mutation controls

Required failures include mutations of:

```text
C93 capsule root/index;
C90 aggregate root;
C94 package root/index;
pair sequence or identity;
normal-form root;
normal-form node content or child order;
primitive dependency or record;
expression AST/opcode;
logical count or order identity;
pair summary;
proof input;
proof result;
proof-certificate root;
theorem specification;
checker source/API fingerprint;
checker node semantics;
page range or digest;
cursor root/query/sequence;
inclusion proof;
no-recomputation guard;
unsafe loading;
mutable return;
C95 import-contract field.
```

The public scientific package must remain invariant under mutations of:

```text
temporary build path;
host/process ID;
page request size;
pagination traversal order;
wall-clock timestamp;
narrative report path.
```

Poison:

```text
C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical coupling;
counterterm interfaces.
```

C94 must remain unchanged.

Create at least **384 focused live mutations** of actual pair records, DAG nodes, primitive records, expressions, theorem/checker objects, pages, cursors, inclusion proofs, loaders, indices, and roots.

Create:

```text
docs/next_level/c94_isolation_report.json
docs/next_level/c94_regression_report.json
```

---

# 27. Readiness and continuation decisions

Select exactly one branch.

## 27.1 Favorable branch

Issue the exact positive status frozen by the C93-to-C94 contract, with preferred semantic identity:

```text
C94_C93_PUBLIC_EQUIVALENCE_IMPORT_READY
```

Required:

```text
C93 capsule authority verifies;
the public packaging route is explicit;
the C94 package root binds C93 and C90;
all 154,830 pairs are publicly enumerable;
pagination and cursors are complete and authenticated;
direct pair lookup and inclusion proofs pass;
normal-form DAG access is complete;
all primitive families are publicly accessible;
all typed expressions are publicly accessible;
the theorem specification is public and unchanged;
the public checker reproduces all 154,830 historical proof results;
all proof inputs and certificates are public;
the public loader performs no reconstruction;
safe immutable loading passes;
exhaustive C93-to-C94 equivalence has zero mismatch;
C91/C92 blockers are explicitly superseded;
the public-only C95 preflight passes;
determinism, restart, pagination invariance, and mutations pass.
```

Next:

> **C95/IFEQUIV7 — exhaustive historical-versus-descendant factorized-semantic equivalence using immutable public APIs only**

## 27.2 C93 payload/public-index incomplete

```text
C94_IFEQUIVAPI2_PAYLOAD_INDEX_INCOMPLETE
```

Next:

> **C95/IFINDEX — repair only the specifically identified pair, DAG, primitive, expression, proof, index, or inclusion-record defect**

## 27.3 Pagination or pair public access incomplete

```text
C94_IFEQUIVAPI2_PAIR_ACCESS_INCOMPLETE
```

Next:

> **C95/IFPAIRPUBLIC — repair only pair ordering, pagination, cursor, direct lookup, inclusion proof, or resolution-root closure**

## 27.4 Primitive or expression access incomplete

```text
C94_IFEQUIVAPI2_PRIMITIVE_EXPRESSION_ACCESS_INCOMPLETE
```

Next:

> **C95/IFPRIMEXPR — repair only primitive-family or expression enumeration, lookup, inclusion, schema, or root verification**

## 27.5 Theorem/checker public export incomplete

```text
C94_IFEQUIVAPI2_CHECKER_EXPORT_INCOMPLETE
```

Next:

> **C95/IFCHECKER2 — repair only theorem specification, checker wrapping, typed-IR validation, fingerprint, or proof-regression behavior**

## 27.6 No-recomputation or safe-loader incomplete

```text
C94_IFEQUIVAPI2_PUBLIC_LOADER_INCOMPLETE
```

Next:

> **C95/IFPUBLIC2 — repair only capsule/index loading, immutability, safe decoding, hidden reconstruction, deterministic build, or API behavior**

Do not issue historical-versus-descendant equivalence, final coefficient persistence, coefficient-times-kernel, contact-matrix, complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD, matching, inference, or production status.

---

# 28. Essential deliverables

Create at least:

```text
docs/next_level/c94_implementation_report.md
docs/next_level/c94_derivation_authority_manifest.json
docs/next_level/c94_input_fidelity_audit.json
docs/next_level/c94_descendant_qualification.json
docs/next_level/c94_claim_boundary.json
docs/next_level/c94_input_freeze.json

docs/next_level/c94_c93_capsule_inventory.json
docs/next_level/c94_public_surface_gap_matrix.json
docs/next_level/c94_packaging_route_decision.json
docs/next_level/c94_package_root_manifest.json
docs/next_level/c94_runtime_inventory.json
docs/next_level/c94_package_root_validation.json

docs/next_level/c94_pair_attestation_schema.json
docs/next_level/c94_pair_attestation_schema_validation.json
docs/next_level/c94_pair_enumeration_manifest.json
docs/next_level/c94_pair_enumeration_validation.json
docs/next_level/c94_pagination_contract.json
docs/next_level/c94_pagination_validation.json
docs/next_level/c94_pair_inclusion_proof_contract.json
docs/next_level/c94_pair_inclusion_proof_validation.json

docs/next_level/c94_normal_form_public_contract.json
docs/next_level/c94_normal_form_public_validation.json
docs/next_level/c94_primitive_family_manifest.json
docs/next_level/c94_primitive_public_access_validation.json
docs/next_level/c94_expression_public_contract.json
docs/next_level/c94_expression_public_validation.json

docs/next_level/c94_theorem_public_specification.json
docs/next_level/c94_theorem_public_validation.json
docs/next_level/c94_expansion_checker_contract.json
docs/next_level/c94_expansion_checker_validation.json
docs/next_level/c94_pair_proof_public_contract.json
docs/next_level/c94_pair_proof_public_validation.json

docs/next_level/c94_no_recomputation_contract.json
docs/next_level/c94_no_recomputation_validation.json
docs/next_level/c94_safe_loading_contract.json
docs/next_level/c94_safe_loading_validation.json
docs/next_level/c94_api_contract.json
docs/next_level/c94_api_validation.json

docs/next_level/c94_exhaustive_c93_public_equivalence.json
docs/next_level/c94_c91_c92_blocker_supersession_report.json
docs/next_level/c94_c95_ifequiv7_preflight.json
docs/next_level/c94_c95_ifequiv7_import_contract.json

docs/next_level/c94_deterministic_reconstruction_report.json
docs/next_level/c94_restart_validation.json
docs/next_level/c94_resource_and_scaling_report.json
docs/next_level/c94_isolation_report.json
docs/next_level/c94_readiness_report.json
docs/next_level/c94_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifequivapi2/
```

Use compact public runtime payloads under:

```text
data/runtime/c94_ifequivapi2/
```

Do not duplicate the 225 MB C93 payload unless Route B requires a specifically justified safe public adapter object. Prefer content-addressed references to unchanged C93 payload files.

Create exactly one next-package contract corresponding to the selected branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the protected C69 prompt.

---

# 29. Acceptance criteria

C94 is complete only when:

1. Baseline `d2aca5f4494d25ca824fc3d152840e5a6e9e9b88` reproduces.
2. Both protected untracked paths remain untouched.
3. C87/C88/C89/C90/C91/C92/C93 historical artifacts remain unchanged.
4. The committed C93-to-C94 import contract is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. The C93 capsule is never called the original C90 runtime payload.
7. C93 and C90 roots remain factorized-semantic authorities, not expanded-stream hashes.
8. The C93 capsule verifies before public packaging.
9. The public packaging route is explicit.
10. The C94 package root binds C93 and C90.
11. Every one of the 154,830 pairs is publicly represented exactly once.
12. Pair order and resolution splits reproduce C93/C90.
13. Pagination has no gap, overlap, or duplicate.
14. Cursors are package-, query-, and sequence-bound.
15. Direct pair lookup verifies inclusion.
16. Every pair normal form is reachable from its root.
17. Every normal-form node is publicly readable and authenticated.
18. Every required primitive family supports complete authenticated access.
19. Every required typed expression supports direct authenticated access.
20. The theorem specification is publicly accessible without change.
21. The checker accepts only typed C90 IR and approved primitive certificates.
22. The public checker reproduces all 154,830 historical proof results.
23. Every pair proof input and certificate is public.
24. No C77/C78/C82/C89/C90/C93 reconstruction route is called by the public loader.
25. Every NumPy load explicitly uses `allow_pickle=False`.
26. Object dtype, unsafe paths, unindexed files, writable arrays, mutable records, and unknown schemas are rejected.
27. Exhaustive C93-to-C94 pair equivalence has zero mismatch.
28. DAG, primitive, expression, summary, proof-input, and proof-result equivalence have zero mismatch.
29. C91 and C92 blockers are explicitly superseded without claiming final equivalence.
30. The C95 public-only preflight passes.
31. Two clean builds agree.
32. Serial, deterministic sharded, and interrupted/resumed builds agree.
33. Different permitted page sizes preserve package identity and order.
34. At least 384 focused live mutations pass.
35. No historical-versus-descendant equivalence claim is made.
36. No final canonical coefficient package is claimed.
37. No expanded logical-record stream is written.
38. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
39. `NO_JOINT_MEASURE`, 216 production routes, all 642 ART25 identities, and all authoritative artifacts remain unchanged.
40. The working tree is clean except for the two protected untracked paths.
41. A local completion commit is created and not pushed.

Do not weaken pair completeness, DAG access, primitive or expression coverage, theorem/checker identity, inclusion authentication, no-recomputation behavior, or exhaustive C93 equivalence to open the gate.

---

# 30. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed C93-to-C94 contract path and hash;
- historical C90 and C82 commits;
- historical environment qualification;
- complete C90, C93 capsule, and C94 package roots;
- selected packaging route;
- C93 capsule file/object counts and size;
- C94 public-index/package file counts and size;
- pair totals and resolution splits;
- page counts under the default page size;
- pair pagination and cursor validation;
- pair inclusion-proof result;
- normal-form node counts and access result;
- primitive-family and primitive-record counts and roots;
- expression counts by class and access result;
- theorem-specification and checker roots;
- number of historical proof results reproduced;
- proof-input/certificate access result;
- no-recomputation poisoning result;
- safe-loading result;
- exhaustive C93-to-C94 mismatch counts;
- C91/C92 blocker supersession result;
- C95 public-only preflight result;
- deterministic/restart/page-size invariance results;
- peak memory and persistent footprint;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime recovery remains unclaimed;
- confirmation that C93 is a descendant recovered preimage, not the original C90 runtime payload;
- confirmation that roots remain factorized-semantic roots, not expanded serialized-record hashes;
- confirmation that no historical-versus-descendant equivalence, final coefficient package, expanded full-record stream, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
