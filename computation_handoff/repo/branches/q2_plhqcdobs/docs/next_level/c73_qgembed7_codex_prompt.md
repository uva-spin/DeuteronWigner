# C73/QGEMBED7 Codex Work Package

## Title

**Execution-first exact CM-ground and color-triplet physical \(qg\) embedding from immutable C64 and C72 authority packages: factorized sparse maps, certified projectors, historical-basis covariance, and descendant-impact closure**

## Authoritative baseline

Start from the clean local C72 completion commit:

```text
369e95e1004c2f896f8a41e5c3270f7644e3391d
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "369e95e1004c2f896f8a41e5c3270f7644e3391d"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

The baseline is authoritative only when it contains and reproduces:

```text
C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY

C66_SOURCE_DERIVED_TRIPLET_ISOMETRY_ARTIFACT_READY

C68_SOURCE_DERIVED_TRIPLET_ISOMETRY_RUNTIME_READY

C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY
```

and the exact inherited authority:

```text
C64 transverse authority:
    733 exact TM blocks;
    171,153 exact coefficient-status records;
    67,920 exact cross-m residue certificates;
    complete row/column/global basis crosswalks;
    exact expression and exact support identities;
    certified numerical arrays and bounds;
    immutable hash-verifying no-regeneration public API;

C72 color authority:
    authenticated scientific-source fingerprint;
    authenticated public-API fingerprint;
    exactly 24 ordered product-color rows;
    exactly 3 ordered retained-triplet columns;
    exactly 72 ordered row-column pair identities;
    72 complete exact-record authorities;
    exact expressions for every nonzero;
    exact-zero certificates for every zero;
    72 per-entry numerical-bound identities;
    certified E_src, Gram, triplet adapter, U3, U3-dagger, and P3;
    one authenticated package root and complete inventory;
    immutable TripletAuthorityPackage public loader;
    strict no-regeneration and no-inference behavior;

exact color science:
    U3 = E_src / sqrt(C_F);
    C_F = 4/3;
    U3-dagger U3 = I3;
    P3 = U3 U3-dagger;
    all-eight-generator intertwining;
    rank-three projector closure;
    zero anti-sextet and 15 leakage;

historical embedding boundary:
    no package through C72 has constructed the complete physical
    CM-ground color-triplet qg embedding.
```

Verify all counts, hashes, paths, basis identities, exact records, numerical bounds, package roots, and API behavior through the immutable C64 and C72 public interfaces. This prompt is not numerical authority.

The exact transverse convention remains:

\[
|n,m\rangle_{\rm polar}
=
(-1)^n
\left|
n+\max(m,0),\,
n+\max(-m,0)
\right\rangle_{\rm circ},
\qquad
L_z=N_+-N_- .
\]

The exact color convention remains:

\[
U_3=\frac{E_{\rm src}}{\sqrt{C_F}},
\qquad
C_F=\frac43,
\qquad
U_3^\dagger U_3=I_3,
\qquad
P_3=U_3U_3^\dagger .
\]

The physical resolution trajectory remains:

```text
(K,Nmax,bHO/GeV)
  = (9/2,8,0.40)
  = (11/2,10,0.45)
  = (13/2,12,0.50).
```

C40 remains:

```text
EXECUTABLE_METHOD_ORACLE_ONLY
```

Create a local completion commit. Do not push.

---

# 1. Execution mandate

C73 must construct the physical \(qg\) embedding.

The two complete immutable inputs now exist:

```text
C64:
    exact/certified raw-product <-> relative/CM transverse maps;

C72:
    exact/certified retained-triplet <-> product-color map with
    complete authority and per-entry bound identities.
```

C73 must not create another package whose only result is a duplicate import wrapper, renamed inventory, additional package root, convenience manifest, or reauthentication of already authenticated C64/C72 content.

A fail-closed import status is authorized only when an actual public C64 or C72 loader:

```text
rejects a hash;
finds a missing indexed object;
returns a mutable object;
returns a basis/exact-record/bound identity inconsistent with its
authenticated root;
or violates its no-regeneration contract.
```

A preference for a different schema, filename, dense export, summary, or wrapper is not an import failure.

Once both import gates pass, C73 must proceed immediately to:

```text
exact CM-ground selection;
TM-orientation proof;
kinematic embedding;
color composition;
physical embed/project actions;
and projector validation.
```

Construct the physical embedding before auditing C47, C52, C53, C57, or C58. A descendant-impact problem may affect the next branch, but it must not erase or retroactively block a successfully constructed C73 embedding.

The authoritative full-scale representation may be factorized, block sparse, and matrix free. Dense full embedding and dense raw-space projector arrays are not required.

---

# 2. Exact purpose

C73 must construct:

```text
authenticated read-only C64 and C72 imports;

exact raw qg kinematic basis;

exact relative/CM kinematic basis;

exact CM-ground physical kinematic basis;

exact raw qg product-color basis;

exact CM-ground retained-triplet physical basis;

exact CM-ground selection through C64 identities;

proved C64 TM orientation;

executable block-sparse kinematic injection J_kin;

exact or metric-adjoint kinematic projection P_kin;

executable factorized CM-ground projector Pi_CM;

explicit kinematic/color basis-order adapters;

executable full physical triplet embedding J_phys;

exact or metric-adjoint physical projection P_phys;

executable factorized CM-ground-triplet projector Pi_CM,3;

threshold-free exact physical support membership;

certified numerical actions and propagated bounds;

independent matrix-free reconstructions;

a derived historical C47 basis adapter;

C47, C52, C53, C57, and C58 impact audits;

a C59/C60 continuation decision;

one and only one authorized continuation branch.
```

C73 must not construct:

```text
the direct-contact endpoint relation;

the retained-q intermediate-witness relation;

a direct-contact numerator or denominator;

a finite-cell contact normalization;

a contact P-minus or M-squared value;

a direct-contact matrix;

a counterterm coefficient;

a complete instantaneous-fermion operator;

a local-HQCD completion;

a Wilson/bilocal TMD or one-loop matching result.
```

The strongest favorable status is:

```text
C73_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY
```

The exact favorable continuation is:

> **C74/IFSUPPORT2 — source-ordered direct-contact endpoint and intermediate-\(q\) witness support using the immutable C73 physical embedding**

---

# 3. Mandatory inputs

Use public immutable package APIs whenever available.

## 3.1 C64

Read and verify the actual repository equivalents of:

```text
C64 package/readiness records;
block census and block identities;
row/column/global basis crosswalks;
exact status and expression domains;
certified arrays and error bounds;
residue certificates;
immutable public import API and API contract.
```

## 3.2 C72

Load only through:

```python
load_triplet_authority_package()
```

or the exact authenticated public equivalent.

Verify:

```text
package root;
source/API fingerprints;
24 row identities;
3 column identities;
72 pair identities;
72 exact-record authorities;
72 per-entry bound authorities;
exact expressions and zero certificates;
certified U3, U3-dagger, P3, and companion objects;
inventory;
immutability;
no-regeneration/no-inference behavior.
```

## 3.3 Historical and descendant records

Read the actual repository equivalents of:

```text
C43 light-front conventions;

C45 longitudinal, HO, spinor, polarization, color, and zero-mode
contracts;

C47 longitudinal partitions, x-scaled coordinate map, TM/CM basis,
many-body truncation, CM plan, physical q/qg basis manifests,
historical quadrature map, basis order, and comparison records;

C52 source-derived colorless canonical vertex;

C53 source-derived physical canonical vertex, full-product and
reduced-triplet routes, generated adjoint, ancestry, and
matrix-free records;

C57 operation order, corresponding-propagating support, canonical
positions, conditional unions, candidate envelopes, and ancestry;

C58 ordered-joint support, mode ledger, q-sector contraction, qg
scope decision, and runtime records;

C59/C60 direct-contact support no-go and exact missing-input
specification.
```

Use actual filenames. Do not manufacture a missing-file blocker from a naming difference.

Create:

```text
docs/next_level/c73_derivation_authority_manifest.json
docs/next_level/c73_input_fidelity_audit.json
```

---

# 4. Immutable import gates

## 4.1 C64 import

Before constructing one kinematic component, require:

```text
733 blocks verified;
171,153 statuses verified;
67,920 residue certificates verified;
package aggregate identity verified;
basis-order and crosswalk identities verified;
expression/support identities verified;
all required arrays and bounds verified;
all required paths present;
returned objects immutable;
no C62 regeneration.
```

Create:

```text
docs/next_level/c73_c64_import_report.json
```

## 4.2 C72 import

Before constructing one color-composed component, require:

```text
package root verified;
source/API fingerprints verified;
24/3/72 basis authority verified;
72 exact records verified;
72 per-entry bounds verified;
all exact-expression and zero-certificate domains verified;
U3/U3-dagger/P3 and companion-object identities verified;
all arrays and bounds verified;
complete inventory verified;
returned objects immutable;
no C66/C68/C70/C72 regeneration or inference.
```

Create:

```text
docs/next_level/c73_c72_import_report.json
```

If an import gate fails, record the exact object, expected identity, observed identity, and failing public operation. Do not bypass the package API.

---

# 5. Freeze calculation and holdouts

Freeze before constructing the maps:

```text
C64 and C72 package roots;

all C64 block orientations;

the C64 global crosswalk;

the exact CM-ground label;

raw qg kinematic ordering;

relative/CM ordering;

physical kinematic ordering;

longitudinal, helicity, Jz, OAM, shell, Nmax, and zero-mode
conventions;

C72 product-color row ordering;

C72 retained-triplet column ordering;

all U3 exact records and per-entry bounds;

historical C47 basis order and phase records.
```

Freeze holdouts:

```text
one lowest-shell CM-ground state;

one highest-shell CM-ground state per resolution;

one CM-excited state per resolution;

one positive-m and one negative-m state;

one physical state with one raw component;

one physical state with multiple raw components;

one exact zero from every C64 zero class;

one smallest-certified C64 nonzero per resolution;

one C72 exact-zero color entry;

one C72 smallest-certified nonzero color entry;

one kinematic round trip per resolution;

one CM-projector holdout per resolution;

all three retained-triplet columns;

one anti-sextet vector;

one 15-representation vector;

one full physical round trip per resolution;

one C47 adapter holdout per shell;

one C52 source-derived matrix-element holdout;

one C53 full-product/reduced-route holdout;

one C57 support edge per resolution;

one C58 admitted mode per resolution.
```

Create:

```text
docs/next_level/c73_calculation_plan.json
docs/next_level/c73_holdout_plan.json
```

No failed holdout may be moved into construction after inspection.

---

# 6. Exact basis manifests

Construct deterministic identities for:

```text
raw qg kinematic basis;

relative/CM kinematic basis;

CM-ground physical kinematic basis;

raw qg product-color basis;

CM-ground retained-triplet physical basis.
```

Every raw kinematic record must retain:

```text
resolution;
K;
k_q and k_g;
q and g transverse-HO labels;
q and g helicities;
total Jz;
total shell;
zero-mode status;
global raw-kinematic index;
C64 block/crosswalk ancestry.
```

Every physical kinematic record must retain:

```text
longitudinal partition;
relative-HO labels;
CM labels;
q and g helicities;
Jz;
many-body Nmax ancestry;
global physical-kinematic index.
```

Every raw product-color record must retain:

```text
raw kinematic ID;
C72 product-color row ID;
fundamental and adjoint color IDs;
global raw-product index.
```

Every physical triplet record must retain:

```text
physical kinematic ID;
C72 retained-triplet column ID;
global physical-triplet index.
```

Verify rather than assume the expected physical dimensions:

```text
physical kinematic:
    448 / 900 / 1,584;

physical triplet:
    1,344 / 2,700 / 4,752.
```

Use authoritative derived values if the manifests differ.

Create:

```text
docs/next_level/c73_raw_qg_basis_manifest.json
docs/next_level/c73_relative_cm_basis_manifest.json
docs/next_level/c73_physical_kinematic_basis_manifest.json
docs/next_level/c73_raw_product_color_basis_manifest.json
docs/next_level/c73_physical_triplet_basis_manifest.json
docs/next_level/c73_basis_order_manifest.json
```

---

# 7. Exact CM-ground selection

Use the C64 crosswalk and basis identities to select exactly:

\[
n_{\rm CM}=0,\qquad m_{\rm CM}=0.
\]

Do not infer CM identity from numerical sparsity, thresholding, or historical C47 row positions.

Each selected record must retain:

```text
C64 block ID;
block-local row/column ID;
global relative/CM basis ID;
physical kinematic basis ID;
basis hashes;
exact status ancestry.
```

Require:

```text
all physical CM-ground states present;
no CM-excited state selected;
no duplicate selection;
no missing selection;
selected count equals physical kinematic dimension.
```

Create:

```text
docs/next_level/c73_cm_ground_selection_manifest.json
docs/next_level/c73_cm_ground_selection_validation.json
```

---

# 8. Prove C64 TM orientation

Determine the exact orientation of every consumed C64 block.

When a block stores:

\[
T_{\mathrm{relCM}\leftarrow\mathrm{raw}}
=
\langle \mathrm{rel},\mathrm{CM}|\mathrm{raw}\rangle,
\]

the physical-to-raw map is:

\[
J_{\mathrm{raw}\leftarrow\mathrm{phys}}
=
T_{\mathrm{relCM}\leftarrow\mathrm{raw}}^\dagger
\big|_{\mathrm{CM}=0},
\]

subject to the authenticated basis metrics.

Prove orientation through:

```text
C64 orientation metadata;
exact low-shell identities;
adjoint/inverse identities;
basis-vector round trips;
consistency with the independently derived C47 adapter.
```

Do not choose an orientation because it gives the smaller numerical residual.

Create:

```text
docs/next_level/c73_tm_orientation_contract.json
docs/next_level/c73_tm_orientation_validation.json
```

---

# 9. Kinematic embedding

Construct:

\[
J_R^{\rm kin}:
\mathcal H_R^{\rm CM=0,kin}
\longrightarrow
\mathcal H_R^{\rm raw,kin}.
\]

Use:

```text
C64 exact statuses for support;
C64 exact-expression identities for ancestry;
C64 certified values and bounds for numerical actions.
```

Construct two disjoint executable routes:

```text
a deterministic block-sparse stored representation;

an independent matrix-free action that reads immutable C64 blocks
and the frozen CM selection but does not multiply by the stored
C73 matrix.
```

Construct the exact or metric adjoint:

\[
P_R^{\rm kin}.
\]

Require:

\[
P_R^{\rm kin}J_R^{\rm kin}=I.
\]

Construct the executable factorized projector:

\[
\Pi_R^{\rm CM}
=
J_R^{\rm kin}P_R^{\rm kin}.
\]

A dense raw-space projector is not required.

Validate:

```text
Hermiticity;
idempotence;
rank and trace;
orthogonality to every retained CM-excited holdout;
block-sparse/matrix-free agreement;
certified propagated bounds.
```

Do not use post-hoc symmetrization, eigenvalue clipping, pseudoinverse replacement, or threshold pruning.

Create:

```text
docs/next_level/c73_exact_kinematic_embedding.json
docs/next_level/c73_exact_kinematic_projection.json
docs/next_level/c73_cm_image_projector.json
docs/next_level/c73_kinematic_embedding_validation.json
docs/next_level/c73_kinematic_matrix_free_report.json
```

---

# 10. Quantum-number closure

Verify exact conservation or declared block compatibility for:

```text
longitudinal partition and total K;
quark helicity;
gluon helicity;
total Jz;
intrinsic and CM OAM;
total transverse shell;
many-body Nmax;
zero-mode policy.
```

Every forbidden component must retain a source or C64 exact certificate.

Create:

```text
docs/next_level/c73_kinematic_quantum_number_report.json
```

---

# 11. Kinematic/color ordering adapters

Derive explicit bijections between:

```text
C73 raw kinematic-major product-color order;

C72 24-row product-color order;

C73 physical kinematic-major triplet order;

C72 3-column retained-triplet order.
```

Do not assume NumPy or library Kronecker ordering.

Construct:

\[
\mathcal P_{\rm raw},
\qquad
\mathcal P_{\rm phys}.
\]

Verify:

```text
bijection;
inverse;
basis-ID preservation;
deterministic hash;
serial/parallel/restart identity.
```

Create:

```text
docs/next_level/c73_kinematic_color_permutation.json
docs/next_level/c73_kinematic_color_permutation_validation.json
```

---

# 12. Full physical color-triplet embedding

Construct the executable physical map:

\[
J_R^{\rm phys}:
\mathcal H_R^{\rm CM=0,triplet}
\longrightarrow
\mathcal H_R^{\rm raw,kin\otimes(3\otimes8)}.
\]

Prove:

\[
J_R^{\rm phys}
=
\mathcal P_{\rm raw}
\left(
J_R^{\rm kin}\otimes U_3
\right)
\mathcal P_{\rm phys}^{-1}.
\]

Construct:

\[
P_R^{\rm phys},
\qquad
\Pi_R^{\rm CM,3}
=
J_R^{\rm phys}P_R^{\rm phys}.
\]

Use factorized sparse/matrix-free representations as the primary full-scale implementation.

Require:

\[
P_R^{\rm phys}J_R^{\rm phys}=I.
\]

Validate:

```text
projector Hermiticity;
projector idempotence;
rank and trace equal physical triplet dimension;
exact CM-ground identity;
zero CM-excited leakage;
zero anti-sextet leakage;
zero 15 leakage;
CM/color projector commutation where claimed;
triplet-basis covariance;
factorized/matrix-free agreement;
certified propagated bounds.
```

Create:

```text
docs/next_level/c73_exact_physical_qg_embedding.json
docs/next_level/c73_exact_physical_qg_projection.json
docs/next_level/c73_physical_image_projector.json
docs/next_level/c73_color_cm_factorization_report.json
docs/next_level/c73_physical_embedding_validation.json
docs/next_level/c73_physical_matrix_free_report.json
```

---

# 13. Threshold-free physical support

Represent complete physical support as an exact factorized relation.

For every queried physical/raw pair return exactly one status:

```text
ZERO_BY_C64_TM_SHELL_RULE;

ZERO_BY_C64_TM_M_RULE;

ZERO_BY_C64_TM_ALGEBRAIC_CANCELLATION;

ZERO_BY_C72_EXACT_COLOR_RULE;

ZERO_BY_CM_OR_BASIS_SELECTION;

NONZERO_EXACT_TM_AND_EXACT_COLOR;

NONZERO_EXACT_TM_CERTIFIED_COLOR;

UNDECIDABLE_BLOCKING.
```

The complete all-pairs table need not be serialized densely. The authoritative representation may be:

```text
C64 exact support relation;
C72 exact support relation;
CM-ground selection;
basis permutations;
sparse nonzero indices;
deterministic exact-membership API.
```

A positive embedding gate requires no undecidable pair in the represented domain.

Do not use:

```text
abs(value) > tolerance;
historical sparse storage;
the C57 1e-12 mask;
evaluated C53 values;
post-hoc pruning.
```

Create:

```text
docs/next_level/c73_exact_physical_support.json
docs/next_level/c73_exact_physical_support_validation.json
```

---

# 14. Certified numerical propagation

Propagate C64 and C72 entry-level bounds through:

```text
complex conjugation;
products;
Kronecker composition;
basis permutation;
sparse accumulation;
embed/project actions;
projector actions.
```

For a product of certified entries \(a\pm\delta a\) and \(b\pm\delta b\), use a rigorous enclosure at least as conservative as:

\[
\delta(ab)
\le
|a|\,\delta b
+
|b|\,\delta a
+
\delta a\,\delta b,
\]

with the exact complex norm/enclosure convention recorded.

For sums, propagate a rigorous accumulation bound rather than relying on observed cancellation.

Export deterministic representations of:

```text
J_kin and P_kin sparse blocks;
factorized Pi_CM;
factorized J_phys and P_phys;
factorized Pi_CM,3;
exact support indices;
entry/action error metadata;
matrix-free reconstruction metadata.
```

Require:

```text
all exact values enclosed;
exact zeros stored as literal zeros;
no exact nonzero pruned;
precision changes preserve exact support;
all round trips and projector identities close within the
propagated certified bounds.
```

Create:

```text
docs/next_level/c73_error_propagation_contract.json
docs/next_level/c73_certified_numerical_embedding_export.json
docs/next_level/c73_precision_stability_report.json
docs/next_level/c73_certified_invariant_report.json
```

---

# 15. Public physical-embedding API

Provide public operations equivalent to:

```python
load_qg_embedding_package(resolution: str)

physical_qg_raw_components(
    physical_basis_id: str,
    resolution: str,
)

physical_support_status(
    physical_basis_id: str,
    raw_basis_id: str,
    resolution: str,
)

embed_physical_qg_to_raw(
    vector,
    resolution: str,
    precision: int | None = None,
)

project_raw_qg_to_physical(
    vector,
    resolution: str,
    precision: int | None = None,
)

apply_cm_ground_projector(
    raw_kinematic_vector,
    resolution: str,
)

apply_physical_triplet_projector(
    raw_product_color_vector,
    resolution: str,
)
```

Return objects must expose:

```text
C64 and C72 package roots;
exact support status;
C64 TM ancestry;
C72 color ancestry;
basis and ordering identities;
CM and triplet ancestry;
certified midpoint and propagated bound.
```

The API must never call C62/C66/C68/C70/C72 builders, regenerate package data, or infer support from magnitude.

Create:

```text
docs/next_level/c73_api_contract.json
docs/next_level/c73_api_validation.json
```

---

# 16. Historical C47 adapter

Derive—not fit—the exact adapter between the C73 kinematic basis and the historical C47 quadrature/argmax basis.

Separate:

```text
basis permutation;
global analytic polar phase;
historical row phase;
quadrature residual;
possible subspace mismatch.
```

Prove orientation and isometry/unitarity where applicable.

Create:

```text
docs/next_level/c73_historical_basis_adapter.json
docs/next_level/c73_historical_basis_adapter_validation.json
```

---

# 17. C47 reconciliation

After applying only the derived adapter, compare the exact C73 kinematic map with the historical C47 map.

Reconfirm through C64 that the historical:

```text
4,032 / 15,840 / 48,048
```

subthreshold residues are exact cross-\(m\) zeros.

Audit all historical above-threshold support.

Classify:

```text
C47_NUMERICAL_EMBEDDING_VALID_IN_HISTORICAL_BASIS;

C47_VALUES_VALID_EXACT_SUPPORT_CERTIFICATE_SUPERSEDED;

C47_BASIS_ADAPTER_REQUIRED_NO_PHYSICS_CHANGE;

C47_NUMERICAL_EMBEDDING_SUPERSESSION_REQUIRED;

C47_IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c73_c47_embedding_reconciliation.json
docs/next_level/c73_c47_support_reconciliation.json
docs/next_level/c73_c47_basis_status_decision.json
```

---

# 18. C52 and C53 impact audits

These audits occur only after the C73 physical embedding and its hashes are frozen.

## 18.1 C52

Audit:

```text
kinematic basis covariance;
support ancestry;
source-derived holdouts;
sparse/matrix-free action;
adjoint and dimensional contracts.
```

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ADAPTER_ONLY_NO_OPERATOR_REBUILD;

SUPPORT_CERTIFICATE_SUPERSEDED_VALUES_VALID;

NUMERICAL_OPERATOR_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

## 18.2 C53

Using C73 kinematics and immutable C72 color authority, audit:

```text
full-product/reduced-triplet equality;
triplet basis covariance;
support ancestry;
generated adjoint;
matrix-free action;
source-derived holdouts.
```

Stored C52/C53 numerical arrays are comparisons only, never construction authority.

Create:

```text
docs/next_level/c73_c52_impact_audit.json
docs/next_level/c73_c52_basis_covariance_report.json
docs/next_level/c73_c53_impact_audit.json
docs/next_level/c73_vertex_basis_covariance_report.json
```

A required C52/C53 numerical rebuild takes precedence over all downstream branches.

---

# 19. C57 impact audit

Reconstruct corresponding-propagating support from:

```text
source endpoint rules;
the exact C73 physical embedding;
the fixed C57 operation order;
IFREG-CORRESPONDING-PROPAGATING-SUPPORT.
```

Do not use C53 numerical values to define support.

Audit:

```text
canonical positions:
    312 / 510 / 756;

conditional unions:
    1,216 / 2,320 / 3,936;

candidate envelopes:
    2,304 / 4,400 / 7,488.
```

Report exact counts, symmetric differences, basis-ID-only differences, and any new or removed exact edges.

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ID_ADAPTER_ONLY;

SUPPORT_CERTIFICATE_SUPERSEDED_NO_SUPPORT_CHANGE;

SUPPORT_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c73_c57_support_impact_audit.json
```

---

# 20. C58 impact audit

Audit:

```text
IFNORM2-ORDERED-JOINT-SUPPORT;

admitted modes:
    4,216 / 8,330 / 14,484;

q-sector primitive:
    6 x 6 with six nonzero entries;

qg scope:
    IFNORM2-SECTOR-SPECIFIC-COUNTERTERM-ONLY.
```

Determine whether C73 changes pair identities, mode ledgers, counts, the q-sector primitive, or qg scope.

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ID_ADAPTER_ONLY;

SUPPORT_LEDGER_REBUILD_REQUIRED_VALUES_UNCHANGED;

NUMERICAL_CONTRACTION_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c73_c58_impact_audit.json
```

---

# 21. C59/C60 continuation audit

Determine whether C73 now supplies:

```text
exact physical-to-raw qg components;
exact raw-to-physical projection;
exact CM-ground and triplet ancestry;
exact projected-cancellation semantics;
threshold-free support membership;
authenticated public embed/project APIs.
```

Classify:

```text
C60_BLOCKER_RESOLVED_READY_FOR_IFSUPPORT2;

ADDITIONAL_ENDPOINT_INPUT_BLOCKING;

UPSTREAM_IFREG_SUPERSESSION_REQUIRED;

UPSTREAM_VERTEX_SUPERSESSION_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c73_c59_c60_continuation_audit.json
```

---

# 22. Exact continuation decision

Select exactly one branch.

## 22.1 Favorable branch

Issue:

```text
C73_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY
```

Required:

```text
physical embedding complete;
C52/C53 require no numerical rebuild;
C57 requires no support rebuild;
C58 requires no contraction rebuild;
C60 blocker resolved.
```

Next:

> **C74/IFSUPPORT2 — source-ordered direct-contact endpoint and intermediate-\(q\) witness support**

## 22.2 IFREG supersession

Issue:

```text
C73_EXACT_QG_EMBEDDING_READY_IFREG_SUPERSESSION_REQUIRED
```

Next:

> **C74/IFREG3 — rebuild C57/C58 support and self-induced-inertia ancestry from C73**

## 22.3 Vertex supersession

Issue:

```text
C73_EXACT_QG_EMBEDDING_READY_VERTEX_SUPERSESSION_REQUIRED
```

Next:

> **C74/VERTEX5 — rebuild C52/C53 in the exact C73 basis and revalidate downstream support**

This branch takes precedence over IFREG supersession.

## 22.4 Descendant impact unresolved

Issue:

```text
C73_QG_EMBEDDING_DESCENDANT_IMPACT_INCOMPLETE
```

This status means the physical embedding is complete and retained, but at least one descendant audit remains unresolved.

Next:

> **C74/QGIMPACT3 — complete basis-covariance and descendant-impact closure**

Do not discard or relabel a completed physical embedding as absent merely because a descendant comparison remains unresolved.

---

# 23. Concrete no-go branches

## A. Actual C64 public import failure

```text
C73_QGEMBED_C64_IMPORT_INCOMPLETE
```

Next:

> **C74/QGTM5 — repair only the specifically identified C64 public-import defect**

## B. Actual C72 public import failure

```text
C73_QGEMBED_C72_IMPORT_INCOMPLETE
```

Next:

> **C74/QGCOLOR6 — repair only the specifically identified C72 public-import defect**

## C. CM-ground selection or orientation failure

```text
C73_QG_CM_GROUND_EMBEDDING_INCOMPLETE
```

Next:

> **C74/QGCM7 — exact CM selection, orientation, adjoint, and projector closure**

## D. Kinematic/color composition failure

```text
C73_QG_KINEMATIC_COLOR_COMPOSITION_INCOMPLETE
```

Next:

> **C74/QGCOMPOSE4 — exact ordering, factorization, and full-map closure**

## E. Numerical certification failure

```text
C73_QG_EMBEDDING_NUMERICAL_CERTIFICATION_FAILED
```

Next:

> **C74/QGNUM7 — propagated bounds and executable-projector certification**

## F. Historical adapter failure after embedding completion

```text
C73_QG_HISTORICAL_BASIS_ADAPTER_INCOMPLETE
```

Next:

> **C74/QGADAPT6 — exact phase/permutation adapter and C47 reconciliation**

---

# 24. Runtime package

Use:

```text
data/runtime/c73_qgembed7/
```

Store deterministic content-addressed objects for each resolution:

```text
basis manifests;
CM-ground selection;
block-sparse/factorized J_kin and P_kin;
factorized Pi_CM;
kinematic/color permutations;
factorized J_phys and P_phys;
factorized Pi_CM,3;
exact support relations and sparse indices;
certified values and propagated bounds;
matrix-free metadata;
historical adapter;
impact records.
```

Create an authenticated package index and root inside C73 as part of the embedding deliverable. Do not split package authentication into a later work package.

Create:

```text
docs/next_level/c73_numerical_object_inventory.json
docs/next_level/c73_runtime_completeness_report.json
docs/next_level/c73_deterministic_reconstruction_report.json
```

Run repeated, clean, serial, supported parallel, and restart reconstructions.

---

# 25. Count-once, isolation, and mutations

Report:

```text
raw and physical dimensions;
CM-ground selection counts;
exact TM nonzero count;
exact color nonzero count;
factorized physical nonzero count;
missing components;
duplicate components;
undecidable support pairs;
unresolved descendant impacts.
```

Every nonzero physical component must have exactly one ancestry:

```text
C64 exact TM entry
 -> CM-ground selection
 -> kinematic identity
 -> C72 exact color entry
 -> ordering adapter
 -> C73 physical component and bound.
```

Prove construction independence from:

```text
historical C47 quadrature values;
historical 1e-12 threshold;
C47 raw tuple values;
C52/C53 stored numerical matrices;
C57/C58 stored numerical values;
raw_emission_E;
historical C53 projectors;
C66/C68/C70/C72 builders;
ART25 files.
```

Create at least **384 focused live mutations** of actual imported identities, basis records, CM selections, orientation records, sparse/factorized operators, support relations, bounds, adapters, and impact decisions.

Create:

```text
docs/next_level/c73_component_ancestry_ledger.json
docs/next_level/c73_count_once_report.json
docs/next_level/c73_isolation_report.json
docs/next_level/c73_regression_report.json
```

---

# 26. Required deliverables

Create at least:

```text
docs/next_level/c73_implementation_report.md
docs/next_level/c73_api.md
docs/next_level/c73_derivation_authority_manifest.json
docs/next_level/c73_input_fidelity_audit.json

docs/next_level/c73_c64_import_report.json
docs/next_level/c73_c72_import_report.json
docs/next_level/c73_calculation_plan.json
docs/next_level/c73_holdout_plan.json

docs/next_level/c73_raw_qg_basis_manifest.json
docs/next_level/c73_relative_cm_basis_manifest.json
docs/next_level/c73_physical_kinematic_basis_manifest.json
docs/next_level/c73_raw_product_color_basis_manifest.json
docs/next_level/c73_physical_triplet_basis_manifest.json
docs/next_level/c73_basis_order_manifest.json

docs/next_level/c73_cm_ground_selection_manifest.json
docs/next_level/c73_cm_ground_selection_validation.json
docs/next_level/c73_tm_orientation_contract.json
docs/next_level/c73_tm_orientation_validation.json

docs/next_level/c73_exact_kinematic_embedding.json
docs/next_level/c73_exact_kinematic_projection.json
docs/next_level/c73_cm_image_projector.json
docs/next_level/c73_kinematic_embedding_validation.json
docs/next_level/c73_kinematic_matrix_free_report.json
docs/next_level/c73_kinematic_quantum_number_report.json

docs/next_level/c73_kinematic_color_permutation.json
docs/next_level/c73_kinematic_color_permutation_validation.json
docs/next_level/c73_exact_physical_qg_embedding.json
docs/next_level/c73_exact_physical_qg_projection.json
docs/next_level/c73_physical_image_projector.json
docs/next_level/c73_color_cm_factorization_report.json
docs/next_level/c73_physical_embedding_validation.json
docs/next_level/c73_physical_matrix_free_report.json

docs/next_level/c73_exact_physical_support.json
docs/next_level/c73_exact_physical_support_validation.json
docs/next_level/c73_error_propagation_contract.json
docs/next_level/c73_certified_numerical_embedding_export.json
docs/next_level/c73_precision_stability_report.json
docs/next_level/c73_certified_invariant_report.json

docs/next_level/c73_api_contract.json
docs/next_level/c73_api_validation.json

docs/next_level/c73_historical_basis_adapter.json
docs/next_level/c73_historical_basis_adapter_validation.json
docs/next_level/c73_c47_embedding_reconciliation.json
docs/next_level/c73_c47_support_reconciliation.json
docs/next_level/c73_c47_basis_status_decision.json

docs/next_level/c73_c52_impact_audit.json
docs/next_level/c73_c52_basis_covariance_report.json
docs/next_level/c73_c53_impact_audit.json
docs/next_level/c73_vertex_basis_covariance_report.json
docs/next_level/c73_c57_support_impact_audit.json
docs/next_level/c73_c58_impact_audit.json
docs/next_level/c73_c59_c60_continuation_audit.json

docs/next_level/c73_descendant_dependency_graph.json
docs/next_level/c73_inherited_impact_summary.json
docs/next_level/c73_supersession_plan.json

docs/next_level/c73_component_ancestry_ledger.json
docs/next_level/c73_count_once_report.json
docs/next_level/c73_isolation_report.json
docs/next_level/c73_numerical_object_inventory.json
docs/next_level/c73_runtime_completeness_report.json
docs/next_level/c73_deterministic_reconstruction_report.json
docs/next_level/c73_readiness_report.json
docs/next_level/c73_source_sufficiency_decision.json
docs/next_level/c73_no_go_decision_tree.json
docs/next_level/c73_missing_calculation_specification.md
docs/next_level/c73_regression_report.json
```

Create exactly one next-package import contract corresponding to the selected continuation branch.

Add implementation under:

```text
src/deuteron_wigner/bridge/qgembed7/
```

or the exact repository-equivalent package.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 27. Acceptance criteria

C73 is complete only when:

1. Baseline commit `369e95e1004c2f896f8a41e5c3270f7644e3391d` reproduces.
2. Both required untracked paths remain untouched.
3. C64 and C72 positive statuses remain explicit.
4. C64 and C72 historical artifacts remain unchanged.
5. Both public package roots and all authority domains verify.
6. Neither package is regenerated or bypassed.
7. Exact raw and physical basis manifests exist.
8. Physical dimensions are derived and verified.
9. CM-ground selection is exact and complete.
10. No CM-excited state is selected.
11. TM orientation is proved.
12. \(J_{\rm kin}\) and \(P_{\rm kin}\) execute.
13. \(P_{\rm kin}J_{\rm kin}=I\) closes.
14. \(\Pi_{\rm CM}\) executes and is Hermitian/idempotent.
15. CM-excited leakage is zero.
16. Quantum-number closure passes.
17. Kinematic/color adapters are explicit and bijective.
18. \(J_{\rm phys}\) and \(P_{\rm phys}\) execute.
19. \(P_{\rm phys}J_{\rm phys}=I\) closes.
20. \(\Pi_{\rm CM,3}\) executes and is Hermitian/idempotent.
21. Anti-sextet and 15 leakage are zero.
22. Support is threshold free.
23. No exact nonzero is pruned.
24. No represented pair remains undecidable.
25. C64/C72 bounds are propagated rigorously.
26. All round trips and projector identities lie within certified bounds.
27. Block-sparse/factorized and independent matrix-free routes agree.
28. No dense full projector is required.
29. The historical adapter is derived, not fitted.
30. C47 reconciliation is typed.
31. C52 and C53 impacts are typed after embedding construction.
32. C57 and C58 impacts are typed.
33. C59/C60 continuation is typed.
34. Exactly one continuation branch is selected.
35. A completed embedding is retained when descendant impact is unresolved.
36. Every physical component has complete ancestry.
37. Missing and duplicate counts are zero.
38. Runtime package and public embedding API reconstruct deterministically.
39. Isolation and at least 384 focused mutations pass.
40. No endpoint, witness, contact value/matrix, complete instantaneous-fermion, Wilson/TMD, matching, inference, or production object is created.
41. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
42. `MSHT20_REP/` remains untouched.
43. The working tree is clean except for the two pre-existing untracked paths.
44. A local completion commit is created and not pushed.

A rigorous no-go or supersession result is valid. Do not weaken authenticated imports, exact CM selection, TM orientation, exact color composition, threshold-free support, numerical certification, or descendant-impact accounting.

---

# 28. Final Codex response

Report:

- starting and final commits;
- untouched untracked paths;
- C64 and C72 package roots and imported authority counts;
- raw-kinematic, relative/CM, physical-kinematic, raw-product-color, and physical-triplet dimensions;
- CM-ground selection counts and hashes;
- TM orientation and proof identities;
- \(J_{\rm kin}\), \(P_{\rm kin}\), and \(\Pi_{\rm CM}\) factorization, support/nnz counts, ranks, traces, and certified residuals;
- quantum-number closure;
- kinematic/color adapter hashes;
- \(J_{\rm phys}\), \(P_{\rm phys}\), and \(\Pi_{\rm CM,3}\) factorization, support counts, ranks, traces, and certified residuals;
- CM-excited, anti-sextet, and 15 leakage;
- factorized versus matrix-free residuals;
- exact support-class counts;
- propagated-bound maxima and interval convention;
- historical C47 adapter and reconciliation;
- C52 and C53 impact decisions;
- C57 exact positions, unions, envelopes, and differences;
- C58 admitted-mode and primitive impact;
- C59/C60 continuation status;
- descendant supersession decision;
- ancestry, missing, duplicate, undecidable, and unresolved-impact counts;
- deterministic reconstruction;
- isolation and mutation results;
- exact readiness/no-go/supersession status;
- exact next branch;
- confirmation that no endpoint/witness relation, contact support/value/matrix, complete instantaneous-fermion operator, TMD/matching, inference, or production object was created;
- confirmation that nothing was pushed.

Do not describe a guessed orientation, thresholded support, assumed Kronecker order, dense-matrix omission, fitted basis adapter, uncertified round trip, or unaudited descendant as a complete physical \(qg\) embedding.
