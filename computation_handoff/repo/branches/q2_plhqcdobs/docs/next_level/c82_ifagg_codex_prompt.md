# C82/IFAGG Codex Work Package

## Title

**Total C78-to-C80 contact-coordinate bridge and evaluated projected coefficients: exact witness-to-raw-coordinate realization, factor-ownership closure, lazy certified aggregation inputs, and C83 bare contact-matrix handoff**

## Authoritative baseline

Start from the clean local C81 completion commit:

```text
8b854cd49f471ab642acc0de0dee45b7b76ef49e
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "8b854cd49f471ab642acc0de0dee45b7b76ef49e"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c82_ifagg_contract.md
```

If that contract freezes exact status names or narrower scientific
boundaries, preserve them. This prompt supplies the executable derivation,
validation, and public-interface plan; it does not authorize weakening the
committed contract.

The baseline is authoritative only when it contains and reproduces:

```text
C77_EXACT_SOURCE_CHAIN_DERIVED_QG_EMBEDDING_READY

C78_SOURCE_DERIVED_IFERM_CONTACT_SUPPORT_READY

C80_EXACT_SOURCE_CHAIN_DERIVED_IFCONTACT_KERNEL_EVALUATOR_READY

C81_IFCONTACT_PAIR_AGGREGATION_INCOMPLETE
```

and the exact C81 finding:

```text
C78:
    exposes source-ordered physical-pair support and symbolic KAPPA
    coordinate domains;
    marks the projected coefficients NOT_EVALUATED;

C80:
    evaluates a caller-supplied canonical raw contact coordinate;
    does not publish a total map from the C78 symbolic coordinate
    domain to its raw-coordinate schema;

consequence:
    no legal product c_kappa K_kappa exists;
    no product bound exists;
    no P-minus or M-squared sparse entry exists;
    all 154,830 supported physical pairs remain unaggregated;

not created:
    sparse matrix;
    matrix-free matrix action;
    physical coupling;
    counterterm;
    C53 sequential contribution;
    C58 self-induced-inertia contribution.
```

Verify every actual count, schema, status, package root, public operation,
and scientific identity from the repository. This prompt is not numerical
authority.

Create a local completion commit. Do not push.

---

# 1. Authority policy

C82 is an exact project-derived composition layer.

External primary authority is already frozen for:

```text
the C55 direct instantaneous-fermion operator;
field ordering and fermion signs;
the C57 conditional graph regulator;
the C77 physical qg embedding;
the C80 finite-cell four-field kernel.
```

No new external paper is required for:

```text
constructing repository-owned coordinate IDs;
mapping a C78 witness leaf to the exact C80 raw coordinate;
evaluating the physical bra/ket projection coefficient;
grouping witness leaves by exact C80 coordinate identity;
constructing a lazy total map;
serializing the map and exposing a public API.
```

Do not issue an external-authority no-go for a repository-specific
coordinate crosswalk, iterator, pair-local coefficient, or content hash.

Fail closed only if:

```text
the frozen C78 and C80 schemas are physically incompatible;
a required source factor has no declared owner;
the same factor would be counted twice;
a C78 witness cannot determine a unique raw C80 coordinate;
or independent exact reconstruction routes disagree.
```

---

# 2. Scientific purpose and stopping point

C82 must close the missing composition object between C78 and C80.

For every C78 source-ordered witness leaf \(\omega\), construct a total map

\[
\chi_R:\omega\longmapsto \xi_R(\omega),
\]

where \(\xi_R(\omega)\) is one canonical raw contact coordinate accepted by
the immutable C80 evaluator.

For every C78-supported physical pair \((b,k)\), construct the exact or
certified projected coefficient grouped by canonical C80 coordinate:

\[
C_{bk}^{[\xi]}
=
\sum_{\omega\in\mathcal W_{bk}:\,\chi_R(\omega)=\xi}
J^{*}_{b,r_b(\omega)}
\,s_\omega\,
J_{r_k(\omega),k}.
\]

Here:

```text
J_b,r_b:
    immutable C77/C78 physical-bra-to-raw coefficient;

J_r_k,k:
    immutable C77/C78 raw-to-physical-ket coefficient;

s_omega:
    only the source-order, fermion, endpoint, and projection sign
    not already owned by the C80 kernel;

xi:
    a complete canonical C80 raw contact coordinate.
```

The equation above is schematic. The exact C78/C77 coefficient convention,
metric, conjugation, and endpoint sign records are authoritative.

C82 must produce:

```text
a total witness-leaf-to-C80-coordinate map;

a reversible relation from every C78 symbolic KAPPA object to its
canonical leaf coordinates and C80 coordinates;

an evaluated projected coefficient and bound for every pair-local
C80 coordinate class;

a complete factor-ownership certificate;

a lazy immutable public aggregation-input API;

and a C83 import contract.
```

C82 must not multiply the evaluated projected coefficients by C80 kernel
values to form physical matrix elements.

C82 must not produce:

```text
a contact P-minus matrix;
a contact M-squared matrix;
a matrix-free contact action;
a physical g_s or alpha_s;
a counterterm;
the complete instantaneous-fermion operator;
a C53 propagated contribution;
a C58 contribution;
a TMD, matching, fit, inference, or production object.
```

The preferred positive semantic status is:

```text
C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY
```

If `c82_ifagg_contract.md` freezes a different exact spelling, preserve that
spelling and record an explicit semantic alias.

The favorable continuation is:

> **C83/IFCONTACT4 — multiply the immutable C82 projected coefficients by immutable C80 kernel values and assemble the certified sparse and matrix-free bare direct-contact matrices**

---

# 3. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C77:
    canonical raw and physical qg basis identities;
    physical-to-raw and raw-to-physical maps;
    exact support classes;
    metric and conjugation conventions;
    certified component values and bounds;

C78:
    physical supported-pair domain;
    absorption and emission endpoint paths;
    retained-q witness triples;
    symbolic KAPPA vocabulary and factorization;
    source-order signs;
    projected-cancellation records;
    support ancestry;
    public API and runtime package;

C80:
    ContactKernelCoordinate schema;
    required raw-coordinate fields;
    factor ownership;
    exact coordinate equivalence;
    evaluator status;
    coordinate validation;
    public API and runtime package;

C81:
    public composition audit;
    exact missing-map diagnosis;
    c82_ifagg_contract.md;
    non-substitution controls.
```

Read C53 and C58 only for negative controls after C82 construction objects
are frozen.

Create:

```text
docs/next_level/c82_derivation_authority_manifest.json
docs/next_level/c82_input_fidelity_audit.json
```

---

# 4. Freeze imports

Consume C77/C78 and C80 only through their authenticated immutable public
interfaces.

Freeze:

```text
all package/root identities;

all three resolution identities;

physical qg basis orders;

154,830 supported physical-pair identities;

all endpoint-path and witness identities;

all symbolic KAPPA domain identities;

all C77 embedding coefficient identities and bounds;

the complete C80 raw-coordinate schema;

all C80 exact coordinate-equivalence identities;

all source-order, regulator, zero-mode, and convention identities.
```

Issue:

```text
C82_INPUTS_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c82_input_freeze.json
```

After this freeze:

```text
do not change C78 support;
do not merge coordinates after observing numerical kernel values;
do not change C80's coordinate schema;
do not add a physical coupling;
do not repair a missing map by using C53 or C58.
```

---

# 5. Factor-ownership audit

Before constructing any map, assign every multiplicative factor to exactly
one owner.

Create an exhaustive ledger with at least the following candidate factors,
adapted to the actual schemas:

```text
physical bra embedding coefficient;
physical ket embedding coefficient;
basis metric or adjoint factor;
fermion reordering sign;
absorption/emission source-order sign;
longitudinal Kronecker identity;
inverse-partial-plus denominator;
quark spinor factor;
gluon polarization factor;
ordered SU(3) color product;
four-HO overlap;
finite-cell normalization;
P-minus to M-squared conversion;
g_s-squared factor.
```

The intended division is:

```text
C82 projected coefficient:
    physical projection amplitudes;
    metric/conjugation factors;
    witness multiplicity;
    source-order/combinatorial signs not inside C80;

C80 kernel:
    longitudinal factor and denominator;
    spin/polarization numerator;
    ordered color kernel;
    four-HO overlap;
    finite-cell normalization;
    P-minus-to-M-squared conversion;
    coefficient of factored g_s-squared.
```

However, the actual C78 and C80 contracts are authoritative. If C78 stores
a symbolic factor also owned numerically by C80, C82 must convert that
symbol into a coordinate label rather than multiply it twice.

Require:

```text
every factor has exactly one owner;
no factor is unowned;
no factor is double counted;
the product of C82 coefficient and C80 kernel reconstructs the
frozen source-level term.
```

Create:

```text
docs/next_level/c82_factor_ownership_contract.json
docs/next_level/c82_factor_ownership_validation.json
```

A factor-ownership contradiction is a legitimate fail-closed result.

---

# 6. Canonical C78 witness-leaf schema

The C78 symbolic KAPPA domain may be factorized or partially symbolic.
Define a canonical immutable leaf record representing one complete
source-ordered raw contribution.

Each leaf must retain all fields actually required to determine the C80
coordinate and the projected coefficient, including as applicable:

```text
resolution;
physical bra ID;
physical ket ID;
witness ID;
retained-q intermediate ID;
raw bra qg component ID;
raw ket qg component ID;
outgoing/incoming quark mode IDs;
outgoing/incoming gluon mode IDs;
quark helicities;
gluon helicities/polarizations;
fundamental and adjoint color IDs;
ordered color-generator identity;
longitudinal channel;
zero-mode/boundary identity;
four transverse-HO mode IDs;
source-order and fermion-sign identity;
C78 symbolic KAPPA parent ID;
canonical witness-leaf ID.
```

Do not materialize all leaves as one dense array. The authoritative domain
may be represented by deterministic nested iterators over:

```text
pair -> witness -> endpoint paths -> factor coordinates.
```

The iterator must nevertheless be total, deterministic, reversible, and
countable.

Create:

```text
docs/next_level/c82_witness_leaf_schema.json
docs/next_level/c82_witness_leaf_domain_validation.json
```

---

# 7. Canonical C80 coordinate construction

Implement a pure deterministic constructor equivalent to:

```python
c80_coordinate_from_witness_leaf(
    leaf: ContactWitnessLeaf,
) -> ContactKernelCoordinate
```

The constructor must populate every required C80 field from authenticated
C78/C77 identities.

It may use only:

```text
identity adapters;
canonical basis crosswalks;
declared source-order maps;
declared helicity/color/mode conversions;
exact resolution metadata.
```

It must not use:

```text
a kernel numerical value;
floating equality;
array position without a basis identity;
C53 numerical values;
C58 values;
a guessed default;
or a lossy placeholder.
```

For every constructed coordinate:

```text
validate it through the public C80 coordinate validator;
verify its canonical ID;
verify its exact coordinate-equivalence class;
verify that its evaluator has a terminal status.
```

Create:

```text
docs/next_level/c82_c78_to_c80_coordinate_map.json
docs/next_level/c82_coordinate_map_validation.json
```

The JSON need not list hundreds of millions of leaves. It may contain the
complete factorized construction, counts, hashes, and selected explicit
records, provided the public API can enumerate or query every leaf.

---

# 8. Totality, single-valuedness, and reverse closure

Prove:

```text
every valid C78 witness leaf maps to exactly one C80 raw coordinate;

no witness leaf maps to zero coordinates;

no witness leaf maps ambiguously to multiple nonequivalent C80
coordinates;

every C80 coordinate used by C82 has at least one C78 witness-leaf
preimage;

every mapped coordinate is valid under the frozen C80 schema;

the reverse relation reconstructs the original pair, witness,
endpoint, and KAPPA ancestry.
```

Many witness leaves may map to one exact C80 coordinate. This is expected
and is the basis for class aggregation.

A single symbolic C78 KAPPA parent may expand to multiple leaf coordinates
only when the expansion is explicit, finite/factorized, reversible, and
source preserving.

Create:

```text
docs/next_level/c82_coordinate_map_totality_report.json
docs/next_level/c82_coordinate_reverse_closure_report.json
```

Report exact counts or exact factorized count formulas by resolution.

---

# 9. Evaluate the projected coefficient

For each witness leaf evaluate the coefficient owned by C82.

Use the exact C77/C78 projection convention, including:

```text
bra conjugation;
ket orientation;
basis metric;
physical/raw ordering;
source-order sign;
fermion sign;
endpoint multiplicity;
and any exact projection factor not owned by C80.
```

Implement two independent routes.

## Route A — C78 endpoint/witness route

```text
load the C78 physical pair;
iterate its source-ordered witnesses and endpoint paths;
load the exact/certified C77 embedding amplitudes;
apply the frozen sign and metric records;
produce the leaf coefficient.
```

## Route B — independent C77 raw expansion

```text
expand the physical bra and ket through the public C77 embedding;
select the exact raw endpoint tuple independently;
apply the frozen source monomial and retained-q identity;
construct the same coefficient without consuming Route A's
assembled leaf coefficient.
```

Require agreement of:

```text
leaf identity;
coordinate identity;
exact zero/nonzero status;
symbolic expression where available;
numerical midpoint;
certified bound.
```

Create:

```text
docs/next_level/c82_projected_coefficient_contract.json
docs/next_level/c82_projected_coefficient_validation.json
docs/next_level/c82_independent_coefficient_reconstruction.json
```

---

# 10. Pair-local aggregation by exact C80 coordinate identity

For each supported pair \((b,k)\), group leaf coefficients by the exact C80
coordinate identity:

\[
C_{bk}^{[\xi]}
=
\sum_{\omega:\chi(\omega)=\xi}
c_{bk,\omega}.
\]

Requirements:

```text
grouping uses the C80 canonical coordinate/equivalence identity;
no floating kernel value is inspected;
exact expressions are summed before numerical evaluation;
intervals are accumulated conservatively;
exact cancellation is distinguished from an interval containing
zero;
all witness ancestry remains reversible.
```

Assign one terminal coefficient status:

```text
NONZERO_EXACT_PROJECTED_COEFFICIENT;

NONZERO_CERTIFIED_PROJECTED_COEFFICIENT_INTERVAL_EXCLUDES_ZERO;

EXACT_ZERO_BY_PROJECTED_COEFFICIENT_CANCELLATION;

CERTIFIED_PROJECTED_COEFFICIENT_INTERVAL_INCLUDES_ZERO_NO_EXACT_ZERO;

UNAVAILABLE_BLOCKING.
```

Create:

```text
docs/next_level/c82_pair_coordinate_coefficient_manifest.json
docs/next_level/c82_pair_coordinate_coefficient_status.json
docs/next_level/c82_pair_coordinate_aggregation_validation.json
```

A favorable gate requires:

```text
every supported pair has at least one terminal pair-coordinate
record;

UNAVAILABLE_BLOCKING = 0.
```

If exact evaluation removes all coordinate coefficients from a C78
structurally supported pair, do not silently retain or delete the pair.
Issue the support-supersession branch defined below.

---

# 11. Bound propagation

For products internal to the projected coefficient, use rigorous certified
arithmetic.

For a bra coefficient \(a\pm\delta a\) and ket coefficient
\(b\pm\delta b\), propagate at least:

\[
\delta(a^*b)
\le
|a|\,\delta b
+
|b|\,\delta a
+
\delta a\,\delta b.
\]

Add source-order signs and exact unit-modulus phases without inflating the
bound unnecessarily.

For sums:

```text
combine exact expressions first;
otherwise accumulate a rigorous enclosure;
do not use observed cancellation as certification;
do not call an interval containing zero an exact zero.
```

Create:

```text
docs/next_level/c82_projected_coefficient_bound_contract.json
docs/next_level/c82_projected_coefficient_bound_report.json
```

---

# 12. Low-shell composition pilot

Before full-domain closure, execute a frozen pilot containing:

```text
one diagonal supported physical pair;
one off-diagonal supported pair;
one pair with multiple witnesses mapping to one C80 coordinate;
one pair with witnesses mapping to several C80 coordinates;
one exact projected-coefficient cancellation when available;
one Hermitian-conjugate pair;
one pair at each resolution.
```

For each pilot report:

```text
physical pair;
C78 KAPPA/witness ancestry;
canonical witness leaves;
mapped C80 coordinates;
Route A and Route B coefficients;
pair-local class coefficients;
bounds;
terminal coefficient statuses;
C80 coordinate validation result.
```

Do not multiply by a C80 kernel value except as a non-authoritative
end-to-end holdout after all C82 objects are frozen.

Create:

```text
docs/next_level/c82_low_shell_aggregation_bridge_pilot.json
```

---

# 13. Factorized execution and resource policy

The C78 address spaces remain:

```text
28,606,464;
165,991,250;
697,394,304.
```

C82 must not materialize one record per Cartesian coordinate in memory or
Git.

Use a factorized representation such as:

```text
pair table;
witness table;
endpoint-path tables;
canonical coordinate constructor;
embedding-coefficient lookup;
exact coordinate-class map;
pair-local coefficient iterator.
```

Requirements:

```text
stream deterministic shards;
evaluate each embedding coefficient identity once per required
context;
cache exact coordinate adapters by content hash;
support restart from verified shards;
record exact logical counts;
record materialized record counts;
record compression ratio and peak memory;
reconstruct arbitrary leaf and pair queries through the public API.
```

Create:

```text
docs/next_level/c82_factorized_mapping_plan.json
docs/next_level/c82_resource_and_scaling_report.json
```

---

# 14. Hermitian-conjugation compatibility

C82 does not assemble a matrix, but it must prove that the aggregation
inputs support the source Hermitian map.

Construct the exact relation:

\[
(b,k,\xi,C_{bk}^{[\xi]})
\longleftrightarrow
(k,b,\bar\xi,[C_{bk}^{[\xi]}]^*).
\]

Validate:

```text
pair reversal;
witness reversal;
raw endpoint reversal;
C80 coordinate conjugation;
projected-coefficient conjugation;
bound equality or certified compatibility.
```

Do not create missing conjugate records by copying them. They must descend
from the independent C78/C77 routes.

Create:

```text
docs/next_level/c82_aggregation_hermiticity_input_report.json
```

---

# 15. Public immutable API

Use:

```text
src/deuteron_wigner/bridge/ifagg/
data/runtime/c82_ifagg/
```

or exact repository-equivalent paths.

Provide immutable operations equivalent to:

```python
load_ifcontact_aggregation_bridge(resolution)

witness_leaf(witness_leaf_id, resolution)

c80_coordinate_for_witness_leaf(witness_leaf_id, resolution)

projected_leaf_coefficient(witness_leaf_id, resolution)

pair_coordinate_contributions(bra_id, ket_id, resolution)

iterate_pair_coordinate_contributions(resolution)

validate_total_coordinate_map(resolution)
```

Each pair-coordinate contribution must expose:

```text
physical bra/ket IDs;
C80 coordinate ID and exact equivalence-class ID;
projected coefficient value/expression;
certified bound;
terminal coefficient status;
all C78 witness ancestry;
all C77 embedding ancestry;
factor-ownership identity;
g_s-squared remains absent from the coefficient.
```

The API must not return a contact matrix element.

All NumPy loads must explicitly use:

```python
allow_pickle=False
```

Reject object dtype, unsafe paths, unindexed files, writable arrays, and
mutable records.

Create:

```text
docs/next_level/c82_api_contract.json
docs/next_level/c82_api_validation.json
docs/next_level/c82_runtime_inventory.json
docs/next_level/c82_deterministic_reconstruction_report.json
```

---

# 16. C83 import contract

Define the exact immutable contract by which C83 consumes:

```text
C82 pair-coordinate contribution records;
C80 kernel evaluator records;
their exact shared coordinate IDs;
their coefficient/kernel factor ownership;
their bounds and value statuses;
their P-minus/M-squared convention identities;
their Hermitian-conjugation maps.
```

C83 must be able to form legally:

\[
C_{bk}^{[\xi]}\,
\widehat{\mathcal K}^{P^-}_{\xi}
\quad\text{and}\quad
C_{bk}^{[\xi]}\,
\widehat{\mathcal K}^{M^2}_{\xi}.
\]

C83 must not:

```text
reconstruct C82 coefficients from private fields;
guess a coordinate map;
merge coordinates by floating equality;
choose a physical coupling;
add C53 propagation;
add C58 self-induced inertia;
or solve a counterterm.
```

Create:

```text
docs/next_level/c82_c83_ifcontact4_import_contract.json
```

---

# 17. C53 and C58 non-substitution controls

After the C82 bridge and hashes are frozen:

```text
poison all C53 numerical vertex and propagator values;
poison all C58 numerical self-induced-inertia values.
```

Require C82 reconstruction to remain byte-identical.

Confirm:

```text
C53 supplies no coordinate or coefficient;
C58 supplies no coordinate or coefficient;
the direct-contact bridge remains a distinct monomial and sector.
```

Create:

```text
docs/next_level/c82_c53_c58_non_substitution_report.json
```

---

# 18. Isolation and mutation controls

Required failures include mutations of:

```text
C78 pair support;
witness ID;
retained-q intermediate;
raw endpoint ID;
C77 embedding coefficient;
bra conjugation;
basis metric;
source-order sign;
fermion sign;
C80 coordinate field;
coordinate equivalence ID;
factor ownership;
duplicate factor ownership;
missing factor ownership;
leaf-to-coordinate ambiguity;
reverse-map closure;
pair-local class grouping;
exact-zero classification;
bound accumulation;
Hermitian-conjugation map;
unsafe loading;
mutable return.
```

Create at least **384 focused live mutations** of actual leaf, map,
coefficient, class, bound, API, and runtime objects.

Create:

```text
docs/next_level/c82_isolation_report.json
docs/next_level/c82_regression_report.json
```

---

# 19. Readiness and continuation decisions

Select exactly one branch.

## 19.1 Favorable branch

Issue the exact positive status frozen by the committed C82 contract, with
preferred semantic identity:

```text
C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY
```

Required:

```text
factor ownership closes;
every C78 witness leaf has exactly one valid C80 coordinate;
the total and reverse maps close;
both projected-coefficient routes agree;
every pair-local coordinate has a terminal coefficient status;
every supported pair has at least one pair-coordinate record;
UNAVAILABLE_BLOCKING = 0;
no C78 support supersession is required;
Hermitian-conjugation compatibility closes;
public API/runtime/determinism/isolation/mutations pass.
```

Next:

> **C83/IFCONTACT4 — certified sparse and matrix-free bare direct-contact matrix assembly**

## 19.2 Factor ownership incomplete

```text
C82_IFAGG_FACTOR_OWNERSHIP_INCOMPLETE
```

Next:

> **C83/IFFACTOR — repair only the specifically identified missing or double-counted factor boundary between C78/C82 and C80**

## 19.3 Coordinate map incomplete

```text
C82_IFAGG_COORDINATE_MAP_INCOMPLETE
```

Next:

> **C83/IFCOORD — repair only the specifically identified non-total, ambiguous, invalid, or non-reversible C78-to-C80 coordinate mapping**

## 19.4 Projected coefficient incomplete

```text
C82_IFAGG_PROJECTED_COEFFICIENT_INCOMPLETE
```

Next:

> **C83/IFCOEFF — repair only the specifically identified C77 embedding, metric, sign, exact-expression, or bound defect**

## 19.5 C78 support supersession required

```text
C82_IFAGG_SUPPORT_SUPERSESSION_REQUIRED
```

Use only if exact evaluated projected coefficients remove every coordinate
from one or more C78 structurally supported pairs.

Next:

> **C83/IFSUPPORT3 — revise only the affected C78 support certificates using the immutable C82 coefficient result**

## 19.6 Public bridge incomplete

```text
C82_IFAGG_PUBLIC_BRIDGE_INCOMPLETE
```

Next:

> **C83/IFAGGAPI — repair only the specifically identified API, runtime, safe-loading, deterministic-shard, or restart defect**

Do not issue a contact-matrix, physical-coupling, counterterm,
complete-instantaneous-fermion, local-QCD, TMD, matching, inference, or
production status.

---

# 20. Essential deliverables

Create at least:

```text
docs/next_level/c82_implementation_report.md
docs/next_level/c82_derivation_authority_manifest.json
docs/next_level/c82_input_fidelity_audit.json
docs/next_level/c82_input_freeze.json

docs/next_level/c82_factor_ownership_contract.json
docs/next_level/c82_factor_ownership_validation.json
docs/next_level/c82_witness_leaf_schema.json
docs/next_level/c82_witness_leaf_domain_validation.json

docs/next_level/c82_c78_to_c80_coordinate_map.json
docs/next_level/c82_coordinate_map_validation.json
docs/next_level/c82_coordinate_map_totality_report.json
docs/next_level/c82_coordinate_reverse_closure_report.json

docs/next_level/c82_projected_coefficient_contract.json
docs/next_level/c82_projected_coefficient_validation.json
docs/next_level/c82_independent_coefficient_reconstruction.json
docs/next_level/c82_pair_coordinate_coefficient_manifest.json
docs/next_level/c82_pair_coordinate_coefficient_status.json
docs/next_level/c82_pair_coordinate_aggregation_validation.json

docs/next_level/c82_projected_coefficient_bound_contract.json
docs/next_level/c82_projected_coefficient_bound_report.json
docs/next_level/c82_low_shell_aggregation_bridge_pilot.json
docs/next_level/c82_factorized_mapping_plan.json
docs/next_level/c82_resource_and_scaling_report.json
docs/next_level/c82_aggregation_hermiticity_input_report.json

docs/next_level/c82_api_contract.json
docs/next_level/c82_api_validation.json
docs/next_level/c82_runtime_inventory.json
docs/next_level/c82_deterministic_reconstruction_report.json
docs/next_level/c82_c83_ifcontact4_import_contract.json
docs/next_level/c82_c53_c58_non_substitution_report.json

docs/next_level/c82_isolation_report.json
docs/next_level/c82_readiness_report.json
docs/next_level/c82_regression_report.json
```

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 21. Acceptance criteria

C82 is complete only when:

1. Baseline `8b854cd49f471ab642acc0de0dee45b7b76ef49e` reproduces.
2. Both protected untracked paths remain untouched.
3. C77/C78/C80/C81 historical artifacts remain unchanged.
4. `c82_ifagg_contract.md` is consumed and preserved.
5. C78 and C80 roots verify before construction.
6. Inputs freeze before coordinate mapping.
7. Every factor has exactly one owner.
8. No C80 kernel factor is duplicated in the C82 coefficient.
9. No required factor is omitted.
10. The canonical witness-leaf schema is complete.
11. Every valid leaf maps to exactly one valid C80 coordinate.
12. No valid leaf remains unmapped.
13. No leaf maps ambiguously to nonequivalent coordinates.
14. The reverse map closes.
15. Every mapped C80 coordinate has a terminal evaluator status.
16. Projected coefficients are evaluated through two independent routes.
17. Route A and Route B agree within exact/certified semantics.
18. Bra conjugation, metrics, and signs are explicit.
19. Grouping uses exact C80 coordinate identity, never floating equality.
20. Exact expressions are combined before numerical evaluation where available.
21. Exact zero is distinguished from an interval containing zero.
22. Every pair-local coordinate has a terminal coefficient status.
23. Every C78-supported pair has at least one pair-coordinate record.
24. `UNAVAILABLE_BLOCKING` is zero in the favorable branch.
25. No C78 support change is made silently.
26. Hermitian-conjugation input closure passes.
27. The full coordinate domains remain factorized.
28. No dense coordinate-domain array is materialized.
29. The public API can query every logical coordinate and pair contribution.
30. The C83 import contract permits a legal coefficient-times-kernel product.
31. C53/C58 poisoning leaves C82 unchanged.
32. Deterministic serial, sharded, clean, and restart reconstruction passes.
33. At least 384 focused live mutations pass.
34. No contact matrix, matrix-free contact action, physical coupling, counterterm, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
35. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
36. The working tree is clean except for the two protected untracked paths.
37. A local completion commit is created and not pushed.

Do not weaken factor ownership, total coordinate mapping, exact projected
coefficient evaluation, interval semantics, reverse closure, or public
query completeness to open the gate.

---

# 22. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C82 contract identity;
- C77/C78/C80 package/root identities;
- 154,830 imported supported-pair count;
- logical witness-leaf counts by resolution;
- materialized factor-table counts;
- total-map and reverse-map hashes;
- unmapped, ambiguous, invalid, duplicate, and reverse-failure counts;
- factor-ownership result;
- pair-local C80 coordinate-class counts;
- projected-coefficient status counts;
- exact cancellation and interval-includes-zero counts;
- maximum projected-coefficient bound;
- Route A versus Route B residuals;
- Hermitian-conjugation compatibility;
- compression ratios, peak memory, and shard strategy;
- public API/runtime-root hashes;
- C53/C58 non-substitution result;
- deterministic reconstruction;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that no coefficient-times-kernel product, contact matrix, physical coupling, counterterm, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
