# C71/QGEMBED6 Codex Work Package

## Title

**Executable exact CM-ground and color-triplet physical \(qg\) embedding from the immutable C64 and C70 packages: factorized sparse maps, certified projectors, historical-basis covariance, and descendant-impact closure**

## Authoritative baseline

Start from the local C70 completion commit:

```text
ac61aecfdc8896f66d3b42204b1e53481eb79cc1
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "ac61aecfdc8896f66d3b42204b1e53481eb79cc1"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, or use either path as scientific authority.

The baseline is authoritative only when it contains and reproduces:

```text
C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY

C66_SOURCE_DERIVED_TRIPLET_ISOMETRY_ARTIFACT_READY

C67_QGEMBED_C66_IMPORT_INCOMPLETE

C68_SOURCE_DERIVED_TRIPLET_ISOMETRY_RUNTIME_READY

C70_SOURCE_DERIVED_TRIPLET_PACKAGE_IMPORT_READY
```

and the exact inherited results:

```text
C64 exact transverse package:
    733 exact TM blocks;
    171,153 exact coefficient-status records;
    67,920 exact cross-m residue certificates;
    complete block and global basis crosswalks;
    exact expression and support identities;
    certified numerical arrays and bounds;
    immutable, hash-verifying, no-regeneration import API;

C66 exact color science:
    U3 = E_src / sqrt(C_F);
    C_F = 4/3;
    exact 24 x 3 source-derived triplet isometry;
    U3-dagger U3 = I3;
    all-eight-generator intertwining;
    rank-three image projector;
    zero anti-sextet and 15 leakage;

C70 authenticated color package:
    byte-identical C68/C66 payload;
    complete 24-row and 3-column basis manifests;
    all 72 terminal entry statuses;
    exact expressions and exact-zero certificates;
    certified E_src, Gram, adapter, U3, U3-dagger, and P3;
    authenticated canonical index and package root;
    complete inventory;
    frozen TripletRuntimePackage;
    immutable, hash-verifying, no-regeneration public API;

historical embedding obstruction:
    the exact transverse and color ingredients existed separately;
    no package before C71 constructed the complete CM-ground,
    color-triplet physical qg embedding.
```

Verify all actual hashes, schemas, basis orders, paths, statuses, bounds, and API identities through the C64 and C70 public loaders. This prompt is not numerical authority.

The exact transverse convention remains:

\[
|n,m\rangle_{\rm polar}
=
(-1)^n
\left|
n+\max(m,0),\,
n+\max(-m,0)
\right\rangle_{\rm circ},
\qquad
L_z=N_+-N_- .
\]

The exact color convention remains:

\[
U_3=\frac{E_{\rm src}}{\sqrt{C_F}},
\qquad
C_F=\frac43,
\qquad
U_3^\dagger U_3=I_3,
\qquad
P_3=U_3U_3^\dagger .
\]

The physical resolution trajectory remains:

```text
(K,Nmax,bHO/GeV)
  = (9/2,8,0.40)
  = (11/2,10,0.45)
  = (13/2,12,0.50).
```

C40 remains:

```text
EXECUTABLE_METHOD_ORACLE_ONLY
```

Create a local completion commit. Do not push.

---

# 1. Execution mandate

C71 must construct the physical \(qg\) embedding.

The two prerequisite immutable packages are complete:

```text
C64:
    exact/certified transverse raw <-> relative/CM transformations;

C70:
    exact/certified retained-triplet <-> product-color isometry.
```

C71 must not create another preparatory package merely to duplicate, rename, summarize, wrap, or rehash C64 or C70.

A fail-closed result is valid only for a concrete obstruction encountered while constructing or validating the physical map, such as:

```text
a failed C64 or C70 authenticated import;

an unresolved orientation of a C64 TM block;

an incomplete exact CM-ground selection;

a failed isometry or metric-adjoint identity;

an unresolved kinematic/color basis permutation;

nonzero CM-excited, anti-sextet, or 15 leakage;

a numerical enclosure too weak to certify a required identity;

or an unresolved descendant-impact comparison.
```

A missing convenience report, preferred filename, duplicate inventory, dense-matrix export, or new wrapper is not a valid reason to avoid constructing the embedding.

The full embedding may and should be represented in a **factorized sparse/matrix-free form** when a dense matrix or dense projector would be wasteful. A factorized exact operator is an actual executable physical embedding when it supplies:

```text
complete basis identities;

exact support membership;

entry-level ancestry;

embed and project actions;

round-trip identities;

projector action;

certified numerical bounds;

and deterministic reconstruction.
```

Do not require a dense \(N_{\rm raw}\times N_{\rm phys}\) array or dense
\(N_{\rm raw}\times N_{\rm raw}\) projector as a readiness condition.

---

# 2. Exact purpose

C71 must construct:

```text
complete authenticated read-only imports of C64 and C70;

exact raw qg kinematic and raw product-color basis manifests;

exact relative/CM and CM-ground physical kinematic basis manifests;

exact CM-ground physical-to-raw kinematic injection;

exact or metric-adjoint raw-to-physical kinematic projection;

executable CM-ground image projector;

exact kinematic/color ordering adapters;

exact full CM-ground color-triplet physical qg embedding;

exact or metric-adjoint full physical projection;

executable CM-ground-triplet image projector;

threshold-free factorized physical support;

certified sparse/factorized numerical representations;

independent matrix-free embed, project, and projector actions;

an exact historical C47-basis phase/permutation adapter;

C47 embedding/support reconciliation;

C52 and C53 basis-covariance and numerical-impact audits;

C57 support and C58 contraction-impact audits;

a C59/C60 continuation decision;

one and only one authorized next branch.
```

C71 must not construct:

```text
the C60 direct-contact endpoint relation;

the q-intermediate witness relation;

a contact numerator, denominator, normalization, P-minus value,
M-squared value, or contact matrix;

a rebuilt canonical vertex inside a no-rebuild branch;

a rebuilt self-induced-inertia contraction inside a no-rebuild
branch;

a counterterm coefficient;

a complete instantaneous-fermion operator;

a Wilson/bilocal TMD or one-loop matching result.
```

The strongest favorable status is:

```text
C71_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY
```

Its exact continuation is:

> **C72/IFSUPPORT2 — source-ordered direct-contact endpoint and intermediate-\(q\) witness support using the immutable C71 physical embedding**

---

# 3. Mandatory inputs

Read and verify through public APIs wherever an immutable package exists.

## 3.1 C64 package

Consume the actual repository equivalents of:

```text
C64 readiness and package-root records;

C64 block census and block identities;

C64 row/column/global basis crosswalks;

C64 exact status and expression records;

C64 certified arrays and error bounds;

C64 residue certificates;

C64 immutable import API and API contract.
```

## 3.2 C70 package

Consume through:

```text
load_triplet_runtime_package()
```

or the exact public equivalent.

Verify:

```text
authenticated index and package root;

source/API fingerprints;

24 product-color rows;

3 retained-triplet columns;

72 terminal statuses;

exact expressions and zero certificates;

certified U3, U3-dagger, P3, and bounds;

complete inventory;

immutability and no-regeneration guarantees.
```

## 3.3 Historical basis and descendants

Read the actual repository equivalents of:

```text
C43 light-front conventions;

C45 longitudinal, HO, spinor, polarization, color, and zero-mode
contracts;

C47 longitudinal partitions, x-scaled coordinates, TM/CM basis,
many-body truncation, CM plan, physical q/qg basis manifests,
historical quadrature arrays, and basis-order records;

C52 source-derived colorless canonical vertex records;

C53 source-derived physical canonical vertex, full/reduced color
routes, generated adjoint, basis order, ancestry, and matrix-free
records;

C57 operation order, conditional corresponding-propagating support,
mode unions, candidate envelopes, ancestry, and runtime records;

C58 ordered-joint support, admitted-mode ledger, q-sector primitive,
qg scope decision, and runtime records;

C59/C60 contact-support no-go and exact missing-input records.
```

Use actual filenames. Do not invent a preferred filename and convert the naming difference into an obstruction.

Create:

```text
docs/next_level/c71_derivation_authority_manifest.json
docs/next_level/c71_input_fidelity_audit.json
```

---

# 4. Immutable import gates

## 4.1 C64

Before constructing one kinematic component, verify:

```text
733 blocks;

171,153 statuses;

67,920 residue certificates;

package aggregate identity;

all basis-order hashes;

all expression/support hashes;

all certified arrays and bounds;

all runtime paths;

global crosswalk completeness;

immutable returns;

no C62 regeneration.
```

Create:

```text
docs/next_level/c71_c64_import_report.json
```

## 4.2 C70

Before composing one color component, verify:

```text
authenticated package root;

source/API fingerprints;

complete inventory;

24-row and 3-column basis hashes;

all 72 statuses;

all expression and zero-certificate domains;

certified U3, U3-dagger, and P3;

all array and bound hashes;

immutable returns;

no C66/C68 regeneration.
```

Create:

```text
docs/next_level/c71_c70_import_report.json
```

Any authenticated-import failure blocks the affected construction. Do not bypass a public loader.

---

# 5. Freeze construction and holdouts

Freeze before assembly:

```text
C64 package root and all block orientations;

C64 global basis crosswalk;

the exact CM-ground label;

raw qg kinematic order;

relative/CM order;

physical kinematic order;

longitudinal, helicity, Jz, OAM, shell, and zero-mode conventions;

C70 package root;

C70 product-color row order;

C70 triplet column order;

U3 phase and normalization;

historical C47 basis order and phase records.
```

Freeze holdouts before construction:

```text
one lowest-shell CM-ground state;

one highest-shell CM-ground state per resolution;

one CM-excited state per resolution;

positive- and negative-m states;

one physical state with one raw component;

one physical state with multiple raw components;

one exact-zero component from each C64 zero class;

one smallest-certified nonzero per resolution;

one kinematic round trip per resolution;

one CM-projector holdout per resolution;

all three C70 triplet columns;

one anti-sextet vector;

one 15-representation vector;

one full physical round trip per resolution;

one C47 adapter holdout per shell;

one C52 matrix-element holdout;

one C53 full/reduced-route holdout;

one C57 support edge per resolution;

one C58 admitted-mode holdout per resolution.
```

Create:

```text
docs/next_level/c71_calculation_plan.json
docs/next_level/c71_holdout_plan.json
```

---

# 6. Exact basis manifests

Construct deterministic manifests for:

```text
raw qg kinematic basis;

relative/CM kinematic basis;

CM-ground physical kinematic basis;

raw qg product-color basis;

physical CM-ground triplet basis.
```

Every record must carry complete scientific identity and ancestry.

The physical kinematic dimensions are expected to reproduce the authoritative C47 values:

```text
448 / 900 / 1,584
```

and the physical triplet dimensions:

```text
1,344 / 2,700 / 4,752.
```

Derive these values from the packages and manifests. Do not hard-code them as authority.

Create:

```text
docs/next_level/c71_raw_qg_basis_manifest.json
docs/next_level/c71_relative_cm_basis_manifest.json
docs/next_level/c71_physical_kinematic_basis_manifest.json
docs/next_level/c71_raw_product_color_basis_manifest.json
docs/next_level/c71_physical_triplet_basis_manifest.json
docs/next_level/c71_basis_order_manifest.json
```

---

# 7. Exact CM-ground selection

Use the C64 crosswalk to select exactly:

\[
n_{\rm CM}=0,\qquad m_{\rm CM}=0.
\]

Do not infer CM-ground identity from numerical sparsity or small values.

For each selected state retain:

```text
C64 block ID;

block-local row/column identity;

global relative/CM basis ID;

physical kinematic basis ID;

basis-order hashes;

exact support ancestry.
```

Require:

```text
all physical CM-ground states present;

no CM-excited state selected;

no duplicate selection;

no missing state;

selected count equals physical kinematic dimension.
```

Create:

```text
docs/next_level/c71_cm_ground_selection_manifest.json
docs/next_level/c71_cm_ground_selection_validation.json
```

---

# 8. Prove TM orientation

Determine the exact stored orientation of every C64 block.

When C64 stores:

\[
T_{\rm relCM\leftarrow raw}
=
\langle {\rm rel,CM}|{\rm raw}\rangle,
\]

the physical-to-raw injection is:

\[
J_{\rm raw\leftarrow phys}
=
T_{\rm relCM\leftarrow raw}^{\dagger}
\big|_{\rm CM=0},
\]

subject to the declared basis metrics.

Prove the orientation using:

```text
C64 metadata;

exact low-shell holdouts;

inverse/adjoint identities;

basis-vector round trips;

historical C47 adapter consistency.
```

Create:

```text
docs/next_level/c71_tm_orientation_contract.json
docs/next_level/c71_tm_orientation_validation.json
```

Do not guess, transpose by convenience, or choose the orientation with the smaller residual.

---

# 9. Kinematic embedding and projection

Construct the executable kinematic injection:

\[
J_R^{\rm kin}:
\mathcal H_R^{\rm CM=0,kin}
\longrightarrow
\mathcal H_R^{\rm raw,kin}.
\]

Use:

```text
C64 exact statuses for support;

C64 exact-expression identities for ancestry;

C64 certified midpoints and bounds for numerical values.
```

Construct:

```text
a block-sparse stored representation;

an independent matrix-free action that reads C64 blocks and the
CM-ground selection directly.
```

Construct the exact or metric adjoint:

\[
P_R^{\rm kin}.
\]

Require:

\[
P_R^{\rm kin}J_R^{\rm kin}=I.
\]

Construct the executable image projector:

\[
\Pi_R^{\rm CM}
=
J_R^{\rm kin}P_R^{\rm kin}.
\]

The full projector may remain factorized/matrix-free. Validate:

```text
Hermiticity;

idempotence;

rank and trace;

CM-excited orthogonality;

sparse/matrix-free agreement;

certified error bounds.
```

Create:

```text
docs/next_level/c71_exact_kinematic_embedding.json
docs/next_level/c71_exact_kinematic_projection.json
docs/next_level/c71_cm_image_projector.json
docs/next_level/c71_kinematic_embedding_validation.json
docs/next_level/c71_kinematic_matrix_free_report.json
```

Do not use eigenvalue clipping, post-hoc symmetrization, or dense-projector construction as a prerequisite.

---

# 10. Quantum-number closure

Verify the exact block structure under:

```text
longitudinal partition and total K;

quark helicity;

gluon helicity;

total Jz;

intrinsic and CM OAM;

total transverse shell;

many-body Nmax;

zero-mode policy.
```

Every forbidden component must have a source or C64 exact certificate.

Create:

```text
docs/next_level/c71_kinematic_quantum_number_report.json
```

---

# 11. Kinematic/color ordering adapters

Derive explicit permutations between:

```text
C71 raw kinematic-major product-color order;

C70 product-color row order;

C71 physical kinematic-major triplet order;

C70 retained-triplet column order.
```

Do not assume NumPy Kronecker ordering.

Construct:

\[
\mathcal P_{\rm raw},
\qquad
\mathcal P_{\rm phys}.
\]

Verify:

```text
bijection;

inverse;

basis-ID preservation;

deterministic hashes;

serial/parallel/restart identity.
```

Create:

```text
docs/next_level/c71_kinematic_color_permutation.json
docs/next_level/c71_kinematic_color_permutation_validation.json
```

---

# 12. Full physical color-triplet embedding

Construct the executable full embedding:

\[
J_R^{\rm phys}:
\mathcal H_R^{\rm CM=0,triplet}
\longrightarrow
\mathcal H_R^{\rm raw,kin\otimes(3\otimes8)}.
\]

Prove the factorized form:

\[
J_R^{\rm phys}
=
\mathcal P_{\rm raw}
\left(
J_R^{\rm kin}\otimes U_3
\right)
\mathcal P_{\rm phys}^{-1}.
\]

Construct:

\[
P_R^{\rm phys},
\qquad
\Pi_R^{\rm CM,3}
=
J_R^{\rm phys}P_R^{\rm phys}.
\]

Use factorized sparse/matrix-free representations as the primary full-scale implementation. Do not require the dense matrices.

Require:

\[
P_R^{\rm phys}J_R^{\rm phys}=I.
\]

Validate:

```text
projector Hermiticity and idempotence;

rank and trace equal physical triplet dimension;

CM-ground identity;

zero CM-excited leakage;

zero anti-sextet leakage;

zero 15 leakage;

CM/color projector commutation;

triplet-basis rotation covariance;

stored-factorized/matrix-free agreement;

certified propagated bounds.
```

Create:

```text
docs/next_level/c71_exact_physical_qg_embedding.json
docs/next_level/c71_exact_physical_qg_projection.json
docs/next_level/c71_physical_image_projector.json
docs/next_level/c71_color_cm_factorization_report.json
docs/next_level/c71_physical_embedding_validation.json
docs/next_level/c71_physical_matrix_free_report.json
```

---

# 13. Threshold-free physical support

Represent support exactly as a factorized relation.

For each queried physical/raw pair return one terminal status:

```text
ZERO_BY_C64_TM_SHELL_RULE;

ZERO_BY_C64_TM_M_RULE;

ZERO_BY_C64_TM_ALGEBRAIC_CANCELLATION;

ZERO_BY_C70_EXACT_COLOR_RULE;

ZERO_BY_CM_OR_BASIS_SELECTION;

NONZERO_EXACT_TM_AND_EXACT_COLOR;

NONZERO_EXACT_TM_CERTIFIED_COLOR;

UNDECIDABLE_BLOCKING.
```

A positive gate requires no undecidable pair in the represented domain.

The complete support need not be serialized as a dense all-pairs table. It may be represented by:

```text
the exact C64 support relation;

the exact C70 color support relation;

the CM selection;

the basis permutations;

a deterministic membership API;

a sparse nonzero index;
```

This factorized representation must answer exact membership without a floating threshold.

Create:

```text
docs/next_level/c71_exact_physical_support.json
docs/next_level/c71_exact_physical_support_validation.json
```

---

# 14. Numerical certification

Propagate C64 and C70 bounds through:

```text
conjugation;

Kronecker composition;

permutation;

sparse accumulation;

embed/project actions;

projector actions.
```

Export deterministic runtime objects for:

```text
J_kin and P_kin sparse blocks;

factorized Pi_CM;

factorized J_phys and P_phys;

factorized Pi_CM,triplet;

support indices;

error-bound metadata;

matrix-free action metadata.
```

Exact zeros remain literal zeros. No exact nonzero may be pruned.

Create:

```text
docs/next_level/c71_error_propagation_contract.json
docs/next_level/c71_certified_numerical_embedding_export.json
docs/next_level/c71_precision_stability_report.json
docs/next_level/c71_certified_invariant_report.json
```

---

# 15. Public embedding API

Provide public operations equivalent to:

```python
load_qg_embedding_package(resolution: str)

physical_qg_raw_components(
    physical_basis_id: str,
    resolution: str,
)

embed_physical_qg_to_raw(
    physical_vector,
    resolution: str,
    precision: int | None = None,
)

project_raw_qg_to_physical(
    raw_vector,
    resolution: str,
    precision: int | None = None,
)

apply_cm_ground_projector(
    raw_kinematic_vector,
    resolution: str,
)

apply_physical_triplet_projector(
    raw_product_color_vector,
    resolution: str,
)

physical_support_status(
    physical_basis_id: str,
    raw_basis_id: str,
    resolution: str,
)
```

Return objects must expose:

```text
C64 and C70 package roots;

exact support status;

entry ancestry;

certified midpoint and bound;

basis identities;

CM and triplet ancestry.
```

The API must call no C62/C66/C68 builder and must never infer support from magnitude.

Create:

```text
docs/next_level/c71_api_contract.json
docs/next_level/c71_api_validation.json
```

---

# 16. Historical C47 basis adapter

Derive—not fit—an adapter between the exact C64/C71 kinematic basis and the historical C47 quadrature/argmax basis.

Separate:

```text
basis permutation;

global analytic polar phase;

historical row phase;

quadrature residual;

possible subspace mismatch.
```

Prove orientation and unitarity/isometry where applicable.

Create:

```text
docs/next_level/c71_historical_basis_adapter.json
docs/next_level/c71_historical_basis_adapter_validation.json
```

---

# 17. C47 reconciliation

After applying only the derived adapter, compare the historical C47 map with the C71 exact kinematic embedding.

Reconfirm:

```text
4,032 / 15,840 / 48,048
```

historical subthreshold entries are exact cross-\(m\) zeros.

Audit every historical above-threshold support entry.

Classify C47 as one of:

```text
C47_NUMERICAL_EMBEDDING_VALID_IN_HISTORICAL_BASIS;

C47_VALUES_VALID_EXACT_SUPPORT_CERTIFICATE_SUPERSEDED;

C47_BASIS_ADAPTER_REQUIRED_NO_PHYSICS_CHANGE;

C47_NUMERICAL_EMBEDDING_SUPERSESSION_REQUIRED;

C47_IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c71_c47_embedding_reconciliation.json
docs/next_level/c71_c47_support_reconciliation.json
docs/next_level/c71_c47_basis_status_decision.json
```

---

# 18. C52 and C53 impact audits

The physical embedding must be constructed before these audits. A packaging issue in C52 or C53 may not retroactively block the C71 embedding.

## 18.1 C52

Audit:

```text
kinematic basis covariance;

support ancestry;

frozen source-derived holdouts;

sparse/matrix-free actions;

adjoint and dimensional contracts.
```

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ADAPTER_ONLY_NO_OPERATOR_REBUILD;

SUPPORT_CERTIFICATE_SUPERSEDED_VALUES_VALID;

NUMERICAL_OPERATOR_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

## 18.2 C53

Using the exact C71 kinematic embedding and immutable C70 color package, audit:

```text
full-product and reduced-triplet route equality;

triplet basis covariance;

physical support ancestry;

generated adjoint;

matrix-free action;

frozen source-derived holdouts.
```

Use stored C52/C53 numerical values only as post-construction comparisons, never as construction authority.

Create:

```text
docs/next_level/c71_c52_impact_audit.json
docs/next_level/c71_c52_basis_covariance_report.json
docs/next_level/c71_c53_impact_audit.json
docs/next_level/c71_vertex_basis_covariance_report.json
```

A required C52/C53 numerical rebuild takes precedence over downstream branches.

---

# 19. C57 impact audit

Reconstruct corresponding-propagating support from:

```text
source endpoint rules;

the exact C71 physical embedding;

the fixed C57 operation order;

IFREG-CORRESPONDING-PROPAGATING-SUPPORT.
```

Do not use C53 numerical values to define support.

Audit the inherited counts:

```text
canonical positions:
    312 / 510 / 756;

conditional unions:
    1,216 / 2,320 / 3,936;

candidate envelopes:
    2,304 / 4,400 / 7,488.
```

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ID_ADAPTER_ONLY;

SUPPORT_CERTIFICATE_SUPERSEDED_NO_SUPPORT_CHANGE;

SUPPORT_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c71_c57_support_impact_audit.json
```

---

# 20. C58 impact audit

Audit:

```text
IFNORM2-ORDERED-JOINT-SUPPORT;

admitted modes:
    4,216 / 8,330 / 14,484;

q-sector primitive:
    6 x 6 with six nonzero entries;

qg scope:
    IFNORM2-SECTOR-SPECIFIC-COUNTERTERM-ONLY.
```

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ID_ADAPTER_ONLY;

SUPPORT_LEDGER_REBUILD_REQUIRED_VALUES_UNCHANGED;

NUMERICAL_CONTRACTION_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c71_c58_impact_audit.json
```

---

# 21. C59/C60 continuation audit

Determine whether C71 now supplies:

```text
exact raw/physical qg components;

exact CM-ground and triplet ancestry;

exact projected-cancellation semantics;

threshold-free support membership;

hash-verifying embed/project APIs.
```

Classify:

```text
C60_BLOCKER_RESOLVED_READY_FOR_IFSUPPORT2;

ADDITIONAL_ENDPOINT_INPUT_BLOCKING;

UPSTREAM_IFREG_SUPERSESSION_REQUIRED;

UPSTREAM_VERTEX_SUPERSESSION_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c71_c59_c60_continuation_audit.json
```

---

# 22. Continuation decision

Select exactly one branch.

## 22.1 Favorable branch

```text
C71_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY
```

Required:

```text
embedding and projectors complete;

C52/C53 require no numerical rebuild;

C57 requires no support rebuild;

C58 requires no contraction rebuild;

C60 blocker resolved.
```

Next:

> **C72/IFSUPPORT2 — source-ordered direct-contact endpoint and intermediate-\(q\) witness support**

## 22.2 IFREG supersession

```text
C71_EXACT_QG_EMBEDDING_READY_IFREG_SUPERSESSION_REQUIRED
```

Next:

> **C72/IFREG3 — rebuild C57/C58 support and self-induced-inertia ancestry from C71**

## 22.3 Vertex supersession

```text
C71_EXACT_QG_EMBEDDING_READY_VERTEX_SUPERSESSION_REQUIRED
```

Next:

> **C72/VERTEX5 — rebuild C52/C53 in the exact C71 basis and revalidate downstream support**

This branch takes precedence over IFREG supersession.

## 22.4 Impact unresolved

```text
C71_QG_EMBEDDING_DESCENDANT_IMPACT_INCOMPLETE
```

This status means the physical embedding itself is complete but one or more descendant audits remain unresolved.

Next:

> **C72/QGIMPACT2 — complete basis-covariance and descendant-impact closure**

Do not discard a completed physical embedding merely because a descendant comparison remains unresolved.

---

# 23. Concrete no-go branches

## A. C64 authenticated import fails

```text
C71_QGEMBED_C64_IMPORT_INCOMPLETE
```

Next:

> **C72/QGTM4 — repair only the concrete C64 import defect**

## B. C70 authenticated import fails

```text
C71_QGEMBED_C70_IMPORT_INCOMPLETE
```

Next:

> **C72/QGCOLOR5 — repair only the concrete C70 package defect**

## C. CM-ground selection or TM orientation fails

```text
C71_QG_CM_GROUND_EMBEDDING_INCOMPLETE
```

Next:

> **C72/QGCM6 — exact CM selection, orientation, adjoint, and projector closure**

## D. Kinematic/color composition fails

```text
C71_QG_KINEMATIC_COLOR_COMPOSITION_INCOMPLETE
```

Next:

> **C72/QGCOMPOSE3 — ordering, factorization, and full-map closure**

## E. Numerical certification fails

```text
C71_QG_EMBEDDING_NUMERICAL_CERTIFICATION_FAILED
```

Next:

> **C72/QGNUM6 — propagated bounds and executable-projector certification**

## F. Historical adapter fails after embedding completion

```text
C71_QG_HISTORICAL_BASIS_ADAPTER_INCOMPLETE
```

Next:

> **C72/QGADAPT5 — exact phase/permutation adapter and C47 reconciliation**

---

# 24. Runtime package and deterministic reconstruction

Use:

```text
data/runtime/c71_qgembed6/
```

Store deterministic, content-addressed objects for each resolution:

```text
basis manifests;

CM-ground selection;

sparse/factorized J_kin and P_kin;

factorized Pi_CM;

kinematic/color permutations;

factorized J_phys and P_phys;

factorized Pi_CM,triplet;

exact support relations and sparse indices;

certified numerical values and bounds;

matrix-free metadata;

historical adapter;

impact records.
```

Create an authenticated package index and root consistent with the proven C64/C70 package patterns, but do this as part of C71—not as a separate package.

Create:

```text
docs/next_level/c71_numerical_object_inventory.json
docs/next_level/c71_runtime_completeness_report.json
docs/next_level/c71_deterministic_reconstruction_report.json
```

Run repeated, clean, serial, supported parallel, and restart builds.

---

# 25. Count-once, isolation, and tests

Report:

```text
physical basis dimensions;

raw basis dimensions;

CM-ground selections;

exact TM nonzeros;

exact color nonzeros;

factorized physical nonzeros;

duplicates;

missing components;

undecidable components;

unresolved descendant impacts.
```

Every nonzero component must have exactly one ancestry:

```text
C64 TM entry
 -> CM selection
 -> kinematic identity
 -> C70 color entry
 -> ordering adapter
 -> C71 physical component.
```

Poison and prove construction independence from:

```text
historical C47 quadrature values;

historical 1e-12 threshold;

C47 raw tuple values;

C52/C53 stored numerical matrices;

C57/C58 stored numerical operator values;

raw_emission_E;

historical C53 projectors;

C66/C68 builders;

ART25 files.
```

Create at least **384 focused live mutations** of actual imports, basis records, selection records, orientations, sparse/factorized operators, support relations, bounds, adapters, and impact decisions.

Create:

```text
docs/next_level/c71_component_ancestry_ledger.json
docs/next_level/c71_count_once_report.json
docs/next_level/c71_isolation_report.json
docs/next_level/c71_regression_report.json
```

---

# 26. Required deliverables

Create at least:

```text
docs/next_level/c71_implementation_report.md
docs/next_level/c71_api.md
docs/next_level/c71_derivation_authority_manifest.json
docs/next_level/c71_input_fidelity_audit.json

docs/next_level/c71_c64_import_report.json
docs/next_level/c71_c70_import_report.json
docs/next_level/c71_calculation_plan.json
docs/next_level/c71_holdout_plan.json

docs/next_level/c71_raw_qg_basis_manifest.json
docs/next_level/c71_relative_cm_basis_manifest.json
docs/next_level/c71_physical_kinematic_basis_manifest.json
docs/next_level/c71_raw_product_color_basis_manifest.json
docs/next_level/c71_physical_triplet_basis_manifest.json
docs/next_level/c71_basis_order_manifest.json

docs/next_level/c71_cm_ground_selection_manifest.json
docs/next_level/c71_cm_ground_selection_validation.json
docs/next_level/c71_tm_orientation_contract.json
docs/next_level/c71_tm_orientation_validation.json

docs/next_level/c71_exact_kinematic_embedding.json
docs/next_level/c71_exact_kinematic_projection.json
docs/next_level/c71_cm_image_projector.json
docs/next_level/c71_kinematic_embedding_validation.json
docs/next_level/c71_kinematic_matrix_free_report.json
docs/next_level/c71_kinematic_quantum_number_report.json

docs/next_level/c71_kinematic_color_permutation.json
docs/next_level/c71_kinematic_color_permutation_validation.json
docs/next_level/c71_exact_physical_qg_embedding.json
docs/next_level/c71_exact_physical_qg_projection.json
docs/next_level/c71_physical_image_projector.json
docs/next_level/c71_color_cm_factorization_report.json
docs/next_level/c71_physical_embedding_validation.json
docs/next_level/c71_physical_matrix_free_report.json

docs/next_level/c71_exact_physical_support.json
docs/next_level/c71_exact_physical_support_validation.json
docs/next_level/c71_error_propagation_contract.json
docs/next_level/c71_certified_numerical_embedding_export.json
docs/next_level/c71_precision_stability_report.json
docs/next_level/c71_certified_invariant_report.json

docs/next_level/c71_api_contract.json
docs/next_level/c71_api_validation.json

docs/next_level/c71_historical_basis_adapter.json
docs/next_level/c71_historical_basis_adapter_validation.json
docs/next_level/c71_c47_embedding_reconciliation.json
docs/next_level/c71_c47_support_reconciliation.json
docs/next_level/c71_c47_basis_status_decision.json

docs/next_level/c71_c52_impact_audit.json
docs/next_level/c71_c52_basis_covariance_report.json
docs/next_level/c71_c53_impact_audit.json
docs/next_level/c71_vertex_basis_covariance_report.json
docs/next_level/c71_c57_support_impact_audit.json
docs/next_level/c71_c58_impact_audit.json
docs/next_level/c71_c59_c60_continuation_audit.json

docs/next_level/c71_descendant_dependency_graph.json
docs/next_level/c71_inherited_impact_summary.json
docs/next_level/c71_supersession_plan.json

docs/next_level/c71_component_ancestry_ledger.json
docs/next_level/c71_count_once_report.json
docs/next_level/c71_isolation_report.json
docs/next_level/c71_numerical_object_inventory.json
docs/next_level/c71_runtime_completeness_report.json
docs/next_level/c71_deterministic_reconstruction_report.json
docs/next_level/c71_readiness_report.json
docs/next_level/c71_source_sufficiency_decision.json
docs/next_level/c71_no_go_decision_tree.json
docs/next_level/c71_missing_calculation_specification.md
docs/next_level/c71_regression_report.json
```

Create exactly one next-package import contract corresponding to the selected branch.

Add implementation under:

```text
src/deuteron_wigner/bridge/qgembed6/
```

or the repository-equivalent package.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add the pre-existing untracked C69 prompt.

---

# 27. Readiness criteria

C71 is complete only when:

1. Baseline commit `ac61aecfdc8896f66d3b42204b1e53481eb79cc1` reproduces.
2. The C64 and C70 positive statuses remain explicit.
3. The two pre-existing untracked paths remain untouched.
4. All C64 and C70 authenticated imports pass.
5. Neither package is regenerated.
6. Raw and physical basis manifests are complete.
7. CM-ground selection is exact and complete.
8. TM orientation is proved.
9. \(J_{\rm kin}\) and \(P_{\rm kin}\) execute.
10. \(P_{\rm kin}J_{\rm kin}=I\) closes.
11. The CM projector executes and is Hermitian/idempotent.
12. CM-excited leakage is zero.
13. Quantum-number closure passes.
14. Kinematic/color permutations are explicit.
15. \(J_{\rm phys}\) and \(P_{\rm phys}\) execute.
16. \(P_{\rm phys}J_{\rm phys}=I\) closes.
17. The full physical projector executes and is Hermitian/idempotent.
18. Anti-sextet and 15 leakage are zero.
19. Support is threshold free.
20. No exact nonzero is pruned.
21. No component remains undecidable.
22. Certified bounds cover all numerical actions and identities.
23. Sparse/factorized and independent matrix-free routes agree.
24. No dense full projector is required.
25. The historical adapter is derived, not fitted.
26. C47 reconciliation is typed.
27. C52 and C53 impacts are typed.
28. C57 and C58 impacts are typed.
29. C59/C60 continuation is typed.
30. Exactly one continuation branch is selected.
31. Completed embedding is retained even when descendant impact is unresolved.
32. Every component has complete ancestry.
33. Duplicate and missing counts are zero.
34. Runtime package and APIs reconstruct deterministically.
35. Poisoning and mutation tests pass.
36. No endpoint, witness, contact, complete instantaneous-fermion, Wilson/TMD, matching, inference, or production object is created.
37. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
38. `MSHT20_REP/` remains untouched.
39. The working tree is clean except for the two pre-existing untracked paths.
40. A local completion commit is created and not pushed.

A rigorous no-go or supersession result is valid. Do not weaken authenticated imports, exact CM selection, orientation, factorized isometry, threshold-free support, numerical certification, or descendant-impact accounting.

---

# 28. Final Codex response

Report:

- starting and final commits;
- untouched untracked paths;
- C64 and C70 package roots and imported counts;
- raw, physical-kinematic, raw-product-color, and physical-triplet dimensions;
- CM-ground selection counts and hashes;
- TM orientation;
- \(J_{\rm kin}\), \(P_{\rm kin}\), and \(\Pi_{\rm CM}\) shapes/factorization, nnz/support counts, ranks, traces, and certified residuals;
- quantum-number closure;
- ordering-adapter hashes;
- \(J_{\rm phys}\), \(P_{\rm phys}\), and \(\Pi_{\rm CM,3}\) shapes/factorization, support counts, ranks, traces, and certified residuals;
- CM-excited, anti-sextet, and 15 leakage;
- sparse/factorized versus matrix-free residuals;
- exact support-class counts;
- historical C47 adapter and reconciliation;
- C52 and C53 impact decisions;
- C57 exact positions/unions/envelopes and differences;
- C58 admitted-mode and primitive impact;
- C59/C60 continuation status;
- descendant supersession decision;
- ancestry, missing, duplicate, undecidable, and unresolved-impact counts;
- deterministic reconstruction;
- isolation and mutation results;
- exact readiness/no-go/supersession status;
- exact next branch;
- confirmation that no contact support/value/matrix or downstream TMD/matching/production object was created;
- confirmation that nothing was pushed.

Do not describe a guessed orientation, thresholded support, assumed Kronecker order, dense-matrix omission, fitted basis adapter, uncertified round trip, or unaudited descendant as a complete physical \(qg\) embedding.
