# ADR 232: Define a new overlap convention

The selected finite-rapidity soft overlap is subtracted exactly once before soft allocation. The historical C32 off-shell zero-bin contract is not presumed operator-identical.
