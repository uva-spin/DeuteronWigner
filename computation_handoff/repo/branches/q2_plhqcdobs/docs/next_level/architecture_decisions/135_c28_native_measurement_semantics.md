# ADR 135: native measurement semantics are identity fields

Status: accepted.

Process, bin edges, integration variables, units, normalization, theory
factor, fiducial cuts, hadron charge, and SIDIS z convention belong to the
measurement identity. They are traced from the executed loader and harpy path;
names and array shapes are insufficient. Bin-center substitutions and silent
normalization conversions fail closed.

