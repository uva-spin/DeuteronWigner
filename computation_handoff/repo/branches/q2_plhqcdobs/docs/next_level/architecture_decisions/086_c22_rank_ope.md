# ADR 086: rank-aware small-b OPE

**Decision.** Each OPE map stores transverse rank, matching Bessel order,
Fourier phase, extracted mass convention, and tensor basis. Matching preserves
rank, and scalar J0 paths are rejected for ranked objects.
