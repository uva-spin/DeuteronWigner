# ADR 184: Export only after every matching gate

Failure of any soft, overlap, UV, rapidity, IR, gauge, state, trajectory, or
remainder gate yields an empty-not-zero export and forbids a bridge rerun.
