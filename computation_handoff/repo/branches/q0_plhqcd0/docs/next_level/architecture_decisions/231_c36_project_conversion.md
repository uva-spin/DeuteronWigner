# ADR 231: Convert only renormalized continuum TMDs

The selected-to-project conversion is a source-qualified finite-order continuum relation with an inverse, round trip, and hard-factor companion. It is never applied directly to the C11 density and uses no ART25 fit input.
