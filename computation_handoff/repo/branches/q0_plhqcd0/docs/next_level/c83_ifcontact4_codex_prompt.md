# C83/IFCONTACT4 Codex Work Package

## Title

**Certified bare direct instantaneous-fermion contact matrix from the immutable C82 aggregation bridge and immutable C80 four-mode kernel: exact coefficient–kernel products, sparse and independent matrix-free actions, source-derived Hermiticity, and C84 complete-operator handoff**

## Authoritative baseline

Start from the clean local C82 completion commit:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "8e47231ab565f0f729d335b39aa98881176ba166"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Read completely and preserve the committed continuation contract:

```text
docs/next_level/c83_ifcontact4_contract.md
```

If that contract freezes exact status names, APIs, or a narrower scientific
boundary, preserve them. This work package supplies the executable
construction and validation plan; it does not authorize weakening the
committed contract.

The baseline is authoritative only when it contains and reproduces:

```text
C77_EXACT_SOURCE_CHAIN_DERIVED_QG_EMBEDDING_READY

C78_SOURCE_DERIVED_IFERM_CONTACT_SUPPORT_READY

C80_EXACT_SOURCE_CHAIN_DERIVED_IFCONTACT_KERNEL_EVALUATOR_READY

C82_SOURCE_DERIVED_IFCONTACT_AGGREGATION_BRIDGE_READY
```

and the exact inherited scientific division:

```text
C78:
    exact structural support, endpoints, retained-q witnesses, and
    source-order ancestry for 154,830 physical qg pairs;

C80:
    exact/certified raw-coordinate W3 contact kernel;
    all longitudinal, inverse-partial-plus, spin/polarization,
    ordered-color, four-HO, finite-cell, and P-minus-to-M-squared
    factors;
    g_s^2 explicitly factored;

C82:
    total deterministic and reversible witness-leaf -> C80 raw
    coordinate map;
    evaluated projected physical bra/ket coefficients and bounds;
    bra conjugation and projection factors;
    exact factor-ownership closure;
    no coefficient-times-kernel product and no matrix.

C82 factor ownership:
    C82 owns only physical projection amplitudes, bra conjugation,
    basis metric/sign factors, witness multiplicity, and their
    certified bounds;
    C80 owns every W3 kernel factor and the explicit g_s^2.
```

Verify the actual package roots, counts, schemas, coordinate identities,
coefficient statuses, bounds, and public APIs from the repository. This
prompt is not numerical authority.

Create a local completion commit. Do not push.

---

# 1. Authority policy and scientific boundary

C83 is an exact project-derived composition of two already authenticated
objects:

\[
C_{bk}^{[\xi]}
\quad\text{from C82},
\qquad
\widehat{\mathcal K}^{P^-}_{R,\xi},
\widehat{\mathcal K}^{M^2}_{R,\xi}
\quad\text{from C80}.
\]

No new external source is required to multiply them, aggregate them by
physical pair, build a sparse matrix, define a matrix-free action, or
serialize the result.

C83 must preserve the factor boundary:

```text
C82 projected coefficient:
    no W3 longitudinal, spin, color, HO, normalization, or
    P-minus-to-M-squared factor;
    no g_s^2;

C80 kernel:
    all W3 kernel factors;
    coefficient of explicit factored g_s^2.
```

A new no-go is valid only when:

```text
the C82 and C80 shared coordinate identities do not match;
a required pair-coordinate coefficient or kernel is unavailable;
the factor-ownership product double counts or omits a source factor;
the certified arithmetic cannot close;
the source Hermitian conjugation map fails;
or the public runtime cannot reproduce the evaluated matrix.
```

Do not fail because a dense matrix is absent, because another external
paper does not publish the repository's sparse ordering, or because a
different internal organization would be convenient.

C83 keeps

\[
M_{\rm contact}^{2,\mathrm{bare}}
=
g_s^2\widehat M_{\rm contact}^{2}
\]

with \(g_s^2\) explicit and factored.

C83 must not choose:

```text
g_s;
alpha_s;
a renormalization scale;
a counterterm coefficient;
a subtraction;
a fitted normalization;
or a continuum extrapolation.
```

---

# 2. Exact purpose and stopping point

For every resolution \(R\), physical bra \(b\), physical ket \(k\), and
pair-local canonical C80 coordinate \(\xi\), C83 must form the legal
products

\[
T_{bk}^{P^-,[\xi]}
=
C_{bk}^{[\xi]}
\widehat{\mathcal K}^{P^-}_{R,\xi},
\]

\[
T_{bk}^{M^2,[\xi]}
=
C_{bk}^{[\xi]}
\widehat{\mathcal K}^{M^2}_{R,\xi}.
\]

Then evaluate

\[
\left[\widehat P^-_{\rm contact,R}\right]_{bk}
=
\sum_{\xi\in\Xi_{bk}}T_{bk}^{P^-,[\xi]},
\]

\[
\left[\widehat M^2_{\rm contact,R}\right]_{bk}
=
\sum_{\xi\in\Xi_{bk}}T_{bk}^{M^2,[\xi]}.
\]

C83 must construct:

```text
one terminal evaluated-value record for every C78-supported pair;

the sparse coefficient-of-g_s^2 P-minus contact matrix;

the sparse coefficient-of-g_s^2 M-squared contact matrix;

an independent factorized matrix-free P-minus action;

an independent factorized matrix-free M-squared action;

certified matrix-entry bounds;

source-derived Hermiticity and diagonal-reality closure;

finite-resolution diagnostics;

an immutable public runtime/API package;

and a C84/IFERM3 import contract.
```

C83 must not:

```text
change C78 support;

change C82 pair-coordinate coefficients;

change or rebuild the C80 kernel;

materialize the full logical witness-coordinate domains;

multiply by a physical coupling;

add the C58 self-induced-inertia block;

add C53 sequential propagation;

solve a counterterm;

assemble the complete instantaneous-fermion operator;

or create a local-QCD Hamiltonian, TMD, matching, fit, inference,
or production object.
```

The preferred positive semantic status is:

```text
C83_SOURCE_DERIVED_BARE_IFERM_CONTACT_MATRIX_READY
```

If `c83_ifcontact4_contract.md` freezes a different exact spelling, preserve
that spelling and record a semantic alias.

The favorable continuation is:

> **C84/IFERM3 — assemble the complete bare instantaneous-fermion operator from the immutable C58 self-induced-inertia block and immutable C83 direct-contact block, type sector-specific counterterm directions, and issue the next local-HQCD gate**

---

# 3. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C58:
    bare q-sector self-induced-inertia matrix;
    ordered Pi_bra delta Pi_ket ancestry;
    admitted-mode ledgers;
    qg counterterm-only scope;
    explicit separation from direct contact;

C77:
    physical qg basis identities and ordering;
    physical/raw embedding and projection;
    metric and conjugation conventions;

C78:
    structural supported-pair domain;
    source-order endpoint/witness ancestry;
    support statuses;
    public support API;

C80:
    canonical ContactKernelCoordinate identities;
    exact coordinate-equivalence classes;
    P-minus and M-squared kernel values;
    terminal evaluator statuses;
    certified bounds;
    P-minus-to-M-squared convention;
    public evaluator API;

C82:
    factor-ownership certificate;
    total C78-to-C80 coordinate map;
    pair-local canonical C80 coordinate contributions;
    projected coefficient expressions/values;
    coefficient statuses and bounds;
    Hermitian-conjugation compatibility;
    public aggregation API;
    C83 import contract;

C53:
    read-only post-construction non-substitution control only.
```

Create:

```text
docs/next_level/c83_derivation_authority_manifest.json
docs/next_level/c83_input_fidelity_audit.json
```

---

# 4. Freeze C82 and C80 before multiplication

Consume C82 and C80 only through their authenticated immutable public APIs.

Freeze:

```text
C82 and C80 package/root identities;

all resolution and physical-basis identities;

all 154,830 C78-supported pair identities;

every pair-local C80 coordinate ID;

every C82 projected coefficient expression/value/status/bound;

every C80 P-minus and M-squared value/status/bound;

the exact shared coordinate-equivalence IDs;

the factor-ownership identity;

the source/operator/regulator/convention identities;

the Hermitian-conjugation maps.
```

Issue:

```text
C83_INPUTS_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c83_input_freeze.json
```

After this freeze:

```text
do not change support;
do not merge coordinates by numerical equality;
do not alter coefficient or kernel statuses;
do not repair Hermiticity by modifying entries;
do not add a physical coupling or counterterm.
```

---

# 5. Shared-coordinate compatibility gate

Before forming one product, verify that each C82 pair-coordinate record and
the C80 evaluator refer to one identical canonical object.

For every pair-coordinate contribution require equality of:

```text
canonical coordinate ID;
coordinate schema version;
resolution;
all mode identities;
longitudinal channel;
helicity/polarization identities;
ordered color identity;
four-HO identity;
zero-mode/boundary identity;
P-minus/M-squared convention;
exact coordinate-equivalence-class ID.
```

Verify:

```text
every C82 coordinate is accepted by C80;
every required C80 coordinate has a terminal evaluator status;
no C82 coordinate maps to multiple nonequivalent C80 objects;
no C80 value is selected by floating-point comparison;
the reverse ancestry reaches the original C78 witness leaves.
```

Create:

```text
docs/next_level/c83_shared_coordinate_compatibility.json
docs/next_level/c83_shared_coordinate_validation.json
```

A mismatch is a legitimate `C83_IFCONTACT_SHARED_COORDINATE_INCOMPLETE`
result.

---

# 6. Reconfirm factor ownership at multiplication time

Consume the immutable C82 ownership certificate and verify the actual
multiplicands.

For a frozen set of coordinates, reconstruct the source-level W3 term and
show that:

```text
the C82 coefficient contains projection amplitudes, metrics,
conjugation, witness multiplicity, and source-order signs only;

the C80 kernel contains the longitudinal denominator,
spin/polarization numerator, ordered color product, four-HO
overlap, finite-cell normalization, and P-minus/M-squared
conversion only;

g_s^2 is not present in the C82 coefficient and appears only as
the declared factored identity of the C80/C83 result;

no factor is omitted or duplicated.
```

Create:

```text
docs/next_level/c83_factor_ownership_revalidation.json
```

Do not continue if the product would double count a sign, color factor,
spin factor, denominator, overlap, normalization, or conversion.

---

# 7. Canonical product records

For each pair-local coordinate contribution construct an immutable product
record with:

```text
resolution;
physical bra ID;
physical ket ID;
canonical C80 coordinate ID;
C82 projected coefficient identity;
C80 kernel identity;
P-minus product value/expression and bound;
M-squared product value/expression and bound;
coefficient status;
kernel status;
product status;
C78/C77/C82/C80 ancestry;
factored-g_s^2 identity;
units;
canonical product ID.
```

Use exact symbolic multiplication where both factors have exact
expressions. Otherwise use certified numerical multiplication.

Create:

```text
docs/next_level/c83_pair_coordinate_product_manifest.json
docs/next_level/c83_pair_coordinate_product_validation.json
```

No product may be created without a matching C82 coefficient and C80
kernel.

---

# 8. Certified product arithmetic

For coefficient \(C\pm\delta C\) and kernel \(K\pm\delta K\), propagate at
least:

\[
\delta(CK)
\le
|C|\delta K
+
|K|\delta C
+
\delta C\,\delta K.
\]

When exact expressions exist:

```text
multiply exact expressions first;
apply declared simplification identities;
classify exact zero/nonzero;
then evaluate with directed or certified bounds.
```

When only intervals exist:

```text
retain the full product enclosure;
do not classify an interval containing zero as exact zero;
do not reduce the bound through observed floating cancellation.
```

Create:

```text
docs/next_level/c83_product_arithmetic_contract.json
docs/next_level/c83_product_bound_report.json
```

---

# 9. Pairwise aggregation

For every supported physical pair, sum all canonical product records.

Combine exact expressions first where possible. Otherwise perform stable,
deterministic, certified accumulation.

Assign one terminal pair-value status:

```text
NONZERO_EXACT_SOURCE_DERIVED_CONTACT_VALUE;

NONZERO_CERTIFIED_CONTACT_VALUE_INTERVAL_EXCLUDES_ZERO;

ZERO_BY_EXACT_KERNEL_VALUE;

ZERO_BY_EXACT_PROJECTED_COEFFICIENT;

ZERO_BY_EXACT_PAIR_COORDINATE_CANCELLATION;

CERTIFIED_CONTACT_VALUE_INTERVAL_INCLUDES_ZERO_NO_EXACT_ZERO;

UNAVAILABLE_BLOCKING.
```

Interpretation:

```text
C78 structural support remains unchanged;

a structurally supported pair may evaluate to exact zero;

an interval containing zero is not an exact-zero certificate;

UNAVAILABLE_BLOCKING is reserved for a genuine coefficient,
kernel, product, or aggregation failure.
```

A favorable gate requires:

```text
every one of the 154,830 supported pairs has exactly one terminal
value record;

UNAVAILABLE_BLOCKING = 0.
```

Create:

```text
docs/next_level/c83_contact_pair_value_manifest.json
docs/next_level/c83_contact_pair_value_status.json
docs/next_level/c83_contact_pair_aggregation_validation.json
```

---

# 10. Low-shell multiplication and aggregation pilot

Before full assembly, execute a frozen pilot containing:

```text
one diagonal supported pair;

one off-diagonal supported pair;

one pair with one C80 coordinate;

one pair with multiple C80 coordinates;

one exact-zero projected coefficient;

one exact-zero kernel value;

one exact pair-coordinate cancellation when available;

one Hermitian-conjugate pair;

one pair at each resolution.
```

For every pilot report:

```text
C82 coefficient records;
C80 kernel records;
shared coordinate identity;
P-minus and M-squared products;
pairwise exact/certified sum;
terminal value status;
bounds;
complete ancestry.
```

Create:

```text
docs/next_level/c83_low_shell_contact_matrix_pilot.json
```

---

# 11. Independent raw-space pair reconstruction

Implement a validation route that does not consume the C83 pair sum and
does not use the C82 pair-local aggregated coefficient as its numerical
input.

For frozen low- and mid-shell pairs:

```text
load physical bra and ket through C77;
expand them into exact/certified raw components;
enumerate source-ordered retained-q witnesses through C78;
construct each raw C80 coordinate directly;
evaluate the C80 kernel;
multiply the raw physical projection amplitudes and source-order
signs directly;
sum to the physical pair.
```

Compare the result with C83.

This route may use the C82 coordinate constructor only as a holdout after
the independent raw coordinate has already been constructed.

Create:

```text
docs/next_level/c83_independent_raw_space_pair_reconstruction.json
```

---

# 12. Sparse P-minus and M-squared matrices

Assemble deterministic sparse matrices for the coefficient of \(g_s^2\):

\[
\widehat P^-_{\rm contact,R},
\qquad
\widehat M^2_{\rm contact,R}.
\]

Use the immutable physical qg basis order inherited through C77/C82.

Store deterministic CSR or equivalent arrays for:

```text
row pointer;
column indices;
complex values;
absolute bounds;
terminal value-status codes;
canonical pair IDs;
support hashes;
C82 coefficient hashes;
C80 kernel hashes;
units;
factored-coupling identity.
```

Do not add unsupported pairs merely to store zeros.

A C78-supported pair that evaluates to exact zero remains in the
value-status domain. Whether it is retained as an explicit sparse zero or
stored only in the pair-value table must be declared deterministically and
must not alter structural support.

Verify entrywise:

\[
\widehat M^2_{\rm contact,R}
=
2P_R^+
\widehat P^-_{\rm contact,R}
\]

under the immutable C80 \(P_\perp=0\) convention.

Create:

```text
docs/next_level/c83_sparse_contact_matrix_manifest.json
docs/next_level/c83_pminus_m2_conversion_report.json
```

---

# 13. Independent matrix-free actions

Implement public actions equivalent to:

```python
apply_bare_iferm_contact_pminus_coefficient(vector, resolution)

apply_bare_iferm_contact_m2_coefficient(vector, resolution)
```

The matrix-free route must not multiply by the stored sparse matrix.

It must independently:

```text
iterate C82 pair-coordinate contributions;
query C80 kernels;
form certified coefficient-times-kernel products;
accumulate pair values;
scatter into the output vector.
```

Compare sparse and matrix-free actions on:

```text
all frozen basis-vector holdouts;
deterministic complex superpositions;
random normalized complex vectors;
small exact dense subblocks;
all three resolutions.
```

Create:

```text
docs/next_level/c83_matrix_free_contact_validation.json
```

---

# 14. Hermiticity and diagonal reality without repair

Use the immutable C82 conjugation relation and the C80 kernel-conjugation
map to prove:

\[
(b,k,\xi)
\longleftrightarrow
(k,b,\bar\xi),
\]

\[
C_{kb}^{[\bar\xi]}
=
\left(C_{bk}^{[\xi]}\right)^*,
\qquad
\widehat{\mathcal K}_{\bar\xi}
=
\left(\widehat{\mathcal K}_{\xi}\right)^*.
\]

Therefore validate:

\[
\widehat M_{\rm contact}^{2\dagger}
=
\widehat M_{\rm contact}^{2}.
\]

Report:

```text
exact conjugation-pair closure;
maximum numerical Hermiticity residual;
certified Hermiticity bound;
maximum imaginary diagonal residual;
certified diagonal-reality bound;
largest offending identity if a bound fails.
```

Do not:

```text
replace M by (M+M-dagger)/2;
copy one triangle;
discard diagonal imaginary parts;
clip entries;
or tune any source factor.
```

Create:

```text
docs/next_level/c83_contact_hermiticity_report.json
```

---

# 15. Quantum-number and block closure

Verify that every matrix entry respects all exact conserved labels declared
by the frozen direct-contact operator and basis, including as applicable:

```text
resolution and total K;
total Jz;
allowed quark/gluon helicity transfer;
longitudinal conservation;
color-triplet sector;
CM-ground sector;
zero-mode/boundary policy;
declared transverse selection.
```

Build a deterministic block decomposition and require zero support outside
allowed blocks.

Create:

```text
docs/next_level/c83_contact_block_structure_report.json
```

---

# 16. Matrix diagnostics without false physical claims

For each resolution report:

```text
physical matrix dimension;
C78 structural-support count;
pair-coordinate product count;
exact nonzero count;
certified nonzero count;
exact projected-coefficient-zero count;
exact kernel-zero count;
exact pair-cancellation count;
interval-includes-zero count;
sparse stored nnz;
density;
Frobenius norm;
operator-norm estimate;
maximum diagonal magnitude;
maximum off-diagonal magnitude;
Hermiticity residual;
matrix-free residual;
maximum entry bound.
```

Optional eigenvalue diagnostics are finite-resolution bare-operator
diagnostics only.

Do not claim:

```text
a physical spectrum;
positivity;
boundedness below;
renormalization;
or a continuum limit.
```

Create:

```text
docs/next_level/c83_contact_matrix_diagnostics.json
docs/next_level/c83_contact_resolution_comparison.json
```

The resolution comparison must keep \(K\), \(N_{\max}\), \(b_{\rm HO}\),
basis size, support, and numerical certification changes separate.

---

# 17. C53 and C58 isolation

After the C83 matrices and hashes are frozen:

## C53

Poison:

```text
canonical vertex values;
adjoints;
propagators;
resolvents;
sequential V-dagger G V interfaces.
```

Require byte-identical C83 reconstruction.

Prove C83 is the direct W3 contact, not a sequential canonical-vertex
substitute.

## C58

Preserve C58 byte-identically as a separate q-sector self-induced-inertia
block.

Verify:

```text
C58 acts on retained q;
C83 acts on physical qg;
monomial ancestries differ;
support identities differ;
no matrix entry, mode, factor, or future counterterm direction is
counted twice.
```

Create:

```text
docs/next_level/c83_c53_non_substitution_report.json
docs/next_level/c83_c58_separation_report.json
```

---

# 18. Public immutable API and runtime package

Use:

```text
src/deuteron_wigner/bridge/ifcontact4/
data/runtime/c83_ifcontact4/
```

or exact repository-equivalent paths.

Provide immutable public operations equivalent to:

```python
load_bare_iferm_contact_matrix_package(resolution)

contact_pair_value_status(bra_id, ket_id, resolution)

contact_pminus_element_coefficient(bra_id, ket_id, resolution)

contact_m2_element_coefficient(bra_id, ket_id, resolution)

load_sparse_contact_pminus_coefficient(resolution)

load_sparse_contact_m2_coefficient(resolution)

apply_bare_iferm_contact_pminus_coefficient(vector, resolution)

apply_bare_iferm_contact_m2_coefficient(vector, resolution)
```

Every returned result must expose:

```text
coefficient-of-g_s^2 identity;
P-minus/M-squared value and bound;
units;
C78 structural-support ancestry;
C82 projected-coefficient ancestry;
C80 kernel ancestry;
terminal value status;
canonical pair identity.
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject object dtype, unsafe paths, unindexed files, writable arrays, and
mutable records.

Create one authenticated C83 runtime index/root in this package.

Create:

```text
docs/next_level/c83_api_contract.json
docs/next_level/c83_api_validation.json
docs/next_level/c83_runtime_inventory.json
docs/next_level/c83_deterministic_reconstruction_report.json
```

---

# 19. C84/IFERM3 import contract

Define the exact immutable contract by which C84 consumes:

```text
the C58 q-sector self-induced-inertia package;
the C83 qg-sector direct-contact package;
their basis orders and sector domains;
their coefficient-of-g_s^2 conventions;
their P-minus/M-squared conventions;
their sparse and matrix-free actions;
their certified bounds;
their monomial and support ancestry;
their non-overlap certificate;
their resolution identities.
```

C84 must be able to assemble the block operator on

\[
\mathcal H_q\oplus\mathcal H_{qg}
\]

without recomputing either block.

C84 must not:

```text
choose a physical coupling;
lift the q block into qg;
lift the qg block into q;
add C53 propagation;
identify a counterterm coefficient;
or call either historical builder.
```

Create:

```text
docs/next_level/c83_c84_iferm3_import_contract.json
```

---

# 20. Factorized execution and resource policy

The logical C78 coordinate domains remain extremely large. C83 must not
materialize them densely.

Use:

```text
C82 pair-coordinate iterators;
C80 exact kernel-class caching;
deterministic pair shards;
stable exact/certified accumulation;
restartable content-addressed checkpoints.
```

Report:

```text
pair-coordinate product count;
unique kernel count;
cache-hit ratio;
compression ratio relative to logical coordinates;
peak memory;
serial/sharded/restart timing;
checkpoint and shard hashes.
```

Run:

```text
two consecutive full builds;
one clean build;
one serial build;
one supported deterministic sharded build;
one restart/resume build.
```

Require byte-identical runtime and tracked artifacts.

Create:

```text
docs/next_level/c83_resource_and_scaling_report.json
```

---

# 21. Isolation and mutation controls

Required failures include mutations of:

```text
C78 support;
C82 coordinate ID;
C82 projected coefficient;
C82 coefficient bound;
C80 kernel value;
C80 kernel bound;
factor-ownership identity;
shared coordinate schema;
product arithmetic;
pair aggregation order;
exact-zero classification;
P-minus/M-squared conversion;
row/column basis order;
Hermitian-conjugation map;
diagonal-reality rule;
sparse storage;
matrix-free iterator;
physical-coupling insertion;
post-hoc symmetrization;
C53 substitution;
C58 substitution;
unsafe loading;
mutable return.
```

Create at least **384 focused live mutations** of actual coordinate,
coefficient, kernel, product, pair, sparse, matrix-free, bound, API, and
runtime objects.

Create:

```text
docs/next_level/c83_isolation_report.json
docs/next_level/c83_regression_report.json
```

---

# 22. Readiness and continuation decisions

Select exactly one branch.

## 22.1 Favorable branch

Issue the exact positive status frozen by the committed C83 contract, with
preferred semantic identity:

```text
C83_SOURCE_DERIVED_BARE_IFERM_CONTACT_MATRIX_READY
```

Required:

```text
shared coordinate compatibility closes;
factor ownership revalidates;
every pair-coordinate product has a terminal result;
every one of the 154,830 supported pairs has one terminal value;
UNAVAILABLE_BLOCKING = 0;
P-minus/M-squared conversion closes;
independent raw-space holdouts agree;
sparse and independent matrix-free actions agree;
Hermiticity and diagonal reality close without repair;
block/quantum-number closure passes;
C53 non-substitution and C58 separation pass;
runtime/API/determinism/isolation/mutations pass.
```

Next:

> **C84/IFERM3 — complete bare instantaneous-fermion operator assembly, counterterm-direction typing, and local-HQCD continuation gate**

## 22.2 Shared-coordinate incompatibility

```text
C83_IFCONTACT_SHARED_COORDINATE_INCOMPLETE
```

Next:

> **C84/IFCOORD2 — repair only the specifically identified C82/C80 coordinate identity or schema mismatch**

## 22.3 Product arithmetic incomplete

```text
C83_IFCONTACT_PRODUCT_EVALUATION_INCOMPLETE
```

Next:

> **C84/IFPRODUCT — repair only the specifically identified coefficient, kernel, exact-expression, interval, or factor-ownership product defect**

## 22.4 Pair aggregation incomplete

```text
C83_IFCONTACT_PAIR_VALUE_INCOMPLETE
```

Next:

> **C84/IFPAIR — repair only the specifically identified pair sum, terminal status, exact-zero, or bound-accumulation defect**

## 22.5 Matrix certification incomplete

```text
C83_IFCONTACT_MATRIX_CERTIFICATION_INCOMPLETE
```

Next:

> **C84/IFMATCERT2 — repair only the sparse/matrix-free, P-minus/M-squared, precision, runtime, or deterministic-certification defect**

## 22.6 Hermiticity incomplete

```text
C83_IFCONTACT_HERMITICITY_INCOMPLETE
```

Next:

> **C84/IFHERMIT2 — repair only the specifically identified conjugation, phase, endpoint, coordinate, or assembly defect**

Do not issue a complete-instantaneous-fermion, counterterm-solved,
renormalized, local-QCD, TMD, matching, inference, or production status.

---

# 23. Essential deliverables

Create at least:

```text
docs/next_level/c83_implementation_report.md
docs/next_level/c83_derivation_authority_manifest.json
docs/next_level/c83_input_fidelity_audit.json
docs/next_level/c83_input_freeze.json

docs/next_level/c83_shared_coordinate_compatibility.json
docs/next_level/c83_shared_coordinate_validation.json
docs/next_level/c83_factor_ownership_revalidation.json

docs/next_level/c83_pair_coordinate_product_manifest.json
docs/next_level/c83_pair_coordinate_product_validation.json
docs/next_level/c83_product_arithmetic_contract.json
docs/next_level/c83_product_bound_report.json

docs/next_level/c83_contact_pair_value_manifest.json
docs/next_level/c83_contact_pair_value_status.json
docs/next_level/c83_contact_pair_aggregation_validation.json
docs/next_level/c83_low_shell_contact_matrix_pilot.json
docs/next_level/c83_independent_raw_space_pair_reconstruction.json

docs/next_level/c83_sparse_contact_matrix_manifest.json
docs/next_level/c83_pminus_m2_conversion_report.json
docs/next_level/c83_matrix_free_contact_validation.json
docs/next_level/c83_contact_hermiticity_report.json
docs/next_level/c83_contact_block_structure_report.json
docs/next_level/c83_contact_matrix_diagnostics.json
docs/next_level/c83_contact_resolution_comparison.json

docs/next_level/c83_c53_non_substitution_report.json
docs/next_level/c83_c58_separation_report.json
docs/next_level/c83_api_contract.json
docs/next_level/c83_api_validation.json
docs/next_level/c83_runtime_inventory.json
docs/next_level/c83_deterministic_reconstruction_report.json
docs/next_level/c83_resource_and_scaling_report.json
docs/next_level/c83_c84_iferm3_import_contract.json

docs/next_level/c83_isolation_report.json
docs/next_level/c83_readiness_report.json
docs/next_level/c83_regression_report.json
```

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 24. Acceptance criteria

C83 is complete only when:

1. Baseline `8e47231ab565f0f729d335b39aa98881176ba166` reproduces.
2. Both protected untracked paths remain untouched.
3. C58/C77/C78/C80/C82 historical artifacts remain unchanged.
4. `c83_ifcontact4_contract.md` is consumed and preserved.
5. C82 and C80 package roots verify before multiplication.
6. Inputs freeze before one product is formed.
7. Every C82 coordinate matches exactly one C80 coordinate.
8. No coordinate is selected by floating equality.
9. Factor ownership closes with no omission or duplication.
10. \(g_s^2\) remains explicit and factored.
11. No physical coupling or counterterm is selected.
12. Every pair-coordinate contribution forms a legal product.
13. Exact multiplication is used where exact expressions exist.
14. Certified product bounds are propagated rigorously.
15. Every supported physical pair has exactly one terminal value record.
16. `UNAVAILABLE_BLOCKING` is zero in the favorable branch.
17. Structural support remains the immutable C78 support.
18. Exact zeros are distinguished from intervals containing zero.
19. The low-shell pilot closes.
20. Independent raw-space pair holdouts agree.
21. Sparse P-minus and M-squared matrices are constructed.
22. Their exact \(2P^+\) conversion closes.
23. Dense logical coordinate domains are never materialized.
24. Dense full physical matrices are not required.
25. Sparse and independent matrix-free actions agree.
26. Hermiticity closes without post-hoc repair.
27. Diagonal reality closes within certified bounds.
28. Exact quantum-number/block closure passes.
29. C53 values and propagators do not enter construction.
30. C58 remains byte-identical and separate.
31. Resolution diagnostics make no continuum or physical-spectrum claim.
32. The public API returns immutable safe objects.
33. Deterministic serial, clean, sharded, and restart builds agree.
34. At least 384 focused live mutations pass.
35. No complete instantaneous-fermion operator, counterterm solution, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
36. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
37. The working tree is clean except for the two protected untracked paths.
38. A local completion commit is created and not pushed.

Do not weaken the shared-coordinate identity, factor boundary, certified
product arithmetic, structural/value distinction, sparse/matrix-free
independence, or Hermiticity to open the gate.

---

# 25. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- consumed C83 contract identity;
- C78/C80/C82 package/root identities;
- imported supported-pair count;
- pair-coordinate product counts by resolution;
- unique C80 coordinate/kernel counts;
- coordinate compatibility and factor-ownership results;
- product-status counts;
- pair-value status counts;
- exact-zero, exact-cancellation, certified-nonzero, and interval-includes-zero counts;
- maximum product and matrix-entry bounds;
- physical matrix shapes;
- sparse nnz and density;
- P-minus/M-squared conversion residual;
- independent raw-space holdout residuals;
- sparse-versus-matrix-free residuals and bounds;
- Hermiticity and diagonal-reality residuals and bounds;
- block-structure closure;
- matrix norms and finite-resolution diagnostics;
- compression, cache, peak-memory, and shard results;
- C53 non-substitution and C58 separation;
- public API/runtime-root and deterministic-reconstruction hashes;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that \(g_s^2\) remains factored and no physical coupling, counterterm, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
