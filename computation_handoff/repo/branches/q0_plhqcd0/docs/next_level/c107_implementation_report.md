# C107/IFCOEFFBIND

Status: `C107_C104_SOURCE_DERIVED_CERTIFIED_COEFFICIENT_SYMBOL_BINDING_READY`.

C107 binds the unchanged C104 `CONJUGATE`/`MULTIPLY` AST to authenticated
project-owned authorities. C77 supplies 37,284 unique endpoint component
midpoints and directed bounds; C74 supplies all 72 retained-triplet U3
entries and bounds. Pair KIN/TRIP identities and C104 coordinate axes define
one unique bra and ket component/color key per logical record.

The evaluator uses the exact AST order and recursively propagates absolute
enclosures through complex multiplication. It never imports C80, forms a
kernel product, consumes C53/C58, or selects a physical coupling. The full
factorized census is 154,830 programs and 891,992,018 evaluator paths.
