# C134/HQCDTARGET implementation report

Four immutable target capsules are published with separate source, target, and finite-basis-adapter semantics. All four remain unavailable: no Tier 1--3 target or exact adapter exists. No numerical default, parameter, counterterm, renormalized matrix, state, or downstream object is created.
