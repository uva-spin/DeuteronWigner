# C111/IFCONTACT7

Status: `C111_C107_C110_SOURCE_DERIVED_QG_DIRECT_CONTACT_OPERATOR_READY`.

C111 consumes immutable C107 coefficients and the C110 corrected M2 kernel
exactly once.  It uses a bounded group-by-coordinate factorized route and
does not write the 891,992,018-record stream.  Product enclosures use
`|c|eK+|K|ec+ec*eK`; `g_s^2` remains factored.  The public pair and operator
surfaces are immutable and source-ordered; C53, C58, physical coupling,
counterterm, and uncorrected C80 P-minus values are not consumed.
