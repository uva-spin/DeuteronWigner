# C92/IFEQUIVAPI Codex Work Package

## Title

**Authenticated immutable C90 public-equivalence import surface: complete 154,830-pair enumeration, paged semantic ledger, primitive-record access, exported expansion-proof checker, strict no-recomputation loading, and C93 exhaustive-equivalence handoff**

## Authoritative baseline

Start from the clean local C91 completion commit:

```text
3ea461f3abcb32f7a56bd71a0dda9dd384e412d6
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "3ea461f3abcb32f7a56bd71a0dda9dd384e412d6"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

Consume the sole committed C92 continuation contract completely. The expected repository path is:

```text
docs/next_level/c92_ifequivapi_contract.json
```

If the committed sole continuation contract uses a different extension or exact path, discover and use that tracked file, record its path and hash, and do not invent a second contract.

If the contract freezes exact status names, schemas, APIs, or a narrower scientific boundary, preserve them. This work package supplies the public packaging, authenticated enumeration, proof-checker export, and validation plan; it does not authorize changes to C90 scientific content.

The baseline is authoritative only when it contains and reproduces:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
```

with the following exact scientific situation:

```text
C90:
    historical C82 factorized-semantic science is complete;
    154,830 historical pair attestations exist in a compact ledger;
    all 891,992,018 logical records are represented through typed
    factorized semantics and the structural expansion checker;
    aggregate historical authority verifies through the immutable
    public API;
    the roots are FACTORIZED_SEMANTIC_PROGRAM_ROOT objects, not
    expanded serialized-record-stream hashes;

C91:
    independently compiles and freezes the complete current
    descendant domain:
        154,830 pair programs;
        891,992,018 logical records;
    does not access private C90 historical pair outputs;
    cannot execute the exhaustive comparison because C90's public
    surface exposes neither:
        an authenticated pair enumeration/paged ledger;
        nor the exported expansion-theorem checker;

C91 scientific conclusion:
    no scientific mismatch is claimed;
    the blocker is a project-owned public-interface deficiency;
    no primitive crosswalk, pair equivalence, equivalence
    certificate, persistence readiness, or physical contact object
    has been claimed.
```

The historical C82 completion commit remains:

```text
8e47231ab565f0f729d335b39aa98881176ba166
```

The historical environment remains qualified as:

```text
HISTORICAL_C82_SOURCE_WITH_C87_CANONICAL_COLOR_AUTHORITY
```

The exact historical C72 runtime instance remains:

```text
UNKNOWN_NOT_CLAIMED
```

Verify and record the full C90 aggregate semantic root, C90 runtime root/index hashes, C90 theorem/checker fingerprints, C91 descendant-domain roots, all resolution counts, and all contract hashes from the repository. Prefixes or prose summaries are not authority.

Create a local completion commit. Do not push.

---

# 1. Scientific classification

C92 is a **public artifact and API completion**.

It is not:

```text
a new scientific derivation;
a new C82 reconstruction;
a new historical authority;
a new primitive-table calculation;
a new semantic normal form;
or a new expansion theorem.
```

C90 science remains authoritative and unchanged. C91's independently
compiled descendant domain remains unchanged.

No external publication is required for:

```text
authenticated pagination;
pair-record inclusion proofs;
public immutable loaders;
primitive-table enumeration;
proof-checker export;
API fingerprints;
or a descendant wrapper around an existing internal C90 ledger.
```

A fail-closed result is required only when:

```text
the complete C90 pair ledger cannot be recovered from authenticated
C90-owned artifacts;

the public package cannot prove exact equivalence to C90;

the pair order or counts cannot be exposed completely;

the primitive records required by the theorem are not available;

the expansion checker cannot be exported without changing its
semantics;

or the public loader must recompute C90/C82 science to answer a
query.
```

Do not seek outside authority for a repository-specific import surface.

---

# 2. Descendant qualifications

Do not alter historical C90 or C91 artifacts.

Create explicit descendant qualifications:

```text
C90_C82_FACTORIZED_SEMANTIC_ATTESTATION_READY
    -> C90_SCIENCE_COMPLETE_PUBLIC_PAIR_IMPORT_INCOMPLETE

C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
    -> descendant compiler complete;
       exhaustive comparison blocked only by absent authenticated
       historical pair enumeration and proof-checker import.
```

These qualifications must not retract or rewrite C90's scientific status.

Create:

```text
docs/next_level/c92_descendant_qualification.json
```

---

# 3. Exact purpose and stopping point

C92 must produce:

```text
a complete audit of the existing C90 runtime payload and internal
ledger representation;

a canonical C92 public package bound to the unchanged C90
historical semantic authority;

authenticated enumeration of every one of the 154,830 historical
pair attestations;

deterministic bounded pagination by resolution and global pair
sequence;

direct immutable lookup by pair ID and sequence;

public access to each pair normal form, summaries, primitive-root
dependencies, and proof inputs;

complete authenticated primitive-family enumeration and record
access sufficient for C93's historical/descendant crosswalk;

an exported immutable C90 structural expansion-theorem
specification and proof checker;

record-, page-, resolution-, and package-level authentication;

strict no-upstream-recomputation behavior;

exhaustive public-package equivalence to the existing C90 ledger;

a public-only C93 preflight;

and a C93/IFEQUIV7 import contract.
```

C92 must not:

```text
recompile historical C82 semantics;

reconstruct C90 from C77/C78/C82 private builders;

change any historical pair normal form, summary, primitive root,
or proof certificate;

compare all historical and descendant programs scientifically;

issue historical-versus-descendant equivalence;

persist 891,992,018 expanded records;

form a projected-coefficient times C80-kernel product;

construct a contact matrix or matrix-free contact action;

choose a physical coupling or counterterm;

add C53 or C58;

or assemble the complete instantaneous-fermion operator.
```

The preferred positive semantic status is:

```text
C92_C90_PUBLIC_EQUIVALENCE_IMPORT_READY
```

If the committed C92 contract freezes another exact spelling, preserve it
and record an explicit semantic alias.

The favorable continuation is:

> **C93/IFEQUIV7 — resume the C91 historical-versus-descendant exhaustive factorized-semantic comparison using only the immutable C92 historical public API and the immutable C91 descendant compiler**

---

# 4. Mandatory inputs

Read completely the actual repository equivalents of:

```text
C87:
    canonical color scientific/capsule roots and claim boundary;

C88:
    scientific-record schema and field classification;
    exact logical census;

C89:
    factorized pair programs and mixed-radix rank/unrank;

C90:
    semantic IR;
    normal-form schema;
    primitive scientific tables;
    Route-A/Route-B compilers;
    structural expansion-equivalence theorem;
    proof checker;
    pair ledger;
    pair summaries;
    record audits;
    historical aggregate semantic root;
    public API;
    runtime root/index;
    deterministic reconstruction;
    tests and validator;

C91:
    independent descendant compiler;
    complete descendant-domain freeze;
    public-equivalence blocker audit;
    C92 continuation contract.
```

Create:

```text
docs/next_level/c92_derivation_authority_manifest.json
docs/next_level/c92_input_fidelity_audit.json
```

---

# 5. Freeze C90 science before public packaging

Consume C90 through every already available public operation and audit its
existing runtime payload without changing it.

Freeze:

```text
historical C82 commit;
historical environment qualification;
C87 scientific/capsule roots;
C88 scientific schema;
C90 semantic-IR and normal-form versions;
C90 theorem and proof-checker versions;
all primitive-family roots;
all three resolution identities;
historical supported-pair total and resolution counts;
historical pair order;
all pair normal-form roots;
all pair summaries;
all historical proof-certificate identities;
resolution roots;
aggregate historical semantic root;
C90 runtime root and index.
```

Issue:

```text
C92_C90_SCIENTIFIC_INPUTS_FROZEN_COMPLETE
```

in:

```text
docs/next_level/c92_input_freeze.json
```

After this freeze, no C90 scientific field may be altered to improve
packaging or pagination.

---

# 6. Audit the actual C90 persisted payload

Determine exactly what the C90 runtime already persists.

Inventory:

```text
pair-ledger files;
pair-order tables;
normal-form tables;
summary tables;
primitive-family tables;
proof certificates;
theorem specification;
proof-checker source;
runtime root/index;
page or shard metadata;
public and private loaders.
```

For each object record:

```text
path;
role;
schema;
record count;
shape/dtype;
size;
SHA-256;
whether covered by the C90 runtime root;
whether currently public;
whether currently private/internal;
whether sufficient for direct immutable exposure;
whether a C92 descendant wrapper is required.
```

Create:

```text
docs/next_level/c92_c90_payload_inventory.json
docs/next_level/c92_c90_public_surface_gap_matrix.json
```

Do not assume that the prose statement “154,830-entry ledger” guarantees
that all records are persisted in consumer-ready form. Verify the payload.

---

# 7. Select exactly one packaging route

Select one mutually exclusive route.

## Route A — direct authenticated C90 facade

Use when the complete pair, primitive, and theorem payloads already exist
under the C90 root and only public methods are missing.

C92 adds a public facade that reads the unchanged C90 payload.

## Route B — C92 descendant public snapshot of existing C90 payload

Use when the complete scientific payload exists but its internal file
layout or loader cannot safely be exposed directly.

C92 may transform only the **existing authenticated C90 records** into a
new public representation.

Require exhaustive record equivalence to C90.

## Route C — unavailable

Use when a required pair, primitive, theorem, or proof-checker object is
not present in authenticated C90-owned artifacts and would require
recomputing historical C82/C90 science.

C92 must not silently invoke historical private builders to manufacture a
new scientific authority.

Create:

```text
docs/next_level/c92_packaging_route_decision.json
```

For Route B, the C92 package receives a new descendant package root while
remaining bound to the unchanged C90 scientific root. It must never be
called the original C90 runtime instance.

---

# 8. Canonical historical pair-attestation schema

Define one immutable public record equivalent to:

```python
HistoricalC90PairAttestation
```

Each record must expose or bind:

```text
schema version;
global pair sequence;
resolution ID and resolution-local sequence;
canonical pair ID;
physical bra and ket IDs;
row/column indices;
historical normal-form root;
normal-form record or authenticated reference;
primitive dependency roots;
exact logical record count;
canonical mixed-radix order identity;
record-construction expression root;
projected-coefficient expression root;
bound-rule root;
status-rule root;
ancestry-rule root;
first and last logical-record identities;
closed-form pair summary;
historical pair proof-certificate identity;
historical environment qualification;
C90 aggregate semantic root;
canonical attestation-record ID.
```

The record must explicitly state:

```text
root_semantics = FACTORIZED_SEMANTIC_PROGRAM_ROOT;
expanded_serialized_record_stream_hash = false;
exact_historical_c72_runtime_instance_recovered = false.
```

Create:

```text
docs/next_level/c92_pair_attestation_schema.json
docs/next_level/c92_pair_attestation_schema_validation.json
```

---

# 9. Complete pair order and enumeration

Freeze and expose the exact historical order:

```text
resolution order;
global pair sequence 0 ... 154,829;
resolution-local pair sequence;
pair ID;
physical bra/ket IDs.
```

Require:

```text
154,830 total records;
every global sequence appears exactly once;
no gap;
no duplicate;
no pair outside the C90 ledger;
every C90 pair appears;
resolution counts match C90;
first and last identities match C90.
```

Create:

```text
docs/next_level/c92_pair_enumeration_manifest.json
docs/next_level/c92_pair_enumeration_validation.json
```

---

# 10. Authenticated bounded pagination

Implement deterministic pagination.

A page record must bind:

```text
C92 package root;
C90 scientific root;
resolution filter;
global sequence start and stop;
record count;
ordered pair IDs;
ordered pair-attestation digests;
previous-page digest where applicable;
page digest;
next-cursor identity.
```

Cursor tokens must bind:

```text
package root;
query/filter identity;
next global sequence;
page-size policy;
schema version.
```

Requirements:

```text
page size has a declared safe maximum;
pagination is stable across repeated calls;
a cursor cannot be used against another package or query;
changed roots invalidate the cursor;
all pages concatenate to the exact global pair order;
no page overlaps or skips another page;
end-of-sequence is explicit.
```

Create:

```text
docs/next_level/c92_pagination_contract.json
docs/next_level/c92_pagination_validation.json
```

---

# 11. Merkle/inclusion authentication

Provide efficient authentication for:

```text
one pair record;
one page;
one resolution;
the complete pair ledger.
```

Use the repository's established digest conventions or define a canonical
Merkle-style tree.

The public API must be able to verify a returned pair against:

```text
its page;
its resolution root;
the C92 package root;
the bound C90 aggregate semantic root.
```

Create:

```text
docs/next_level/c92_pair_inclusion_proof_contract.json
docs/next_level/c92_pair_inclusion_proof_validation.json
```

Do not confuse inclusion in the C92 package with a new scientific proof of
the pair program. Scientific semantics remain C90-owned.

---

# 12. Public normal-form and summary access

Expose complete immutable access to:

```text
historical pair normal form;
node graph;
primitive dependencies;
exact cardinality;
canonical order;
record/coefficient/bound/status/ancestry expression roots;
first/last logical identities;
pair summary;
pair proof-certificate inputs.
```

The public result may inline compact records or return authenticated
content-addressed references.

No public method may inspect a private C90 object after returning data.

Create:

```text
docs/next_level/c92_normal_form_public_contract.json
docs/next_level/c92_normal_form_public_validation.json
```

---

# 13. Primitive scientific-family public package

C93 must be able to perform the historical/descendant primitive crosswalk
without private C90 access.

Expose every C90 primitive family required by the theorem, including the
actual repository equivalents of:

```text
supported-pair identities;
physical bra/ket identities;
C77 projection-amplitude values/exact records/bounds;
C78 witnesses, endpoints, retained-q intermediates, and signs;
C87 color records;
C80 coordinate templates and equivalence identities;
metric and conjugation records;
coefficient-expression primitives;
bound primitives;
status rules;
ancestry primitives.
```

For every family expose:

```text
family ID;
schema;
canonical order;
record count;
family scientific root;
producer/provenance identity;
paged record access;
direct record lookup;
record inclusion proof.
```

Create:

```text
docs/next_level/c92_primitive_family_manifest.json
docs/next_level/c92_primitive_public_access_validation.json
```

A root and count without record-level or theorem-level access is
insufficient.

---

# 14. Export the immutable expansion-theorem specification

Expose the complete C90 theorem specification through a public immutable
record.

It must include:

```text
semantic-IR node types;
node expansion semantics;
normal-form rules;
primitive-root requirements;
cardinality rules;
ordering rules;
record-map requirements;
coefficient/bound/status/ancestry rules;
theorem statement;
proof-certificate schema;
theorem version and scientific hash.
```

Create:

```text
docs/next_level/c92_expansion_theorem_public_spec.json
docs/next_level/c92_expansion_theorem_spec_validation.json
```

The theorem remains C90 science. C92 only makes it publicly consumable.

---

# 15. Export the expansion-proof checker

Implement a safe public checker equivalent to:

```python
verify_factorized_expansion_equivalence(
    historical_normal_form,
    descendant_normal_form,
    primitive_equivalence_certificates,
    *,
    theorem_version,
) -> ExpansionEquivalenceResult
```

The checker must:

```text
use the unchanged C90 theorem semantics;
accept only the typed C90 IR;
reject unknown node types and opaque callables;
verify schemas and scientific roots;
verify primitive-equivalence certificates;
verify cardinality and order;
verify record/coefficient/bound/status/ancestry expressions;
return a frozen result and proof-certificate root;
perform no leaf expansion over the complete domain;
query no C80 numerical kernel values.
```

The public checker source/API fingerprint and theorem hash must be bound by
the C92 package.

Create:

```text
docs/next_level/c92_expansion_checker_contract.json
docs/next_level/c92_expansion_checker_validation.json
```

Run the checker against the frozen C90 Route-A/Route-B pair proofs for all
154,830 historical pairs and require the public checker to reproduce the
existing C90 result.

This is a regression of the exported checker, not a new historical-
descendant comparison.

---

# 16. Strict no-recomputation public loader

Implement under:

```text
src/deuteron_wigner/bridge/ifequivapi/
```

or the exact repository-equivalent package.

The public loader must read only authenticated C90/C92 payloads.

Poison and forbid calls to:

```text
C77 builders;
C78 builders;
C82 witness/coordinate/coefficient constructors;
C89 pair-program builder;
C90 Route-A compiler;
C90 Route-B normalizer;
C90 historical worktree reconstruction;
build-if-missing;
repair-if-missing;
network access.
```

The public loader may verify records and execute the exported proof checker,
but it may not recreate missing historical pair programs.

Create:

```text
docs/next_level/c92_no_recomputation_contract.json
docs/next_level/c92_no_recomputation_validation.json
```

---

# 17. Safe immutable payloads

Use safe payloads under a repository-equivalent root:

```text
data/runtime/c92_ifequivapi/
```

or, for a compact tracked authority package when repository policy permits:

```text
data/authority/c92_c90_public_equivalence_import/
```

Every NumPy load must explicitly use:

```python
allow_pickle=False
```

Reject:

```text
object dtype;
pickle-dependent payloads;
path traversal;
unexpected symlinks;
unindexed files;
shape/dtype/endianness mismatch;
writable arrays;
mutable returned records;
unknown schema versions.
```

Create:

```text
docs/next_level/c92_safe_loading_contract.json
docs/next_level/c92_safe_loading_validation.json
```

---

# 18. Public API

Provide immutable operations equivalent to:

```python
load_verified_c90_public_equivalence_authority()

verify_c90_public_equivalence_authority()

historical_pair_count(resolution=None)

historical_pair_page(
    *,
    resolution=None,
    cursor=None,
    limit=...,
)

historical_pair_by_sequence(global_sequence)

historical_pair_attestation(pair_id, resolution)

historical_pair_normal_form(pair_id, resolution)

historical_pair_summary(pair_id, resolution)

historical_pair_proof_inputs(pair_id, resolution)

historical_primitive_family(family_id)

historical_primitive_page(
    family_id,
    *,
    cursor=None,
    limit=...,
)

historical_primitive_record(family_id, record_id)

expansion_theorem_specification()

verify_factorized_expansion_equivalence(...)
```

Every returned object must expose its package, page/record, and scientific
root identities.

Create:

```text
docs/next_level/c92_api_contract.json
docs/next_level/c92_api_validation.json
docs/next_level/c92_runtime_inventory.json
```

---

# 19. Exhaustive C90-to-C92 equivalence

Use builder/validation access to compare every public C92 object with the
existing authenticated C90 ledger.

For all 154,830 pairs compare:

```text
pair sequence;
pair ID;
resolution;
normal-form root;
normal-form content;
primitive dependencies;
logical count;
order identity;
expression roots;
first/last identities;
summary;
proof-certificate identity.
```

For every primitive family compare all records or apply the exact C90
source-chain theorem that proves the public representation equivalent.

Require:

```text
missing pairs = 0;
extra pairs = 0;
order mismatches = 0;
pair-content mismatches = 0;
primitive mismatches = 0;
proof-checker mismatches = 0;
page/inclusion failures = 0.
```

Create:

```text
docs/next_level/c92_exhaustive_c90_public_equivalence.json
```

A sampled comparison is insufficient.

---

# 20. C91 blocker supersession

Create an explicit descendant supersession record:

```text
C91_IFEQUIV6_PUBLIC_EQUIVALENCE_INCOMPLETE
    -> closed by C92 authenticated pair enumeration, primitive
       access, and exported proof checker.
```

The supersession must not claim the C91 scientific comparison has already
been performed.

Create:

```text
docs/next_level/c92_c91_blocker_supersession_report.json
```

---

# 21. Public-only C93 preflight

Using only:

```text
the C92 public historical authority;
the frozen C91 descendant compiler/public artifacts;
```

perform a preflight that verifies:

```text
all 154,830 historical pairs can be enumerated exactly once;
all historical pair normal forms can be loaded publicly;
all primitive families needed by C91 are accessible;
all historical proof inputs are accessible;
the exported proof checker executes;
the descendant pair domain exposes the compatible schema;
beginning, middle, ending, largest, and boundary pair checks pass;
no private C90 access occurs.
```

Do not execute the complete historical-versus-descendant comparison in
C92.

Create:

```text
docs/next_level/c92_c93_ifequiv7_preflight.json
docs/next_level/c92_c93_ifequiv7_import_contract.json
```

---

# 22. Deterministic reconstruction

Run:

```text
two complete C92 package/facade builds;
one clean build;
one serial build;
one deterministic sharded build where applicable;
one interrupted/resumed build;
two complete pagination traversals with different permitted page
sizes.
```

Require:

```text
identical scientific records;
identical pair order;
identical primitive records;
identical page-independent pair/resolution/package roots;
identical proof-checker result;
identical C93 preflight.
```

Physical page boundaries may differ by requested page size, but the
scientific pair order and package root must remain invariant.

Create:

```text
docs/next_level/c92_deterministic_reconstruction_report.json
docs/next_level/c92_resource_and_scaling_report.json
```

---

# 23. Isolation and mutation controls

Required failures include mutations of:

```text
C90 aggregate root;
C90 runtime root/index;
C92 package root/index;
pair sequence;
pair ID;
resolution;
normal-form root or content;
primitive dependency;
logical count;
order identity;
record/coefficient/bound/status/ancestry expression root;
pair summary;
proof-certificate identity;
page range;
page digest;
cursor root or cursor sequence;
resolution root;
record inclusion proof;
primitive-family root;
primitive record;
theorem specification;
proof-checker source/API fingerprint;
proof-checker node semantics;
no-recomputation guard;
unsafe loading;
mutable return;
C93 import-contract field.
```

The scientific public package must remain invariant under mutations of:

```text
temporary build path;
host/process ID;
page request size;
pagination traversal order;
wall-clock timestamp;
narrative report path.
```

Poison:

```text
C80 numerical kernel values;
C53 values and propagators;
C58 values;
physical coupling;
counterterm interfaces.
```

C92 must remain unchanged.

Create at least **384 focused live mutations** of actual pair records,
pages, cursors, primitive records, theorem/checker objects, loaders,
indices, and roots.

Create:

```text
docs/next_level/c92_isolation_report.json
docs/next_level/c92_regression_report.json
```

---

# 24. Readiness and continuation decisions

Select exactly one branch.

## 24.1 Favorable branch

Issue the exact positive status frozen by the C92 contract, with preferred
semantic identity:

```text
C92_C90_PUBLIC_EQUIVALENCE_IMPORT_READY
```

Required:

```text
C90 science verifies and remains unchanged;
the complete C90 persisted payload is audited;
one valid packaging route is selected;
all 154,830 pair attestations are publicly enumerable;
pagination is complete, stable, bounded, and authenticated;
direct pair lookup and inclusion proof pass;
all pair normal forms and summaries are publicly available;
all required primitive scientific families are publicly available;
the immutable C90 theorem specification is exported;
the public proof checker reproduces all historical C90 pair proofs;
the public loader performs no upstream recomputation;
safe immutable loading passes;
exhaustive C90-to-C92 equivalence has zero mismatches;
the C91 blocker is explicitly superseded;
the public-only C93 preflight passes;
determinism and mutations pass.
```

Next:

> **C93/IFEQUIV7 — exhaustive historical-versus-descendant factorized-semantic comparison using only immutable public APIs**

## 24.2 C90 payload incomplete

```text
C92_IFEQUIVAPI_C90_PAYLOAD_INCOMPLETE
```

Next:

> **C93/IFC90PAYLOAD — recover only the specifically identified missing authenticated C90 pair, primitive, theorem, or proof-certificate payload**

## 24.3 Pair enumeration incomplete

```text
C92_IFEQUIVAPI_PAIR_ENUMERATION_INCOMPLETE
```

Next:

> **C93/IFPAIRAPI — repair only the pair order, count, page, cursor, direct lookup, inclusion proof, or resolution-root defect**

## 24.4 Primitive public access incomplete

```text
C92_IFEQUIVAPI_PRIMITIVE_IMPORT_INCOMPLETE
```

Next:

> **C93/IFPRIMAPI — repair only the primitive family schema, enumeration, record lookup, inclusion proof, or scientific-root defect**

## 24.5 Expansion checker export incomplete

```text
C92_IFEQUIVAPI_EXPANSION_CHECKER_INCOMPLETE
```

Next:

> **C93/IFCHECKER — repair only the theorem specification, typed-IR loader, proof-checker export, fingerprint, or historical-regression defect**

## 24.6 No-recomputation/public loader incomplete

```text
C92_IFEQUIVAPI_PUBLIC_LOADER_INCOMPLETE
```

Next:

> **C93/IFPUBLIC — repair only the package root/index, safe loading, immutability, private-access, build-if-missing, or deterministic-reconstruction defect**

Do not issue historical-versus-descendant scientific equivalence, final
factorized persistence, coefficient-times-kernel, contact-matrix,
complete-instantaneous-fermion, counterterm-solved, local-QCD, TMD,
matching, inference, or production status.

---

# 25. Essential deliverables

Create at least:

```text
docs/next_level/c92_implementation_report.md
docs/next_level/c92_derivation_authority_manifest.json
docs/next_level/c92_input_fidelity_audit.json
docs/next_level/c92_descendant_qualification.json
docs/next_level/c92_input_freeze.json

docs/next_level/c92_c90_payload_inventory.json
docs/next_level/c92_c90_public_surface_gap_matrix.json
docs/next_level/c92_packaging_route_decision.json

docs/next_level/c92_pair_attestation_schema.json
docs/next_level/c92_pair_attestation_schema_validation.json
docs/next_level/c92_pair_enumeration_manifest.json
docs/next_level/c92_pair_enumeration_validation.json
docs/next_level/c92_pagination_contract.json
docs/next_level/c92_pagination_validation.json
docs/next_level/c92_pair_inclusion_proof_contract.json
docs/next_level/c92_pair_inclusion_proof_validation.json

docs/next_level/c92_normal_form_public_contract.json
docs/next_level/c92_normal_form_public_validation.json
docs/next_level/c92_primitive_family_manifest.json
docs/next_level/c92_primitive_public_access_validation.json

docs/next_level/c92_expansion_theorem_public_spec.json
docs/next_level/c92_expansion_theorem_spec_validation.json
docs/next_level/c92_expansion_checker_contract.json
docs/next_level/c92_expansion_checker_validation.json

docs/next_level/c92_no_recomputation_contract.json
docs/next_level/c92_no_recomputation_validation.json
docs/next_level/c92_safe_loading_contract.json
docs/next_level/c92_safe_loading_validation.json

docs/next_level/c92_api_contract.json
docs/next_level/c92_api_validation.json
docs/next_level/c92_runtime_inventory.json
docs/next_level/c92_exhaustive_c90_public_equivalence.json
docs/next_level/c92_c91_blocker_supersession_report.json
docs/next_level/c92_c93_ifequiv7_preflight.json
docs/next_level/c92_c93_ifequiv7_import_contract.json

docs/next_level/c92_deterministic_reconstruction_report.json
docs/next_level/c92_resource_and_scaling_report.json
docs/next_level/c92_isolation_report.json
docs/next_level/c92_readiness_report.json
docs/next_level/c92_regression_report.json
```

Add implementation under:

```text
src/deuteron_wigner/bridge/ifequivapi/
```

Use compact runtime or retained authority payloads under the selected,
documented C92 package root.

Create exactly one next-package contract corresponding to the selected
branch.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 26. Acceptance criteria

C92 is complete only when:

1. Baseline `3ea461f3abcb32f7a56bd71a0dda9dd384e412d6` reproduces.
2. Both protected untracked paths remain untouched.
3. C87/C88/C89/C90/C91 historical artifacts remain unchanged.
4. The sole committed C92 continuation contract is consumed and preserved.
5. Exact historical C72 runtime-instance recovery remains unclaimed.
6. C90 roots remain labeled factorized-semantic roots, not expanded-stream hashes.
7. C90 science verifies before public packaging.
8. The actual C90 persisted payload is inventoried completely.
9. The packaging route is explicit.
10. No C77/C78/C82/C89/C90 scientific recomputation is used to answer public queries.
11. Every one of the 154,830 historical pair attestations is publicly represented.
12. Pair global sequences are complete and unique.
13. Resolution counts and order reproduce C90.
14. Pagination has no gaps or overlaps.
15. Cursors are root-, query-, and sequence-bound.
16. Direct pair lookup verifies inclusion.
17. Pair normal forms and summaries are publicly available.
18. Primitive dependencies are publicly available.
19. Every required primitive family supports complete authenticated access.
20. The C90 theorem specification is exported without semantic change.
21. The public proof checker accepts only typed C90 IR.
22. The public proof checker reproduces all historical C90 pair-proof results.
23. The public loader verifies root/index/page/record identities.
24. Every NumPy load explicitly uses `allow_pickle=False`.
25. Object dtype, unsafe paths, unindexed files, writable arrays, and mutable records are rejected.
26. Exhaustive C90-to-C92 pair equivalence has zero mismatch.
27. Primitive equivalence has zero mismatch.
28. The C91 public-interface blocker is explicitly superseded.
29. The C93 public-only preflight traverses all historical pairs.
30. The C93 preflight uses no private C90 access.
31. Two clean builds agree.
32. Serial, deterministic sharded, and restart builds agree.
33. Different permitted page sizes preserve the package scientific root and pair order.
34. At least 384 focused live mutations pass.
35. No historical-versus-descendant equivalence claim is made.
36. No final canonical coefficient persistence package is claimed.
37. No expanded full-record stream is written.
38. No coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, local-QCD Hamiltonian, TMD/matching, fit, inference, or production object is created.
39. `NO_JOINT_MEASURE`, the 216-route registry, all 642 ART25 identities, and authoritative artifacts remain unchanged.
40. The working tree is clean except for the two protected untracked paths.
41. A local completion commit is created and not pushed.

Do not weaken pair completeness, primitive access, theorem/checker identity,
inclusion authentication, no-recomputation behavior, or exhaustive C90
equivalence merely to open the gate.

---

# 27. Final Codex response

Report:

- starting and final commits;
- untouched protected paths;
- exact consumed C92 contract path and hash;
- historical C82 commit and environment qualification;
- full C90 scientific/runtime roots;
- C91 descendant-domain roots and counts;
- selected packaging route;
- C90 payload file/object counts;
- C92 package path, root, index, size, and file count;
- pair totals and resolution splits;
- page count under the canonical default page size;
- pagination and cursor validation;
- direct pair inclusion-proof result;
- normal-form and summary-access result;
- primitive-family counts, record counts, and roots;
- theorem-specification and proof-checker hashes;
- number of historical pair proofs reproduced by the public checker;
- no-recomputation poisoning result;
- exhaustive C90-to-C92 mismatch counts;
- C91 blocker supersession result;
- C93 public-only preflight result;
- deterministic/restart/page-size invariance results;
- isolation and mutation results;
- exact readiness/no-go status;
- exact next branch;
- confirmation that exact historical C72 runtime-instance recovery remains unclaimed;
- confirmation that roots are factorized-semantic roots, not expanded serialized-record hashes;
- confirmation that no historical-versus-descendant equivalence, final canonical coefficient persistence, expanded full-record stream, coefficient-times-kernel product, contact matrix/action, physical coupling, counterterm, C53/C58 contribution, complete instantaneous-fermion operator, TMD/matching, fit, inference, or production object was created;
- confirmation that nothing was pushed.
