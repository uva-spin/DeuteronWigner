# C403 ChatGPT-Stage Implementation Report

## Result

C403 implements the first numerical substructure of the C117 `I2_density_projector` direction while retaining the complete C117 coordinate action as unavailable.

Implemented:

- an exact finite candidate/member axis at K9, K11, and K13;
- an exact support theorem replacing the missing C64 runtime dependency at this narrow scope;
- 1774 exhaustive partition/species/mode support records;
- numerical C45-convention transverse-HO density kernels for all 139 admitted internal modes on the C47 intrinsic/relative qg external basis;
- analytic exact-rational and independent Gauss--Laguerre routes;
- sparse CSR and independently evaluated matrix-free actions;
- a C396 binding overlay updating three `c_C117_1` rows at primitive scope.

The one-quark external C45 matrix basis and full q/qg target embedding remain unassembled. The full numerical C396 apply count remains six. No numerical C117 coordinate action, coefficient, physical state, production current, rank, fit, or activation is claimed.

## Main mathematical results

The finite support is exact:

```text
ADMITTED iff 2*n+abs(m) <= Nmax-2
REJECTED iff 2*n+abs(m) = Nmax-1
```

For a quark mode of shell `N`, the exact C62 CM-ground witness is `x_g^(N/2)`. For a gluon mode it is `(-1)^N x_q^(N/2)`.

The spatial primitive is

```text
I[a,b;r] = integral d^2x phi_a^*(x) phi_b(x) |phi_r(x)|^2
```

with units `GeV^2`. Every single-member matrix is Hermitian and positive semidefinite. The largest representative analytic/quadrature residual is below `4.1e-17 GeV^2`; sparse and independent matrix-free actions agree to zero at the recorded floating representation.

## Validation

```text
C403 focused tests:                    16 passed
C401 regression:                       14 passed
C400.S2 regression:                    26 passed
C45/C47 direct source regression:       4 passed
Python compilation:                    PASS
Two clean generator builds:            21/21 byte-identical
```

The full historical C62 readiness test remains blocked by the pre-existing missing C53 runtime array:

```text
data/runtime/c53_vertex2/physical_diagnostic_m2_K9_2_N8_b0.40.npy
```

C403 does not require that artifact because it calls the exact C62 coefficient algebra directly and exhaustively.

## Remaining frontier

The smallest missing object is the K-local, source-faithful contraction of the C114 source/inverse-transfer factor, C119 current factors, C115 spin/color/normalization factors, the C403 finite axis and spatial kernel, and the q/qg target aggregation with Hermitian reverse action.
