# C74/QGCOLOR6 Codex Work Package

## Title

**Public immutable authority-API completion for the C72 triplet package: authenticated basis/status/exact-record/bound loaders, explicit `allow_pickle=False` enforcement, frozen package-object closure, and C75 physical-embedding preflight**

## Authoritative baseline

Start from the clean local C73 fail-closed completion commit:

```text
db309507b36ab2053bd7d2c85bd6b7944e68a3c1
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "db309507b36ab2053bd7d2c85bd6b7944e68a3c1"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

The baseline is authoritative only when it contains and reproduces:

```text
C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY

C66_SOURCE_DERIVED_TRIPLET_ISOMETRY_ARTIFACT_READY

C68_SOURCE_DERIVED_TRIPLET_ISOMETRY_RUNTIME_READY

C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY

C73_QGEMBED_C72_IMPORT_INCOMPLETE
```

and the exact C73 import finding:

```text
C72 aggregate package:
    authenticated index and package root exist;
    scientific color payload is unchanged;
    exact 24-row / 3-column / 72-entry authority data are present
    in the authenticated package;

C72 public interface actually exposed to a consumer:
    numerical-array loading only;

missing public immutable operations:
    product-color basis loader;
    retained-triplet basis loader;
    72-pair-domain loader;
    terminal-status loader;
    exact-record loader;
    exact-expression loader;
    exact-zero-certificate loader;
    per-entry-bound loader;
    complete frozen authority-package loader;

security defect:
    numerical loading does not explicitly and universally enforce
    numpy.load(..., allow_pickle=False);

C73 consequence:
    C73 cannot infer basis identities, exact records, statuses, or
    bound identities from private fields, array positions, shapes,
    or numerical values;
    no CM-ground or physical qg embedding was constructed;
    no descendant-impact or contact work was created.
```

Verify every statement from the actual C72 package, C72 implementation, C72 public exports, and C73 import audit. This prompt is not numerical authority.

The exact scientific payload remains immutable:

\[
U_3=\frac{E_{\rm src}}{\sqrt{C_F}},
\qquad
C_F=\frac43,
\qquad
U_3^\dagger U_3=I_3,
\qquad
P_3=U_3U_3^\dagger .
\]

C74 must not change:

```text
the C72 scientific package root;

the 24 ordered product-color rows;

the 3 ordered retained-triplet columns;

the 72 ordered row-column identities;

the 72 terminal exact statuses;

any exact expression;

any exact-zero certificate;

any numerical midpoint or certified bound;

E_src, Gram, triplet adapter, U3, U3-dagger, or P3;

the all-eight-generator intertwining result;

the rank-three projector result;

the zero anti-sextet and 15 leakage result;

the inherited C53-impact status.
```

C64 remains immutable and read-only.

Create a local completion commit. Do not push.

---

# 1. Descendant correction and ownership

C72 completed the **scientific authority content**, but C73 proved that the **public import implementation** was incomplete.

Do not alter historical C72 files. Create an explicit descendant correction:

```text
historical status:
    C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY

descendant qualification:
    C72_TRIPLET_AUTHORITY_CONTENT_COMPLETE_PUBLIC_API_INCOMPLETE

reason:
    the authenticated payload contains the required authority, but
    the public package does not expose immutable loaders for the
    basis, pair domain, statuses, exact records, expressions,
    zero certificates, and per-entry bounds, and the numerical
    loading path does not explicitly enforce allow_pickle=False.
```

Ownership is:

```text
C66:
    exact triplet-isometry science;

C68:
    first exact/certified runtime payload;

C70:
    partial authenticated array package;

C72:
    complete authenticated scientific-authority payload;

C74:
    complete authenticated public import implementation for the
    unchanged C72 payload.
```

C74 is an interface descendant. It must not copy or repackage the full scientific payload unless a byte-identical copy is strictly required by the existing repository architecture. The preferred implementation references and verifies the unchanged C72 package root.

---

# 2. Execution mandate

C74 must close all C73 public-import blockers in this package:

1. public immutable basis loaders;
2. public immutable status and exact-record loaders;
3. public immutable exact-expression and zero-certificate loaders;
4. public immutable per-entry-bound loaders;
5. one complete frozen top-level package object;
6. explicit `allow_pickle=False` on every NumPy load path;
7. rejection of object-dtype or pickle-dependent numerical payloads;
8. authenticated source/API identity for the new public interface;
9. a passing C75 public-only import preflight.

Do not create another authority-content package. Do not derive another \(U_3\). Do not create a new basis convention, exact-record vocabulary, or bound convention.

A fail-closed result is valid only for a concrete inconsistency in the existing authenticated C72 content or for an implementation limitation that prevents a safe immutable public loader.

A preferred filename, extra report, alternate schema, or convenience wrapper is not a valid no-go.

---

# 3. Exact purpose

C74 must create:

```text
a complete audit of the existing C72 authenticated content;

an explicit descendant correction to the C72 readiness claim;

a frozen source fingerprint for the public loader implementation;

a frozen public-API fingerprint;

a minimal authenticated C74 interface root bound to the unchanged
C72 scientific package root;

immutable public loaders for:
    24 product-color basis records;
    3 retained-triplet basis records;
    72 ordered pair identities;
    72 terminal statuses;
    72 exact-record authorities;
    all exact expressions;
    all exact-zero certificates;
    72 per-entry bound authorities;
    companion-object authorities;
    certified numerical objects and their bounds;

one frozen top-level TripletAuthorityPackage object;

explicit safe numerical loading with allow_pickle=False;

strict rejection of object-dtype/pickle-dependent arrays;

strict no-regeneration and no-inference behavior;

complete public-loader equivalence with the C72 package content;

package-only invariant/action validation;

a C75/QGEMBED8 public import contract;

and a dry C75 preflight using only the public C64 and C74 APIs.
```

C74 must not construct:

```text
a CM-ground kinematic embedding;

a full physical qg embedding;

C47/C52/C53/C57/C58 descendant-impact results beyond preserving
the inherited color-side C53-impact record;

direct-contact endpoint or witness support;

a contact value or matrix;

a complete instantaneous-fermion operator;

a Wilson/TMD or matching object.
```

The strongest positive status is:

```text
C74_SOURCE_DERIVED_TRIPLET_PUBLIC_IMPORT_READY
```

The exact positive continuation is:

> **C75/QGEMBED8 — construct the exact CM-ground and color-triplet physical \(qg\) embedding and close descendant impact using immutable C64 and C74 public packages**

---

# 4. Mandatory repository audit

Inventory actual files and public exports:

```bash
git ls-files 'docs/next_level/c72*'
git ls-files 'docs/next_level/c73*'
git ls-files 'src/deuteron_wigner/**/qgcolor5*'
git ls-files 'tests/*c72*'
git ls-files 'tests/*c73*'
find data/runtime/c72_qgcolor5 -maxdepth 3 -type f | sort
```

Read completely the actual repository equivalents of:

```text
C72 package-root manifest and authenticated index;

C72 source/API fingerprints;

C72 24-row, 3-column, and 72-pair manifests;

C72 exact-record, expression, zero-certificate, and per-entry-bound
manifests;

C72 companion-object and inventory records;

C72 public implementation and exported symbols;

C72 package-object contract;

C72 no-regeneration/no-inference report;

C72 tests and validator;

C73 C72-import report, implementation report, missing-calculation
specification, readiness report, and regression report.
```

Use actual filenames. Do not create a missing-file obstruction from a naming difference when the authenticated content exists under another path.

Create:

```text
docs/next_level/c74_derivation_authority_manifest.json
docs/next_level/c74_input_fidelity_audit.json
```

---

# 5. Preserve and verify the C72 scientific package root

Before changing public loader code, load and verify the C72 package using the lowest-level authenticated path that the C72 validator already trusts.

Record:

```text
C72 package-root hash;

index hash;

source/API fingerprints stored by C72;

row, column, pair-domain hashes;

exact-record, expression, zero-certificate, support, and
per-entry-bound hashes;

companion-object hashes;

payload and inventory counts.
```

This low-level route is permitted only inside the C74 implementation and validation layer for constructing the new public loader. C75 must not use it.

Create:

```text
docs/next_level/c74_c72_payload_import_report.json
docs/next_level/c74_c72_payload_integrity_validation.json
```

Any mismatch blocks C74. Do not repair or regenerate the C72 payload.

---

# 6. Public-loader source fingerprint

Freeze every tracked source file that defines the C74 public import behavior.

Include:

```text
public loader module;

path and index verification;

JSON/exact-record parsing;

NumPy safe-loading helper;

immutability conversion;

top-level package object;

public action helpers;

failure types;

no-regeneration guards.
```

Every source record must contain:

```text
repository-relative path;

SHA-256;

public symbols;

role;

construction/validation classification;

schema or serializer identity.
```

Compute:

```text
c74_loader_source_fingerprint_sha256
```

Create:

```text
docs/next_level/c74_loader_source_fingerprint.json
docs/next_level/c74_loader_source_fingerprint_validation.json
```

---

# 7. Public-API fingerprint

Define the complete public surface before implementation.

At minimum include operations equivalent to:

```python
load_triplet_authority_package() -> TripletAuthorityPackage

load_product_color_basis()
load_retained_triplet_basis()
load_triplet_pair_domain()

load_triplet_statuses()
load_triplet_exact_records()
load_triplet_exact_expressions()
load_triplet_zero_certificates()
load_triplet_per_entry_bounds()

load_companion_object_authorities()

load_certified_source_emission()
load_certified_gram()
load_certified_triplet_adapter()
load_certified_triplet_isometry()
load_certified_triplet_adjoint()
load_certified_triplet_projector()

embed_triplet_color_to_product(...)
project_product_color_to_triplet(...)
apply_triplet_projector(...)
```

For every public symbol authenticate:

```text
module and qualified name;

call signature;

return schema/type;

immutability semantics;

C72-root verification semantics;

safe-loading semantics;

failure behavior;

no-regeneration/no-inference behavior.
```

Compute:

```text
c74_public_api_fingerprint_sha256
```

Create:

```text
docs/next_level/c74_api_fingerprint_manifest.json
docs/next_level/c74_api_fingerprint_validation.json
```

---

# 8. Minimal authenticated C74 interface root

Create a minimal interface root such as:

```text
data/runtime/c74_qgcolor6/
```

It must not duplicate the C72 scientific payload.

The interface root contains only deterministic control records needed to authenticate the public implementation, including:

```text
interface.json;

interface.sha256;

expected C72 package-root hash;

C74 loader-source fingerprint;

C74 public-API fingerprint;

public return-schema identities;

safe-loading policy;

immutability policy;

no-regeneration/no-inference policy;

expected public authority counts:
    rows = 24;
    columns = 3;
    pairs = 72;
    statuses = 72;
    exact records = 72;
    per-entry bounds = 72.
```

The authenticated `interface.json` hash is the C74 interface-root identity.

Create:

```text
docs/next_level/c74_interface_schema_contract.json
docs/next_level/c74_interface_root_manifest.json
docs/next_level/c74_interface_authentication_report.json
```

The top-level loader must verify both:

```text
the C74 interface root;

the bound C72 scientific package root.
```

---

# 9. Safe NumPy loading policy

Implement one internal numerical-loading helper used by every public numerical loader.

Every call to `numpy.load` must pass explicitly:

```python
allow_pickle=False
```

This is required even when the installed NumPy default is already false.

For `.npz` and `.npy` files:

```text
reject object dtype;

reject arrays requiring pickle;

reject unexpected structured/object fields;

verify shape, dtype, endianness, and hashes before return;

set writeable=False before return;

never return an NpzFile handle to the consumer;

close archive handles deterministically.
```

Reject any package path that:

```text
escapes the authenticated root;

uses `..`;

resolves through an unexpected symlink;

is absent from the authenticated index;

or changes after index verification.
```

Create:

```text
docs/next_level/c74_safe_numpy_loading_contract.json
docs/next_level/c74_safe_numpy_loading_validation.json
```

Required negative controls include:

```text
omitted allow_pickle argument;

allow_pickle=True;

object-dtype .npy;

object-dtype .npz;

pickle-dependent payload;

writable returned array;

path traversal;

unindexed numerical file;

changed array after root verification.
```

---

# 10. Immutable authority record types

Create frozen public record types for:

```text
ProductColorBasisRow;

RetainedTripletBasisColumn;

TripletPairIdentity;

TripletTerminalStatus;

TripletExactRecord;

TripletExactExpression;

TripletZeroCertificate;

TripletPerEntryBound;

CompanionObjectAuthority;

CertifiedArray;

TripletAuthorityPackage.
```

Use:

```text
frozen dataclasses;

tuples;

read-only mappings / MappingProxyType;

or equivalent immutable structures.
```

Do not return mutable dictionaries, lists, or sets as authoritative records.

Every record must expose its authenticated identity hash.

Create:

```text
docs/next_level/c74_immutable_record_contract.json
docs/next_level/c74_immutable_record_validation.json
```

---

# 11. Public basis and pair-domain loaders

Implement public immutable loaders for:

```text
24 ordered product-color rows;

3 ordered retained-triplet columns;

72 ordered pair identities.
```

The loaders must:

```text
verify both roots;

load authenticated C72 basis records;

preserve canonical order;

preserve row/column/pair IDs;

preserve ancestry and basis hashes;

return immutable tuples.
```

They must not infer identities from:

```text
U3 array position;

array shape;

private C72 object fields;

or a generator constructor.
```

Create:

```text
docs/next_level/c74_basis_loader_contract.json
docs/next_level/c74_basis_loader_validation.json
```

---

# 12. Public status and exact-record loaders

Implement public immutable loaders for:

```text
all 72 terminal statuses;

all 72 exact-record authorities.
```

Each returned exact record must retain:

```text
entry ID;

row ID;

column ID;

terminal exact status;

exact support;

exact-expression or zero-certificate identity;

normalization ancestry;

basis-adapter ancestry;

source-emission ancestry;

per-entry-bound identity.
```

The status loader must not simplify the inherited exact vocabulary to only zero/nonzero.

Support must not be inferred from numerical magnitude.

Create:

```text
docs/next_level/c74_status_exact_record_loader_contract.json
docs/next_level/c74_status_exact_record_loader_validation.json
```

---

# 13. Public exact-expression and zero-certificate loaders

Implement public immutable loaders for:

```text
every exact nonzero expression;

every exact-zero certificate.
```

Exact-expression records must support:

```text
canonical equality;

content hashing;

arbitrary-precision evaluation through an explicitly declared safe
evaluator;

source ancestry.
```

The public loader must not execute arbitrary code from a serialized expression.

Zero-certificate records must retain the exact inherited proof class and certificate hash.

Do not infer an exact expression from a numerical midpoint.

Do not infer a zero certificate from a zero or small midpoint.

Create:

```text
docs/next_level/c74_expression_zero_loader_contract.json
docs/next_level/c74_expression_zero_loader_validation.json
```

---

# 14. Public per-entry-bound loader

Implement a public immutable loader for all 72 per-entry bound authorities.

Each returned record must couple:

```text
entry ID;

row and column IDs;

exact-record hash;

terminal status;

midpoint real and imaginary parts;

absolute-error bound;

interval convention;

precision;

dtype and endianness;

array object IDs and indices;

midpoint scalar hash;

bound scalar hash;

combined numerical identity hash.
```

Verify every record against the authenticated C72 per-entry-bound domain.

The loader must not derive entry-level bound identity solely from an array-level hash.

Create:

```text
docs/next_level/c74_per_entry_bound_loader_contract.json
docs/next_level/c74_per_entry_bound_loader_validation.json
```

---

# 15. Public certified numerical loaders

Implement safe immutable loaders for:

```text
E_src;

Gram;

triplet adapter;

U3;

U3-dagger;

P3;

required permutations;

all associated bound arrays.
```

Every `CertifiedArray` must expose:

```text
midpoint array;

bound array;

object ID;

shape;

dtype;

basis hashes;

exact-object identity;

array and bound hashes.
```

The midpoint and bound must be returned together.

All arrays must be non-writeable.

All loads must use the single safe helper with explicit `allow_pickle=False`.

Create:

```text
docs/next_level/c74_certified_array_loader_contract.json
docs/next_level/c74_certified_array_loader_validation.json
```

---

# 16. Frozen top-level package object

Implement:

```python
load_triplet_authority_package() -> TripletAuthorityPackage
```

The loader must:

1. verify the C74 interface root;
2. verify the bound C72 scientific package root;
3. verify the C72 complete inventory;
4. load every public authority domain through the authenticated paths;
5. verify the expected counts and cross-domain identities;
6. return one frozen object.

The package object must expose:

```text
C72 scientific package root;

C74 interface root;

source/API fingerprints;

basis and pair domains;

statuses and exact records;

expressions and zero certificates;

per-entry bounds;

companion-object authorities;

certified numerical objects;

C53-impact status;

public embed/project/projector actions.
```

No consumer may need to access a private field or runtime path.

Create:

```text
docs/next_level/c74_package_object_contract.json
docs/next_level/c74_package_object_validation.json
```

---

# 17. No-regeneration and no-inference guards

The public implementation must be unable to call:

```text
C66 build();

C68 build();

C70 build();

C72 build();

the exact U3 constructor;

raw_emission_E normalization;

P3 factorization;

a C53 vertex builder;

a build-if-missing fallback;

a repair-if-missing fallback.
```

It must never infer:

```text
basis identity from array position;

status from support Boolean alone;

support from numerical magnitude;

an exact expression from a midpoint;

a zero certificate from a zero midpoint;

a per-entry bound identity from an array-level hash.
```

Create:

```text
docs/next_level/c74_no_regeneration_no_inference_report.json
```

---

# 18. Complete public-loader equivalence

Compare the public C74 returns with the authenticated C72 payload.

Require complete equivalence for:

```text
24 rows;

3 columns;

72 pair IDs;

72 statuses;

72 exact records;

every exact expression;

every zero certificate;

72 per-entry bounds;

all companion-object authorities;

all certified numerical arrays and bounds;

C53-impact status.
```

Require exact record equality and byte/numerical identity as appropriate.

Create:

```text
docs/next_level/c74_public_loader_equivalence_report.json
```

A sampled comparison is insufficient.

---

# 19. Package-only invariant and action validation

Using only the public C74 package object, verify:

\[
U_3^\dagger U_3=I_3,
\]

\[
P_3=U_3U_3^\dagger,
\qquad
P_3^\dagger=P_3,
\qquad
P_3^2=P_3.
\]

Also verify:

```text
rank(P3) = 3;

trace(P3) = 3;

all-eight-generator intertwining;

triplet preservation;

zero anti-sextet leakage;

zero 15 leakage;

embed/project round trips;

entry-level interval containment.
```

No private C72 access or builder call is permitted in this validation route.

Create:

```text
docs/next_level/c74_public_package_invariant_report.json
```

---

# 20. C75/QGEMBED8 import contract

Define the exact contract by which C75 consumes:

```text
the C74 interface root;

the bound C72 scientific package root;

source/API fingerprints;

24/3/72 basis authority;

72 exact records;

all expressions and zero certificates;

72 per-entry bounds;

certified U3/U3-dagger/P3 and companion objects;

complete public TripletAuthorityPackage;

public embed/project/projector actions.
```

C75 may use only public C64 and C74 symbols.

C75 may not:

```text
open C72 runtime paths;

inspect private C72 fields;

call C66/C68/C70/C72/C74 builders;

infer identity from array position;

infer support from magnitude;

load numerical files without allow_pickle=False;

renormalize U3;

factor P3;

or choose new phases.
```

Create:

```text
docs/next_level/c74_c75_qgembed8_import_contract.json
```

---

# 21. Dry C75 public-only import preflight

Run a dry preflight using only:

```text
the C64 immutable public API;

the C74 immutable public API.
```

Verify:

```text
both package/interface roots;

all source/API fingerprints;

C64 basis/crosswalk authority;

C74 24/3/72 authority;

C74 exact records and per-entry bounds;

all arrays and bounds;

immutability;

explicit safe NumPy loading;

no-regeneration/no-inference guards;

metadata needed for future kinematic/color composition.
```

Do not construct the CM-ground or full physical embedding in C74.

Create:

```text
docs/next_level/c74_c73_blocker_supersession_report.json
docs/next_level/c74_c75_import_preflight.json
```

The report must explicitly establish that:

```text
C73_QGEMBED_C72_IMPORT_INCOMPLETE
```

is superseded by the C74 public API.

---

# 22. Deterministic reconstruction

Run:

```text
two consecutive complete C74 interface builds;

one clean temporary build;

one serial build;

one supported parallel/sharded build where supported;

one restart/resume build.
```

Require byte-identical:

```text
interface.json;

interface.sha256;

source/API fingerprint records;

public-schema records;

all tracked manifests and reports.
```

The C72 scientific payload must remain byte-identical.

Create:

```text
docs/next_level/c74_deterministic_reconstruction_report.json
docs/next_level/c74_restart_parallel_report.json
docs/next_level/c74_environment_manifest.json
```

---

# 23. Count-once, isolation, and mutation controls

Report:

```text
rows loaded = 24;

columns loaded = 3;

pairs loaded = 72;

statuses loaded = 72;

exact records loaded = 72;

exact expressions;

zero certificates;

per-entry bounds loaded = 72;

certified numerical objects;

mutable records;

writable arrays;

unsafe NumPy loads;

object-dtype arrays accepted;

private-field dependencies;

builder calls;

missing identities;

duplicate identities;

hash mismatches;

unresolved-impact entries.
```

A positive gate requires every error count to be zero.

Prove C74 construction and public loading are independent of:

```text
C53 stored physical vertex values;

historical raw_emission_E values after ancestry is frozen;

historical C53 projectors;

C47/C50/C52/C57/C58 numerical objects;

C64 arrays except during the C75 preflight;

ART25 files.
```

Create at least **320 focused live mutations** including:

```text
C72 root;

C74 interface root;

source fingerprint;

API fingerprint;

public signature;

basis row/column/pair ID;

status;

exact record;

expression;

zero certificate;

per-entry midpoint;

per-entry bound;

array index;

dtype;

endianness;

allow_pickle flag;

object dtype;

path traversal;

symlink escape;

array writeability;

record mutability;

private-field access;

builder-call guard;

C75 import-contract field.
```

Create:

```text
docs/next_level/c74_count_once_report.json
docs/next_level/c74_isolation_report.json
docs/next_level/c74_regression_report.json
```

---

# 24. Readiness gate

Issue:

```text
C74_SOURCE_DERIVED_TRIPLET_PUBLIC_IMPORT_READY
```

only when:

```text
the full C73 baseline reproduces;

both required untracked paths remain untouched;

the C72 scientific package root and payload remain unchanged;

the C72 public-API limitation is recorded as a descendant
qualification;

the C74 loader-source fingerprint exists and verifies;

the C74 public-API fingerprint exists and verifies;

the minimal C74 interface root exists and binds the exact C72 root;

all 24 row records load publicly and immutably;

all 3 column records load publicly and immutably;

all 72 pair records load publicly and immutably;

all 72 statuses load publicly and immutably;

all 72 exact records load publicly and immutably;

all exact expressions load publicly and immutably;

all zero certificates load publicly and immutably;

all 72 per-entry bounds load publicly and immutably;

all certified numerical objects load publicly with midpoint and
bound coupled;

every NumPy load explicitly uses allow_pickle=False;

object-dtype and pickle-dependent arrays are rejected;

all arrays are non-writeable;

all records are immutable;

the top-level TripletAuthorityPackage verifies both roots before
returning data;

no loader calls a builder or inference fallback;

complete public-loader equivalence passes;

public-package invariants and actions pass;

the C75 import contract is complete;

the C75 public-only preflight passes;

the C73 blocker supersession is explicit;

deterministic reconstruction passes;

count-once, isolation, and mutation tests pass.
```

Do not issue:

```text
C74_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY;

C74_IFERM_CONTACT_SUPPORT_READY;

C74_DIRECT_IFERM_CONTACT_READY;

C74_SOURCE_DERIVED_INSTANTANEOUS_FERMION_READY;

C74_COMPLETE_LOCAL_HQCD_SUBSTRATE_READY.
```

---

# 25. Exact no-go branches

## A. C72 authenticated content is inconsistent

```text
C74_QGCOLOR6_C72_CONTENT_INCONSISTENT
```

Next:

> **C75/QGCOLOR-CONTENT — repair only the specifically identified authenticated C72 content defect**

## B. Public authority-record loaders remain incomplete

```text
C74_QGCOLOR6_PUBLIC_RECORD_IMPORT_INCOMPLETE
```

Next:

> **C75/QGCOLOR-RECORDS3 — close only the basis/status/exact-record/expression/zero/bound public-loader defect**

## C. Safe numerical loading remains incomplete

```text
C74_QGCOLOR6_SAFE_NUMERICAL_IMPORT_INCOMPLETE
```

Next:

> **C75/QGCOLOR-SAFELOAD — close only allow_pickle, object-dtype, path, immutability, or midpoint/bound coupling defects**

## D. Top-level package/API authentication remains incomplete

```text
C74_QGCOLOR6_PUBLIC_PACKAGE_INCOMPLETE
```

Next:

> **C75/QGCOLOR-API2 — close only interface-root, API fingerprint, frozen package-object, or no-regeneration defects**

## E. Public import closure succeeds

```text
C74_SOURCE_DERIVED_TRIPLET_PUBLIC_IMPORT_READY
```

Next:

> **C75/QGEMBED8 — exact physical \(qg\) embedding and descendant-impact closure**

---

# 26. Required deliverables

Create at least:

```text
docs/next_level/c74_implementation_report.md
docs/next_level/c74_api.md
docs/next_level/c74_derivation_authority_manifest.json
docs/next_level/c74_input_fidelity_audit.json

docs/next_level/c74_c72_descendant_correction.json
docs/next_level/c74_c72_payload_import_report.json
docs/next_level/c74_c72_payload_integrity_validation.json

docs/next_level/c74_loader_source_fingerprint.json
docs/next_level/c74_loader_source_fingerprint_validation.json
docs/next_level/c74_api_fingerprint_manifest.json
docs/next_level/c74_api_fingerprint_validation.json

docs/next_level/c74_interface_schema_contract.json
docs/next_level/c74_interface_root_manifest.json
docs/next_level/c74_interface_authentication_report.json

docs/next_level/c74_safe_numpy_loading_contract.json
docs/next_level/c74_safe_numpy_loading_validation.json
docs/next_level/c74_immutable_record_contract.json
docs/next_level/c74_immutable_record_validation.json

docs/next_level/c74_basis_loader_contract.json
docs/next_level/c74_basis_loader_validation.json
docs/next_level/c74_status_exact_record_loader_contract.json
docs/next_level/c74_status_exact_record_loader_validation.json
docs/next_level/c74_expression_zero_loader_contract.json
docs/next_level/c74_expression_zero_loader_validation.json
docs/next_level/c74_per_entry_bound_loader_contract.json
docs/next_level/c74_per_entry_bound_loader_validation.json
docs/next_level/c74_certified_array_loader_contract.json
docs/next_level/c74_certified_array_loader_validation.json

docs/next_level/c74_package_object_contract.json
docs/next_level/c74_package_object_validation.json
docs/next_level/c74_no_regeneration_no_inference_report.json

docs/next_level/c74_public_loader_equivalence_report.json
docs/next_level/c74_public_package_invariant_report.json

docs/next_level/c74_c75_qgembed8_import_contract.json
docs/next_level/c74_c73_blocker_supersession_report.json
docs/next_level/c74_c75_import_preflight.json

docs/next_level/c74_deterministic_reconstruction_report.json
docs/next_level/c74_restart_parallel_report.json
docs/next_level/c74_environment_manifest.json
docs/next_level/c74_count_once_report.json
docs/next_level/c74_isolation_report.json

docs/next_level/c74_readiness_report.json
docs/next_level/c74_source_sufficiency_decision.json
docs/next_level/c74_no_go_decision_tree.json
docs/next_level/c74_missing_calculation_specification.md
docs/next_level/c74_regression_report.json
```

Add or update implementation under:

```text
src/deuteron_wigner/bridge/qgcolor6/
```

or the repository-equivalent public package.

Use the minimal interface root:

```text
data/runtime/c74_qgcolor6/
```

Do not copy the complete C72 scientific payload into C74.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 27. Acceptance criteria

C74 is complete only when:

1. Baseline `db309507b36ab2053bd7d2c85bd6b7944e68a3c1` reproduces.
2. Both required untracked paths remain untouched.
3. The C72 scientific package root and payload remain byte-identical.
4. Historical C72 records remain unchanged.
5. The C72 public-interface limitation is recorded only in a descendant record.
6. The C74 loader-source fingerprint exists and verifies.
7. The C74 public-API fingerprint exists and verifies.
8. The C74 interface root authenticates the loader and exact C72 root.
9. Exactly 24 basis rows load through public API.
10. Exactly 3 basis columns load through public API.
11. Exactly 72 pair identities load through public API.
12. Exactly 72 statuses load through public API.
13. Exactly 72 exact records load through public API.
14. Every nonzero expression loads through public API.
15. Every zero certificate loads through public API.
16. Exactly 72 per-entry bounds load through public API.
17. Every record retains its authenticated ID and ancestry.
18. Every numerical midpoint is coupled to its bound.
19. Every NumPy load explicitly passes `allow_pickle=False`.
20. Object-dtype and pickle-dependent arrays are rejected.
21. Path traversal, symlink escape, and unindexed paths are rejected.
22. Every numerical array is non-writeable.
23. Every record, collection, and mapping is immutable.
24. The top-level package verifies both roots before returning any object.
25. No public loader accesses private consumer-visible fields as authority.
26. No public loader calls a builder or repair fallback.
27. No public loader infers basis, support, exact record, or bound identity.
28. Complete public-loader equivalence with C72 passes.
29. Public-package invariants and actions pass.
30. The C75 import contract is complete.
31. The C75 public-only preflight with C64 and C74 passes.
32. The C73 blocker is explicitly superseded.
33. Repeated, clean, serial, parallel, and restart reconstruction is deterministic.
34. All count-once and isolation error counts are zero.
35. At least 320 focused live mutations are detected.
36. No physical \(qg\) embedding is constructed.
37. No descendant support promotion, endpoint/witness relation, contact value/matrix, complete instantaneous-fermion operator, Wilson/TMD, matching, inference, or production object is created.
38. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
39. `MSHT20_REP/` remains untouched.
40. The working tree is clean except for the two pre-existing untracked paths.
41. A local completion commit is created and not pushed.

Do not weaken public authority coverage, explicit `allow_pickle=False`, object-dtype rejection, immutability, midpoint/bound coupling, API authentication, or no-regeneration/no-inference semantics to open the gate.

---

# 28. Final Codex response

Report:

- starting and final commits;
- untouched untracked paths;
- descendant correction applied to the C72 readiness claim;
- unchanged C72 scientific package root;
- C74 interface-root hash;
- loader-source and public-API fingerprints;
- public counts for 24 rows, 3 columns, 72 pairs, 72 statuses, 72 exact records, expression records, zero certificates, and 72 per-entry bounds;
- safe NumPy loading results, including explicit `allow_pickle=False`;
- object-dtype/pickle/path-traversal/symlink negative-control results;
- immutable record and non-writeable array validation;
- certified companion-object loader results;
- top-level TripletAuthorityPackage validation;
- no-regeneration/no-inference guard results;
- complete C72 public-loader equivalence;
- public-package isometry/projector/intertwiner/leakage results;
- preserved C53-impact status;
- C73 blocker-supersession result;
- C75 public-only preflight result;
- deterministic reconstruction;
- count-once, isolation, and mutation results;
- exact readiness or no-go status;
- exact next branch;
- confirmation that no physical embedding, descendant support promotion, endpoint/witness relation, contact value/matrix, complete instantaneous-fermion operator, TMD/matching, inference, or production object was created;
- confirmation that nothing was pushed.

Do not describe numerical-array-only loading, private-field access, basis inference from array positions, simplified statuses without exact records, array-level hashes without per-entry bounds, implicit NumPy defaults, object-dtype acceptance, writable arrays, or a loader that calls a builder as a complete public triplet authority API.
