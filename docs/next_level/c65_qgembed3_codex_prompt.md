# C65/QGEMBED3 Codex Work Package

## Title

**Exact CM-ground and color-triplet physical \(qg\) embedding from immutable C64 artifacts: threshold-free raw/physical maps, certified sparse and matrix-free actions, historical-basis covariance, and C52/C53/C57/C58 descendant-impact closure**

## Authoritative baseline

Start from the clean local C64/QGTM2 completion commit:

```text
6f74663f3a70e853940665c30b1561766b6b75a3
```

Its scientific ancestors include:

```text
C62 exact TM algebra:
    cfe1680c381b9531a88e27571e3898a75f6ba784

C63 read-only import no-go:
    be7c1c7f085ae06829b99b31eee2ca2d39056129
```

Before changing code, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
git merge-base --is-ancestor cfe1680c381b9531a88e27571e3898a75f6ba784 HEAD
git merge-base --is-ancestor be7c1c7f085ae06829b99b31eee2ca2d39056129 HEAD
```

The baseline is authoritative only when it contains and reproduces:

```text
C62_SOURCE_DERIVED_EXACT_TM_ALGEBRA_READY

C63_QGEMBED_C62_IMPORT_INCOMPLETE

C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY
```

and the exact C64 materialization result:

```text
exact TM blocks:
    733

exact coefficient-status records:
    171,153

exact cross-m residue certificates:
    67,920

artifact properties:
    immutable;
    content addressed;
    threshold free;
    basis-order explicit;
    expression/support hashed;
    certified numerical values with bounds;
    deterministic runtime paths;
    hash-verifying read-only import API;

next contract:
    C65/QGEMBED3;

not created by C64:
    no CM-ground physical qg embedding;
    no color-triplet physical qg embedding;
    no contact support or matrix;
    no Wilson/TMD or matching object;
    no proton, ART25, fit, inference, or production object.
```

Verify every count, hash, runtime path, block identity, support status, basis order, expression record, certified bound, and API contract from the committed C64 artifacts. Do not rely on this prompt as numerical authority.

The exact C62 convention remains immutable:

\[
|n,m\rangle_{\rm polar}
=
(-1)^n
\left|
n+\max(m,0),\,
n+\max(-m,0)
\right\rangle_{\rm circ},
\qquad
L_z=N_+-N_-.
\]

The physical trajectory remains:

```text
(K,Nmax,bHO/GeV)
  = (9/2,8,0.40)
  = (11/2,10,0.45)
  = (13/2,12,0.50).
```

The fixed local-QCD ancestry remains:

```text
C43:
    source-locked G0 light-front-gauge action;

C45:
    one-particle longitudinal, HO, spinor, polarization, color,
    and zero-mode conventions;

C47:
    physical q/qg basis architecture and historical quadrature map;

C52:
    source-derived colorless canonical vertex primitives;

C53:
    source-derived physical SU(3)/triplet canonical vertex;

C57:
    source-derived conditional corresponding-propagating regulator;

C58:
    source-derived q-sector self-induced-inertia contraction;

C59/C60:
    direct-contact support blocked only by exact qg embedding semantics.
```

C40 remains:

```text
EXECUTABLE_METHOD_ORACLE_ONLY
```

The pre-existing untracked directory:

```text
MSHT20_REP/
```

must remain untouched and outside Git.

Create a local completion commit. Do not push.

---

# 1. Scientific correction and endpoint

C64 has already supplied the exact materialized TM layer that C63 lacked. C65 must therefore perform the physical embedding calculation rather than create another preparatory metadata package.

The authority chain is now:

```text
C62 exact algebra
    ->
C64 immutable block/status/expression/certification bundle
    ->
C65 physical CM-ground and color-triplet embedding.
```

C65 may fail closed only for a concrete scientific or numerical inconsistency, such as:

```text
a C64 hash or import-contract failure;

an incomplete CM-ground orientation;

a failed exact adjoint/isometry identity;

an unresolved kinematic/color basis permutation;

nonzero nontriplet leakage;

a certified numerical interval that cannot support the required
round-trip bound;

or an unresolved descendant-impact comparison.
```

C65 must not stop merely because a new summary, duplicate inventory, or differently named metadata layer could be convenient. The C64 contract is the required import layer.

Historical artifacts remain byte-identical. Any changed interpretation, support certificate, basis adapter, or numerical operator must be represented by a descendant status and supersession plan.

---

# 2. Exact purpose

C65 must construct:

```text
a complete read-only C64 import and integrity report;

the exact raw qg basis at every physical resolution;

the exact relative/CM basis and CM-ground physical kinematic basis;

the exact CM-ground physical-to-raw injection assembled only from
C64 blocks and basis crosswalks;

the exact or metric adjoint raw-to-physical projection;

the raw-space CM-ground image projector;

the complete longitudinal/helicity/Jz/OAM extension of the
kinematic embedding;

the read-only import of the C53 24 x 3 color-triplet isometry;

the exact full CM-ground color-triplet physical qg embedding and
projection;

threshold-free exact support ledgers;

certified sparse numerical arrays and independent matrix-free
embed/project actions;

an exact historical C47-basis phase/permutation adapter;

complete C47 embedding and support reconciliation;

complete C52/C53 basis-covariance and numerical-impact audits;

complete C57 support and C58 contraction-impact audits;

a C59/C60 continuation decision;

one and only one authorized next branch.
```

C65 must not construct:

```text
C60 absorption or emission endpoint relations;

the direct-contact intermediate-witness relation;

a direct-contact numerator, inverse derivative, normalization,
P-minus value, M-squared value, or matrix;

a new canonical vertex unless a later supersession package is
selected;

a new self-induced-inertia contraction;

a physical counterterm coefficient;

a complete instantaneous-fermion operator;

a Wilson/bilocal TMD or matching result.
```

The strongest no-upstream-rebuild status is:

```text
C65_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY
```

Its exact continuation is:

> **C66/IFSUPPORT2 — reconstruct source-ordered direct-contact endpoint and intermediate-witness support using the immutable C65 exact physical embedding**

---

# 3. Scientific boundary

C65 is:

```text
physical qg embedding specific;
exact CM-ground specific;
exact/certified color-triplet specific;
threshold free;
read-only C64 consumer;
basis-adapter aware;
descendant-impact aware;
deterministic;
validation only.
```

C65 is not:

```text
a recalculation of C62 exact TM algebra;

a regeneration of missing C64 artifacts;

a fit to historical C47 quadrature;

a contact-amplitude calculation;

a propagating-loop calculation;

a physical renormalization calculation;

a promotion to a physical TMD.
```

A phase or basis-permutation difference is not automatically a physics change. A numerical near-equality is not automatically a basis-covariance proof.

---

# 4. Mandatory inputs

Read completely:

```text
docs/next_level/c43_light_front_conventions.json

docs/next_level/c45_longitudinal_cell_contract.json
docs/next_level/c45_longitudinal_mode_manifest.json
docs/next_level/c45_transverse_ho_contract.json
docs/next_level/c45_transverse_mode_manifest.json
docs/next_level/c45_light_front_spinor_contract.json
docs/next_level/c45_gluon_polarization_contract.json
docs/next_level/c45_zero_mode_projection_contract.json

docs/next_level/c47_qg_longitudinal_partition_manifest.json
docs/next_level/c47_x_scaled_coordinate_contract.json
docs/next_level/c47_qg_tm_transformation.json
docs/next_level/c47_qg_tm_validation.json
docs/next_level/c47_many_body_truncation_contract.json
docs/next_level/c47_cm_plan.json
docs/next_level/c47_cm_factorization_report.json
docs/next_level/c47_physical_q_basis_manifest.json
docs/next_level/c47_physical_qg_basis_manifest.json
docs/next_level/c47_qg_triplet_basis_manifest.json
docs/next_level/c47_physical_basis_validation.json
docs/next_level/c47_physical_basis_comparison_maps.json
docs/next_level/c47_numerical_object_inventory.json

docs/next_level/c52_component_vocabulary.json
docs/next_level/c52_colorless_component_matrices.json
docs/next_level/c52_colorless_matrix_free_report.json
docs/next_level/c52_numerical_object_inventory.json
docs/next_level/c52_readiness_report.json

docs/next_level/c53_su3_convention_manifest.json
docs/next_level/c53_triplet_image_equivalence.json
docs/next_level/c53_triplet_color_intertwiner.json
docs/next_level/c53_basis_order_manifest.json
docs/next_level/c53_physical_entry_ancestry.json
docs/next_level/c53_count_once_report.json
docs/next_level/c53_physical_matrix_free_report.json
docs/next_level/c53_numerical_object_inventory.json
docs/next_level/c53_readiness_report.json

docs/next_level/c57_operation_order_contract.json
docs/next_level/c57_regulator_plan_decision.json
docs/next_level/c57_corresponding_propagating_projector.json
docs/next_level/c57_conditional_mode_support.json
docs/next_level/c57_field_to_qg_embedding.json
docs/next_level/c57_canonical_support_validation.json
docs/next_level/c57_mode_ancestry_ledger.json
docs/next_level/c57_numerical_object_inventory.json
docs/next_level/c57_readiness_report.json

docs/next_level/c58_c57_import_report.json
docs/next_level/c58_pair_support_decision.json
docs/next_level/c58_mode_contribution_ledger.json
docs/next_level/c58_q_sector_contraction.json
docs/next_level/c58_q_sector_validation.json
docs/next_level/c58_qg_sector_scope_decision.json
docs/next_level/c58_c53_support_holdout.json
docs/next_level/c58_numerical_object_inventory.json
docs/next_level/c58_readiness_report.json

docs/next_level/c59_implementation_report.md
docs/next_level/c59_direct_contact_source_ledger.json
docs/next_level/c59_missing_calculation_specification.md
docs/next_level/c59_readiness_report.json

docs/next_level/c60_implementation_report.md
docs/next_level/c60_support_layer_contract.json
docs/next_level/c60_exact_zero_semantics.json
docs/next_level/c60_missing_calculation_specification.md
docs/next_level/c60_readiness_report.json

docs/next_level/c62_implementation_report.md
docs/next_level/c62_polar_circular_phase_contract.json
docs/next_level/c62_exact_polar_tm_contract.json
docs/next_level/c62_readiness_report.json

docs/next_level/c63_implementation_report.md
docs/next_level/c63_c62_import_report.json
docs/next_level/c63_readiness_report.json

docs/next_level/c64_implementation_report.md
docs/next_level/c64_derivation_authority_manifest.json
docs/next_level/c64_input_fidelity_audit.json
docs/next_level/c64_c62_source_fingerprint.json
docs/next_level/c64_c62_api_fingerprint.json
docs/next_level/c64_exact_tm_block_census.json
docs/next_level/c64_block_identity_contract.json
docs/next_level/c64_basis_order_manifest.json
docs/next_level/c64_expression_hash_manifest.json
docs/next_level/c64_exact_support_hash_manifest.json
docs/next_level/c64_exact_expression_table_manifest.json
docs/next_level/c64_certified_array_hash_manifest.json
docs/next_level/c64_precision_and_rounding_contract.json
docs/next_level/c64_error_bound_validation.json
docs/next_level/c64_runtime_path_manifest.json
docs/next_level/c64_block_metadata_manifest.json
docs/next_level/c64_numerical_object_inventory.json
docs/next_level/c64_residue_certificate_manifest.json
docs/next_level/c64_c65_basis_crosswalk.json
docs/next_level/c64_api_contract.json
docs/next_level/c64_c65_qgembed3_import_contract.json
docs/next_level/c64_deterministic_reconstruction_report.json
docs/next_level/c64_environment_manifest.json
docs/next_level/c64_readiness_report.json
```

Use actual repository filenames when they differ. Do not invent an absent artifact.

Read the exact import implementation under the repository-equivalent of:

```text
src/deuteron_wigner/bridge/qgtm2/
```

Create:

```text
docs/next_level/c65_derivation_authority_manifest.json
docs/next_level/c65_input_fidelity_audit.json
```

---

# 5. Immutable C64 import gate

Before constructing one embedding column, verify through the C64 read-only API:

```text
C64 status and ancestry;

733 exact block records;

171,153 coefficient-status records;

67,920 exact cross-m residue certificates;

all package aggregate hashes;

every per-block basis-order hash;

every per-block expression hash;

every per-block status/support hash;

every certified sparse-array hash;

every error-bound artifact;

every runtime path;

the global C65 basis crosswalk;

the declared serializer and certification versions.
```

Verify that:

```text
the loader returns immutable arrays;

the loader calls no C62 generator;

a missing or changed artifact fails closed;

support is exposed independently of numerical magnitude;

the basis crosswalk determines every global offset.
```

C65 may not:

```text
reconstruct a missing C64 block;

change one C64 basis order;

change one exact status;

introduce a support threshold;

replace a C64 value with C47 quadrature;

or bypass the hash-verifying loader.
```

Create:

```text
docs/next_level/c65_c64_import_report.json
docs/next_level/c65_c64_import_integrity_validation.json
```

Any mismatch blocks all physical embedding work.

---

# 6. Freeze construction and holdouts

Freeze before assembly:

```text
the C64 package aggregate hashes;

every C64 block orientation;

the global row/column crosswalk;

the exact CM-ground label;

the raw qg basis order;

the relative/CM basis order;

the physical kinematic qg basis order;

quark/gluon longitudinal and helicity order;

Jz and OAM conventions;

the C47 many-body Nmax rule;

the C53 product-color order;

the C53 triplet phase and isometry;

the historical C47 basis and recorded quadrature/phase identities.
```

Freeze holdouts:

```text
one lowest-shell CM-ground state;

one highest-shell CM-ground state per resolution;

one CM-excited state;

one positive-m and one negative-m intrinsic state;

one physical state with one exact raw component;

one physical state with multiple exact raw components;

one exact-zero raw component certified by C64;

one exact nonzero with the smallest certified magnitude per resolution;

one physical-to-raw-to-physical round trip;

one raw image-projector vector;

one color-triplet vector;

one basis-rotated triplet vector;

one C47 historical-adapter state per shell;

one C52 colorless vertex element;

one C53 physical vertex element;

one C57 canonical-support edge;

one C58 admitted mode;

one adjacent-resolution comparison vector.
```

No failed holdout may be moved into construction.

Create:

```text
docs/next_level/c65_calculation_plan.json
docs/next_level/c65_holdout_plan.json
```

---

# 7. Exact raw and physical basis manifests

Construct exact basis manifests for:

```text
raw qg kinematic space;

raw qg product-color space;

relative-plus-CM kinematic space;

CM-ground physical kinematic qg space;

CM-ground total-color-triplet physical qg space.
```

Every raw basis record must retain:

```text
resolution;

K;

k_q and k_g;

q and g transverse polar-HO labels;

q and g helicities;

fundamental and adjoint colors where applicable;

total Jz;

total transverse shell;

zero-mode status;

global basis index;

C64 block/crosswalk ancestry.
```

Every physical kinematic basis record must retain:

```text
longitudinal partition;

relative HO labels;

CM label fixed to the exact ground state;

q and g helicities;

Jz;

many-body Nmax ancestry;

global basis index.
```

Verify expected dimensions from committed manifests rather than hard-coding:

```text
colorless physical qg:
    448 / 900 / 1,584

triplet physical qg:
    1,344 / 2,700 / 4,752.
```

If repository manifests specify different values, report and use the authoritative values.

Create:

```text
docs/next_level/c65_raw_qg_basis_manifest.json
docs/next_level/c65_physical_kinematic_qg_basis_manifest.json
docs/next_level/c65_physical_triplet_qg_basis_manifest.json
docs/next_level/c65_basis_order_manifest.json
```

---

# 8. CM-ground column selection

Use the C64 basis crosswalk to identify every exact column or row corresponding to:

```text
n_CM = 0;
m_CM = 0;
```

in the committed C62/C64 orientation.

Do not infer CM-ground columns from:

```text
array sparsity;

small numerical values;

historical C47 row labels alone;

or an assumed block concatenation order.
```

Create a complete CM-ground selection record containing:

```text
C64 block ID;

block-local row/column index;

global relative/CM basis ID;

raw basis partner IDs;

exact status ancestry;

basis-order hashes.
```

Verify:

```text
all required CM-ground states are present;

no CM-excited state is selected;

no CM-ground state is duplicated;

the total selected dimension matches the physical kinematic basis.
```

Create:

```text
docs/next_level/c65_cm_ground_column_manifest.json
docs/next_level/c65_cm_ground_column_validation.json
```

---

# 9. Physical-to-raw TM orientation

C64 blocks preserve the exact C62 matrix orientation. Derive the physical-to-raw injection explicitly.

If the C64 block stores:

\[
T_{\mathrm{relCM}\leftarrow\mathrm{raw}}
=
\langle \mathrm{rel},\mathrm{CM}|\mathrm{raw}\rangle,
\]

then the physical-to-raw injection is:

\[
J_{\mathrm{raw}\leftarrow\mathrm{phys}}
=
\left(
T_{\mathrm{relCM}\leftarrow\mathrm{raw}}
\right)^\dagger
\Big|_{\mathrm{CM}=0},
\]

subject to the exact basis and metric conventions.

Do not silently transpose or conjugate.

Prove the orientation through:

```text
the C64 orientation metadata;

one exact low-shell analytic holdout;

the inverse/adjoint identity;

basis-vector round trips.
```

Create:

```text
docs/next_level/c65_tm_orientation_contract.json
docs/next_level/c65_tm_orientation_validation.json
```

---

# 10. Exact/certified CM-ground kinematic injection

Construct:

\[
J_{qg,R}^{\rm kin}:
\mathcal H_{qg,R}^{\rm CM=0,kin}
\longrightarrow
\mathcal H_{qg,R}^{\rm raw,kin}.
\]

The exact support of every entry comes from C64 status artifacts.

The numerical value and bound come from C64 certified arrays.

Each nonzero component retains:

```text
physical basis ID;

raw basis ID;

C64 block ID;

C64 exact-expression hash;

C64 exact-support status;

certified midpoint;

certified error bound;

longitudinal/helicity/Jz/OAM ancestry;

CM-ground certificate.
```

Construct a direct matrix-free injection that iterates C64 blocks and the CM-ground crosswalk without multiplying by the stored C65 sparse matrix.

Create:

```text
docs/next_level/c65_exact_kinematic_injection.json
docs/next_level/c65_kinematic_injection_validation.json
docs/next_level/c65_kinematic_matrix_free_report.json
```

---

# 11. Exact or metric-adjoint projection

Construct:

\[
P_{qg,R}^{\rm kin}:
\mathcal H_{qg,R}^{\rm raw,kin}
\longrightarrow
\mathcal H_{qg,R}^{\rm CM=0,kin}.
\]

Use:

```text
ordinary Hermitian adjoint
```

only if both basis metrics are identity in the committed normalization.

Otherwise derive and use the exact Gram-metric adjoint.

Require:

\[
P_{qg,R}^{\rm kin}J_{qg,R}^{\rm kin}=I_{\rm phys,kin}
\]

exactly at the expression/support level where possible and within propagated C64 certification bounds numerically.

Construct:

\[
\Pi_{\rm CM,R}^{\rm raw}
=
J_{qg,R}^{\rm kin}P_{qg,R}^{\rm kin}.
\]

Verify:

```text
Hermiticity;

idempotence;

rank;

trace;

orthogonality to every retained CM-excited state;

matrix-free action agreement.
```

Create:

```text
docs/next_level/c65_exact_kinematic_projection.json
docs/next_level/c65_cm_image_projector.json
docs/next_level/c65_cm_projector_validation.json
```

Do not post-process the projector by eigenvalue clipping.

---

# 12. Longitudinal, helicity, Jz, and OAM closure

Verify that the complete kinematic map is block diagonal or block compatible under the exact conserved labels.

Report:

```text
longitudinal-partition conservation;

total-K conservation;

quark and gluon helicity identity;

Jz conservation;

intrinsic/CM OAM relation;

total-shell support;

zero-mode policy.
```

Every exact zero must retain a source or C64 certificate.

Do not introduce a tolerance-based selection rule.

Create:

```text
docs/next_level/c65_kinematic_quantum_number_report.json
```

---

# 13. Read-only C53 color import

Import read-only:

```text
SU(3) generator convention;

fundamental and adjoint basis orders;

24-dimensional product-color basis;

3-dimensional retained triplet basis;

24 x 3 triplet isometry U3;

triplet projector;

triplet image-equivalence certificate;

anti-sextet and 15 leakage certificates;

triplet basis-rotation contract;

all hashes.
```

The C53 numerical vertex matrices are not required for physical embedding construction.

Create:

```text
docs/next_level/c65_c53_triplet_import_report.json
```

Any color-contract mismatch blocks full physical assembly.

---

# 14. Kinematic/color basis permutation

Derive the exact permutations between:

```text
C65 raw kinematic-major ordering;

C53 raw product-color ordering;

C65 physical kinematic-major ordering;

C53 triplet ordering.
```

Do not assume that the full map is a raw `kron(J_kin,U3)`.

Construct explicit permutation/isometry records:

\[
\mathcal P_{\rm raw},
\qquad
\mathcal P_{\rm phys}.
\]

Verify:

```text
bijection;

basis-ID preservation;

shape;

inverse;

hash;

parallel/restart determinism.
```

Create:

```text
docs/next_level/c65_kinematic_color_permutation.json
docs/next_level/c65_kinematic_color_permutation_validation.json
```

---

# 15. Full physical color-triplet embedding

Construct:

\[
J_{qg,R}^{\rm phys}:
\mathcal H_{qg,R}^{\rm CM=0,triplet}
\longrightarrow
\mathcal H_{qg,R}^{\rm raw,kin\otimes(3\otimes8)}.
\]

When factorization is proved:

\[
J_{qg,R}^{\rm phys}
=
\mathcal P_{\rm raw}
\left(
J_{qg,R}^{\rm kin}\otimes U_3
\right)
\mathcal P_{\rm phys}^{-1}.
\]

Construct the exact or metric adjoint:

\[
P_{qg,R}^{\rm phys}.
\]

Construct the raw image projector:

\[
\Pi_{qg,R}^{\rm CM=0,triplet}
=
J_{qg,R}^{\rm phys}
P_{qg,R}^{\rm phys}.
\]

Required checks:

```text
P_phys J_phys = I;

image-projector Hermiticity;

image-projector idempotence;

rank equals the physical triplet dimension;

zero anti-sextet leakage;

zero 15 leakage;

exact CM-ground identity;

CM/color projector commutation where claimed;

triplet-basis rotation covariance;

sparse/matrix-free agreement.
```

Create:

```text
docs/next_level/c65_exact_physical_qg_embedding.json
docs/next_level/c65_exact_physical_qg_projection.json
docs/next_level/c65_physical_image_projector.json
docs/next_level/c65_physical_embedding_validation.json
docs/next_level/c65_color_cm_factorization_report.json
docs/next_level/c65_physical_matrix_free_report.json
```

---

# 16. Threshold-free physical support ledger

For every physical/raw basis pair assign one terminal status:

```text
ZERO_BY_C64_TM_SHELL_RULE;

ZERO_BY_C64_TM_M_RULE;

ZERO_BY_C64_TM_ALGEBRAIC_CANCELLATION;

ZERO_BY_EXACT_COLOR_RULE;

ZERO_BY_CM_OR_BASIS_SELECTION;

NONZERO_EXACT_TM_AND_EXACT_COLOR;

NONZERO_EXACT_TM_CERTIFIED_COLOR;

UNDECIDABLE_BLOCKING.
```

A positive gate requires:

```text
UNDECIDABLE_BLOCKING = 0.
```

The support record must retain:

```text
C64 block/expression/status hashes;

C53 color certificate;

basis permutation ancestry;

CM-ground ancestry;

certified numerical value/bound where applicable.
```

Never derive support from:

```text
abs(value) > tolerance;

historical sparse storage;

the C57 1e-12 support mask;

C53 evaluated vertex values;

or post-hoc matrix pruning.
```

Create:

```text
docs/next_level/c65_exact_physical_support.json
docs/next_level/c65_exact_physical_support_validation.json
```

---

# 17. Certified numerical export

Export deterministic sparse numerical representations for:

```text
J_kin;
P_kin;
Pi_CM;
J_phys;
P_phys;
Pi_CM_triplet.
```

Every nonzero must descend from:

```text
a C64 exact expression/status certificate;

a C53 exact/certified color certificate;

a deterministic basis permutation.
```

Propagate C64 numerical bounds through:

```text
conjugation;

Kronecker/color composition;

basis permutation;

sparse accumulation;

projector multiplication.
```

State the error model for complex entries and matrix actions.

Required checks:

```text
all exact values lie inside exported bounds;

precision doubling preserves support;

round trips close within propagated bounds;

projector residuals close within propagated bounds;

no exact zero appears as a tiny stored entry;

no nonzero is pruned;

serial/restart/parallel arrays are byte-identical.
```

Create:

```text
docs/next_level/c65_certified_numerical_embedding_export.json
docs/next_level/c65_error_propagation_contract.json
docs/next_level/c65_precision_stability_report.json
docs/next_level/c65_certified_invariant_report.json
```

---

# 18. Historical C47 basis adapter

Construct an explicit adapter between:

```text
the exact C62/C64/C65 physical kinematic basis;

the historical C47 quadrature/argmax basis.
```

Separate:

```text
basis permutation;

global analytic polar phase;

recorded historical shell/row phase;

quadrature residual;

possible subspace mismatch.
```

The adapter must descend from exact conventions and recorded basis IDs. It must not be fitted to minimize the full embedding residual.

One valid relation may be:

\[
J_{\rm hist}
=
J_{\rm exact}A_{\rm exact\leftarrow hist}
+
\Delta_{\rm quad},
\]

with orientation proved.

Required checks:

```text
adapter unitary/isometric status;

shell and m block structure;

longitudinal-partition consistency;

exact support invariance;

historical/exact round trips;

quadrature residual bounded by the historical numerical method.
```

Create:

```text
docs/next_level/c65_historical_basis_adapter.json
docs/next_level/c65_historical_basis_adapter_validation.json
```

---

# 19. C47 embedding reconciliation

Compare the exact C65 embedding with the immutable C47 quadrature embedding after applying only the proved adapter.

Reconfirm through the C64 residue certificates that all:

```text
4,032 / 15,840 / 48,048
```

historical subthreshold residues are exact cross-\(m\) zeros.

Also audit all historical above-threshold support entries.

Report:

```text
historical support count;

exact support count;

symmetric difference after adapter;

exact-zero quadrature-noise count;

ordinary nonzero quadrature-error distribution;

maximum certified discrepancy;

row/column Gram residuals;

unexplained discrepancy count.
```

Classify C47 as exactly one of:

```text
C47_NUMERICAL_EMBEDDING_VALID_IN_HISTORICAL_BASIS;

C47_VALUES_VALID_EXACT_SUPPORT_CERTIFICATE_SUPERSEDED;

C47_BASIS_ADAPTER_REQUIRED_NO_PHYSICS_CHANGE;

C47_NUMERICAL_EMBEDDING_SUPERSESSION_REQUIRED;

C47_IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c65_c47_embedding_reconciliation.json
docs/next_level/c65_c47_support_reconciliation.json
docs/next_level/c65_c47_basis_status_decision.json
```

A positive no-upstream-rebuild branch requires no unexplained support discrepancy.

---

# 20. C52 colorless-vertex impact audit

Do not overwrite C52.

Audit:

```text
basis covariance under the C65 historical adapter;

support ancestry;

frozen source-derived matrix elements;

sparse and matrix-free actions;

adjoint and dimensional contracts.
```

Reconstruct a frozen but representative set of C52 elements from source primitives plus the exact C65 kinematic embedding while poisoning stored C52 values during construction.

Where tractable, compare the complete historical C52 matrix transformed into the exact basis.

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ADAPTER_ONLY_NO_OPERATOR_REBUILD;

SUPPORT_CERTIFICATE_SUPERSEDED_VALUES_VALID;

NUMERICAL_OPERATOR_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c65_c52_impact_audit.json
docs/next_level/c65_c52_basis_covariance_report.json
```

---

# 21. C53 physical-vertex impact audit

Do not overwrite C53.

Audit independently:

```text
kinematic basis covariance;

triplet basis covariance;

physical vertex support ancestry;

generated adjoint;

matrix-free action;

full-product versus reduced-triplet equality.
```

Reconstruct frozen C53 holdouts from:

```text
C52 source primitives;

C65 exact kinematic embedding;

read-only C53 color algebra.
```

Do not use stored C53 values as construction authority.

Classify:

```text
UNCHANGED_EXACTLY;

BASIS_ADAPTER_ONLY_NO_OPERATOR_REBUILD;

SUPPORT_ANCESTRY_CERTIFICATE_SUPERSEDED_VALUES_VALID;

NUMERICAL_OPERATOR_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c65_c53_impact_audit.json
docs/next_level/c65_vertex_basis_covariance_report.json
```

A C53 numerical rebuild takes precedence over every downstream branch.

---

# 22. C57 corresponding-propagating-support impact audit

Reconstruct canonical reachability from:

```text
the source canonical endpoint rules;

the exact C65 embedding;

the fixed C57 operation order;

the fixed C57 IFREG-CORRESPONDING-PROPAGATING-SUPPORT plan.
```

Do not consume C53 numerical values.

Compare exact identities and counts with:

```text
canonical support positions:
    312 / 510 / 756;

conditional field-mode unions:
    1,216 / 2,320 / 3,936;

candidate envelopes:
    2,304 / 4,400 / 7,488.
```

Report:

```text
exact counts;

symmetric differences;

basis-ID adapter differences;

new exact support edges;

removed historical edges;

mode-union changes;

envelope changes;

CM/color/zero-mode causes.
```

Classify C57:

```text
UNCHANGED_EXACTLY;

BASIS_ID_ADAPTER_ONLY;

SUPPORT_CERTIFICATE_SUPERSEDED_NO_SUPPORT_CHANGE;

SUPPORT_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c65_c57_support_impact_audit.json
```

---

# 23. C58 self-induced-inertia impact audit

Audit read-only:

```text
IFNORM2-ORDERED-JOINT-SUPPORT;

admitted modes:
    4,216 / 8,330 / 14,484;

q-sector primitive:
    6 x 6 with six nonzero entries;

qg status:
    IFNORM2-SECTOR-SPECIFIC-COUNTERTERM-ONLY.
```

Determine whether the exact C65 embedding changes:

```text
pair-support identities;

mode ledger identities;

mode counts;

q-sector numerical primitive;

qg sector status.
```

Classify C58:

```text
UNCHANGED_EXACTLY;

BASIS_ID_ADAPTER_ONLY;

SUPPORT_LEDGER_REBUILD_REQUIRED_VALUES_UNCHANGED;

NUMERICAL_CONTRACTION_REBUILD_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c65_c58_impact_audit.json
```

---

# 24. C59/C60 continuation audit

C59 and C60 created no direct-contact value or matrix.

Determine whether C65 now supplies the exact inputs C60 lacked:

```text
raw/physical qg component map;

exact projected-cancellation semantics;

threshold-free support certificate;

CM-ground and triplet ancestry;

read-only embed/project API.
```

Classify:

```text
C60_BLOCKER_RESOLVED_READY_FOR_IFSUPPORT2;

ADDITIONAL_ENDPOINT_INPUT_BLOCKING;

UPSTREAM_IFREG_SUPERSESSION_REQUIRED;

UPSTREAM_VERTEX_SUPERSESSION_REQUIRED;

IMPACT_UNRESOLVED_BLOCKING.
```

Create:

```text
docs/next_level/c65_c59_c60_continuation_audit.json
```

---

# 25. Complete descendant dependency graph

Trace:

```text
C47 quadrature embedding;

C52 colorless canonical vertex;

C53 physical canonical vertex;

C57 conditional regulator;

C58 self-induced inertia;

C59 contact preflight;

C60 contact-support no-go.
```

For every descendant record:

```text
historical hash;

exact C65 dependency;

basis adapter;

support impact;

numerical impact;

supersession requirement;

authorized next consumer.
```

Historical artifacts remain immutable.

Create:

```text
docs/next_level/c65_descendant_dependency_graph.json
docs/next_level/c65_inherited_impact_summary.json
docs/next_level/c65_supersession_plan.json
```

---

# 26. Exact embedding API

Create APIs equivalent to:

```python
physical_qg_raw_components(
    physical_qg_basis_id: str,
    resolution: str,
) -> tuple[ExactPhysicalEmbeddingComponent, ...]

embed_physical_qg_to_raw(
    physical_vector,
    resolution: str,
    precision: int | None = None,
) -> CertifiedVectorResult

project_raw_qg_to_physical(
    raw_vector,
    resolution: str,
    precision: int | None = None,
) -> CertifiedVectorResult

cm_ground_image_projector(
    resolution: str,
    precision: int | None = None,
)

physical_triplet_image_projector(
    resolution: str,
    precision: int | None = None,
)

historical_exact_basis_adapter(
    resolution: str,
    precision: int | None = None,
)
```

Return records must expose:

```text
exact support status;

C64 TM expression/status hashes;

C53 color certificate;

certified midpoint and error bound;

basis ancestry;

CM ancestry;

triplet ancestry;

historical-adapter identity.
```

Do not expose:

```text
a support threshold;

a prune-small option;

an argmax phase option;

a fit-to-C47 option;

an option to regenerate C64.
```

Create:

```text
docs/next_level/c65_api_contract.json
docs/next_level/c65_api_validation.json
```

---

# 27. Physical-resolution comparison

Construct typed comparison maps between adjacent physical resolutions.

Because \(K\), \(N_{\max}\), and \(b_{\rm HO}\) all change, do not claim literal inclusion.

Compare:

\[
R_{\rm raw}
J^{\rm phys}_{R'}
P_{\rm phys}
\quad\text{against}\quad
J^{\rm phys}_{R}.
\]

Separate:

```text
longitudinal nonnesting;

finite-shell change;

bHO scale change;

exact TM-block change;

CM-ground change;

triplet-basis adapter;

historical-basis adapter;

certified numerical rounding;

exact support change.
```

Do not tune the embedding or adapter to reduce the comparison residual.

Create:

```text
docs/next_level/c65_embedding_comparison_maps.json
docs/next_level/c65_embedding_comparison_report.json
docs/next_level/c65_comparison_remainder_ledger.json
```

---

# 28. Count-once and provenance

Report:

```text
candidate physical/raw pair count;

exact TM-zero count by C64 class;

exact color-zero count;

CM/basis selection-zero count;

exact nonzero count;

certified color-nonzero count;

undecidable count;

duplicate component count;

missing component count;

CM-ground component count;

triplet-combined component count;

basis-adapter record count;

support-changed descendant count;

operator-rebuild descendant count;

unresolved-impact count.
```

Every nonzero component must have exactly one ancestry path:

```text
C64 block/status/expression
    ->
CM-ground selection
    ->
longitudinal/helicity extension
    ->
C53 triplet certificate
    ->
basis permutation
    ->
C65 support/value/bound.
```

A positive gate requires:

```text
undecidable = 0;

duplicate = 0;

missing = 0;

unresolved impact = 0.
```

Create:

```text
docs/next_level/c65_component_ancestry_ledger.json
docs/next_level/c65_count_once_report.json
```

---

# 29. Isolation and poisoning controls

Prove C65 embedding construction is unchanged when:

```text
all historical C47 quadrature values are poisoned after IDs and
holdout roles are loaded;

the historical 1e-12 threshold changes;

all historical C47 argmax phases are poisoned after exact adapter
inputs are frozen;

all C47 canonical tuple values are poisoned;

all C50 combined values are poisoned;

all C52/C53 numerical matrices are poisoned during embedding
construction;

all C57/C58 numerical operator values are poisoned during embedding
construction;

all ART25 files are inaccessible.
```

Impact-comparison stages may load immutable descendant outputs only after C65 construction objects and hashes are complete.

The build must fail when:

```text
a C64 import hash changes;

a C64 status changes;

a C64 runtime path is absent;

a CM-ground column changes;

the TM orientation changes;

the physical basis order changes;

the C53 triplet isometry changes;

a support threshold enters;

a genuine exact nonzero is pruned;

an exact zero is inferred from magnitude;

the historical adapter is fitted;

an impact status is promoted without its required comparison.
```

Create:

```text
docs/next_level/c65_isolation_report.json
```

---

# 30. Deterministic runtime bundles

For every resolution produce content-addressed bundles containing:

```text
raw and physical basis manifests;

CM-ground column selection;

kinematic injection and projection;

CM image projector;

kinematic/color permutations;

full triplet injection and projection;

full image projector;

exact support ledger;

certified sparse arrays and error bounds;

matrix-free reconstruction metadata;

historical basis adapter;

C47 reconciliation;

C52/C53/C57/C58 impact records;

comparison-map blocks.
```

Use a deterministic runtime root:

```text
data/runtime/c65_qgembed3/
```

Commit an inventory containing:

```text
relative runtime path;

object type;

shape or record count;

dtype;

working precision;

error-bound convention;

basis-order hash;

C64 package hash;

CM-selection hash;

triplet hash;

support hash;

adapter hash;

array hash;

generator command.
```

Create:

```text
docs/next_level/c65_numerical_object_inventory.json
docs/next_level/c65_runtime_completeness_report.json
```

All committed JSON and every runtime artifact must reconstruct byte-for-byte.

---

# 31. End-to-end C64-artifact-to-physical-embedding test

Implement an end-to-end test that starts from the C64 read-only API, C47 basis definitions, and C53 color certificates—not from prebuilt C65 arrays.

It must:

```text
verify all C64 imports;

construct raw and physical basis identities;

select exact CM-ground columns;

prove TM orientation;

construct J_kin and P_kin;

construct the CM image projector;

extend all longitudinal/helicity/Jz/OAM identities;

verify C53 color imports;

derive kinematic/color permutations;

construct J_phys and P_phys;

construct the full image projector;

derive threshold-free support;

export certified numerical arrays;

construct the historical basis adapter;

reconcile C47;

audit C52, C53, C57, and C58;

select exactly one continuation branch;

run comparison, count-once, poisoning, precision, serial/restart,
and deterministic-reconstruction tests;

reproduce every hash.
```

It must fail when:

```text
C65 regenerates C62/C64 coefficients;

a C64 block is skipped;

the TM orientation is guessed;

a CM-excited state enters;

a nontriplet color path enters;

raw Kronecker ordering is assumed without the permutation proof;

a support threshold is used;

an exact nonzero is pruned;

the historical adapter is fitted;

C52/C53 values enter construction;

C57/C58 counts are preserved by tuning;

an upstream supersession branch is skipped;

a runtime hash changes.
```

---

# 32. Focused mutation tests

Create at least **320 focused live mutations** of actual import, basis, embedding, support, certification, adapter, or impact objects.

Include mutations of:

```text
C64 package hash;

C64 block hash;

C64 basis-order hash;

C64 exact status;

C64 numerical error bound;

CM-ground column;

TM orientation;

longitudinal partition;

raw q mode;

raw g mode;

relative mode;

CM label;

metric adjoint;

CM image projector;

quark helicity;

gluon helicity;

Jz;

OAM;

raw basis order;

physical basis order;

triplet isometry;

triplet phase;

kinematic/color permutation;

physical support status;

certified numerical value;

propagated bound;

historical basis adapter;

C47 reconciliation status;

C52 impact status;

C53 impact status;

C57 support edge;

C58 mode-ledger status;

continuation branch;

comparison map;

runtime path;

runtime array hash.
```

Every mutation must fail a concrete import, source, exact-zero, basis, isometry, CM, color, support, certification, adapter, impact, count-once, comparison, or deterministic-reconstruction test.

Do not inflate the count with identifier-only dispatch.

---

# 33. Readiness gate

The exact physical-embedding gate requires:

```text
the full C64 baseline reproduces;

C64 positive status remains explicit;

all 733 blocks import read-only;

all 171,153 statuses import read-only;

all 67,920 residue certificates import read-only;

all C64 hashes, paths, bounds, and basis orders verify;

the raw and physical basis manifests are complete;

the CM-ground selection is complete;

the TM orientation is proved;

J_kin and P_kin exist;

P_kin J_kin = I closes;

the CM image projector is Hermitian and idempotent;

all retained CM-excited states are orthogonal to the image;

longitudinal/helicity/Jz/OAM closure passes;

C53 triplet import passes;

kinematic/color permutations close;

J_phys and P_phys exist;

P_phys J_phys = I closes;

the full image projector is Hermitian and idempotent;

anti-sextet and 15 leakage are zero;

threshold-free support is complete;

no component remains undecidable;

certified arrays and matrix-free actions agree;

the historical adapter closes without fitting;

C47 reconciliation has no unexplained discrepancy;

C52 impact is fully typed;

C53 impact is fully typed;

C57 impact is fully typed;

C58 impact is fully typed;

C59/C60 continuation is fully typed;

count-once and provenance close;

comparison maps execute;

poisoning controls pass;

runtime bundles reproduce byte-for-byte;

the end-to-end test passes.
```

After those conditions pass, issue exactly one status from Section 34.

Do not issue:

```text
C65_IFERM_CONTACT_SUPPORT_READY;

C65_DIRECT_IFERM_CONTACT_READY;

C65_SOURCE_DERIVED_INSTANTANEOUS_FERMION_READY;

C65_COMPLETE_LOCAL_HQCD_SUBSTRATE_READY;

C65_ONE_LOOP_MATCHING_VALIDATED.
```

---

# 34. Exact continuation decision

Select exactly one branch.

## 34.1 Exact embedding ready; no upstream numerical rebuild

Issue:

```text
C65_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY
```

Required impact decisions:

```text
C52/C53:
    unchanged exactly,
    basis-adapter only,
    or support-certificate supersession with valid numerical values;

C57:
    unchanged exactly,
    basis-ID adapter only,
    or exact support-certificate supersession with no support change;

C58:
    unchanged exactly or basis-ID adapter only;

C60 blocker:
    resolved.
```

Next:

> **C66/IFSUPPORT2 — source-ordered direct-contact endpoint and intermediate-witness support using C65**

## 34.2 C57/C58 support supersession required

Issue:

```text
C65_EXACT_QG_EMBEDDING_READY_IFREG_SUPERSESSION_REQUIRED
```

Conditions:

```text
the exact physical embedding is complete;

C52/C53 do not require a numerical operator rebuild;

C57 support identities or C58 mode ledgers change.
```

Next:

> **C66/IFREG3 — rebuild the corresponding-propagating regulator and self-induced-inertia support from C65**

## 34.3 C52/C53 vertex supersession required

Issue:

```text
C65_EXACT_QG_EMBEDDING_READY_VERTEX_SUPERSESSION_REQUIRED
```

Conditions:

```text
the exact physical embedding is complete;

a C52/C53 numerical operator or indispensable support ancestry
requires rebuilding.
```

This branch takes precedence over IFREG supersession.

Next:

> **C66/VERTEX4 — rebuild the colorless and physical canonical vertex in the exact C65 basis, then revalidate downstream support**

## 34.4 Descendant impact unresolved

Issue:

```text
C65_QG_EMBEDDING_DESCENDANT_IMPACT_INCOMPLETE
```

Next:

> **C66/QGIMPACT — complete basis-adapter, operator-covariance, and support/mode-ledger impact closure**

Do not proceed to IFSUPPORT2 when an upstream positive package requires supersession.

---

# 35. Exact no-go branches

## A. C64 import fails

```text
C65_QGEMBED_C64_IMPORT_INCOMPLETE
```

Next:

> **C66/QGTM3 — repair concrete C64 artifact, hash, basis-order, or certification failures only**

## B. CM-ground selection or orientation remains incomplete

```text
C65_QG_CM_GROUND_EMBEDDING_INCOMPLETE
```

Next:

> **C66/QGCM3 — exact CM-ground column selection, orientation, adjoint, and projector closure**

## C. Triplet embedding remains incomplete

```text
C65_QG_TRIPLET_EMBEDDING_INCOMPLETE
```

Next:

> **C66/QGCOLOR2 — kinematic/color permutation, triplet isometry, and leakage closure**

## D. Certified numerical export fails

```text
C65_QG_EMBEDDING_NUMERICAL_CERTIFICATION_FAILED
```

Next:

> **C66/QGNUM3 — propagated bounds, sparse/matrix-free action, and projector certification**

## E. Historical basis adapter remains incomplete

```text
C65_QG_HISTORICAL_BASIS_ADAPTER_INCOMPLETE
```

Next:

> **C66/QGADAPT2 — exact phase/permutation adapter and C47 reconciliation**

## F. Exact embedding closes

Use one of the continuation statuses in Section 34.

---

# 36. Required deliverables

Create at least:

```text
docs/next_level/c65_implementation_report.md
docs/next_level/c65_api.md
docs/next_level/c65_derivation_authority_manifest.json
docs/next_level/c65_input_fidelity_audit.json

docs/next_level/c65_c64_import_report.json
docs/next_level/c65_c64_import_integrity_validation.json
docs/next_level/c65_calculation_plan.json
docs/next_level/c65_holdout_plan.json

docs/next_level/c65_raw_qg_basis_manifest.json
docs/next_level/c65_physical_kinematic_qg_basis_manifest.json
docs/next_level/c65_physical_triplet_qg_basis_manifest.json
docs/next_level/c65_basis_order_manifest.json

docs/next_level/c65_cm_ground_column_manifest.json
docs/next_level/c65_cm_ground_column_validation.json
docs/next_level/c65_tm_orientation_contract.json
docs/next_level/c65_tm_orientation_validation.json

docs/next_level/c65_exact_kinematic_injection.json
docs/next_level/c65_kinematic_injection_validation.json
docs/next_level/c65_kinematic_matrix_free_report.json
docs/next_level/c65_exact_kinematic_projection.json
docs/next_level/c65_cm_image_projector.json
docs/next_level/c65_cm_projector_validation.json
docs/next_level/c65_kinematic_quantum_number_report.json

docs/next_level/c65_c53_triplet_import_report.json
docs/next_level/c65_kinematic_color_permutation.json
docs/next_level/c65_kinematic_color_permutation_validation.json

docs/next_level/c65_exact_physical_qg_embedding.json
docs/next_level/c65_exact_physical_qg_projection.json
docs/next_level/c65_physical_image_projector.json
docs/next_level/c65_physical_embedding_validation.json
docs/next_level/c65_color_cm_factorization_report.json
docs/next_level/c65_physical_matrix_free_report.json

docs/next_level/c65_exact_physical_support.json
docs/next_level/c65_exact_physical_support_validation.json
docs/next_level/c65_certified_numerical_embedding_export.json
docs/next_level/c65_error_propagation_contract.json
docs/next_level/c65_precision_stability_report.json
docs/next_level/c65_certified_invariant_report.json

docs/next_level/c65_historical_basis_adapter.json
docs/next_level/c65_historical_basis_adapter_validation.json
docs/next_level/c65_c47_embedding_reconciliation.json
docs/next_level/c65_c47_support_reconciliation.json
docs/next_level/c65_c47_basis_status_decision.json

docs/next_level/c65_c52_impact_audit.json
docs/next_level/c65_c52_basis_covariance_report.json
docs/next_level/c65_c53_impact_audit.json
docs/next_level/c65_vertex_basis_covariance_report.json
docs/next_level/c65_c57_support_impact_audit.json
docs/next_level/c65_c58_impact_audit.json
docs/next_level/c65_c59_c60_continuation_audit.json

docs/next_level/c65_descendant_dependency_graph.json
docs/next_level/c65_inherited_impact_summary.json
docs/next_level/c65_supersession_plan.json

docs/next_level/c65_api_contract.json
docs/next_level/c65_api_validation.json
docs/next_level/c65_embedding_comparison_maps.json
docs/next_level/c65_embedding_comparison_report.json
docs/next_level/c65_comparison_remainder_ledger.json
docs/next_level/c65_component_ancestry_ledger.json
docs/next_level/c65_count_once_report.json
docs/next_level/c65_isolation_report.json

docs/next_level/c65_numerical_object_inventory.json
docs/next_level/c65_runtime_completeness_report.json
docs/next_level/c65_readiness_report.json
docs/next_level/c65_source_sufficiency_decision.json
docs/next_level/c65_no_go_decision_tree.json
docs/next_level/c65_missing_calculation_specification.md
docs/next_level/c65_regression_report.json
```

Create exactly one next-package import contract:

```text
docs/next_level/c65_c66_ifsupport2_import_contract.json

or

docs/next_level/c65_c66_ifreg3_import_contract.json

or

docs/next_level/c65_c66_vertex4_import_contract.json

or

docs/next_level/c65_c66_qgimpact_import_contract.json.
```

Add source code under:

```text
src/deuteron_wigner/bridge/qgembed3/
```

or the repository-equivalent package.

Add focused tests for:

```text
C64 read-only import;
basis manifests;
CM-ground selection;
TM orientation;
kinematic injection/projection;
CM projector;
quantum-number closure;
triplet import;
kinematic/color permutations;
full physical embedding;
exact support;
certified arrays and matrix-free actions;
historical adapter;
C47 reconciliation;
C52/C53 basis covariance and impact;
C57/C58 support and mode-ledger impact;
C59/C60 continuation;
count once;
isolation;
comparison maps;
end-to-end reconstruction.
```

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

All committed JSON and every runtime artifact must reconstruct byte-for-byte.

---

# 37. Acceptance criteria

C65 is complete only when:

1. The full C64 baseline reproduces.
2. The C64 positive status remains explicit.
3. C62/C63/C64 historical files remain unchanged.
4. C47/C52/C53/C57/C58 historical artifacts remain unchanged.
5. C40 remains method-oracle only.
6. No C64 artifact is regenerated by C65.
7. No historical quadrature value defines exact support.
8. The historical \(10^{-12}\) threshold cannot change support.
9. No physical coupling, subtraction, or counterterm coefficient is chosen.
10. No contact value or matrix is evaluated.
11. All 733 C64 blocks import read-only.
12. All 171,153 statuses import read-only.
13. All 67,920 residue certificates import read-only.
14. All C64 expression/support/basis/array hashes pass.
15. All required C64 runtime paths exist.
16. The C64 loader returns immutable data and calls no C62 generator.
17. Raw and physical basis manifests are complete.
18. Expected dimensions are verified rather than assumed.
19. CM-ground columns are selected through the C64 crosswalk.
20. No CM-excited column is selected.
21. The TM orientation is proved.
22. The kinematic injection exists.
23. The exact/metric adjoint projection exists.
24. \(P_{\rm kin}J_{\rm kin}=I\) closes.
25. The CM image projector is Hermitian and idempotent.
26. CM-excited states are orthogonal to the image.
27. Longitudinal, helicity, Jz, OAM, shell, and zero-mode identities close.
28. C53 color objects import read-only.
29. The kinematic/color permutation is explicit.
30. The full physical embedding exists.
31. \(P_{\rm phys}J_{\rm phys}=I\) closes.
32. The full image projector is Hermitian and idempotent.
33. No anti-sextet or 15 leakage is hidden.
34. CM/color factorization is proved where claimed.
35. Support uses no numerical threshold.
36. No component remains undecidable.
37. Certified arrays carry propagated bounds.
38. Precision changes do not alter support.
39. Sparse and matrix-free embed/project actions agree.
40. The historical adapter is derived, not fitted.
41. C47 reconciliation has no unexplained discrepancy.
42. C52 impact is fully typed.
43. C53 impact is fully typed.
44. C57 support positions, unions, and envelopes are independently audited.
45. C58 pair support, admitted modes, primitive, and qg status are independently audited.
46. C59/C60 continuation is fully typed.
47. No descendant status is preserved by threshold tuning.
48. Basis-only changes are distinguished from operator rebuilds.
49. Vertex supersession takes precedence over IFREG supersession.
50. Exactly one next branch is selected.
51. Every component has complete ancestry.
52. Duplicate, missing, undecidable, and unresolved-impact counts are zero.
53. Comparison maps retain all nonnested and adapter remainders.
54. Static and runtime poisoning controls pass.
55. Runtime bundles contain actual certified embeddings and impact records.
56. End-to-end reconstruction passes.
57. At least 320 focused live mutations are detected.
58. No endpoint relation, witness relation, contact support/value/matrix, complete instantaneous-fermion operator, local-HQCD matrix, projected identity, JMY Wilson/bilocal, soft, physical-renormalization, one-loop, matching, proton, ART25, fit, inference, process, or production object is created.
59. Historical roots, `NO_JOINT_MEASURE`, 216 routes, ART25 identities, and authoritative artifacts remain unchanged.
60. `MSHT20_REP/` remains untouched and outside Git.
61. The working tree is clean except for the pre-existing untracked directory.
62. A local completion commit is created and not pushed.

A rigorous no-go or supersession branch is valid. Do not weaken C64 read-only integrity, exact CM selection, TM orientation, triplet leakage, threshold-free support, numerical certification, historical-adapter derivation, or descendant-impact accounting to open the no-upstream-rebuild gate.

---

# 38. Final Codex response

Report:

- full starting and final commits;
- exact C64 aggregate and import hashes;
- reproduced counts for 733 blocks, 171,153 statuses, and 67,920 residue certificates;
- raw and physical qg dimensions and block decompositions;
- CM-ground selection counts and hashes;
- TM orientation and validation identities;
- kinematic injection/projection shapes, ranks, nnz, support counts, and round-trip residuals;
- CM image-projector rank, Hermiticity, idempotence, and CM-excited orthogonality;
- quantum-number closure results;
- C53 triplet import hashes;
- kinematic/color permutation shapes and hashes;
- full physical embedding/projection shapes, ranks, nnz, support counts, and round-trip residuals;
- full image-projector residuals;
- anti-sextet and 15 leakage residuals;
- certified numerical precisions, maximum entry bounds, and propagated invariant bounds;
- sparse/matrix-free residuals;
- historical basis-adapter formula, shape, phase/permutation content, and residuals;
- C47 support and numerical reconciliation;
- C47 basis-status decision;
- C52 impact status and basis-covariance residuals;
- C53 impact status and basis-covariance residuals;
- exact C57 support positions, unions, envelopes, and symmetric differences;
- exact C58 pair-support/admitted-mode counts and primitive-impact status;
- C59/C60 continuation status;
- complete descendant supersession decision;
- comparison-map residuals and separated remainders;
- ancestry, duplicate, missing, undecidable, and unresolved-impact counts;
- isolation and poisoning results;
- runtime support, adapter, bound, and array hashes;
- focused mutation results;
- exact readiness/no-go/supersession status;
- exact next branch;
- confirmation that no endpoint/witness relation, direct-contact support/value/matrix, complete instantaneous-fermion operator, local-HQCD matrix, projected identity, JMY Wilson/bilocal, soft, physical-renormalization, one-loop, matching, proton, ART25, fit, inference, process, or production object was created;
- integrity and deterministic-reconstruction status;
- local completion commit;
- confirmation that nothing was pushed.

Do not describe an in-memory regeneration of C64, a guessed TM orientation, a thresholded CM projection, an assumed Kronecker ordering, a fitted historical adapter, a numerically close but uncertified round trip, or an unaudited descendant as the exact physical \(qg\) embedding.
