# C180/HQCDB0RESLINKSCHEME1 implementation report

Status: `C180_HQCDB0RESLINKSCHEME1_BOUNDARY_OWNERSHIP_INCOMPLETE`
Plan: `B0RESLINKSCHEME1-H`
Next: `C181/HQCDB0HOBOUNDARY3`

The C179-to-C180 continuation contract was present and hash-verified:
`docs/next_level/c179_c180_hqcdb0reslinkscheme1_continuation_contract.json`,
SHA-256 `714a2f1ad8784155660f524e1e2513a32cb4ce591a4ed78b0257a50ebc7f7da3`.
The supplied prompt was read completely and hash-verified with SHA-256
`7de045ba08e26369d083a9ad03fda34938d3326ed981da456373bfc6d69b6c3b`.

## Result

The complete retained project P0 transverse-vector domain closes from the
immutable C176 dimensions: K9 72, K11 110, and K13 156.  The two vector
components are factorized over the retained scalar mode rank and remain
separate from the C151 physical one-gluon source and C175 ghost scalar modes.
The rank rule is `scalar_rank*2 + component_index` and is reversible.

The ordered pair domain is factorized, with no `(alpha,beta)` identification:

| resolution | vector modes | ordered pairs |
|---|---:|---:|
| K9 | 72 | 5,184 |
| K11 | 110 | 12,100 |
| K13 | 156 | 24,336 |

The pair rank is `first_rank*V + second_rank`; reverse pairs remain distinct.

The data-only `FINITE_HO_PATH_SIGNATURE_PROGRAM_V1` grammar is safe: it has
fixed opcodes and rejects arbitrary callables, `eval`, dynamic imports,
pickle, network access, physical fields, color matrices, and coupling values.
Full-mode degree-one and ordered degree-two programs expose six independent
routes each.  Degree-two order is retained and is neither symmetrized nor
Abelianized.  The shuffle identity is recorded at project normalization, with
separate symmetric and order-sensitive components.

The affine reference remains exactly
`PROJECT_FINITE_HO_AFFINE_TRANSVERSE_CONNECTOR_V1`.  XY and YX remain
deterministic holdouts and are not averaged or fitted.  Both conversion maps
are explicit scheme maps, never additive physical terms, and each is split
into retained, C176 boundary-owned, source-scope, and unresolved components.
The C176 leakage records are consumed read-only and remain unpruned:

* K9: dimension 36, 16 entries, rank 8, norm 2.4 GeV;
* K11: dimension 55, 20 entries, rank 10, norm 3.337289319193048 GeV;
* K13: dimension 78, 24 entries, rank 12, norm 4.415880433163924 GeV.

The boundary owner is present for every factorized conversion class, but the
retained/boundary/source-scope decomposition is not yet closed.  This is why
plan H is selected.  No claim is made that all path dependence is HO-owned.
K9/K11/K13 remain separate and no continuum extrapolation is made.

Future DIS and past DY, antisymmetric/PV transport, cut-shift covariance,
holonomy, open adjoint color, and C171 d/f multiplicities remain explicit.
Color is metadata only: no color matrix is multiplied and the external
adjoint coordinate is not quotiented.

## Boundaries and preservation

C43 and C130 through C179 are preserved.  The C179 affine representative,
the C178 periodic cut/holonomy interface, C177 linearized source-path scope,
and C176 finite-HO boundary authority are read-only.  The unrelated tracked
`handoff/ROADMAP.md` modification and all protected untracked paths are
preserved.  C134 remains the pre-existing unrelated expectation diagnostic;
it was quarantined and not repaired.  The inherited untracked C157 test was
not modified.  No new source was searched for or acquired, no C158 values were
used, and no C166 dependency-graph node or edge was added.

All six inherited request records remain visible.  The two B0-active requests
terminate at the C180 boundary-ownership-incomplete status and each receives
the typed capsule `C180-C176-BOUNDARY-OWNERSHIP-REFINEMENT`.  The other four
requests remain preserved inherited records.

The C180 public API is implemented in
`src/deuteron_wigner/bridge/hqcdb0reslinkscheme1/`, with runtime metadata in
`data/runtime/c180_hqcdb0reslinkscheme1/`.  Every returned record is frozen;
the loader verifies the runtime package root and contract hash.  The runtime
manifest is deterministic across two clean builds.  The focused C180 suite
passes 5 tests, including 384 live mutation guards.  C179 inheritance and
the prior C153-C179 boundary results remain recorded in the C180 regression
and preservation manifests; the known C134 diagnostic remains outside the
repair scope.

The single next continuation is
`docs/next_level/c180_c181_hqcdb0hoboundary3_continuation_contract.json`.
Nothing was pushed.

## Explicit nonclaims

No endpoint value, physical field coefficient, physical coupling, complete
Wilson coefficient, ghost-link kernel, B0 kernel, gluon self-energy, qg 1PI
vertex, standard-scheme adapter, target-minus-C43 result, running, threshold,
matching window, counterterm, null representative, state, quantum object,
physical TMD staple, soft factor, BRST closure, or full Slavnov–Taylor closure
was created.  The JMY staple was not imported.  No diagnostic constant-mode
result was promoted as proof by itself, no ordered degree-two term was
symmetrized, no future/past class was merged, no holonomy was dropped, and no
finite-HO leakage or omitted Hilbert space was threshold-pruned or
materialized.
