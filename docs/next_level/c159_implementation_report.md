# C159/HQCDMATCHIR3 implementation report

The existing local pytest runner was found at
`/Users/dustin/miniforge3/bin/python3.9` (pytest 8.4.2). The inherited C153
and C156 tests passed, but two C157 test assertions failed because the
untracked inherited test expects the superseded plan `MATCHIR2-D` and the
superseded continuation `C158/HQCDMATCHWINDOW2`. The frozen authority requires
`MATCHIR2-B` and `C158/HQCDFBNUM`.

C159 therefore selected MATCHIR3-I and stopped before target coefficients,
common-IR differences, remainders, or scale brackets. Target programs are
only immutable source-qualified descriptors; no target formula or coefficient
was invented. The C158 public package root, direct validators, safe reload,
and 384 mutation controls passed without recomputing C158 coefficients.

The sole evidence-driven continuation is C160/HQCDFBTEST.
