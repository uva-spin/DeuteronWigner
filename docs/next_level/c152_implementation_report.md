# C152/HQCDQGVERT implementation report

Status: `C152_C151_SOURCE_DERIVED_CONDITIONAL_AMPUTATED_QG_VERTEX_AUTHORITY_READY`.

C152 binds incoming and outgoing C150 quark schemes, a C151 gluon scheme,
three explicit subtraction records, C77 qg embedding, orientation,
kinematics, conservation identities, and an explicit fixture or parameter
record. The connected qgq response is exposed through direct, block,
matrix-free, and response-derivative routes; each external leg is amputated
with its declared inverse-two-point scheme.

The retained qg proper vertex is distinct from the connected response and
from a complete full-QCD 1PI vertex. qgg, qqbar, pure-gluon, zero-mode, and
boundary corrections remain unavailable. The finite light-front vertex
tensor basis has rank eight with dual-Gram, analytic, response, and free
projector routes. `V_B`, `Z_1F`, and conditional `g_R^FB` are exposed with a
derivative guard at (g_s=0), but remain nonphysical and scheme-explicit.

The historical `g_R_FB(K_R)` coordinate is crosswalked without overwrite.
Nullspace and counterterm sensitivity remain prospective. The next
continuation is `C153/HQCDMATCHFB`.
