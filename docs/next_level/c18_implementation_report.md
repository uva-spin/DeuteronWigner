# C18/N3 implementation report

C18 adds explicit DeltaDelta and compact six-quark validation branches to
the immutable NN plus continuum-NNPI N2 state. The DeltaDelta sector uses the
four charge channels generated by the normalized isospin-zero Clebsch vector
and retains the Pauli-allowed 3S1, 3D1, and 7D1 waves. Its threshold is 2.464
in the declared oracle units and has exactly zero absorptive support at the
deuteron pole.

The compact color space retains all five Young-[2,2,2] SU(3) singlet
multiplicities. One direction represents the declared 3+3 cluster vector;
four form its hidden-color complement. Two unitary hidden bases give the
same complete observable. Exact signed S6 identity covers flavor, color,
helicity, longitudinal, transverse/OAM, and cluster labels before operator
assembly.

NN and DeltaDelta cluster embeddings generate an executable Gram projector.
Plan C adds only its orthogonal compact complement; the independent
subtraction representation closes within 2.1e-13. Explicit and induced
DeltaDelta/six-quark descriptions remain alternatives with visible
remainders 0.0031 and 0.0046.

The coupled state diagnostics are Z_NN=0.845, Z_NNPI=0.1084241685,
Z_DeltaDelta=0.028, and Z_6q=0.0185758315. These resolution-dependent values
are not fitted physical probabilities. The Hamiltonian is Hermitian and
exact, matrix-free Krylov, and full-bond TTN routes close. A low-bond state
has only 0.07% energy error while losing 31--55% of non-nucleonic observables.

The declared-order current certificate has no unexplained retained-term
gap, and all supported sector-pair continuity blocks close. Direct u/d
six-quark operators are explicit; antiquarks are unavailable at six-quark-
only order and gluons are induced with a visible 0.0042 remainder. Missing
species are never reported as zeros.

The common-parent tensor diagnostic gives matched delta_T=0.0058,
f1LL=-0.0038666667, and b1 moment 0.0046. These are validation signals, not
physical predictions. Coherent amplitudes include NNPI, DeltaDelta, and 6q
channels before CP reduction; an early trace loses 0.011 of interference.

C18 covers 762 requirements, 400 ordered negative injections, and all 18
N3 benchmark families. It remains finite-resolution, unmatched, unevolved,
validation-only, and disconnected from the 216 production routes.
