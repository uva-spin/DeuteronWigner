# ADR 159: Separate kinematic overlap from comparison readiness

**Decision.** Record the twelve frozen points as `BRIDGE_COMMON_DOMAIN_ONLY`.

**Reason.** Shared x, b, Q, flavor, rank, and target coordinates do not establish
a shared operator/scheme definition.
