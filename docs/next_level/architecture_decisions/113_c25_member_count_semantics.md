# ADR 113: Central and replica-count semantics

Accepted. The machine-readable header and rows govern: 642 stochastic members plus initialization and central/mean technical records. The “500 replicas” prose is retained as a documented discrepancy and is not used to truncate the ensemble.
