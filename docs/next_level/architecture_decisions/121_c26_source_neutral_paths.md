# ADR 121: Source-neutral set paths only

Accepted. Runtime aliases may change a filesystem path only when content, metadata, names, and numbering are byte-identical. C26 uses no alias because the exact PDF source is absent.
