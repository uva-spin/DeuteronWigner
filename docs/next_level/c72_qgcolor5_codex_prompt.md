# C72/QGCOLOR5 Codex Work Package

## Title

**Authoritative completion of the triplet-color import package: authenticated scientific-source and public-API fingerprints, explicit \(24\times3\) basis identities, exact 72-entry record ancestry, per-entry certified-bound identities, and a single immutable C73 embedding contract**

## Authoritative baseline

Start from the clean local C71 completion commit:

```text
e33862b86eebca185daaf8beddf170b6761ca6f0
```

Before changing tracked files, run and record:

```bash
git status --short
git rev-parse HEAD
git show -1 --oneline --stat
test "$(git rev-parse HEAD)" = "e33862b86eebca185daaf8beddf170b6761ca6f0"
```

The following pre-existing untracked paths must remain untouched and outside Git:

```text
MSHT20_REP/
docs/next_level/c69_qgembed5_codex_prompt.md
```

Do not add, modify, remove, rename, stage, or consume either path as scientific authority.

The baseline is authoritative only when it contains and reproduces:

```text
C64_SOURCE_DERIVED_EXACT_TM_ARTIFACTS_READY

C66_SOURCE_DERIVED_TRIPLET_ISOMETRY_ARTIFACT_READY

C68_SOURCE_DERIVED_TRIPLET_ISOMETRY_RUNTIME_READY

C70_SOURCE_DERIVED_TRIPLET_PACKAGE_IMPORT_READY

C71_QGEMBED_C70_IMPORT_INCOMPLETE
```

and the exact C71 descendant correction:

```text
C70 authenticated content:
    six numerical arrays;
    72 simplified terminal-status entries;
    an authenticated package root/index for those objects;
    immutable numerical-array returns;

missing downstream-authority content:
    authenticated scientific-source fingerprints;
    authenticated public-API fingerprints;
    explicit ordered 24-row product-color basis manifest;
    explicit ordered 3-column retained-triplet basis manifest;
    complete 72-entry exact-record identities;
    complete exact-expression or exact-zero-certificate ancestry;
    per-entry certified-bound identities coupling each midpoint,
    bound, exact record, row ID, and column ID;

C71 consequence:
    C70 cannot be consumed as the complete immutable triplet package
    required before physical embedding;
    no CM-ground or full physical qg embedding was constructed;
    no descendant-impact or direct-contact work was performed.
```

Verify the actual repository state, C66/C68/C70 scientific payload, C70 package, and C71 import audit. This prompt is not numerical authority.

The exact scientific object is immutable:

\[
U_3=\frac{E_{\rm src}}{\sqrt{C_F}},
\qquad
C_F=\frac43,
\qquad
U_3^\dagger U_3=I_3,
\qquad
P_3=U_3U_3^\dagger.
\]

The following identities must remain unchanged:

```text
the 24 product-color rows;

the 3 retained-triplet columns;

the 72 exact U3 entries;

all exact zero and nonzero classifications;

all exact expressions;

all exact-zero certificates;

E_src, Gram, triplet adapter, U3, U3-dagger, and P3;

all numerical midpoints and certified bounds;

all-eight-generator intertwining;

rank-three projector closure;

zero anti-sextet and 15 leakage;

the inherited C53-impact decision.
```

C64 remains immutable and read-only.

Create a local completion commit. Do not push.

---

# 1. Descendant correction and ownership

C72 is a narrow descendant correction to the C70 package-readiness claim.

Do not alter historical C70 records. Instead create an explicit descendant status relation:

```text
historical status:
    C70_SOURCE_DERIVED_TRIPLET_PACKAGE_IMPORT_READY

descendant qualification:
    C70_TRIPLET_PACKAGE_PARTIAL_IMPORT_ONLY

reason:
    authenticated numerical payload exists, but the package does not
    authenticate the complete scientific-source/API/basis/exact-record/
    per-entry-bound authority required by a physical-embedding
    consumer.
```

C72 owns the completed authority package:

```text
C66:
    exact triplet-isometry science;

C68:
    first exact/certified runtime payload;

C70:
    authenticated partial package around arrays and simplified
    statuses;

C72:
    complete scientific-authority package with one authenticated
    root covering source/API fingerprints, basis identities,
    exact-record identities, and per-entry bound identities.
```

C72 must not claim that the missing content was already present in C70.

---

# 2. Execution mandate

C72 must close all four C71 import blockers in this package:

1. authenticated scientific-source fingerprints;
2. authenticated public-API fingerprints;
3. authenticated ordered \(24\times3\) basis and pair identities;
4. authenticated exact-record and per-entry certified-bound identities.

C72 must produce one complete package object and one complete package root.

Do not create another wrapper-only continuation. A fail-closed result is valid only if one of the inherited exact records, basis identities, numerical bounds, or scientific-source/API identities is internally inconsistent.

Missing convenience prose, a preferred filename, a duplicate inventory, or a desire for a further package layer is not a valid no-go.

---

# 3. Exact purpose

C72 must create:

```text
a complete C66/C68/C70 scientific-source fingerprint;

a complete C72 public-API fingerprint;

canonical ordered manifests for all 24 product-color rows and all
3 retained-triplet columns;

canonical identities for all 72 row-column pairs;

a complete exact-record domain for all 72 U3 entries;

a canonical exact expression or exact-zero certificate identity for
every entry;

a canonical per-entry numerical identity coupling midpoint, bound,
exact record, row, column, status, and ancestry;

authenticated companion-object identities for E_src, Gram, triplet
adapter, U3, U3-dagger, and P3;

one self-contained deterministic runtime root;

one canonical authenticated index containing every authority and
payload identity;

one aggregate package-root hash;

one frozen immutable package object;

read-only loaders for every authority domain;

complete C66/C68/C70 equivalence;

a C73/QGEMBED7 import contract;

a dry C73 import preflight with C64 and C72;

and an explicit supersession of the C71 import blocker.
```

C72 must not construct:

```text
a CM-ground kinematic embedding;

a full physical qg embedding;

C47/C52/C53/C57/C58 descendant-impact results beyond preserving
the inherited color-side C53-impact record;

direct-contact endpoint or witness support;

a contact value or matrix;

a complete instantaneous-fermion operator;

a Wilson/TMD or matching object.
```

The strongest positive status is:

```text
C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY
```

The exact positive continuation is:

> **C73/QGEMBED7 — construct the exact CM-ground and color-triplet physical \(qg\) embedding and close descendant impact using immutable C64 and C72 packages**

---

# 4. Mandatory repository audit

Inventory actual files rather than assuming filenames:

```bash
git ls-files 'docs/next_level/c66*'
git ls-files 'docs/next_level/c68*'
git ls-files 'docs/next_level/c70*'
git ls-files 'docs/next_level/c71*'
git ls-files 'src/deuteron_wigner/**/qgcolor2*'
git ls-files 'src/deuteron_wigner/**/qgcolor_runtime*'
git ls-files 'src/deuteron_wigner/**/qgcolor4*'
git ls-files 'tests/*c66*'
git ls-files 'tests/*c68*'
git ls-files 'tests/*c70*'
git ls-files 'tests/*c71*'
find data/runtime -maxdepth 3 -type f | sort
```

Read completely the actual equivalents of:

```text
C66 source authority, SU(3), basis, source-emission, Gram,
normalization, exact-isometry, intertwiner, projector, expression,
support, numerical certification, C53-impact, and readiness records;

C68 exact/runtime payload records and equivalence reports;

C70 package schema, index, package root, runtime inventory, public
loaders, package object, equivalence, determinism, and readiness
records;

C71 C70-import audit, implementation report, missing-calculation
specification, readiness report, and regression records.
```

Use actual filenames. Do not treat an absent preferred filename as an obstruction when the same authoritative content exists elsewhere.

Create:

```text
docs/next_level/c72_derivation_authority_manifest.json
docs/next_level/c72_input_fidelity_audit.json
```

---

# 5. Complete scientific-source fingerprint

Construct one canonical source-authority manifest covering every tracked source that can alter:

```text
SU(3) fundamental and adjoint generators;

product-color row order;

retained-triplet column order and basis adapter;

E_src;

Gram normalization;

U3;

U3-dagger;

P3;

terminal statuses;

exact expressions;

zero certificates;

certified numerical values and bounds;

C70 package serialization and loading.
```

Every source record must contain:

```text
repository-relative path;

SHA-256;

scientific role;

public symbols consumed;

construction-authority / validation-only classification;

schema or serializer identity;

ancestry package.
```

Compute a canonical aggregate:

```text
scientific_source_fingerprint_sha256
```

Authenticate this fingerprint inside the C72 package root.

Create:

```text
docs/next_level/c72_source_fingerprint_manifest.json
docs/next_level/c72_source_fingerprint_validation.json
```

Any construction-authority source change must fail the package import.

---

# 6. Complete public-API fingerprint

Define the exact public C72 import surface before implementation.

For every public symbol record:

```text
module path;

qualified symbol name;

call signature;

return schema/type;

immutability semantics;

package-root verification semantics;

failure behavior;

no-regeneration guarantee.
```

The API must include a single top-level loader:

```python
load_triplet_authority_package() -> TripletAuthorityPackage
```

and may include subordinate read-only loaders.

Compute:

```text
public_api_fingerprint_sha256
```

Authenticate it inside the package root.

Create:

```text
docs/next_level/c72_api_fingerprint_manifest.json
docs/next_level/c72_api_fingerprint_validation.json
```

A signature or return-schema change requires descendant supersession.

---

# 7. Canonical ordered basis authority

Materialize explicit authoritative basis manifests.

## 7.1 Product-color rows

Exactly 24 ordered records:

```text
row_index;

canonical_row_id;

fundamental_color_id;

adjoint_color_id;

C53/C66 basis ancestry;

basis-order source hash.
```

## 7.2 Retained-triplet columns

Exactly 3 ordered records:

```text
column_index;

canonical_column_id;

retained-triplet state ID;

canonical-fundamental adapter identity;

phase/permutation ancestry;

basis-order source hash.
```

## 7.3 Complete pair domain

Exactly 72 ordered pair records:

```text
pair_index;

row_index and row_id;

column_index and column_id;

canonical_entry_id;

matrix orientation.
```

Compute and authenticate:

```text
row_basis_sha256;

column_basis_sha256;

pair_domain_sha256;

combined_basis_authority_sha256.
```

Create:

```text
docs/next_level/c72_triplet_basis_authority_manifest.json
docs/next_level/c72_triplet_basis_authority_validation.json
```

The package must fail on any row/column reordering, phase change, missing record, duplicate record, or changed pair orientation.

---

# 8. Complete 72-entry exact-record authority

For each of the 72 entries create one immutable authority record containing:

```text
canonical_entry_id;

row_id;

column_id;

terminal exact status;

exact support Boolean derived from status;

exact expression identity for nonzero entries;

exact-zero certificate identity for zero entries;

normalization ancestry;

basis-adapter ancestry;

source-emission ancestry;

C66 exact-constructor fingerprint;

C68/C70 payload identity.
```

The terminal status vocabulary must be the actual inherited C66/C68 vocabulary. Do not collapse exact classes into a simplified zero/nonzero label.

For nonzero entries, authenticate a canonical exact expression.

For zero entries, authenticate a canonical proof/certificate identity.

Compute:

```text
exact_record_domain_sha256;

expression_domain_sha256;

zero_certificate_domain_sha256;

exact_support_domain_sha256.
```

Create:

```text
docs/next_level/c72_triplet_exact_record_manifest.json
docs/next_level/c72_triplet_exact_record_validation.json
```

A positive gate requires:

```text
candidate entries = 72;

missing exact records = 0;

duplicate exact records = 0;

unclassified entries = 0;

nonzero without expression = 0;

zero without certificate = 0.
```

---

# 9. Per-entry certified-bound authority

C71 specifically requires per-entry bound identities, not only array-level hashes.

For every one of the 72 \(U_3\) entries create a canonical numerical-authority record containing:

```text
canonical_entry_id;

row_id;

column_id;

exact-record hash;

terminal status;

midpoint real and imaginary components;

absolute-error bound;

interval/enclosure convention;

working precision;

output dtype and endianness;

midpoint-array object ID and index;

bound-array object ID and index;

midpoint scalar hash;

bound scalar hash;

combined per-entry numerical identity hash.
```

For exact zeros require:

```text
literal zero midpoint;

zero exact support;

explicit zero-certificate identity;

an exact-zero or rigorously declared zero bound identity.
```

For exact nonzeros require the certified interval to exclude zero.

Compute:

```text
per_entry_bound_domain_sha256
```

Create:

```text
docs/next_level/c72_triplet_per_entry_bound_manifest.json
docs/next_level/c72_triplet_per_entry_bound_validation.json
```

Array-level hashing is retained but is not a substitute for this entry-level identity domain.

---

# 10. Companion-object authority

Materialize authenticated object records for:

```text
E_src;

Gram;

triplet basis adapter;

U3;

U3-dagger;

P3;

required basis permutations.
```

Each object authority record must contain:

```text
scientific object ID;

shape;

ordered row/column basis hashes;

exact-object identity;

exact-expression/status ancestry;

midpoint-array identity;

bound-array identity;

array-level hashes;

per-entry identity-domain hash;

scientific-source fingerprint.
```

Create:

```text
docs/next_level/c72_companion_object_authority_manifest.json
docs/next_level/c72_companion_object_authority_validation.json
```

Do not numerically reconstruct a companion object from another payload when a byte-identical inherited payload exists.

---

# 11. Self-contained deterministic runtime package

Use:

```text
data/runtime/c72_qgcolor5/
```

Create one self-contained package containing:

```text
basis authority manifests;

72-entry exact-record domain;

72-entry per-entry-bound domain;

exact expressions;

zero certificates;

certified numerical midpoint and bound arrays;

companion-object metadata;

C53-impact preservation record;

source fingerprint;

API fingerprint;

canonical index;

index authentication sidecar.
```

When copying inherited payload files, record source and destination hashes and require byte identity.

Create:

```text
docs/next_level/c72_payload_equivalence_report.json
docs/next_level/c72_payload_completeness_report.json
```

---

# 12. Canonical authenticated index and package root

Create:

```text
data/runtime/c72_qgcolor5/index.json
data/runtime/c72_qgcolor5/index.sha256
```

The canonical `index.json` must include:

```text
schema and serializer versions;

scientific status;

scientific-source fingerprint;

public-API fingerprint;

row, column, pair-domain, exact-record, expression,
zero-certificate, exact-support, and per-entry-bound hashes;

ordered payload inventory;

for each payload:
    relative path;
    object ID;
    scientific role;
    schema;
    file size;
    SHA-256;
    shape/record count;
    dtype/endianness;
    basis identity;
    exact-object identity;
    bound identity;

expected payload count;

allowed control files;

C66/C68/C70/C71 ancestry;

C53-impact status.
```

The index must not contain its own hash.

Authenticate it with `index.sha256`.

Create a tracked package-root manifest containing the expected index hash and all top-level authority fingerprints.

The authenticated `index.json` hash is the package root because the index covers every authority and payload identity.

Create:

```text
docs/next_level/c72_package_root_manifest.json
docs/next_level/c72_index_authentication_report.json
```

---

# 13. Complete immutable package object

Implement under:

```text
src/deuteron_wigner/bridge/qgcolor5/
```

or the repository-equivalent package.

Provide:

```python
load_triplet_authority_package() -> TripletAuthorityPackage
```

The frozen package object must expose:

```text
package-root hash;

scientific-source fingerprint;

public-API fingerprint;

schema/serializer identities;

24-row basis manifest;

3-column basis manifest;

72-pair domain;

72 exact-record authorities;

72 per-entry-bound authorities;

exact expressions;

zero certificates;

certified companion objects;

complete verified inventory;

C53-impact status.
```

Loading the package must verify the complete authenticated root before returning any object.

All arrays must be non-writeable.

All records, mappings, and sequences must be immutable.

Create:

```text
docs/next_level/c72_package_object_contract.json
docs/next_level/c72_package_object_validation.json
```

---

# 14. Public read-only loaders

Provide public operations equivalent to:

```python
load_triplet_authority_package()

load_product_color_basis()

load_retained_triplet_basis()

load_triplet_pair_domain()

load_triplet_exact_records()

load_triplet_exact_expressions()

load_triplet_zero_certificates()

load_triplet_per_entry_bounds()

load_certified_source_emission()

load_certified_triplet_isometry()

load_certified_triplet_adjoint()

load_certified_triplet_projector()

embed_triplet_color_to_product(...)

project_product_color_to_triplet(...)

apply_triplet_projector(...)
```

Every loader must:

```text
verify the package root;

return immutable objects;

expose exact status independently of numerical magnitude;

couple every numerical midpoint to its bound identity;

call no C66/C68/C70 builder;

regenerate nothing;

fail on a missing, changed, unindexed, or writable artifact.
```

Create:

```text
docs/next_level/c72_api_contract.json
docs/next_level/c72_api_validation.json
```

The actual public surface must reproduce the authenticated API fingerprint.

---

# 15. Strict no-regeneration and no-inference guards

The public package must be unable to call:

```text
C66 build();

C68 build();

C70 build();

the exact U3 constructor;

raw_emission_E normalization;

P3 factorization;

a C53 vertex builder;

an expression reconstruction fallback;

a build-if-missing or repair-if-missing path.
```

The package must never infer:

```text
basis order from array position without the basis manifest;

support from midpoint magnitude;

an exact expression from a numerical value;

a zero certificate from a small bound;

a per-entry bound identity from an array-level hash alone.
```

Create:

```text
docs/next_level/c72_no_regeneration_no_inference_report.json
```

---

# 16. Complete equivalence checks

Compare C72 against C66/C68/C70 authority.

For all 72 entries compare:

```text
row/column/pair IDs;

exact status;

support;

exact expression or zero certificate;

midpoint;

bound;

per-entry numerical identity;

source and ancestry identities.
```

Also compare all companion objects and basis orders.

Require:

```text
exact identity for exact records;

byte identity for copied payloads;

all inherited exact values inside certified intervals;

all inherited array hashes preserved where copied.
```

Create:

```text
docs/next_level/c72_entry_equivalence_report.json
docs/next_level/c72_object_equivalence_report.json
```

A sampled comparison is insufficient.

---

# 17. Package-only invariant validation

Using only the C72 package API, validate:

\[
U_3^\dagger U_3=I_3,
\]

\[
P_3=U_3U_3^\dagger,
\qquad
P_3^\dagger=P_3,
\qquad
P_3^2=P_3.
\]

Also validate:

```text
rank(P3) = 3;

trace(P3) = 3;

all-eight-generator intertwining;

triplet preservation;

zero anti-sextet leakage;

zero 15 leakage;

embed/project round trips;

entry-level interval containment.
```

No C66/C68/C70 builder may be called.

Create:

```text
docs/next_level/c72_package_invariant_report.json
```

---

# 18. Complete runtime inventory

The authenticated index is the authoritative inventory.

Require:

```text
every indexed payload exists;

every hash, size, shape, dtype, and record count matches;

every required scientific object appears exactly once;

every file under the package root is indexed or is an allowed
control file;

no extra, orphan, duplicate, unhashed, mutable, writable, or
unbounded object exists.
```

Create:

```text
docs/next_level/c72_runtime_inventory_report.json
docs/next_level/c72_runtime_inventory_validation.json
```

A positive gate requires every error count to be zero.

---

# 19. C73/QGEMBED7 import contract

Define the exact immutable contract by which C73 consumes:

```text
the C72 package root;

source/API fingerprints;

basis authority manifests;

72-pair domain;

72 exact-record authorities;

72 per-entry-bound authorities;

exact expressions and zero certificates;

certified U3/U3-dagger/P3 and companion objects;

complete inventory;

C53-impact status;

public frozen package object and actions.
```

C73 must verify the C72 package root before combining it with C64.

C73 may not:

```text
call C66/C68/C70/C72 builders;

open unindexed paths;

infer basis identity;

infer exact support from magnitude;

infer an exact record from a simplified status;

infer a per-entry bound from an array hash;

renormalize U3;

factor P3;

or select new phases.
```

Create:

```text
docs/next_level/c72_c73_qgembed7_import_contract.json
```

---

# 20. Dry C73 import preflight

Run a dry preflight using only:

```text
the C64 immutable public API;

the C72 immutable public API.
```

Verify:

```text
both package roots;

all authority fingerprints;

all required basis/crosswalk information;

all exact support records;

all per-entry bound identities;

all arrays and bounds;

immutability;

no-regeneration guards;

the metadata needed to form the future kinematic/color composition.
```

Do not construct the CM-ground or full physical embedding in C72.

Create:

```text
docs/next_level/c72_c71_blocker_supersession_report.json
docs/next_level/c72_c73_import_preflight.json
```

The report must explicitly establish that:

```text
C71_QGEMBED_C70_IMPORT_INCOMPLETE
```

is superseded by the C72 package.

---

# 21. Deterministic reconstruction

Run:

```text
two consecutive complete builds;

one clean temporary-runtime build;

one serial build;

one supported parallel/sharded build where supported;

one restart/resume build.
```

Require byte-identical:

```text
payloads;

basis manifests;

exact-record domains;

per-entry-bound domains;

index.json;

index.sha256;

package-root manifest;

all tracked reports.
```

Create:

```text
docs/next_level/c72_deterministic_reconstruction_report.json
docs/next_level/c72_restart_parallel_report.json
docs/next_level/c72_environment_manifest.json
```

---

# 22. Count-once and provenance

Report:

```text
row basis records = 24;

column basis records = 3;

pair records = 72;

terminal statuses = 72;

exact nonzero expressions;

exact-zero certificates;

per-entry bound records = 72;

companion objects;

payload files;

control files;

missing records/files;

duplicate IDs/paths;

extra/orphan files;

hash mismatches;

schema mismatches;

entries without exact records;

entries without bound identities;

writable arrays;

mutable records;

unresolved-impact entries.
```

Every entry must have one ancestry:

```text
C53/C66 convention
 -> C66 exact entry
 -> C68/C70 payload
 -> C72 basis/pair authority
 -> C72 exact-record authority
 -> C72 per-entry bound authority
 -> authenticated package root
 -> immutable loader.
```

Create:

```text
docs/next_level/c72_artifact_ancestry_ledger.json
docs/next_level/c72_count_once_report.json
```

---

# 23. Isolation and mutation controls

Prove C72 construction is independent of:

```text
C53 stored physical vertex values;

raw_emission_E numerical values after exact ancestry is frozen;

historical C53 projectors;

C47/C50/C52/C57/C58 numerical objects;

C64 arrays except during the C73 preflight;

ART25 files.
```

The package must fail when any of the following changes:

```text
a source fingerprint;

an API signature;

a row or column basis identity/order;

a pair identity;

an exact status;

an exact expression;

a zero certificate;

a midpoint;

a per-entry bound;

an array hash;

index.json;

index.sha256;

the package-root manifest;

an inventory row;

an immutability flag;

a no-regeneration guard;

the C73 import contract.
```

Create at least **320 focused live mutations** of real objects.

Create:

```text
docs/next_level/c72_isolation_report.json
docs/next_level/c72_regression_report.json
```

---

# 24. Readiness gate

Issue:

```text
C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY
```

only when:

```text
the full C71 baseline reproduces;

the two required untracked paths remain untouched;

the C66/C68/C70 scientific payload remains unchanged;

the C70 partial-readiness descendant correction is explicit;

the complete scientific-source fingerprint exists and verifies;

the complete public-API fingerprint exists and verifies;

24 ordered row records exist and verify;

3 ordered column records exist and verify;

72 ordered pair identities exist and verify;

all 72 exact-record authorities exist;

every nonzero has an exact-expression identity;

every zero has an exact-zero-certificate identity;

all 72 per-entry bound identities exist;

every midpoint is coupled to its exact record and bound;

all companion-object authorities exist;

one self-contained deterministic runtime package exists;

the authenticated index covers every authority and payload identity;

the package root authenticates source, API, basis, exact-record,
per-entry-bound, companion-object, and inventory domains;

all public loaders return immutable objects;

the complete package object verifies before returning data;

no loader regenerates or infers missing authority;

complete equivalence passes;

package-only invariants pass;

the complete inventory has no error;

the C73 import contract is complete;

the C73 dry preflight with C64 and C72 passes;

the C71 blocker supersession is explicit;

deterministic reconstruction passes;

count-once, ancestry, isolation, and mutation tests pass.
```

Do not issue:

```text
C72_SOURCE_DERIVED_EXACT_QG_EMBEDDING_READY;

C72_IFERM_CONTACT_SUPPORT_READY;

C72_DIRECT_IFERM_CONTACT_READY;

C72_SOURCE_DERIVED_INSTANTANEOUS_FERMION_READY;

C72_COMPLETE_LOCAL_HQCD_SUBSTRATE_READY.
```

---

# 25. Exact no-go branches

## A. Source/API authority incomplete

```text
C72_QGCOLOR5_AUTHORITY_FINGERPRINT_INCOMPLETE
```

Next:

> **C73/QGCOLOR-AUTH — close only the concrete source/API fingerprint defect**

## B. Basis/pair authority incomplete

```text
C72_QGCOLOR5_BASIS_AUTHORITY_INCOMPLETE
```

Next:

> **C73/QGCOLOR-BASIS2 — close only the 24-row, 3-column, and 72-pair identity defect**

## C. Exact-record authority incomplete

```text
C72_QGCOLOR5_EXACT_RECORD_AUTHORITY_INCOMPLETE
```

Next:

> **C73/QGCOLOR-EXACT2 — close only the status/expression/zero-certificate identity defect**

## D. Per-entry bound authority incomplete

```text
C72_QGCOLOR5_PER_ENTRY_BOUND_INCOMPLETE
```

Next:

> **C73/QGCOLOR-BOUND2 — close only the midpoint/bound/exact-record identity defect**

## E. Runtime/index/loader integrity incomplete

```text
C72_QGCOLOR5_PACKAGE_INTEGRITY_INCOMPLETE
```

Next:

> **C73/QGCOLOR-PACKAGE2 — close only the concrete package-root, inventory, immutability, or loader defect**

## F. Full import closure succeeds

```text
C72_SOURCE_DERIVED_TRIPLET_FULL_IMPORT_READY
```

Next:

> **C73/QGEMBED7 — exact physical \(qg\) embedding and descendant-impact closure**

---

# 26. Required deliverables

Create at least:

```text
docs/next_level/c72_implementation_report.md
docs/next_level/c72_api.md
docs/next_level/c72_derivation_authority_manifest.json
docs/next_level/c72_input_fidelity_audit.json

docs/next_level/c72_c70_descendant_correction.json

docs/next_level/c72_source_fingerprint_manifest.json
docs/next_level/c72_source_fingerprint_validation.json
docs/next_level/c72_api_fingerprint_manifest.json
docs/next_level/c72_api_fingerprint_validation.json

docs/next_level/c72_triplet_basis_authority_manifest.json
docs/next_level/c72_triplet_basis_authority_validation.json
docs/next_level/c72_triplet_exact_record_manifest.json
docs/next_level/c72_triplet_exact_record_validation.json
docs/next_level/c72_triplet_per_entry_bound_manifest.json
docs/next_level/c72_triplet_per_entry_bound_validation.json
docs/next_level/c72_companion_object_authority_manifest.json
docs/next_level/c72_companion_object_authority_validation.json

docs/next_level/c72_payload_equivalence_report.json
docs/next_level/c72_payload_completeness_report.json
docs/next_level/c72_package_root_manifest.json
docs/next_level/c72_index_authentication_report.json

docs/next_level/c72_package_object_contract.json
docs/next_level/c72_package_object_validation.json
docs/next_level/c72_api_contract.json
docs/next_level/c72_api_validation.json
docs/next_level/c72_no_regeneration_no_inference_report.json

docs/next_level/c72_entry_equivalence_report.json
docs/next_level/c72_object_equivalence_report.json
docs/next_level/c72_package_invariant_report.json
docs/next_level/c72_runtime_inventory_report.json
docs/next_level/c72_runtime_inventory_validation.json

docs/next_level/c72_c73_qgembed7_import_contract.json
docs/next_level/c72_c71_blocker_supersession_report.json
docs/next_level/c72_c73_import_preflight.json

docs/next_level/c72_deterministic_reconstruction_report.json
docs/next_level/c72_restart_parallel_report.json
docs/next_level/c72_environment_manifest.json
docs/next_level/c72_artifact_ancestry_ledger.json
docs/next_level/c72_count_once_report.json
docs/next_level/c72_isolation_report.json

docs/next_level/c72_readiness_report.json
docs/next_level/c72_source_sufficiency_decision.json
docs/next_level/c72_no_go_decision_tree.json
docs/next_level/c72_missing_calculation_specification.md
docs/next_level/c72_regression_report.json
```

Use:

```text
src/deuteron_wigner/bridge/qgcolor5/
data/runtime/c72_qgcolor5/
```

or exact repository-equivalent paths.

Update:

```text
handoff/ROADMAP.md
references/formalism_volume_index.md
```

Do not add or modify the old untracked C69 prompt.

---

# 27. Acceptance criteria

C72 is complete only when:

1. Baseline `e33862b86eebca185daaf8beddf170b6761ca6f0` reproduces.
2. Both required untracked paths remain untouched.
3. C66/C68/C70 numerical and exact scientific content remains unchanged.
4. Historical C70 files remain byte-identical.
5. The C70 readiness overstatement is corrected only in a descendant record.
6. Complete scientific-source fingerprints are authenticated.
7. Complete public-API fingerprints are authenticated.
8. Exactly 24 ordered row identities exist.
9. Exactly 3 ordered column identities exist.
10. Exactly 72 ordered pair identities exist.
11. Every pair has one terminal exact status.
12. Every nonzero has one exact-expression identity.
13. Every zero has one exact-zero-certificate identity.
14. Every entry has one per-entry numerical-bound identity.
15. Every midpoint is coupled to its bound and exact record.
16. Exact zeros remain literal zeros.
17. Exact nonzero intervals exclude zero.
18. Companion-object identities are complete.
19. The self-contained C72 package exists.
20. The canonical index covers all authority and payload domains.
21. The index is authenticated noncircularly.
22. The package root authenticates source, API, basis, exact-record, per-entry-bound, and inventory identities.
23. Every payload is indexed exactly once.
24. No extra, orphan, duplicate, unhashed, mutable, writable, or unbounded object exists.
25. The top-level package object is frozen.
26. All public loaders verify the root.
27. All public loaders return immutable objects.
28. No loader calls a builder or inference fallback.
29. Complete entry and object equivalence passes.
30. Package-only invariants and actions pass.
31. The inherited C53-impact status is preserved.
32. The C73 import contract is complete.
33. The C73 dry preflight passes using only C64 and C72 public APIs.
34. The C71 blocker is explicitly superseded.
35. Repeated, clean, serial, parallel, and restart reconstruction is deterministic.
36. Every entry and object has complete ancestry.
37. All error and unresolved counts are zero.
38. At least 320 focused live mutations are detected.
39. No physical \(qg\) embedding is constructed.
40. No descendant support, endpoint, witness, contact, instantaneous-fermion, Wilson/TMD, matching, inference, or production object is created.
41. `NO_JOINT_MEASURE`, 216 routes, 642 ART25 identities, and authoritative artifacts remain unchanged.
42. `MSHT20_REP/` remains untouched.
43. The working tree is clean except for the two pre-existing untracked paths.
44. A local completion commit is created and not pushed.

Do not weaken source/API authentication, basis authority, exact-record identity, per-entry bound identity, package-root coverage, immutability, or no-regeneration/no-inference semantics to open the gate.

---

# 28. Final Codex response

Report:

- starting and final commits;
- untouched untracked paths;
- descendant correction applied to the C70 readiness claim;
- scientific-source and public-API fingerprints;
- 24-row, 3-column, and 72-pair basis hashes;
- exact status counts by class;
- exact-expression and zero-certificate counts and hashes;
- 72 per-entry bound identities and aggregate hash;
- companion-object identities and hashes;
- runtime root;
- authenticated index and package-root hashes;
- payload/control-file counts;
- inventory error counts;
- immutable package-object and loader validation;
- no-regeneration/no-inference guard results;
- complete C66/C68/C70 equivalence;
- package-only isometry/projector/intertwiner/leakage results;
- preserved C53-impact status;
- C71 blocker-supersession result;
- C73 import-preflight result;
- deterministic reconstruction;
- ancestry and count-once results;
- isolation and mutation results;
- exact readiness or no-go status;
- exact next branch;
- confirmation that no physical embedding, descendant support promotion, endpoint/witness relation, contact value/matrix, complete instantaneous-fermion operator, TMD/matching, inference, or production object was created;
- confirmation that nothing was pushed.

Do not describe six authenticated arrays plus simplified statuses, an unauthenticated basis order, an entry without exact-record identity, an array-level bound without per-entry identity, a mutable loader return, or a package that omits source/API fingerprints as a complete immutable triplet authority package.
